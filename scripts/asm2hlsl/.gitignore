# The extracted shader corpus is a working artifact: 1113 files pulled out of the .tre/.toc
# stack by shadercorpus.py, plus the provenance table it writes alongside. It is reproducible
# by running that script and does not belong in this repository -- the archives are the
# source of truth.
corpus/
corpus-manifest.tsv
__pycache__/
