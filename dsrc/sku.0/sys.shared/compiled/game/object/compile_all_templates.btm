@echo off
@SET TPF_COMPILER=TemplateCompiler

set startDir=%_cwd

for /a:d /h /r %i in (*) do (
	cd "%i"
	for %j in (*.tpf) DO (
		@echo Compiling %j
		%TPF_COMPILER -compile %j
	)
)

for %j in (*.tpf) DO (
	@echo Compiling %j
	%TPF_COMPILER -compile %j
)

:end
cd "%startDir"
