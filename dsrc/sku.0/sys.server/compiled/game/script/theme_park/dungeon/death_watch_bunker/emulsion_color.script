include library.hue;

trigger OnAttach()
{
	string item = getTemplateName (self);
	if (item == "object/tangible/loot/dungeon/death_watch_bunker/emulsion_protection.iff")
	{
		hue.setColor (self, hue.INDEX_1, 9);
	}

	else
	{
		hue.setColor (self, hue.INDEX_1, 29);
	}

	return SCRIPT_CONTINUE;
}