// ======================================================================
//
// ep3_clone_relics_nym_starmap_nym.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.skill;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/ep3_clone_relics_nym_starmap_nym";

// ======================================================================
// Script Conditions
// ======================================================================

boolean ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_onQuest1 (obj_id player, obj_id npc)
{
	return	(groundquests.isQuestActive(player, "ep3_clone_relics_nym_starmap_1"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_onTask1 (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ep3_clone_relics_nym_starmap_1", "talkToNym1"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_foundFirstMap (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ep3_clone_relics_nym_starmap_1", "foundFirstMap"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_completedQuest1 (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_nym_starmap_1"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_onQuest2 (obj_id player, obj_id npc)
{
	return	(groundquests.isQuestActive(player, "ep3_clone_relics_nym_starmap_2"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_foundSecondMap (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ep3_clone_relics_nym_starmap_2", "foundSecondMap"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_completedQuest2 (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_nym_starmap_2"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_onQuest3 (obj_id player, obj_id npc)
{
	return	(groundquests.isQuestActive(player, "ep3_clone_relics_nym_starmap_3"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_foundThirdMap (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ep3_clone_relics_nym_starmap_3", "foundThirdMap"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_completedQuest3 (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_nym_starmap_3"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_onQuest4 (obj_id player, obj_id npc)
{
	return	(groundquests.isQuestActive(player, "ep3_clone_relics_nym_starmap_4"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_talkedToSolo1 (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ep3_clone_relics_nym_starmap_4", "talkedToSolo1"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_foundFourthMap (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ep3_clone_relics_nym_starmap_4", "foundFourthMap"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_isGm (obj_id player, obj_id npc)
{
	return (hasObjVar(player, "gm" ));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_completedQuest4 (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_nym_starmap_4"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_nym_starmap_nym_condition_notHighEnough (obj_id player, obj_id npc)
{
	int phase = skill.getProfessionPhase(player);
	
	return (phase < skill.PHASE_TWO);
}

// ======================================================================
// Script Actions
// ======================================================================

void ep3_clone_relics_nym_starmap_nym_action_sendSignalTalked1 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "findMap1");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_removeQuest1 (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_nym_starmap_1");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_sendSignalWantedCash (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "gotCash");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_sendSignalGaveFirstMap (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "foundMap1");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_grantQuest2 (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_nym_starmap_2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_sendSignalGaveSecondMap (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "foundMap2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_sendSignalGaveThirdMap (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "foundMap3");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_grantQuest4 (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_nym_starmap_4");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_grantQuest3 (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_nym_starmap_3");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_sendSignalGiveMoneyToSolo (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "giveMoneyToSolo");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_sendSignalGaveFourthMap (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "foundMap4");
	setObjVar (player, "doneWithNym", 1);
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_removeQuest2 (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_nym_starmap_2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_removeQuest3 (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_nym_starmap_3");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_removeQuest4 (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_nym_starmap_4");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_nym_starmap_nym_action_removeQuestAll (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_nym_starmap_1");
	groundquests.clearQuest(player, "ep3_clone_relics_nym_starmap_2");
	groundquests.clearQuest(player, "ep3_clone_relics_nym_starmap_3");
	groundquests.clearQuest(player, "ep3_clone_relics_nym_starmap_4");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int ep3_clone_relics_nym_starmap_nym_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Here's our treasure hunter now Kole! Did Solo come true on his end of the deal?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, he seemed very pleased.
	if (response == "s_382")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm sure he did, that... well never mind. Let me have the map.
			string_id message = new string_id (c_stringFile, "s_384");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: < hand Nym the torn map >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_386");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_677")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm sure he did, that... well never mind. Let me have the map.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: < hand Nym the torn map >
	if (response == "s_386")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Excellent! You have definitely earned this last reward. For those times when you want to go on the offensive, nothing beats the Beamrail Gauss! Guaranteed to blow a hole through the sturdiest of enemies!
			string_id message = new string_id (c_stringFile, "s_388");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you. There was talk of credits as well though?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_390");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent! You have definitely earned this last reward. For those times when you want to go on the offensive, nothing beats the Beamrail Gauss! Guaranteed to blow a hole through the sturdiest of enemies!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you. There was talk of credits as well though?
	if (response == "s_390")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes, yes. Let me first make sure everything is in order here. < Nym pieces the map together and starts to study it. > Very interesting... hmm, wait a minute. That can't be right! I've been there, that planet is dead now! This is worthless and that smug mynock bowel Solo knew it! You, %NU you should have suspected this and not given him the money!
			string_id message = new string_id (c_stringFile, "s_392");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But, how could I possibly...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_394");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 5);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, yes. Let me first make sure everything is in order here. < Nym pieces the map together and starts to study it. > Very interesting... hmm, wait a minute. That can't be right! I've been there, that planet is dead now! This is worthless and that smug mynock bowel Solo knew it! You, %NU you should have suspected this and not given him the money!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: But, how could I possibly...
	if (response == "s_394")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_sendSignalGaveFourthMap (player, npc);

			//-- NPC: Silence! There will be no money for you! You're lucky I've given you all these upgrades for your ship! Now get out of here! Kole and I have a trip to Tatooine to plan!
			string_id message = new string_id (c_stringFile, "s_396");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome back %NU. Did you find Solo?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure did. He has the map but wants 50,000 credits for it.
	if (response == "s_400")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: That filthy kreetle! I should have guessed he wanted money for it. Did you even try to haggle?
			string_id message = new string_id (c_stringFile, "s_402");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course but it wasn't easy with his friend in the room...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_404");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_636")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That filthy kreetle! I should have guessed he wanted money for it. Did you even try to haggle?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course but it wasn't easy with his friend in the room...
	if (response == "s_404")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: That blasted Wookiee! You didn't mention that I was involved did you?
			string_id message = new string_id (c_stringFile, "s_406");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No but he seemed to suspect that I was working for someone else.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_408");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That blasted Wookiee! You didn't mention that I was involved did you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No but he seemed to suspect that I was working for someone else.
	if (response == "s_408")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_sendSignalGiveMoneyToSolo (player, npc);

			//-- NPC: Yeah Solo's not stupid. Very well, we will pay the scoundrel. Make sure that it's the actual map before you give him the money! Don't even think about taking off with these credits, the galaxy isn't big enough for you to hide if you do.
			string_id message = new string_id (c_stringFile, "s_410");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're supposed to be on Tatooine.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will have to postpone that for a bit, something came up.
	if (response == "s_414")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hurry up. Just the thought of Solo sitting there rubbing his hands with my map in his pocket makes me angry.
			string_id message = new string_id (c_stringFile, "s_416");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Was it Mos Entha or Mos Eisley Solo was in?
	if (response == "s_418")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Mos Eisley. Remember, he's a tricky one, be careful with what you say to him!
			string_id message = new string_id (c_stringFile, "s_420");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_589")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good, you're back. Let's get right in to the last clue shall we?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm going to need some more time, I'm pretty beat.
	if (response == "s_424")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fine, don't take too long though. Only one more piece %NU, we're so close.
			string_id message = new string_id (c_stringFile, "s_426");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let's.
	if (response == "s_428")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Alright, this clue reads 'Apparently Jabba the Hutt had come across a piece of the map. He was to have it shipped to his palace by the smuggler Han Solo but it somehow disappeared en route to Tatooine.' Solo? That no good womp rat of a scoundrel. He probably has it himself!
			string_id message = new string_id (c_stringFile, "s_430");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You know that smuggler?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_432");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_541")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alright, this clue reads 'Apparently Jabba the Hutt had come across a piece of the map. He was to have it shipped to his palace by the smuggler Han Solo but it somehow disappeared en route to Tatooine.' Solo? That no good womp rat of a scoundrel. He probably has it himself!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You know that smuggler?
	if (response == "s_432")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yeah I've had the displeasure of running in to him a few times. Last I know, he was in Mos Eisley on Tatooine. Figure out what it would take to have him give up the map. What ever you do, don't mention my name though.
			string_id message = new string_id (c_stringFile, "s_434");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Anything else I should know?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_436");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yeah I've had the displeasure of running in to him a few times. Last I know, he was in Mos Eisley on Tatooine. Figure out what it would take to have him give up the map. What ever you do, don't mention my name though.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Anything else I should know?
	if (response == "s_436")
	{
		doAnimationAction (player, "nod");

		ep3_clone_relics_nym_starmap_nym_action_grantQuest4 (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yeah, don't anger his co-pilot... it can prove quite painful.
			string_id message = new string_id (c_stringFile, "s_438");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What happened to you?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm never going back to Dathomir, mark my words...
	if (response == "s_442")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: I told it was tough. < Nym chuckles > Anyway, are we happy?
			string_id message = new string_id (c_stringFile, "s_444");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: At least you are. < hand Nym the torn map >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_446");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_493")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I told it was tough. < Nym chuckles > Anyway, are we happy?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: At least you are. < hand Nym the torn map >
	if (response == "s_446")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: I sure am! < Nym puts all the pieces together and starts to study them > Excellent, we are very close %NU. Now to put a smile on your face. I have something for those times when all the extra protection I've provided you with just isn't enough. Nothing will be able to catch you once you fit your ship with this Quantum Ion Drive. It's guaranteed to leave any irritating customs agent in the dust.
			string_id message = new string_id (c_stringFile, "s_448");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sure could come in handy.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_450");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 21);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I sure am! < Nym puts all the pieces together and starts to study them > Excellent, we are very close %NU. Now to put a smile on your face. I have something for those times when all the extra protection I've provided you with just isn't enough. Nothing will be able to catch you once you fit your ship with this Quantum Ion Drive. It's guaranteed to leave any irritating customs agent in the dust.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sure could come in handy.
	if (response == "s_450")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		ep3_clone_relics_nym_starmap_nym_action_sendSignalGaveThirdMap (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: It sure could, couldn't it? Now go get some rest. By the look of you, I'll expect I will see you back in a week. < Nym laughs >
			string_id message = new string_id (c_stringFile, "s_452");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What is it %NU?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have to take care of something first. I'll be back soon though.
	if (response == "s_456")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: I want this map completed so don't be surprised if I hired someone else when you come back...
			string_id message = new string_id (c_stringFile, "s_458");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe I could get an advance? Some weapon?
	if (response == "s_460")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sorry, I don't do business that way.
			string_id message = new string_id (c_stringFile, "s_462");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I am going to Dathomir you know...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_464");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_445")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sorry, I don't do business that way.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am going to Dathomir you know...
	if (response == "s_464")
	{
		doAnimationAction (player, "tap_foot");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: You'll be fine %NU, you're tough, right? < Nym grins >
			string_id message = new string_id (c_stringFile, "s_466");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good, you are back. I hope you got some good rest because looking at the clue for this map piece, you will need it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready.
	if (response == "s_470")
	{
		doAnimationAction (player, "pose_proudly");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope so. Ever been to Dathomir? Nasty place. Anyway, this is the next clue. 'An Imperial officer had one of the pieces and he never let it leave his pocket. He was stationed on the planet of Dathomir and apparently was killed there during a patrol. The only survivor, almost unable to communicate after the attack, rambled on and on about witches of the night and giant spiders.'
			string_id message = new string_id (c_stringFile, "s_472");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's that supposed to mean? Sounds like a fairy tale.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_474");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Great. Well I'm going to need to prepare some more then.
	if (response == "s_482")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: I suppose. Don't take too long though, I want this map finished.
			string_id message = new string_id (c_stringFile, "s_484");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_397")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I hope so. Ever been to Dathomir? Nasty place. Anyway, this is the next clue. 'An Imperial officer had one of the pieces and he never let it leave his pocket. He was stationed on the planet of Dathomir and apparently was killed there during a patrol. The only survivor, almost unable to communicate after the attack, rambled on and on about witches of the night and giant spiders.'

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's that supposed to mean? Sounds like a fairy tale.
	if (response == "s_474")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: If I knew that, I wouldn't...
			string_id message = new string_id (c_stringFile, "s_476");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I know, I know. You wouldn't need me. Dathomir it is.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_478");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If I knew that, I wouldn't...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know, I know. You wouldn't need me. Dathomir it is.
	if (response == "s_478")
	{
		doAnimationAction (player, "nod");

		ep3_clone_relics_nym_starmap_nym_action_grantQuest3 (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Exactly. Good luck!
			string_id message = new string_id (c_stringFile, "s_480");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've had a bad day, I hope you have some good news.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: If you think that finding the next map piece is good news...
	if (response == "s_488")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Great news! I'm starting to like you... %NU was it? Let me have a look at it!
			string_id message = new string_id (c_stringFile, "s_490");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: < hand Nym the fragments >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_492");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 33);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_364")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Great news! I'm starting to like you... %NU was it? Let me have a look at it!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: < hand Nym the fragments >
	if (response == "s_492")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: What did you do to it?! Feed it to a mynock?!
			string_id message = new string_id (c_stringFile, "s_494");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The dumb slavers tore it apart and split it between them.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_496");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What did you do to it?! Feed it to a mynock?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The dumb slavers tore it apart and split it between them.
	if (response == "s_496")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Those spawns of a hairless gurk! I will remember that! Let me try and piece this thing together. < Nym goes to work on fitting all the pieces together >
			string_id message = new string_id (c_stringFile, "s_498");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Now about the...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_500");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Those spawns of a hairless gurk! I will remember that! Let me try and piece this thing together. < Nym goes to work on fitting all the pieces together >

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Now about the...
	if (response == "s_500")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hold on! < Nym starts to closely study all the pieces > Yes indeed, we are starting to get closer here. I know, I know, your payment. This time I've dug up a real treat for you.
			string_id message = new string_id (c_stringFile, "s_502");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I hope so, this wasn't easy you know.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_504");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hold on! < Nym starts to closely study all the pieces > Yes indeed, we are starting to get closer here. I know, I know, your payment. This time I've dug up a real treat for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I hope so, this wasn't easy you know.
	if (response == "s_504")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_sendSignalGaveSecondMap (player, npc);

			//-- NPC: Oh it's a beauty. To further enhance the survivability of your fighter... and you, you should outfit it with these top of the line ProtectoNet shields. Now go and get some rest... and maybe change those clothes, you seem to have some blood on them.
			string_id message = new string_id (c_stringFile, "s_506");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have to postpone, something came up.
	if (response == "s_510")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: < Nym sighs > Fine. Don't be surprised if I gave the job to someone else when you come back though.
			string_id message = new string_id (c_stringFile, "s_512");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You said Kashyyyk right?
	if (response == "s_514")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're not writing this down?
			string_id message = new string_id (c_stringFile, "s_516");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course, just making sure I got it right.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_518");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_340")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch40 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're not writing this down?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course, just making sure I got it right.
	if (response == "s_518")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes Kashyyyk. Now hurry up.
			string_id message = new string_id (c_stringFile, "s_520");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Let's get down to business then shall we?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course.
	if (response == "s_524")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Alright, the second clue that your predecessor found was the following. 'A piece of the map was to be transported to an art collector named Gally Arc on the planet of Brentaal. When it was passing through the Kashyyyk system, it was marauded by Trandoshan slavers and hasn't been seen since.'
			string_id message = new string_id (c_stringFile, "s_526");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That's it?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_528");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need a little more time.
	if (response == "s_544")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then what are you doing back here?
			string_id message = new string_id (c_stringFile, "s_546");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I just thought we could hang out a little, play some...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_548");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_316")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alright, the second clue that your predecessor found was the following. 'A piece of the map was to be transported to an art collector named Gally Arc on the planet of Brentaal. When it was passing through the Kashyyyk system, it was marauded by Trandoshan slavers and hasn't been seen since.'

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's it?
	if (response == "s_528")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: That's it.
			string_id message = new string_id (c_stringFile, "s_530");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What should I do, kill every Trandoshan on Kashyyyk?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_532");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch44 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What should I do, kill every Trandoshan on Kashyyyk?
	if (response == "s_532")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well they are scum anyway. < Nym chuckles > I'm sure you'll find a way. After all, that's what I hired you for, right?
			string_id message = new string_id (c_stringFile, "s_534");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I need to think about this...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Right. I'll head for Kashyyyk.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_536");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_540");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well they are scum anyway. < Nym chuckles > I'm sure you'll find a way. After all, that's what I hired you for, right?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to think about this...
	if (response == "s_536")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well don't think too long. I want those maps and if you can't do it, I'll find someone who can.
			string_id message = new string_id (c_stringFile, "s_538");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Right. I'll head for Kashyyyk.
	if (response == "s_540")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_grantQuest2 (player, npc);

			//-- NPC: Good. I'll be expecting you back here soon, with the map piece.
			string_id message = new string_id (c_stringFile, "s_542");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch48 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then what are you doing back here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I just thought we could hang out a little, play some...
	if (response == "s_548")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: What do you think this is? A vacation? Kole and I have some serious business to attend to here and you're testing my patience. Now get out of here and come back when you're ready to get some work done.
			string_id message = new string_id (c_stringFile, "s_550");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch50 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hey! I told you he'd be back Kole. Any luck?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It wasn't easy but yes, I have another map piece.
	if (response == "s_554")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Splendid! Don't just stand there, let me see it!
			string_id message = new string_id (c_stringFile, "s_556");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: < hand Nym the torn starmap >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There was the matter of payment...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_558");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_566");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 51);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_292")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: My... corporation has substantial resources in star fighter upgrades. You bring me the map pieces and I'll provide you with the best hardware out there today. On top of that, I'll throw in... 30,000 credits.
			string_id message = new string_id (c_stringFile, "s_613");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good. Any idea where I should start?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 30,000? After looking at that crash site, 50,000 sounds better.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_615");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_635");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch51 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Splendid! Don't just stand there, let me see it!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: < hand Nym the torn starmap >
	if (response == "s_558")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: < Nym closely studies the piece of the map, fitting it next to the other piece > Hmm, yes, very interesting... but completely useless. I have to get the other pieces!
			string_id message = new string_id (c_stringFile, "s_560");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will get it for you but first...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_562");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 52);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: There was the matter of payment...
	if (response == "s_566")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes, yes! After, now let me see it!
			string_id message = new string_id (c_stringFile, "s_568");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Fine. < hand Nym the torn starmap >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_570");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: < Nym closely studies the piece of the map, fitting it next to the other piece > Hmm, yes, very interesting... but completely useless. I have to get the other pieces!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will get it for you but first...
	if (response == "s_562")
	{
		ep3_clone_relics_nym_starmap_nym_action_sendSignalGaveFirstMap (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes of course. I had this excellent fighter armor brought in. FiberTech Steel Weave will make your ship all but impenetrable! Now go get some rest and come back when you are ready to hunt down the second piece.
			string_id message = new string_id (c_stringFile, "s_564");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, yes! After, now let me see it!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine. < hand Nym the torn starmap >
	if (response == "s_570")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: < Nym closely studies the piece of the map, fitting it next to the other piece > Hmm, yes, very interesting... but completely useless. I have to get the other pieces!
			string_id message = new string_id (c_stringFile, "s_560");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will get it for you but first...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_562");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 52);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch56 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We asked to not be disturbed. Well you are disturbing us.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sorry.
	if (response == "s_595")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: You will be, if you don't go on your way.
			string_id message = new string_id (c_stringFile, "s_597");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I believe you will be interested in what I have though.
	if (response == "s_599")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope so fringer.
			string_id message = new string_id (c_stringFile, "s_601");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Take a look at this. < hand Nym the documents >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_603");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_230")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I hope so fringer.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Take a look at this. < hand Nym the documents >
	if (response == "s_603")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Where did you get this? For your sake, I hope the answer pleases me.
			string_id message = new string_id (c_stringFile, "s_605");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I came upon a crashed star ship not far from here, it was there.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's not important. Money on the other hand, is.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_607");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_665");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch59 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Where did you get this? For your sake, I hope the answer pleases me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I came upon a crashed star ship not far from here, it was there.
	if (response == "s_607")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: That would explain a thing or two, wouldn't you say Kole? Yes I believe so. Alright stranger, here's the thing. This is a piece of a map that was torn to pieces and scattered through out the galaxy. I hired a treasure hunter to find the pieces but it would appear that he's had an accident. How would you feel about helping me find the remaining parts of the map?
			string_id message = new string_id (c_stringFile, "s_609");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That all depends, what's in it for me?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm not exactly a treasure hunter...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I don't think so. I would like a reward for finding this piece though.
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_611");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_645");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_655");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's not important. Money on the other hand, is.
	if (response == "s_665")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: It is, and your demise is quickly growing in value stranger.
			string_id message = new string_id (c_stringFile, "s_667");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Calm down! I found it at a crash site not far from here.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_669");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch60 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That would explain a thing or two, wouldn't you say Kole? Yes I believe so. Alright stranger, here's the thing. This is a piece of a map that was torn to pieces and scattered through out the galaxy. I hired a treasure hunter to find the pieces but it would appear that he's had an accident. How would you feel about helping me find the remaining parts of the map?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That all depends, what's in it for me?
	if (response == "s_611")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: My... corporation has substantial resources in star fighter upgrades. You bring me the map pieces and I'll provide you with the best hardware out there today. On top of that, I'll throw in... 30,000 credits.
			string_id message = new string_id (c_stringFile, "s_613");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good. Any idea where I should start?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 30,000? After looking at that crash site, 50,000 sounds better.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_615");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_635");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not exactly a treasure hunter...
	if (response == "s_645")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Oh it would be a piece of cake, just take some traveling.
			string_id message = new string_id (c_stringFile, "s_647");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I suppose... Alright, what will I get out of this deal?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: After seeing that crash site, I'd say it's dangerous, so no thanks.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_649");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_651");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't think so. I would like a reward for finding this piece though.
	if (response == "s_655")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you sure? It could prove quite profitable.
			string_id message = new string_id (c_stringFile, "s_657");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No thank you. Now about that money.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I could use the creds... how much are we talking here?
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_659");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_663");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: My... corporation has substantial resources in star fighter upgrades. You bring me the map pieces and I'll provide you with the best hardware out there today. On top of that, I'll throw in... 30,000 credits.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good. Any idea where I should start?
	if (response == "s_615")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Your predecessor had tracked down some clues for the other pieces. The first one is, '...apparently a child was taken captive by the natives on the planet of Tatooine. After his rescue, he recalled spotting one of the strangely ornated boxes near a pool in the cave that was his prison'.
			string_id message = new string_id (c_stringFile, "s_617");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Interesting, I might be able to track that down.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where's that supposed to be?
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_619");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_623");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 30,000? After looking at that crash site, 50,000 sounds better.
	if (response == "s_635")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are starting to get on my nerves here... Kole, what do you think? Is he worth that kind of investment? No? I'll stretch it to 40,000, my last offer.
			string_id message = new string_id (c_stringFile, "s_637");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 40.000... alright, you've got a deal.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sorry, I don't do charity work.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_639");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_641");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your predecessor had tracked down some clues for the other pieces. The first one is, '...apparently a child was taken captive by the natives on the planet of Tatooine. After his rescue, he recalled spotting one of the strangely ornated boxes near a pool in the cave that was his prison'.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Interesting, I might be able to track that down.
	if (response == "s_619")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_sendSignalTalked1 (player, npc);

			//-- NPC: Excellent. Kole and I have some unfinished business in Narmle so come back here to see me when you've found it.
			string_id message = new string_id (c_stringFile, "s_621");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where's that supposed to be?
	if (response == "s_623")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: That's why I hired you. Are you telling me you can't do the job?
			string_id message = new string_id (c_stringFile, "s_625");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No no, I will find it.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yeah this doesn't sound like my kind of job.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_627");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_631");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 64);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's why I hired you. Are you telling me you can't do the job?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No no, I will find it.
	if (response == "s_627")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_sendSignalTalked1 (player, npc);

			//-- NPC: Good. Kole and I have some unfinished business in Narmle so come back here to see me when you've found it.
			string_id message = new string_id (c_stringFile, "s_629");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah this doesn't sound like my kind of job.
	if (response == "s_631")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuest1 (player, npc);

			//-- NPC: Suit yourself. We have some business to go over so you can leave now.
			string_id message = new string_id (c_stringFile, "s_633");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch67 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are starting to get on my nerves here... Kole, what do you think? Is he worth that kind of investment? No? I'll stretch it to 40,000, my last offer.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 40.000... alright, you've got a deal.
	if (response == "s_639")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Your predecessor had tracked down some clues for the other pieces. The first one is, '...apparently a child was taken captive by the natives on the planet of Tatooine. After his rescue, he recalled spotting one of the strangely ornated boxes near a pool in the cave that was his prison'.
			string_id message = new string_id (c_stringFile, "s_617");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Interesting, I might be able to track that down.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where's that supposed to be?
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_619");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_623");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sorry, I don't do charity work.
	if (response == "s_641")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuest1 (player, npc);

			//-- NPC: Suit yourself. Now if you'll excuse us, we have business to attend to.
			string_id message = new string_id (c_stringFile, "s_643");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch69 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh it would be a piece of cake, just take some traveling.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I suppose... Alright, what will I get out of this deal?
	if (response == "s_649")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: My... corporation has substantial resources in star fighter upgrades. You bring me the map pieces and I'll provide you with the best hardware out there today. On top of that, I'll throw in... 30,000 credits.
			string_id message = new string_id (c_stringFile, "s_613");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good. Any idea where I should start?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 30,000? After looking at that crash site, 50,000 sounds better.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_615");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_635");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: After seeing that crash site, I'd say it's dangerous, so no thanks.
	if (response == "s_651")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuest1 (player, npc);

			//-- NPC: Suit yourself. We have work to do so if you excuse us.
			string_id message = new string_id (c_stringFile, "s_653");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch71 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you sure? It could prove quite profitable.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thank you. Now about that money.
	if (response == "s_659")
	{
		doAnimationAction (player, "shake_head_no");

		ep3_clone_relics_nym_starmap_nym_action_sendSignalTalked1 (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_sendSignalWantedCash (player, npc);

			//-- NPC: Suit yourself. Here's some cash for your troubles. Now leave us.
			string_id message = new string_id (c_stringFile, "s_661");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I could use the creds... how much are we talking here?
	if (response == "s_663")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: My... corporation has substantial resources in star fighter upgrades. You bring me the map pieces and I'll provide you with the best hardware out there today. On top of that, I'll throw in... 30,000 credits.
			string_id message = new string_id (c_stringFile, "s_613");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good. Any idea where I should start?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 30,000? After looking at that crash site, 50,000 sounds better.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_615");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_635");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch73 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It is, and your demise is quickly growing in value stranger.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Calm down! I found it at a crash site not far from here.
	if (response == "s_669")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: That would explain a thing or two, wouldn't you say Kole? Yes I believe so. Alright stranger, here's the thing. This is a piece of a map that was torn to pieces and scattered through out the galaxy. I hired a treasure hunter to find the pieces but it would appear that he's had an accident. How would you feel about helping me find the remaining parts of the map?
			string_id message = new string_id (c_stringFile, "s_609");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That all depends, what's in it for me?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm not exactly a treasure hunter...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I don't think so. I would like a reward for finding this piece though.
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_611");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_645");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_655");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch74 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Which quest?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 1
	if (response == "s_253")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuest1 (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_258");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 2
	if (response == "s_254")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuest2 (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_260");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 3
	if (response == "s_255")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuest3 (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_262");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 4
	if (response == "s_256")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuest4 (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_264");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: All
	if (response == "s_257")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuestAll (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_266");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch80 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Was there something else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I can't do this right now, something came up.
	if (response == "s_574")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're not planning to get the map for yourself now that you have the first clue are you? I hope you're not stupid enough to try and trick me, that would be a bad mistake. You sure you don't want the job?
			string_id message = new string_id (c_stringFile, "s_576");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm, alright, I'll keep the job. I never intended to...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes sorry, I can't do it right now.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_578");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_582");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you sure that was the only clue in the documents?
	if (response == "s_586")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes, that was it.
			string_id message = new string_id (c_stringFile, "s_588");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Maybe I could take another look at them?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_590");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_268")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_252");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: All
			boolean hasResponse4 = false;
			if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_253");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_256");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch81 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're not planning to get the map for yourself now that you have the first clue are you? I hope you're not stupid enough to try and trick me, that would be a bad mistake. You sure you don't want the job?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hmm, alright, I'll keep the job. I never intended to...
	if (response == "s_578")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: Save it.
			string_id message = new string_id (c_stringFile, "s_580");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes sorry, I can't do it right now.
	if (response == "s_582")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_nym_starmap_nym_action_removeQuest1 (player, npc);

			//-- NPC: Fine. Maybe I'll see you around fringer.
			string_id message = new string_id (c_stringFile, "s_584");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_nym_starmap_nym_handleBranch84 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, that was it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe I could take another look at them?
	if (response == "s_590")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't think so. Now get a move on or I'll find someone else to do it.
			string_id message = new string_id (c_stringFile, "s_592");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.ep3_clone_relics_nym_starmap_nym");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	setName(self, new string_id( "ep3/npc_names", "clone_relics_nym" ));
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	setName(self, new string_id( "ep3/npc_names", "clone_relics_nym" ));
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.ep3_clone_relics_nym_starmap_nym");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_completedQuest4 (player, npc))
	{
		//-- NPC: Leave us %NU, we need to figure out how to get back at that scoundrel Solo!
		string_id message = new string_id (c_stringFile, "s_265");
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_foundFourthMap (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: Here's our treasure hunter now Kole! Did Solo come true on his end of the deal?
		string_id message = new string_id (c_stringFile, "s_380");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, he seemed very pleased.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_382");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_677");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 2);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_talkedToSolo1 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: Welcome back %NU. Did you find Solo?
		string_id message = new string_id (c_stringFile, "s_398");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sure did. He has the map but wants 50,000 credits for it.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_400");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_636");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 7);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_onQuest4 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: You're supposed to be on Tatooine.
		string_id message = new string_id (c_stringFile, "s_412");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I will have to postpone that for a bit, something came up.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Was it Mos Entha or Mos Eisley Solo was in?
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_414");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_418");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_589");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 11);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_completedQuest3 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: Good, you're back. Let's get right in to the last clue shall we?
		string_id message = new string_id (c_stringFile, "s_422");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm going to need some more time, I'm pretty beat.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Let's.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_424");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_428");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_541");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 14);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_foundThirdMap (player, npc))
	{
		doAnimationAction (player, "catchbreath");

		//-- NPC: What happened to you?!
		string_id message = new string_id (c_stringFile, "s_440");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm never going back to Dathomir, mark my words...
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_442");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_493");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 19);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_onQuest3 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: What is it %NU?
		string_id message = new string_id (c_stringFile, "s_454");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I have to take care of something first. I'll be back soon though.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Maybe I could get an advance? Some weapon?
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_456");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_460");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_445");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 23);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_completedQuest2 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: Good, you are back. I hope you got some good rest because looking at the clue for this map piece, you will need it.
		string_id message = new string_id (c_stringFile, "s_468");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm ready.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Great. Well I'm going to need to prepare some more then.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_470");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_482");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_397");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 27);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_foundSecondMap (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: I've had a bad day, I hope you have some good news.
		string_id message = new string_id (c_stringFile, "s_486");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: If you think that finding the next map piece is good news...
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_488");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_364");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 32);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_onQuest2 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: Yes?
		string_id message = new string_id (c_stringFile, "s_508");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I have to postpone, something came up.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: You said Kashyyyk right?
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_510");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_514");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_340");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 38);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_completedQuest1 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: Let's get down to business then shall we?
		string_id message = new string_id (c_stringFile, "s_522");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Of course.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I need a little more time.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_524");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_544");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_316");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 42);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_foundFirstMap (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: Hey! I told you he'd be back Kole. Any luck?
		string_id message = new string_id (c_stringFile, "s_552");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: It wasn't easy but yes, I have another map piece.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_554");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_292");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 50);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_notHighEnough (player, npc))
	{
		doAnimationAction (npc, "dismiss");

		//-- NPC: You're a little too wet behind your ears fringer. Come back when you have some more experience and I might have use for you.
		string_id message = new string_id (c_stringFile, "s_175");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_onTask1 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: We asked to not be disturbed. Well you are disturbing us.
		string_id message = new string_id (c_stringFile, "s_1544");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm sorry.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I believe you will be interested in what I have though.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_595");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_599");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_230");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 56);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition_onQuest1 (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: Was there something else?
		string_id message = new string_id (c_stringFile, "s_572");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes I can't do this right now, something came up.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Are you sure that was the only clue in the documents?
		boolean hasResponse1 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_nym_starmap_nym_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_574");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_586");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_268");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId", 80);

			npcStartConversation (player, npc, "ep3_clone_relics_nym_starmap_nym", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_nym_starmap_nym_condition__defaultCondition (player, npc))
	{
		doAnimationAction (player, "tiphat");

		//-- NPC: I think we asked to not be disturbed, didn't we Kole? Yes I think so.
		string_id message = new string_id (c_stringFile, "s_240");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "ep3_clone_relics_nym_starmap_nym")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

	if (branchId == 2 && ep3_clone_relics_nym_starmap_nym_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && ep3_clone_relics_nym_starmap_nym_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && ep3_clone_relics_nym_starmap_nym_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && ep3_clone_relics_nym_starmap_nym_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && ep3_clone_relics_nym_starmap_nym_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && ep3_clone_relics_nym_starmap_nym_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && ep3_clone_relics_nym_starmap_nym_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && ep3_clone_relics_nym_starmap_nym_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && ep3_clone_relics_nym_starmap_nym_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && ep3_clone_relics_nym_starmap_nym_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && ep3_clone_relics_nym_starmap_nym_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && ep3_clone_relics_nym_starmap_nym_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && ep3_clone_relics_nym_starmap_nym_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && ep3_clone_relics_nym_starmap_nym_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && ep3_clone_relics_nym_starmap_nym_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && ep3_clone_relics_nym_starmap_nym_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && ep3_clone_relics_nym_starmap_nym_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && ep3_clone_relics_nym_starmap_nym_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && ep3_clone_relics_nym_starmap_nym_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && ep3_clone_relics_nym_starmap_nym_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && ep3_clone_relics_nym_starmap_nym_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && ep3_clone_relics_nym_starmap_nym_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && ep3_clone_relics_nym_starmap_nym_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && ep3_clone_relics_nym_starmap_nym_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && ep3_clone_relics_nym_starmap_nym_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 40 && ep3_clone_relics_nym_starmap_nym_handleBranch40 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && ep3_clone_relics_nym_starmap_nym_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && ep3_clone_relics_nym_starmap_nym_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 44 && ep3_clone_relics_nym_starmap_nym_handleBranch44 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && ep3_clone_relics_nym_starmap_nym_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 48 && ep3_clone_relics_nym_starmap_nym_handleBranch48 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 50 && ep3_clone_relics_nym_starmap_nym_handleBranch50 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 51 && ep3_clone_relics_nym_starmap_nym_handleBranch51 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && ep3_clone_relics_nym_starmap_nym_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && ep3_clone_relics_nym_starmap_nym_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 56 && ep3_clone_relics_nym_starmap_nym_handleBranch56 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && ep3_clone_relics_nym_starmap_nym_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 59 && ep3_clone_relics_nym_starmap_nym_handleBranch59 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 60 && ep3_clone_relics_nym_starmap_nym_handleBranch60 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && ep3_clone_relics_nym_starmap_nym_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && ep3_clone_relics_nym_starmap_nym_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && ep3_clone_relics_nym_starmap_nym_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 67 && ep3_clone_relics_nym_starmap_nym_handleBranch67 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 69 && ep3_clone_relics_nym_starmap_nym_handleBranch69 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 71 && ep3_clone_relics_nym_starmap_nym_handleBranch71 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 73 && ep3_clone_relics_nym_starmap_nym_handleBranch73 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 74 && ep3_clone_relics_nym_starmap_nym_handleBranch74 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 80 && ep3_clone_relics_nym_starmap_nym_handleBranch80 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 81 && ep3_clone_relics_nym_starmap_nym_handleBranch81 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 84 && ep3_clone_relics_nym_starmap_nym_handleBranch84 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.ep3_clone_relics_nym_starmap_nym.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

