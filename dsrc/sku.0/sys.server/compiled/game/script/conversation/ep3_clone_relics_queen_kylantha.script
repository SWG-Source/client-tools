// ======================================================================
//
// ep3_clone_relics_queen_kylantha.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.features;
include library.groundquests;
include library.space_flags;
include library.space_quest;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/ep3_clone_relics_queen_kylantha";

// ======================================================================
// Script Conditions
// ======================================================================

boolean ep3_clone_relics_queen_kylantha_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_playerIsRebel (obj_id player, obj_id npc)
{
	return factions.isRebel(player);
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_isWookie (obj_id player, obj_id npc)
{
	return ( getSpecies(player) == SPECIES_WOOKIEE );
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_hasCompletedQuestOne (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_queen_1"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_hasCompletedSpaceMission (obj_id player, obj_id npc)
{
	if (space_quest.hasWonQuest(player, "space_battle", "ep3_clone_relics_queen_3") &&
	!space_quest.hasReceivedReward (player, "space_battle", "ep3_clone_relics_queen_3"))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_failedSpaceMission (obj_id player, obj_id npc)
{
	if (space_quest.hasAbortedQuest (player, "space_battle", "ep3_clone_relics_queen_3") ||
	   space_quest.hasFailedQuest (player, "space_battle", "ep3_clone_relics_queen_3"))
	
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_receivedReward (obj_id player, obj_id npc)
{
	if	(space_quest.hasWonQuest( player, "space_battle", "ep3_clone_relics_queen_3") &&
		space_quest.hasReceivedReward( player, "space_battle", "ep3_clone_relics_queen_3"))

		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_onQuestOne (obj_id player, obj_id npc)
{
	return (groundquests.isQuestActive(player, "ep3_clone_relics_queen_1"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_isGm (obj_id player, obj_id npc)
{
	return (hasObjVar(player, "gm" ));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_hasEp3 (obj_id player, obj_id npc)
{
	return features.hasEpisode3Expansion(player);
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_failedQuestTwo (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_queen_1") &&
	!groundquests.isQuestActiveOrComplete(player, "ep3_clone_relics_queen_2"));
	
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_playerIsOnEmperor01 (obj_id player, obj_id npc)
{
	return factions.isImperial(player) && 
		groundquests.isTaskActive(player, "itp_emperor_02", "itp_emperor_02_02");
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_queen_kylantha_condition_playerIsOnEmperor02 (obj_id player, obj_id npc)
{
	return factions.isImperial(player) && 
		groundquests.isTaskActive(player, "itp_emperor_03", "itp_emperor_03_02");
}

// ======================================================================
// Script Actions
// ======================================================================

void ep3_clone_relics_queen_kylantha_action_giveReturnSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "talkedToOvarra");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_grantQuestOne (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_queen_1");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_removeQuest1 (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_queen_1");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_clearQuestOneAndTwo (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_queen_1");
	groundquests.clearQuest(player, "ep3_clone_relics_queen_2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_grantSpaceMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest(player, "space_battle", "ep3_clone_relics_queen_3");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_grantReward (obj_id player, obj_id npc)
{
	space_quest.giveReward( player, "space_battle", "ep3_clone_relics_queen_3", 10000, "object/tangible/ship/crafted/chassis/arc170_reward_deed.iff");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_removeQuest2 (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_queen_2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_removeQuestAll (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_queen_1");
	groundquests.clearQuest(player, "ep3_clone_relics_queen_2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_grantQuestTwo (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_queen_2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_sendEmperor01Signal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "itp_emperor_02_02");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_queen_kylantha_action_sendEmperor02Signal (obj_id player, obj_id npc)
{
		groundquests.sendSignal(player, "itp_emperor_03_02");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int ep3_clone_relics_queen_kylantha_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Great to see you %NU, it's always a pleasure.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You too, your highness.
	if (response == "s_1135")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: So tell me of your latest adventures, I'm dying to hear what has happened since we last met.
			string_id message = new string_id (c_stringFile, "s_1137");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Oh nothing special, your highness. < tell the Queen a story >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1139");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_1276")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_1277");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: All
			boolean hasResponse2 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1278");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1279");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1280");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So tell me of your latest adventures, I'm dying to hear what has happened since we last met.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Oh nothing special, your highness. < tell the Queen a story >
	if (response == "s_1139")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			//-- NPC: Truly amazing %NU. You continue to surprise me. I wish I could stay and hear another but duty calls I'm afraid. I hope to see you again soon.
			string_id message = new string_id (c_stringFile, "s_1141");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've already heard %TU. I can't even begin to tell you how grateful I am.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My pleasure, your highness. I'm just glad I could save them.
	if (response == "s_1145")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I wish the galaxy had more true heroes like you %NU, then it would surely be a better place. I understand that you have new adventures waiting for you but I wanted your time to offer a small token of my appreciation.
			string_id message = new string_id (c_stringFile, "s_1147");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You are much too kind, your highness.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1149");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 7);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_1274")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_1277");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: All
			boolean hasResponse2 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1278");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1279");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1280");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I wish the galaxy had more true heroes like you %NU, then it would surely be a better place. I understand that you have new adventures waiting for you but I wanted your time to offer a small token of my appreciation.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You are much too kind, your highness.
	if (response == "s_1149")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Nonsense. I'm sure you saw the ship the informants used in escaping Dathomir. They are a bit old but my personal ship technician has rehauled a few of them and turned them in to formidable fighters indeed. I would love for you to have one, for all that you have done.
			string_id message = new string_id (c_stringFile, "s_1151");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would be honored, your highness. I will put it to great use.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1153");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Nonsense. I'm sure you saw the ship the informants used in escaping Dathomir. They are a bit old but my personal ship technician has rehauled a few of them and turned them in to formidable fighters indeed. I would love for you to have one, for all that you have done.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would be honored, your highness. I will put it to great use.
	if (response == "s_1153")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			ep3_clone_relics_queen_kylantha_action_grantReward (player, npc);

			//-- NPC: I know you will %NU. Take care and come back to visit anytime.
			string_id message = new string_id (c_stringFile, "s_1155");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There you are %TU. You have to get back to the Dathomir system immediately!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I failed, your highness. The informants were shot down...
	if (response == "s_1159")
	{
		doAnimationAction (player, "slump_head");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: But they survived. They have repaired their ship and are ready to take off again but still require your assistance in breaking through the blockade.
			string_id message = new string_id (c_stringFile, "s_1161");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They need a more skilled pilot than I. I'm sorry, your highness.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm on my way. I won't fail this time, your highness!
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1163");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1167");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_1272")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_1277");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: All
			boolean hasResponse2 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1278");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1279");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1280");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: But they survived. They have repaired their ship and are ready to take off again but still require your assistance in breaking through the blockade.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They need a more skilled pilot than I. I'm sorry, your highness.
	if (response == "s_1163")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			//-- NPC: I think you are wrong %TU but I understand and will see if I can find someone else. Take well care of yourself and come back to see me anytime.
			string_id message = new string_id (c_stringFile, "s_1165");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way. I won't fail this time, your highness!
	if (response == "s_1167")
	{
		doAnimationAction (player, "pound_fist_palm");

		ep3_clone_relics_queen_kylantha_action_grantSpaceMission (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			//-- NPC: I know you wont %NU! Good luck and be careful.
			string_id message = new string_id (c_stringFile, "s_1169");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How are things going %TU?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Good, your highness. I've already found one of the informants.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			ep3_clone_relics_queen_kylantha_action_grantQuestTwo (player, npc);

			//-- NPC: That is excellent news. Keep me updated %TU.
			string_id message = new string_id (c_stringFile, "s_84");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How are things progressing %TU?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Good, your highness. I've already found one of the informants.
	if (response == "s_1173")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			//-- NPC: That is excellent news. Keep me updated %TU.
			string_id message = new string_id (c_stringFile, "s_1175");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm afraid something really important came up, your highness...
	if (response == "s_1177")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			ep3_clone_relics_queen_kylantha_action_clearQuestOneAndTwo (player, npc);

			//-- NPC: You don't have to explain %TU. It was a long shot and I understand completely. I will find another way. Take well care of yourself and come back to see me anytime.
			string_id message = new string_id (c_stringFile, "s_1179");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_1270")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_1277");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: All
			boolean hasResponse2 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1278");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1279");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1280");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Did you need anything %TU?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just checking if there's been any news before I take off, your highness?
	if (response == "s_1183")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: I'm afraid not %TU, I still haven't heard from my informants.
			string_id message = new string_id (c_stringFile, "s_1185");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Very well, I shall be off then, your highness.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1187");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 20);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm afraid something really important came up, your highness...
	if (response == "s_1191")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			ep3_clone_relics_queen_kylantha_action_removeQuest1 (player, npc);

			//-- NPC: You don't have to explain %TU. It was a long shot and I understand completely. I will find another way. Take well care of yourself and come back to see me anytime.
			string_id message = new string_id (c_stringFile, "s_1193");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_1268")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_1277");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: All
			boolean hasResponse2 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1278");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1279");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1280");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm afraid not %TU, I still haven't heard from my informants.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very well, I shall be off then, your highness.
	if (response == "s_1187")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			//-- NPC: I hope to hear some good news from you soon %TU. Again, be careful out there.
			string_id message = new string_id (c_stringFile, "s_1189");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I was hoping you would stop by %TU.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You know who I am, your highness?
	if (response == "s_1197")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Stories about your adventures have been whispered among some of the staff on several occasions. When I heard you were here, I was hoping I'd get the chance to talk to you.
			string_id message = new string_id (c_stringFile, "s_1199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You give me too much credit, your highness.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition_hasEp3 (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What could they possibly be talking about me for?
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition_hasEp3 (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1201");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1255");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_1266")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_1277");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: All
			boolean hasResponse2 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1278");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1279");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1280");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Stories about your adventures have been whispered among some of the staff on several occasions. When I heard you were here, I was hoping I'd get the chance to talk to you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You give me too much credit, your highness.
	if (response == "s_1201")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You're being too modest %TU. Could I have a few minutes of your time? I need help with something that concerns me gravely. Most likely you're busy and I understand but maybe you would like to hear me out anyway?
			string_id message = new string_id (c_stringFile, "s_1203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm sort of in the middle of jobs anyway so sure.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Of course, your highness.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1205");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1253");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 25);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What could they possibly be talking about me for?
	if (response == "s_1255")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Maybe some of the stories have been exaggerated a bit but rumors are started for a reason %TU.
			string_id message = new string_id (c_stringFile, "s_1257");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But my adventures are as exciting as those of a moisture farmer.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1259");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 38);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're being too modest %TU. Could I have a few minutes of your time? I need help with something that concerns me gravely. Most likely you're busy and I understand but maybe you would like to hear me out anyway?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sort of in the middle of jobs anyway so sure.
	if (response == "s_1205")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition_isWookie (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Excellent. This actually concerns your home planet of Kashyyyk %TU. I employed some of your natives to investigate the disturbing news about slavery on the planet. I'm afraid that after their last assignment, I have not heard a word from them and I'm quite worried.
			string_id message = new string_id (c_stringFile, "s_1207");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What was their last assignment, your highness?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1209");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 26);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Excellent. Have you heard of the planet Kashyyyk %TU? It's the home planet of the Wookiee species. I employed some of the Wookiees to investigate the disturbing news of slavery on the planet. After their last assignment, I have yet to hear from them and I'm very worried.
			string_id message = new string_id (c_stringFile, "s_1225");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What was their last assignment, your highness?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: From what I've heard, Kashyyyk is quite the dangerous place.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1227");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1243");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 31);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course, your highness.
	if (response == "s_1253")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition_isWookie (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Excellent. This actually concerns your home planet of Kashyyyk %TU. I employed some of your natives to investigate the disturbing news about slavery on the planet. I'm afraid that after their last assignment, I have not heard a word from them and I'm quite worried.
			string_id message = new string_id (c_stringFile, "s_1207");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What was their last assignment, your highness?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1209");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 26);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Excellent. Have you heard of the planet Kashyyyk %TU? It's the home planet of the Wookiee species. I employed some of the Wookiees to investigate the disturbing news of slavery on the planet. After their last assignment, I have yet to hear from them and I'm very worried.
			string_id message = new string_id (c_stringFile, "s_1225");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What was their last assignment, your highness?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: From what I've heard, Kashyyyk is quite the dangerous place.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1227");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1243");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 31);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent. This actually concerns your home planet of Kashyyyk %TU. I employed some of your natives to investigate the disturbing news about slavery on the planet. I'm afraid that after their last assignment, I have not heard a word from them and I'm quite worried.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What was their last assignment, your highness?
	if (response == "s_1209")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: They were suspecting that the Imperial force stationed on the planet was assisting the Trandoshan slavers. They informed me that they were heading in to investigate a Trandoshan camp east of Kachirho and that's the last I've heard from them.
			string_id message = new string_id (c_stringFile, "s_1211");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who were your informants, your highness?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I wish I could help but I just can't go to Kashyyyk, your highness...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1213");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1221");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They were suspecting that the Imperial force stationed on the planet was assisting the Trandoshan slavers. They informed me that they were heading in to investigate a Trandoshan camp east of Kachirho and that's the last I've heard from them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who were your informants, your highness?
	if (response == "s_1213")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: There names were Orrekazzapirr, Drrlirm and Imarrra. Does this mean that you will help me %TU?
			string_id message = new string_id (c_stringFile, "s_1215");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course, your highness. My people are in danger.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1217");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 28);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I wish I could help but I just can't go to Kashyyyk, your highness...
	if (response == "s_1221")
	{
		doAnimationAction (player, "slump_head");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			//-- NPC: You don't have to explain %TU. It was a long shot and I understand completely. I will find another way. Take well care of yourself and come back to see me anytime.
			string_id message = new string_id (c_stringFile, "s_1223");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There names were Orrekazzapirr, Drrlirm and Imarrra. Does this mean that you will help me %TU?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course, your highness. My people are in danger.
	if (response == "s_1217")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			ep3_clone_relics_queen_kylantha_action_grantQuestOne (player, npc);

			//-- NPC: I can tell that the stories about you were not exaggerated at all %TU. Please be careful, let me know if you need anything and keep me informed.
			string_id message = new string_id (c_stringFile, "s_1219");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent. Have you heard of the planet Kashyyyk %TU? It's the home planet of the Wookiee species. I employed some of the Wookiees to investigate the disturbing news of slavery on the planet. After their last assignment, I have yet to hear from them and I'm very worried.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What was their last assignment, your highness?
	if (response == "s_1227")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: They were suspecting that the Imperial force stationed on the planet was assisting the Trandoshan slavers. They informed me that they were heading in to investigate a Trandoshan camp east of Kachirho and that's the last I've heard from them.
			string_id message = new string_id (c_stringFile, "s_1229");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who were your informants, your highness?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I wish I could help but right now, it's impossible...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1231");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1239");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: From what I've heard, Kashyyyk is quite the dangerous place.
	if (response == "s_1243")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes but my employees were natives to the planet and experts on surviving the wilderness of Kashyyyk. Even still, I almost hope that you are right and an accident is what happened, not what I suspect...
			string_id message = new string_id (c_stringFile, "s_1245");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What did you hire them for?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I wish I could help but right now, it's just not possible.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1247");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1249");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They were suspecting that the Imperial force stationed on the planet was assisting the Trandoshan slavers. They informed me that they were heading in to investigate a Trandoshan camp east of Kachirho and that's the last I've heard from them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who were your informants, your highness?
	if (response == "s_1231")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: There names were Orrekazzapirr, Drrlirm and Imarrra. Does this mean that you will help me %TU?
			string_id message = new string_id (c_stringFile, "s_1233");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It would be my honor, your highness.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1235");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 33);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I wish I could help but right now, it's impossible...
	if (response == "s_1239")
	{
		doAnimationAction (player, "slump_head");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			//-- NPC: You don't have to explain %TU. It was a long shot and I understand completely. I will find another way. Take well care of yourself and come back to see me anytime.
			string_id message = new string_id (c_stringFile, "s_1241");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There names were Orrekazzapirr, Drrlirm and Imarrra. Does this mean that you will help me %TU?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It would be my honor, your highness.
	if (response == "s_1235")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			ep3_clone_relics_queen_kylantha_action_grantQuestOne (player, npc);

			//-- NPC: I can tell that the stories about you were not exaggerated at all %TU. Please be careful, let me know if you need anything and keep me informed.
			string_id message = new string_id (c_stringFile, "s_1237");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes but my employees were natives to the planet and experts on surviving the wilderness of Kashyyyk. Even still, I almost hope that you are right and an accident is what happened, not what I suspect...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What did you hire them for?
	if (response == "s_1247")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: They were suspecting that the Imperial force stationed on the planet was assisting the Trandoshan slavers. They informed me that they were heading in to investigate a Trandoshan camp east of Kachirho and that's the last I've heard from them.
			string_id message = new string_id (c_stringFile, "s_1229");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who were your informants, your highness?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I wish I could help but right now, it's impossible...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1231");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1239");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I wish I could help but right now, it's just not possible.
	if (response == "s_1249")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey");

			doAnimationAction (player, "bow");

			//-- NPC: You don't have to explain %TU. It was a long shot and I understand completely. I will find another way. Take well care of yourself and come back to see me anytime.
			string_id message = new string_id (c_stringFile, "s_1251");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Maybe some of the stories have been exaggerated a bit but rumors are started for a reason %TU.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: But my adventures are as exciting as those of a moisture farmer.
	if (response == "s_1259")
	{
		doAnimationAction (player, "slump_head");

		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You're being too modest %TU. Could I have a few minutes of your time? I need help with something that concerns me gravely. Most likely you're busy and I understand but maybe you would like to hear me out anyway?
			string_id message = new string_id (c_stringFile, "s_1203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm sort of in the middle of jobs anyway so sure.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Of course, your highness.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1205");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1253");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 25);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Which quest?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 1
	if (response == "s_1278")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_queen_kylantha_action_removeQuest1 (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_1281");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 2
	if (response == "s_1279")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_queen_kylantha_action_removeQuest2 (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_1283");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: All
	if (response == "s_1280")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_queen_kylantha_action_removeQuestAll (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_1285");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_queen_kylantha_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're not supposed to be back here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who are you?
	if (response == "s_1263")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			//-- NPC: No one important. Now please, return to the public parts of the palace.
			string_id message = new string_id (c_stringFile, "s_1265");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.ep3_clone_relics_queen_kylantha");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	setName(self, "");
	setName(self, new string_id( "ep3/npc_names", "clone_relics_queen_kylantha" ));
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	setName(self, "");
	setName(self, new string_id( "ep3/npc_names", "clone_relics_queen_kylantha" ));
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);
	faceTo (self, player);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.ep3_clone_relics_queen_kylantha");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_playerIsOnEmperor02 (player, npc))
	{
		ep3_clone_relics_queen_kylantha_action_sendEmperor02Signal (player, npc);

		//-- NPC: I am shocked that the Rebel Alliance would stoop so low as to make a personal attack on myself, the Queen of Naboo. You may tell the Emperor that I apologize for my misgivings, and that he has my full support.
		string_id message = new string_id (c_stringFile, "s_89");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_playerIsOnEmperor01 (player, npc))
	{
		ep3_clone_relics_queen_kylantha_action_sendEmperor01Signal (player, npc);

		//-- NPC: So the Emperor sends his assurances that the Empire had nothing to do with the attack on my chambers? As you say. I am not yet convinced, but I am listening.
		string_id message = new string_id (c_stringFile, "s_88");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_receivedReward (player, npc))
	{
		doAnimationAction (npc, "offer_affection");

		doAnimationAction (player, "bow");

		//-- NPC: Great to see you %NU, it's always a pleasure.
		string_id message = new string_id (c_stringFile, "s_1133");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: You too, your highness.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_queen_kylantha_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1135");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1276");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 3);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_queen_kylantha", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_hasCompletedSpaceMission (player, npc))
	{
		doAnimationAction (npc, "offer_affection");

		doAnimationAction (player, "bow");

		//-- NPC: I've already heard %TU. I can't even begin to tell you how grateful I am.
		string_id message = new string_id (c_stringFile, "s_1143");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: My pleasure, your highness. I'm just glad I could save them.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_queen_kylantha_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1145");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1274");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 6);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_queen_kylantha", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_failedSpaceMission (player, npc))
	{
		doAnimationAction (player, "bow");

		//-- NPC: There you are %TU. You have to get back to the Dathomir system immediately!
		string_id message = new string_id (c_stringFile, "s_1157");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I failed, your highness. The informants were shot down...
		boolean hasResponse0 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_queen_kylantha_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1159");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1272");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 10);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_queen_kylantha", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_failedQuestTwo (player, npc))
	{
		doAnimationAction (npc, "offer_affection");

		doAnimationAction (player, "bow");

		//-- NPC: How are things going %TU?
		string_id message = new string_id (c_stringFile, "s_82");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Good, your highness. I've already found one of the informants.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_83");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 14);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_queen_kylantha", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_hasCompletedQuestOne (player, npc))
	{
		doAnimationAction (npc, "offer_affection");

		doAnimationAction (player, "bow");

		//-- NPC: How are things progressing %TU?
		string_id message = new string_id (c_stringFile, "s_1171");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Good, your highness. I've already found one of the informants.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm afraid something really important came up, your highness...
		boolean hasResponse1 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_queen_kylantha_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1173");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1177");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1270");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 16);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_queen_kylantha", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_onQuestOne (player, npc))
	{
		doAnimationAction (npc, "offer_affection");

		doAnimationAction (player, "bow");

		//-- NPC: Did you need anything %TU?
		string_id message = new string_id (c_stringFile, "s_1181");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Just checking if there's been any news before I take off, your highness?
		boolean hasResponse0 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm afraid something really important came up, your highness...
		boolean hasResponse1 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_queen_kylantha_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1183");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1191");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1268");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 19);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_queen_kylantha", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition_playerIsRebel (player, npc))
	{
		doAnimationAction (npc, "offer_affection");

		doAnimationAction (player, "bow");

		//-- NPC: I was hoping you would stop by %TU.
		string_id message = new string_id (c_stringFile, "s_1195");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: You know who I am, your highness?
		boolean hasResponse0 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_queen_kylantha_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1197");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1266");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 23);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_queen_kylantha", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
	{
		//-- NPC: You're not supposed to be back here.
		string_id message = new string_id (c_stringFile, "s_1261");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Who are you?
		boolean hasResponse0 = false;
		if (ep3_clone_relics_queen_kylantha_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1263");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId", 43);

			npcStartConversation (player, npc, "ep3_clone_relics_queen_kylantha", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "ep3_clone_relics_queen_kylantha")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

	if (branchId == 3 && ep3_clone_relics_queen_kylantha_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && ep3_clone_relics_queen_kylantha_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && ep3_clone_relics_queen_kylantha_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && ep3_clone_relics_queen_kylantha_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && ep3_clone_relics_queen_kylantha_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && ep3_clone_relics_queen_kylantha_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && ep3_clone_relics_queen_kylantha_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && ep3_clone_relics_queen_kylantha_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && ep3_clone_relics_queen_kylantha_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && ep3_clone_relics_queen_kylantha_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && ep3_clone_relics_queen_kylantha_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && ep3_clone_relics_queen_kylantha_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && ep3_clone_relics_queen_kylantha_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && ep3_clone_relics_queen_kylantha_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && ep3_clone_relics_queen_kylantha_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && ep3_clone_relics_queen_kylantha_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && ep3_clone_relics_queen_kylantha_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && ep3_clone_relics_queen_kylantha_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && ep3_clone_relics_queen_kylantha_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && ep3_clone_relics_queen_kylantha_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && ep3_clone_relics_queen_kylantha_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && ep3_clone_relics_queen_kylantha_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && ep3_clone_relics_queen_kylantha_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && ep3_clone_relics_queen_kylantha_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.ep3_clone_relics_queen_kylantha.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

