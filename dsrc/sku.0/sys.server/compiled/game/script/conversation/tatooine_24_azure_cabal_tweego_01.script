// ======================================================================
//
// tatooine_24_azure_cabal_tweego_01.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/tatooine_24_azure_cabal_tweego_01";

// ======================================================================
// Script Conditions
// ======================================================================

boolean tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasActiveTaskGetTweegosEndorsement (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_tweego");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasCompletedHuntersPath2 (obj_id player, obj_id npc)
{
return groundquests.hasCompletedQuest(player, "tatooine_24_azure_cabal_hunters_path_02");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasActiveTaskHuntersPath2Return (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "tatooine_24_azure_cabal_hunters_path_02", "azure_cabal_hunters_path_02_return");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasActiveTaskContactTweego (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_meet_tweego");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasReturnInDisgrace (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "tatooine_24_azure_cabal_hunters_path_01");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_needsHuntersPath (obj_id player, obj_id npc)
{
	return !groundquests.isQuestActive(player, "tatooine_24_azure_cabal_hunters_path_02");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasHuntersPath (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "tatooine_24_azure_cabal_hunters_path_02");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasActiveQuestHuntersPathNotFinished (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "tatooine_24_azure_cabal_hunters_path_02"); 
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasActiveTaskHuntersPath2Banthas (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "tatooine_24_azure_cabal_hunters_path_02", "azure_cabal_hunters_path_02_destroy_loot_1");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_tweego_01_condition_hasActiveTaskHuntersPath2Brambles (obj_id player, obj_id npc)
{
	return (groundquests.isQuestActive(player, "tatooine_24_azure_cabal_hunters_path_02")
		&& !groundquests.isTaskActive(player, "tatooine_24_azure_cabal_hunters_path_02", "azure_cabal_hunters_path_02_destroy_loot_1"));
}

// ======================================================================
// Script Actions
// ======================================================================

void tatooine_24_azure_cabal_tweego_01_action_signalTweegoEndorsement (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "endorsedByTweego");
}

// ----------------------------------------------------------------------

void tatooine_24_azure_cabal_tweego_01_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ----------------------------------------------------------------------

void tatooine_24_azure_cabal_tweego_01_action_grantHunterPath_2 (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.grantQuest(player, "quest/tatooine_24_azure_cabal_hunters_path_02");
	groundquests.sendSignal(player, "workingForTweego");
	groundquests.sendSignal(player, "azure_cabal_hunters_path_01_try_again");
}

// ----------------------------------------------------------------------

void tatooine_24_azure_cabal_tweego_01_action_signalHuntersPath2Complete (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "azure_cabal_hunters_path_02_returned_heart");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int tatooine_24_azure_cabal_tweego_01_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You survived? Well...I guess you might be good for something other than bait. Don't expect a parade! I'll send word to Loruna.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I suppose that's your highest praise.
	if (response == "s_58")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: It is what it is. Don't read anything into it.
			string_id message = new string_id (c_stringFile, "s_59");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, you made it farther than I thought you would, but you failed in the last part.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So do I get the endorsement?
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You did not do as badly as I had expected, nor as well as I had hoped... I will grant the endorsement if that is what you want, but I will not pay the full amount. You did not complete the job.
			string_id message = new string_id (c_stringFile, "s_47");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just endorse me already.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me try again!
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to try again.
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_tweego_01_action_grantHunterPath_2 (player, npc);

			//-- NPC: I respect that. Request granted.
			string_id message = new string_id (c_stringFile, "s_46");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You did not do as badly as I had expected, nor as well as I had hoped... I will grant the endorsement if that is what you want, but I will not pay the full amount. You did not complete the job.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just endorse me already.
	if (response == "s_48")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_tweego_01_action_signalHuntersPath2Complete (player, npc);

			//-- NPC: Done.
			string_id message = new string_id (c_stringFile, "s_50");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let me try again!
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_tweego_01_action_grantHunterPath_2 (player, npc);

			//-- NPC: I respect that. Request granted.
			string_id message = new string_id (c_stringFile, "s_46");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What do you need? You should be out hunting banthas.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can't find any banthas around here!
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: It's called hunting for a reason. I didn't even specify a type of bantha. Any bantha will have the gland you need. Just find one, kill it, and wait for my comm. Pretty simple, I'd say.
			string_id message = new string_id (c_stringFile, "s_64");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Fine.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll figure it out.
	if (response == "s_62")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Come back when you have completed the task I set before you.
			string_id message = new string_id (c_stringFile, "s_63");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's called hunting for a reason. I didn't even specify a type of bantha. Any bantha will have the gland you need. Just find one, kill it, and wait for my comm. Pretty simple, I'd say.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine.
	if (response == "s_65")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Come back when you have completed the task I set before you.
			string_id message = new string_id (c_stringFile, "s_63");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Now what?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What should I do now?
	if (response == "s_67")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Come back when you have completed the task I set before you.
			string_id message = new string_id (c_stringFile, "s_63");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Loruna sent you? Someday she's going to trust the wrong person...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: She said you would have something for me to do...
	if (response == "s_18")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: She's the boss, though what the Azure Cabal needs with someone like you I can't understand.
			string_id message = new string_id (c_stringFile, "s_20");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: As you said, she's the boss.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: She's the boss, though what the Azure Cabal needs with someone like you I can't understand.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: As you said, she's the boss.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'll test you, but understand this: I've known her for years and I take her safety *very* seriously.
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Why do you think I'm a threat?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_26");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_28");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'll test you, but understand this: I've known her for years and I take her safety *very* seriously.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let's get on with it.
	if (response == "s_26")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_tweego_01_action_grantHunterPath_2 (player, npc);

			//-- NPC: You're going to bring me the heart of a dominant bantha bull. To identify the bull, you must get a hold of a musk gland from a smaller beast. By rubbing the hormones on yourself, you'll smell like a challenger. Trust me, the dominant bull will find you soon enough.
			string_id message = new string_id (c_stringFile, "s_44");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Easy.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why do you think I'm a threat?
	if (response == "s_28")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I am a hunter! I know a predator when I see one. As of now I have no reason to trust you and every reason to gladly watch you die.
			string_id message = new string_id (c_stringFile, "s_30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Too bad. Just upload the data already.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, give me a chance to prove myself.
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_32");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am a hunter! I know a predator when I see one. As of now I have no reason to trust you and every reason to gladly watch you die.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Too bad. Just upload the data already.
	if (response == "s_32")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_tweego_01_action_grantHunterPath_2 (player, npc);

			//-- NPC: You're going to bring me the heart of a dominant bantha bull. To identify the bull, you must get a hold of a musk gland from a smaller beast. By rubbing the hormones on yourself, you'll smell like a challenger. Trust me, the dominant bull will find you soon enough.
			string_id message = new string_id (c_stringFile, "s_44");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Easy.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, give me a chance to prove myself.
	if (response == "s_34")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: All I can do is give you a chance to prove your skills. Only time - and a heap of successful service to the Cabal - will prove your worth.
			string_id message = new string_id (c_stringFile, "s_36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just upload the data.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Fine. What's my first task?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_40");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: All I can do is give you a chance to prove your skills. Only time - and a heap of successful service to the Cabal - will prove your worth.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just upload the data.
	if (response == "s_38")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_tweego_01_action_grantHunterPath_2 (player, npc);

			//-- NPC: You're going to bring me the heart of a dominant bantha bull. To identify the bull, you must get a hold of a musk gland from a smaller beast. By rubbing the hormones on yourself, you'll smell like a challenger. Trust me, the dominant bull will find you soon enough.
			string_id message = new string_id (c_stringFile, "s_44");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Easy.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine. What's my first task?
	if (response == "s_40")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_tweego_01_action_grantHunterPath_2 (player, npc);

			//-- NPC: You're going to bring me the heart of a dominant bantha bull. To identify the bull, you must get a hold of a musk gland from a smaller beast. By rubbing the hormones on yourself, you'll smell like a challenger. Trust me, the dominant bull will find you soon enough.
			string_id message = new string_id (c_stringFile, "s_44");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Easy.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_tweego_01_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're going to bring me the heart of a dominant bantha bull. To identify the bull, you must get a hold of a musk gland from a smaller beast. By rubbing the hormones on yourself, you'll smell like a challenger. Trust me, the dominant bull will find you soon enough.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Easy.
	if (response == "s_52")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You think so, do you?
			string_id message = new string_id (c_stringFile, "s_54");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isTangible (self)) || (isPlayer (self)))
		detachScript(self, "conversation.tatooine_24_azure_cabal_tweego_01");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.tatooine_24_azure_cabal_tweego_01");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_tweego_01_condition_hasCompletedHuntersPath2 (player, npc))
	{
		tatooine_24_azure_cabal_tweego_01_action_facePlayer (player, npc);

		//-- NPC: Loruna's the boss. When she tells me to give you a job, I give you a job. Until then, you're wasting air.
		string_id message = new string_id (c_stringFile, "s_9");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_tweego_01_condition_hasActiveTaskHuntersPath2Return (player, npc))
	{
		tatooine_24_azure_cabal_tweego_01_action_signalHuntersPath2Complete (player, npc);

		//-- NPC: You survived? Well...I guess you might be good for something other than bait. Don't expect a parade! I'll send word to Loruna.
		string_id message = new string_id (c_stringFile, "s_5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I suppose that's your highest praise.
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_58");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 2);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_tweego_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_tweego_01_condition_hasReturnInDisgrace (player, npc))
	{
		//-- NPC: Well, you made it farther than I thought you would, but you failed in the last part.
		string_id message = new string_id (c_stringFile, "s_41");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: So do I get the endorsement?
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I want to try again.
		boolean hasResponse1 = false;
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_45");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_43");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 4);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_tweego_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_tweego_01_condition_hasActiveTaskHuntersPath2Banthas (player, npc))
	{
		tatooine_24_azure_cabal_tweego_01_action_facePlayer (player, npc);

		//-- NPC: What do you need? You should be out hunting banthas.
		string_id message = new string_id (c_stringFile, "s_57");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I can't find any banthas around here!
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'll figure it out.
		boolean hasResponse1 = false;
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_62");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 8);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_tweego_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_tweego_01_condition_hasActiveQuestHuntersPathNotFinished (player, npc))
	{
		//-- NPC: Now what?
		string_id message = new string_id (c_stringFile, "s_66");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What should I do now?
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_67");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 11);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_tweego_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_tweego_01_condition_hasActiveTaskGetTweegosEndorsement (player, npc))
	{
		tatooine_24_azure_cabal_tweego_01_action_facePlayer (player, npc);

		//-- NPC: Loruna sent you? Someday she's going to trust the wrong person...
		string_id message = new string_id (c_stringFile, "s_16");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: She said you would have something for me to do...
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_18");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId", 12);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_tweego_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_tweego_01_condition__defaultCondition (player, npc))
	{
		tatooine_24_azure_cabal_tweego_01_action_facePlayer (player, npc);

		//-- NPC: Trust me...I'm not who you're looking for.
		string_id message = new string_id (c_stringFile, "s_56");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "tatooine_24_azure_cabal_tweego_01")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

	if (branchId == 2 && tatooine_24_azure_cabal_tweego_01_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && tatooine_24_azure_cabal_tweego_01_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && tatooine_24_azure_cabal_tweego_01_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && tatooine_24_azure_cabal_tweego_01_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && tatooine_24_azure_cabal_tweego_01_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && tatooine_24_azure_cabal_tweego_01_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && tatooine_24_azure_cabal_tweego_01_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && tatooine_24_azure_cabal_tweego_01_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && tatooine_24_azure_cabal_tweego_01_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && tatooine_24_azure_cabal_tweego_01_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && tatooine_24_azure_cabal_tweego_01_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && tatooine_24_azure_cabal_tweego_01_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_tweego_01.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

