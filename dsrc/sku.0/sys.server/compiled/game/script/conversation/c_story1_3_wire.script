// ======================================================================
//
// c_story1_3_wire.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.34 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.space_flags;
include library.space_quest;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/c_story1_3_wire";

// ======================================================================
// Script Conditions
// ======================================================================

boolean c_story1_3_wire_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean c_story1_3_wire_condition_needTalkWire1 (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive(player, "c_story1_3_imp", "talkWire1") ||
		groundquests.isTaskActive(player, "c_story1_3_neu", "talkWire1") ||
		groundquests.isTaskActive(player, "c_story1_3_reb", "talkWire1"))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean c_story1_3_wire_condition_onSpaceMission (obj_id player, obj_id npc)
{
	return (space_quest.hasQuest(player, "escort", "c_story1_3_space"));
}

// ----------------------------------------------------------------------

boolean c_story1_3_wire_condition_wonSpaceMission (obj_id player, obj_id npc)
{
	if (space_quest.hasWonQuest(player, "escort", "c_story1_3_space") &&
	!space_quest.hasReceivedReward (player, "escort", "c_story1_3_space"))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean c_story1_3_wire_condition_finished (obj_id player, obj_id npc)
{
	return (space_quest.hasWonQuest(player, "escort", "c_story1_3_space"));
}

// ----------------------------------------------------------------------

boolean c_story1_3_wire_condition_failedSpaceMission (obj_id player, obj_id npc)
{
	return (space_quest.hasFailedQuest(player, "escort", "c_story1_3_space"));
}

// ======================================================================
// Script Actions
// ======================================================================

void c_story1_3_wire_action_signalTalked1 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "talkedWire1");
	space_quest.clearQuestFlags(player, "escort", "c_story1_3_space");
	space_quest.grantQuest(player, "escort", "c_story1_3_space");
}

// ----------------------------------------------------------------------

void c_story1_3_wire_action_signalTalked2 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "talkedWire2");
	space_quest.giveReward( player, "escort", "c_story1_3_space", 0, "" );
}

// ----------------------------------------------------------------------

void c_story1_3_wire_action_quitQuest3 (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "c_story1_3_reb");
	groundquests.clearQuest(player, "c_story1_3_neu");
	groundquests.clearQuest(player, "c_story1_3_imp");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int c_story1_3_wire_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There you are. I knew you were the right person for the job. I'm most pleased %NU.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was expecting more of a challenge. Satisfying none the less.
	if (response == "s_39")
	{
		doAnimationAction (player, "pose_proudly");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: What ever. Well we had a deal and I will of course come through on it. The pilot you are looking for did indeed come to me a while ago. He was trying to get information on who was out to kill him.
			string_id message = new string_id (c_stringFile, "s_41");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Did you have an answer for him?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm just glad I got out of it alive. Now for your end of the deal.
	if (response == "s_40")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Your missing pilot was visiting me trying to find out who wanted him dead.
			string_id message = new string_id (c_stringFile, "s_42");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Did you know who?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What ever. Well we had a deal and I will of course come through on it. The pilot you are looking for did indeed come to me a while ago. He was trying to get information on who was out to kill him.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Did you have an answer for him?
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. You see this pilot friend of yours had his hands in a lot of pockets, trying to get money where ever he could. For a while he was even shipping slaves for the Trandoshan's. I found out that they knew about his business with the Rebels and wanted him retired, permanently.
			string_id message = new string_id (c_stringFile, "s_44");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Trandoshan slavers eh? Why were they so upset about that?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_46");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certainly. You see this pilot friend of yours had his hands in a lot of pockets, trying to get money where ever he could. For a while he was even shipping slaves for the Trandoshan's. I found out that they knew about his business with the Rebels and wanted him retired, permanently.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Trandoshan slavers eh? Why were they so upset about that?
	if (response == "s_46")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			c_story1_3_wire_action_signalTalked2 (player, npc);

			//-- NPC: That, I don't know. I however know of someone that probably does. On Naboo resides a slavemaster for the Blackscale clan, I will point you to his building. Be warned though, it's like a fortress and the slavers are not reasonable. It will probably be suicide to go there. That of course is not my problem.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course. Your missing pilot was visiting me trying to find out who wanted him dead.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Did you know who?
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. You see this pilot friend of yours had his hands in a lot of pockets, trying to get money where ever he could. For a while he was even shipping slaves for the Trandoshan's. I found out that they knew about his business with the Rebels and wanted him retired, permanently.
			string_id message = new string_id (c_stringFile, "s_44");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Trandoshan slavers eh? Why were they so upset about that?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_46");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What can I do for you stranger?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've heard you're the person to talk to for information.
	if (response == "s_22")
	{
		doAnimationAction (player, "point_forward");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, who ever told you that are obviously in need of some, since he's misinformed.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm looking for an acquaintance of yours.
	if (response == "s_26")
	{
		doAnimationAction (player, "point_forward");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Who would that be?
			string_id message = new string_id (c_stringFile, "s_28");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Someone smuggling sensitive things if you know what I mean?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A smuggler, you had a chat with him a while ago.
			boolean hasResponse1 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_30");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was hoping you could help me find someone.
	if (response == "s_102")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should file a missing person report with the authorities; I'm just here to enjoy myself.
			string_id message = new string_id (c_stringFile, "s_104");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Who would that be?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Someone smuggling sensitive things if you know what I mean?
	if (response == "s_30")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Actually, I have no idea and you should probably leave me alone.
			string_id message = new string_id (c_stringFile, "s_32");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A smuggler, you had a chat with him a while ago.
	if (response == "s_34")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: I talk to many pilots; you will have to be more specific.
			string_id message = new string_id (c_stringFile, "s_36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: He was shipping a very special spice.
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I believe he was shipping contraband for the Rebels.
			boolean hasResponse1 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I talk to many pilots; you will have to be more specific.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: He was shipping a very special spice.
	if (response == "s_38")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes I know who you're talking about and I know who you are %NU. What I don't know is what you want from me?
			string_id message = new string_id (c_stringFile, "s_50");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you know about him?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A few days after you talked to him, he was shot down.
			boolean hasResponse1 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_56");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 12);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I believe he was shipping contraband for the Rebels.
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're wasting my time here. Now if you'll excuse me, I have a show to watch.
			string_id message = new string_id (c_stringFile, "s_94");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes I know who you're talking about and I know who you are %NU. What I don't know is what you want from me?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you know about him?
	if (response == "s_52")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Nothing and I don't really care to either. Now if you'll excuse me.
			string_id message = new string_id (c_stringFile, "s_54");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A few days after you talked to him, he was shot down.
	if (response == "s_56")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: I know, go on?
			string_id message = new string_id (c_stringFile, "s_58");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I searched the wreck but couldn't find him, was hoping you could help with that.
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I know, go on?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I searched the wreck but couldn't find him, was hoping you could help with that.
	if (response == "s_60")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Perhaps but help in this part of the galaxy doesn't come free.
			string_id message = new string_id (c_stringFile, "s_62");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How much do you want?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_64");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Perhaps but help in this part of the galaxy doesn't come free.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How much do you want?
	if (response == "s_64")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: This information doesn't come with a credit price. From what I've heard about you, you're quite the hero %NU and right now, that's what I need.
			string_id message = new string_id (c_stringFile, "s_66");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you need done?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 16);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This information doesn't come with a credit price. From what I've heard about you, you're quite the hero %NU and right now, that's what I need.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you need done?
	if (response == "s_68")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Black Sun is a menace in this system lately. Anything that isn't guarded by a fleet, they try to get their grubby little hands on. Well I can't afford an army and I'm expecting an extremely important shipment in a bit. I want you to make sure that it makes it safely here.
			string_id message = new string_id (c_stringFile, "s_70");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I believe I can do that. I have a score to settle with them anyway.
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Space battles aren't really my deal.
			boolean hasResponse1 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I don't have a ship I'm afraid.
			boolean hasResponse2 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_72");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Black Sun is a menace in this system lately. Anything that isn't guarded by a fleet, they try to get their grubby little hands on. Well I can't afford an army and I'm expecting an extremely important shipment in a bit. I want you to make sure that it makes it safely here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I believe I can do that. I have a score to settle with them anyway.
	if (response == "s_72")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			c_story1_3_wire_action_signalTalked1 (player, npc);

			//-- NPC: Excellent. You should take off at once. These are the coordinates where you will find my employee. Do this and I will make sure that it's worth your while.
			string_id message = new string_id (c_stringFile, "s_74");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Space battles aren't really my deal.
	if (response == "s_76")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Everything has a cost %NU but if you're not willing to pay the price...
			string_id message = new string_id (c_stringFile, "s_78");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Fine. What's the details?
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm afraid not.
			boolean hasResponse1 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 19);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't have a ship I'm afraid.
	if (response == "s_95")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: And you can't get one?
			string_id message = new string_id (c_stringFile, "s_96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Maybe, I'll be back.
			boolean hasResponse0 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No I don't think so.
			boolean hasResponse1 = false;
			if (c_story1_3_wire_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98");

				utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Everything has a cost %NU but if you're not willing to pay the price...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine. What's the details?
	if (response == "s_80")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			c_story1_3_wire_action_signalTalked1 (player, npc);

			//-- NPC: Excellent. You should take off at once. These are the coordinates where you will find my employee. Do this and I will make sure that it's worth your while.
			string_id message = new string_id (c_stringFile, "s_74");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm afraid not.
	if (response == "s_82")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Very well. You take care of yourself %NU.
			string_id message = new string_id (c_stringFile, "s_84");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, pp);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int c_story1_3_wire_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And you can't get one?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe, I'll be back.
	if (response == "s_97")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good, I'll most likely be here.
			string_id message = new string_id (c_stringFile, "s_99");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No I don't think so.
	if (response == "s_98")
	{
		//-- [NOTE] 
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			c_story1_3_wire_action_quitQuest3 (player, npc);

			//-- NPC: That is too bad %NU. I guess I'm going to have to find someone else. Take care.
			string_id message = new string_id (c_stringFile, "s_100");
			utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, pp);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.c_story1_3_wire");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	setName(self, new string_id( "npc_name", "wire" ));
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	setName(self, new string_id( "npc_name", "wire" ));
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.c_story1_3_wire");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (c_story1_3_wire_condition_failedSpaceMission (player, npc))
	{
		c_story1_3_wire_action_signalTalked1 (player, npc);

		//-- NPC: I would be more upset with you %NU but it appears my crafty employee has escaped and is ready for a second try. You better not fail me this time!
		string_id message = new string_id (c_stringFile, "s_48");
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (c_story1_3_wire_condition_wonSpaceMission (player, npc))
	{
		doAnimationAction (player, "greet");

		//-- NPC: There you are. I knew you were the right person for the job. I'm most pleased %NU.
		string_id message = new string_id (c_stringFile, "s_5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I was expecting more of a challenge. Satisfying none the less.
		boolean hasResponse0 = false;
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm just glad I got out of it alive. Now for your end of the deal.
		boolean hasResponse1 = false;
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_39");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_40");

			utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 2);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "c_story1_3_wire", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (c_story1_3_wire_condition_needTalkWire1 (player, npc))
	{
		doAnimationAction (player, "nod");

		//-- NPC: What can I do for you stranger?
		string_id message = new string_id (c_stringFile, "s_20");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I've heard you're the person to talk to for information.
		boolean hasResponse0 = false;
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm looking for an acquaintance of yours.
		boolean hasResponse1 = false;
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I was hoping you could help me find someone.
		boolean hasResponse2 = false;
		if (c_story1_3_wire_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_26");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_102");

			utils.setScriptVar (player, "conversation.c_story1_3_wire.branchId", 7);

			npcStartConversation (player, npc, "c_story1_3_wire", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (c_story1_3_wire_condition_finished (player, npc))
	{
		doAnimationAction (player, "greet");

		//-- NPC: I believe our business is concluded %NU.
		string_id message = new string_id (c_stringFile, "s_16");
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (c_story1_3_wire_condition_onSpaceMission (player, npc))
	{
		doAnimationAction (player, "greet");

		//-- NPC: My employee is waiting %NU. You need to get up there now.
		string_id message = new string_id (c_stringFile, "s_18");
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (c_story1_3_wire_condition__defaultCondition (player, npc))
	{
		doAnimationAction (player, "greet");

		//-- NPC: Questions, the galaxy is filled with them. Unfortunately, I don't have time for answers right now.
		string_id message = new string_id (c_stringFile, "s_106");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "c_story1_3_wire")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.c_story1_3_wire.branchId");

	if (branchId == 2 && c_story1_3_wire_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && c_story1_3_wire_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && c_story1_3_wire_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && c_story1_3_wire_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && c_story1_3_wire_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && c_story1_3_wire_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && c_story1_3_wire_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && c_story1_3_wire_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && c_story1_3_wire_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && c_story1_3_wire_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && c_story1_3_wire_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && c_story1_3_wire_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && c_story1_3_wire_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && c_story1_3_wire_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.c_story1_3_wire.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

