// ======================================================================
//
// talus_dearic_coa_vayde_quellim.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/talus_dearic_coa_vayde_quellim";

// ======================================================================
// Script Conditions
// ======================================================================

boolean talus_dearic_coa_vayde_quellim_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean talus_dearic_coa_vayde_quellim_condition_supplies_deliverable (obj_id player, obj_id npc)
{
	//has player retrieved the supplies for Vayde, but not delivered them yet
	return groundquests.isTaskActive(player, "talus_dearic_coa_rebel_view_end", "talus_dearic_coa_med_supplies_delivered_wfs");
}

// ----------------------------------------------------------------------

boolean talus_dearic_coa_vayde_quellim_condition_rebel_quest_accepted (obj_id player, obj_id npc)
{
	//checks if the PC has accepted the quests from Captain Quellium
	if (groundquests.isTaskActive(player, "talus_dearic_coa_rebel_view_end", "talus_dearic_coa_report_to_trezya_wfs") ||
	groundquests.isQuestActive(player, "talus_dearic_coa_rebel_view"))
		return true;
	else
		return false;	
}

// ----------------------------------------------------------------------

boolean talus_dearic_coa_vayde_quellim_condition_rebel_quest_complete (obj_id player, obj_id npc)
{
	//checks if the PC has completed the Rebel quest
	return groundquests.hasCompletedQuest(player, "talus_dearic_coa_rebel_view_end");
}

// ----------------------------------------------------------------------

boolean talus_dearic_coa_vayde_quellim_condition_rebel_tasks_complete (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "talus_dearic_coa_rebel_view_end", "talus_dearic_coa_rebel_gratitude_wfs");
}

// ----------------------------------------------------------------------

boolean talus_dearic_coa_vayde_quellim_condition_main_quest_accepted (obj_id player, obj_id npc)
{
	// Has the PC accepted the main quest from Dathnaeya
	return groundquests.isQuestActive(player, "talus_dearic_crisis_of_allegiance");
}

// ======================================================================
// Script Actions
// ======================================================================

void talus_dearic_coa_vayde_quellim_action_accept_rebel_quest (obj_id player, obj_id npc)
{
	// grant the quest to begin the Rebel quest
	groundquests.grantQuest(player, "talus_dearic_coa_rebel_view");
	groundquests.sendSignal(player, "talus_dearic_coa_spoke_with_vayde");
}

// ----------------------------------------------------------------------

void talus_dearic_coa_vayde_quellim_action_deliver_supplies (obj_id player, obj_id npc)
{
	// Send the signal that the Rebel quests are now complete
	groundquests.sendSignal(player, "talus_dearic_coa_med_supplies_delivered");
}

// ----------------------------------------------------------------------

void talus_dearic_coa_vayde_quellim_action_complete_rebel_quests (obj_id player, obj_id npc)
{
	// complete the rebel branch quest
	groundquests.sendSignal(player, "talus_dearic_coa_rebel_gratitude_accepted");
	groundquests.sendSignal(player, "talus_dearic_aclo_investigated");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int talus_dearic_coa_vayde_quellim_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome back.  What can I do for you today?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just wanted to see how the cause was fairing here, Captain.
	if (response == "s_45")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: We are always having our challenges, but we will prevail.  Of that, I have no doubt.
			string_id message = new string_id (c_stringFile, "s_46");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Glad to hear it.  May the Force be with you.
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing, Captain.  Just stopping in to say hello.
	if (response == "s_42")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Hello to you, then!  Stop by any time.
			string_id message = new string_id (c_stringFile, "s_44");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are always having our challenges, but we will prevail.  Of that, I have no doubt.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Glad to hear it.  May the Force be with you.
	if (response == "s_47")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			//-- NPC: And also with you.
			string_id message = new string_id (c_stringFile, "s_48");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well done!  Trezya says that you were a great help with the N.S.H.  I really appreciate that.  Those medical supplies will also go far in helping treat the unfortunate innocents who the Empire has harmed through their tyranny.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, the N.S.H. was nothing compared to trained Imperials.
	if (response == "s_29")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			talus_dearic_coa_vayde_quellim_action_deliver_supplies (player, npc);

			//-- NPC: Excellent.  You are a tenacious warrior, my friend.
			string_id message = new string_id (c_stringFile, "s_54");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I do what I must.
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Honestly, I'm not quite done yet.
	if (response == "s_56")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Oh?  Well, come on back when you finish what you need to.  I'll be waiting!
			string_id message = new string_id (c_stringFile, "s_58");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent.  You are a tenacious warrior, my friend.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I do what I must.
	if (response == "s_55")
	{
		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			talus_dearic_coa_vayde_quellim_action_complete_rebel_quests (player, npc);

			//-- NPC: And how!  Here is that monetary gratitude I spoke of when you took this job as well as the A.C.L.O.'s from Trezya.  So, have you decided to join the Alliance, then?
			string_id message = new string_id (c_stringFile, "s_59");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not yet, Captian.  I'm still gathering research.  But I'm considering it.
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, I don't think the Rebellion is the place for me, Captain.  Thanks for the opportunity to learn, though.
			boolean hasResponse1 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_63");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_71");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And how!  Here is that monetary gratitude I spoke of when you took this job as well as the A.C.L.O.'s from Trezya.  So, have you decided to join the Alliance, then?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet, Captian.  I'm still gathering research.  But I'm considering it.
	if (response == "s_63")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: I understand.  Come on back if you decide to join our cause.  Until then, take care of yourself.
			string_id message = new string_id (c_stringFile, "s_67");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I don't think the Rebellion is the place for me, Captain.  Thanks for the opportunity to learn, though.
	if (response == "s_71")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Ah well.  If you reconsider, I'll be around.  Have a great day!
			string_id message = new string_id (c_stringFile, "s_75");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome back. What can I do for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have those supplies you asked for.
	if (response == "s_65")
	{
		doAnimationAction (player, "explain");

		talus_dearic_coa_vayde_quellim_action_deliver_supplies (player, npc);

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "thank");

			//-- NPC: Great job!  Please speak to Trezya if you have not done so already.
			string_id message = new string_id (c_stringFile, "s_69");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Talk to Trezya if you haven't already.  She needs some help with the Nightstrike Soldier Hegemony.  Please return with those supplies the N.S.H. stole from us, also.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have the supplies.
	if (response == "s_51")
	{
		talus_dearic_coa_vayde_quellim_action_deliver_supplies (player, npc);

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fantastic!  Please speak to Trezya if you have not done so already.
			string_id message = new string_id (c_stringFile, "s_52");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome!  Are you here to join the Rebel Alliance?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe.
	if (response == "s_64")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "fakepunch");

			doAnimationAction (player, "taken_aback");

			//-- NPC: Maybe!  I like that.  Research your decisions first.  What can I do for you?
			string_id message = new string_id (c_stringFile, "s_66");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm interested in learning a bit more about the Rebellion first hand.
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No.
	if (response == "s_88")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: You're not an Imperial are you? No, of course not!  I'm just kidding with you.  What can I do for you?
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm interested in learning a bit more about the Rebellion first hand.
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not likely!
	if (response == "s_92")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Well then, there really isn't anything I can do for you.  Have a pleasant day...citizen!
			string_id message = new string_id (c_stringFile, "s_94");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Maybe!  I like that.  Research your decisions first.  What can I do for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm interested in learning a bit more about the Rebellion first hand.
	if (response == "s_68")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "udaman");

			talus_dearic_coa_vayde_quellim_action_accept_rebel_quest (player, npc);

			//-- NPC: Oho! Dathnaeya sent you.  Excellent!  Our lovely friend here is from the Aggregate Civil Liberties Organization.  Her organization is looking to join the Rebellion on a more permanent basis.  Perhaps now I can kill two flits with one stone.
			string_id message = new string_id (c_stringFile, "s_70");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds intriguing.  What's the plan?
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: The A.C.L.O.?  Not interested.  Sorry.
			boolean hasResponse1 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_72");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oho! Dathnaeya sent you.  Excellent!  Our lovely friend here is from the Aggregate Civil Liberties Organization.  Her organization is looking to join the Rebellion on a more permanent basis.  Perhaps now I can kill two flits with one stone.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds intriguing.  What's the plan?
	if (response == "s_72")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			doAnimationAction (player, "listen");

			//-- NPC: Well, first off.  There is an organization of mercenaries here on Talus that is giving me fits.  They are called the Nightstrike Soldier Hegemony.  Recently, they confiscated a shipment of medical supplies that would go a long way in helping the Alliance's efforts here.  There are 25 crates in all.  If you could retrieve those and bring them back to me, I'd be grateful in a large, monetary way.
			string_id message = new string_id (c_stringFile, "s_74");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: N.S.H., huh?  I've heard of them.  I'll take the job.
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Mercenary group?  On second thought, I think I'll pass.
			boolean hasResponse1 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The A.C.L.O.?  Not interested.  Sorry.
	if (response == "s_84")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Fair enough.  Have a pleasant day.
			string_id message = new string_id (c_stringFile, "s_86");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, first off.  There is an organization of mercenaries here on Talus that is giving me fits.  They are called the Nightstrike Soldier Hegemony.  Recently, they confiscated a shipment of medical supplies that would go a long way in helping the Alliance's efforts here.  There are 25 crates in all.  If you could retrieve those and bring them back to me, I'd be grateful in a large, monetary way.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: N.S.H., huh?  I've heard of them.  I'll take the job.
	if (response == "s_76")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "paper");

			//-- NPC: Thank you.  Be careful.  The base has a lot of enforcers training around it as well as a possible small Imperial force there.  Don't forget to talk to Trezya over there and see what she might need.  She will provide you with the location of the base.
			string_id message = new string_id (c_stringFile, "s_78");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will see what she needs.
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Mercenary group?  On second thought, I think I'll pass.
	if (response == "s_80")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Suit yourself.  Someone else will come along, I'm sure.  Have a pleasant day!
			string_id message = new string_id (c_stringFile, "s_82");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Thank you.  Be careful.  The base has a lot of enforcers training around it as well as a possible small Imperial force there.  Don't forget to talk to Trezya over there and see what she might need.  She will provide you with the location of the base.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will see what she needs.
	if (response == "s_81")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			//-- NPC: Excellent!
			string_id message = new string_id (c_stringFile, "s_83");
			utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int talus_dearic_coa_vayde_quellim_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're not an Imperial are you? No, of course not!  I'm just kidding with you.  What can I do for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm interested in learning a bit more about the Rebellion first hand.
	if (response == "s_68")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "udaman");

			talus_dearic_coa_vayde_quellim_action_accept_rebel_quest (player, npc);

			//-- NPC: Oho! Dathnaeya sent you.  Excellent!  Our lovely friend here is from the Aggregate Civil Liberties Organization.  Her organization is looking to join the Rebellion on a more permanent basis.  Perhaps now I can kill two flits with one stone.
			string_id message = new string_id (c_stringFile, "s_70");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds intriguing.  What's the plan?
			boolean hasResponse0 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: The A.C.L.O.?  Not interested.  Sorry.
			boolean hasResponse1 = false;
			if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_72");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isTangible (self)) || (isPlayer (self)))
		detachScript(self, "conversation.talus_dearic_coa_vayde_quellim");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.talus_dearic_coa_vayde_quellim");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (talus_dearic_coa_vayde_quellim_condition_rebel_quest_complete (player, npc))
	{
		doAnimationAction (npc, "greet");

		doAnimationAction (player, "greet");

		//-- NPC: Welcome back.  What can I do for you today?
		string_id message = new string_id (c_stringFile, "s_4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Just wanted to see how the cause was fairing here, Captain.
		boolean hasResponse0 = false;
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Nothing, Captain.  Just stopping in to say hello.
		boolean hasResponse1 = false;
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_45");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_42");

			utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 1);

			npcStartConversation (player, npc, "talus_dearic_coa_vayde_quellim", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (talus_dearic_coa_vayde_quellim_condition_rebel_tasks_complete (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: Well done!  Trezya says that you were a great help with the N.S.H.  I really appreciate that.  Those medical supplies will also go far in helping treat the unfortunate innocents who the Empire has harmed through their tyranny.
		string_id message = new string_id (c_stringFile, "s_20");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Well, the N.S.H. was nothing compared to trained Imperials.
		boolean hasResponse0 = false;
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Honestly, I'm not quite done yet.
		boolean hasResponse1 = false;
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_29");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_56");

			utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 5);

			npcStartConversation (player, npc, "talus_dearic_coa_vayde_quellim", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (talus_dearic_coa_vayde_quellim_condition_supplies_deliverable (player, npc))
	{
		doAnimationAction (npc, "greet");

		doAnimationAction (player, "greet");

		//-- NPC: Welcome back. What can I do for you?
		string_id message = new string_id (c_stringFile, "s_61");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I have those supplies you asked for.
		boolean hasResponse0 = false;
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_65");

			utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 11);

			npcStartConversation (player, npc, "talus_dearic_coa_vayde_quellim", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (talus_dearic_coa_vayde_quellim_condition_rebel_quest_accepted (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Talk to Trezya if you haven't already.  She needs some help with the Nightstrike Soldier Hegemony.  Please return with those supplies the N.S.H. stole from us, also.
		string_id message = new string_id (c_stringFile, "s_60");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I have the supplies.
		boolean hasResponse0 = false;
		if (talus_dearic_coa_vayde_quellim_condition_supplies_deliverable (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_51");

			utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 13);

			npcStartConversation (player, npc, "talus_dearic_coa_vayde_quellim", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (talus_dearic_coa_vayde_quellim_condition_main_quest_accepted (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: Welcome!  Are you here to join the Rebel Alliance?
		string_id message = new string_id (c_stringFile, "s_62");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Maybe.
		boolean hasResponse0 = false;
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No.
		boolean hasResponse1 = false;
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Not likely!
		boolean hasResponse2 = false;
		if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_64");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_88");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_92");

			utils.setScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId", 15);

			npcStartConversation (player, npc, "talus_dearic_coa_vayde_quellim", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (talus_dearic_coa_vayde_quellim_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: I apologize, but I have guests I must attend to right now.  Can you come back later?
		string_id message = new string_id (c_stringFile, "s_96");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "talus_dearic_coa_vayde_quellim")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

	if (branchId == 1 && talus_dearic_coa_vayde_quellim_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && talus_dearic_coa_vayde_quellim_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && talus_dearic_coa_vayde_quellim_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && talus_dearic_coa_vayde_quellim_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && talus_dearic_coa_vayde_quellim_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && talus_dearic_coa_vayde_quellim_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && talus_dearic_coa_vayde_quellim_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && talus_dearic_coa_vayde_quellim_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && talus_dearic_coa_vayde_quellim_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && talus_dearic_coa_vayde_quellim_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && talus_dearic_coa_vayde_quellim_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && talus_dearic_coa_vayde_quellim_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && talus_dearic_coa_vayde_quellim_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.talus_dearic_coa_vayde_quellim.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

