// ======================================================================
//
// tatooine_eisley_aigorn.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/tatooine_eisley_aigorn";

// ======================================================================
// Script Conditions
// ======================================================================

boolean tatooine_eisley_aigorn_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_aigorn_condition_epiOnTask (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tatooine_eisley_epidemic");
	int tat_eisley_epidemic_e1 = groundquests.getTaskId(questId1, "tat_eisley_epidemic_e1"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_eisley_epidemic_e1, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_aigorn_condition_epiQuestComplete (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tatooine_eisley_epidemic");
	
	boolean OnTask = 	(questIsQuestComplete(questId1, player));
	
	return OnTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_aigorn_condition_epiTaskComplete (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tatooine_eisley_epidemic");
	int tat_eisley_epidemic_e2 = groundquests.getTaskId(questId1, "tat_eisley_epidemic_e2"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_eisley_epidemic_e2, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_aigorn_condition_digitQuestComplete (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tatooine_eisley_digital_infection");
	
	boolean OnTask = 	(questIsQuestComplete(questId1, player));
	
	return OnTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_aigorn_condition_boneQuestComplete (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tatooine_eisley_bone_gnasher");
	
	boolean OnTask = 	(questIsQuestComplete(questId1, player));
	
	return OnTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_aigorn_condition_icOnTask (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tatooine_eisley_information_control");
	int tat_eisley_information_control_e1 = groundquests.getTaskId(questId1, "tat_eisley_information_control_e1"); 
	int tat_eisley_information_control_e2 = groundquests.getTaskId(questId1, "tat_eisley_information_control_e2"); 
		
	boolean onTask1 = questIsTaskActive(questId1, tat_eisley_information_control_e1, player);
	boolean onTask2 = questIsTaskActive(questId1, tat_eisley_information_control_e2, player);
		
	boolean onTask = false;
	
	if (onTask1 || onTask2)
		onTask = true;
				
	return onTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_aigorn_condition_icTaskComplete (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tatooine_eisley_information_control");
	int tat_eisley_epidemic_e2 = groundquests.getTaskId(questId1, "tat_eisley_information_control_e2"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_eisley_epidemic_e2, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_aigorn_condition_blocker (obj_id player, obj_id npc)
{
	return groundquests.isQuestDisabled("tatooine_eisley_epidemic");
}

// ======================================================================
// Script Actions
// ======================================================================

void tatooine_eisley_aigorn_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ----------------------------------------------------------------------

void tatooine_eisley_aigorn_action_signalEpiReward (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "tat_eisley_epidemic_e2");
}

// ----------------------------------------------------------------------

void tatooine_eisley_aigorn_action_grantEpiQuest (obj_id player, obj_id npc)
{
    int questId = questGetQuestId("quest/tatooine_eisley_epidemic");
    groundquests.grantQuest(questId, player, npc, true);

}

// ----------------------------------------------------------------------

void tatooine_eisley_aigorn_action_grantICQuest (obj_id player, obj_id npc)
{
    int questId = questGetQuestId("quest/tatooine_eisley_information_control");
    groundquests.grantQuest(questId, player, npc, true);

}

// ----------------------------------------------------------------------

void tatooine_eisley_aigorn_action_signalICReward (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_eisley_information_control_e2");
}

// ----------------------------------------------------------------------

void tatooine_eisley_aigorn_action_grantDFQuest (obj_id player, obj_id npc)
{
    int questId = questGetQuestId("quest/tatooine_eisley_drensfolly");
    groundquests.grantQuest(questId, player, npc, true);

}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int tatooine_eisley_aigorn_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Have you demonstrated to the subject just how important it is that she remain quiet?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: She will not be telling any more stories.
	if (response == "s_76")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_aigorn_action_signalICReward (player, npc);

			//-- NPC: Good, because there is another leak. It appears Dren is in charge at White Horse, and he knows about the research. He needs to be eliminated as well.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Understood. He will not live through the night.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet. I am on my way now.
	if (response == "s_73")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Excellent.
			string_id message = new string_id (c_stringFile, "s_74");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good, because there is another leak. It appears Dren is in charge at White Horse, and he knows about the research. He needs to be eliminated as well.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Understood. He will not live through the night.
	if (response == "s_103")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_aigorn_action_grantDFQuest (player, npc);

			//-- NPC: Very good.
			string_id message = new string_id (c_stringFile, "s_105");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have done well. I am sure this will bring a good pay off. There is only one problem remaining.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is that?
	if (response == "s_40")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Apparently, there is someone that was involved in these experiements that is now informing other parties about the medical data. We cannot allow that to continue.
			string_id message = new string_id (c_stringFile, "s_41");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are you suggesting?
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_42");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Apparently, there is someone that was involved in these experiements that is now informing other parties about the medical data. We cannot allow that to continue.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are you suggesting?
	if (response == "s_42")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: I want you to end that source of information.
			string_id message = new string_id (c_stringFile, "s_43");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You want me to kill them?
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I want you to end that source of information.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You want me to kill them?
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did I say that? Murder is a crime. I would never do anything illegal. How you end that source of information is your problem. That is why I pay you so well.
			string_id message = new string_id (c_stringFile, "s_45");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will persuade this person that silence is often golden.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_46");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Did I say that? Murder is a crime. I would never do anything illegal. How you end that source of information is your problem. That is why I pay you so well.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will persuade this person that silence is often golden.
	if (response == "s_46")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Exactly my wishes. You will go far on Tatooine my friend. This planet revolves around people like you and I.
			string_id message = new string_id (c_stringFile, "s_47");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is this talkative person located?
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No way. I'm through working for you.
			boolean hasResponse1 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_62");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Exactly my wishes. You will go far on Tatooine my friend. This planet revolves around people like you and I.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is this talkative person located?
	if (response == "s_48")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_aigorn_action_grantICQuest (player, npc);

			//-- NPC: Here is her location.
			string_id message = new string_id (c_stringFile, "s_49");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No way. I'm through working for you.
	if (response == "s_62")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: No fool. You're just through.
			string_id message = new string_id (c_stringFile, "s_63");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Have you got the data?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No.
	if (response == "s_64")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then why are you here?
			string_id message = new string_id (c_stringFile, "s_65");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I want more money.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_66");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then why are you here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want more money.
	if (response == "s_66")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course you do, doesn't everyone?
			string_id message = new string_id (c_stringFile, "s_67");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm serious. I want more of the cut.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course you do, doesn't everyone?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm serious. I want more of the cut.
	if (response == "s_68")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: We'll see when you get the data.
			string_id message = new string_id (c_stringFile, "s_69");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be expecting a good pay off, Aigorn.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We'll see when you get the data.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll be expecting a good pay off, Aigorn.
	if (response == "s_70")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: You'll get it and more.
			string_id message = new string_id (c_stringFile, "s_71");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So. you've decided to dump that short-sighted Reimus and get with the winning team? Good choice. Do what I need, and you'll be a rich man.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just what is it you need?
	if (response == "s_36")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: I want the medical data Reimus uncovered. My own sources have stated it could be very useful in the right hands.
			string_id message = new string_id (c_stringFile, "s_38");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who has the right hands?
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm not really concerned with what you want to do with it.
			boolean hasResponse1 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_51");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I want the medical data Reimus uncovered. My own sources have stated it could be very useful in the right hands.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who has the right hands?
	if (response == "s_51")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not a good start, my friend. Part of the deal is you do what I ask without worrying about the details. That's my job.
			string_id message = new string_id (c_stringFile, "s_53");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've got a bad feeling about this. I'm out.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I go where the credits are. That's all I really need to know.
			boolean hasResponse1 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not really concerned with what you want to do with it.
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: That's smart. You're looking for credits, and I have them. I'll make sure it's worth your while if you gave that medical data to me.
			string_id message = new string_id (c_stringFile, "s_83");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll make sure you get the information.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not a good start, my friend. Part of the deal is you do what I ask without worrying about the details. That's my job.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've got a bad feeling about this. I'm out.
	if (response == "s_55")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: You aren't as smart as my sources led me to believe.
			string_id message = new string_id (c_stringFile, "s_57");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I go where the credits are. That's all I really need to know.
	if (response == "s_59")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm glad we're clear on this.
			string_id message = new string_id (c_stringFile, "s_75");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: We are, indeed.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm glad we're clear on this.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: We are, indeed.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Just one more thing. If anyone else gets this data besides me. I will make sure you suffer for a lifetime, understand?
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I understand. Bad things will happen.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's smart. You're looking for credits, and I have them. I'll make sure it's worth your while if you gave that medical data to me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll make sure you get the information.
	if (response == "s_85")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go back in to the base and find their infirmary. Get the data from their medical computer and return to me.
			string_id message = new string_id (c_stringFile, "s_87");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It will be easy. You'll have that data soon.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_89");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Go back in to the base and find their infirmary. Get the data from their medical computer and return to me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It will be easy. You'll have that data soon.
	if (response == "s_89")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Just one more thing. If anyone else gets this data besides me. I will make sure you suffer for a lifetime, understand?
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I understand. Bad things will happen.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Just one more thing. If anyone else gets this data besides me. I will make sure you suffer for a lifetime, understand?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I understand. Bad things will happen.
	if (response == "s_93")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_aigorn_action_grantEpiQuest (player, npc);

			//-- NPC: Very bad things.
			string_id message = new string_id (c_stringFile, "s_95");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So Tori mentioned me did she? I'll have to make sure she doesn't do that again. You however, appear to be someone I can depend on.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Depend on for what?
	if (response == "s_99")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: I need data extracted from a computer inside a shipping company near here. It would be worth quite a few credits if delivered into the right hands.
			string_id message = new string_id (c_stringFile, "s_101");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who has the right hands?
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_aigorn_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I need data extracted from a computer inside a shipping company near here. It would be worth quite a few credits if delivered into the right hands.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who has the right hands?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not a good start, my friend. Part of the deal is you do what I ask without worrying about the details. That's my job.
			string_id message = new string_id (c_stringFile, "s_53");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've got a bad feeling about this. I'm out.
			boolean hasResponse0 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I go where the credits are. That's all I really need to know.
			boolean hasResponse1 = false;
			if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.tatooine_eisley_aigorn");

    setCondition (self, CONDITION_CONVERSABLE);
    setCondition (self, CONDITION_INTERESTING);
    setInvulnerable (self, true);

    setName (self, "Aigorn");

    ai_lib.setDefaultCalmBehavior( self, ai_lib.BEHAVIOR_SENTINEL );

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
    setCondition (self, CONDITION_CONVERSABLE);
    setCondition (self, CONDITION_INTERESTING);
    setInvulnerable (self, true);

    setName (self, "Aigorn");

    ai_lib.setDefaultCalmBehavior( self, ai_lib.BEHAVIOR_SENTINEL );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.tatooine_eisley_aigorn");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (tatooine_eisley_aigorn_condition_icOnTask (player, npc))
	{
		tatooine_eisley_aigorn_action_facePlayer (player, npc);

		//-- NPC: Have you demonstrated to the subject just how important it is that she remain quiet?
		string_id message = new string_id (c_stringFile, "s_72");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: She will not be telling any more stories.
		boolean hasResponse0 = false;
		if (tatooine_eisley_aigorn_condition_icTaskComplete (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not yet. I am on my way now.
		boolean hasResponse1 = false;
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_76");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_73");

			utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 1);

			npcStartConversation (player, npc, "tatooine_eisley_aigorn", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_aigorn_condition_epiTaskComplete (player, npc))
	{
		tatooine_eisley_aigorn_action_signalEpiReward (player, npc);

		//-- NPC: You have done well. I am sure this will bring a good pay off. There is only one problem remaining.
		string_id message = new string_id (c_stringFile, "s_39");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What is that?
		boolean hasResponse0 = false;
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_40");

			utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 5);

			npcStartConversation (player, npc, "tatooine_eisley_aigorn", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_aigorn_condition_epiOnTask (player, npc))
	{
		//-- NPC: Have you got the data?
		string_id message = new string_id (c_stringFile, "s_61");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No.
		boolean hasResponse0 = false;
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_64");

			utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 12);

			npcStartConversation (player, npc, "tatooine_eisley_aigorn", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_aigorn_condition_digitQuestComplete (player, npc))
	{
		//-- NPC: So. you've decided to dump that short-sighted Reimus and get with the winning team? Good choice. Do what I need, and you'll be a rich man.
		string_id message = new string_id (c_stringFile, "s_34");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Just what is it you need?
		boolean hasResponse0 = false;
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_36");

			utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 17);

			npcStartConversation (player, npc, "tatooine_eisley_aigorn", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_aigorn_condition_boneQuestComplete (player, npc))
	{
		//-- NPC: So Tori mentioned me did she? I'll have to make sure she doesn't do that again. You however, appear to be someone I can depend on.
		string_id message = new string_id (c_stringFile, "s_97");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Depend on for what?
		boolean hasResponse0 = false;
		if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_99");

			utils.setScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId", 26);

			npcStartConversation (player, npc, "tatooine_eisley_aigorn", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_aigorn_condition__defaultCondition (player, npc))
	{
		//-- NPC: You'd be a lot safer if you left me alone.
		string_id message = new string_id (c_stringFile, "s_108");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "tatooine_eisley_aigorn")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

	if (branchId == 1 && tatooine_eisley_aigorn_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && tatooine_eisley_aigorn_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && tatooine_eisley_aigorn_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && tatooine_eisley_aigorn_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && tatooine_eisley_aigorn_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && tatooine_eisley_aigorn_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && tatooine_eisley_aigorn_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && tatooine_eisley_aigorn_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && tatooine_eisley_aigorn_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && tatooine_eisley_aigorn_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && tatooine_eisley_aigorn_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && tatooine_eisley_aigorn_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && tatooine_eisley_aigorn_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && tatooine_eisley_aigorn_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && tatooine_eisley_aigorn_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && tatooine_eisley_aigorn_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && tatooine_eisley_aigorn_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && tatooine_eisley_aigorn_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && tatooine_eisley_aigorn_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && tatooine_eisley_aigorn_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.tatooine_eisley_aigorn.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

