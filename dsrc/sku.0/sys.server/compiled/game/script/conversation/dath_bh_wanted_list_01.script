// ======================================================================
//
// dath_bh_wanted_list_01.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.25 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.skill;
include library.space_quest;
include library.space_skill;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/dath_bh_wanted_list_01";

// ======================================================================
// Script Conditions
// ======================================================================

boolean dath_bh_wanted_list_01_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_remembersPlayer (obj_id player, obj_id npc)
{
	return ( utils.hasScriptVar( player, "metRsfTrainer" ));
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasCompleted_Quest_4 (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_04" ) );
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_alreadyHasAQuest (obj_id player, obj_id npc)
{
	return ( space_quest.hasQuest( player ));
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_isPilot_IMPERIAL (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_imperial_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_isPilot_REBEL (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_has_hate (obj_id player, obj_id npc)
{
	return (hasObjVar(player, "dath_bh_hate"));
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasCompleted_Quest_1 (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_01" ) );
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasCompleted_Quest_3 (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_03_c" ) );
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasCompleted_Quest_2 (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_02" ) );
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasFailed_Quest_2 (obj_id player, obj_id npc)
{
	if ( space_quest.hasFailedQuest( player, "destroy", "lok_bounty_hunter_2" ) ||
		 space_quest.hasAbortedQuest( player, "destroy", "lok_bounty_hunter_2" ) )
		 return true;
		 
	return false;	 
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasSkill_BOUNTY_HUNTER (obj_id player, obj_id npc)
{
	if (!hasSkill( player, "class_bountyhunter_phase1_novice") )
		return false;
	else	
		return true;
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasFailed_Quest_4 (obj_id player, obj_id npc)
{
	if ( space_quest.hasFailedQuest( player, "destroy", "lok_bounty_hunter_4" ) ||
		 space_quest.hasAbortedQuest( player, "destroy", "lok_bounty_hunter_4" ) )
		 return true;
		 
	return false;
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasBeenRewarded_Quest_2 (obj_id player, obj_id npc)
{
	//if the player hasn't completed this quest, then we don't want to give
	//the reward!  So return true here.
	if (!space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_02"))
		return false;
		
	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_02")) && 
				( space_quest.hasReceivedReward( player, "assassinate", "dath_bh_wanted_list_02")) );
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasBeenRewarded_Quest_4 (obj_id player, obj_id npc)
{
	return (space_quest.hasReceivedReward( player, "assassinate", "dath_bh_wanted_list_04"));
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasBeenRewarded_Quest_3 (obj_id player, obj_id npc)
{
	//if the player hasn't completed this quest, then we don't want to give
	//the reward!  So return true here.
	if (!space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_03_c"))
		return false;
		
	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_03_c")) && 
				( space_quest.hasReceivedReward( player, "assassinate", "dath_bh_wanted_list_03_c")) );
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasBeenRewarded_Quest_1 (obj_id player, obj_id npc)
{
	//if the player hasn't completed this quest, then we don't want to give
	//the reward!  So return true here.
	if (!space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_01"))
		return false;
		
	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuest( player, "assassinate", "dath_bh_wanted_list_01")) && 
				( space_quest.hasReceivedReward( player, "assassinate", "dath_bh_wanted_list_01")) );
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasFailed_Quest_3 (obj_id player, obj_id npc)
{
	if ( space_quest.hasFailedQuest( player, "destroy", "lok_bounty_hunter_3_a" ) ||
		 space_quest.hasAbortedQuest( player, "destroy", "lok_bounty_hunter_3_a" ) )
		 return true;
		 
	return false; 
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasFailed_Quest_1 (obj_id player, obj_id npc)
{
	if ( space_quest.hasFailedQuest( player, "assassinate", "dath_bh_wanted_list_01" ) ||
		 space_quest.hasAbortedQuest( player, "assassinate", "dath_bh_wanted_list_01" ) )
		 return true;
		 
	return false;	 
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_hasSkill_BOUNTY_HUNTER_isMale (obj_id player, obj_id npc)
{
	if (!hasSkill( player, "class_bountyhunter_phase1_novice") &&
		 ( getGender( player ) == GENDER_MALE ) )
		return false;
	else	
		return true;
}

// ----------------------------------------------------------------------

boolean dath_bh_wanted_list_01_condition_isGender_MALE (obj_id player, obj_id npc)
{
	return ( getGender( player ) == GENDER_MALE );
}

// ======================================================================
// Script Actions
// ======================================================================

void dath_bh_wanted_list_01_action_rememberPlayer (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "metRsfTrainer", true );
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_set_hate (obj_id player, obj_id npc)
{
	setObjVar (player, "dath_bh_hate", true);
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveQuest_Revenge_1 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy_surpriseattack", "dath_bh_revenge_01" );
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveReward_Quest_2 (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "assassinate", "dath_bh_wanted_list_02" ))
		space_quest.giveReward( player, "assassinate", "dath_bh_wanted_list_02", 0);
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveReward_Quest_4 (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "assassinate", "dath_bh_wanted_list_04" ))
		space_quest.giveReward( player, "assassinate", "dath_bh_wanted_list_04", 1000 );
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveReward_Quest_3 (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "assassinate", "dath_bh_wanted_list_03_c" ))
		space_quest.giveReward( player, "assassinate", "dath_bh_wanted_list_03_c", 0 );
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveReward_Quest_1 (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "assassinate", "dath_bh_wanted_list_01"))
		space_quest.giveReward( player, "assassinate", "dath_bh_wanted_list_01", 200 );
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveQuest_Quest_1 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "assassinate", "dath_bh_wanted_list_01" );
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveQuest_Quest_2 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "assassinate", "dath_bh_wanted_list_02" );
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveQuest_Quest_3 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "assassinate", "dath_bh_wanted_list_03_a" );
}

// ----------------------------------------------------------------------

void dath_bh_wanted_list_01_action_giveQuest_Quest_4 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "assassinate", "dath_bh_wanted_list_04" );
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int dath_bh_wanted_list_01_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What do you want?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing. I was just leaving.
	if (response == "s_6c03db3")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good. Keep leaving...
			string_id message = new string_id (c_stringFile, "s_c0917a4d");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What are you still doing around here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was just leaving.
	if (response == "s_d424c740")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good. Stay gone, Bounty Hunter!
			string_id message = new string_id (c_stringFile, "s_9bd0f533");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's it to you?
	if (response == "s_ff607592")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			dath_bh_wanted_list_01_action_giveQuest_Revenge_1 (player, npc);

			//-- NPC: It's not wise for you to linger here in Dathomir system, my friend.
			string_id message = new string_id (c_stringFile, "s_9caba7d5");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good work, %NU. You were good for business. Here's an extra thousand to get you on your way.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Goodbye.
	if (response == "s_e6772f13")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Yeah, yeah...
			string_id message = new string_id (c_stringFile, "s_7e1ada01");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So. Tell me...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Bwar Brothers are history.
	if (response == "s_6bef416f")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: That's good news, I guess. You know, word is getting around that you are responsible for all of my recent success... and it's starting to make me look bad!
			string_id message = new string_id (c_stringFile, "s_5e49253");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nobody needs to know the truth.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_53fb1d89");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's good news, I guess. You know, word is getting around that you are responsible for all of my recent success... and it's starting to make me look bad!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nobody needs to know the truth.
	if (response == "s_53fb1d89")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Too late, %NU. The damage is done. Now, I want you out of here. It's only fair that I offer you one final mission, with good payout... before you go away for good. Do you think we can make a deal?
			string_id message = new string_id (c_stringFile, "s_a351fef0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it.
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52917b0d");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ca932c28");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 13);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Too late, %NU. The damage is done. Now, I want you out of here. It's only fair that I offer you one final mission, with good payout... before you go away for good. Do you think we can make a deal?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course.
	if (response == "s_52917b0d")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: And you promise to leave once you've eliminated the target and gotten your reward?
			string_id message = new string_id (c_stringFile, "s_ba82c312");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No.
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d70dba34");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4c695dbd");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Forget it.
	if (response == "s_ca932c28")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			dath_bh_wanted_list_01_action_set_hate (player, npc);

			//-- NPC: Then we have nothing more to say to each other. You're dead to me, %NU. Dead. So vanish... like dead people vanish...
			string_id message = new string_id (c_stringFile, "s_f1bd52fe");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, pp);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And you promise to leave once you've eliminated the target and gotten your reward?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_d70dba34")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Good. I like parting friends. It's much simpler, you know?
			string_id message = new string_id (c_stringFile, "s_94e8b49c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92ccbd55");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No.
	if (response == "s_4c695dbd")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			dath_bh_wanted_list_01_action_set_hate (player, npc);

			//-- NPC: Then get out of here. I don't ever want to see your face again!
			string_id message = new string_id (c_stringFile, "s_14e3a046");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good. I like parting friends. It's much simpler, you know?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Get on with it.
	if (response == "s_92ccbd55")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			dath_bh_wanted_list_01_action_giveQuest_Quest_4 (player, npc);

			//-- NPC: Your final target is Juree La'Zar, who was once a beautiful and gifted bounty hunter... driven insane after being horribly disfigured while captive to the Black Sun.
			string_id message = new string_id (c_stringFile, "s_2e64927c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: A hunter like me?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What did she do wrong?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_37778692");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fe02e665");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your final target is Juree La'Zar, who was once a beautiful and gifted bounty hunter... driven insane after being horribly disfigured while captive to the Black Sun.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A hunter like me?
	if (response == "s_37778692")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: No, not like you. Juree La'Zar is a killer of innocents. A savage. Not like you. Certainly not like me, either!
			string_id message = new string_id (c_stringFile, "s_1e2e55e8");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What did she do wrong?
	if (response == "s_fe02e665")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: She and her gang crippled an Imperial bacta freighter on the edge of the Dathomir system. Left the crew alive, made off with some components. After the rescue team got the freighter to the Imperial base and unloaded the bacta... they found that Juree's gang had poisoned the whole supply. Killed hundreds.
			string_id message = new string_id (c_stringFile, "s_34efde77");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way.
	if (response == "s_e42b260e")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Before you leave for good... come back by. I'll have something extra for you.
			string_id message = new string_id (c_stringFile, "s_2cafb575");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Still alive, huh?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Rae'Lux was no match for me.
	if (response == "s_a019416c")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I don't think there's many lowlifes out there who could take you one-on-one, %NU. Maybe you'd find three villains more challenging?
			string_id message = new string_id (c_stringFile, "s_3552cf48");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me more.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_159911f5");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 23);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't think there's many lowlifes out there who could take you one-on-one, %NU. Maybe you'd find three villains more challenging?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more.
	if (response == "s_159911f5")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Perhaps you have heard of the Bwar Brothers from Tatooine? An infamous trio of kidnappers who terrorized the Outer Rim for years before Jabba's henchmen ran them off. Guess they were bad for business...
			string_id message = new string_id (c_stringFile, "s_b5a05d56");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Pay 500 credits)
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47efee53");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Perhaps you have heard of the Bwar Brothers from Tatooine? An infamous trio of kidnappers who terrorized the Outer Rim for years before Jabba's henchmen ran them off. Guess they were bad for business...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Pay 500 credits)
	if (response == "s_47efee53")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			dath_bh_wanted_list_01_action_giveQuest_Quest_3 (player, npc);

			//-- NPC: Well, my friend... you'll find all three of them hiding here in the Dathomir system under the watchful eyes of Elite Mercenaries. They've spared no expense!
			string_id message = new string_id (c_stringFile, "s_1508f5fc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Elite Mercenaries?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm on my way!
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_21ef58ea");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dc649bb3");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, my friend... you'll find all three of them hiding here in the Dathomir system under the watchful eyes of Elite Mercenaries. They've spared no expense!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Elite Mercenaries?
	if (response == "s_21ef58ea")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm sure they won't be any threat to you, %NU.
			string_id message = new string_id (c_stringFile, "s_785a9a91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Elite Mercenaries?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm on my way!
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_21ef58ea");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dc649bb3");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 25);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way!
	if (response == "s_dc649bb3")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good luck!
			string_id message = new string_id (c_stringFile, "s_a80d9308");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm sure they won't be any threat to you, %NU.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Elite Mercenaries?
	if (response == "s_21ef58ea")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm sure they won't be any threat to you, %NU.
			string_id message = new string_id (c_stringFile, "s_785a9a91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Elite Mercenaries?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm on my way!
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_21ef58ea");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dc649bb3");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 25);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way!
	if (response == "s_dc649bb3")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good luck!
			string_id message = new string_id (c_stringFile, "s_a80d9308");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So you made it, huh?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That last one was too easy.
	if (response == "s_1287b3")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: So tell me then... what do you know about the 'Avenger of Alderaan?'
			string_id message = new string_id (c_stringFile, "s_412a3b40");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nothing.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bd02793");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So tell me then... what do you know about the 'Avenger of Alderaan?'

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing.
	if (response == "s_6bd02793")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Well this 'Avenger' managed to work his way onto the Empire's most wanted list. They'll pay handsomely if you hunt him down... enough to make you forget about having to pay my 500 credit fee. What do you say?
			string_id message = new string_id (c_stringFile, "s_90ae19e2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Pay the 500 credits)
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f8a99129");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well this 'Avenger' managed to work his way onto the Empire's most wanted list. They'll pay handsomely if you hunt him down... enough to make you forget about having to pay my 500 credit fee. What do you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Pay the 500 credits)
	if (response == "s_f8a99129")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			dath_bh_wanted_list_01_action_giveQuest_Quest_2 (player, npc);

			//-- NPC: So you're looking for a pilot calling himself Rae'Lux... convicted of detonating over a dozen civilian transports in the Naboo system. Thinks he can find refuge here on Dathomir. You'll show him otherwise, eh?
			string_id message = new string_id (c_stringFile, "s_fc70ad30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: See you later.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where will I find him?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_782c3536");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a0277a4c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44ca831b");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So you're looking for a pilot calling himself Rae'Lux... convicted of detonating over a dozen civilian transports in the Naboo system. Thinks he can find refuge here on Dathomir. You'll show him otherwise, eh?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: See you later.
	if (response == "s_782c3536")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good luck, Bounty Hunter.
			string_id message = new string_id (c_stringFile, "s_fafc5a4e");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I know him?
	if (response == "s_a0277a4c")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Rae'Lux flies a modified Y-wing that he stole from a small Rebel base in the Outer Rim. He's rumored to use experimental ion-sheet armor. Supposed to be super lightweight, but still really tough!
			string_id message = new string_id (c_stringFile, "s_2ad07084");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: See you later.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where will I find him?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_782c3536");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a0277a4c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44ca831b");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where will I find him?
	if (response == "s_44ca831b")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Word on the street... Rae'Lux likes to hide in the Witchblood nebula. They tell me he leaps out of the clouds every now and again to rob deep space transports that are flying solo.
			string_id message = new string_id (c_stringFile, "s_af58f306");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: See you later.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where will I find him?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_782c3536");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a0277a4c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44ca831b");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Rae'Lux flies a modified Y-wing that he stole from a small Rebel base in the Outer Rim. He's rumored to use experimental ion-sheet armor. Supposed to be super lightweight, but still really tough!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: See you later.
	if (response == "s_782c3536")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good luck, Bounty Hunter.
			string_id message = new string_id (c_stringFile, "s_fafc5a4e");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I know him?
	if (response == "s_a0277a4c")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Rae'Lux flies a modified Y-wing that he stole from a small Rebel base in the Outer Rim. He's rumored to use experimental ion-sheet armor. Supposed to be super lightweight, but still really tough!
			string_id message = new string_id (c_stringFile, "s_2ad07084");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: See you later.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where will I find him?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_782c3536");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a0277a4c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44ca831b");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where will I find him?
	if (response == "s_44ca831b")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Word on the street... Rae'Lux likes to hide in the Witchblood nebula. They tell me he leaps out of the clouds every now and again to rob deep space transports that are flying solo.
			string_id message = new string_id (c_stringFile, "s_af58f306");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: See you later.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where will I find him?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_782c3536");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a0277a4c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44ca831b");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Word on the street... Rae'Lux likes to hide in the Witchblood nebula. They tell me he leaps out of the clouds every now and again to rob deep space transports that are flying solo.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: See you later.
	if (response == "s_782c3536")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good luck, Bounty Hunter.
			string_id message = new string_id (c_stringFile, "s_fafc5a4e");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I know him?
	if (response == "s_a0277a4c")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Rae'Lux flies a modified Y-wing that he stole from a small Rebel base in the Outer Rim. He's rumored to use experimental ion-sheet armor. Supposed to be super lightweight, but still really tough!
			string_id message = new string_id (c_stringFile, "s_2ad07084");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: See you later.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where will I find him?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_782c3536");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a0277a4c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44ca831b");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where will I find him?
	if (response == "s_44ca831b")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Word on the street... Rae'Lux likes to hide in the Witchblood nebula. They tell me he leaps out of the clouds every now and again to rob deep space transports that are flying solo.
			string_id message = new string_id (c_stringFile, "s_af58f306");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: See you later.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where will I find him?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_782c3536");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a0277a4c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44ca831b");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well if it isn't the famous %TU... bounty hunter extraordinaire! What can I do for you today?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am looking for work.
	if (response == "s_a560e84c")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I think we can arrange something. There are three or four fresh bounties on the Imperial 'net. For the right price, I can give you exclusive rights to the contracts. Are you interested?
			string_id message = new string_id (c_stringFile, "s_a6425c9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How much?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a71ed59");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I think we can arrange something. There are three or four fresh bounties on the Imperial 'net. For the right price, I can give you exclusive rights to the contracts. Are you interested?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How much?
	if (response == "s_1a71ed59")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: We can get started for the low, low price of 500 credits.
			string_id message = new string_id (c_stringFile, "s_a0102a9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, I'm interested.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What does 500 credits get me?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What's the reward?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_315f7d8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edddcc46");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d1a93a0e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We can get started for the low, low price of 500 credits.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'm interested.
	if (response == "s_315f7d8")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Good... then there's just the small matter of payment...
			string_id message = new string_id (c_stringFile, "s_4f7b0318");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Pay the 500 credits)
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f8a99129");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What does 500 credits get me?
	if (response == "s_edddcc46")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I give you all the info you'll need to track down and vaporize this degenerate killer. What do you say?
			string_id message = new string_id (c_stringFile, "s_958f6d7d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, I'm interested.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What does 500 credits get me?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What's the reward?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_315f7d8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edddcc46");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d1a93a0e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the reward?
	if (response == "s_d1a93a0e")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Empire pays 1000 credits... and I throw in a bonus of 200 if you do it quickly. What do you think about that?
			string_id message = new string_id (c_stringFile, "s_234c808f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, I'm interested.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What does 500 credits get me?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What's the reward?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_315f7d8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edddcc46");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d1a93a0e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good... then there's just the small matter of payment...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Pay the 500 credits)
	if (response == "s_f8a99129")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			dath_bh_wanted_list_01_action_giveQuest_Quest_1 (player, npc);

			//-- NPC: So, your target is a vile killer named Gutcher Gorai. He was convicted of twenty counts before they finally caught him on Coruscant. Too bad for the Empire he got away. Good for you though, huh?
			string_id message = new string_id (c_stringFile, "s_ecaf7e94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is he now?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I recognize him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_265d5a02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3aeefc74");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So, your target is a vile killer named Gutcher Gorai. He was convicted of twenty counts before they finally caught him on Coruscant. Too bad for the Empire he got away. Good for you though, huh?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is he now?
	if (response == "s_265d5a02")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: He's been lurking around the Dathomir system for a while now. The Empire thinks he may have acquired hired some low-rent mercenaries for protection.
			string_id message = new string_id (c_stringFile, "s_4fb7a65d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is he now?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I recognize him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_265d5a02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3aeefc74");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I recognize him?
	if (response == "s_3aeefc74")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Gutcher Gorai favors a Hutt-style chassis that has been heavily modified with illegal equipment. Some say his weapon system is overcharged with some sort of distressed codellium capacitor.
			string_id message = new string_id (c_stringFile, "s_f0b711b4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is he now?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I recognize him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_265d5a02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3aeefc74");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way.
	if (response == "s_e42b260e")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good luck, Bounty Hunter.
			string_id message = new string_id (c_stringFile, "s_fafc5a4e");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch40 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He's been lurking around the Dathomir system for a while now. The Empire thinks he may have acquired hired some low-rent mercenaries for protection.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is he now?
	if (response == "s_265d5a02")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: He's been lurking around the Dathomir system for a while now. The Empire thinks he may have acquired hired some low-rent mercenaries for protection.
			string_id message = new string_id (c_stringFile, "s_4fb7a65d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is he now?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I recognize him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_265d5a02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3aeefc74");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I recognize him?
	if (response == "s_3aeefc74")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Gutcher Gorai favors a Hutt-style chassis that has been heavily modified with illegal equipment. Some say his weapon system is overcharged with some sort of distressed codellium capacitor.
			string_id message = new string_id (c_stringFile, "s_f0b711b4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is he now?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I recognize him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_265d5a02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3aeefc74");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way.
	if (response == "s_e42b260e")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good luck, Bounty Hunter.
			string_id message = new string_id (c_stringFile, "s_fafc5a4e");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Gutcher Gorai favors a Hutt-style chassis that has been heavily modified with illegal equipment. Some say his weapon system is overcharged with some sort of distressed codellium capacitor.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is he now?
	if (response == "s_265d5a02")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: He's been lurking around the Dathomir system for a while now. The Empire thinks he may have acquired hired some low-rent mercenaries for protection.
			string_id message = new string_id (c_stringFile, "s_4fb7a65d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is he now?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I recognize him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_265d5a02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3aeefc74");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I recognize him?
	if (response == "s_3aeefc74")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Gutcher Gorai favors a Hutt-style chassis that has been heavily modified with illegal equipment. Some say his weapon system is overcharged with some sort of distressed codellium capacitor.
			string_id message = new string_id (c_stringFile, "s_f0b711b4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is he now?
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I recognize him?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_265d5a02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3aeefc74");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way.
	if (response == "s_e42b260e")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good luck, Bounty Hunter.
			string_id message = new string_id (c_stringFile, "s_fafc5a4e");
			removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I give you all the info you'll need to track down and vaporize this degenerate killer. What do you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'm interested.
	if (response == "s_315f7d8")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Good... then there's just the small matter of payment...
			string_id message = new string_id (c_stringFile, "s_4f7b0318");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Pay the 500 credits)
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f8a99129");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What does 500 credits get me?
	if (response == "s_edddcc46")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I give you all the info you'll need to track down and vaporize this degenerate killer. What do you say?
			string_id message = new string_id (c_stringFile, "s_958f6d7d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, I'm interested.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What does 500 credits get me?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What's the reward?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_315f7d8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edddcc46");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d1a93a0e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the reward?
	if (response == "s_d1a93a0e")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Empire pays 1000 credits... and I throw in a bonus of 200 if you do it quickly. What do you think about that?
			string_id message = new string_id (c_stringFile, "s_234c808f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, I'm interested.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What does 500 credits get me?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What's the reward?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_315f7d8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edddcc46");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d1a93a0e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int dath_bh_wanted_list_01_handleBranch44 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Empire pays 1000 credits... and I throw in a bonus of 200 if you do it quickly. What do you think about that?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'm interested.
	if (response == "s_315f7d8")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Good... then there's just the small matter of payment...
			string_id message = new string_id (c_stringFile, "s_4f7b0318");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Pay the 500 credits)
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f8a99129");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What does 500 credits get me?
	if (response == "s_edddcc46")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I give you all the info you'll need to track down and vaporize this degenerate killer. What do you say?
			string_id message = new string_id (c_stringFile, "s_958f6d7d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, I'm interested.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What does 500 credits get me?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What's the reward?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_315f7d8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edddcc46");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d1a93a0e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the reward?
	if (response == "s_d1a93a0e")
	{
		//-- [NOTE] 
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Empire pays 1000 credits... and I throw in a bonus of 200 if you do it quickly. What do you think about that?
			string_id message = new string_id (c_stringFile, "s_234c808f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, I'm interested.
			boolean hasResponse0 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What does 500 credits get me?
			boolean hasResponse1 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What's the reward?
			boolean hasResponse2 = false;
			if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_315f7d8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edddcc46");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d1a93a0e");

				setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.dath_bh_wanted_list_01");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	
	faceTo( self, player );

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.dath_bh_wanted_list_01");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_has_hate (player, npc))
	{
		doAnimationAction (npc, "point_accusingly");

		//-- NPC: You betrayed me! Now get out of here, %NU!
		string_id message = new string_id (c_stringFile, "s_3a71c840");
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_alreadyHasAQuest (player, npc))
	{
		doAnimationAction (npc, "shrug_shoulders");

		//-- NPC: What do you want?
		string_id message = new string_id (c_stringFile, "s_6a1562ad");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Nothing. I was just leaving.
		boolean hasResponse0 = false;
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6c03db3");

			setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 2);

			npcStartConversation (player, npc, "dath_bh_wanted_list_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_hasFailed_Quest_2 (player, npc))
	{
		doAnimationAction (npc, "shake_head_disgust");

		dath_bh_wanted_list_01_action_giveQuest_Quest_2 (player, npc);

		//-- NPC: You failed that mission? Terrible! Here it is again... for a price.
		string_id message = new string_id (c_stringFile, "s_43d9d825");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_hasFailed_Quest_1 (player, npc))
	{
		doAnimationAction (npc, "shake_head_disgust");

		dath_bh_wanted_list_01_action_giveQuest_Quest_1 (player, npc);

		//-- NPC: You have failed the mission? Here it is again... for a price.
		string_id message = new string_id (c_stringFile, "s_9c098d52");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_hasBeenRewarded_Quest_4 (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: What are you still doing around here?
		string_id message = new string_id (c_stringFile, "s_89b2deb1");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I was just leaving.
		boolean hasResponse0 = false;
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What's it to you?
		boolean hasResponse1 = false;
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d424c740");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ff607592");

			setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 6);

			npcStartConversation (player, npc, "dath_bh_wanted_list_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_hasCompleted_Quest_4 (player, npc))
	{
		doAnimationAction (npc, "snap_finger2");

		dath_bh_wanted_list_01_action_giveReward_Quest_4 (player, npc);

		//-- NPC: Good work, %NU. You were good for business. Here's an extra thousand to get you on your way.
		string_id message = new string_id (c_stringFile, "s_a6519855");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Goodbye.
		boolean hasResponse0 = false;
		if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e6772f13");

			setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 9);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "dath_bh_wanted_list_01", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_hasCompleted_Quest_3 (player, npc))
	{
		doAnimationAction (npc, "beckon");

		//-- NPC: So. Tell me...
		string_id message = new string_id (c_stringFile, "s_a366b511");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: The Bwar Brothers are history.
		boolean hasResponse0 = false;
		if (!dath_bh_wanted_list_01_condition_hasCompleted_Quest_4 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6bef416f");

			setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 11);

			npcStartConversation (player, npc, "dath_bh_wanted_list_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_hasCompleted_Quest_2 (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: Still alive, huh?
		string_id message = new string_id (c_stringFile, "s_fb42a64c");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Rae'Lux was no match for me.
		boolean hasResponse0 = false;
		if (!dath_bh_wanted_list_01_condition_hasCompleted_Quest_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a019416c");

			setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 22);

			npcStartConversation (player, npc, "dath_bh_wanted_list_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_hasCompleted_Quest_1 (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: So you made it, huh?
		string_id message = new string_id (c_stringFile, "s_aa66418e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: That last one was too easy.
		boolean hasResponse0 = false;
		if (!dath_bh_wanted_list_01_condition_hasCompleted_Quest_2 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1287b3");

			setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 28);

			npcStartConversation (player, npc, "dath_bh_wanted_list_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition_hasSkill_BOUNTY_HUNTER (player, npc))
	{
		doAnimationAction (npc, "snap_finger2");

		//-- NPC: Well if it isn't the famous %TU... bounty hunter extraordinaire! What can I do for you today?
		string_id message = new string_id (c_stringFile, "s_42a7f468");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I am looking for work.
		boolean hasResponse0 = false;
		if (!dath_bh_wanted_list_01_condition_hasCompleted_Quest_1 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a560e84c");

			setObjVar (player, "conversation.dath_bh_wanted_list_01.branchId", 35);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "dath_bh_wanted_list_01", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (dath_bh_wanted_list_01_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "wave_on_dismissing");

		//-- NPC: Bah, leave me be!
		string_id message = new string_id (c_stringFile, "s_d030521");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "dath_bh_wanted_list_01")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = getIntObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

	if (branchId == 2 && dath_bh_wanted_list_01_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && dath_bh_wanted_list_01_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && dath_bh_wanted_list_01_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && dath_bh_wanted_list_01_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && dath_bh_wanted_list_01_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && dath_bh_wanted_list_01_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && dath_bh_wanted_list_01_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && dath_bh_wanted_list_01_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && dath_bh_wanted_list_01_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && dath_bh_wanted_list_01_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && dath_bh_wanted_list_01_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && dath_bh_wanted_list_01_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && dath_bh_wanted_list_01_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && dath_bh_wanted_list_01_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && dath_bh_wanted_list_01_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && dath_bh_wanted_list_01_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && dath_bh_wanted_list_01_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && dath_bh_wanted_list_01_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && dath_bh_wanted_list_01_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && dath_bh_wanted_list_01_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && dath_bh_wanted_list_01_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && dath_bh_wanted_list_01_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && dath_bh_wanted_list_01_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && dath_bh_wanted_list_01_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && dath_bh_wanted_list_01_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 40 && dath_bh_wanted_list_01_handleBranch40 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && dath_bh_wanted_list_01_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && dath_bh_wanted_list_01_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 44 && dath_bh_wanted_list_01_handleBranch44 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	removeObjVar (player, "conversation.dath_bh_wanted_list_01.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

