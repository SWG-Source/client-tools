// ======================================================================
//
// privateer_broker_retired_pilot.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.25 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.features;
include library.skill;
include library.space_quest;
include library.space_skill;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/privateer_broker_retired_pilot";

// ======================================================================
// Script Conditions
// ======================================================================

boolean privateer_broker_retired_pilot_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean privateer_broker_retired_pilot_condition_isImperialPilot (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_imperial_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean privateer_broker_retired_pilot_condition_isRebelPilot (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean privateer_broker_retired_pilot_condition_isPrivateerPilot (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_neutral_novice" );
}

// ======================================================================
// Script Actions
// ======================================================================

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int privateer_broker_retired_pilot_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you looking for the Free Pilots Guild? I have what you might describe as a 'good news, bad news' situation.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm here to see Gil Burtin.
	if (response == "s_c88254be")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: He's in the lounge, playing bartender for this little party. Between us, I think he's been drowning his sorrows a bit between pilot interviews. Don't let that stop you, though. He was the founder of the Free Pilots Guild, after all. I can only imagine how this has affected him.
			string_id message = new string_id (c_stringFile, "s_7d00c660");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks!
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Can I get a drink?
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4682ea6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8f67e0e7");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the bad news?
	if (response == "s_3069ecae")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: There is no Free Pilots Guild any more. Bad enough for you?
			string_id message = new string_id (c_stringFile, "s_61129613");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why not?
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are you doing here then?
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7f9b8387");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6e54c48b");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the good news?
	if (response == "s_5e303134")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: The good news... the good news... let me see. Now that I think about it, I lied. This is a 'bad news' situation. You a pilot?
			string_id message = new string_id (c_stringFile, "s_53d912d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not yet.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I fly for the Alliance.
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition_isRebelPilot (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I fly for the Empire.
			boolean hasResponse2 = false;
			if (privateer_broker_retired_pilot_condition_isImperialPilot (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am a free pilot, myself.
			boolean hasResponse3 = false;
			if (privateer_broker_retired_pilot_condition_isPrivateerPilot (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b5f15b19");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_252fcfaf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bccf4e9");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cc070e7b");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He's in the lounge, playing bartender for this little party. Between us, I think he's been drowning his sorrows a bit between pilot interviews. Don't let that stop you, though. He was the founder of the Free Pilots Guild, after all. I can only imagine how this has affected him.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks!
	if (response == "s_c4682ea6")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good-bye!
			string_id message = new string_id (c_stringFile, "s_fa94d5c5");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can I get a drink?
	if (response == "s_8f67e0e7")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't know. Ask Gil.
			string_id message = new string_id (c_stringFile, "s_6579c571");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There is no Free Pilots Guild any more. Bad enough for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why not?
	if (response == "s_7f9b8387")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Shut down by order of the Empire! I guess they don't want anybody fooling around in space unless they fly a TIE fighter for the Imperial Navy!
			string_id message = new string_id (c_stringFile, "s_b3aefb70");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That makes sense.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't want to fly a TIE fighter.
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ef85b67c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c8d0555c");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are you doing here then?
	if (response == "s_6e54c48b")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Well, I'm... Hmm. Let me think...
			string_id message = new string_id (c_stringFile, "s_c6667cfa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me where I can find Gil.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You don't know?
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49c76620");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77b660fd");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Shut down by order of the Empire! I guess they don't want anybody fooling around in space unless they fly a TIE fighter for the Imperial Navy!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That makes sense.
	if (response == "s_ef85b67c")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: Oh dear. How can so many people be so wrong...?
			string_id message = new string_id (c_stringFile, "s_89553663");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't want to fly a TIE fighter.
	if (response == "s_c8d0555c")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then I think you should talk to Gil. He can direct you to CorSec or the Royal Security Forces. You won't have to fly a TIE fighter, I can assure you!
			string_id message = new string_id (c_stringFile, "s_8d4beebb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Gil?
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks!
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23bbba18");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4682ea6");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then I think you should talk to Gil. He can direct you to CorSec or the Royal Security Forces. You won't have to fly a TIE fighter, I can assure you!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Gil?
	if (response == "s_23bbba18")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: He's in the lounge, playing bartender for this little party. Between us, I think he's been drowning his sorrows a bit between pilot interviews. Don't let that stop you, though. He was the founder of the Free Pilots Guild, after all. I can only imagine how this has affected him.
			string_id message = new string_id (c_stringFile, "s_7d00c660");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks!
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Can I get a drink?
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4682ea6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8f67e0e7");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks!
	if (response == "s_c4682ea6")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good-bye!
			string_id message = new string_id (c_stringFile, "s_fa94d5c5");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He's in the lounge, playing bartender for this little party. Between us, I think he's been drowning his sorrows a bit between pilot interviews. Don't let that stop you, though. He was the founder of the Free Pilots Guild, after all. I can only imagine how this has affected him.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks!
	if (response == "s_c4682ea6")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good-bye!
			string_id message = new string_id (c_stringFile, "s_fa94d5c5");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can I get a drink?
	if (response == "s_8f67e0e7")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't know. Ask Gil.
			string_id message = new string_id (c_stringFile, "s_6579c571");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, I'm... Hmm. Let me think...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me where I can find Gil.
	if (response == "s_49c76620")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: He's in the lounge, playing bartender for this little party. Between us, I think he's been drowning his sorrows a bit between pilot interviews. Don't let that stop you, though. He was the founder of the Free Pilots Guild, after all. I can only imagine how this has affected him.
			string_id message = new string_id (c_stringFile, "s_7d00c660");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks!
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Can I get a drink?
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4682ea6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8f67e0e7");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You don't know?
	if (response == "s_77b660fd")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: There's really nothing left for me now. I haven't had any time to figure out my next move. I'm thinking that I'm going to need to pick up a job with the shuttle services... and that's going to be horrible!
			string_id message = new string_id (c_stringFile, "s_e288626b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Good luck, then.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Why so horrible?
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2e674765");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a596ff6");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He's in the lounge, playing bartender for this little party. Between us, I think he's been drowning his sorrows a bit between pilot interviews. Don't let that stop you, though. He was the founder of the Free Pilots Guild, after all. I can only imagine how this has affected him.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks!
	if (response == "s_c4682ea6")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good-bye!
			string_id message = new string_id (c_stringFile, "s_fa94d5c5");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can I get a drink?
	if (response == "s_8f67e0e7")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm not the bartender. Go ask Gil.
			string_id message = new string_id (c_stringFile, "s_ea090abe");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There's really nothing left for me now. I haven't had any time to figure out my next move. I'm thinking that I'm going to need to pick up a job with the shuttle services... and that's going to be horrible!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Good luck, then.
	if (response == "s_2e674765")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Thanks.
			string_id message = new string_id (c_stringFile, "s_fc27931b");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why so horrible?
	if (response == "s_8a596ff6")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't think you appreciate the sort of safety violations that are running rampant through the transportation service!
			string_id message = new string_id (c_stringFile, "s_877a4502");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I don't know what you mean.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Safety violations? By whom?
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_54818bbc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d495751");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't think you appreciate the sort of safety violations that are running rampant through the transportation service!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what you mean.
	if (response == "s_54818bbc")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: In the old days, a transport pilot was never allowed to run more than two shifts back-to-back. Now, the Empire's oversight plan has pilots running as many as they can handle. A Zabrak buddy of mine logged more than a dozen shifts in a row just last week!
			string_id message = new string_id (c_stringFile, "s_ab39b18c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds like good money.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That sounds harsh!
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_811a5ea0");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f4760c16");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Safety violations? By whom?
	if (response == "s_2d495751")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Empire has installed military jurisdiction over the galactic transit services. It's a special oversight group that sets flight schedules, repair budgets, and so forth. It's a nightmare!
			string_id message = new string_id (c_stringFile, "s_b0cc84d1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Well, good luck!
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How is this a nightmare?
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fbdd44a6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82e40fb4");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: In the old days, a transport pilot was never allowed to run more than two shifts back-to-back. Now, the Empire's oversight plan has pilots running as many as they can handle. A Zabrak buddy of mine logged more than a dozen shifts in a row just last week!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like good money.
	if (response == "s_811a5ea0")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Right... except for the fact that the Empire is also taking a substantial chunk of credits out of your pay to support their blasted war effort!
			string_id message = new string_id (c_stringFile, "s_26256229");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It's our civic duty.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's not fair!
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1bc3ac43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31097eef");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sounds harsh!
	if (response == "s_f4760c16")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: And that's not all! The equipment is suffering just as much. Most transports are supposed to receive regular maintenance four times each cycle... but I know of a few here in Bestine that haven't seen maintenance for over nine months!
			string_id message = new string_id (c_stringFile, "s_1fbaff94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That's terrible.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10b13d41");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Right... except for the fact that the Empire is also taking a substantial chunk of credits out of your pay to support their blasted war effort!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's our civic duty.
	if (response == "s_1bc3ac43")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Wow. You sound rich, my friend. Stay that way.
			string_id message = new string_id (c_stringFile, "s_426a2f78");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's not fair!
	if (response == "s_31097eef")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: No kidding! But, what can one person do about it? I'll tell you: nothing.
			string_id message = new string_id (c_stringFile, "s_2d1ee89c");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And that's not all! The equipment is suffering just as much. Most transports are supposed to receive regular maintenance four times each cycle... but I know of a few here in Bestine that haven't seen maintenance for over nine months!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's terrible.
	if (response == "s_10b13d41")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I know, but what can you do?
			string_id message = new string_id (c_stringFile, "s_2174cfc4");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Empire has installed military jurisdiction over the galactic transit services. It's a special oversight group that sets flight schedules, repair budgets, and so forth. It's a nightmare!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see. Well, good luck!
	if (response == "s_fbdd44a6")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: You too.
			string_id message = new string_id (c_stringFile, "s_39510e77");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How is this a nightmare?
	if (response == "s_82e40fb4")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: In the old days, a transport pilot was never allowed to run more than two shifts back-to-back. Now, the Empire's oversight plan has pilots running as many as they can handle. A Zabrak buddy of mine logged more than a dozen shifts in a row just last week!
			string_id message = new string_id (c_stringFile, "s_ab39b18c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds like good money.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That sounds harsh!
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_811a5ea0");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f4760c16");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: In the old days, a transport pilot was never allowed to run more than two shifts back-to-back. Now, the Empire's oversight plan has pilots running as many as they can handle. A Zabrak buddy of mine logged more than a dozen shifts in a row just last week!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like good money.
	if (response == "s_811a5ea0")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Right... except for the fact that the Empire is also taking a substantial chunk of credits out of your pay to support their blasted war effort!
			string_id message = new string_id (c_stringFile, "s_26256229");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It's our civic duty.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's not fair!
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1bc3ac43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31097eef");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sounds harsh!
	if (response == "s_f4760c16")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: And that's not all! The equipment is suffering just as much. Most transports are supposed to receive regular maintenance four times each cycle... but I know of a few here in Bestine that haven't seen maintenance for over nine months!
			string_id message = new string_id (c_stringFile, "s_1fbaff94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That's awful.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b93cdbd8");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Right... except for the fact that the Empire is also taking a substantial chunk of credits out of your pay to support their blasted war effort!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's our civic duty.
	if (response == "s_1bc3ac43")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Wow. You sound rich, my friend. Stay that way.
			string_id message = new string_id (c_stringFile, "s_426a2f78");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's not fair!
	if (response == "s_31097eef")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: No kidding! But, what can one person do about it? I'll tell you: nothing.
			string_id message = new string_id (c_stringFile, "s_2d1ee89c");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And that's not all! The equipment is suffering just as much. Most transports are supposed to receive regular maintenance four times each cycle... but I know of a few here in Bestine that haven't seen maintenance for over nine months!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's awful.
	if (response == "s_b93cdbd8")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I agree... but what can anyone do?
			string_id message = new string_id (c_stringFile, "s_7c525a54");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The good news... the good news... let me see. Now that I think about it, I lied. This is a 'bad news' situation. You a pilot?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet.
	if (response == "s_b5f15b19")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then you'll probably want to see Gil. He's in the lounge, playing bartender.
			string_id message = new string_id (c_stringFile, "s_9f74e2fc");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I fly for the Alliance.
	if (response == "s_252fcfaf")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nervous");

			//-- NPC: You realize you are in Bestine, right? Do you know where to find the starport so you can get out of here... right away?!
			string_id message = new string_id (c_stringFile, "s_d31921cd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks for the warning.
			boolean hasResponse0 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm not afraid!
			boolean hasResponse1 = false;
			if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ccec1c2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4e888f16");

				setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I fly for the Empire.
	if (response == "s_6bccf4e9")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: Oh. Good. Wonderful! There's no heartbreaking shame in that, now is there? No filthy guilt eating your brain away, right?
			string_id message = new string_id (c_stringFile, "s_5ad17cbf");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am a free pilot, myself.
	if (response == "s_cc070e7b")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Then I don't know what you're doing here. Gil only refers pilots to CorSec and RSF. There's no work to be found here on Tatooine anymore, my friend. None whatsoever...
			string_id message = new string_id (c_stringFile, "s_b5ff56cf");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int privateer_broker_retired_pilot_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You realize you are in Bestine, right? Do you know where to find the starport so you can get out of here... right away?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks for the warning.
	if (response == "s_2ccec1c2")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "greet");

			//-- NPC: I never saw you...
			string_id message = new string_id (c_stringFile, "s_3e25fcee");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not afraid!
	if (response == "s_4e888f16")
	{
		//-- [NOTE] 
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Relax. I'm not saying you were.
			string_id message = new string_id (c_stringFile, "s_ecfd8a1");
			removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.privateer_broker_retired_pilot");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.privateer_broker_retired_pilot");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "check_wrist_device");

		//-- NPC: Are you looking for the Free Pilots Guild? I have what you might describe as a 'good news, bad news' situation.
		string_id message = new string_id (c_stringFile, "s_75f774fd");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm here to see Gil Burtin.
		boolean hasResponse0 = false;
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What's the bad news?
		boolean hasResponse1 = false;
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: What's the good news?
		boolean hasResponse2 = false;
		if (privateer_broker_retired_pilot_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c88254be");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3069ecae");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5e303134");

			setObjVar (player, "conversation.privateer_broker_retired_pilot.branchId", 1);

			npcStartConversation (player, npc, "privateer_broker_retired_pilot", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "privateer_broker_retired_pilot")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = getIntObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

	if (branchId == 1 && privateer_broker_retired_pilot_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && privateer_broker_retired_pilot_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && privateer_broker_retired_pilot_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && privateer_broker_retired_pilot_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && privateer_broker_retired_pilot_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && privateer_broker_retired_pilot_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && privateer_broker_retired_pilot_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && privateer_broker_retired_pilot_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && privateer_broker_retired_pilot_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && privateer_broker_retired_pilot_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && privateer_broker_retired_pilot_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && privateer_broker_retired_pilot_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && privateer_broker_retired_pilot_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && privateer_broker_retired_pilot_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && privateer_broker_retired_pilot_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && privateer_broker_retired_pilot_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && privateer_broker_retired_pilot_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && privateer_broker_retired_pilot_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && privateer_broker_retired_pilot_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	removeObjVar (player, "conversation.privateer_broker_retired_pilot.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

