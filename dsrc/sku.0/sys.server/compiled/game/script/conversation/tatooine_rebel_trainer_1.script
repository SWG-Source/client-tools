// ======================================================================
//
// tatooine_rebel_trainer_1.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.37 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.conversation;
include library.factions;
include library.features;
include library.groundquests;
include library.skill;
include library.space_flags;
include library.space_quest;
include library.space_skill;
include library.utils;
include library.xp;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/tatooine_rebel_trainer_1";

// ======================================================================
// Script Conditions
// ======================================================================

boolean tatooine_rebel_trainer_1_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasNoviceSkillBox (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_novice" ) );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasRebelQuest (obj_id player, obj_id npc)
{
	return ( space_quest.hasQuest( player ) && tatooine_rebel_trainer_1_condition_hasNoviceSkillBox(player, npc ) );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_isImperialPilot (obj_id player, obj_id npc)
{
	int playerFactionID = pvpGetAlignedFaction(player);
	if ( playerFactionID == ##"imperial" )
		return true;
	else
		return ( hasSkill( player, "pilot_imperial_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_readyForSecondSkill (obj_id player, obj_id npc)
{
	if ( space_quest.hasCompletedQuest( player, "assassinate", "tatooine_rebel_4" ) &&
		 !hasSkill( player, "pilot_rebel_navy_starships_01" ) &&
		 !hasSkill( player, "pilot_rebel_navy_weapons_01" ) &&
		 !hasSkill( player, "pilot_rebel_navy_procedures_01" ) &&
		 !hasSkill( player, "pilot_rebel_navy_droid_01" ) )
		 return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasCompletedQuestOne (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "destroy_surpriseattack", "tatooine_rebel_1") );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasCompletedQuestTwo (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "destroy", "tatooine_rebel_3") );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasCompletedQuestThree (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "escort", "tatooine_rebel_2") );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasCompletedQuestFour (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "assassinate", "tatooine_rebel_4") );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasFailedQuestOne (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest(player, "destroy_surpriseattack", "tatooine_rebel_1" ) ||
			 space_quest.hasAbortedQuest(player, "destroy_surpriseattack", "tatooine_rebel_1" ) ||
			 space_quest.hasFailedQuest(player, "patrol", "tatooine_rebel_1" ) ||
			 space_quest.hasAbortedQuest(player, "patrol", "tatooine_rebel_1" ));
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_isReadyForFirstTraining (obj_id player, obj_id npc)
{
	//You have completed all 4 missions is handled by previous convo conditions
	
	if ( 	hasSkill( player, "pilot_rebel_navy_starships_01") 	|| 
			hasSkill( player, "pilot_rebel_navy_procedures_01") 	||
			hasSkill( player, "pilot_rebel_navy_weapons_01") 		||
			hasSkill( player, "pilot_rebel_navy_droid_01" ) 	)
		return false;//You have already received your initial skill (or more!)
	else	
		return true;
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_isReadyForMoreTraining (obj_id player, obj_id npc)
{
	//You have completed all 4 missions - checked via previous convo options
	//but don't have all the tier-1 skills yet, - //checked via previous convo options
	
	//if you have enough xp for this skill, then you have enough for
	//any other skill at this tier:
	return ( space_quest.isPlayerQualifiedForSkill( player, "pilot_rebel_navy_starships_01") );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasAllTierOneSkills (obj_id player, obj_id npc)
{
	if(space_flags.hasCompletedTierOne(player))
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasFailedQuestTwo (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "destroy", "tatooine_rebel_3" ) ||
			 space_quest.hasAbortedQuest(player, "destroy", "tatooine_rebel_3" ));
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_beenRewardedForQuestOne (obj_id player, obj_id npc)
{
	//this returns true if you have completed this quest but have not been rewarded for it
	return space_quest.hasReceivedReward( player, "destroy_surpriseattack", "tatooine_rebel_1");
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_beenRewardedForQuestTwo (obj_id player, obj_id npc)
{
	//this returns true if you have completed this quest but have not been rewarded for it
	return space_quest.hasReceivedReward( player, "destroy", "tatooine_rebel_3");
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_beenRewardedForQuestThree (obj_id player, obj_id npc)
{
	//this returns true if you have completed this quest but have not been rewarded for it
	return space_quest.hasReceivedReward( player, "escort", "tatooine_rebel_2");
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_beenRewardedForQuestFour (obj_id player, obj_id npc)
{
	//this returns true if you have completed this quest but have not been rewarded for it
	return space_quest.hasReceivedReward( player, "assassinate", "tatooine_rebel_4");
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasFailedQuestThree (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest(player, "escort", "tatooine_rebel_2" ) ||
			 space_quest.hasAbortedQuest(player, "escort", "tatooine_rebel_2" ) ||
			 space_quest.hasFailedQuest(player, "patrol", "tatooine_rebel_2" ) ||
			 space_quest.hasAbortedQuest(player, "patrol", "tatooine_rebel_2" ));
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasFailedQuestFour (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "assassinate", "tatooine_rebel_4" ) ||
			 space_quest.hasAbortedQuest(player, "assassinate", "tatooine_rebel_4" ));
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_isPrivateerPilot (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_neutral_novice" );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasStarships1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_starships_01" ) );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasWeapons1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_weapons_01" ) );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasProcedures1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_procedures_01" ) );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasFailedQuestOne_PATROL (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "patrol", "tatooine_rebel_1" ) ||
			 space_quest.hasAbortedQuest(player, "patrol", "tatooine_rebel_1" ));
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasSpaceExpansion (obj_id player, obj_id npc)
{
	return ( features.isSpaceEdition( player ) );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasSpaceShip (obj_id player, obj_id npc)
{
	return (!space_quest.canGrantNewbieShip(player));
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_is_GRADUATED_goto_Yavin4 (obj_id player, obj_id npc)
{
	// return ( hasObjVar( player, "ekerPilot" ));
	
	return space_flags.getBooleanSpaceFlag( player, "ekerPilot" );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_hasVeryFirstQuest (obj_id player, obj_id npc)
{
	//You've got your assignment, and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
	if (!space_quest.hasCertifiedShip(player))
		return false; 
	else	
		return ( space_quest.hasQuest( player, "patrol", "tatooine_rebel_1" ) ||
			  	 space_quest.hasQuest( player, "destroy_surpriseattack", "tatooine_rebel_1" ) );

}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_canFlyNonTrackDuty (obj_id player, obj_id npc)
{
	if((hasSkill( player, "pilot_rebel_navy_starships_01") || 
	    hasSkill( player, "pilot_rebel_navy_procedures_01") ||
	    hasSkill( player, "pilot_rebel_navy_weapons_01") ||
	    hasSkill( player, "pilot_rebel_navy_droid_01" )) && !space_quest.hasQuest(player))
	    return true;
	    
	return false;
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_onMyTrack (obj_id player, obj_id npc)
{
	//this is a temporary thing to convert BETA players to the
	//new flagging system.
	if ( hasSkill( player, "pilot_rebel_navy_novice" ))
	{
		string currentTrack = space_flags.getSpaceTrack( player );
		if ( currentTrack == null )
			space_flags.setSpaceTrack( player, space_flags.REBEL_TATOOINE);
	}
	
	return space_flags.isSpaceTrack(player, space_flags.REBEL_TATOOINE);
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_isWookieeOrIthorian (obj_id player, obj_id npc)
{
	int plrSpecies = getSpecies( player );
	return ( plrSpecies == SPECIES_WOOKIEE || plrSpecies == SPECIES_ITHORIAN );
}

// ----------------------------------------------------------------------

boolean tatooine_rebel_trainer_1_condition_isNotRebel (obj_id player, obj_id npc)
{
	return !factions.isRebel(player);
}

// ======================================================================
// Script Actions
// ======================================================================

void tatooine_rebel_trainer_1_action_giveMajorEkerWaypoint (obj_id player, obj_id npc)
{
	location loc = new location(-6969, 73, -5667, "yavin4");  
	obj_id ekerWpt = createWaypointInDatapad(player, loc);
	setWaypointName(ekerWpt, "Major Eker");
	setWaypointActive(ekerWpt, true);
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_grantNoviceSkillBox (obj_id player, obj_id npc)
{
	space_flags.setSpaceTrack( player, space_flags.REBEL_TATOOINE );//do this first, to get the right music!
	skill.noisyGrantSkill( player, "pilot_rebel_navy_novice" );
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_grantQuestOne (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "patrol", "tatooine_rebel_1");
	space_flags.restoreClientPath( player );
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_grantQuestTwo (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "destroy", "tatooine_rebel_3");
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_grantQuestThree (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "patrol", "tatooine_rebel_2");
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_grantQuestFour (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "assassinate", "tatooine_rebel_4");
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_anim_poundFist (obj_id player, obj_id npc)
{
	doAnimationAction( npc, "pound_fist_palm" );
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_giveSkill1 (obj_id player, obj_id npc)
{
	faceTo( npc, player );
	doAnimationAction( npc, "salute1" );
	
	if ( tatooine_rebel_trainer_1_condition_readyForSecondSkill( player, npc ) )
	{
		skill.noisyGrantSkill( player, "pilot_rebel_navy_starships_01" );
	}
	else
	{
		skill.purchaseSkill( player, "pilot_rebel_navy_starships_01" );
	}
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_giveSkill2 (obj_id player, obj_id npc)
{
	faceTo( npc, player );
	doAnimationAction( npc, "salute1" );

	if ( tatooine_rebel_trainer_1_condition_readyForSecondSkill( player, npc ) )
	{
		skill.noisyGrantSkill( player, "pilot_rebel_navy_weapons_01" );
	}
	else
	{
		skill.purchaseSkill( player, "pilot_rebel_navy_weapons_01" );
	}	
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_giveSkill3 (obj_id player, obj_id npc)
{
	faceTo( npc, player );
	doAnimationAction( npc, "salute1" );

	if ( tatooine_rebel_trainer_1_condition_readyForSecondSkill( player, npc ) )
	{
		skill.noisyGrantSkill( player, "pilot_rebel_navy_procedures_01" );
	}
	else
	{
		skill.purchaseSkill( player, "pilot_rebel_navy_procedures_01" );
	}	
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_giveSkill4 (obj_id player, obj_id npc)
{
	faceTo( npc, player );
	doAnimationAction( npc, "salute1" );

	if ( tatooine_rebel_trainer_1_condition_readyForSecondSkill( player, npc ) )
	{
		skill.noisyGrantSkill( player, "pilot_rebel_navy_droid_01" );
	}
	else
	{
		skill.purchaseSkill( player, "pilot_rebel_navy_droid_01" );
	}	
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_grantDutyDESTROY_1 (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "destroy_duty", "tatooine_rebel_6");
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_grantDutyESCORT_1 (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "escort_duty", "tatooine_rebel_7");
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_rewardQuest1 (obj_id player, obj_id npc)
{
	// Animation.
	faceTo( npc, player );
	
	// Give credits.
	if (!space_quest.hasReceivedReward( player, "destroy_surpriseattack", "tatooine_rebel_1") )
	{
		space_quest.giveReward( player, "destroy_surpriseattack", "tatooine_rebel_1", 100 );
		factions.addFactionStanding( player, factions.FACTION_REBEL, 25.0f );
	}
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_rewardQuest2 (obj_id player, obj_id npc)
{
	// Animation.
	faceTo( npc, player );
	
	// Give credits.
	if (!space_quest.hasReceivedReward( player, "destroy", "tatooine_rebel_3") )
	{
		space_quest.giveReward( player, "destroy", "tatooine_rebel_3", 200 );
		factions.addFactionStanding( player, factions.FACTION_REBEL, 50.0f );
	}
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_rewardQuest3 (obj_id player, obj_id npc)
{
	if (space_quest.hasReceivedReward( player, "escort", "tatooine_rebel_2") )
		return;

	// Animation.
	faceTo( npc, player );
	
	// Give credits.
	factions.addFactionStanding( player, factions.FACTION_REBEL, 50.0f );
	if (!tatooine_rebel_trainer_1_condition_isWookieeOrIthorian(player, npc ))
		space_quest.giveReward( player, "escort", "tatooine_rebel_2", 500, "object/tangible/wearables/bodysuit/rebel_bodysuit_s14.iff");
	else if ( getSpecies( player ) == SPECIES_ITHORIAN )
	{
		space_quest.giveReward( player, "escort", "tatooine_rebel_2", 500, "object/tangible/wearables/bandolier/ith_multipocket_bandolier.iff");
	}
	else
		space_quest.giveReward( player, "escort", "tatooine_rebel_2", 500, "object/tangible/wearables/bandolier/multipocket_bandolier.iff");
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_rewardQuest4 (obj_id player, obj_id npc)
{
	// Animation.
	faceTo( npc, player );
	
	// Give credits.
	if (!space_quest.hasReceivedReward( player, "assassinate", "tatooine_rebel_4") )
	{
		space_quest.giveReward( player, "assassinate", "tatooine_rebel_4", 1000, "object/tangible/ship/components/armor/arm_mission_reward_rebel_incom_ultralight.iff" );
		factions.addFactionStanding( player, factions.FACTION_REBEL, 75.0f );
	}
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_grantSpaceShip (obj_id player, obj_id npc)
{
	space_quest.grantNewbieShip( player, "rebel");
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_set_GRADUATED_goto_Yavin4 (obj_id player, obj_id npc)
{
	// setObjVar( player, "ekerPilot", true );
	
	space_flags.setSpaceFlag( player, "ekerPilot", true );
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_eraseClientPath (obj_id player, obj_id npc)
{
	space_flags.clearClientPath( player );
}

// ----------------------------------------------------------------------

void tatooine_rebel_trainer_1_action_sendVourkSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal (player, "tat_vourk_rebpilot_e1");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int tatooine_rebel_trainer_1_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You've got your assignment, and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So now what?
	if (response == "s_8a4652c6")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Now you need to go to the starport. Go inside and look for a 'starship terminal.' Use the terminal to launch your starship into space. When you launch, a waypoint will be created to help guide you back to your landing spot. When you want to land - comm the space station and ask for landing clearance.
			string_id message = new string_id (c_stringFile, "s_59dceadb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back from space?
	if (response == "s_680cda08")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you are done with your patrol, you will need to find your way to a space station above Tatooine. Communicate with the station operatives and request landing clearance. The rest is automatic.
			string_id message = new string_id (c_stringFile, "s_9a159641");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where's my starship?
	if (response == "s_5bfac9f1")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: A number of slightly-used Z95 starships are parked around Tatooine... hidden from Imperial eyes. The codes I uploaded to your datapad will allow you to assume control of one. Just head down to the starport...
			string_id message = new string_id (c_stringFile, "s_d806feaa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Now you need to go to the starport. Go inside and look for a 'starship terminal.' Use the terminal to launch your starship into space. When you launch, a waypoint will be created to help guide you back to your landing spot. When you want to land - comm the space station and ask for landing clearance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So now what?
	if (response == "s_8a4652c6")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Now you need to go to the starport. Go inside and look for a 'starship terminal.' Use the terminal to launch your starship into space. When you launch, a waypoint will be created to help guide you back to your landing spot. When you want to land - comm the space station and ask for landing clearance.
			string_id message = new string_id (c_stringFile, "s_59dceadb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back from space?
	if (response == "s_680cda08")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you are done with your patrol, you will need to find your way to a space station above Tatooine. Communicate with the station operatives and request landing clearance. The rest is automatic.
			string_id message = new string_id (c_stringFile, "s_9a159641");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where's my starship?
	if (response == "s_5bfac9f1")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: A number of slightly-used Z95 starships are parked around Tatooine... hidden from Imperial eyes. The codes I uploaded to your datapad will allow you to assume control of one. Just head down to the starport...
			string_id message = new string_id (c_stringFile, "s_d806feaa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: When you are done with your patrol, you will need to find your way to a space station above Tatooine. Communicate with the station operatives and request landing clearance. The rest is automatic.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I find a space station?
	if (response == "s_4738e99f")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Use your zone map interface to locate space stations while in-flight. Or - use the automatic 'landing waypoint' that your ship computer creates for you when you take off. The 'landing waypoint' can be activated from your datapad. Just find your way back to that point when you are done with your assignment.
			string_id message = new string_id (c_stringFile, "s_29b9c1a7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I comm the space station?
	if (response == "s_c4c3d546")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Target the space station and then comm the space station. Most likely the station will recognize you and will comm you first. But if you are in a hurry, use SHIFT+C to target the space station and open communication with the station commander.
			string_id message = new string_id (c_stringFile, "s_6440d5e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And then what?
	if (response == "s_d038a005")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then you rejoice! You're still alive, you're done with your assignment, and life is good... right? Come back here when you're done so I can give you your next assignment.
			string_id message = new string_id (c_stringFile, "s_a6c2aaa1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Use your zone map interface to locate space stations while in-flight. Or - use the automatic 'landing waypoint' that your ship computer creates for you when you take off. The 'landing waypoint' can be activated from your datapad. Just find your way back to that point when you are done with your assignment.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I find a space station?
	if (response == "s_4738e99f")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Use your zone map interface to locate space stations while in-flight. Or - use the automatic 'landing waypoint' that your ship computer creates for you when you take off. The 'landing waypoint' can be activated from your datapad. Just find your way back to that point when you are done with your assignment.
			string_id message = new string_id (c_stringFile, "s_29b9c1a7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I comm the space station?
	if (response == "s_c4c3d546")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Target the space station and then comm the space station. Most likely the station will recognize you and will comm you first. But if you are in a hurry, use SHIFT+C to target the space station and open communication with the station commander.
			string_id message = new string_id (c_stringFile, "s_6440d5e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And then what?
	if (response == "s_d038a005")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then you rejoice! You're still alive, you're done with your assignment, and life is good... right? Come back here when you're done so I can give you your next assignment.
			string_id message = new string_id (c_stringFile, "s_a6c2aaa1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Target the space station and then comm the space station. Most likely the station will recognize you and will comm you first. But if you are in a hurry, use SHIFT+C to target the space station and open communication with the station commander.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I find a space station?
	if (response == "s_4738e99f")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Use your zone map interface to locate space stations while in-flight. Or - use the automatic 'landing waypoint' that your ship computer creates for you when you take off. The 'landing waypoint' can be activated from your datapad. Just find your way back to that point when you are done with your assignment.
			string_id message = new string_id (c_stringFile, "s_29b9c1a7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I comm the space station?
	if (response == "s_c4c3d546")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Target the space station and then comm the space station. Most likely the station will recognize you and will comm you first. But if you are in a hurry, use SHIFT+C to target the space station and open communication with the station commander.
			string_id message = new string_id (c_stringFile, "s_6440d5e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And then what?
	if (response == "s_d038a005")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then you rejoice! You're still alive, you're done with your assignment, and life is good... right? Come back here when you're done so I can give you your next assignment.
			string_id message = new string_id (c_stringFile, "s_a6c2aaa1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then you rejoice! You're still alive, you're done with your assignment, and life is good... right? Come back here when you're done so I can give you your next assignment.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So now what?
	if (response == "s_8a4652c6")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Now you need to go to the starport. Go inside and look for a 'starship terminal.' Use the terminal to launch your starship into space. When you launch, a waypoint will be created to help guide you back to your landing spot. When you want to land - comm the space station and ask for landing clearance.
			string_id message = new string_id (c_stringFile, "s_59dceadb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back from space?
	if (response == "s_680cda08")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you are done with your patrol, you will need to find your way to a space station above Tatooine. Communicate with the station operatives and request landing clearance. The rest is automatic.
			string_id message = new string_id (c_stringFile, "s_9a159641");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where's my starship?
	if (response == "s_5bfac9f1")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: A number of slightly-used Z95 starships are parked around Tatooine... hidden from Imperial eyes. The codes I uploaded to your datapad will allow you to assume control of one. Just head down to the starport...
			string_id message = new string_id (c_stringFile, "s_d806feaa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A number of slightly-used Z95 starships are parked around Tatooine... hidden from Imperial eyes. The codes I uploaded to your datapad will allow you to assume control of one. Just head down to the starport...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So now what?
	if (response == "s_8a4652c6")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Now you need to go to the starport. Go inside and look for a 'starship terminal.' Use the terminal to launch your starship into space. When you launch, a waypoint will be created to help guide you back to your landing spot. When you want to land - comm the space station and ask for landing clearance.
			string_id message = new string_id (c_stringFile, "s_59dceadb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back from space?
	if (response == "s_680cda08")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you are done with your patrol, you will need to find your way to a space station above Tatooine. Communicate with the station operatives and request landing clearance. The rest is automatic.
			string_id message = new string_id (c_stringFile, "s_9a159641");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I find a space station?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I comm the space station?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: And then what?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4738e99f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4c3d546");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d038a005");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where's my starship?
	if (response == "s_5bfac9f1")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: A number of slightly-used Z95 starships are parked around Tatooine... hidden from Imperial eyes. The codes I uploaded to your datapad will allow you to assume control of one. Just head down to the starport...
			string_id message = new string_id (c_stringFile, "s_d806feaa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So now what?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back from space?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where's my starship?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] This is what the NPC says when he talks to a player who is not currently signed-up with any pilot profession.
	//-- NPC: Good to meet you. My name is Da'la Socuna and I am here on behalf of the Rebel Alliance naval forces. I train pilots to fly and fight.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like fun. Where do I sign?
	if (response == "s_78679b31")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_anim_poundFist (player, npc);

			//-- NPC: Fun? This is war, my friend. There's nothing 'fun' about having your friends swept away by the bloody tides of battle, losing your home, your future, everything that ever meant anything to you...
			string_id message = new string_id (c_stringFile, "s_1eed018e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: We can skip the moral lesson, sister.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Relax, lady. I didn't mean anything by that!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8268c49d");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55ea0c1b");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know of the rebellion. I want to help!
	if (response == "s_3f5c320e")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Then it is fortunate that we have met. I can induct you to the Alliance pilot training program immediately.
			string_id message = new string_id (c_stringFile, "s_790724e4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I can't wait!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Train me to fly for the Alliance.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c8fa77b8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_705f0112");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: We are kindred spirits, Da'la Socuna.
	if (response == "s_db6b2243")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Your kindness is radiant, my friend. We could really use someone like you in our organization. Are you willing to place your trust in the Rebel Alliance?
			string_id message = new string_id (c_stringFile, "s_33e1f5f2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Alliance has my eternal faith.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes... I am willing. Yes.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31d45080");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8234fa32");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fun? This is war, my friend. There's nothing 'fun' about having your friends swept away by the bloody tides of battle, losing your home, your future, everything that ever meant anything to you...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: We can skip the moral lesson, sister.
	if (response == "s_8268c49d")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Charming. What can I do to get you out of here?
			string_id message = new string_id (c_stringFile, "s_d497fa22");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Make me a part of the Rebel Alliance.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't want to leave. I want to help.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4101ebdd");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8631e93");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Relax, lady. I didn't mean anything by that!
	if (response == "s_55ea0c1b")
	{
		doAnimationAction (player, "gesticulate_wildly");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Then tell me what you want or get out of here.
			string_id message = new string_id (c_stringFile, "s_66ae2bdf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just let me in your club, okay?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want to be part of the Alliance.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_768b62bc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7ed5fec3");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Charming. What can I do to get you out of here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Make me a part of the Rebel Alliance.
	if (response == "s_4101ebdd")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: I hope I don't regret this. I hereby admit you into the Alliance pilot training program.
			string_id message = new string_id (c_stringFile, "s_a13bb0b0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I need a space ship.
			boolean hasResponse0 = false;
			if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It's about time.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e095ac8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5cc6139");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't want to leave. I want to help.
	if (response == "s_8631e93")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Well, it takes all kinds, doesn't it? Welcome to the Rebel Alliance.
			string_id message = new string_id (c_stringFile, "s_a93c5fce");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I need a space ship.
			boolean hasResponse0 = false;
			if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It's about time.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_33");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_37");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I hope I don't regret this. I hereby admit you into the Alliance pilot training program.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need a space ship.
	if (response == "s_e095ac8")
	{
		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: What a surprise. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_39f7d683");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's about time.
	if (response == "s_c5cc6139")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_b3408be1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What a surprise. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, it takes all kinds, doesn't it? Welcome to the Rebel Alliance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need a space ship.
	if (response == "s_33")
	{
		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: What a surprise. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_35");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's about time.
	if (response == "s_37")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_39");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What a surprise. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then tell me what you want or get out of here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just let me in your club, okay?
	if (response == "s_768b62bc")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "beckon");

			tatooine_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Fine. You're in.
			string_id message = new string_id (c_stringFile, "s_492ca0a5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I need a space ship.
			boolean hasResponse0 = false;
			if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It's about time.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to be part of the Alliance.
	if (response == "s_7ed5fec3")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: We can always use cannon fodder. Welcome to the Alliance.
			string_id message = new string_id (c_stringFile, "s_987055b5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I need a space ship.
			boolean hasResponse0 = false;
			if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It's about time.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fine. You're in.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need a space ship.
	if (response == "s_45")
	{
		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: What a surprise. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_47");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's about time.
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_51");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What a surprise. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We can always use cannon fodder. Welcome to the Alliance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need a space ship.
	if (response == "s_55")
	{
		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: What a surprise. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_57");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's about time.
	if (response == "s_59")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_61");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What a surprise. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then it is fortunate that we have met. I can induct you to the Alliance pilot training program immediately.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can't wait!
	if (response == "s_c8fa77b8")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			tatooine_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Your enthusiasm is welcome, friend!
			string_id message = new string_id (c_stringFile, "s_84d7bb8e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ma'am, I require a starship.
			boolean hasResponse0 = false;
			if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thank you very much.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bd53784c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4136c2df");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Train me to fly for the Alliance.
	if (response == "s_705f0112")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			tatooine_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Then we shall begin. Welcome to the Rebel Alliance.
			string_id message = new string_id (c_stringFile, "s_2fdfe99f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ma'am, I require a starship.
			boolean hasResponse0 = false;
			if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thank you very much.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your enthusiasm is welcome, friend!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ma'am, I require a starship.
	if (response == "s_bd53784c")
	{
		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Not a problem. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_b2133e77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you very much.
	if (response == "s_4136c2df")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_70");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not a problem. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then we shall begin. Welcome to the Rebel Alliance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ma'am, I require a starship.
	if (response == "s_74")
	{
		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: The Alliance has you covered. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_42ae7917");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you very much.
	if (response == "s_77")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Alliance has you covered. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your kindness is radiant, my friend. We could really use someone like you in our organization. Are you willing to place your trust in the Rebel Alliance?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Alliance has my eternal faith.
	if (response == "s_31d45080")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			tatooine_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Then it is my pleasure to welcome you to the Alliance space navy.
			string_id message = new string_id (c_stringFile, "s_7d2cd92d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will require a starship.
			boolean hasResponse0 = false;
			if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thank you very much.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aed0dff");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes... I am willing. Yes.
	if (response == "s_8234fa32")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: I know that you are making the right decision, friend.
			string_id message = new string_id (c_stringFile, "s_23567daf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will need a starship.
			boolean hasResponse0 = false;
			if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thank you very much.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a42932d7");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then it is my pleasure to welcome you to the Alliance space navy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will require a starship.
	if (response == "s_aed0dff")
	{
		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You shall have one. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_f6e9edd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you very much.
	if (response == "s_87")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You shall have one. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I know that you are making the right decision, friend.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will need a starship.
	if (response == "s_a42932d7")
	{
		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Understood, my friend. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_f29cdd1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you very much.
	if (response == "s_95")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch40 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Understood, my friend. I'll grant access to one of the refurbished Z95's we have hidden here on Tatooine. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Always good to see another Rebel pilot... but you're not one of my recruits. What can I do for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you have any missions I could fly?
	if (response == "s_1583743c")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition_canFlyNonTrackDuty (player, npc))
		{
			//-- NPC: I don't have any pressing assignments right now, but I do have some elective duty missions if you're interested?
			string_id message = new string_id (c_stringFile, "s_c5ac7e76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, thanks.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready to enlist with the ground forces!
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6e646769");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e0cb3e19");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 190);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Unfortunately, since you are not part of my unit, I cannot assign you missions. You should probably return and speak with your commanding officer.
			string_id message = new string_id (c_stringFile, "s_b2c15146");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't have any pressing assignments right now, but I do have some elective duty missions if you're interested?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is a good way to earn experience. Especially for a pilot looking for training like yourself. Duty missions have no real end. You just finish when you finish, know what I mean? So what do you say?
			string_id message = new string_id (c_stringFile, "s_cb60fa8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, thanks.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready to enlist with the ground forces!
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6e646769");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e0cb3e19");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 190);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am interested in a duty mission.
	if (response == "s_4ffd4b9d")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Great. You can either patrol the Tatooine system looking for Imperials to send to the scrapyard, or you can help escort Alliance transports. What sounds good to you?
			string_id message = new string_id (c_stringFile, "s_bf55aded");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would like to patrol Tatooine space.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I feel our transports need escort...
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a0aba27");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2cb7683a");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 192);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, thanks.
	if (response == "s_6e646769")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			//-- NPC: The Force be with you, then...
			string_id message = new string_id (c_stringFile, "s_1c2f3f24");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to enlist with the ground forces!
	if (response == "s_e0cb3e19")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Are you sure about that, %NU?  We could officially put your name on the roster, but even if you remain Covert, there's always a chance of an Imperial uncovering you.
			string_id message = new string_id (c_stringFile, "s_8da0fa08");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, never mind.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I know the dangers
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d01d0154");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_831a22ee");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 196);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Just how exactly do you expect to be a Rebel Alliance pilot without a ship?  Here.  I'll add these access codes for a small fighter to your datapad.  You ought to upgrade to a better ship pretty soon, though.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, yeah!
	if (response == "s_7553991")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: Try to keep this one from blowing up, okay?
			string_id message = new string_id (c_stringFile, "s_33823c2e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ma'am! Thank you, ma'am!
	if (response == "s_288ce434")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			//-- NPC: Outstanding!
			string_id message = new string_id (c_stringFile, "s_7077f9f7");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For the Alliance!
	if (response == "s_a994ca63")
	{
		doAnimationAction (player, "salute1");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_2d4bb338");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch50 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're not supposed to be here, pilot. You're assigned to Major Eker's group on the moon Yavin IV.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get there?
	if (response == "s_b3bebc5e")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			tatooine_rebel_trainer_1_action_giveMajorEkerWaypoint (player, npc);

			//-- NPC: Fly there, Pilot. Your ship is equipped with a hyperdrive. Major Eker is in the Medical Center at the labor outpost on the moon Yavin IV. I've placed the location in your datapad.
			string_id message = new string_id (c_stringFile, "s_e2a3d627");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have all the tier-1 skills, so would you like some tier-2 content?
	//-- NPC: It appears that our time together has come to an end, pilot. Alliance HQ wants you reallocated to special operations on Yavin IV and placed under the command of Major Eker... immediately.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay. Bye!
	if (response == "s_abc65d0")
	{
		doAnimationAction (player, "goodbye");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			tatooine_rebel_trainer_1_action_giveMajorEkerWaypoint (player, npc);

			//-- NPC: Perhaps you will show Major Eker more respect. The Major is currently located at the labor outpost on the moon Yavin IV. I've placed the location in your datapad. Good luck, %NU.
			string_id message = new string_id (c_stringFile, "s_7eff4d79");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, ma'am!
	if (response == "s_f843dc6a")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Major Eker and his special operations team have a wide-range of responsibilities on Yavin IV. Before you head off, I want you to appreciate just how dangerous this assignment could be.
			string_id message = new string_id (c_stringFile, "s_f5255b8a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I do, ma'am.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8685174d");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: If it is my duty, Da'la.
	if (response == "s_d8913d37")
	{
		doAnimationAction (player, "bow2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You have done great things here in Tatooine, %NU... but the Alliance needs you on Yavin. The stakes in this conflict are continuing to rise, and our pilots must go where they are needed. I am sure you will flourish under Major Eker's command.
			string_id message = new string_id (c_stringFile, "s_95310ec0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can you tell me of Major Eker?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you know of the Yavin 4 operation?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you, Da'la...
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6f3e992");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fdb94a71");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cd0ae4c4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 56);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Major Eker and his special operations team have a wide-range of responsibilities on Yavin IV. Before you head off, I want you to appreciate just how dangerous this assignment could be.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I do, ma'am.
	if (response == "s_8685174d")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			tatooine_rebel_trainer_1_action_giveMajorEkerWaypoint (player, npc);

			//-- NPC: Good luck to you, pilot! The Major is currently located in the Medical Center at the labor outpost on the moon Yavin IV. I've placed the location in your datapad. Hope to see you again when this war is over...
			string_id message = new string_id (c_stringFile, "s_2b43e56e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch56 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have done great things here in Tatooine, %NU... but the Alliance needs you on Yavin. The stakes in this conflict are continuing to rise, and our pilots must go where they are needed. I am sure you will flourish under Major Eker's command.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can you tell me of Major Eker?
	if (response == "s_6f3e992")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We exchanged messages through the Alliance 'net right after the Battle of Yavin. He was in need of pilots then, too. So, we didn't discuss anything outside of that. Of course, I have heard rumors of his history before joining the Alliance... but nothing concrete.
			string_id message = new string_id (c_stringFile, "s_cfba4c0a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What rumors?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d5c41154");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you know of the Yavin 4 operation?
	if (response == "s_fdb94a71")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I know only that it involves researching the Death Star space station technology. With so much wreckage in the area, the Alliance has an opportunity to deconstruct the Empire's elite doomsday weapon... and prepare an adequate response if they were to build another one.
			string_id message = new string_id (c_stringFile, "s_5f15e36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can you tell me of Major Eker?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you know of the Yavin 4 operation?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you, Da'la...
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6f3e992");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fdb94a71");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cd0ae4c4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with you, Da'la...
	if (response == "s_cd0ae4c4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: And with you as well. The Major is currently located at the labor outpost on the moon Yavin 4. Good luck, %NU. I know you will serve the Alliance well on Yavin 4.
			string_id message = new string_id (c_stringFile, "s_111cd324");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch57 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We exchanged messages through the Alliance 'net right after the Battle of Yavin. He was in need of pilots then, too. So, we didn't discuss anything outside of that. Of course, I have heard rumors of his history before joining the Alliance... but nothing concrete.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What rumors?
	if (response == "s_d5c41154")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I've heard that Eker was a decorated Imperial pilot and leader of a vicious TIE squadron for several years... until he sickened of their tactics. I believe he defected after being ordered to fire on an unarmed medical transport.
			string_id message = new string_id (c_stringFile, "s_855c7780");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can you tell me of Major Eker?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you know of the Yavin 4 operation?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you, Da'la...
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6f3e992");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fdb94a71");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cd0ae4c4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've heard that Eker was a decorated Imperial pilot and leader of a vicious TIE squadron for several years... until he sickened of their tactics. I believe he defected after being ordered to fire on an unarmed medical transport.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can you tell me of Major Eker?
	if (response == "s_6f3e992")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We exchanged messages through the Alliance 'net right after the Battle of Yavin. He was in need of pilots then, too. So, we didn't discuss anything outside of that. Of course, I have heard rumors of his history before joining the Alliance... but nothing concrete.
			string_id message = new string_id (c_stringFile, "s_cfba4c0a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What rumors?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d5c41154");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you know of the Yavin 4 operation?
	if (response == "s_fdb94a71")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I know only that it involves researching the Death Star space station technology. With so much wreckage in the area, the Alliance has an opportunity to deconstruct the Empire's elite doomsday weapon... and prepare an adequate response if they were to build another one.
			string_id message = new string_id (c_stringFile, "s_5f15e36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can you tell me of Major Eker?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you know of the Yavin 4 operation?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you, Da'la...
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6f3e992");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fdb94a71");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cd0ae4c4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with you, Da'la...
	if (response == "s_cd0ae4c4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: And with you as well. The Major is currently located at the labor outpost on the moon Yavin 4. Good luck, %NU. I know you will serve the Alliance well on Yavin 4.
			string_id message = new string_id (c_stringFile, "s_111cd324");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch59 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I know only that it involves researching the Death Star space station technology. With so much wreckage in the area, the Alliance has an opportunity to deconstruct the Empire's elite doomsday weapon... and prepare an adequate response if they were to build another one.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can you tell me of Major Eker?
	if (response == "s_6f3e992")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We exchanged messages through the Alliance 'net right after the Battle of Yavin. He was in need of pilots then, too. So, we didn't discuss anything outside of that. Of course, I have heard rumors of his history before joining the Alliance... but nothing concrete.
			string_id message = new string_id (c_stringFile, "s_cfba4c0a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What rumors?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d5c41154");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you know of the Yavin 4 operation?
	if (response == "s_fdb94a71")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I know only that it involves researching the Death Star space station technology. With so much wreckage in the area, the Alliance has an opportunity to deconstruct the Empire's elite doomsday weapon... and prepare an adequate response if they were to build another one.
			string_id message = new string_id (c_stringFile, "s_5f15e36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can you tell me of Major Eker?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you know of the Yavin 4 operation?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you, Da'la...
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6f3e992");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fdb94a71");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cd0ae4c4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with you, Da'la...
	if (response == "s_cd0ae4c4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: And with you as well. The Major is currently located at the labor outpost on the moon Yavin 4. Good luck, %NU. I know you will serve the Alliance well on Yavin 4.
			string_id message = new string_id (c_stringFile, "s_111cd324");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Report, pilot!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was unable to finish.
	if (response == "s_4f90bab3")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Why not?
			string_id message = new string_id (c_stringFile, "s_7f9b8387");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I don't know.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Lie about what happened.)
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7d4eaca7");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_583707c4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I failed the mission.
	if (response == "s_b9c55cab")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: This is unacceptable! You get back out there and finish this mission, pilot!
			string_id message = new string_id (c_stringFile, "s_bb761ef6");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why not?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know.
	if (response == "s_7d4eaca7")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Maybe you should try again. And this time... don't fail!
			string_id message = new string_id (c_stringFile, "s_c4b9eaff");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Lie about what happened.)
	if (response == "s_583707c4")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Hm. Interesting. Are you ready to try again?
			string_id message = new string_id (c_stringFile, "s_a7a6018f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d70dba34");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4c695dbd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 64);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hm. Interesting. Are you ready to try again?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_d70dba34")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Good! Your orders have been renewed. Good luck, pilot!
			string_id message = new string_id (c_stringFile, "s_cc82ca6b");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No.
	if (response == "s_4c695dbd")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I don't think this is going to work out, pilot.
			string_id message = new string_id (c_stringFile, "s_dcf7d387");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch68 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Report!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I failed the mission.
	if (response == "s_151")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Well, I'll admit that I didn't prep you for a TIE fighter attack. How do you think you'll fare on your second try?
			string_id message = new string_id (c_stringFile, "s_aadc4812");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Piece of cake.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I will complete the mission, ma'am!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm ready for anything.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2683b4a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ffb79794");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b3e9c738");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I could not finish the mission.
	if (response == "s_68ee2577")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: We cannot afford for you to fail, pilot. The Empire outnumbers us - outguns us... the only hope we have is to out-fight them! Are you ready to try again?
			string_id message = new string_id (c_stringFile, "s_ca06f3a2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_162");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_165");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch69 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, I'll admit that I didn't prep you for a TIE fighter attack. How do you think you'll fare on your second try?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Piece of cake.
	if (response == "s_b2683b4a")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Yeah, right. Prove it!
			string_id message = new string_id (c_stringFile, "s_34bc4ce6");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will complete the mission, ma'am!
	if (response == "s_ffb79794")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Excellent. Good luck, pilot.
			string_id message = new string_id (c_stringFile, "s_ed5af630");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready for anything.
	if (response == "s_b3e9c738")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: I truly hope so.
			string_id message = new string_id (c_stringFile, "s_9e21ae0d");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch73 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We cannot afford for you to fail, pilot. The Empire outnumbers us - outguns us... the only hope we have is to out-fight them! Are you ready to try again?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_162")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Good. Then get back out there!
			string_id message = new string_id (c_stringFile, "s_834edaa1");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No.
	if (response == "s_165")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: Get your act together!
			string_id message = new string_id (c_stringFile, "s_fb7175d6");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch76 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't like what I'm hearing, pilot! Report, now!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It wasn't my fault!
	if (response == "s_6657b5a3")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Whose fault was it, then?
			string_id message = new string_id (c_stringFile, "s_a9163b5e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well... the Empire's?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, so it was mine.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Tell a bold lie.)
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_adacb6c2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a45033de");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6cf8e18b");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sorry, ma'am!
	if (response == "s_740e2e66")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Are you ready to redeem yourself, pilot?!
			string_id message = new string_id (c_stringFile, "s_2aab204b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ma'am! Yes, ma'am!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm... not sure
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_347e4a03");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b488c78");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I failed to destroy the targets.
	if (response == "s_54ec182b")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Will you fail a second time if I re-assign the mission to you?
			string_id message = new string_id (c_stringFile, "s_f7ebcc5f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No. I will not!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I make no guarantees.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_968c130f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f892eb03");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch77 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Whose fault was it, then?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well... the Empire's?
	if (response == "s_adacb6c2")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Well... do something about it!
			string_id message = new string_id (c_stringFile, "s_add5c243");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, so it was mine.
	if (response == "s_a45033de")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: You're learning, then. Good. Try again!
			string_id message = new string_id (c_stringFile, "s_5100b9ac");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Tell a bold lie.)
	if (response == "s_6cf8e18b")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Hm. You're not making any sense. Why don't you try this mission again?
			string_id message = new string_id (c_stringFile, "s_8e0b0814");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch81 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you ready to redeem yourself, pilot?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ma'am! Yes, ma'am!
	if (response == "s_347e4a03")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Outstanding! I'll see you when you're done!
			string_id message = new string_id (c_stringFile, "s_ac596e57");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm... not sure
	if (response == "s_1b488c78")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: You really disappoint me, pilot.
			string_id message = new string_id (c_stringFile, "s_648f0fc");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch84 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Will you fail a second time if I re-assign the mission to you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No. I will not!
	if (response == "s_968c130f")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Good! Try again!
			string_id message = new string_id (c_stringFile, "s_71370b1e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I make no guarantees.
	if (response == "s_f892eb03")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: You must try harder! The Alliance doesn't stand a chance with pilots like you... with your attitude!
			string_id message = new string_id (c_stringFile, "s_63c4c628");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch87 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I already know what happened. Do you have anything to say for yourself?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It wasn't my fault!
	if (response == "s_190")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: Are you kidding me?! Not your fault? I have grown so sick of your attitude... I just can't stand it! Tell me you're going to get back up there and do it right this time!
			string_id message = new string_id (c_stringFile, "s_b7af9aa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay, fine!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Listen, lady...
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fc1f4e33");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2c3fe4f");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 88);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ma'am! I want another chance, ma'am!
	if (response == "s_877e028c")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Affirmative. Report when your patrol is complete.
			string_id message = new string_id (c_stringFile, "s_8b9926b6");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not in the habit of making excuses.
	if (response == "s_eab1caa8")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Then get back out there and make it happen, pilot!
			string_id message = new string_id (c_stringFile, "s_b5719d30");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch88 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you kidding me?! Not your fault? I have grown so sick of your attitude... I just can't stand it! Tell me you're going to get back up there and do it right this time!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, fine!
	if (response == "s_fc1f4e33")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Fine!
			string_id message = new string_id (c_stringFile, "s_3fa4f0ff");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Listen, lady...
	if (response == "s_2c3fe4f")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Save it! I'm assigning a new mission to you. Don't come back until it's finished!
			string_id message = new string_id (c_stringFile, "s_616dbcc8");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch93 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This cannot stand, pilot! We need you to take down that Veteran TIE pilot!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, yeah...
	if (response == "s_7e1ada01")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: What's wrong with you? Get out there and prove your worth!
			string_id message = new string_id (c_stringFile, "s_ca0c0b13");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative, ma'am!
	if (response == "s_8ef10336")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: Outstanding! Good luck, pilot!
			string_id message = new string_id (c_stringFile, "s_cd1af2b9");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way.
	if (response == "s_e42b260e")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: The Force be with you.
			string_id message = new string_id (c_stringFile, "s_1f7c77cd");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch97 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] grantQuest 1
	//-- NPC: Greetings, pilot! Are you ready for your assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch98 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Focus! You need to get over yourself or you won't be any use to us.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Get on with it, lady.
	if (response == "s_b7b3029f")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You think you can handle a simple four-point patrol?
			string_id message = new string_id (c_stringFile, "s_a5ecfd93");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think, sweetie?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Just four points, huh?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9c9c683");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31123adb");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, okay. I'm sorry.
	if (response == "s_1a1806c1")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: That's better! You're assignment is a four-point patrol above Tatooine. Keep your sensors running the whole time - and when you come back, we'll want to download the data.
			string_id message = new string_id (c_stringFile, "s_9f625af9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back in a snap!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Looking for something in particular?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: When do I get paid?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e28249ac");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aa521016");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2dfa0bf9");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch99 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You think you can handle a simple four-point patrol?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No problem.
	if (response == "s_c261cfae")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: Good luck, pilot.
			string_id message = new string_id (c_stringFile, "s_aceff31e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you think, sweetie?
	if (response == "s_c9c9c683")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			//-- NPC: I think you're going to get fried up there if you don't take this seriously.
			string_id message = new string_id (c_stringFile, "s_91d36035");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think, sweetie?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Just four points, huh?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9c9c683");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31123adb");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just four points, huh?
	if (response == "s_31123adb")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Don't worry, I'll have a gonzo patrol ready for you when you come back!
			string_id message = new string_id (c_stringFile, "s_cac4a12c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think, sweetie?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Just four points, huh?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9c9c683");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31123adb");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch101 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I think you're going to get fried up there if you don't take this seriously.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No problem.
	if (response == "s_c261cfae")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: Good luck, pilot.
			string_id message = new string_id (c_stringFile, "s_aceff31e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you think, sweetie?
	if (response == "s_c9c9c683")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			//-- NPC: I think you're going to get fried up there if you don't take this seriously.
			string_id message = new string_id (c_stringFile, "s_91d36035");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think, sweetie?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Just four points, huh?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9c9c683");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31123adb");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just four points, huh?
	if (response == "s_31123adb")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Don't worry, I'll have a gonzo patrol ready for you when you come back!
			string_id message = new string_id (c_stringFile, "s_cac4a12c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think, sweetie?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Just four points, huh?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9c9c683");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31123adb");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch102 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't worry, I'll have a gonzo patrol ready for you when you come back!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No problem.
	if (response == "s_c261cfae")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: Good luck, pilot.
			string_id message = new string_id (c_stringFile, "s_aceff31e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you think, sweetie?
	if (response == "s_c9c9c683")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			//-- NPC: I think you're going to get fried up there if you don't take this seriously.
			string_id message = new string_id (c_stringFile, "s_91d36035");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think, sweetie?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Just four points, huh?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9c9c683");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31123adb");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just four points, huh?
	if (response == "s_31123adb")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Don't worry, I'll have a gonzo patrol ready for you when you come back!
			string_id message = new string_id (c_stringFile, "s_cac4a12c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think, sweetie?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Just four points, huh?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9c9c683");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31123adb");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch103 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's better! You're assignment is a four-point patrol above Tatooine. Keep your sensors running the whole time - and when you come back, we'll want to download the data.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll be back in a snap!
	if (response == "s_e28249ac")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes. If you're lucky...
			string_id message = new string_id (c_stringFile, "s_4536f86f");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looking for something in particular?
	if (response == "s_aa521016")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Something might be looking for you, pilot... so be careful.
			string_id message = new string_id (c_stringFile, "s_9487e7cd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back in a snap!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Looking for something in particular?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: When do I get paid?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e28249ac");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aa521016");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2dfa0bf9");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: When do I get paid?
	if (response == "s_2dfa0bf9")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: When you're done with the mission, slaymo.
			string_id message = new string_id (c_stringFile, "s_79e3a94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back in a snap!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Looking for something in particular?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: When do I get paid?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e28249ac");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aa521016");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2dfa0bf9");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch105 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Something might be looking for you, pilot... so be careful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll be back in a snap!
	if (response == "s_e28249ac")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes. If you're lucky...
			string_id message = new string_id (c_stringFile, "s_4536f86f");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looking for something in particular?
	if (response == "s_aa521016")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Something might be looking for you, pilot... so be careful.
			string_id message = new string_id (c_stringFile, "s_9487e7cd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back in a snap!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Looking for something in particular?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: When do I get paid?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e28249ac");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aa521016");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2dfa0bf9");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: When do I get paid?
	if (response == "s_2dfa0bf9")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: When you're done with the mission, slaymo.
			string_id message = new string_id (c_stringFile, "s_79e3a94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back in a snap!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Looking for something in particular?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: When do I get paid?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e28249ac");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aa521016");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2dfa0bf9");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch106 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: When you're done with the mission, slaymo.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll be back in a snap!
	if (response == "s_e28249ac")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes. If you're lucky...
			string_id message = new string_id (c_stringFile, "s_4536f86f");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looking for something in particular?
	if (response == "s_aa521016")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Something might be looking for you, pilot... so be careful.
			string_id message = new string_id (c_stringFile, "s_9487e7cd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back in a snap!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Looking for something in particular?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: When do I get paid?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e28249ac");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aa521016");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2dfa0bf9");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: When do I get paid?
	if (response == "s_2dfa0bf9")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: When you're done with the mission, slaymo.
			string_id message = new string_id (c_stringFile, "s_79e3a94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back in a snap!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Looking for something in particular?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: When do I get paid?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e28249ac");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aa521016");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2dfa0bf9");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch107 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we looking for?
	if (response == "s_c7939f2f")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.
			string_id message = new string_id (c_stringFile, "s_ce6b43af");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where am I patrolling?
	if (response == "s_2adb435")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.
			string_id message = new string_id (c_stringFile, "s_9757e73a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll blast any Imperial who crosses my path!
	if (response == "s_7a27e11")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Don't make this more than it is, pilot. This is not 'search and destroy' - this is a sensor sweep. Stay out of trouble. Come back in one piece!
			string_id message = new string_id (c_stringFile, "s_9b562c5a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) The Force be with you.
	if (response == "s_baae2f70")
	{
		doAnimationAction (player, "salute1");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: And with you as well.
			string_id message = new string_id (c_stringFile, "s_d4f7f6d5");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch108 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we looking for?
	if (response == "s_c7939f2f")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.
			string_id message = new string_id (c_stringFile, "s_ce6b43af");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where am I patrolling?
	if (response == "s_2adb435")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.
			string_id message = new string_id (c_stringFile, "s_9757e73a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll blast any Imperial who crosses my path!
	if (response == "s_7a27e11")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Don't make this more than it is, pilot. This is not 'search and destroy' - this is a sensor sweep. Stay out of trouble. Come back in one piece!
			string_id message = new string_id (c_stringFile, "s_9b562c5a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) The Force be with you.
	if (response == "s_baae2f70")
	{
		doAnimationAction (player, "salute1");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: And with you as well.
			string_id message = new string_id (c_stringFile, "s_d4f7f6d5");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch109 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we looking for?
	if (response == "s_c7939f2f")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.
			string_id message = new string_id (c_stringFile, "s_ce6b43af");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where am I patrolling?
	if (response == "s_2adb435")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.
			string_id message = new string_id (c_stringFile, "s_9757e73a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll blast any Imperial who crosses my path!
	if (response == "s_7a27e11")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Don't make this more than it is, pilot. This is not 'search and destroy' - this is a sensor sweep. Stay out of trouble. Come back in one piece!
			string_id message = new string_id (c_stringFile, "s_9b562c5a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) The Force be with you.
	if (response == "s_baae2f70")
	{
		doAnimationAction (player, "salute1");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: And with you as well.
			string_id message = new string_id (c_stringFile, "s_d4f7f6d5");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch110 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't make this more than it is, pilot. This is not 'search and destroy' - this is a sensor sweep. Stay out of trouble. Come back in one piece!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we looking for?
	if (response == "s_c7939f2f")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.
			string_id message = new string_id (c_stringFile, "s_ce6b43af");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where am I patrolling?
	if (response == "s_2adb435")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.
			string_id message = new string_id (c_stringFile, "s_9757e73a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll blast any Imperial who crosses my path!
	if (response == "s_7a27e11")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Don't make this more than it is, pilot. This is not 'search and destroy' - this is a sensor sweep. Stay out of trouble. Come back in one piece!
			string_id message = new string_id (c_stringFile, "s_9b562c5a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) The Force be with you.
	if (response == "s_baae2f70")
	{
		doAnimationAction (player, "salute1");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: And with you as well.
			string_id message = new string_id (c_stringFile, "s_d4f7f6d5");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch112 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't worry. I will be fine.
	if (response == "s_92e1301f")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: The Force be with you, friend...
			string_id message = new string_id (c_stringFile, "s_4e8bbd4d");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What am I looking for?
	if (response == "s_658f174d")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Any Imperial activities. This is a little-used Alliance supply route. It will become more important in the near future - and we are concerned that the Empire has knowledge of our operations.
			string_id message = new string_id (c_stringFile, "s_6360ad02");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How should I stay hidden?
	if (response == "s_93470245")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Just try not to stir up any pirate action - and don't fire on any Imperials unless they threaten you.
			string_id message = new string_id (c_stringFile, "s_5d157c36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch114 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Any Imperial activities. This is a little-used Alliance supply route. It will become more important in the near future - and we are concerned that the Empire has knowledge of our operations.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't worry. I will be fine.
	if (response == "s_92e1301f")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: The Force be with you, friend...
			string_id message = new string_id (c_stringFile, "s_4e8bbd4d");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What am I looking for?
	if (response == "s_658f174d")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Any Imperial activities. This is a little-used Alliance supply route. It will become more important in the near future - and we are concerned that the Empire has knowledge of our operations.
			string_id message = new string_id (c_stringFile, "s_6360ad02");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How should I stay hidden?
	if (response == "s_93470245")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Just try not to stir up any pirate action - and don't fire on any Imperials unless they threaten you.
			string_id message = new string_id (c_stringFile, "s_5d157c36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch115 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Just try not to stir up any pirate action - and don't fire on any Imperials unless they threaten you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't worry. I will be fine.
	if (response == "s_92e1301f")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: The Force be with you, friend...
			string_id message = new string_id (c_stringFile, "s_4e8bbd4d");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What am I looking for?
	if (response == "s_658f174d")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Any Imperial activities. This is a little-used Alliance supply route. It will become more important in the near future - and we are concerned that the Empire has knowledge of our operations.
			string_id message = new string_id (c_stringFile, "s_6360ad02");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How should I stay hidden?
	if (response == "s_93470245")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Just try not to stir up any pirate action - and don't fire on any Imperials unless they threaten you.
			string_id message = new string_id (c_stringFile, "s_5d157c36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch116 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Focus! You need to get over yourself or you won't be any use to us.
			string_id message = new string_id (c_stringFile, "s_a6ec653a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Get on with it, lady.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, okay. I'm sorry.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7b3029f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a1806c1");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Outstanding. Your responsibility is to run a short-range, four-point patrol. When you are finished, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review. Any questions?
			string_id message = new string_id (c_stringFile, "s_4416b2d0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial who crosses my path!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) The Force be with you.
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_baae2f70");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Da'la. I am ready.
	if (response == "s_6bb935f1")
	{
		doAnimationAction (player, "nod_head_once");

		tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Tatooine. We will need your ship's sensor data when you are finished. Stay hidden... sorry to say, I have a bad feeling about this one.
			string_id message = new string_id (c_stringFile, "s_8720792f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, Pilot!  You need to complete a few assignments before I'll train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_38485fa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, Da'la. I am ready.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch117 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good to see you again, pilot! I will hear your report, now.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Brag about blasting TIE fighters)
	if (response == "s_64b4e5f7")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: You have no clue, do you?
			string_id message = new string_id (c_stringFile, "s_d7f5cd5b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Shouldn't you be thanking me?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are you talking about?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Well... I'm sorry.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7e77b672");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2313ac9e");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2427d272");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 118);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Brief Da'la with all due accuracy)
	if (response == "s_97e13b2e")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Hm. This does not sound like a random encounter. I would bet the Empire has knowledge of our operations here. Alliance HQ isn't going to like this report... but I'm very glad you're safe and sound.
			string_id message = new string_id (c_stringFile, "s_e03d1a04");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is our next move, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm ready for my next mission!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74474b33");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bac616e6");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 122);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The patrol was interrupted by a TIE attack.
	if (response == "s_11327023")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			tatooine_rebel_trainer_1_action_rewardQuest1 (player, npc);

			//-- NPC: So the Empire knows a bit more than we expected. This is very bad news, pilot. Alliance HQ will want to learn of this attack as soon as possible.
			string_id message = new string_id (c_stringFile, "s_2784686");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch118 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have no clue, do you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Shouldn't you be thanking me?
	if (response == "s_7e77b672")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			tatooine_rebel_trainer_1_action_rewardQuest1 (player, npc);

			//-- NPC: Probably.
			string_id message = new string_id (c_stringFile, "s_bc865091");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are you talking about?
	if (response == "s_2313ac9e")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			tatooine_rebel_trainer_1_action_rewardQuest1 (player, npc);

			//-- NPC: You got LUCKY! You need to get your act together or you'll be space dust. Understand me?
			string_id message = new string_id (c_stringFile, "s_8a7cca63");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well... I'm sorry.
	if (response == "s_2427d272")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			tatooine_rebel_trainer_1_action_rewardQuest1 (player, npc);

			//-- NPC: I don't want to hear it. Consider yourself lucky to be alive.
			string_id message = new string_id (c_stringFile, "s_71239ef6");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch122 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hm. This does not sound like a random encounter. I would bet the Empire has knowledge of our operations here. Alliance HQ isn't going to like this report... but I'm very glad you're safe and sound.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is our next move, ma'am?
	if (response == "s_74474b33")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			tatooine_rebel_trainer_1_action_rewardQuest1 (player, npc);

			//-- NPC: Our next move? We wait. Alliance HQ is going to want time to review our situation and re-evaluate our combat options. Check back with me later...
			string_id message = new string_id (c_stringFile, "s_a46d9f8b");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready for my next mission!
	if (response == "s_bac616e6")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			tatooine_rebel_trainer_1_action_rewardQuest1 (player, npc);

			//-- NPC: Let's give Alliance HQ a moment to digest this information. A change of plans may be in store...
			string_id message = new string_id (c_stringFile, "s_a018cd7e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch126 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It will take a bit to decode your ship sensor data. In the meantime, we have another assignment for you... if you are ready.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure. Why not.
	if (response == "s_c92d07b4")
	{
		tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Hmph. Take off. Find any Imperial Tie Fighters in the Tatooine system and take out at least four of them.
			string_id message = new string_id (c_stringFile, "s_e28bc942");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More TIE fighters, huh?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where do I find them?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, lady.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e9da2a94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57a53dcf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5aa307dc");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Ma'am! Yes, ma'am!
	if (response == "s_fbac0bde")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Outstanding! Find any Imperial TIE Fighters in the Tatooine system and take out at least four of them.
			string_id message = new string_id (c_stringFile, "s_8001a590");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only TIE fighters, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a78d00e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am more than ready, Da'la.
	if (response == "s_7d15be50")
	{
		tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Excellent. Find any Imperial Tie Fighters in the Tatooine system and take out at least four of them.
			string_id message = new string_id (c_stringFile, "s_306de5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have more target information?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ab56e9c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_290")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not just yet, Pilot!  I have a few more assignments for you before I can train you.  Are you ready to get to work?
			string_id message = new string_id (c_stringFile, "s_6b52240");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure. Why not.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Ma'am! Yes, ma'am!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I am more than ready, Da'la.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c92d07b4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fbac0bde");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7d15be50");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_290");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 126);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch127 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hmph. Take off. Find any Imperial Tie Fighters in the Tatooine system and take out at least four of them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: More TIE fighters, huh?
	if (response == "s_e9da2a94")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just four should do it. We want the Empire to understand that their intel is compromised. This will buy us some time for other operations. Any more questions, or are you ready to shut up and get to work?
			string_id message = new string_id (c_stringFile, "s_949ea1ef");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More TIE fighters, huh?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where do I find them?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, lady.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e9da2a94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57a53dcf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5aa307dc");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find them?
	if (response == "s_57a53dcf")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We can't hold your hand all the time pilot. Search the system, you're bound to find some!
			string_id message = new string_id (c_stringFile, "s_7f3c5f06");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More TIE fighters, huh?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where do I find them?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, lady.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e9da2a94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57a53dcf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5aa307dc");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Piece o'cake, lady.
	if (response == "s_5aa307dc")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: Make sure you stay in one piece, pilot.
			string_id message = new string_id (c_stringFile, "s_434aa00b");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch128 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Just four should do it. We want the Empire to understand that their intel is compromised. This will buy us some time for other operations. Any more questions, or are you ready to shut up and get to work?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: More TIE fighters, huh?
	if (response == "s_e9da2a94")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just four should do it. We want the Empire to understand that their intel is compromised. This will buy us some time for other operations. Any more questions, or are you ready to shut up and get to work?
			string_id message = new string_id (c_stringFile, "s_949ea1ef");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More TIE fighters, huh?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where do I find them?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, lady.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e9da2a94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57a53dcf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5aa307dc");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find them?
	if (response == "s_57a53dcf")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We can't hold your hand all the time pilot. Search the system, you're bound to find some!
			string_id message = new string_id (c_stringFile, "s_7f3c5f06");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More TIE fighters, huh?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where do I find them?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, lady.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e9da2a94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57a53dcf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5aa307dc");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Piece o'cake, lady.
	if (response == "s_5aa307dc")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: Make sure you stay in one piece, pilot.
			string_id message = new string_id (c_stringFile, "s_434aa00b");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch129 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We can't hold your hand all the time pilot. Search the system, you're bound to find some!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: More TIE fighters, huh?
	if (response == "s_e9da2a94")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just four should do it. We want the Empire to understand that their intel is compromised. This will buy us some time for other operations. Any more questions, or are you ready to shut up and get to work?
			string_id message = new string_id (c_stringFile, "s_949ea1ef");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More TIE fighters, huh?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where do I find them?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, lady.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e9da2a94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57a53dcf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5aa307dc");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find them?
	if (response == "s_57a53dcf")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We can't hold your hand all the time pilot. Search the system, you're bound to find some!
			string_id message = new string_id (c_stringFile, "s_7f3c5f06");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More TIE fighters, huh?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where do I find them?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, lady.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e9da2a94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57a53dcf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5aa307dc");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Piece o'cake, lady.
	if (response == "s_5aa307dc")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: Make sure you stay in one piece, pilot.
			string_id message = new string_id (c_stringFile, "s_434aa00b");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch131 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Outstanding! Find any Imperial TIE Fighters in the Tatooine system and take out at least four of them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Only TIE fighters, ma'am?
	if (response == "s_8a78d00e")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Empire does not yet know that their fleet info has been compromised. Strike the new TIE fighters moving into this area with extreme prejudice, and they will have no choice but to halt their invasion strategy.
			string_id message = new string_id (c_stringFile, "s_b0778713");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only TIE fighters, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a78d00e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is the rally point?
	if (response == "s_97e129cd")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There is none, it is your job to find them pilot. Search the Tatooine system for targets of opportunity.
			string_id message = new string_id (c_stringFile, "s_f1147d11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only TIE fighters, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a78d00e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch132 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Empire does not yet know that their fleet info has been compromised. Strike the new TIE fighters moving into this area with extreme prejudice, and they will have no choice but to halt their invasion strategy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Only TIE fighters, ma'am?
	if (response == "s_8a78d00e")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Empire does not yet know that their fleet info has been compromised. Strike the new TIE fighters moving into this area with extreme prejudice, and they will have no choice but to halt their invasion strategy.
			string_id message = new string_id (c_stringFile, "s_b0778713");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only TIE fighters, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a78d00e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is the rally point?
	if (response == "s_97e129cd")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There is none, it is your job to find them pilot. Search the Tatooine system for targets of opportunity.
			string_id message = new string_id (c_stringFile, "s_f1147d11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only TIE fighters, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a78d00e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch133 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There is none, it is your job to find them pilot. Search the Tatooine system for targets of opportunity.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Only TIE fighters, ma'am?
	if (response == "s_8a78d00e")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Empire does not yet know that their fleet info has been compromised. Strike the new TIE fighters moving into this area with extreme prejudice, and they will have no choice but to halt their invasion strategy.
			string_id message = new string_id (c_stringFile, "s_b0778713");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only TIE fighters, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a78d00e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is the rally point?
	if (response == "s_97e129cd")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There is none, it is your job to find them pilot. Search the Tatooine system for targets of opportunity.
			string_id message = new string_id (c_stringFile, "s_f1147d11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only TIE fighters, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a78d00e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch134 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent. Find any Imperial Tie Fighters in the Tatooine system and take out at least four of them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the location?
	if (response == "s_1809c6fc")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There is none, it is your job to find them pilot. Search the Tatooine system for targets of opportunity.
			string_id message = new string_id (c_stringFile, "s_282");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have more target information?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ab56e9c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you have more target information?
	if (response == "s_ab56e9c6")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Empire plans to move several wings of TIE fighters into this zone. If they are allowed to succeed, it could spell disaster for the Alliance here on Tatooine.
			string_id message = new string_id (c_stringFile, "s_a89f2288");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have more target information?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ab56e9c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with you.
	if (response == "s_286")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: And with you as well.
			string_id message = new string_id (c_stringFile, "s_288");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch135 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There is none, it is your job to find them pilot. Search the Tatooine system for targets of opportunity.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the location?
	if (response == "s_1809c6fc")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There is none, it is your job to find them pilot. Search the Tatooine system for targets of opportunity.
			string_id message = new string_id (c_stringFile, "s_282");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have more target information?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ab56e9c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you have more target information?
	if (response == "s_ab56e9c6")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Empire plans to move several wings of TIE fighters into this zone. If they are allowed to succeed, it could spell disaster for the Alliance here on Tatooine.
			string_id message = new string_id (c_stringFile, "s_a89f2288");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have more target information?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ab56e9c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with you.
	if (response == "s_286")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: And with you as well.
			string_id message = new string_id (c_stringFile, "s_288");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch136 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Empire plans to move several wings of TIE fighters into this zone. If they are allowed to succeed, it could spell disaster for the Alliance here on Tatooine.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the location?
	if (response == "s_1809c6fc")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There is none, it is your job to find them pilot. Search the Tatooine system for targets of opportunity.
			string_id message = new string_id (c_stringFile, "s_282");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have more target information?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ab56e9c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you have more target information?
	if (response == "s_ab56e9c6")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Empire plans to move several wings of TIE fighters into this zone. If they are allowed to succeed, it could spell disaster for the Alliance here on Tatooine.
			string_id message = new string_id (c_stringFile, "s_a89f2288");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have more target information?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ab56e9c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with you.
	if (response == "s_286")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: And with you as well.
			string_id message = new string_id (c_stringFile, "s_288");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch138 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not just yet, Pilot!  I have a few more assignments for you before I can train you.  Are you ready to get to work?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure. Why not.
	if (response == "s_c92d07b4")
	{
		tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Hmph. Take off. Find any Imperial Tie Fighters in the Tatooine system and take out at least four of them.
			string_id message = new string_id (c_stringFile, "s_e28bc942");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More TIE fighters, huh?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where do I find them?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, lady.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e9da2a94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57a53dcf");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5aa307dc");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Ma'am! Yes, ma'am!
	if (response == "s_fbac0bde")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Outstanding! Find any Imperial TIE Fighters in the Tatooine system and take out at least four of them.
			string_id message = new string_id (c_stringFile, "s_8001a590");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only TIE fighters, ma'am?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8a78d00e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am more than ready, Da'la.
	if (response == "s_7d15be50")
	{
		tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Excellent. Find any Imperial Tie Fighters in the Tatooine system and take out at least four of them.
			string_id message = new string_id (c_stringFile, "s_306de5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have more target information?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ab56e9c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_290")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not just yet, Pilot!  I have a few more assignments for you before I can train you.  Are you ready to get to work?
			string_id message = new string_id (c_stringFile, "s_6b52240");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure. Why not.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Ma'am! Yes, ma'am!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I am more than ready, Da'la.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c92d07b4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fbac0bde");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7d15be50");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_290");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 126);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch139 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Report mission status!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Brag about blasting even more TIE fighters)
	if (response == "s_6a7a0499")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			tatooine_rebel_trainer_1_action_rewardQuest2 (player, npc);

			//-- NPC: You know, all the pilots say that you're the BEST!
			string_id message = new string_id (c_stringFile, "s_1500b5d6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Whatever...
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Really?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3b6d2f05");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ea6d944");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 140);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: All targets eliminated, ma'am!
	if (response == "s_6d7d5f1b")
	{
		doAnimationAction (player, "salute1");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_rewardQuest2 (player, npc);

			//-- NPC: Outstanding! Check back with me when you are ready for your next assignment!
			string_id message = new string_id (c_stringFile, "s_b91ba199");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The mission is a success, Da'la.
	if (response == "s_dcbc639a")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			tatooine_rebel_trainer_1_action_rewardQuest2 (player, npc);

			//-- NPC: I anticipated as much. Good work, pilot! Check back with me soon for your next assignment.
			string_id message = new string_id (c_stringFile, "s_463f3bbc");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch140 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You know, all the pilots say that you're the BEST!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever...
	if (response == "s_3b6d2f05")
	{
		doAnimationAction (player, "wave_on_dismissing");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Maybe I got that completely backwards. Who knows?
			string_id message = new string_id (c_stringFile, "s_d18e28ba");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Really?
	if (response == "s_2ea6d944")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: No.
			string_id message = new string_id (c_stringFile, "s_299");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch145 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your ship sensor data has been completely decoded. The Alliance supply route is entirely compromised. We need you to scout a new route for our in-system supply convoy. Are you up to it?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, yeah.
	if (response == "s_3f1a842c")
	{
		tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: You'll be the death of me, pilot.
			string_id message = new string_id (c_stringFile, "s_bb68963e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, ma'am!
	if (response == "s_308")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: Outstanding!
			string_id message = new string_id (c_stringFile, "s_310");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course. Those supplies are critical!
	if (response == "s_1e626caa")
	{
		tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Critical to say the least, my friend. We must resume supply flights as soon as possible. To accomplish this, we need you to sweep a five-point area. Again, your ship sensor data will be used to plot an effective route for our larger transports.
			string_id message = new string_id (c_stringFile, "s_ab640430");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Force be with us.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0233b4b");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 148);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me yet?
	if (response == "s_1895ee37")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: %TU, you're too impatient.  Right now I need you to scout that supply route.  We'll talk about training later.
			string_id message = new string_id (c_stringFile, "s_d89a93ff");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, yeah.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Yes, ma'am!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Of course. Those supplies are critical!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me yet?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f1a842c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_308");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1e626caa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1895ee37");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 145);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch148 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Critical to say the least, my friend. We must resume supply flights as soon as possible. To accomplish this, we need you to sweep a five-point area. Again, your ship sensor data will be used to plot an effective route for our larger transports.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with us.
	if (response == "s_d0233b4b")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Always.
			string_id message = new string_id (c_stringFile, "s_ca51315f");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch150 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: %TU, you're too impatient.  Right now I need you to scout that supply route.  We'll talk about training later.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, yeah.
	if (response == "s_3f1a842c")
	{
		tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: You'll be the death of me, pilot.
			string_id message = new string_id (c_stringFile, "s_bb68963e");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, ma'am!
	if (response == "s_308")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: Outstanding!
			string_id message = new string_id (c_stringFile, "s_310");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course. Those supplies are critical!
	if (response == "s_1e626caa")
	{
		tatooine_rebel_trainer_1_action_grantQuestThree (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Critical to say the least, my friend. We must resume supply flights as soon as possible. To accomplish this, we need you to sweep a five-point area. Again, your ship sensor data will be used to plot an effective route for our larger transports.
			string_id message = new string_id (c_stringFile, "s_ab640430");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Force be with us.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0233b4b");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 148);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me yet?
	if (response == "s_1895ee37")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: %TU, you're too impatient.  Right now I need you to scout that supply route.  We'll talk about training later.
			string_id message = new string_id (c_stringFile, "s_d89a93ff");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, yeah.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Yes, ma'am!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Of course. Those supplies are critical!
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me yet?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f1a842c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_308");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1e626caa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1895ee37");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 145);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch151 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I hear you had some surprises up there?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing I couldn't handle.
	if (response == "s_766563c7")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			tatooine_rebel_trainer_1_action_rewardQuest3 (player, npc);

			//-- NPC: Lucky for us.  Here's a little bonus for you.
			string_id message = new string_id (c_stringFile, "s_f2ef125b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's this?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8be95ab4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 152);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I responded to a distress call, ma'am!
	if (response == "s_3ccb2ef2")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_rewardQuest3 (player, npc);

			//-- NPC: Outstanding work, pilot! Get some rest. Check back with me later for a new assignment.  Also, I've got something extra for you.
			string_id message = new string_id (c_stringFile, "s_16949037");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's this?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8be95ab4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 152);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: An Alliance freighter needed my help.
	if (response == "s_9109c202")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			tatooine_rebel_trainer_1_action_rewardQuest3 (player, npc);

			//-- NPC: And you performed beautifully. The Alliance thanks you.  I've got something special for you.
			string_id message = new string_id (c_stringFile, "s_4c1acfbf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's this?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8be95ab4");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 152);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch152 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Lucky for us.  Here's a little bonus for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's this?
	if (response == "s_8be95ab4")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition_isWookieeOrIthorian (player, npc))
		{
			//-- NPC: It's a multipocket bandolier.  I'd get you a flightsuit but I don't think I have one that would fit you... anyway, this will help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f4642ca3");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: It's a flightsuit.  You'll look like a real pilot, now.
			string_id message = new string_id (c_stringFile, "s_5b4a6885");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch155 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Outstanding work, pilot! Get some rest. Check back with me later for a new assignment.  Also, I've got something extra for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's this?
	if (response == "s_8be95ab4")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition_isWookieeOrIthorian (player, npc))
		{
			//-- NPC: It's a multipocket bandolier.  I'd get you a flightsuit but I don't think I have one that would fit you... anyway, this will help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f4642ca3");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: It's a flightsuit.  You'll look like a real pilot, now.
			string_id message = new string_id (c_stringFile, "s_5b4a6885");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch156 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And you performed beautifully. The Alliance thanks you.  I've got something special for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's this?
	if (response == "s_8be95ab4")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition_isWookieeOrIthorian (player, npc))
		{
			//-- NPC: It's a multipocket bandolier.  I'd get you a flightsuit but I don't think I have one that would fit you... anyway, this will help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f4642ca3");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: It's a flightsuit.  You'll look like a real pilot, now.
			string_id message = new string_id (c_stringFile, "s_5b4a6885");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch157 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: While you were away we gathered enough information to put the Imperial fleet movement out of commission. Are you ready to take flight again?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, I'm not getting any younger.
	if (response == "s_cd68fc71")
	{
		tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: This is good - for some day you shall rejoin the infinite... and I may then be allowed to rest.
			string_id message = new string_id (c_stringFile, "s_994615e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's the job, honey?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let's have some details.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ddea1965");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a150013");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 158);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Affirmative!
	if (response == "s_6b490480")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Superb! We have located a rally point from which the Empire has staged their recent aggressions. Go to this point and bring down a veteran TIE fighter pilot - the ringleader, as it were.
			string_id message = new string_id (c_stringFile, "s_2f0da72f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes ma'am!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_640d20f4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 161);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For the Alliance... always.
	if (response == "s_2477af4a")
	{
		tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Then you will be glad to know we have reliable information that the Empire is rallying TIE fighter forces at a nav point high above Tatooine. They are being led by an experienced TIE veteran. Eliminate this TIE veteran pilot with extreme prejudice. This should bring their plans crashing to the ground.
			string_id message = new string_id (c_stringFile, "s_61c8b0c2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance, ma'am.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d693d0");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready for training?
	if (response == "s_f80372b3")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: No, you're not!
			string_id message = new string_id (c_stringFile, "s_22ce0785");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes I am!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eb7aaed2");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 168);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch158 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This is good - for some day you shall rejoin the infinite... and I may then be allowed to rest.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the job, honey?
	if (response == "s_ddea1965")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Nothing you can't handle. Destroy the veteran TIE pilot leader at a specific rally point. Come back here... if you survive.
			string_id message = new string_id (c_stringFile, "s_55ce5bee");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let's have some details.
	if (response == "s_6a150013")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The recent Imperial activity is being led by a veteran TIE fighter pilot. We know of his location. Fly to the nav point, and eliminate this pilot with extreme prejudice.
			string_id message = new string_id (c_stringFile, "s_a20f8416");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch161 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Superb! We have located a rally point from which the Empire has staged their recent aggressions. Go to this point and bring down a veteran TIE fighter pilot - the ringleader, as it were.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes ma'am!
	if (response == "s_640d20f4")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: Good luck, my friend.
			string_id message = new string_id (c_stringFile, "s_c62cae3d");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I know this pilot?
	if (response == "s_87a8cca7")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It should not be difficult. He should be one of very few veteran TIE fighter pilots in this vicinity. But, if you want to be thorough... bring down any TIE fighter pilots you see in this area.
			string_id message = new string_id (c_stringFile, "s_8916a30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes ma'am!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_640d20f4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 161);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch163 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It should not be difficult. He should be one of very few veteran TIE fighter pilots in this vicinity. But, if you want to be thorough... bring down any TIE fighter pilots you see in this area.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes ma'am!
	if (response == "s_640d20f4")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: Good luck, my friend.
			string_id message = new string_id (c_stringFile, "s_c62cae3d");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I know this pilot?
	if (response == "s_87a8cca7")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It should not be difficult. He should be one of very few veteran TIE fighter pilots in this vicinity. But, if you want to be thorough... bring down any TIE fighter pilots you see in this area.
			string_id message = new string_id (c_stringFile, "s_8916a30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes ma'am!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_640d20f4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 161);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch164 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then you will be glad to know we have reliable information that the Empire is rallying TIE fighter forces at a nav point high above Tatooine. They are being led by an experienced TIE veteran. Eliminate this TIE veteran pilot with extreme prejudice. This should bring their plans crashing to the ground.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What more do we know of this TIE veteran?
	if (response == "s_cf04e474")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Not very much. You may find more than one in the general area - but Alliance intelligence indicates that the Empire has charged one veteran TIE pilot with coordinating aggressions in this system.
			string_id message = new string_id (c_stringFile, "s_f4cc85fa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance, ma'am.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d693d0");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other targets are in the area?
	if (response == "s_d779178a")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Expect the rally point to be patrolled by junior TIE cadets. They will be quick to respond if they see you first - so be very careful. Keep your eye out for the veteran TIE fighter pilot, eliminate him, and get back here as soon as you can.
			string_id message = new string_id (c_stringFile, "s_c3d77bb4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance, ma'am.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d693d0");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will bring honor to the Alliance, ma'am.
	if (response == "s_3d693d0")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			doAnimationAction (player, "bow");

			//-- NPC: You already have. Good luck to you... I hope to see you again very soon.
			string_id message = new string_id (c_stringFile, "s_e24d9d51");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch165 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not very much. You may find more than one in the general area - but Alliance intelligence indicates that the Empire has charged one veteran TIE pilot with coordinating aggressions in this system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What more do we know of this TIE veteran?
	if (response == "s_cf04e474")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Not very much. You may find more than one in the general area - but Alliance intelligence indicates that the Empire has charged one veteran TIE pilot with coordinating aggressions in this system.
			string_id message = new string_id (c_stringFile, "s_f4cc85fa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance, ma'am.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d693d0");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other targets are in the area?
	if (response == "s_d779178a")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Expect the rally point to be patrolled by junior TIE cadets. They will be quick to respond if they see you first - so be very careful. Keep your eye out for the veteran TIE fighter pilot, eliminate him, and get back here as soon as you can.
			string_id message = new string_id (c_stringFile, "s_c3d77bb4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance, ma'am.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d693d0");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will bring honor to the Alliance, ma'am.
	if (response == "s_3d693d0")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			doAnimationAction (player, "bow");

			//-- NPC: You already have. Good luck to you... I hope to see you again very soon.
			string_id message = new string_id (c_stringFile, "s_e24d9d51");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch166 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Expect the rally point to be patrolled by junior TIE cadets. They will be quick to respond if they see you first - so be very careful. Keep your eye out for the veteran TIE fighter pilot, eliminate him, and get back here as soon as you can.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What more do we know of this TIE veteran?
	if (response == "s_cf04e474")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Not very much. You may find more than one in the general area - but Alliance intelligence indicates that the Empire has charged one veteran TIE pilot with coordinating aggressions in this system.
			string_id message = new string_id (c_stringFile, "s_f4cc85fa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance, ma'am.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d693d0");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other targets are in the area?
	if (response == "s_d779178a")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Expect the rally point to be patrolled by junior TIE cadets. They will be quick to respond if they see you first - so be very careful. Keep your eye out for the veteran TIE fighter pilot, eliminate him, and get back here as soon as you can.
			string_id message = new string_id (c_stringFile, "s_c3d77bb4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance, ma'am.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d693d0");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will bring honor to the Alliance, ma'am.
	if (response == "s_3d693d0")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			doAnimationAction (player, "bow");

			//-- NPC: You already have. Good luck to you... I hope to see you again very soon.
			string_id message = new string_id (c_stringFile, "s_e24d9d51");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch168 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No, you're not!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I am!
	if (response == "s_eb7aaed2")
	{
		doAnimationAction (player, "pose_proudly");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Well... maybe you are.  But this is more important, right now.  I'll make a deal with you, complete this mission and THEN I will train you.  This is urgent, so I hope you're ready.
			string_id message = new string_id (c_stringFile, "s_b9c90f82");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, I'm not getting any younger.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Affirmative!
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: For the Alliance... always.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready for training?
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cd68fc71");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6b490480");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2477af4a");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f80372b3");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 157);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch169 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well... maybe you are.  But this is more important, right now.  I'll make a deal with you, complete this mission and THEN I will train you.  This is urgent, so I hope you're ready.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, I'm not getting any younger.
	if (response == "s_cd68fc71")
	{
		tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: This is good - for some day you shall rejoin the infinite... and I may then be allowed to rest.
			string_id message = new string_id (c_stringFile, "s_994615e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's the job, honey?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let's have some details.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ddea1965");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a150013");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 158);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Affirmative!
	if (response == "s_6b490480")
	{
		doAnimationAction (player, "salute2");

		tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Superb! We have located a rally point from which the Empire has staged their recent aggressions. Go to this point and bring down a veteran TIE fighter pilot - the ringleader, as it were.
			string_id message = new string_id (c_stringFile, "s_2f0da72f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes ma'am!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_640d20f4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 161);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For the Alliance... always.
	if (response == "s_2477af4a")
	{
		tatooine_rebel_trainer_1_action_grantQuestFour (player, npc);

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Then you will be glad to know we have reliable information that the Empire is rallying TIE fighter forces at a nav point high above Tatooine. They are being led by an experienced TIE veteran. Eliminate this TIE veteran pilot with extreme prejudice. This should bring their plans crashing to the ground.
			string_id message = new string_id (c_stringFile, "s_61c8b0c2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance, ma'am.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d693d0");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready for training?
	if (response == "s_f80372b3")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: No, you're not!
			string_id message = new string_id (c_stringFile, "s_22ce0785");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes I am!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eb7aaed2");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 168);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch170 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Report on the TIE veteran target!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Brag about defeating the TIE Veteran)
	if (response == "s_a8b44679")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "yawn");

			//-- NPC: What was that? I didn't catch that...
			string_id message = new string_id (c_stringFile, "s_4ba4be80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Brag more)
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: The TIE Vet is dead, okay?
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f30cd7b");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_40440fa");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 171);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Mission accomplished, ma'am!
	if (response == "s_ce2c09ec")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_rewardQuest4 (player, npc);

			//-- NPC: Great work! Now, get some rest and check back with me later. I will have more for you to do.
			string_id message = new string_id (c_stringFile, "s_2da98563");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: He has joined the infinite, Da'la.
	if (response == "s_67104689")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			tatooine_rebel_trainer_1_action_rewardQuest4 (player, npc);

			//-- NPC: You bring honor to the Alliance, my friend. This was no easy task.
			string_id message = new string_id (c_stringFile, "s_95ad8f55");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: For the Alliance!
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: The Force be with you.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_364");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_368");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 175);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch171 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What was that? I didn't catch that...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Brag more)
	if (response == "s_3f30cd7b")
	{
		doAnimationAction (player, "gesticulate_wildly");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "hug_self");

			//-- NPC: So thrilling... 
			string_id message = new string_id (c_stringFile, "s_3cb5ab17");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The TIE Vet is dead, okay?
	if (response == "s_40440fa")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			tatooine_rebel_trainer_1_action_rewardQuest4 (player, npc);

			//-- NPC: That's good to know. You're dismissed.
			string_id message = new string_id (c_stringFile, "s_5c7ecf43");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch175 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You bring honor to the Alliance, my friend. This was no easy task.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For the Alliance!
	if (response == "s_364")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			doAnimationAction (player, "salute2");

			//-- NPC: For the Alliance!
			string_id message = new string_id (c_stringFile, "s_366");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with you.
	if (response == "s_368")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: And also with you.
			string_id message = new string_id (c_stringFile, "s_30c1fec7");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch178 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Congratulations, pilot. Our immediate goals have been met. We have time for training.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What kind of training?
	if (response == "s_413")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: It looks like you're up for some basic starfighter training.
			string_id message = new string_id (c_stringFile, "s_414");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_415");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 179);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch179 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're up for some basic starfighter training.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright.
	if (response == "s_415")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_giveSkill1 (player, npc);

			//-- NPC: Let's upgrade your starship license then, shall we?
			string_id message = new string_id (c_stringFile, "s_418");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch181 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There is more I have to teach you, pilot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What should I learn today?
	if (response == "s_419")
	{
		//-- [NOTE] 
		if (!tatooine_rebel_trainer_1_condition_hasStarships1 (player, npc))
		{
			//-- NPC: It looks like you're up for some basic starfighter training.
			string_id message = new string_id (c_stringFile, "s_420");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_425");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 182);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!tatooine_rebel_trainer_1_condition_hasWeapons1 (player, npc))
		{
			//-- NPC: It looks like you're up for some training in basic Alliance component use.
			string_id message = new string_id (c_stringFile, "s_421");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_426");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 184);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!tatooine_rebel_trainer_1_condition_hasProcedures1 (player, npc))
		{
			//-- NPC: It looks like you're up for some training in starfighter survival tactics.
			string_id message = new string_id (c_stringFile, "s_422");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_428");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 186);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: It looks like you're up for some training in droid interface basics.
			string_id message = new string_id (c_stringFile, "s_423");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_430");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 188);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch182 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're up for some basic starfighter training.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright.
	if (response == "s_425")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_giveSkill1 (player, npc);

			//-- NPC: Let's upgrade your starship license then, shall we?
			string_id message = new string_id (c_stringFile, "s_432");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch184 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're up for some training in basic Alliance component use.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright.
	if (response == "s_426")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_giveSkill2 (player, npc);

			//-- NPC: I have adjusted your pilot certification to handle more intricate components.
			string_id message = new string_id (c_stringFile, "s_434");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch186 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're up for some training in starfighter survival tactics.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright.
	if (response == "s_428")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_giveSkill3 (player, npc);

			//-- NPC: Here is an update to your procedures manual.
			string_id message = new string_id (c_stringFile, "s_436");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch188 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're up for some training in droid interface basics.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright.
	if (response == "s_430")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tatooine_rebel_trainer_1_action_giveSkill4 (player, npc);

			//-- NPC: Here are some new droid programs. Use them wisely.
			string_id message = new string_id (c_stringFile, "s_438");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch190 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good day, pilot. Are you here for elective duty mission? There is still plenty of work for us here in Tatooine.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is a good way to earn experience. Especially for a pilot looking for training like yourself. Duty missions have no real end. You just finish when you finish, know what I mean? So what do you say?
			string_id message = new string_id (c_stringFile, "s_cb60fa8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, thanks.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready to enlist with the ground forces!
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6e646769");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e0cb3e19");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 190);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am interested in a duty mission.
	if (response == "s_4ffd4b9d")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Great. You can either patrol the Tatooine system looking for Imperials to send to the scrapyard, or you can help escort Alliance transports. What sounds good to you?
			string_id message = new string_id (c_stringFile, "s_bf55aded");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would like to patrol Tatooine space.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I feel our transports need escort...
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a0aba27");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2cb7683a");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 192);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, thanks.
	if (response == "s_6e646769")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			//-- NPC: The Force be with you, then...
			string_id message = new string_id (c_stringFile, "s_1c2f3f24");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to enlist with the ground forces!
	if (response == "s_e0cb3e19")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Are you sure about that, %NU?  We could officially put your name on the roster, but even if you remain Covert, there's always a chance of an Imperial uncovering you.
			string_id message = new string_id (c_stringFile, "s_8da0fa08");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, never mind.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I know the dangers
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d01d0154");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_831a22ee");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 196);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch191 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A duty mission is a good way to earn experience. Especially for a pilot looking for training like yourself. Duty missions have no real end. You just finish when you finish, know what I mean? So what do you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is a good way to earn experience. Especially for a pilot looking for training like yourself. Duty missions have no real end. You just finish when you finish, know what I mean? So what do you say?
			string_id message = new string_id (c_stringFile, "s_cb60fa8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, thanks.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready to enlist with the ground forces!
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6e646769");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e0cb3e19");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 190);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am interested in a duty mission.
	if (response == "s_4ffd4b9d")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Great. You can either patrol the Tatooine system looking for Imperials to send to the scrapyard, or you can help escort Alliance transports. What sounds good to you?
			string_id message = new string_id (c_stringFile, "s_bf55aded");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would like to patrol Tatooine space.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I feel our transports need escort...
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a0aba27");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2cb7683a");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 192);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, thanks.
	if (response == "s_6e646769")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			//-- NPC: The Force be with you, then...
			string_id message = new string_id (c_stringFile, "s_1c2f3f24");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to enlist with the ground forces!
	if (response == "s_e0cb3e19")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Are you sure about that, %NU?  We could officially put your name on the roster, but even if you remain Covert, there's always a chance of an Imperial uncovering you.
			string_id message = new string_id (c_stringFile, "s_8da0fa08");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, never mind.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I know the dangers
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d01d0154");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_831a22ee");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 196);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch192 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Great. You can either patrol the Tatooine system looking for Imperials to send to the scrapyard, or you can help escort Alliance transports. What sounds good to you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would like to patrol Tatooine space.
	if (response == "s_6a0aba27")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			tatooine_rebel_trainer_1_action_grantDutyDESTROY_1 (player, npc);

			//-- NPC: Interesting. Alliance HQ has a number of possible Imperial rally points for you to investigate. These 'duty' missions have been prepared for you to gain some much needed space combat experience. You can return to the ground at any time when you feel that you have learned enough. Good luck!
			string_id message = new string_id (c_stringFile, "s_66dc0f99");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I feel our transports need escort...
	if (response == "s_2cb7683a")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			tatooine_rebel_trainer_1_action_grantDutyESCORT_1 (player, npc);

			//-- NPC: You're right. The Alliance is attempting to move a large number of refugees out of Tatooine space. Protect as many of them as you can. These 'duty' missions have been prepared for you to gain some much needed space combat experience. You can return to the ground at any time when you feel that you have learned enough.
			string_id message = new string_id (c_stringFile, "s_d6e44b20");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch196 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you sure about that, %NU?  We could officially put your name on the roster, but even if you remain Covert, there's always a chance of an Imperial uncovering you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, never mind.
	if (response == "s_d01d0154")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: So would you like a duty?
			string_id message = new string_id (c_stringFile, "s_236daee8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, thanks.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready to enlist with the ground forces!
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6e646769");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e0cb3e19");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 190);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know the dangers
	if (response == "s_831a22ee")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Very well, %NU. You can change your level of commitment to the Rebellion using the /pvp command. This will allow you to switch between being a combatant and in special forces.
			string_id message = new string_id (c_stringFile, "s_618c3a1c");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_rebel_trainer_1_handleBranch197 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So would you like a duty?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is a good way to earn experience. Especially for a pilot looking for training like yourself. Duty missions have no real end. You just finish when you finish, know what I mean? So what do you say?
			string_id message = new string_id (c_stringFile, "s_cb60fa8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, thanks.
			boolean hasResponse2 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready to enlist with the ground forces!
			boolean hasResponse3 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6e646769");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e0cb3e19");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 190);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am interested in a duty mission.
	if (response == "s_4ffd4b9d")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Great. You can either patrol the Tatooine system looking for Imperials to send to the scrapyard, or you can help escort Alliance transports. What sounds good to you?
			string_id message = new string_id (c_stringFile, "s_bf55aded");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would like to patrol Tatooine space.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I feel our transports need escort...
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a0aba27");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2cb7683a");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 192);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, thanks.
	if (response == "s_6e646769")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			//-- NPC: The Force be with you, then...
			string_id message = new string_id (c_stringFile, "s_1c2f3f24");
			utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to enlist with the ground forces!
	if (response == "s_e0cb3e19")
	{
		//-- [NOTE] 
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Are you sure about that, %NU?  We could officially put your name on the roster, but even if you remain Covert, there's always a chance of an Imperial uncovering you.
			string_id message = new string_id (c_stringFile, "s_8da0fa08");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, never mind.
			boolean hasResponse0 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I know the dangers
			boolean hasResponse1 = false;
			if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d01d0154");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_831a22ee");

				utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 196);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.tatooine_rebel_trainer_1");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );
	factions.setFaction( self, factions.FACTION_REBEL );

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );
	factions.setFaction( self, factions.FACTION_REBEL );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.tatooine_rebel_trainer_1");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_hasVeryFirstQuest (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: You've got your assignment, and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
		string_id message = new string_id (c_stringFile, "s_c19ed115");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: So now what?
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: How do I get back from space?
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Where's my starship?
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8a4652c6");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_680cda08");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5bfac9f1");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 1);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_hasSpaceExpansion (player, npc))
	{
		doAnimationAction (npc, "shake_head_no");

		//-- NPC: You don't look like any sort of pilot to me.  I can't help you.
		string_id message = new string_id (c_stringFile, "s_1d3d8b65");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_isPrivateerPilot (player, npc))
	{
		doAnimationAction (npc, "wave_on_dismissing");

		//-- NPC: I can't help you, smuggler. Try sniffing around Wayfar... you'll find something that suits you.
		string_id message = new string_id (c_stringFile, "s_19d3ee2b");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_isImperialPilot (player, npc))
	{
		doAnimationAction (npc, "threaten");

		//-- NPC: Heh. What do you know. An Imperial. Get out of here while you still can!
		string_id message = new string_id (c_stringFile, "s_d6c8f764");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_isNotRebel (player, npc))
	{
		//-- NPC: If we are to have any dealings together, you will need to officially join our side. Go find a Recruiter and sign on the dotted line friend.
		string_id message = new string_id (c_stringFile, "s_eff4eef6");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] This is what the NPC says when he talks to a player who is not currently signed-up with any pilot profession.
	if (!tatooine_rebel_trainer_1_condition_hasNoviceSkillBox (player, npc))
	{
		doAnimationAction (npc, "greet");

		tatooine_rebel_trainer_1_action_eraseClientPath (player, npc);

		//-- NPC: Good to meet you. My name is Da'la Socuna and I am here on behalf of the Rebel Alliance naval forces. I train pilots to fly and fight.
		string_id message = new string_id (c_stringFile, "s_1622e456");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sounds like fun. Where do I sign?
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I know of the rebellion. I want to help!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: We are kindred spirits, Da'la Socuna.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_78679b31");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3f5c320e");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_db6b2243");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 12);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_onMyTrack (player, npc))
	{
		//-- NPC: Always good to see another Rebel pilot... but you're not one of my recruits. What can I do for you?
		string_id message = new string_id (c_stringFile, "s_57c3f83e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Do you have any missions I could fly?
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1583743c");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 42);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_hasSpaceShip (player, npc))
	{
		doAnimationAction (npc, "shake_head_disgust");

		tatooine_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- NPC: Just how exactly do you expect to be a Rebel Alliance pilot without a ship?  Here.  I'll add these access codes for a small fighter to your datapad.  You ought to upgrade to a better ship pretty soon, though.
		string_id message = new string_id (c_stringFile, "s_19ec1fce");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yeah, yeah!
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Ma'am! Thank you, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: For the Alliance!
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7553991");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_288ce434");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a994ca63");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 45);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You are a member of the Smuggler's Alliance and you already have a quest.  Go do it!
	if (tatooine_rebel_trainer_1_condition_hasRebelQuest (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: The duty logs indicate that you are in mid-mission. We have nothing to discuss until you are finished.
		string_id message = new string_id (c_stringFile, "s_e31d65b7");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_is_GRADUATED_goto_Yavin4 (player, npc))
	{
		doAnimationAction (npc, "wave_on_dismissing");

		//-- NPC: You're not supposed to be here, pilot. You're assigned to Major Eker's group on the moon Yavin IV.
		string_id message = new string_id (c_stringFile, "s_2e56f341");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: How do I get there?
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b3bebc5e");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 50);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have all the tier-1 skills, so would you like some tier-2 content?
	if (tatooine_rebel_trainer_1_condition_hasAllTierOneSkills (player, npc))
	{
		doAnimationAction (npc, "salute2");

		tatooine_rebel_trainer_1_action_set_GRADUATED_goto_Yavin4 (player, npc);

		//-- NPC: It appears that our time together has come to an end, pilot. Alliance HQ wants you reallocated to special operations on Yavin IV and placed under the command of Major Eker... immediately.
		string_id message = new string_id (c_stringFile, "s_63642f8");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Okay. Bye!
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Yes, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: If it is my duty, Da'la.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_abc65d0");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f843dc6a");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d8913d37");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 52);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_hasFailedQuestOne_PATROL (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: Report, pilot!
		string_id message = new string_id (c_stringFile, "s_fb9b39e5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I was unable to finish.
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I failed the mission.
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4f90bab3");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b9c55cab");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 61);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_hasFailedQuestOne (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: Report!
		string_id message = new string_id (c_stringFile, "s_fe789dac");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I failed the mission.
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I could not finish the mission.
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_151");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_68ee2577");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 68);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_hasFailedQuestTwo (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: I don't like what I'm hearing, pilot! Report, now!
		string_id message = new string_id (c_stringFile, "s_9c146b24");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: It wasn't my fault!
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Sorry, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I failed to destroy the targets.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6657b5a3");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_740e2e66");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_54ec182b");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 76);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_hasFailedQuestThree (player, npc))
	{
		doAnimationAction (npc, "shake_head_disgust");

		//-- NPC: I already know what happened. Do you have anything to say for yourself?
		string_id message = new string_id (c_stringFile, "s_a87c06b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: It wasn't my fault!
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Ma'am! I want another chance, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm not in the habit of making excuses.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_190");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_877e028c");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_eab1caa8");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 87);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_hasFailedQuestFour (player, npc))
	{
		doAnimationAction (npc, "pound_fist_palm");

		//-- NPC: This cannot stand, pilot! We need you to take down that Veteran TIE pilot!
		string_id message = new string_id (c_stringFile, "s_fcd9ce0e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yeah, yeah...
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Affirmative, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm on my way.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7e1ada01");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8ef10336");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 93);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] grantQuest 1
	if (!tatooine_rebel_trainer_1_condition_hasCompletedQuestOne (player, npc))
	{
		doAnimationAction (npc, "salute1");

		//-- NPC: Greetings, pilot! Are you ready for your assignment?
		string_id message = new string_id (c_stringFile, "s_784540b7");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Shrug and smile.)
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Affirmative!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Yes, Da'la. I am ready.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Will you train me after this?
		boolean hasResponse3 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6bb935f1");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 97);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_beenRewardedForQuestOne (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: Good to see you again, pilot! I will hear your report, now.
		string_id message = new string_id (c_stringFile, "s_2c052357");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Brag about blasting TIE fighters)
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Brief Da'la with all due accuracy)
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: The patrol was interrupted by a TIE attack.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_64b4e5f7");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_97e13b2e");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_11327023");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 117);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_hasCompletedQuestTwo (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: It will take a bit to decode your ship sensor data. In the meantime, we have another assignment for you... if you are ready.
		string_id message = new string_id (c_stringFile, "s_406f8d3e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sure. Why not.
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Ma'am! Yes, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I am more than ready, Da'la.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Will you train me after this?
		boolean hasResponse3 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c92d07b4");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_fbac0bde");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7d15be50");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_290");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 126);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_beenRewardedForQuestTwo (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: Report mission status!
		string_id message = new string_id (c_stringFile, "s_1f0828c5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Brag about blasting even more TIE fighters)
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: All targets eliminated, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: The mission is a success, Da'la.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6a7a0499");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6d7d5f1b");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_dcbc639a");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 139);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_hasCompletedQuestThree (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Your ship sensor data has been completely decoded. The Alliance supply route is entirely compromised. We need you to scout a new route for our in-system supply convoy. Are you up to it?
		string_id message = new string_id (c_stringFile, "s_bda1eb17");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yeah, yeah.
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Yes, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Of course. Those supplies are critical!
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Will you train me yet?
		boolean hasResponse3 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3f1a842c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_308");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1e626caa");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1895ee37");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 145);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_beenRewardedForQuestThree (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: I hear you had some surprises up there?
		string_id message = new string_id (c_stringFile, "s_52efbcd5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Nothing I couldn't handle.
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I responded to a distress call, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: An Alliance freighter needed my help.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_766563c7");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3ccb2ef2");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9109c202");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 151);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_hasCompletedQuestFour (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: While you were away we gathered enough information to put the Imperial fleet movement out of commission. Are you ready to take flight again?
		string_id message = new string_id (c_stringFile, "s_ae1edd02");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Well, I'm not getting any younger.
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Affirmative!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: For the Alliance... always.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'm ready for training?
		boolean hasResponse3 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_cd68fc71");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6b490480");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2477af4a");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f80372b3");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 157);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!tatooine_rebel_trainer_1_condition_beenRewardedForQuestFour (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: Report on the TIE veteran target!
		string_id message = new string_id (c_stringFile, "s_2f8ec5d4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Brag about defeating the TIE Veteran)
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Mission accomplished, ma'am!
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: He has joined the infinite, Da'la.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a8b44679");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ce2c09ec");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_67104689");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 170);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_isReadyForFirstTraining (player, npc))
	{
		doAnimationAction (npc, "nod_head_once");

		//-- NPC: Congratulations, pilot. Our immediate goals have been met. We have time for training.
		string_id message = new string_id (c_stringFile, "s_6221bd32");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What kind of training?
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_413");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 178);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition_isReadyForMoreTraining (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: There is more I have to teach you, pilot.
		string_id message = new string_id (c_stringFile, "s_2c8ee9d");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What should I learn today?
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_419");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 181);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "salute2");

		tatooine_rebel_trainer_1_action_sendVourkSignal (player, npc);

		//-- NPC: Good day, pilot. Are you here for elective duty mission? There is still plenty of work for us here in Tatooine.
		string_id message = new string_id (c_stringFile, "s_59c382bc");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What is a duty mission?
		boolean hasResponse0 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I am interested in a duty mission.
		boolean hasResponse1 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: No, thanks.
		boolean hasResponse2 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'm ready to enlist with the ground forces!
		boolean hasResponse3 = false;
		if (tatooine_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6e646769");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e0cb3e19");

			utils.setScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId", 190);

			npcStartConversation (player, npc, "tatooine_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "tatooine_rebel_trainer_1")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

	if (branchId == 1 && tatooine_rebel_trainer_1_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && tatooine_rebel_trainer_1_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && tatooine_rebel_trainer_1_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && tatooine_rebel_trainer_1_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && tatooine_rebel_trainer_1_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && tatooine_rebel_trainer_1_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && tatooine_rebel_trainer_1_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && tatooine_rebel_trainer_1_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && tatooine_rebel_trainer_1_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && tatooine_rebel_trainer_1_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && tatooine_rebel_trainer_1_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && tatooine_rebel_trainer_1_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && tatooine_rebel_trainer_1_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && tatooine_rebel_trainer_1_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && tatooine_rebel_trainer_1_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && tatooine_rebel_trainer_1_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && tatooine_rebel_trainer_1_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && tatooine_rebel_trainer_1_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && tatooine_rebel_trainer_1_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && tatooine_rebel_trainer_1_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && tatooine_rebel_trainer_1_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && tatooine_rebel_trainer_1_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && tatooine_rebel_trainer_1_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && tatooine_rebel_trainer_1_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && tatooine_rebel_trainer_1_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && tatooine_rebel_trainer_1_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && tatooine_rebel_trainer_1_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && tatooine_rebel_trainer_1_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && tatooine_rebel_trainer_1_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && tatooine_rebel_trainer_1_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && tatooine_rebel_trainer_1_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && tatooine_rebel_trainer_1_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && tatooine_rebel_trainer_1_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && tatooine_rebel_trainer_1_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && tatooine_rebel_trainer_1_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 40 && tatooine_rebel_trainer_1_handleBranch40 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && tatooine_rebel_trainer_1_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && tatooine_rebel_trainer_1_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && tatooine_rebel_trainer_1_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && tatooine_rebel_trainer_1_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 50 && tatooine_rebel_trainer_1_handleBranch50 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && tatooine_rebel_trainer_1_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && tatooine_rebel_trainer_1_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 56 && tatooine_rebel_trainer_1_handleBranch56 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 57 && tatooine_rebel_trainer_1_handleBranch57 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && tatooine_rebel_trainer_1_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 59 && tatooine_rebel_trainer_1_handleBranch59 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && tatooine_rebel_trainer_1_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && tatooine_rebel_trainer_1_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && tatooine_rebel_trainer_1_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 68 && tatooine_rebel_trainer_1_handleBranch68 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 69 && tatooine_rebel_trainer_1_handleBranch69 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 73 && tatooine_rebel_trainer_1_handleBranch73 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 76 && tatooine_rebel_trainer_1_handleBranch76 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 77 && tatooine_rebel_trainer_1_handleBranch77 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 81 && tatooine_rebel_trainer_1_handleBranch81 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 84 && tatooine_rebel_trainer_1_handleBranch84 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 87 && tatooine_rebel_trainer_1_handleBranch87 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 88 && tatooine_rebel_trainer_1_handleBranch88 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 93 && tatooine_rebel_trainer_1_handleBranch93 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 97 && tatooine_rebel_trainer_1_handleBranch97 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 98 && tatooine_rebel_trainer_1_handleBranch98 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 99 && tatooine_rebel_trainer_1_handleBranch99 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 101 && tatooine_rebel_trainer_1_handleBranch101 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 102 && tatooine_rebel_trainer_1_handleBranch102 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 103 && tatooine_rebel_trainer_1_handleBranch103 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 105 && tatooine_rebel_trainer_1_handleBranch105 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 106 && tatooine_rebel_trainer_1_handleBranch106 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 107 && tatooine_rebel_trainer_1_handleBranch107 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 108 && tatooine_rebel_trainer_1_handleBranch108 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 109 && tatooine_rebel_trainer_1_handleBranch109 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 110 && tatooine_rebel_trainer_1_handleBranch110 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 112 && tatooine_rebel_trainer_1_handleBranch112 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 114 && tatooine_rebel_trainer_1_handleBranch114 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 115 && tatooine_rebel_trainer_1_handleBranch115 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 116 && tatooine_rebel_trainer_1_handleBranch116 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 117 && tatooine_rebel_trainer_1_handleBranch117 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 118 && tatooine_rebel_trainer_1_handleBranch118 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 122 && tatooine_rebel_trainer_1_handleBranch122 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 126 && tatooine_rebel_trainer_1_handleBranch126 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 127 && tatooine_rebel_trainer_1_handleBranch127 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 128 && tatooine_rebel_trainer_1_handleBranch128 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 129 && tatooine_rebel_trainer_1_handleBranch129 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 131 && tatooine_rebel_trainer_1_handleBranch131 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 132 && tatooine_rebel_trainer_1_handleBranch132 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 133 && tatooine_rebel_trainer_1_handleBranch133 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 134 && tatooine_rebel_trainer_1_handleBranch134 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 135 && tatooine_rebel_trainer_1_handleBranch135 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 136 && tatooine_rebel_trainer_1_handleBranch136 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 138 && tatooine_rebel_trainer_1_handleBranch138 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 139 && tatooine_rebel_trainer_1_handleBranch139 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 140 && tatooine_rebel_trainer_1_handleBranch140 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 145 && tatooine_rebel_trainer_1_handleBranch145 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 148 && tatooine_rebel_trainer_1_handleBranch148 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 150 && tatooine_rebel_trainer_1_handleBranch150 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 151 && tatooine_rebel_trainer_1_handleBranch151 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 152 && tatooine_rebel_trainer_1_handleBranch152 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 155 && tatooine_rebel_trainer_1_handleBranch155 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 156 && tatooine_rebel_trainer_1_handleBranch156 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 157 && tatooine_rebel_trainer_1_handleBranch157 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 158 && tatooine_rebel_trainer_1_handleBranch158 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 161 && tatooine_rebel_trainer_1_handleBranch161 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 163 && tatooine_rebel_trainer_1_handleBranch163 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 164 && tatooine_rebel_trainer_1_handleBranch164 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 165 && tatooine_rebel_trainer_1_handleBranch165 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 166 && tatooine_rebel_trainer_1_handleBranch166 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 168 && tatooine_rebel_trainer_1_handleBranch168 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 169 && tatooine_rebel_trainer_1_handleBranch169 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 170 && tatooine_rebel_trainer_1_handleBranch170 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 171 && tatooine_rebel_trainer_1_handleBranch171 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 175 && tatooine_rebel_trainer_1_handleBranch175 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 178 && tatooine_rebel_trainer_1_handleBranch178 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 179 && tatooine_rebel_trainer_1_handleBranch179 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 181 && tatooine_rebel_trainer_1_handleBranch181 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 182 && tatooine_rebel_trainer_1_handleBranch182 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 184 && tatooine_rebel_trainer_1_handleBranch184 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 186 && tatooine_rebel_trainer_1_handleBranch186 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 188 && tatooine_rebel_trainer_1_handleBranch188 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 190 && tatooine_rebel_trainer_1_handleBranch190 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 191 && tatooine_rebel_trainer_1_handleBranch191 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 192 && tatooine_rebel_trainer_1_handleBranch192 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 196 && tatooine_rebel_trainer_1_handleBranch196 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 197 && tatooine_rebel_trainer_1_handleBranch197 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.tatooine_rebel_trainer_1.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

