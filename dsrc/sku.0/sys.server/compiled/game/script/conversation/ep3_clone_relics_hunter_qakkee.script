// ======================================================================
//
// ep3_clone_relics_hunter_qakkee.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.features;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/ep3_clone_relics_hunter_qakkee";

// ======================================================================
// Script Conditions
// ======================================================================

boolean ep3_clone_relics_hunter_qakkee_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedAllQuests (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_hunter_qakkee_5"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskOne (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedTask(player, "ep3_clone_relics_hunter_qakkee_1", "killDewbacks"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskTwo (obj_id player, obj_id npc)
{
	// Returns TRUE if this quest is complete
	return (groundquests.hasCompletedTask(player, "ep3_clone_relics_hunter_qakkee_2", "killSnorbal"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_isOnQuestOne (obj_id player, obj_id npc)
{
	// Returns TRUE if the quest is active on the player
	return (groundquests.isTaskActive(player, "ep3_clone_relics_hunter_qakkee_1", "killDewbacks"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_isOnQuestTwo (obj_id player, obj_id npc)
{
	// Returns TRUE if the quest is active on the player
	return (groundquests.isQuestActive(player, "ep3_clone_relics_hunter_qakkee_2"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedQuestTwo (obj_id player, obj_id npc)
{
	// Returns TRUE if this quest is complete
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_hunter_qakkee_2"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskThree (obj_id player, obj_id npc)
{
	// Returns TRUE if this quest is complete
	return (groundquests.hasCompletedTask(player, "ep3_clone_relics_hunter_qakkee_3", "killVoritorLizards"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_isOnQuestThree (obj_id player, obj_id npc)
{
	// Returns TRUE if the quest is active on the player
	return (groundquests.isQuestActive(player, "ep3_clone_relics_hunter_qakkee_3"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedQuestThree (obj_id player, obj_id npc)
{
	// Returns TRUE if this quest is complete
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_hunter_qakkee_3"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_isOnQuestFour (obj_id player, obj_id npc)
{
	// Returns TRUE if the quest is active on the player
	return (groundquests.isQuestActive(player, "ep3_clone_relics_hunter_qakkee_4"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskFour (obj_id player, obj_id npc)
{
	// Returns TRUE if this quest is complete
	return (groundquests.hasCompletedTask(player, "ep3_clone_relics_hunter_qakkee_4", "killRancor"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedQuestFour (obj_id player, obj_id npc)
{
	// Returns TRUE if this quest is complete
	return (groundquests.hasCompletedQuest(player, "ep3_clone_relics_hunter_qakkee_4"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_isOnQuestFive (obj_id player, obj_id npc)
{
	// Returns TRUE if the quest is active on the player
	return (groundquests.isQuestActive(player, "ep3_clone_relics_hunter_qakkee_5"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskFive (obj_id player, obj_id npc)
{
	// Returns TRUE if this quest is complete
	return (groundquests.hasCompletedTask(player, "ep3_clone_relics_hunter_qakkee_5", "killSawtooth"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_isGm (obj_id player, obj_id npc)
{
	return (hasObjVar(player, "gm" ));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_hunter_qakkee_condition_hasEp3 (obj_id player, obj_id npc)
{
	return features.hasEpisode3Expansion(player);
}

// ======================================================================
// Script Actions
// ======================================================================

void ep3_clone_relics_hunter_qakkee_action_removeQuestOne (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_1");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_removeQuestTwo (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_grantQuestOne (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_hunter_qakkee_1");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_grantQuestTwo (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_hunter_qakkee_2");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_rewardOne (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "giveRewardOne");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_grantQuestThree (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_hunter_qakkee_3");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_stopTimer (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "completeQuestThree");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_removeQuestThree (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_3");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_grantQuestFour (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_hunter_qakkee_4");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_removeQuestFour (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_4");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_rewardTwo (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "giveRewardTwo");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_grantQuestFive (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_clone_relics_hunter_qakkee_5");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_rewardThree (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "giveRewardThree");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_finishQuestOne (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "doneWithDewbacks");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_removeQuestFive (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_5");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_hunter_qakkee_action_removeQuestAll (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_1");
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_2");
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_3");
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_4");
	groundquests.clearQuest(player, "ep3_clone_relics_hunter_qakkee_5");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int ep3_clone_relics_hunter_qakkee_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: %NU! Good to see you my friend, how are things going?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Great Qakkee! I'm out getting new trophies every day!
	if (response == "s_308")
	{
		doAnimationAction (player, "thumbs_up");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "hi5_tandem");

			doAnimationAction (player, "hi5_tandem");

			//-- NPC: Awesome! How I wish I could be out there with you. Now have a drink and tell me a story.
			string_id message = new string_id (c_stringFile, "s_310");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Last week, I was on Lok and this huge... < tell Qakee a story >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_312");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not too bad! How are you doing?
	if (response == "s_423")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Not too shabby! The nightmares are long gone, I've met someone and...
			string_id message = new string_id (c_stringFile, "s_425");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Woah! Slow down Qakkee, let's have a drink and tell me all about it.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_427");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not too good Qakkee, not too good.
	if (response == "s_431")
	{
		doAnimationAction (player, "slump_head");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "huh");

			//-- NPC: Why not? Have a seat, a drink and tell me all about it.
			string_id message = new string_id (c_stringFile, "s_433");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well I was out hunting with a total beginner and... < tell Qakkee a story >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_435");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_914")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Awesome! How I wish I could be out there with you. Now have a drink and tell me a story.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Last week, I was on Lok and this huge... < tell Qakee a story >
	if (response == "s_312")
	{
		doAnimationAction (player, "huge");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Fantastic %NU, just fantastic! Well look at the time. I don't mean to brush you off my friend but I have a special someone coming by and... well...
			string_id message = new string_id (c_stringFile, "s_421");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not too shabby! The nightmares are long gone, I've met someone and...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Woah! Slow down Qakkee, let's have a drink and tell me all about it.
	if (response == "s_427")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "snap_finger1");

			//-- NPC: Excellent idea! Bartender! So anyway, I met her last week and... < Qakkee tells you a story >
			string_id message = new string_id (c_stringFile, "s_429");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why not? Have a seat, a drink and tell me all about it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well I was out hunting with a total beginner and... < tell Qakkee a story >
	if (response == "s_435")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Sounds to me like you just had a streak of bad luck my friend, it will turn around again. After all, you're the second best hunter in galaxy, right? < Qakkee laughs > Thanks for coming by and stop by soon again, it's always a pleasure.
			string_id message = new string_id (c_stringFile, "s_437");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are back... it... it's done?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It is done Qakkee, it is done...
	if (response == "s_441")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "hug_tandem");

			doAnimationAction (player, "hug_tandem");

			//-- NPC: Come here %NU, let me hug you! How did you do it?!
			string_id message = new string_id (c_stringFile, "s_443");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Unlike you, I had some luck. It was still tough...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_193");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 9);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_910")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Come here %NU, let me hug you! How did you do it?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Unlike you, I had some luck. It was still tough...
	if (response == "s_193")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bounce");

			//-- NPC: I bet it was! My friend, you are a truly a god among men! Let me see that evil eye...
			string_id message = new string_id (c_stringFile, "s_447");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course, here you go.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_449");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I bet it was! My friend, you are a truly a god among men! Let me see that evil eye...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course, here you go.
	if (response == "s_449")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "squirm");

			//-- NPC: Yuck, that's old Sawtooth's alright. Maybe now, I can finally get it out of my mind. I can't begin to explain how grateful I am. As promised though, the draft schematic is yours. A small gift in comparison to what you have done my friend.
			string_id message = new string_id (c_stringFile, "s_451");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you Qakkee. So what happens now?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_453");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yuck, that's old Sawtooth's alright. Maybe now, I can finally get it out of my mind. I can't begin to explain how grateful I am. As promised though, the draft schematic is yours. A small gift in comparison to what you have done my friend.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you Qakkee. So what happens now?
	if (response == "s_453")
	{
		doAnimationAction (player, "tiphat");

		ep3_clone_relics_hunter_qakkee_action_rewardThree (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I'm afraid this is it my friend. I have no more challenges since you have surpassed me. Make sure to come by and visit now and then though, if nothing else but to share some memories.
			string_id message = new string_id (c_stringFile, "s_455");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will Qakkee. It was quite an adventure!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm going to miss you so much Qakkee...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_461");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm afraid this is it my friend. I have no more challenges since you have surpassed me. Make sure to come by and visit now and then though, if nothing else but to share some memories.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will Qakkee. It was quite an adventure!
	if (response == "s_457")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "handshake_tandem");

			doAnimationAction (player, "handshake_tandem");

			//-- NPC: It sure was my friend, it sure was...
			string_id message = new string_id (c_stringFile, "s_459");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm going to miss you so much Qakkee...
	if (response == "s_461")
	{
		doAnimationAction (player, "weeping");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "hug_tandem");

			doAnimationAction (player, "hug_tandem");

			//-- NPC: There, there my friend. Old Qakkee is not going anywhere. Feel free to stop by anytime and we'll share a drink and some stories.
			string_id message = new string_id (c_stringFile, "s_463");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I see that you are still here %NU, was there anything else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, I just can't do this Qakkee. I've changed my mind, sorry.
	if (response == "s_467")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: I understand my friend, it's a dangerous task. If you ever change your mind though, you know where to find me.
			string_id message = new string_id (c_stringFile, "s_469");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I'm just getting some final preparations done.
	if (response == "s_471")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "thumbs_up");

			//-- NPC: Excellent %NU! Again, good luck out there.
			string_id message = new string_id (c_stringFile, "s_473");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_906")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome back %NU. Let me go over the details for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm still too angry from the last time to talk to you right now.
	if (response == "s_477")
	{
		doAnimationAction (player, "point_accusingly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "refuse_offer_affection");

			//-- NPC: Woah! Calm down there %NU. It was a beautiful painting after all!
			string_id message = new string_id (c_stringFile, "s_479");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine but this time I want to see the reward up front!
	if (response == "s_481")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Fair enough. Let me give you the background details first. My last year as a active hunter, my clan sent me to the planet of Kashyyyk. It was a horrible place, full of giant trees and inhabited by all sorts of strange aliens.
			string_id message = new string_id (c_stringFile, "s_483");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah I've been there.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Interesting, go on.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_485");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_521");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_902")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fair enough. Let me give you the background details first. My last year as a active hunter, my clan sent me to the planet of Kashyyyk. It was a horrible place, full of giant trees and inhabited by all sorts of strange aliens.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah I've been there.
	if (response == "s_485")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Then you know what I mean. Anyway, for hunting, it's something else though. Dathomir almost pales in comparison to the wonderful beasts that inhabit Kashyyyk. My main purpose for being there though was Sawtooth.
			string_id message = new string_id (c_stringFile, "s_487");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sawtooth?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_489");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Interesting, go on.
	if (response == "s_521")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: As vile a planet as it is, the hunting there is the best there is. Dathomir almost pales in comparison to the wonderful beasts that inhabit Kashyyyk. My main purpose for being there though was Sawtooth.
			string_id message = new string_id (c_stringFile, "s_523");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sawtooth?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds painful. They had a good dentist there?
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_525");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_557");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then you know what I mean. Anyway, for hunting, it's something else though. Dathomir almost pales in comparison to the wonderful beasts that inhabit Kashyyyk. My main purpose for being there though was Sawtooth.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sawtooth?
	if (response == "s_489")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Sawtooth is a monstrosity of a beast. He's what the locals on the planet call a Uller but they are nothing compared to him. Three times as big and pale grey, no one knows how old he is and how many lives he has taken.
			string_id message = new string_id (c_stringFile, "s_491");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Now we're talking.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_220");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sawtooth is a monstrosity of a beast. He's what the locals on the planet call a Uller but they are nothing compared to him. Three times as big and pale grey, no one knows how old he is and how many lives he has taken.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Now we're talking.
	if (response == "s_220")
	{
		doAnimationAction (player, "thumb_up");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed we are. After several weeks of tracking, I finally found the old beast. Unfortunately, he had found me as well and quickly charged me. I got off a few well aimed shots with my rifle but it wasn't enough, he was upon me and still breathing.
			string_id message = new string_id (c_stringFile, "s_495");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What happened?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_497");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed we are. After several weeks of tracking, I finally found the old beast. Unfortunately, he had found me as well and quickly charged me. I got off a few well aimed shots with my rifle but it wasn't enough, he was upon me and still breathing.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happened?
	if (response == "s_497")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: He engulfed my weapon and took my arm right with it, tossing me in to a tree! Dazed I tried to escape but he was too fast. He took both my legs in his mouth and tossed me around like I was a toy! Then, all of a sudden, he stopped and lumbered off. To this day, I don't know why he didn't finish the job.
			string_id message = new string_id (c_stringFile, "s_499");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You couldn't possibly survive long with those injuries.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_501");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He engulfed my weapon and took my arm right with it, tossing me in to a tree! Dazed I tried to escape but he was too fast. He took both my legs in his mouth and tossed me around like I was a toy! Then, all of a sudden, he stopped and lumbered off. To this day, I don't know why he didn't finish the job.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You couldn't possibly survive long with those injuries.
	if (response == "s_501")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: That is true. I lay in that forest that entire night, staying alive only with the help of my steel will and lust for revenge. The next day, my Rodian clan brothers came upon me and took me back to camp.
			string_id message = new string_id (c_stringFile, "s_503");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I can't believe I'm saying it, but I'm impressed Qakkee.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_505");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That is true. I lay in that forest that entire night, staying alive only with the help of my steel will and lust for revenge. The next day, my Rodian clan brothers came upon me and took me back to camp.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can't believe I'm saying it, but I'm impressed Qakkee.
	if (response == "s_505")
	{
		doAnimationAction (player, "taken_aback");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			//-- NPC: Thank you %NU. To finish the story, my friends found a cybernetic scientist on the planet. He was brilliant... at making arms, unfortunately not as good at the leg part. He put me back together and gave me the draft schematics for the parts, in case they needed repairs. If you manage to slay Sawtooth, I will give you the schematic to the ingenious arm cybernetics that you see on my very self.
			string_id message = new string_id (c_stringFile, "s_507");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's so special about it?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_509");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 26);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Thank you %NU. To finish the story, my friends found a cybernetic scientist on the planet. He was brilliant... at making arms, unfortunately not as good at the leg part. He put me back together and gave me the draft schematics for the parts, in case they needed repairs. If you manage to slay Sawtooth, I will give you the schematic to the ingenious arm cybernetics that you see on my very self.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's so special about it?
	if (response == "s_509")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It will make your arm steady as a rock and improve your aim with a ranged weapon. It will slow down your movements a tiny bit so if you're ever in a brawl, you may find it a disadvantage. But as a hunter, you don't want to get that close anyway, do you?
			string_id message = new string_id (c_stringFile, "s_511");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Agreed. Alright Qakkee, you have yourself a deal.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Not interested I'm afraid, that reward is not worth the risk.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_513");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_517");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It will make your arm steady as a rock and improve your aim with a ranged weapon. It will slow down your movements a tiny bit so if you're ever in a brawl, you may find it a disadvantage. But as a hunter, you don't want to get that close anyway, do you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Agreed. Alright Qakkee, you have yourself a deal.
	if (response == "s_513")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			ep3_clone_relics_hunter_qakkee_action_grantQuestFive (player, npc);

			//-- NPC: Excellent!I hope you can do it %NU, then I can finally put all this behind me. You will find the beast somewhere in the region called Kkowir Forest. Return one of his eyeballs to prove the deed. I will never forget those eyes...
			string_id message = new string_id (c_stringFile, "s_515");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not interested I'm afraid, that reward is not worth the risk.
	if (response == "s_517")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slump_head");

			//-- NPC: I understand %NU... well if you change your mind, you know where to find me.
			string_id message = new string_id (c_stringFile, "s_519");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: As vile a planet as it is, the hunting there is the best there is. Dathomir almost pales in comparison to the wonderful beasts that inhabit Kashyyyk. My main purpose for being there though was Sawtooth.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sawtooth?
	if (response == "s_525")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Sawtooth is a monstrosity of a beast. He's what the locals on the planet call a Uller but they are nothing compared to him. Three times as big and pale grey, no one knows how old he is and how many lives he has taken.
			string_id message = new string_id (c_stringFile, "s_527");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Amazing. Did you find him?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_529");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds painful. They had a good dentist there?
	if (response == "s_557")
	{
		doAnimationAction (player, "squirm");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: No you fool! Sawtooth is a monstrosity of a beast. He's what the locals on the planet call a Uller but they are nothing compared to him. Three times as big and pale grey, no one knows how old he is and how many lives he has taken.
			string_id message = new string_id (c_stringFile, "s_559");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Amazing. Did you find him?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_529");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sawtooth is a monstrosity of a beast. He's what the locals on the planet call a Uller but they are nothing compared to him. Three times as big and pale grey, no one knows how old he is and how many lives he has taken.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Amazing. Did you find him?
	if (response == "s_529")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Indeed I did. It took me several weeks but one night, I was finally face to face, or rather face to hoof, with the old beast.
			string_id message = new string_id (c_stringFile, "s_531");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: < gulp >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_533");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed I did. It took me several weeks but one night, I was finally face to face, or rather face to hoof, with the old beast.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: < gulp >
	if (response == "s_533")
	{
		doAnimationAction (player, "catchbreath");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bang");

			//-- NPC: Unfortunately, the mighty monstrosity had spotted me as well! He came charging at me, full speed! I carefully took aim, not wavering for a second and started putting bolts in him... but it wasn't enough.
			string_id message = new string_id (c_stringFile, "s_535");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What happened?!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_537");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 33);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Unfortunately, the mighty monstrosity had spotted me as well! He came charging at me, full speed! I carefully took aim, not wavering for a second and started putting bolts in him... but it wasn't enough.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happened?!
	if (response == "s_537")
	{
		doAnimationAction (player, "taken_aback");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: He engulfed my weapon and took my arm right with it, tossing me in to a tree! Dazed I tried to escape but he was too fast. He took both my legs in his mouth and tossed me around like I was a toy! Then, all of a sudden, he stopped and lumbered off. To this day, I don't know why he didn't finish the job.
			string_id message = new string_id (c_stringFile, "s_539");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But you must have been terribly injured?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_245");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He engulfed my weapon and took my arm right with it, tossing me in to a tree! Dazed I tried to escape but he was too fast. He took both my legs in his mouth and tossed me around like I was a toy! Then, all of a sudden, he stopped and lumbered off. To this day, I don't know why he didn't finish the job.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: But you must have been terribly injured?
	if (response == "s_245")
	{
		doAnimationAction (player, "gesticulate_wildly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Indeed I was. I lay in that forest that entire night, staying alive only with the help of my steel will and lust for revenge. The next day, my Rodian clan brothers came upon me and took me back to camp.
			string_id message = new string_id (c_stringFile, "s_543");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Incredible!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_545");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed I was. I lay in that forest that entire night, staying alive only with the help of my steel will and lust for revenge. The next day, my Rodian clan brothers came upon me and took me back to camp.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Incredible!
	if (response == "s_545")
	{
		doAnimationAction (player, "taken_aback");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes, luckily, a cybernetic scientist was to be found near our camp. He was brilliant... at making arms, unfortunately not as good at the leg part. He put me back together and gave me the draft schematics for the parts, in case they needed repairs. If you manage to slay Sawtooth, I will give you the schematic to the ingenious arm cybernetics that you see on my very self.
			string_id message = new string_id (c_stringFile, "s_547");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will get you your revenge Qakkee!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: If you, the mighty Qakkee couldn't do it, I'm afraid I can't either.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_549");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_553");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, luckily, a cybernetic scientist was to be found near our camp. He was brilliant... at making arms, unfortunately not as good at the leg part. He put me back together and gave me the draft schematics for the parts, in case they needed repairs. If you manage to slay Sawtooth, I will give you the schematic to the ingenious arm cybernetics that you see on my very self.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will get you your revenge Qakkee!
	if (response == "s_549")
	{
		doAnimationAction (player, "pound_fist_chest");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			ep3_clone_relics_hunter_qakkee_action_grantQuestFive (player, npc);

			//-- NPC: Excellent!I hope you can do it my friend, then I can finally put all this behind me. You will find the beast somewhere in the region called Kkowir Forest. Return one of his eyeballs to prove the deed. I will never forget those eyes...
			string_id message = new string_id (c_stringFile, "s_551");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: If you, the mighty Qakkee couldn't do it, I'm afraid I can't either.
	if (response == "s_553")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slump_head");

			//-- NPC: I understand my friend and I don't blame you. You have still accomplished great deeds. Take care of yourself and good luck out there!
			string_id message = new string_id (c_stringFile, "s_555");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No you fool! Sawtooth is a monstrosity of a beast. He's what the locals on the planet call a Uller but they are nothing compared to him. Three times as big and pale grey, no one knows how old he is and how many lives he has taken.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Amazing. Did you find him?
	if (response == "s_529")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Indeed I did. It took me several weeks but one night, I was finally face to face, or rather face to hoof, with the old beast.
			string_id message = new string_id (c_stringFile, "s_531");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: < gulp >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_533");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch40 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are back %NU! In one piece as well! How did it go? Tell me all about it!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That thing was huge! I still can't believe I killed it.
	if (response == "s_563")
	{
		doAnimationAction (player, "huge");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "thumb_up");

			//-- NPC: I knew you had it in you %NU. Fantastic indeed! You brought back the tooth right?
			string_id message = new string_id (c_stringFile, "s_565");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Oh of course, here. Now there was talk of a reward..?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_567");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 41);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It was fun, not as petty as the other stuff you've sent me out to hunt.
	if (response == "s_587")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "expect_tip");

			//-- NPC: I'm glad I didn't bore you. Can I see the tooth please?
			string_id message = new string_id (c_stringFile, "s_283");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here. Now about that reward?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_591");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_898")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I knew you had it in you %NU. Fantastic indeed! You brought back the tooth right?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Oh of course, here. Now there was talk of a reward..?
	if (response == "s_567")
	{
		doAnimationAction (player, "bounce");

		ep3_clone_relics_hunter_qakkee_action_rewardTwo (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "thumbs_up");

			//-- NPC: Reward? Oh of course. Here you go, it's... another painting! Yay!
			string_id message = new string_id (c_stringFile, "s_569");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Another what?! I killed a Rancor and you give me a painting?!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's it, I'm going to kill you.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_571");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_579");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 42);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Reward? Oh of course. Here you go, it's... another painting! Yay!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Another what?! I killed a Rancor and you give me a painting?!
	if (response == "s_571")
	{
		doAnimationAction (player, "gesticulate_wildly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "refuse_offer_affection");

			//-- NPC: It's all I have I'm afraid. Well I do have another thing that would be an amazing reward... but I couldn't possibly give you that for just killing a simple Rancor. Now go get some rest my friend, then come back and we'll discuss the next step.
			string_id message = new string_id (c_stringFile, "s_573");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It better be worth it this time, or I'll report you to the authorities!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_575");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's it, I'm going to kill you.
	if (response == "s_579")
	{
		doAnimationAction (player, "slit_throat");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "hands_above_head");

			//-- NPC: It's all I have, I swear! Well maybe I could scrounge up something that would be a great reward... although I couldn't possibly give that out just for killing a measly Rancor. Listen, get some rest, come back and we'll discuss the next step.
			string_id message = new string_id (c_stringFile, "s_581");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You are lucky I'm always up for a challenge. This time it better be worth it!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_583");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's all I have I'm afraid. Well I do have another thing that would be an amazing reward... but I couldn't possibly give you that for just killing a simple Rancor. Now go get some rest my friend, then come back and we'll discuss the next step.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It better be worth it this time, or I'll report you to the authorities!
	if (response == "s_575")
	{
		doAnimationAction (player, "point_accusingly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: It will be my friend, it will be.
			string_id message = new string_id (c_stringFile, "s_577");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's all I have, I swear! Well maybe I could scrounge up something that would be a great reward... although I couldn't possibly give that out just for killing a measly Rancor. Listen, get some rest, come back and we'll discuss the next step.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You are lucky I'm always up for a challenge. This time it better be worth it!
	if (response == "s_583")
	{
		doAnimationAction (player, "threaten");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			//-- NPC: Of course %NU, you will be most pleased, I promise.
			string_id message = new string_id (c_stringFile, "s_585");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm glad I didn't bore you. Can I see the tooth please?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here. Now about that reward?
	if (response == "s_591")
	{
		doAnimationAction (player, "nod_head_once");

		ep3_clone_relics_hunter_qakkee_action_rewardTwo (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "thumbs_up");

			//-- NPC: Reward? Oh of course. Here you go, it's... another painting! Yay!
			string_id message = new string_id (c_stringFile, "s_569");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Another what?! I killed a Rancor and you give me a painting?!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's it, I'm going to kill you.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_571");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_579");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 42);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch48 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You still preparing for your trip to Dathomir %NU?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah trying to find a holo recorder to memorize the event.
	if (response == "s_288")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Alright... well good luck with that...
			string_id message = new string_id (c_stringFile, "s_290");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes... yes. Because I'm not scared. I'm not!
	if (response == "s_293")
	{
		doAnimationAction (player, "point_accusingly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			//-- NPC: Oh I'm sure you're not %NU. This is a walk in the park for you, right?
			string_id message = new string_id (c_stringFile, "s_295");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Something else came up and I have to postpone for now.
	if (response == "s_297")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: What came up? You had to take out the trash? Your pet Kreetle is not used to being without his master for that long? Too many excuses %NU, too little action!
			string_id message = new string_id (c_stringFile, "s_299");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_894")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So this is it %NU. Your final test in becoming the ultimate hunter, the killing machine of obscene power, the reaper of beasts and god among men!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Oh it's you. Thought you were someone else, see you later.
	if (response == "s_305")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stamp_feet");

			//-- NPC: But...what?! When are you going to do the final test then? I've been planning this for so long now and you keep dodging me. Let's get this over with already!
			string_id message = new string_id (c_stringFile, "s_309");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes! Now we're talking!
	if (response == "s_313")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed we are! This will be a grand adventure, worthy of closing your quest!
			string_id message = new string_id (c_stringFile, "s_315");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Quests usually have a reward? Usually not a painting...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_318");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Have you been drinking again?
	if (response == "s_341")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: What?! No! Well maybe a little but that's beside the point. Let's get down to business and discuss your next, last and greatest adventure!
			string_id message = new string_id (c_stringFile, "s_343");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I hope you have a better reward than a painting this time.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_345");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_890")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed we are! This will be a grand adventure, worthy of closing your quest!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Quests usually have a reward? Usually not a painting...
	if (response == "s_318")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nervous");

			//-- NPC: Reward? Yes of course! No painting, duly noted. So anyway, this time, you will travel to the far away planet of Dathomir. A gruesome planet, filled with danger at your every turn. Instead of avoiding it though, you will go straight for the most fearsome beast on the planet, the Rancor!
			string_id message = new string_id (c_stringFile, "s_320");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: A Rancor eh? I've heard some horror stories about those...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A Rancor? I'm a hunter, not a suicidal maniac! I'm out!
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_322");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_336");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 55);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch55 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Reward? Yes of course! No painting, duly noted. So anyway, this time, you will travel to the far away planet of Dathomir. A gruesome planet, filled with danger at your every turn. Instead of avoiding it though, you will go straight for the most fearsome beast on the planet, the Rancor!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A Rancor eh? I've heard some horror stories about those...
	if (response == "s_322")
	{
		doAnimationAction (player, "nervous");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			//-- NPC: And they are all true! Which is why it's the ultimate challenge! I see a lot of promise in you young one. If I didn't think you could do this, I would never suggest it.
			string_id message = new string_id (c_stringFile, "s_324");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But you have never even seen me hunt...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_326");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A Rancor? I'm a hunter, not a suicidal maniac! I'm out!
	if (response == "s_336")
	{
		doAnimationAction (player, "gesticulate_wildly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: Come on! It will be exciting, a great adventure! Fine, go home to your mommy why don't you!
			string_id message = new string_id (c_stringFile, "s_338");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch56 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And they are all true! Which is why it's the ultimate challenge! I see a lot of promise in you young one. If I didn't think you could do this, I would never suggest it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: But you have never even seen me hunt...
	if (response == "s_326")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've heard the stories though! The entire town, maybe even planet, is talking about your adventures and how they almost rival my own. They are all waiting for the conclusion, you can't let down your fans now, can you?
			string_id message = new string_id (c_stringFile, "s_328");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They are? Maybe I won't be single after this... Let's do it!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Fine, I'll do it, how bad could it be.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_330");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_334");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch57 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've heard the stories though! The entire town, maybe even planet, is talking about your adventures and how they almost rival my own. They are all waiting for the conclusion, you can't let down your fans now, can you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They are? Maybe I won't be single after this... Let's do it!
	if (response == "s_330")
	{
		doAnimationAction (player, "taken_aback");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			ep3_clone_relics_hunter_qakkee_action_grantQuestFour (player, npc);

			//-- NPC: Indeed! I won't have a time limit on you this time, it will be hard enough as it is. Travel to Dathomir, track down a Rancor and bring back one of it's teeth, hopefully not attached to your stomach. Oh and there's an Imperial blockade around the planet but you should be able to get past that no problem... I'm sure.
			string_id message = new string_id (c_stringFile, "s_332");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine, I'll do it, how bad could it be.
	if (response == "s_334")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			ep3_clone_relics_hunter_qakkee_action_grantQuestFour (player, npc);

			//-- NPC: Indeed! I won't have a time limit on you this time, it will be hard enough as it is. Travel to Dathomir, track down a Rancor and bring back one of it's teeth, hopefully not attached to your stomach. Oh and there's an Imperial blockade around the planet but you should be able to get past that no problem... I'm sure.
			string_id message = new string_id (c_stringFile, "s_332");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch60 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?! No! Well maybe a little but that's beside the point. Let's get down to business and discuss your next, last and greatest adventure!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I hope you have a better reward than a painting this time.
	if (response == "s_345")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nervous");

			//-- NPC: Reward? Yes of course! It will be much better. So anyway, this time, you will travel to the far away planet of Dathomir. A gruesome planet, filled with danger at your every turn. Instead of avoiding it though, you will go straight for the most fearsome beast on the planet, the Rancor!
			string_id message = new string_id (c_stringFile, "s_347");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure. what do you want me to bring back this time? His left leg?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I can't go to Dathomir, the Empire has locked down that planet, sorry.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_349");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_355");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Reward? Yes of course! It will be much better. So anyway, this time, you will travel to the far away planet of Dathomir. A gruesome planet, filled with danger at your every turn. Instead of avoiding it though, you will go straight for the most fearsome beast on the planet, the Rancor!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure. what do you want me to bring back this time? His left leg?
	if (response == "s_349")
	{
		doAnimationAction (player, "yawn");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "taken_aback");

			//-- NPC: Sure? This is no Womprat you know! This is a Rancor, a fearsome beast that can rip a person in two like he's eating wavor snacks!
			string_id message = new string_id (c_stringFile, "s_351");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I know what a Rancor is. Let's get down to business.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_353");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can't go to Dathomir, the Empire has locked down that planet, sorry.
	if (response == "s_355")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "mock");

			//-- NPC: You can't get past one silly little blockade? I'm sure that's the reason, I'm sure your not scared of the Rancor.
			string_id message = new string_id (c_stringFile, "s_357");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sure? This is no Womprat you know! This is a Rancor, a fearsome beast that can rip a person in two like he's eating wavor snacks!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know what a Rancor is. Let's get down to business.
	if (response == "s_353")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			ep3_clone_relics_hunter_qakkee_action_grantQuestFour (player, npc);

			//-- NPC: Indeed! I won't have a time limit on you this time, it will be hard enough as it is. Travel to Dathomir, track down a Rancor and bring back one of it's teeth, hopefully not attached to your stomach. Oh and there's an Imperial blockade around the planet but you should be able to get past that no problem... I'm sure.
			string_id message = new string_id (c_stringFile, "s_332");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are back! With some time left too. So how many lizards did you manage to hunt down? 3? 4?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Try six! That's right, six!
	if (response == "s_362")
	{
		doAnimationAction (player, "mock");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "taken_aback");

			//-- NPC: You... you killed six of them? Let me see those horns! One, two.. six... Well this one is kind of small! And you had all that extra time! I could probably have killed 8 with all that time! That's right. Anyway, let's move on!
			string_id message = new string_id (c_stringFile, "s_365");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: < laugh >
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_367");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I must have been lucky, I managed to get six of them.
	if (response == "s_671")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "taken_aback");

			//-- NPC: You... you killed six? That's very impressive, you must have been lucky indeed. Of course you had some extra time but still, very impressive. I believe you are ready for the next and final step %NU.
			string_id message = new string_id (c_stringFile, "s_673");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I am ready Qakkee!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_675");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 68);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_886")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch65 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You... you killed six of them? Let me see those horns! One, two.. six... Well this one is kind of small! And you had all that extra time! I could probably have killed 8 with all that time! That's right. Anyway, let's move on!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: < laugh >
	if (response == "s_367")
	{
		doAnimationAction (player, "laugh");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: You think you are all that eh? Well you have come to the final step and this one, will make you choke on that laughter!
			string_id message = new string_id (c_stringFile, "s_369");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm sure it will.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_371");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch66 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You think you are all that eh? Well you have come to the final step and this one, will make you choke on that laughter!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sure it will.
	if (response == "s_371")
	{
		doAnimationAction (player, "laugh_titter");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			ep3_clone_relics_hunter_qakkee_action_stopTimer (player, npc);

			//-- NPC: Collect your gear and return to me when you think you are ready...
			string_id message = new string_id (c_stringFile, "s_373");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch68 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You... you killed six? That's very impressive, you must have been lucky indeed. Of course you had some extra time but still, very impressive. I believe you are ready for the next and final step %NU.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am ready Qakkee!
	if (response == "s_675")
	{
		doAnimationAction (player, "thumb_up");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			ep3_clone_relics_hunter_qakkee_action_stopTimer (player, npc);

			//-- NPC: Yes, I hope so, for your sake. Get some rest and get your gear in order before you come back and we'll discuss what's next.
			string_id message = new string_id (c_stringFile, "s_378");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch70 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Clock is ticking you know. Maybe you should get going?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Oh I'm in no rush.
	if (response == "s_681")
	{
		doAnimationAction (player, "yawn");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: I'm sure you're not, you cocky...
			string_id message = new string_id (c_stringFile, "s_683");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know but I need those directions again.
	if (response == "s_685")
	{
		doAnimationAction (player, "implore");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course you do. Maybe you should write them down this time? Anyway, the camp of Voritors I came upon was located a fair bit south of the Imperial outpost.
			string_id message = new string_id (c_stringFile, "s_687");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Something came up, I can't do it right now.
	if (response == "s_689")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			ep3_clone_relics_hunter_qakkee_action_removeQuestThree (player, npc);

			//-- NPC: Did your mother think it was too dangerous, is that it? < Qakkee laughs > Alright, come back when you DO have time.
			string_id message = new string_id (c_stringFile, "s_691");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_882")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch74 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There you are %NU. Are you ready to continue your quest to become almost as good a hunter as I?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's not what I call the quest but yeah, I am.
	if (response == "s_695")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "thumbs_up");

			//-- NPC: Excellent! This step in the safari will take you to the planet of Dantooine. I was there a few years ago hunting the vicious Voritor Jungle Lizard! I came upon this spot south of the Imperial outpost where the little bastards were all over the place.
			string_id message = new string_id (c_stringFile, "s_697");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Really? Sounds exciting!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: < Yawn > How exciting...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_699");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_727");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't have time for you right now Qakkee.
	if (response == "s_735")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Don't have time?! Well you will take the time! I'm just kidding, see you later then... wimp!
			string_id message = new string_id (c_stringFile, "s_737");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_878")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch75 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent! This step in the safari will take you to the planet of Dantooine. I was there a few years ago hunting the vicious Voritor Jungle Lizard! I came upon this spot south of the Imperial outpost where the little bastards were all over the place.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Really? Sounds exciting!
	if (response == "s_699")
	{
		doAnimationAction (player, "taken_aback");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Oh it was, I approached them with the wind in my back so they couldn't sense my presence and started to carefully take aim at the ones that were separated from the flock. BOOM! Head shot! BOOM! Head shot! I managed to take out 5 of them before the day was over!
			string_id message = new string_id (c_stringFile, "s_701");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Aren't you supposed to approach with the wind in your face...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_703");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: < Yawn > How exciting...
	if (response == "s_727")
	{
		doAnimationAction (player, "yawn");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "backhand_threaten");

			//-- NPC: Are you listening to me youngster?! If you want to live out there you better pay attention!
			string_id message = new string_id (c_stringFile, "s_618");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure, sure. Go on.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_731");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 83);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch76 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh it was, I approached them with the wind in my back so they couldn't sense my presence and started to carefully take aim at the ones that were separated from the flock. BOOM! Head shot! BOOM! Head shot! I managed to take out 5 of them before the day was over!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Aren't you supposed to approach with the wind in your face...
	if (response == "s_703")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Yes, just like I said! Are you paying attention here or what? Anyway, so, I got 5 in one day, think you can beat it?
			string_id message = new string_id (c_stringFile, "s_705");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, I'm not even on Dantooine yet...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_707");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch77 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, just like I said! Are you paying attention here or what? Anyway, so, I got 5 in one day, think you can beat it?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, I'm not even on Dantooine yet...
	if (response == "s_707")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Excuses excuses. Fine, I will give you some extra time to make up for that little fact...and some more time because you are slower than a three legged Bantha. How's that?
			string_id message = new string_id (c_stringFile, "s_709");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds fair. I will be back soon!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't need a handicap!
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No thanks, I'm tired of this.
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_711");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_715");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_723");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 78);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch78 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excuses excuses. Fine, I will give you some extra time to make up for that little fact...and some more time because you are slower than a three legged Bantha. How's that?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds fair. I will be back soon!
	if (response == "s_711")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			ep3_clone_relics_hunter_qakkee_action_grantQuestThree (player, npc);

			//-- NPC: Excellent, bring me back their horns as proof of the deed!
			string_id message = new string_id (c_stringFile, "s_594");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't need a handicap!
	if (response == "s_715")
	{
		doAnimationAction (player, "point_accusingly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: I know, you already have more than enough. < Qakkee laughs >
			string_id message = new string_id (c_stringFile, "s_600");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not the one with the metal legs.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_719");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 80);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks, I'm tired of this.
	if (response == "s_723")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "snap_finger1");

			//-- NPC: I bet you're one of those people that get tired as soon as they get out of bed in the morning. You go sleep for a few days and then maybe you'll change your mind. Bartender, I need another over here!
			string_id message = new string_id (c_stringFile, "s_612");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch80 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I know, you already have more than enough. < Qakkee laughs >

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not the one with the metal legs.
	if (response == "s_719")
	{
		doAnimationAction (player, "mock");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			ep3_clone_relics_hunter_qakkee_action_grantQuestThree (player, npc);

			//-- NPC: Just get out of here! And bring back their horns as proof, if you make it back at all!
			string_id message = new string_id (c_stringFile, "s_606");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch83 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you listening to me youngster?! If you want to live out there you better pay attention!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure, sure. Go on.
	if (response == "s_731")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bang");

			//-- NPC: Very well. I approached them with the wind in my back so they couldn't sense my presence and started to carefully take aim at the ones that were separated from the flock. BOOM! Headshot! BOOM! Headshot! I managed to take out 5 of them before the day was over!
			string_id message = new string_id (c_stringFile, "s_733");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Aren't you supposed to approach with the wind in your face...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_703");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch84 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well. I approached them with the wind in my back so they couldn't sense my presence and started to carefully take aim at the ones that were separated from the flock. BOOM! Headshot! BOOM! Headshot! I managed to take out 5 of them before the day was over!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Aren't you supposed to approach with the wind in your face...
	if (response == "s_703")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Yes, just like I said! Are you paying attention here or what? Anyway, so, I got 5 in one day, think you can beat it?
			string_id message = new string_id (c_stringFile, "s_705");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, I'm not even on Dantooine yet...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_707");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch86 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome back! How was Lok? You did make it to Lok even though I didn't draw you a map right?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: < Laugh at Qakkee > You are SO funny!
	if (response == "s_741")
	{
		doAnimationAction (player, "laugh");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Yeah yeah. Did you hunt down the Snorbals or what?
			string_id message = new string_id (c_stringFile, "s_743");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah I got your trunks. What do you do with this stuff anyway?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_745");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 87);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I could find a hydro spring in a dung mite stack, so yes!
	if (response == "s_650")
	{
		doAnimationAction (player, "2hot4u");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "hold_nose");

			//-- NPC: I wish you had washed up before you came to see me then. < Qakkee laughs > Enough chatter, did you manage to hunt down the Snorbals or were they too hard to track for you?
			string_id message = new string_id (c_stringFile, "s_654");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah real hard. Here's your trunks and that should explain the smell.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_874")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch87 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yeah yeah. Did you hunt down the Snorbals or what?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah I got your trunks. What do you do with this stuff anyway?
	if (response == "s_745")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: Let's just say that I wouldn't order the meat scruzer here this week if I were you. Anyway, back to business. I must say that you start to impress me %NU and I suppose you have earned some sort of reward.
			string_id message = new string_id (c_stringFile, "s_747");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would say so.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_749");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 88);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch88 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Let's just say that I wouldn't order the meat scruzer here this week if I were you. Anyway, back to business. I must say that you start to impress me %NU and I suppose you have earned some sort of reward.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would say so.
	if (response == "s_749")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			ep3_clone_relics_hunter_qakkee_action_rewardOne (player, npc);

			//-- NPC: You can have this old painting, it brings me nothing but bad memories anyway. Now when you have rested yourself, come back and we'll continue this little adventure. I expect to see you in about a week then. < Qakkee laughs >
			string_id message = new string_id (c_stringFile, "s_646");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch90 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I wish you had washed up before you came to see me then. < Qakkee laughs > Enough chatter, did you manage to hunt down the Snorbals or were they too hard to track for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah real hard. Here's your trunks and that should explain the smell.
	if (response == "s_658")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: < Qakkee laughs > Indeed! Not bad %NU. I must say that you start to impress me. I suppose you have earned some sort of reward.
			string_id message = new string_id (c_stringFile, "s_662");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would say so.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_749");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 88);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch91 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: < Qakkee laughs > Indeed! Not bad %NU. I must say that you start to impress me. I suppose you have earned some sort of reward.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would say so.
	if (response == "s_749")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			ep3_clone_relics_hunter_qakkee_action_rewardOne (player, npc);

			//-- NPC: You can have this old painting, it brings me nothing but bad memories anyway. Now when you have rested yourself, come back and we'll continue this little adventure. I expect to see you in about a week then. < Qakkee laughs >
			string_id message = new string_id (c_stringFile, "s_646");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch92 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There's no way you've been to Lok already. Is there a problem?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No problem, just picking up some refreshments for the trip.
	if (response == "s_672")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_cackle");

			//-- NPC: Oh, yeah don't forget to ask for umbrellas for your drink too!
			string_id message = new string_id (c_stringFile, "s_676");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Something came up, I can't do this right now.
	if (response == "s_680")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rofl");

			ep3_clone_relics_hunter_qakkee_action_removeQuestOne (player, npc);

			//-- NPC: 'Something came up' < Qakkee laughs hysterically > Sure it did, sure it did.
			string_id message = new string_id (c_stringFile, "s_684");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_870")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch95 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well you've been gone long enough. Did you find the Dewbacks or did a pack of angry Kreetles chase you away?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very funny! 10 cannibal Dewback jaws, as ordered!
	if (response == "s_694")
	{
		doAnimationAction (player, "pose_proudly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not too bad. Didn't take you much longer than it did old Erikk on his 80'th birthday either! < Qakkee laughs > So, that was the warm up round, are you ready to sink your teeth in to some challenge?
			string_id message = new string_id (c_stringFile, "s_698");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More? I thought this would shut you up.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I was hoping you would say that.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_702");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_748");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 96);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No but I had to take care of a Krayt dragon in my way.
	if (response == "s_751")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: < Qakkee laughs > I'm starting to like you, youngster. So how about it, any luck with the Dewbacks?
			string_id message = new string_id (c_stringFile, "s_753");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course. 10 jaws, as you said.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_755");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 104);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_866")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch96 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not too bad. Didn't take you much longer than it did old Erikk on his 80'th birthday either! < Qakkee laughs > So, that was the warm up round, are you ready to sink your teeth in to some challenge?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: More? I thought this would shut you up.
	if (response == "s_702")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "mock");

			//-- NPC: I thought you wanted to prove that you were almost as good a hunter as I? I guess you're just going to have to go through life being number two...thousand.
			string_id message = new string_id (c_stringFile, "s_706");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: There's something wrong with you. I'm out of here!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well I'll be...fine! What now?!
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_710");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_716");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was hoping you would say that.
	if (response == "s_748")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			ep3_clone_relics_hunter_qakkee_action_finishQuestOne (player, npc);

			//-- NPC: Alright, I'm cranking it up a notch so you may have to buy some new armor to protect that frail body of yours. Ever been to Lok? Well that's where you're going for this one. They have a strange looking creature called the Snorbal. Hunt down 8, no 12, of them and bring me back their trunks.
			string_id message = new string_id (c_stringFile, "s_718");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I thought you said you would crank it up a notch?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm really not comfortable with chopping off all these body parts.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_721");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_726");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch97 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I thought you wanted to prove that you were almost as good a hunter as I? I guess you're just going to have to go through life being number two...thousand.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: There's something wrong with you. I'm out of here!
	if (response == "s_710")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rofl");

			ep3_clone_relics_hunter_qakkee_action_finishQuestOne (player, npc);

			//-- NPC: Sounds like a plan. Just be careful, I saw some Womprats out there earlier and they looked mighty hungry!
			string_id message = new string_id (c_stringFile, "s_713");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well I'll be...fine! What now?!
	if (response == "s_716")
	{
		doAnimationAction (player, "wtf");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			ep3_clone_relics_hunter_qakkee_action_finishQuestOne (player, npc);

			//-- NPC: Alright, I'm cranking it up a notch so you may have to buy some new armor to protect that frail body of yours. Ever been to Lok? Well that's where you're going for this one. They have a strange looking creature called the Snorbal. Hunt down 8, no 12, of them and bring me back their trunks.
			string_id message = new string_id (c_stringFile, "s_718");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I thought you said you would crank it up a notch?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm really not comfortable with chopping off all these body parts.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_721");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_726");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch99 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alright, I'm cranking it up a notch so you may have to buy some new armor to protect that frail body of yours. Ever been to Lok? Well that's where you're going for this one. They have a strange looking creature called the Snorbal. Hunt down 8, no 12, of them and bring me back their trunks.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I thought you said you would crank it up a notch?
	if (response == "s_721")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			ep3_clone_relics_hunter_qakkee_action_grantQuestTwo (player, npc);

			//-- NPC: Don't get smart with me youngster! Snorbals might look peaceful but they can get quite ferocious if angered!
			string_id message = new string_id (c_stringFile, "s_724");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm really not comfortable with chopping off all these body parts.
	if (response == "s_726")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well you kill them first! They won't feel a thing dummy!
			string_id message = new string_id (c_stringFile, "s_729");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But still, I think I've had enough of this. Take care.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Alright, I'll do it.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_732");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_740");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 101);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch101 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well you kill them first! They won't feel a thing dummy!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: But still, I think I've had enough of this. Take care.
	if (response == "s_732")
	{
		doAnimationAction (player, "giveup");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: And you wanted to be a hunter? You're more squemish than a cleaning droid! Get out of here!
			string_id message = new string_id (c_stringFile, "s_736");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'll do it.
	if (response == "s_740")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "cough_polite");

			ep3_clone_relics_hunter_qakkee_action_grantQuestTwo (player, npc);

			//-- NPC: You mean you'll try to do it? Be careful now!
			string_id message = new string_id (c_stringFile, "s_744");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch104 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: < Qakkee laughs > I'm starting to like you, youngster. So how about it, any luck with the Dewbacks?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course. 10 jaws, as you said.
	if (response == "s_755")
	{
		doAnimationAction (player, "2hot4u");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not too bad. Didn't take you much longer than it did old Erikk on his 80'th birthday either! < Qakkee laughs > So, that was the warm up round, are you ready to sink your teeth in to some challenge?
			string_id message = new string_id (c_stringFile, "s_698");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: More? I thought this would shut you up.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I was hoping you would say that.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_702");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_748");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 96);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch105 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Aww, are you back already? It proved a little too tough for you eh?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No! I'm just here to get the directions again.
	if (response == "s_760")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: What, you need me to draw a map or something?! Several cannibal Dewbacks has been spotted in the western Dune Sea south west of here.
			string_id message = new string_id (c_stringFile, "s_762");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No I'm just here to tell you that I will do this unarmed.
	if (response == "s_764")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Unarmed? What are you crazy? Well I expect not to see you again. At least you provided some fun around here so your life wasn't a total waste.
			string_id message = new string_id (c_stringFile, "s_766");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No but I can't right now, I have more important things to do.
	if (response == "s_768")
	{
		doAnimationAction (player, "refuse_offer_affection");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rofl");

			ep3_clone_relics_hunter_qakkee_action_removeQuestOne (player, npc);

			//-- NPC: 'I have more important things to do' <Qakkee laughs hysterically> Sure you do, sure you do!
			string_id message = new string_id (c_stringFile, "s_770");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a developer/tester and want to clear quests.
	if (response == "s_862")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Which quest?
			string_id message = new string_id (c_stringFile, "s_915");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: 1
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 2
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: 3
			boolean hasResponse2 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: 4
			boolean hasResponse3 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: 5
			boolean hasResponse4 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: All
			boolean hasResponse5 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_916");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_917");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_918");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_919");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_920");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_921");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch109 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Which quest?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 1
	if (response == "s_916")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_hunter_qakkee_action_removeQuestOne (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_922");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 2
	if (response == "s_917")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_hunter_qakkee_action_removeQuestTwo (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_924");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 3
	if (response == "s_918")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_hunter_qakkee_action_removeQuestThree (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_926");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 4
	if (response == "s_919")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_hunter_qakkee_action_removeQuestFour (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_928");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 5
	if (response == "s_920")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_hunter_qakkee_action_removeQuestFive (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_930");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: All
	if (response == "s_921")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			ep3_clone_relics_hunter_qakkee_action_removeQuestAll (player, npc);

			//-- NPC: Okay
			string_id message = new string_id (c_stringFile, "s_932");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch116 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I suppose you are another young hunter that has come to hear the stories of the great Qakkee?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Indeed I am, he was something else!
	if (response == "s_788")
	{
		doAnimationAction (player, "thumbs_up");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Was?! I'm still here you know! Granted, I may not be in my prime anymore but I can still put a bolt through a dewbacks eye from a 1000 m away!
			string_id message = new string_id (c_stringFile, "s_790");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That's impossible, maybe up to 63-64 m away...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_792");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 117);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who's Qakkee?
	if (response == "s_840")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: Who's Qakkee?! Have you been living in a rock mite den or something? I'm Qakkee, the greatest hunter of all times!
			string_id message = new string_id (c_stringFile, "s_844");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You look a little under the weather to me.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_848");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 126);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I thought you had something important to say.
	if (response == "s_856")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Everything I say is important but you're obviously not, so be gone!
			string_id message = new string_id (c_stringFile, "s_860");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch117 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Was?! I'm still here you know! Granted, I may not be in my prime anymore but I can still put a bolt through a dewbacks eye from a 1000 m away!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's impossible, maybe up to 63-64 m away...
	if (response == "s_792")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "belly_laugh");

			//-- NPC: Ha! You are quite the beginner aren't you? So what story do you want to hear? How about when I fought off a rancor in his den armed with only a thigh bone? Or what about when I wrestled down a hungry scyk with my bare hands?
			string_id message = new string_id (c_stringFile, "s_794");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You're full of it. I'm out of here.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've done all that, don't you have anything better?
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_796");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_800");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 118);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch118 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ha! You are quite the beginner aren't you? So what story do you want to hear? How about when I fought off a rancor in his den armed with only a thigh bone? Or what about when I wrestled down a hungry scyk with my bare hands?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You're full of it. I'm out of here.
	if (response == "s_796")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: Bah! When you tell the young people of today, they just won't believe you. Are you all made of mynock jello these days?
			string_id message = new string_id (c_stringFile, "s_798");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've done all that, don't you have anything better?
	if (response == "s_800")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: You think you're better than me eh? Are you strong enough to back that up? I don't think so.
			string_id message = new string_id (c_stringFile, "s_802");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure, what would you have me do?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't have to, people already cower in awe when they hear of me.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_804");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_820");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 120);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch120 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You think you're better than me eh? Are you strong enough to back that up? I don't think so.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure, what would you have me do?
	if (response == "s_804")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well by the look of you, I better go easy...hmm. How about you go out in to the desert, don't forget some water now, and kill 10 of them cannibal Dewbacks. I'll even make it easy for you and explain a good location to find them. Several have been spotted in the western Dune Sea south west of here.
			string_id message = new string_id (c_stringFile, "s_806");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back before you can finish that drink.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I can't right now, I have more important things to do.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_808");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_812");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 121);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't have to, people already cower in awe when they hear of me.
	if (response == "s_820")
	{
		doAnimationAction (player, "pose_proudly");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "chicken");

			//-- NPC: Aww are you scared eh? I've met kreetles with more courage than you!
			string_id message = new string_id (c_stringFile, "s_824");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm out of here!
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Fine! What should I do to prove my greatness?!
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_828");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_836");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 124);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch121 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well by the look of you, I better go easy...hmm. How about you go out in to the desert, don't forget some water now, and kill 10 of them cannibal Dewbacks. I'll even make it easy for you and explain a good location to find them. Several have been spotted in the western Dune Sea south west of here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll be back before you can finish that drink.
	if (response == "s_808")
	{
		doAnimationAction (player, "pound_fist_chest");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rofl");

			ep3_clone_relics_hunter_qakkee_action_grantQuestOne (player, npc);

			//-- NPC: Sure, sure. You be careful now little one and remember to bring water!
			string_id message = new string_id (c_stringFile, "s_810");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can't right now, I have more important things to do.
	if (response == "s_812")
	{
		doAnimationAction (player, "refuse_offer_affection");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rofl");

			//-- NPC: 'I have more important things to do' <Qakkee laughs hysterically> Sure you do, sure you do!
			string_id message = new string_id (c_stringFile, "s_816");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch124 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Aww are you scared eh? I've met kreetles with more courage than you!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm out of here!
	if (response == "s_828")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "belly_laugh");

			//-- NPC: Yes indeed, run along little one!
			string_id message = new string_id (c_stringFile, "s_832");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine! What should I do to prove my greatness?!
	if (response == "s_836")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well by the look of you, I better go easy...hmm. How about you go out in to the desert, don't forget some water now, and kill 10 of them cannibal Dewbacks. I'll even make it easy for you and explain a good location to find them. Several have been spotted in the western Dune Sea south west of here.
			string_id message = new string_id (c_stringFile, "s_806");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be back before you can finish that drink.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I can't right now, I have more important things to do.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_808");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_812");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 121);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch126 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Who's Qakkee?! Have you been living in a rock mite den or something? I'm Qakkee, the greatest hunter of all times!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You look a little under the weather to me.
	if (response == "s_848")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Under the weather?! Granted, I may not be in my prime anymore but I can still put a bolt through a dewbacks eye from a 1000 m away!
			string_id message = new string_id (c_stringFile, "s_852");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That's impossible, maybe up to 63-64 m away...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_792");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 117);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_hunter_qakkee_handleBranch127 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Under the weather?! Granted, I may not be in my prime anymore but I can still put a bolt through a dewbacks eye from a 1000 m away!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's impossible, maybe up to 63-64 m away...
	if (response == "s_792")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "belly_laugh");

			//-- NPC: Ha! You are quite the beginner aren't you? So what story do you want to hear? How about when I fought off a rancor in his den armed with only a thigh bone? Or what about when I wrestled down a hungry scyk with my bare hands?
			string_id message = new string_id (c_stringFile, "s_794");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You're full of it. I'm out of here.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've done all that, don't you have anything better?
			boolean hasResponse1 = false;
			if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_796");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_800");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 118);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.ep3_clone_relics_hunter_qakkee");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	setName(self, new string_id( "ep3/npc_names", "clone_relics_qakkee" ));

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	setName(self, new string_id( "ep3/npc_names", "clone_relics_qakkee" ));

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);
	faceTo (self, player);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.ep3_clone_relics_hunter_qakkee");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedAllQuests (player, npc))
	{
		doAnimationAction (npc, "hug_tandem");

		doAnimationAction (player, "hug_tandem");

		//-- NPC: %NU! Good to see you my friend, how are things going?
		string_id message = new string_id (c_stringFile, "s_306");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Great Qakkee! I'm out getting new trophies every day!
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not too bad! How are you doing?
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Not too good Qakkee, not too good.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse3 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_308");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_423");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_431");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_914");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 1);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskFive (player, npc))
	{
		doAnimationAction (npc, "catchbreath");

		//-- NPC: You are back... it... it's done?!
		string_id message = new string_id (c_stringFile, "s_439");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: It is done Qakkee, it is done...
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_441");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_910");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 8);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_isOnQuestFive (player, npc))
	{
		//-- NPC: I see that you are still here %NU, was there anything else?
		string_id message = new string_id (c_stringFile, "s_465");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yeah, I just can't do this Qakkee. I've changed my mind, sorry.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No, I'm just getting some final preparations done.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_467");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_471");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_906");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 15);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedQuestFour (player, npc))
	{
		//-- NPC: Welcome back %NU. Let me go over the details for you.
		string_id message = new string_id (c_stringFile, "s_475");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm still too angry from the last time to talk to you right now.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Fine but this time I want to see the reward up front!
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_477");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_481");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_902");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 18);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskFour (player, npc))
	{
		doAnimationAction (npc, "celebrate");

		//-- NPC: You are back %NU! In one piece as well! How did it go? Tell me all about it!
		string_id message = new string_id (c_stringFile, "s_561");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: That thing was huge! I still can't believe I killed it.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: It was fun, not as petty as the other stuff you've sent me out to hunt.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_563");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_587");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_898");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 40);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_isOnQuestFour (player, npc))
	{
		doAnimationAction (npc, "nod_head_once");

		//-- NPC: You still preparing for your trip to Dathomir %NU?
		string_id message = new string_id (c_stringFile, "s_593");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yeah trying to find a holo recorder to memorize the event.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes... yes. Because I'm not scared. I'm not!
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Something else came up and I have to postpone for now.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse3 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_288");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_293");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_297");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_894");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 48);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedQuestThree (player, npc))
	{
		doAnimationAction (npc, "nod_head_once");

		//-- NPC: So this is it %NU. Your final test in becoming the ultimate hunter, the killing machine of obscene power, the reaper of beasts and god among men!
		string_id message = new string_id (c_stringFile, "s_302");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Oh it's you. Thought you were someone else, see you later.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes! Now we're talking!
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Have you been drinking again?
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse3 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_305");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_313");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_341");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_890");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 52);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskThree (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: You are back! With some time left too. So how many lizards did you manage to hunt down? 3? 4?
		string_id message = new string_id (c_stringFile, "s_360");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Try six! That's right, six!
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I must have been lucky, I managed to get six of them.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_362");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_671");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_886");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 64);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_isOnQuestThree (player, npc))
	{
		//-- NPC: Clock is ticking you know. Maybe you should get going?
		string_id message = new string_id (c_stringFile, "s_679");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Oh I'm in no rush.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I know but I need those directions again.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Something came up, I can't do it right now.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse3 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_681");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_685");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_689");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_882");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 70);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedQuestTwo (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: There you are %NU. Are you ready to continue your quest to become almost as good a hunter as I?
		string_id message = new string_id (c_stringFile, "s_693");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: That's not what I call the quest but yeah, I am.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I don't have time for you right now Qakkee.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_695");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_735");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_878");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 74);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskTwo (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: Welcome back! How was Lok? You did make it to Lok even though I didn't draw you a map right?
		string_id message = new string_id (c_stringFile, "s_739");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: < Laugh at Qakkee > You are SO funny!
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I could find a hydro spring in a dung mite stack, so yes!
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_741");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_650");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_874");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 86);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_isOnQuestTwo (player, npc))
	{
		doAnimationAction (npc, "huh");

		//-- NPC: There's no way you've been to Lok already. Is there a problem?
		string_id message = new string_id (c_stringFile, "s_668");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No problem, just picking up some refreshments for the trip.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Something came up, I can't do this right now.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_672");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_680");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_870");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 92);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_hasCompletedTaskOne (player, npc))
	{
		//-- NPC: Well you've been gone long enough. Did you find the Dewbacks or did a pack of angry Kreetles chase you away?
		string_id message = new string_id (c_stringFile, "s_690");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Very funny! 10 cannibal Dewback jaws, as ordered!
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No but I had to take care of a Krayt dragon in my way.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_694");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_751");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_866");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 95);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition_isOnQuestOne (player, npc))
	{
		doAnimationAction (npc, "laugh_titter");

		//-- NPC: Aww, are you back already? It proved a little too tough for you eh?
		string_id message = new string_id (c_stringFile, "s_758");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No! I'm just here to get the directions again.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No I'm just here to tell you that I will do this unarmed.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: No but I can't right now, I have more important things to do.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'm a developer/tester and want to clear quests.
		boolean hasResponse3 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_isGm (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_760");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_764");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_768");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_862");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 105);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: I suppose you are another young hunter that has come to hear the stories of the great Qakkee?
		string_id message = new string_id (c_stringFile, "s_786");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Indeed I am, he was something else!
		boolean hasResponse0 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_hasEp3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Who's Qakkee?
		boolean hasResponse1 = false;
		if (ep3_clone_relics_hunter_qakkee_condition_hasEp3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: No, I thought you had something important to say.
		boolean hasResponse2 = false;
		if (ep3_clone_relics_hunter_qakkee_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_788");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_840");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_856");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId", 116);

			npcStartConversation (player, npc, "ep3_clone_relics_hunter_qakkee", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "ep3_clone_relics_hunter_qakkee")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

	if (branchId == 1 && ep3_clone_relics_hunter_qakkee_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && ep3_clone_relics_hunter_qakkee_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && ep3_clone_relics_hunter_qakkee_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && ep3_clone_relics_hunter_qakkee_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && ep3_clone_relics_hunter_qakkee_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && ep3_clone_relics_hunter_qakkee_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && ep3_clone_relics_hunter_qakkee_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && ep3_clone_relics_hunter_qakkee_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && ep3_clone_relics_hunter_qakkee_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && ep3_clone_relics_hunter_qakkee_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && ep3_clone_relics_hunter_qakkee_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && ep3_clone_relics_hunter_qakkee_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && ep3_clone_relics_hunter_qakkee_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && ep3_clone_relics_hunter_qakkee_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && ep3_clone_relics_hunter_qakkee_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && ep3_clone_relics_hunter_qakkee_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && ep3_clone_relics_hunter_qakkee_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && ep3_clone_relics_hunter_qakkee_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && ep3_clone_relics_hunter_qakkee_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && ep3_clone_relics_hunter_qakkee_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && ep3_clone_relics_hunter_qakkee_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && ep3_clone_relics_hunter_qakkee_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && ep3_clone_relics_hunter_qakkee_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && ep3_clone_relics_hunter_qakkee_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && ep3_clone_relics_hunter_qakkee_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && ep3_clone_relics_hunter_qakkee_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && ep3_clone_relics_hunter_qakkee_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 40 && ep3_clone_relics_hunter_qakkee_handleBranch40 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && ep3_clone_relics_hunter_qakkee_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && ep3_clone_relics_hunter_qakkee_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && ep3_clone_relics_hunter_qakkee_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && ep3_clone_relics_hunter_qakkee_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && ep3_clone_relics_hunter_qakkee_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 48 && ep3_clone_relics_hunter_qakkee_handleBranch48 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && ep3_clone_relics_hunter_qakkee_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && ep3_clone_relics_hunter_qakkee_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 55 && ep3_clone_relics_hunter_qakkee_handleBranch55 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 56 && ep3_clone_relics_hunter_qakkee_handleBranch56 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 57 && ep3_clone_relics_hunter_qakkee_handleBranch57 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 60 && ep3_clone_relics_hunter_qakkee_handleBranch60 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && ep3_clone_relics_hunter_qakkee_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && ep3_clone_relics_hunter_qakkee_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && ep3_clone_relics_hunter_qakkee_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 65 && ep3_clone_relics_hunter_qakkee_handleBranch65 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 66 && ep3_clone_relics_hunter_qakkee_handleBranch66 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 68 && ep3_clone_relics_hunter_qakkee_handleBranch68 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 70 && ep3_clone_relics_hunter_qakkee_handleBranch70 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 74 && ep3_clone_relics_hunter_qakkee_handleBranch74 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 75 && ep3_clone_relics_hunter_qakkee_handleBranch75 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 76 && ep3_clone_relics_hunter_qakkee_handleBranch76 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 77 && ep3_clone_relics_hunter_qakkee_handleBranch77 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 78 && ep3_clone_relics_hunter_qakkee_handleBranch78 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 80 && ep3_clone_relics_hunter_qakkee_handleBranch80 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 83 && ep3_clone_relics_hunter_qakkee_handleBranch83 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 84 && ep3_clone_relics_hunter_qakkee_handleBranch84 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 86 && ep3_clone_relics_hunter_qakkee_handleBranch86 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 87 && ep3_clone_relics_hunter_qakkee_handleBranch87 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 88 && ep3_clone_relics_hunter_qakkee_handleBranch88 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 90 && ep3_clone_relics_hunter_qakkee_handleBranch90 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 91 && ep3_clone_relics_hunter_qakkee_handleBranch91 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 92 && ep3_clone_relics_hunter_qakkee_handleBranch92 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 95 && ep3_clone_relics_hunter_qakkee_handleBranch95 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 96 && ep3_clone_relics_hunter_qakkee_handleBranch96 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 97 && ep3_clone_relics_hunter_qakkee_handleBranch97 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 99 && ep3_clone_relics_hunter_qakkee_handleBranch99 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 101 && ep3_clone_relics_hunter_qakkee_handleBranch101 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 104 && ep3_clone_relics_hunter_qakkee_handleBranch104 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 105 && ep3_clone_relics_hunter_qakkee_handleBranch105 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 109 && ep3_clone_relics_hunter_qakkee_handleBranch109 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 116 && ep3_clone_relics_hunter_qakkee_handleBranch116 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 117 && ep3_clone_relics_hunter_qakkee_handleBranch117 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 118 && ep3_clone_relics_hunter_qakkee_handleBranch118 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 120 && ep3_clone_relics_hunter_qakkee_handleBranch120 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 121 && ep3_clone_relics_hunter_qakkee_handleBranch121 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 124 && ep3_clone_relics_hunter_qakkee_handleBranch124 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 126 && ep3_clone_relics_hunter_qakkee_handleBranch126 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 127 && ep3_clone_relics_hunter_qakkee_handleBranch127 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.ep3_clone_relics_hunter_qakkee.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

