// ======================================================================
//
// tutorial_imperial_broker.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.25 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.features;
include library.space_skill;
include library.trainerlocs;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/tutorial_imperial_broker";

// ======================================================================
// Script Conditions
// ======================================================================

boolean tutorial_imperial_broker_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_isImperialFaction (obj_id player, obj_id npc)
{
	string factionName = factions.getFaction( player );
	if ( factionName == null || factionName == "" )
		return false;
	else	
		return ( factionName.equals( factions.FACTION_IMPERIAL ));
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_isMaleImperialFaction (obj_id player, obj_id npc)
{
	return ( tutorial_imperial_broker_condition_isImperialFaction( player, npc ) && ( getGender(player)==GENDER_MALE ) );
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_isMalePlayer (obj_id player, obj_id npc)
{
	return ( getGender( player )==GENDER_MALE );
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_justSpokeToPlayer (obj_id player, obj_id npc)
{
	return ( utils.hasScriptVar( player, "justTalkedToImpBroker"));
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_justSpokeToMalePlayer (obj_id player, obj_id npc)
{
	return ( tutorial_imperial_broker_condition_isMalePlayer(player,npc) && tutorial_imperial_broker_condition_justSpokeToPlayer(player,npc) );
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_isRebelFaction (obj_id player, obj_id npc)
{
	string factionName = factions.getFaction( player );
	if ( factionName == null || factionName == "" )
		return false;
	else	
		return ( factionName.equals( factions.FACTION_REBEL ));
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_isWookieePlayer (obj_id player, obj_id npc)
{
	return ( getSpecies(player)==SPECIES_WOOKIEE );
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_wasDismissedRebel (obj_id player, obj_id npc)
{
	return ( utils.getIntScriptVar( player, "brokerDismissed")==3 );
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_wasDismissedWookiee (obj_id player, obj_id npc)
{
	return ( utils.getIntScriptVar( player, "brokerDismissed")==2 );
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_wasDismissedWoman (obj_id player, obj_id npc)
{
	return ( utils.getIntScriptVar( player, "brokerDismissed")==1 );
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_isImperialPilot (obj_id player, obj_id npc)
{
	return space_skill.hasImperialSkill(player);
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_isPilot (obj_id player, obj_id npc)
{
	return space_skill.hasSpaceSkills(player);
}

// ----------------------------------------------------------------------

boolean tutorial_imperial_broker_condition_hasSpace (obj_id player, obj_id npc)
{
	return ( features.isSpaceEdition(player) );
}

// ======================================================================
// Script Actions
// ======================================================================

void tutorial_imperial_broker_action_addCorelliaImpWaypoint (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "justTalkedToImpBroker", true );
	trainerlocs.getTrainerLocationWaypoint( player, "imperial", "corellia" );
}

// ----------------------------------------------------------------------

void tutorial_imperial_broker_action_addTatooineImpWaypoint (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "justTalkedToImpBroker", true );
	trainerlocs.getTrainerLocationWaypoint( player, "imperial", "tatooine" );
}

// ----------------------------------------------------------------------

void tutorial_imperial_broker_action_addNabooImpWaypoint (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "justTalkedToImpBroker", true );
	trainerlocs.getTrainerLocationWaypoint( player, "imperial", "naboo" );
}

// ----------------------------------------------------------------------

void tutorial_imperial_broker_action_dismissWoman (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "brokerDismissed", 1 );
}

// ----------------------------------------------------------------------

void tutorial_imperial_broker_action_dismissWookiee (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "brokerDismissed", 2 );
}

// ----------------------------------------------------------------------

void tutorial_imperial_broker_action_dismissRebel (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "brokerDismissed", 3 );
}

// ----------------------------------------------------------------------

void tutorial_imperial_broker_action_retireImperial (obj_id player, obj_id npc)
{
	space_skill.retire(player, space_skill.IMPERIAL);
}

// ----------------------------------------------------------------------

void tutorial_imperial_broker_action_doSystemMessage (obj_id player, obj_id npc)
{
	sendSystemMessage( player, new string_id( "skill_teacher", "requires_jtl" ) );
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int tutorial_imperial_broker_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am a pilot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can I be a pilot?
	if (response == "s_6edcb265")
	{
		tutorial_imperial_broker_action_doSystemMessage (player, npc);

		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: You don't look like much of a pilot to me. You're not what we call, 'properly equipped', if you know what I mean.
			string_id message = new string_id (c_stringFile, "s_e671e2ef");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I know what you mean.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean.
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b369f7cd");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_54818bbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You don't look like much of a pilot to me. You're not what we call, 'properly equipped', if you know what I mean.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know what you mean.
	if (response == "s_b369f7cd")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Well, off you go, then. Use that terminal there to select your destination. I'll fly you there myself.
			string_id message = new string_id (c_stringFile, "s_2698cd26");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what you mean.
	if (response == "s_54818bbc")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: It takes a special breed to be a pilot like me.
			string_id message = new string_id (c_stringFile, "s_7b522690");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Was there something else, sir?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, sir.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, sir.
			string_id message = new string_id (c_stringFile, "s_772c81dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, sir.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_929272e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Was there something else, ma'am?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, ma'am.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, ma'am.
			string_id message = new string_id (c_stringFile, "s_c2c89e35");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, ma'am.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_b89f31b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, sir!  How may I be of assistance?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you recruiting Imperial Pilots?
	if (response == "s_4b0c16d2")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Sir! Yes, sir!  Are you here to sign-up?
			string_id message = new string_id (c_stringFile, "s_8c387e5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, just checking.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes.  What do I need to do?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8ed93c34");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0ea6e8c");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Never mind.
	if (response == "s_169df3bb")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Of course.  My mistake.
			string_id message = new string_id (c_stringFile, "s_f5f50ae3");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sir! Yes, sir!  Are you here to sign-up?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, just checking.
	if (response == "s_8ed93c34")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: Very well, sir.  Please come see me if you change your mind.  The Empire could use more soldiers of your caliber in space!
			string_id message = new string_id (c_stringFile, "s_8755e6eb");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.  What do I need to do?
	if (response == "s_b0ea6e8c")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			//-- NPC: I'm authorized to send you to one of three assignments, sir.  You can choose Storm Squadron in the Tatooine system, Black Epsilon in the Corellia system, or the Imperial Inquisition in the Naboo system.
			string_id message = new string_id (c_stringFile, "s_4577214d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm authorized to send you to one of three assignments, sir.  You can choose Storm Squadron in the Tatooine system, Black Epsilon in the Corellia system, or the Imperial Inquisition in the Naboo system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, sir.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, sir.
			string_id message = new string_id (c_stringFile, "s_772c81dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, sir.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_929272e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, sir.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, sir.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Very well, sir.  What about Storm Squadron or the Imperial Inquisition?
			string_id message = new string_id (c_stringFile, "s_a7d90105");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more
	if (response == "s_df2917b1")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tutorial_imperial_broker_action_addCorelliaImpWaypoint (player, npc);

			//-- NPC: Yes, sir.  I can't release any specific details about their current engagements, but I will add the location of their own recruiter to your datapad.  If you'll speak with this officer in the Imperial Base on Talus, he will get you squared away.
			string_id message = new string_id (c_stringFile, "s_cc37bca4");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well, sir.  What about Storm Squadron or the Imperial Inquisition?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, sir.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, sir.
			string_id message = new string_id (c_stringFile, "s_772c81dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, sir.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_929272e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Yes, sir.  What about Black Epsilon or the Imperial Inquisition?
			string_id message = new string_id (c_stringFile, "s_a8b71650");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more
	if (response == "s_df2917b1")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			tutorial_imperial_broker_action_addTatooineImpWaypoint (player, npc);

			//-- NPC: Yes, sir.  I can't release any specific details about their current engagements, but I will add the location of the Storm Squadron commander to your datapad.  He'll be glad to have a soldier of your standing.  You can find him in the Bestine Pilot's Club next to the capitol building on Tatooine.
			string_id message = new string_id (c_stringFile, "s_5817eef2");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, sir.  What about Black Epsilon or the Imperial Inquisition?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, sir.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, sir.
			string_id message = new string_id (c_stringFile, "s_772c81dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, sir.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_929272e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, sir.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: Are you interested in either Black Epsilon or the Storm Squadron, sir?
			string_id message = new string_id (c_stringFile, "s_ed23ffff");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me what you can
	if (response == "s_13a23fbc")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "manipulate_medium");

			tutorial_imperial_broker_action_addNabooImpWaypoint (player, npc);

			//-- NPC: There's not much more I can say, sir, but I will add the location to your datapad.  Talk to the Imperial Pilot trainer in Kaadara.  You'll find the association hall in the main plaza.
			string_id message = new string_id (c_stringFile, "s_4874a395");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you interested in either Black Epsilon or the Storm Squadron, sir?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, sir.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, sir.
			string_id message = new string_id (c_stringFile, "s_772c81dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, sir.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_929272e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, ma'am!  How may I be of assistance?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you recruiting Imperial Pilots?
	if (response == "s_4b0c16d2")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Ma'am!  Yes, ma'am!  Are you here to sign-up?
			string_id message = new string_id (c_stringFile, "s_d78c7ec0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, just checking.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes.  What do I need to do?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8ed93c34");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0ea6e8c");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Never mind.
	if (response == "s_169df3bb")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Of course.  My mistake.
			string_id message = new string_id (c_stringFile, "s_f5f50ae3");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ma'am!  Yes, ma'am!  Are you here to sign-up?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, just checking.
	if (response == "s_8ed93c34")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: Very well, ma'am.  Please come see me if you change your mind.  The Empire could use more soldiers of your caliber in space!
			string_id message = new string_id (c_stringFile, "s_83a7ae89");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.  What do I need to do?
	if (response == "s_b0ea6e8c")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I'm authorized to send you to one of three assignments, ma'am.  You can choose Storm Squadron in the Tatooine system, Black Epsilon in the Corellia system, or the Imperial Inquisition in the Naboo system.
			string_id message = new string_id (c_stringFile, "s_b8884586");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm authorized to send you to one of three assignments, ma'am.  You can choose Storm Squadron in the Tatooine system, Black Epsilon in the Corellia system, or the Imperial Inquisition in the Naboo system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, ma'am.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, ma'am.
			string_id message = new string_id (c_stringFile, "s_c2c89e35");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, ma'am.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_b89f31b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, ma'am.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, ma'am.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Very well, ma'am.  What about Storm Squadron or the Imperial Inquisition?
			string_id message = new string_id (c_stringFile, "s_407fb5e3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more
	if (response == "s_df2917b1")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			tutorial_imperial_broker_action_addCorelliaImpWaypoint (player, npc);

			//-- NPC: Yes, ma'am.  I can't release any specific details about their current engagements, but I will add the location of their own recruiter to your datapad.  If you'll speak with this officer in the Imperial Base on Talus, he will get you squared away.
			string_id message = new string_id (c_stringFile, "s_b945b46a");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well, ma'am.  What about Storm Squadron or the Imperial Inquisition?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, ma'am.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, ma'am.
			string_id message = new string_id (c_stringFile, "s_c2c89e35");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, ma'am.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_b89f31b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I see.  What about Black Epsilon or the Imperial Inquisition?
			string_id message = new string_id (c_stringFile, "s_42693321");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more
	if (response == "s_df2917b1")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			tutorial_imperial_broker_action_addTatooineImpWaypoint (player, npc);

			//-- NPC: Yes, ma'am.  I can't release any specific details about their current engagements, but I will add this location to your datapad.  Talk to the trainer in the Bestine Pilot's Club, near the capitol building on Tatooine.
			string_id message = new string_id (c_stringFile, "s_cd41504a");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I see.  What about Black Epsilon or the Imperial Inquisition?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, ma'am.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, ma'am.
			string_id message = new string_id (c_stringFile, "s_c2c89e35");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, ma'am.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_b89f31b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, ma'am.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Would you be interested in either Black Epsilon or the Storm Squadron, ma'am?
			string_id message = new string_id (c_stringFile, "s_d28bda49");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me what you can
	if (response == "s_13a23fbc")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			tutorial_imperial_broker_action_addNabooImpWaypoint (player, npc);

			//-- NPC: There's not much more I can say, ma'am, but I will add this location to your datapad.  Talk to the Pilot Trainer in the association hall on Kaadara, near the main plaza.
			string_id message = new string_id (c_stringFile, "s_64bd89e0");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Would you be interested in either Black Epsilon or the Storm Squadron, ma'am?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, ma'am.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, ma'am.
			string_id message = new string_id (c_stringFile, "s_c2c89e35");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, ma'am.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_b89f31b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Greetings, citizen! Would you like to learn about exciting opportunities for pilots in the Imperial Navy?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. Tell me.
	if (response == "s_57d2e86d")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Emperor is building a vast and powerful navy to defend the galaxy! We have been commanded to create vacancies within our ranks - for dutiful citizens such as you.  Among them are the Storm Squadron in the Tatooine system, Black Epsilon in the Corellia system, and the Imperial Inquisition in the Naboo system.
			string_id message = new string_id (c_stringFile, "s_b26292fd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can you tell me about Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about Storm Squadron.
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. The Imperial Inquisition. Enlighten me, pilot.
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ee369d68");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5612445a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3930f2e7");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No. Never mind.
	if (response == "s_445a2be4")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Ok.
			string_id message = new string_id (c_stringFile, "s_895d093");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Emperor is building a vast and powerful navy to defend the galaxy! We have been commanded to create vacancies within our ranks - for dutiful citizens such as you.  Among them are the Storm Squadron in the Tatooine system, Black Epsilon in the Corellia system, and the Imperial Inquisition in the Naboo system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can you tell me about Black Epsilon?
	if (response == "s_ee369d68")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area.
			string_id message = new string_id (c_stringFile, "s_86cb9899");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about Storm Squadron.
	if (response == "s_5612445a")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 41);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. The Imperial Inquisition. Enlighten me, pilot.
	if (response == "s_3930f2e7")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system.  Their mission is highly classified.
			string_id message = new string_id (c_stringFile, "s_3c9c6441");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Then tell me what you can.
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98a4e725");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: What about Storm Squadron or the Imperial Inquisition?
			string_id message = new string_id (c_stringFile, "s_9f862c74");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can you tell me about Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about Storm Squadron.
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. The Imperial Inquisition. Enlighten me, pilot.
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ee369d68");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5612445a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3930f2e7");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more
	if (response == "s_df2917b1")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			tutorial_imperial_broker_action_addCorelliaImpWaypoint (player, npc);

			//-- NPC: I won't release any specific details about their current engagements, but I will add the location of their recruiting officer to your datapad.  You can find him in the Imperial Base on Talus, in the Corellia system. Use the travel terminal here to reach Corellia. From there - take a transport to Talus.
			string_id message = new string_id (c_stringFile, "s_c8bbb089");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you, sir.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_987445c9");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What about Storm Squadron or the Imperial Inquisition?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can you tell me about Black Epsilon?
	if (response == "s_ee369d68")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area.
			string_id message = new string_id (c_stringFile, "s_86cb9899");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about Storm Squadron.
	if (response == "s_5612445a")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 41);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. The Imperial Inquisition. Enlighten me, pilot.
	if (response == "s_3930f2e7")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system.  Their mission is highly classified.
			string_id message = new string_id (c_stringFile, "s_3c9c6441");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Then tell me what you can.
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98a4e725");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I won't release any specific details about their current engagements, but I will add the location of their recruiting officer to your datapad.  You can find him in the Imperial Base on Talus, in the Corellia system. Use the travel terminal here to reach Corellia. From there - take a transport to Talus.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, sir.
	if (response == "s_987445c9")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Good hunting!
			string_id message = new string_id (c_stringFile, "s_f4792d0c");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: What about Black Epsilon or the Imperial Inquisition?
			string_id message = new string_id (c_stringFile, "s_727cf8b6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more
	if (response == "s_df2917b1")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "manipulate_medium");

			tutorial_imperial_broker_action_addTatooineImpWaypoint (player, npc);

			//-- NPC: I won't release any specific details about their current engagements, but I will add the location of the Storm Squadron pilot trainer to your datapad.  You'll find him in the Bestine Pilot's club on Tatooine, next to the capitol building. Use the travel terminal to reach Tatooine. Select the Bestine Township location.
			string_id message = new string_id (c_stringFile, "s_d0f9b54");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you, sir.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_987445c9");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What about Black Epsilon or the Imperial Inquisition?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, sir.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, sir.
			string_id message = new string_id (c_stringFile, "s_772c81dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, sir.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_929272e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I won't release any specific details about their current engagements, but I will add the location of the Storm Squadron pilot trainer to your datapad.  You'll find him in the Bestine Pilot's club on Tatooine, next to the capitol building. Use the travel terminal to reach Tatooine. Select the Bestine Township location.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, sir.
	if (response == "s_987445c9")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Good hunting!
			string_id message = new string_id (c_stringFile, "s_f4792d0c");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Imperial Inquisition is a special unit located in the Naboo system.  Their mission is highly classified.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in that.
	if (response == "s_70f18874")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Are you interested in either Black Epsilon or the Storm Squadron, sir?
			string_id message = new string_id (c_stringFile, "s_ed23ffff");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Epsilon?
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Storm Squadron?
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Inquisition?
			boolean hasResponse2 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then tell me what you can.
	if (response == "s_98a4e725")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			tutorial_imperial_broker_action_addNabooImpWaypoint (player, npc);

			//-- NPC: I can't tell you anything. It's highly classified. However, I can add the location of the unit's pilot trainer to your datapad.  You may speak to him in the main plaza association hall in Kaadara, on Naboo. Use the travel terminal here to reach Naboo. Choose the Kaadara starport as your landing site.
			string_id message = new string_id (c_stringFile, "s_f202ee89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you, sir.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_987445c9");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch46 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you interested in either Black Epsilon or the Storm Squadron, sir?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Epsilon?
	if (response == "s_1b2086de")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, sir.  Black Epsilon is stationed at the Imperial Base on Talus, as Corellia itself is officially neutral... for now.  There are reports of considerable Rebel activity in the area, as you can imagine.  They could really use more pilots there, sir.
			string_id message = new string_id (c_stringFile, "s_772c81dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Storm Squadron?
	if (response == "s_4cafd748")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Storm Squadron is one of the Empire's finest military units, in space or otherwise.  They're the very model of the sort of hard-core, dedicated, soldiers that secure the Empire throughout the galaxy.
			string_id message = new string_id (c_stringFile, "s_e0db13e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df2917b1");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Inquisition?
	if (response == "s_23ef3e33")
	{
		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: Imperial Inquisition is a special unit located in the Naboo system, sir.  They tend to deal in highly classified missions, so I'm afraid there's not a lot I'm authorized to tell you.
			string_id message = new string_id (c_stringFile, "s_929272e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not interested in that.
			boolean hasResponse0 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me what you can
			boolean hasResponse1 = false;
			if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70f18874");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_13a23fbc");

				setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tutorial_imperial_broker_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I can't tell you anything. It's highly classified. However, I can add the location of the unit's pilot trainer to your datapad.  You may speak to him in the main plaza association hall in Kaadara, on Naboo. Use the travel terminal here to reach Naboo. Choose the Kaadara starport as your landing site.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, sir.
	if (response == "s_987445c9")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Good hunting!
			string_id message = new string_id (c_stringFile, "s_f4792d0c");
			removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.tutorial_imperial_broker");

	setCondition (self, CONDITION_SPACE_INTERESTING);
	setInvulnerable( self, true );
	setName( self, "Colonel Dorran" );
	factions.setFaction( self, factions.FACTION_IMPERIAL );
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_SPACE_INTERESTING);
	setInvulnerable( self, true );
	setName( self, "Colonel Dorran" );
	factions.setFaction( self, factions.FACTION_IMPERIAL );
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceToBehavior( self, player );
	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.tutorial_imperial_broker");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (!tutorial_imperial_broker_condition_hasSpace (player, npc))
	{
		doAnimationAction (npc, "point_to_self");

		//-- NPC: I am a pilot.
		string_id message = new string_id (c_stringFile, "s_b2884420");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Can I be a pilot?
		boolean hasResponse0 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6edcb265");

			setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 1);

			npcStartConversation (player, npc, "tutorial_imperial_broker", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tutorial_imperial_broker_condition_justSpokeToMalePlayer (player, npc))
	{
		doAnimationAction (npc, "shrug_hands");

		//-- NPC: Was there something else, sir?
		string_id message = new string_id (c_stringFile, "s_8d91ee22");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Black Epsilon?
		boolean hasResponse0 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Storm Squadron?
		boolean hasResponse1 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Imperial Inquisition?
		boolean hasResponse2 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

			setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 10);

			npcStartConversation (player, npc, "tutorial_imperial_broker", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tutorial_imperial_broker_condition_justSpokeToPlayer (player, npc))
	{
		doAnimationAction (npc, "shrug_hands");

		//-- NPC: Was there something else, ma'am?
		string_id message = new string_id (c_stringFile, "s_ac926b4b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Black Epsilon?
		boolean hasResponse0 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Storm Squadron?
		boolean hasResponse1 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Imperial Inquisition?
		boolean hasResponse2 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1b2086de");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4cafd748");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_23ef3e33");

			setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 24);

			npcStartConversation (player, npc, "tutorial_imperial_broker", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tutorial_imperial_broker_condition_isMaleImperialFaction (player, npc))
	{
		doAnimationAction (npc, "salute1");

		doAnimationAction (player, "salute1");

		//-- NPC: Yes, sir!  How may I be of assistance?
		string_id message = new string_id (c_stringFile, "s_2eee3e2e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you recruiting Imperial Pilots?
		boolean hasResponse0 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Never mind.
		boolean hasResponse1 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4b0c16d2");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_169df3bb");

			setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 7);

			npcStartConversation (player, npc, "tutorial_imperial_broker", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tutorial_imperial_broker_condition_isImperialFaction (player, npc))
	{
		doAnimationAction (npc, "salute1");

		doAnimationAction (player, "salute1");

		//-- NPC: Yes, ma'am!  How may I be of assistance?
		string_id message = new string_id (c_stringFile, "s_fda95628");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you recruiting Imperial Pilots?
		boolean hasResponse0 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Never mind.
		boolean hasResponse1 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4b0c16d2");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_169df3bb");

			setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 21);

			npcStartConversation (player, npc, "tutorial_imperial_broker", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "salute2");

		//-- NPC: Greetings, citizen! Would you like to learn about exciting opportunities for pilots in the Imperial Navy?
		string_id message = new string_id (c_stringFile, "s_f4f79047");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes. Tell me.
		boolean hasResponse0 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No. Never mind.
		boolean hasResponse1 = false;
		if (tutorial_imperial_broker_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_57d2e86d");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_445a2be4");

			setObjVar (player, "conversation.tutorial_imperial_broker.branchId", 35);

			npcStartConversation (player, npc, "tutorial_imperial_broker", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "tutorial_imperial_broker")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = getIntObjVar (player, "conversation.tutorial_imperial_broker.branchId");

	if (branchId == 1 && tutorial_imperial_broker_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && tutorial_imperial_broker_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && tutorial_imperial_broker_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && tutorial_imperial_broker_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && tutorial_imperial_broker_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && tutorial_imperial_broker_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && tutorial_imperial_broker_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && tutorial_imperial_broker_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && tutorial_imperial_broker_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && tutorial_imperial_broker_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && tutorial_imperial_broker_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && tutorial_imperial_broker_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && tutorial_imperial_broker_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && tutorial_imperial_broker_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && tutorial_imperial_broker_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && tutorial_imperial_broker_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && tutorial_imperial_broker_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && tutorial_imperial_broker_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && tutorial_imperial_broker_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && tutorial_imperial_broker_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && tutorial_imperial_broker_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && tutorial_imperial_broker_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && tutorial_imperial_broker_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && tutorial_imperial_broker_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && tutorial_imperial_broker_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && tutorial_imperial_broker_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && tutorial_imperial_broker_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && tutorial_imperial_broker_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && tutorial_imperial_broker_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && tutorial_imperial_broker_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && tutorial_imperial_broker_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 46 && tutorial_imperial_broker_handleBranch46 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && tutorial_imperial_broker_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	removeObjVar (player, "conversation.tutorial_imperial_broker.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

