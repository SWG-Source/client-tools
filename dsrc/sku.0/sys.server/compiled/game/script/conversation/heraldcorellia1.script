// ======================================================================
//
// heraldcorellia1.script
// Copyright 2003, Sony Online Entertainment
// All Rights Reserved.
//
// DO NOT EDIT THIS AUTO-GENERATED FILE.  PLEASE USE THE CONVERATION EDITOR!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/heraldcorellia1";

// ======================================================================
// Script Conditions
// ======================================================================

boolean heraldcorellia1_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ======================================================================
// Script Actions
// ======================================================================

void heraldcorellia1_action__defaultAction (obj_id player, obj_id npc)
{
}

// ----------------------------------------------------------------------

void heraldcorellia1_action_waypoint1 (obj_id player, obj_id npc)
{
location nyax = new location(1392, 0, -304);
obj_id waypoint = createWaypointInDatapad(player, nyax);
setWaypointName(waypoint, "Lord Nyax Followers");
setWaypointColor( waypoint, "blue" );
setWaypointVisible( waypoint, true );
setWaypointActive( waypoint, true );
doAnimationAction(npc, "shush");
}

// ----------------------------------------------------------------------

void heraldcorellia1_action_waypoint2 (obj_id player, obj_id npc)
{
location cave = new location(-2492, 0, 2906);
obj_id waypoint = createWaypointInDatapad(player, cave);
setWaypointName(waypoint, "Afarathu Cave");
setWaypointColor( waypoint, "blue" );
setWaypointVisible( waypoint, true );
setWaypointActive( waypoint, true );
}

// ----------------------------------------------------------------------

void heraldcorellia1_action_waypoint3 (obj_id player, obj_id npc)
{
location imperial = new location(4664, 0, -5784);
obj_id waypoint = createWaypointInDatapad(player, imperial);
setWaypointName(waypoint, "Imperial Stronghold");
setWaypointColor( waypoint, "blue" );
setWaypointVisible( waypoint, true );
setWaypointActive( waypoint, true );
}

// ----------------------------------------------------------------------

void heraldcorellia1_action_abovehead (obj_id player, obj_id npc)
{
doAnimationAction(npc, "hands_above_head");
}

// ----------------------------------------------------------------------

void heraldcorellia1_action_shush (obj_id player, obj_id npc)
{
doAnimationAction(npc, "shush");
}

// ----------------------------------------------------------------------

void heraldcorellia1_action_thoughtful (obj_id player, obj_id npc)
{
doAnimationAction(npc, "rub_chin_thoughtful");
}

// ----------------------------------------------------------------------

void heraldcorellia1_action_dismiss (obj_id player, obj_id npc)
{
doAnimationAction(npc, "wave_on_dismissing");
}

// ======================================================================
// Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "npc.conversation.heraldcorellia1");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}
// ----------------------------------------------------------------------

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "npc.conversation.heraldcorellia1");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	if (ai_lib.isInCombat (self) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (heraldcorellia1_condition__defaultCondition (player, self))
	{
		heraldcorellia1_action_abovehead (player, self);

		//-- NPC: AHH! Stay away from me! I didn't mean to--oh. You're not a part of Lord Nyax's Clan. What a relief! You scared me there. I am right, aren't I? You're not a spy, are you? ... are you?!
		string_id message = new string_id (c_stringFile, "s_62db6450");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No, I'm not.
		boolean hasResponse0 = false;
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I am a part of Lord Nyax's Clan, actually.
		boolean hasResponse1 = false;
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7634b077");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_fd0eff19");

			setObjVar (player, "conversation.heraldcorellia1.branchId", 1);

			npcStartConversation (player, self, "heraldcorellia1", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (self, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "heraldcorellia1")
		return SCRIPT_CONTINUE;

	int branchId = getIntObjVar (player, "conversation.heraldcorellia1.branchId");

	//-- [BRANCH NOTE] 
	//-- NPC: AHH! Stay away from me! I didn't mean to--oh. You're not a part of Lord Nyax's Clan. What a relief! You scared me there. I am right, aren't I? You're not a spy, are you? ... are you?!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I'm not.
	if (branchId == 1 && response == "s_7634b077")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: I'm glad to hear that. You had me scared there for a little bit.
			string_id message = new string_id (c_stringFile, "s_94914b83");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why are you so scared of them?
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yeah, well. Bye.
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4f78edca");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92abbb2d");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'AHH! Stay away from me! I didn't mean to--oh. You're not a part of Lord Nyax's Clan. What a relief! You scared me there. I am right, aren't I? You're not a spy, are you? ... are you?!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: AHH! Stay away from me! I didn't mean to--oh. You're not a part of Lord Nyax's Clan. What a relief! You scared me there. I am right, aren't I? You're not a spy, are you? ... are you?!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am a part of Lord Nyax's Clan, actually.
	if (branchId == 1 && response == "s_fd0eff19")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_thoughtful (player, self);

			//-- NPC: Hmm... I'm suspicious. You don't look like someone Lord Nyax would recruit. Fine. If you are a part of the clan, then where is the hideout?
			string_id message = new string_id (c_stringFile, "s_b24dbaa5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay, you got me. I'm not a part of the Clan.
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Um... West of here!
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_302a0829");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_16ef1618");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'AHH! Stay away from me! I didn't mean to--oh. You're not a part of Lord Nyax's Clan. What a relief! You scared me there. I am right, aren't I? You're not a spy, are you? ... are you?!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I'm glad to hear that. You had me scared there for a little bit.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why are you so scared of them?
	if (branchId == 2 && response == "s_4f78edca")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: I was one of them. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.
			string_id message = new string_id (c_stringFile, "s_2dc5fa6c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure. Where is Nyax?
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other activities?
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bb636d95");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f20cf001");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I'm glad to hear that. You had me scared there for a little bit.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I'm glad to hear that. You had me scared there for a little bit.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, well. Bye.
	if (branchId == 2 && response == "s_92abbb2d")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Bye!
			string_id message = new string_id (c_stringFile, "s_60613aaf");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I'm glad to hear that. You had me scared there for a little bit.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I was one of them. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure. Where is Nyax?
	if (branchId == 3 && response == "s_bb636d95")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint1 (player, self);

			//-- NPC: It's too hard to explain. Besides, someone else could be listening. I don't want them getting the information for free. I downloaded the information into your datapad.
			string_id message = new string_id (c_stringFile, "s_15b3396c");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I was one of them. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I was one of them. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other activities?
	if (branchId == 3 && response == "s_f20cf001")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
			string_id message = new string_id (c_stringFile, "s_5933dda");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Uh. Lord Nyax, actually.
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Afarathu Cave.
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Stronghold.
			boolean hasResponse2 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d004968e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4e268ec");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ce93e2a4");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I was one of them. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Uh. Lord Nyax, actually.
	if (branchId == 5 && response == "s_d004968e")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint1 (player, self);

			//-- NPC: I'm not going to tell you the location outloud. Downloading it into your datapad would be much easier. I hope you find the place with ease.
			string_id message = new string_id (c_stringFile, "s_1350775c");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Afarathu Cave.
	if (branchId == 5 && response == "s_4e268ec")
	{
		heraldcorellia1_action_waypoint2 (player, self);

		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: I actually don't know the location offhand. Let me see. Hmmm--Ahh! There, downloaded into your datapad. It may or may not be exact. I hope you won't get lost.
			string_id message = new string_id (c_stringFile, "s_a7c04702");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Stronghold.
	if (branchId == 5 && response == "s_ce93e2a4")
	{
		heraldcorellia1_action_waypoint3 (player, self);

		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Be careful there. They don't like visitors unless you're an Imperial. Better than being a Nyax follower! Eh. May as well be the same thing. Oh well! The location has been downloaded into your datapad.
			string_id message = new string_id (c_stringFile, "s_5bd24f96");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hmm... I'm suspicious. You don't look like someone Lord Nyax would recruit. Fine. If you are a part of the clan, then where is the hideout?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, you got me. I'm not a part of the Clan.
	if (branchId == 10 && response == "s_302a0829")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: That's what I thought. I knew you weren't. I should know. I was a part of the clan myself. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.
			string_id message = new string_id (c_stringFile, "s_a343f6b2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure. Where is Nyax?
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other activities?
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bb636d95");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f20cf001");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hmm... I'm suspicious. You don't look like someone Lord Nyax would recruit. Fine. If you are a part of the clan, then where is the hideout?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hmm... I'm suspicious. You don't look like someone Lord Nyax would recruit. Fine. If you are a part of the clan, then where is the hideout?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Um... West of here!
	if (branchId == 10 && response == "s_16ef1618")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Lies! I knew you weren't a part of the clan. I should know. I was one of them! I left because I didn't agree with what they were doing.
			string_id message = new string_id (c_stringFile, "s_43ff531e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What were they doing?
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't want to hear your life story.
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14db17db");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8487276");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hmm... I'm suspicious. You don't look like someone Lord Nyax would recruit. Fine. If you are a part of the clan, then where is the hideout?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: That's what I thought. I knew you weren't. I should know. I was a part of the clan myself. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure. Where is Nyax?
	if (branchId == 11 && response == "s_bb636d95")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint1 (player, self);

			//-- NPC: It's too hard to explain. Besides, someone else could be listening. I don't want them getting the information for free. I downloaded the information into your datapad.
			string_id message = new string_id (c_stringFile, "s_15b3396c");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'That's what I thought. I knew you weren't. I should know. I was a part of the clan myself. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: That's what I thought. I knew you weren't. I should know. I was a part of the clan myself. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other activities?
	if (branchId == 11 && response == "s_f20cf001")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
			string_id message = new string_id (c_stringFile, "s_5933dda");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Uh. Lord Nyax, actually.
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Afarathu Cave.
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Stronghold.
			boolean hasResponse2 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d004968e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4e268ec");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ce93e2a4");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'That's what I thought. I knew you weren't. I should know. I was a part of the clan myself. They don't like the fact that I left just to cash in on their location by providing others with their whereabouts. Say, would you like to know? Free of charge! I also know a little bit about other activities on this planet.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Uh. Lord Nyax, actually.
	if (branchId == 13 && response == "s_d004968e")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint1 (player, self);

			//-- NPC: I'm not going to tell you the location outloud. Downloading it into your datapad would be much easier. I hope you find the place with ease.
			string_id message = new string_id (c_stringFile, "s_1350775c");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Afarathu Cave.
	if (branchId == 13 && response == "s_4e268ec")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint2 (player, self);

			//-- NPC: I actually don't know the location offhand. Let me see. Hmmm--Ahh! There, downloaded into your datapad. It may or may not be exact. I hope you won't get lost.
			string_id message = new string_id (c_stringFile, "s_a7c04702");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Stronghold.
	if (branchId == 13 && response == "s_ce93e2a4")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint3 (player, self);

			//-- NPC: Be careful there. They don't like visitors unless you're an Imperial. Better than being a Nyax follower! Eh. May as well be the same thing. Oh well! The location has been downloaded into your datapad.
			string_id message = new string_id (c_stringFile, "s_5bd24f96");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Lies! I knew you weren't a part of the clan. I should know. I was one of them! I left because I didn't agree with what they were doing.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: What were they doing?
	if (branchId == 17 && response == "s_14db17db")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Uhh.. I'm not going to tell you. You'd probably hurt me if I did. I did some of those things, you know. Why don't you go there yourself and find out. Unless, you'd rather do something else on this planet.
			string_id message = new string_id (c_stringFile, "s_1abf1335");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll check out Nyax's Clan.
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What else is there to do on this planet?
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b5cd3612");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eff34704");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Lies! I knew you weren't a part of the clan. I should know. I was one of them! I left because I didn't agree with what they were doing.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Lies! I knew you weren't a part of the clan. I should know. I was one of them! I left because I didn't agree with what they were doing.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't want to hear your life story.
	if (branchId == 17 && response == "s_8487276")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_dismiss (player, self);

			//-- NPC: Hrmph! Fine! I was going to give you information, but now I'm not going to!
			string_id message = new string_id (c_stringFile, "s_508d181c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Fine! Good bye!
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Look, I'm sorry.
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7170206c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f4a2753b");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Lies! I knew you weren't a part of the clan. I should know. I was one of them! I left because I didn't agree with what they were doing.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Uhh.. I'm not going to tell you. You'd probably hurt me if I did. I did some of those things, you know. Why don't you go there yourself and find out. Unless, you'd rather do something else on this planet.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll check out Nyax's Clan.
	if (branchId == 18 && response == "s_b5cd3612")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint1 (player, self);

			//-- NPC: I'm not going to tell you the location outloud. Downloading it into your datapad would be much easier. I hope you find the place with ease.
			string_id message = new string_id (c_stringFile, "s_1350775c");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Uhh.. I'm not going to tell you. You'd probably hurt me if I did. I did some of those things, you know. Why don't you go there yourself and find out. Unless, you'd rather do something else on this planet.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Uhh.. I'm not going to tell you. You'd probably hurt me if I did. I did some of those things, you know. Why don't you go there yourself and find out. Unless, you'd rather do something else on this planet.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: What else is there to do on this planet?
	if (branchId == 18 && response == "s_eff34704")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
			string_id message = new string_id (c_stringFile, "s_5933dda");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Uh. Lord Nyax, actually.
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Afarathu Cave.
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Imperial Stronghold.
			boolean hasResponse2 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d004968e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4e268ec");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ce93e2a4");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Uhh.. I'm not going to tell you. You'd probably hurt me if I did. I did some of those things, you know. Why don't you go there yourself and find out. Unless, you'd rather do something else on this planet.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Uh. Lord Nyax, actually.
	if (branchId == 20 && response == "s_d004968e")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint1 (player, self);

			//-- NPC: I'm not going to tell you the location outloud. Downloading it into your datapad would be much easier. I hope you find the place with ease.
			string_id message = new string_id (c_stringFile, "s_1350775c");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Afarathu Cave.
	if (branchId == 20 && response == "s_4e268ec")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint2 (player, self);

			//-- NPC: I actually don't know the location offhand. Let me see. Hmmm--Ahh! There, downloaded into your datapad. It may or may not be exact. I hope you won't get lost.
			string_id message = new string_id (c_stringFile, "s_a7c04702");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Stronghold.
	if (branchId == 20 && response == "s_ce93e2a4")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint3 (player, self);

			//-- NPC: Be careful there. They don't like visitors unless you're an Imperial. Better than being a Nyax follower! Eh. May as well be the same thing. Oh well! The location has been downloaded into your datapad.
			string_id message = new string_id (c_stringFile, "s_5bd24f96");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hrmph! Fine! I was going to give you information, but now I'm not going to!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine! Good bye!
	if (branchId == 24 && response == "s_7170206c")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Hrmph!
			string_id message = new string_id (c_stringFile, "s_9d54ab3b");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hrmph! Fine! I was going to give you information, but now I'm not going to!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hrmph! Fine! I was going to give you information, but now I'm not going to!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Look, I'm sorry.
	if (branchId == 24 && response == "s_f4a2753b")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: You're lucky I'm desperate for help. I have some information regarding the Nyax Clan and their whereabouts. I also have information about other activities as well.
			string_id message = new string_id (c_stringFile, "s_abab52df");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What other activities?
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where's the Nyax Clan?
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f20cf001");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8aeb5275");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hrmph! Fine! I was going to give you information, but now I'm not going to!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: You're lucky I'm desperate for help. I have some information regarding the Nyax Clan and their whereabouts. I also have information about other activities as well.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other activities?
	if (branchId == 26 && response == "s_f20cf001")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
			string_id message = new string_id (c_stringFile, "s_5933dda");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Afarathu Cave.
			boolean hasResponse0 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Imperial Stronghold.
			boolean hasResponse1 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Uh. Lord Nyax, actually.
			boolean hasResponse2 = false;
			if (heraldcorellia1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4e268ec");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ce93e2a4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d004968e");

				setObjVar (player, "conversation.heraldcorellia1.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.heraldcorellia1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'You're lucky I'm desperate for help. I have some information regarding the Nyax Clan and their whereabouts. I also have information about other activities as well.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: You're lucky I'm desperate for help. I have some information regarding the Nyax Clan and their whereabouts. I also have information about other activities as well.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where's the Nyax Clan?
	if (branchId == 26 && response == "s_8aeb5275")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint1 (player, self);

			//-- NPC: I'm not going to tell you the location outloud. Downloading it into your datapad would be much easier. I hope you find the place with ease.
			string_id message = new string_id (c_stringFile, "s_1350775c");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'You're lucky I'm desperate for help. I have some information regarding the Nyax Clan and their whereabouts. I also have information about other activities as well.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Afarathu Cave.
	if (branchId == 27 && response == "s_4e268ec")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint2 (player, self);

			//-- NPC: I actually don't know the location offhand. Let me see. Hmmm--Ahh! There, downloaded into your datapad. It may or may not be exact. I hope you won't get lost.
			string_id message = new string_id (c_stringFile, "s_a7c04702");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperial Stronghold.
	if (branchId == 27 && response == "s_ce93e2a4")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint3 (player, self);

			//-- NPC: Be careful there. They don't like visitors unless you're an Imperial. Better than being a Nyax follower! Eh. May as well be the same thing. Oh well! The location has been downloaded into your datapad.
			string_id message = new string_id (c_stringFile, "s_5bd24f96");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Uh. Lord Nyax, actually.
	if (branchId == 27 && response == "s_d004968e")
	{
		//-- [NOTE] 
		if (heraldcorellia1_condition__defaultCondition (player, self))
		{
			heraldcorellia1_action_waypoint1 (player, self);

			//-- NPC: I'm not going to tell you the location outloud. Downloading it into your datapad would be much easier. I hope you find the place with ease.
			string_id message = new string_id (c_stringFile, "s_1350775c");
			removeObjVar (player, "conversation.heraldcorellia1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Well, there's the Afarathu Cave and the Imperial Strong hold. What are you interested in?' were false.");

		return SCRIPT_CONTINUE;
	}

	chat.chat (self, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	removeObjVar (player, "conversation.heraldcorellia1.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

