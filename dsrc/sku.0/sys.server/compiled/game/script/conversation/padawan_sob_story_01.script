// ======================================================================
//
// padawan_sob_story_01.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.34 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.jedi_trials;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/padawan_sob_story_01";

// ======================================================================
// Script Conditions
// ======================================================================

boolean padawan_sob_story_01_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean padawan_sob_story_01_condition_acceptedQuest (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		return hasObjVar(player, "jedi_trials.padawan_trials.temp.acceptedTask");
	}
	return false;
}

// ----------------------------------------------------------------------

boolean padawan_sob_story_01_condition_isTrialPlayer (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		string trialName = jedi_trials.getJediTrialName(player);
	
		if ( trialName != null && trialName.equals("sob_story") )
		{	
			return !hasObjVar(npc, "padawan_trials.playerFailed");
		}
	}
	return false;
}

// ----------------------------------------------------------------------

boolean padawan_sob_story_01_condition_spokeToNpc (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		return hasObjVar(player, "jedi_trials.padawan_trials.temp.spokeToTarget_01");
	}
	return false;
}

// ----------------------------------------------------------------------

boolean padawan_sob_story_01_condition_completedQuest (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		string trialName = jedi_trials.getJediTrialName(player);
	
		if ( trialName != null && !trialName.equals("sob_story") )
		{	
			return hasObjVar(npc, "padawan_trials.playerSucceeded");
		}
	}
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void padawan_sob_story_01_action_questFailure (obj_id player, obj_id npc)
{
	setObjVar(npc, "padawan_trials.playerFailed", true);
	
	messageTo(player, "handleQuestFailure", null, 1, false);
	return;
}

// ----------------------------------------------------------------------

void padawan_sob_story_01_action_questSuccess (obj_id player, obj_id npc)
{
	setObjVar(npc, "padawan_trials.playerSucceeded", true);
	
	messageTo(player, "handleTrialComplete", null, 1, false);
	return;
}

// ----------------------------------------------------------------------

void padawan_sob_story_01_action_sendToNextLoc (obj_id player, obj_id npc)
{
	setObjVar(npc, "padawan_trials.playerAccepted", true);
	setObjVar(player, "jedi_trials.padawan_trials.temp.acceptedTask", true);
	messageTo(player, "handleSetSecondLoc", null, 1, false);
	return;
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int padawan_sob_story_01_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Do you have the blaster? Were you able to find it?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I found his remains and the blaster in the area you expected.
	if (response == "s_da7087d0")
	{
		//-- [NOTE] Quest complete.
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "weeping");

			padawan_sob_story_01_action_questSuccess (player, npc);

			//-- NPC: Good. Good. I... thank you. This is hitting me harder than I thought it would. Please excuse me.
			string_id message = new string_id (c_stringFile, "s_91b4a77f");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] If player returns without blaster.
	//-- NPC: Why are you back without the blaster?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm working on it. Not quite finished yet.
	if (response == "s_e6ae286a")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Just hurry up and get me that blaster before someone else makes off with it.
			string_id message = new string_id (c_stringFile, "s_e2b8c011");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've decided you're a lunatic, lady, and probably don't even have a brother.
	if (response == "s_2438dea0")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_questFailure (player, npc);

			//-- NPC: I see. Then go. And thanks for nothing.
			string_id message = new string_id (c_stringFile, "s_5bd31ce");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh good, you are already here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I am.
	if (response == "s_36a4e374")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are here to help find my brother right?
			string_id message = new string_id (c_stringFile, "s_dbff867f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Um.. yeah.  That is exactly why I'm here.
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, not exactly.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_390df542");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f89b1ce4");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where else would I be?
	if (response == "s_b7139d72")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Now I'm confused.
			string_id message = new string_id (c_stringFile, "s_ab866ec6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You are not the only one.
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2e220ef9");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are here to help find my brother right?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Um.. yeah.  That is exactly why I'm here.
	if (response == "s_390df542")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Lies don't score you any points.
			string_id message = new string_id (c_stringFile, "s_83d29997");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, if that's the case, what are you talking about?
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's a shame, but I'm off.  I have things to do that I actually care about.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_291d0c7a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8de1965a");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, not exactly.
	if (response == "s_f89b1ce4")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good, because I could care less about that wasted soul.  I just want the blaster he stole from me back.  
			string_id message = new string_id (c_stringFile, "s_72071926");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, I have some time to spare.
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would rather kiss a Gamorrean.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6845d2f7");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94c910a4");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Lies don't score you any points.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, if that's the case, what are you talking about?
	if (response == "s_291d0c7a")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: My brother went out with a group of his friends last week, and has yet to return.  Neither have any of his friends.
			string_id message = new string_id (c_stringFile, "s_ae1fd723");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will help you find your brother.
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds like he's dead.  Sorry to break the news to you.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1cf278e9");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9a6c3b2a");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's a shame, but I'm off.  I have things to do that I actually care about.
	if (response == "s_8de1965a")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_questFailure (player, npc);

			//-- NPC: Right, I'm sure you do.
			string_id message = new string_id (c_stringFile, "s_164cf4c2");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: My brother went out with a group of his friends last week, and has yet to return.  Neither have any of his friends.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will help you find your brother.
	if (response == "s_1cf278e9")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please, I could care less about that waste of space.  I just want the blaster he took back.  
			string_id message = new string_id (c_stringFile, "s_182378be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where can I find him?
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5bdbb8de");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like he's dead.  Sorry to break the news to you.
	if (response == "s_9a6c3b2a")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: That's not what I'm worried about.  He took my father's blaster with him.  I want that thing back.
			string_id message = new string_id (c_stringFile, "s_4f7b1e26");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where can I find him?
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: So go and get it.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_25");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9525d947");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please, I could care less about that waste of space.  I just want the blaster he took back.  

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where can I find him?
	if (response == "s_5bdbb8de")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_sendToNextLoc (player, npc);

			//-- NPC: My brother and his friends were talking all last week about going camping somewhere in the wild.
			string_id message = new string_id (c_stringFile, "s_b7015989");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's not what I'm worried about.  He took my father's blaster with him.  I want that thing back.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where can I find him?
	if (response == "s_25")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_sendToNextLoc (player, npc);

			//-- NPC: Good. My brother and his friends were talking all last week about going camping somewhere in the wild.
			string_id message = new string_id (c_stringFile, "s_be83966b");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So go and get it.
	if (response == "s_9525d947")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Believe me, I would if I could.  Sadly I have other obligations, like standing here sending smart mouthed adventurers out to their most certain doom.
			string_id message = new string_id (c_stringFile, "s_b6e31aa4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Your charm has won me over, how can I help?
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, I'm afraid I've had enough of you as well.  Good bye.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128b86a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_290ad18b");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Believe me, I would if I could.  Sadly I have other obligations, like standing here sending smart mouthed adventurers out to their most certain doom.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Your charm has won me over, how can I help?
	if (response == "s_a128b86a")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_sendToNextLoc (player, npc);

			//-- NPC: Good. My brother and his friends were talking all last week about going camping somewhere in the wild.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I'm afraid I've had enough of you as well.  Good bye.
	if (response == "s_290ad18b")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_questFailure (player, npc);

			//-- NPC: You will not be missed.
			string_id message = new string_id (c_stringFile, "s_10163a72");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good, because I could care less about that wasted soul.  I just want the blaster he stole from me back.  

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, I have some time to spare.
	if (response == "s_6845d2f7")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_sendToNextLoc (player, npc);

			//-- NPC: Good. My brother and his friends were talking all last week about going camping somewhere in the wild.
			string_id message = new string_id (c_stringFile, "s_40");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would rather kiss a Gamorrean.
	if (response == "s_94c910a4")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_questFailure (player, npc);

			//-- NPC: That could be arranged.  Forget I asked.
			string_id message = new string_id (c_stringFile, "s_360d601b");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Now I'm confused.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You are not the only one.
	if (response == "s_2e220ef9")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: So are you, or are you not, here to help me.
			string_id message = new string_id (c_stringFile, "s_6f0025f1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm here, and yes, willing to help you.
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm here, but why would I want to help you. Jump off.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_297f30e9");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d3249f0b");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So are you, or are you not, here to help me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm here, and yes, willing to help you.
	if (response == "s_297f30e9")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: My brother went out with a group of his friends last week, and has yet to return.  Neither have any of his friends.
			string_id message = new string_id (c_stringFile, "s_986473");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will find your brother.
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds like he's dead.  Sorry to break the news to you.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d4cfb5b8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_69");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm here, but why would I want to help you. Jump off.
	if (response == "s_d3249f0b")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_questFailure (player, npc);

			//-- NPC: I figured as much, well, you are smarter than you look.
			string_id message = new string_id (c_stringFile, "s_ed0b42f4");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: My brother went out with a group of his friends last week, and has yet to return.  Neither have any of his friends.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will find your brother.
	if (response == "s_d4cfb5b8")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please, I could care less about that waste of space.  I just want the blaster he took back.  
			string_id message = new string_id (c_stringFile, "s_51");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where can I find him?
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: So go and get it.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_53");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like he's dead.  Sorry to break the news to you.
	if (response == "s_69")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: That's not what I'm worried about.  He took my father's blaster with him.  I want that thing back.
			string_id message = new string_id (c_stringFile, "s_71");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where can I find him?
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_73");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please, I could care less about that waste of space.  I just want the blaster he took back.  

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where can I find him?
	if (response == "s_53")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_sendToNextLoc (player, npc);

			//-- NPC: My brother and his friends were talking all last week about going camping somewhere in the wild.
			string_id message = new string_id (c_stringFile, "s_55");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So go and get it.
	if (response == "s_57")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Believe me, I would if I could.  Sadly I have other obligations, like standing here sending smart mouthed adventurers out to their most certain doom.
			string_id message = new string_id (c_stringFile, "s_59");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Your charm has won me over, how can I help?
			boolean hasResponse0 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, I'm afraid I've had enough of you as well.  Good bye.
			boolean hasResponse1 = false;
			if (padawan_sob_story_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_61");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65");

				utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Believe me, I would if I could.  Sadly I have other obligations, like standing here sending smart mouthed adventurers out to their most certain doom.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Your charm has won me over, how can I help?
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_sendToNextLoc (player, npc);

			//-- NPC: Good. My brother and his friends were talking all last week about going camping somewhere in the wild.
			string_id message = new string_id (c_stringFile, "s_63");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I'm afraid I've had enough of you as well.  Good bye.
	if (response == "s_65")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_questFailure (player, npc);

			//-- NPC: You will not be missed.
			string_id message = new string_id (c_stringFile, "s_67");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_sob_story_01_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's not what I'm worried about.  He took my father's blaster with him.  I want that thing back.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where can I find him?
	if (response == "s_73")
	{
		//-- [NOTE] 
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			padawan_sob_story_01_action_sendToNextLoc (player, npc);

			//-- NPC: Good,. My brother and his friends were talking all last week about going camping somewhere in the wild.
			string_id message = new string_id (c_stringFile, "s_f8143d38");
			utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.padawan_sob_story_01");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.padawan_sob_story_01");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (padawan_sob_story_01_condition_completedQuest (player, npc))
	{
		doAnimationAction (npc, "embarrassed");

		//-- NPC: This means quite a lot to me. Thank you.
		string_id message = new string_id (c_stringFile, "s_1f8a26f8");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (padawan_sob_story_01_condition_spokeToNpc (player, npc))
	{
		//-- NPC: Do you have the blaster? Were you able to find it?
		string_id message = new string_id (c_stringFile, "s_3d146002");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, I found his remains and the blaster in the area you expected.
		boolean hasResponse0 = false;
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_da7087d0");

			utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 2);

			npcStartConversation (player, npc, "padawan_sob_story_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] If player returns without blaster.
	if (padawan_sob_story_01_condition_acceptedQuest (player, npc))
	{
		//-- NPC: Why are you back without the blaster?
		string_id message = new string_id (c_stringFile, "s_45240d04");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm working on it. Not quite finished yet.
		boolean hasResponse0 = false;
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I've decided you're a lunatic, lady, and probably don't even have a brother.
		boolean hasResponse1 = false;
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e6ae286a");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2438dea0");

			utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 4);

			npcStartConversation (player, npc, "padawan_sob_story_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (padawan_sob_story_01_condition_isTrialPlayer (player, npc))
	{
		//-- NPC: Oh good, you are already here.
		string_id message = new string_id (c_stringFile, "s_4c1008a4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, I am.
		boolean hasResponse0 = false;
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Where else would I be?
		boolean hasResponse1 = false;
		if (padawan_sob_story_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_36a4e374");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b7139d72");

			utils.setScriptVar (player, "conversation.padawan_sob_story_01.branchId", 7);

			npcStartConversation (player, npc, "padawan_sob_story_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (padawan_sob_story_01_condition__defaultCondition (player, npc))
	{
		//-- NPC: More the fool am I. I should have stopped him. Or something. Now he's gone and he took it with him. Yes, quite the fool.
		string_id message = new string_id (c_stringFile, "s_202dc610");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "padawan_sob_story_01")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.padawan_sob_story_01.branchId");

	if (branchId == 2 && padawan_sob_story_01_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && padawan_sob_story_01_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && padawan_sob_story_01_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && padawan_sob_story_01_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && padawan_sob_story_01_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && padawan_sob_story_01_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && padawan_sob_story_01_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && padawan_sob_story_01_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && padawan_sob_story_01_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && padawan_sob_story_01_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && padawan_sob_story_01_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && padawan_sob_story_01_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && padawan_sob_story_01_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && padawan_sob_story_01_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && padawan_sob_story_01_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && padawan_sob_story_01_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.padawan_sob_story_01.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

