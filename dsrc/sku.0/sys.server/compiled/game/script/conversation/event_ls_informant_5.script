// ======================================================================
//
// event_ls_informant_5.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.34 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/event_ls_informant_5";

// ======================================================================
// Script Conditions
// ======================================================================

boolean event_ls_informant_5_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ======================================================================
// Script Actions
// ======================================================================

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int event_ls_informant_5_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Dramont told me to help out anyone looking for her ships. You one of them?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. What help do you have?
	if (response == "s_5")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sensors can't pinpoint the ships, but we can tell what area they fly over when moved. Search everywhere south and east of waypoint 4000, 0.
			string_id message = new string_id (c_stringFile, "s_6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what you mean. What is this all about?
	if (response == "s_7")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Niles Ferrier is the greatest ship thief I ever heard of. Been driving the RSF crazy for years trying to catch up to him.
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How often do the ships get moved? 
	if (response == "s_8")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Every two to five hours the ships are picked up. They drop a new one at the same time somewhere else in the region. Each time a new ship is dropped, you have a chance to get another Navigational Core.
			string_id message = new string_id (c_stringFile, "s_13");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I deliver the Navigational Cores?
	if (response == "s_9")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once you have a core from each of the six regions, bring them to Captain Dramont. She's runs a secret RSF task force from the basement of a house in Wayfar. The waypoint is Tatooine -5090, -6620.
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are the other informants?
	if (response == "s_10")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go to any starport on Dathomir, Endor or Yavin 4 (but not the Imperial Base). You'll find one of my mates getting a drink in the cantina. They can tell you if any of Ferrier's stolen ships have been seen in the area.
			string_id message = new string_id (c_stringFile, "s_25");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know enough. Goodbye.
	if (response == "s_11")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good luck.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sensors can't pinpoint the ships, but we can tell what area they fly over when moved. Search everywhere south and east of waypoint 4000, 0.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. What help do you have?
	if (response == "s_5")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sensors can't pinpoint the ships, but we can tell what area they fly over when moved. Search everywhere south and east of waypoint 4000, 0.
			string_id message = new string_id (c_stringFile, "s_6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what you mean. What is this all about?
	if (response == "s_7")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Niles Ferrier is the greatest ship thief I ever heard of. Been driving the RSF crazy for years trying to catch up to him.
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How often do the ships get moved? 
	if (response == "s_8")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Every two to five hours the ships are picked up. They drop a new one at the same time somewhere else in the region. Each time a new ship is dropped, you have a chance to get another Navigational Core.
			string_id message = new string_id (c_stringFile, "s_13");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I deliver the Navigational Cores?
	if (response == "s_9")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once you have a core from each of the six regions, bring them to Captain Dramont. She's runs a secret RSF task force from the basement of a house in Wayfar. The waypoint is Tatooine -5090, -6620.
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are the other informants?
	if (response == "s_10")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go to any starport on Dathomir, Endor or Yavin 4 (but not the Imperial Base). You'll find one of my mates getting a drink in the cantina. They can tell you if any of Ferrier's stolen ships have been seen in the area.
			string_id message = new string_id (c_stringFile, "s_25");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know enough. Goodbye.
	if (response == "s_11")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good luck.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Niles Ferrier is the greatest ship thief I ever heard of. Been driving the RSF crazy for years trying to catch up to him.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Go on.
	if (response == "s_14")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Seems he recently acquired a squadron each of X-wings and Tie Bombers. Hard to hide that much hardware, so the RSF hopes to find the ships.
			string_id message = new string_id (c_stringFile, "s_15");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What does the RSF want with faction ships?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_16");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Seems he recently acquired a squadron each of X-wings and Tie Bombers. Hard to hide that much hardware, so the RSF hopes to find the ships.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What does the RSF want with faction ships?
	if (response == "s_16")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: They want the Navigational Cores. That will tell the RSF where the ship's been and perhaps help them find their man.
			string_id message = new string_id (c_stringFile, "s_17");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I help?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_18");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They want the Navigational Cores. That will tell the RSF where the ship's been and perhaps help them find their man.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I help?
	if (response == "s_18")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Speak to the informants and search those regions. If you are the first to find that ship, you will be able to grab the Navigational Core.
			string_id message = new string_id (c_stringFile, "s_19");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then what?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_20");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Speak to the informants and search those regions. If you are the first to find that ship, you will be able to grab the Navigational Core.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then what?
	if (response == "s_20")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once you have a core from all six regions, bring them to Captain Dramont. She's got a headquarters in the basement of a house in Wayfar, Tatooine. Waypoint -5090 -6619.
			string_id message = new string_id (c_stringFile, "s_21");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I get if I return all six cores?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Once you have a core from all six regions, bring them to Captain Dramont. She's got a headquarters in the basement of a house in Wayfar, Tatooine. Waypoint -5090 -6619.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I get if I return all six cores?
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Dunno. Some sort of prize, but only if you are one of the first to do so.
			string_id message = new string_id (c_stringFile, "s_23");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Dunno. Some sort of prize, but only if you are one of the first to do so.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. What help do you have?
	if (response == "s_5")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sensors can't pinpoint the ships, but we can tell what area they fly over when moved. Search everywhere south and east of waypoint 4000, 0.
			string_id message = new string_id (c_stringFile, "s_6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what you mean. What is this all about?
	if (response == "s_7")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Niles Ferrier is the greatest ship thief I ever heard of. Been driving the RSF crazy for years trying to catch up to him.
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How often do the ships get moved? 
	if (response == "s_8")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Every two to five hours the ships are picked up. They drop a new one at the same time somewhere else in the region. Each time a new ship is dropped, you have a chance to get another Navigational Core.
			string_id message = new string_id (c_stringFile, "s_13");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I deliver the Navigational Cores?
	if (response == "s_9")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once you have a core from each of the six regions, bring them to Captain Dramont. She's runs a secret RSF task force from the basement of a house in Wayfar. The waypoint is Tatooine -5090, -6620.
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are the other informants?
	if (response == "s_10")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go to any starport on Dathomir, Endor or Yavin 4 (but not the Imperial Base). You'll find one of my mates getting a drink in the cantina. They can tell you if any of Ferrier's stolen ships have been seen in the area.
			string_id message = new string_id (c_stringFile, "s_25");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know enough. Goodbye.
	if (response == "s_11")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good luck.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Every two to five hours the ships are picked up. They drop a new one at the same time somewhere else in the region. Each time a new ship is dropped, you have a chance to get another Navigational Core.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. What help do you have?
	if (response == "s_5")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sensors can't pinpoint the ships, but we can tell what area they fly over when moved. Search everywhere south and east of waypoint 4000, 0.
			string_id message = new string_id (c_stringFile, "s_6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what you mean. What is this all about?
	if (response == "s_7")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Niles Ferrier is the greatest ship thief I ever heard of. Been driving the RSF crazy for years trying to catch up to him.
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How often do the ships get moved? 
	if (response == "s_8")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Every two to five hours the ships are picked up. They drop a new one at the same time somewhere else in the region. Each time a new ship is dropped, you have a chance to get another Navigational Core.
			string_id message = new string_id (c_stringFile, "s_13");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I deliver the Navigational Cores?
	if (response == "s_9")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once you have a core from each of the six regions, bring them to Captain Dramont. She's runs a secret RSF task force from the basement of a house in Wayfar. The waypoint is Tatooine -5090, -6620.
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are the other informants?
	if (response == "s_10")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go to any starport on Dathomir, Endor or Yavin 4 (but not the Imperial Base). You'll find one of my mates getting a drink in the cantina. They can tell you if any of Ferrier's stolen ships have been seen in the area.
			string_id message = new string_id (c_stringFile, "s_25");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know enough. Goodbye.
	if (response == "s_11")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good luck.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Once you have a core from each of the six regions, bring them to Captain Dramont. She's runs a secret RSF task force from the basement of a house in Wayfar. The waypoint is Tatooine -5090, -6620.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. What help do you have?
	if (response == "s_5")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sensors can't pinpoint the ships, but we can tell what area they fly over when moved. Search everywhere south and east of waypoint 4000, 0.
			string_id message = new string_id (c_stringFile, "s_6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what you mean. What is this all about?
	if (response == "s_7")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Niles Ferrier is the greatest ship thief I ever heard of. Been driving the RSF crazy for years trying to catch up to him.
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How often do the ships get moved? 
	if (response == "s_8")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Every two to five hours the ships are picked up. They drop a new one at the same time somewhere else in the region. Each time a new ship is dropped, you have a chance to get another Navigational Core.
			string_id message = new string_id (c_stringFile, "s_13");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I deliver the Navigational Cores?
	if (response == "s_9")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once you have a core from each of the six regions, bring them to Captain Dramont. She's runs a secret RSF task force from the basement of a house in Wayfar. The waypoint is Tatooine -5090, -6620.
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are the other informants?
	if (response == "s_10")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go to any starport on Dathomir, Endor or Yavin 4 (but not the Imperial Base). You'll find one of my mates getting a drink in the cantina. They can tell you if any of Ferrier's stolen ships have been seen in the area.
			string_id message = new string_id (c_stringFile, "s_25");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know enough. Goodbye.
	if (response == "s_11")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good luck.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int event_ls_informant_5_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Go to any starport on Dathomir, Endor or Yavin 4 (but not the Imperial Base). You'll find one of my mates getting a drink in the cantina. They can tell you if any of Ferrier's stolen ships have been seen in the area.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. What help do you have?
	if (response == "s_5")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sensors can't pinpoint the ships, but we can tell what area they fly over when moved. Search everywhere south and east of waypoint 4000, 0.
			string_id message = new string_id (c_stringFile, "s_6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what you mean. What is this all about?
	if (response == "s_7")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Niles Ferrier is the greatest ship thief I ever heard of. Been driving the RSF crazy for years trying to catch up to him.
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How often do the ships get moved? 
	if (response == "s_8")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Every two to five hours the ships are picked up. They drop a new one at the same time somewhere else in the region. Each time a new ship is dropped, you have a chance to get another Navigational Core.
			string_id message = new string_id (c_stringFile, "s_13");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I deliver the Navigational Cores?
	if (response == "s_9")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once you have a core from each of the six regions, bring them to Captain Dramont. She's runs a secret RSF task force from the basement of a house in Wayfar. The waypoint is Tatooine -5090, -6620.
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are the other informants?
	if (response == "s_10")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go to any starport on Dathomir, Endor or Yavin 4 (but not the Imperial Base). You'll find one of my mates getting a drink in the cantina. They can tell you if any of Ferrier's stolen ships have been seen in the area.
			string_id message = new string_id (c_stringFile, "s_25");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. What help do you have?
			boolean hasResponse0 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know what you mean. What is this all about?
			boolean hasResponse1 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How often do the ships get moved? 
			boolean hasResponse2 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Where do I deliver the Navigational Cores?
			boolean hasResponse3 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where are the other informants?
			boolean hasResponse4 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know enough. Goodbye.
			boolean hasResponse5 = false;
			if (event_ls_informant_5_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_11");

				utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know enough. Goodbye.
	if (response == "s_11")
	{
		//-- [NOTE] 
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good luck.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.event_ls_informant_5");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.event_ls_informant_5");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (event_ls_informant_5_condition__defaultCondition (player, npc))
	{
		//-- NPC: Dramont told me to help out anyone looking for her ships. You one of them?
		string_id message = new string_id (c_stringFile, "s_3");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes. What help do you have?
		boolean hasResponse0 = false;
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I don't know what you mean. What is this all about?
		boolean hasResponse1 = false;
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: How often do the ships get moved? 
		boolean hasResponse2 = false;
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Where do I deliver the Navigational Cores?
		boolean hasResponse3 = false;
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: Where are the other informants?
		boolean hasResponse4 = false;
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		//-- PLAYER: I know enough. Goodbye.
		boolean hasResponse5 = false;
		if (event_ls_informant_5_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse5 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_10");

			if (hasResponse5)
				responses [responseIndex++] = new string_id (c_stringFile, "s_11");

			utils.setScriptVar (player, "conversation.event_ls_informant_5.branchId", 1);

			npcStartConversation (player, npc, "event_ls_informant_5", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "event_ls_informant_5")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.event_ls_informant_5.branchId");

	if (branchId == 1 && event_ls_informant_5_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && event_ls_informant_5_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && event_ls_informant_5_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && event_ls_informant_5_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && event_ls_informant_5_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && event_ls_informant_5_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && event_ls_informant_5_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && event_ls_informant_5_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && event_ls_informant_5_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && event_ls_informant_5_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && event_ls_informant_5_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.event_ls_informant_5.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

