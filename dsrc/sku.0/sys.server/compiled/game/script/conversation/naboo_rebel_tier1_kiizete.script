// ======================================================================
//
// naboo_rebel_tier1_kiizete.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.28 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.features;
include library.skill;
include library.space_flags;
include library.space_quest;
include library.space_skill;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/naboo_rebel_tier1_kiizete";

// ======================================================================
// Script Conditions
// ======================================================================

boolean naboo_rebel_tier1_kiizete_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean naboo_rebel_tier1_kiizete_condition_isImperialPilot (obj_id player, obj_id npc)
{
	int playerFactionID = pvpGetAlignedFaction(player);
	if ( playerFactionID == ##"imperial" )
		return true;
	else
		return ( hasSkill( player, "pilot_imperial_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_tier1_kiizete_condition_hasCompletedQuestOne (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "destroy_surpriseattack", "naboo_rebel_1") );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_tier1_kiizete_condition_hasFailedQuestOne (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "destroy_surpriseattack", "naboo_rebel_1_surprise" ) ||
			 space_quest.hasAbortedQuest( player, "destroy_surpriseattack", "naboo_rebel_1_surprise" ) ||
			 space_quest.hasFailedQuest ( player, "patrol", "naboo_rebel_1" ) ||
			 space_quest.hasAbortedQuest( player, "patrol", "naboo_rebel_1" ));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_tier1_kiizete_condition_hasSpaceExpansion (obj_id player, obj_id npc)
{
	return ( features.isSpaceEdition( player ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_tier1_kiizete_condition_hasVeryFirstQuest (obj_id player, obj_id npc)
{
	//You've got your assignment, and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
	if (!space_quest.hasCertifiedShip(player))
		return false; 
	else	
		return ( space_quest.hasQuest( player, "patrol", "naboo_rebel_1" ) ||
			  	 space_quest.hasQuest( player, "destroy_surpriseattack", "naboo_rebel_1" ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_tier1_kiizete_condition_imp_isasking (obj_id player, obj_id npc)
{
	return ( utils.hasScriptVar( player, "imp_isasking" ));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_tier1_kiizete_condition_q1_has_discussed (obj_id player, obj_id npc)
{
	return ( utils.hasScriptVar( player, "q1_discussed" ));
}

// ======================================================================
// Script Actions
// ======================================================================

void naboo_rebel_tier1_kiizete_action_an_imp_is_asking (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "imp_isasking", true );
}

// ----------------------------------------------------------------------

void naboo_rebel_tier1_kiizete_action_q1_set_discussed (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "q1_discussed", true );
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int naboo_rebel_tier1_kiizete_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This droid is going to be the death of me! I'm sorry... I'm getting nowhere with this thing and I really need to get back home.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where's home?
	if (response == "s_ee55c9ed")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_right");

			//-- NPC: Dee'ja. My starship is grounded for the moment. I'm fulfilling a contract for the Empire right now. Got to get this droid's memory wiped and rebuilt.
			string_id message = new string_id (c_stringFile, "s_bc76e9cc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Your starship?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A contract?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8668104f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6d949f10");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's wrong with it?
	if (response == "s_7fa15433")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Its memory is defunct. Got an old pilot program in it. I gotta remove it... but it's proving to be difficult.
			string_id message = new string_id (c_stringFile, "s_46b93374");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Dee'ja. My starship is grounded for the moment. I'm fulfilling a contract for the Empire right now. Got to get this droid's memory wiped and rebuilt.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Your starship?
	if (response == "s_8668104f")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slump_head");

			//-- NPC: Yeah. I really miss flying! I can't wait to get my ship repaired.
			string_id message = new string_id (c_stringFile, "s_6472f6c1");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A contract?
	if (response == "s_6d949f10")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yeah... sure... a contract.
			string_id message = new string_id (c_stringFile, "s_88b50e");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sorry. I really don't have time to talk right now. I've been trying to get this droid reprogrammed all day. It's turning out to be a nightmare!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why reprogram it?
	if (response == "s_9ddf5e8a")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Order of the Empire! What can you do? I just grabbed the contract from a terminal up in Dee'ja Peak.
			string_id message = new string_id (c_stringFile, "s_dd3ba522");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why reprogram it?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's wrong with it?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Is this your job?
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition_imp_isasking (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9ddf5e8a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7fa15433");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e15545");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's wrong with it?
	if (response == "s_7fa15433")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: It's got this old pilot trainer program burned into it. Empire wants it out of there. It's not going to be easy, though. Moisture from the pond has gummed up this droid's circuitry. I'm going to have to replace a lot of parts!
			string_id message = new string_id (c_stringFile, "s_8379d6bb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why reprogram it?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's wrong with it?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Is this your job?
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition_imp_isasking (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9ddf5e8a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7fa15433");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e15545");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is this your job?
	if (response == "s_5e15545")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Sort of. I normally work a shift on the Kai'rek mining ship... but I got tagged by pirates on my first day of shore leave. I figure I'll need to run a few more contracts here on Naboo before I can afford to pay for repairs.
			string_id message = new string_id (c_stringFile, "s_489ec48f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you fly?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You erase droids on a mining ship?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fdbd2351");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dff67cdc");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Order of the Empire! What can you do? I just grabbed the contract from a terminal up in Dee'ja Peak.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why reprogram it?
	if (response == "s_9ddf5e8a")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Order of the Empire! What can you do? I just grabbed the contract from a terminal up in Dee'ja Peak.
			string_id message = new string_id (c_stringFile, "s_dd3ba522");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why reprogram it?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's wrong with it?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Is this your job?
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition_imp_isasking (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9ddf5e8a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7fa15433");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e15545");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's wrong with it?
	if (response == "s_7fa15433")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: It's got this old pilot trainer program burned into it. Empire wants it out of there. It's not going to be easy, though. Moisture from the pond has gummed up this droid's circuitry. I'm going to have to replace a lot of parts!
			string_id message = new string_id (c_stringFile, "s_8379d6bb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why reprogram it?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's wrong with it?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Is this your job?
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition_imp_isasking (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9ddf5e8a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7fa15433");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e15545");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is this your job?
	if (response == "s_5e15545")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Sort of. I normally work a shift on the Kai'rek mining ship... but I got tagged by pirates on my first day of shore leave. I figure I'll need to run a few more contracts here on Naboo before I can afford to pay for repairs.
			string_id message = new string_id (c_stringFile, "s_489ec48f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you fly?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You erase droids on a mining ship?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fdbd2351");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dff67cdc");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's got this old pilot trainer program burned into it. Empire wants it out of there. It's not going to be easy, though. Moisture from the pond has gummed up this droid's circuitry. I'm going to have to replace a lot of parts!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why reprogram it?
	if (response == "s_9ddf5e8a")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Order of the Empire! What can you do? I just grabbed the contract from a terminal up in Dee'ja Peak.
			string_id message = new string_id (c_stringFile, "s_dd3ba522");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why reprogram it?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's wrong with it?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Is this your job?
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition_imp_isasking (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9ddf5e8a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7fa15433");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e15545");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's wrong with it?
	if (response == "s_7fa15433")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: It's got this old pilot trainer program burned into it. Empire wants it out of there. It's not going to be easy, though. Moisture from the pond has gummed up this droid's circuitry. I'm going to have to replace a lot of parts!
			string_id message = new string_id (c_stringFile, "s_8379d6bb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why reprogram it?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's wrong with it?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Is this your job?
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition_imp_isasking (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9ddf5e8a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7fa15433");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e15545");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is this your job?
	if (response == "s_5e15545")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Sort of. I normally work a shift on the Kai'rek mining ship... but I got tagged by pirates on my first day of shore leave. I figure I'll need to run a few more contracts here on Naboo before I can afford to pay for repairs.
			string_id message = new string_id (c_stringFile, "s_489ec48f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you fly?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You erase droids on a mining ship?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fdbd2351");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dff67cdc");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sort of. I normally work a shift on the Kai'rek mining ship... but I got tagged by pirates on my first day of shore leave. I figure I'll need to run a few more contracts here on Naboo before I can afford to pay for repairs.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you fly?
	if (response == "s_fdbd2351")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: A '95. Hand-me-down. Look, I really need to get back to work...
			string_id message = new string_id (c_stringFile, "s_af558512");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You erase droids on a mining ship?
	if (response == "s_dff67cdc")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I do odd jobs, okay? And I need to get back to this one...
			string_id message = new string_id (c_stringFile, "s_8dee77bb");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So you're part of the Alliance now. How does it feel?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Meh.
	if (response == "s_b41e5c00")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "hair_flip");

			//-- NPC: I thought you would be excited.
			string_id message = new string_id (c_stringFile, "s_cbba56ce");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, I'm not.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am excited!
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a1ce31d");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_69a02cf2");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7c075dce");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know what to do next!
	if (response == "s_8bf64edf")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Take it easy. What don't you understand?
			string_id message = new string_id (c_stringFile, "s_bd21db14");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I launch?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do I do up there?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e97d81c5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3704b308");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It is my destiny.
	if (response == "s_1162efff")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: I see. Pretty intense. For me it's just a question of survival.
			string_id message = new string_id (c_stringFile, "s_9cd181e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Survival?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I know what you mean.
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c48bff0");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b369f7cd");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I thought you would be excited.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, I'm not.
	if (response == "s_6a1ce31d")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: I think you're scared.
			string_id message = new string_id (c_stringFile, "s_34d8b295");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am excited!
	if (response == "s_69a02cf2")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Well, just be safe up there. Okay?
			string_id message = new string_id (c_stringFile, "s_fd0a45a7");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_7c075dce")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_left");

			//-- NPC: Walk directly down the stairs... then wade into the water and slosh toward the starport. It's fun!
			string_id message = new string_id (c_stringFile, "s_fe562082");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Take it easy. What don't you understand?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I launch?
	if (response == "s_e97d81c5")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The starship terminal in the starport will allow you to do that. Go to the starport, and use the ship terminal to launch.
			string_id message = new string_id (c_stringFile, "s_5c46c104");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I land?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ed31d3f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fc27931b");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I do up there?
	if (response == "s_3704b308")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fly your route. Keep a low profile. Don't get into anything you can't handle. Come home safe.
			string_id message = new string_id (c_stringFile, "s_c1499ae3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I launch?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do I do up there?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e97d81c5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3704b308");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The starship terminal in the starport will allow you to do that. Go to the starport, and use the ship terminal to launch.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I land?
	if (response == "s_4ed31d3f")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: Fly back to a space station. Comm the space station and tell them you want to land.
			string_id message = new string_id (c_stringFile, "s_3e796521");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I comm a space station?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where will I find a space station?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a65b9c24");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5aa590e");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_fc27931b")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			//-- NPC: Take care.
			string_id message = new string_id (c_stringFile, "s_f6c52e55");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fly back to a space station. Comm the space station and tell them you want to land.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I comm a space station?
	if (response == "s_a65b9c24")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Use SHIFT+C to target it and open communication with the station commander. You can choose to repair your ship or land.
			string_id message = new string_id (c_stringFile, "s_890f3624");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I comm a space station?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where will I find a space station?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a65b9c24");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5aa590e");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where will I find a space station?
	if (response == "s_e5aa590e")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you launch, your ship computer system will create a 'launch waypoint' that you can use to find your way back. Keep this point active by opening your datapad, choosing the waypoint labeled 'Launch Waypoint', and choosing to 'Activate' it.
			string_id message = new string_id (c_stringFile, "s_2895a98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I comm a space station?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where will I find a space station?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a65b9c24");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5aa590e");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Use SHIFT+C to target it and open communication with the station commander. You can choose to repair your ship or land.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I comm a space station?
	if (response == "s_a65b9c24")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Use SHIFT+C to target it and open communication with the station commander. You can choose to repair your ship or land.
			string_id message = new string_id (c_stringFile, "s_890f3624");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I comm a space station?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where will I find a space station?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a65b9c24");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5aa590e");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where will I find a space station?
	if (response == "s_e5aa590e")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you launch, your ship computer system will create a 'launch waypoint' that you can use to find your way back. Keep this point active by opening your datapad, choosing the waypoint labeled 'Launch Waypoint', and choosing to 'Activate' it.
			string_id message = new string_id (c_stringFile, "s_2895a98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I comm a space station?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where will I find a space station?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a65b9c24");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5aa590e");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: When you launch, your ship computer system will create a 'launch waypoint' that you can use to find your way back. Keep this point active by opening your datapad, choosing the waypoint labeled 'Launch Waypoint', and choosing to 'Activate' it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I comm a space station?
	if (response == "s_a65b9c24")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Use SHIFT+C to target it and open communication with the station commander. You can choose to repair your ship or land.
			string_id message = new string_id (c_stringFile, "s_890f3624");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I comm a space station?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where will I find a space station?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a65b9c24");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5aa590e");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where will I find a space station?
	if (response == "s_e5aa590e")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you launch, your ship computer system will create a 'launch waypoint' that you can use to find your way back. Keep this point active by opening your datapad, choosing the waypoint labeled 'Launch Waypoint', and choosing to 'Activate' it.
			string_id message = new string_id (c_stringFile, "s_2895a98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I comm a space station?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where will I find a space station?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a65b9c24");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5aa590e");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fly your route. Keep a low profile. Don't get into anything you can't handle. Come home safe.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I launch?
	if (response == "s_e97d81c5")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The starship terminal in the starport will allow you to do that. Go to the starport, and use the ship terminal to launch.
			string_id message = new string_id (c_stringFile, "s_5c46c104");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I land?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ed31d3f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fc27931b");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I do up there?
	if (response == "s_3704b308")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fly your route. Keep a low profile. Don't get into anything you can't handle. Come home safe.
			string_id message = new string_id (c_stringFile, "s_c1499ae3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I launch?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do I do up there?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e97d81c5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3704b308");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I see. Pretty intense. For me it's just a question of survival.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Survival?
	if (response == "s_1c48bff0")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Yeah. I'll tell you about it sometime.
			string_id message = new string_id (c_stringFile, "s_4669a3e4");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know what you mean.
	if (response == "s_b369f7cd")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Sorry to hear that.
			string_id message = new string_id (c_stringFile, "s_705da14d");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Stinks to fail missions doesn't it?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you mean?
	if (response == "s_77e48d5b")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Nothing. Forget I said anything.
			string_id message = new string_id (c_stringFile, "s_d2083ee4");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. It does.
	if (response == "s_5df1433d")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Don't beat yourself up about it. Just get back up there and try again.
			string_id message = new string_id (c_stringFile, "s_5655b98e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Any pointers?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fc27931b");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_670bb9d5");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't beat yourself up about it. Just get back up there and try again.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_fc27931b")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: No problem.
			string_id message = new string_id (c_stringFile, "s_c261cfae");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Any pointers?
	if (response == "s_670bb9d5")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Me? No. I never made it as a fighter pilot. Sorry.
			string_id message = new string_id (c_stringFile, "s_b9934459");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hey... so tell me. How are things going?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Couldn't be better.
	if (response == "s_4bd66142")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: It's interesting you say that. I hear the Alliance is scrambling to stay ahead of the Empire in the Naboo system. You're tangled up with a mystery squadron of rookie TIE fighters... or something... What are you hearing?
			string_id message = new string_id (c_stringFile, "s_cff6adcf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: We're doing just fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm living it! Back off!
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: You don't need to worry.
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4c90db01");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8dbe594");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5dc9a083");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just doing my duty.
	if (response == "s_ce894b84")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: I hear things about a mysterious wing of TIE fighters. Like the Empire is relocating some combat school to the Naboo system. Stinks! We'll have squints crawling all over us!
			string_id message = new string_id (c_stringFile, "s_65372fa4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll take care of it.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5fe9928");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's all falling apart.
	if (response == "s_93f420a0")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stop");

			//-- NPC: Are you kidding me?
			string_id message = new string_id (c_stringFile, "s_85d464bf");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's interesting you say that. I hear the Alliance is scrambling to stay ahead of the Empire in the Naboo system. You're tangled up with a mystery squadron of rookie TIE fighters... or something... What are you hearing?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: We're doing just fine.
	if (response == "s_4c90db01")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: I hope you're right.
			string_id message = new string_id (c_stringFile, "s_82288395");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm living it! Back off!
	if (response == "s_8dbe594")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Sorry! Keep your cool, okay?
			string_id message = new string_id (c_stringFile, "s_49a108e5");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You don't need to worry.
	if (response == "s_5dc9a083")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: We'll see about that...
			string_id message = new string_id (c_stringFile, "s_5aff38e2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I hear things about a mysterious wing of TIE fighters. Like the Empire is relocating some combat school to the Naboo system. Stinks! We'll have squints crawling all over us!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll take care of it.
	if (response == "s_a5fe9928")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I think you just might.
			string_id message = new string_id (c_stringFile, "s_8920c0f2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Who are you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Give Kiizete your name)
	if (response == "s_e518378e")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Nice to meet you, %NU. What are you doing around here?
			string_id message = new string_id (c_stringFile, "s_46dcc469");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nothing.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want to join the Rebel Alliance!
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Destiny has drawn me here.
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bd02793");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c899d08d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2f212745");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 43);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: First, who are you?
	if (response == "s_17edec66")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "greet");

			//-- NPC: My name is Kiizete. I am a technician.
			string_id message = new string_id (c_stringFile, "s_2b5181ae");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are you doing here?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b26194cb");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Nice to meet you, %NU. What are you doing around here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing.
	if (response == "s_6bd02793")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: I see.
			string_id message = new string_id (c_stringFile, "s_61657d0f");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to join the Rebel Alliance!
	if (response == "s_c899d08d")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: What makes you think I would have anything to do with that?
			string_id message = new string_id (c_stringFile, "s_695e9ca2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I got a recruitment email from the Alliance.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That droid is an Alliance training droid!
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9940f453");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ff33d98b");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Destiny has drawn me here.
	if (response == "s_2f212745")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: I see, %NU... What destiny are you talking about?
			string_id message = new string_id (c_stringFile, "s_9d788910");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Alliance needs me.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it.
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I've come to join the Rebel Alliance.
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ed72de53");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ca932c28");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c39cb5eb");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 51);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What makes you think I would have anything to do with that?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I got a recruitment email from the Alliance.
	if (response == "s_9940f453")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You did, huh? Why don't you talk to V3 over there. He has a state-of-the-art Alliance Pilot training program!
			string_id message = new string_id (c_stringFile, "s_861c2497");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: State-of-the-art?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you teach?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9274747");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14aade81");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 46);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That droid is an Alliance training droid!
	if (response == "s_ff33d98b")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: For now, maybe. But I'm going to fix that. So... let me get back to it!
			string_id message = new string_id (c_stringFile, "s_565d17f2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch46 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You did, huh? Why don't you talk to V3 over there. He has a state-of-the-art Alliance Pilot training program!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: State-of-the-art?
	if (response == "s_c9274747")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Well, maybe not. It's just the best that we can afford out here on Naboo. The Alliance doesn't have many friends in this system. It works, though. It's a very effective program.
			string_id message = new string_id (c_stringFile, "s_8a824d71");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I use it?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afd1368");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you teach?
	if (response == "s_14aade81")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: No. Never really got the urge to teach piloting. Try the droid... He's good.
			string_id message = new string_id (c_stringFile, "s_a5edd9e5");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, maybe not. It's just the best that we can afford out here on Naboo. The Alliance doesn't have many friends in this system. It works, though. It's a very effective program.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I use it?
	if (response == "s_afd1368")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Just talk to the droid. He'll take it from there!
			string_id message = new string_id (c_stringFile, "s_2e823c5f");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch51 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I see, %NU... What destiny are you talking about?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Alliance needs me.
	if (response == "s_ed72de53")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: For what?
			string_id message = new string_id (c_stringFile, "s_e5b2f859");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: To save the galaxy!
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know yet...
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a95b1747");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95658182");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 52);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Forget it.
	if (response == "s_ca932c28")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "greet");

			//-- NPC: Ah. I probably wouldn't understand anyhow... Take care!
			string_id message = new string_id (c_stringFile, "s_e22b6287");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've come to join the Rebel Alliance.
	if (response == "s_c39cb5eb")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_right");

			//-- NPC: Then get to it! Grab hold of that droid and get your ship codes and flight clearance!
			string_id message = new string_id (c_stringFile, "s_6a22af26");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: For what?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: To save the galaxy!
	if (response == "s_a95b1747")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_right");

			//-- NPC: You should talk to the droid...
			string_id message = new string_id (c_stringFile, "s_92a0a895");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know yet...
	if (response == "s_95658182")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: But you know that this is your calling? Did it come to you in a dream? I'm not trying to insult you - I do have dreams myself from time to time... sitting in a cockpit, blasting TIE fighters left and right!
			string_id message = new string_id (c_stringFile, "s_7136aa3f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. Like that.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: My destiny is different.
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9f32bc26");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6103a248");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: But you know that this is your calling? Did it come to you in a dream? I'm not trying to insult you - I do have dreams myself from time to time... sitting in a cockpit, blasting TIE fighters left and right!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. Like that.
	if (response == "s_9f32bc26")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Then you should speak with V3-FX right here. He can get you set up as an Alliance Pilot!
			string_id message = new string_id (c_stringFile, "s_104f81b8");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My destiny is different.
	if (response == "s_6103a248")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow");

			//-- NPC: I wish you good luck, my friend.
			string_id message = new string_id (c_stringFile, "s_1ae7f565");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch59 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: My name is Kiizete. I am a technician.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are you doing here?
	if (response == "s_b26194cb")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I've taken a contract to reprogram the droid... but it's proving to be a lot harder than anyone thought it would be. Maybe impossible!
			string_id message = new string_id (c_stringFile, "s_ea3a9cb0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Give Kiizete your name)
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Good luck, then!
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e518378e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1628fad8");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch60 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've taken a contract to reprogram the droid... but it's proving to be a lot harder than anyone thought it would be. Maybe impossible!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Give Kiizete your name)
	if (response == "s_e518378e")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "greet");

			//-- NPC: Nice to meet you, %NU. What are you doing around here?
			string_id message = new string_id (c_stringFile, "s_46dcc469");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nothing.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want to join the Rebel Alliance!
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Destiny has drawn me here.
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bd02793");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c899d08d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2f212745");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 61);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Good luck, then!
	if (response == "s_1628fad8")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Thanks!
			string_id message = new string_id (c_stringFile, "s_c4682ea6");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Nice to meet you, %NU. What are you doing around here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing.
	if (response == "s_6bd02793")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I see.
			string_id message = new string_id (c_stringFile, "s_61657d0f");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to join the Rebel Alliance!
	if (response == "s_c899d08d")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: What makes you think I would have anything to do with that?
			string_id message = new string_id (c_stringFile, "s_695e9ca2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I got a recruitment email from the Alliance.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That droid is an Alliance training droid!
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9940f453");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ff33d98b");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Destiny has drawn me here.
	if (response == "s_2f212745")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: I see, %NU... What destiny are you talking about?
			string_id message = new string_id (c_stringFile, "s_9d788910");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Alliance needs me.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it.
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I've come to join the Rebel Alliance.
			boolean hasResponse2 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ed72de53");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ca932c28");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c39cb5eb");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 69);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				chat.chat (npc, player, pp);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch63 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What makes you think I would have anything to do with that?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I got a recruitment email from the Alliance.
	if (response == "s_9940f453")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You did, huh? Why don't you talk to V3 over there. He has a state-of-the-art Alliance Pilot training program!
			string_id message = new string_id (c_stringFile, "s_861c2497");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: State-of-the-art?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you teach?
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c9274747");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14aade81");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 64);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That droid is an Alliance training droid!
	if (response == "s_ff33d98b")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: For now, maybe. But I'm going to fix that. So... let me get back to it!
			string_id message = new string_id (c_stringFile, "s_565d17f2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You did, huh? Why don't you talk to V3 over there. He has a state-of-the-art Alliance Pilot training program!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: State-of-the-art?
	if (response == "s_c9274747")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Well, maybe not. It's just the best that we can afford out here on Naboo. The Alliance doesn't have many friends in this system. It works, though. It's a very effective program.
			string_id message = new string_id (c_stringFile, "s_8a824d71");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I use it?
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afd1368");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you teach?
	if (response == "s_14aade81")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_right");

			//-- NPC: No. Never really got the urge to teach piloting. Try the droid... He's good.
			string_id message = new string_id (c_stringFile, "s_a5edd9e5");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch65 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, maybe not. It's just the best that we can afford out here on Naboo. The Alliance doesn't have many friends in this system. It works, though. It's a very effective program.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I use it?
	if (response == "s_afd1368")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_right");

			//-- NPC: Just talk to the droid. He'll take it from there!
			string_id message = new string_id (c_stringFile, "s_2e823c5f");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch69 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I see, %NU... What destiny are you talking about?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Alliance needs me.
	if (response == "s_ed72de53")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: For what?
			string_id message = new string_id (c_stringFile, "s_e5b2f859");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: To save the galaxy!
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't know yet...
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a95b1747");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95658182");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Forget it.
	if (response == "s_ca932c28")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "greet");

			//-- NPC: Ah. I probably wouldn't understand anyhow... Take care!
			string_id message = new string_id (c_stringFile, "s_e22b6287");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've come to join the Rebel Alliance.
	if (response == "s_c39cb5eb")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_right");

			//-- NPC: Then get to it! Grab hold of that droid and get your ship codes and flight clearance!
			string_id message = new string_id (c_stringFile, "s_6a22af26");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch70 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: For what?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: To save the galaxy!
	if (response == "s_a95b1747")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_right");

			//-- NPC: You should talk to the droid...
			string_id message = new string_id (c_stringFile, "s_92a0a895");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know yet...
	if (response == "s_95658182")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: But you know that this is your calling? Did it come to you in a dream? I'm not trying to insult you - I do have dreams myself from time to time... sitting in a cockpit, blasting TIE fighters left and right!
			string_id message = new string_id (c_stringFile, "s_7136aa3f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. Like that.
			boolean hasResponse0 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: My destiny is different.
			boolean hasResponse1 = false;
			if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9f32bc26");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6103a248");

				utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 72);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_tier1_kiizete_handleBranch72 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: But you know that this is your calling? Did it come to you in a dream? I'm not trying to insult you - I do have dreams myself from time to time... sitting in a cockpit, blasting TIE fighters left and right!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. Like that.
	if (response == "s_9f32bc26")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Then you should speak with V3-FX right here. He can get you set up as an Alliance Pilot!
			string_id message = new string_id (c_stringFile, "s_104f81b8");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My destiny is different.
	if (response == "s_6103a248")
	{
		//-- [NOTE] 
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "greet");

			//-- NPC: I wish you good luck, my friend.
			string_id message = new string_id (c_stringFile, "s_1ae7f565");
			utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.naboo_rebel_tier1_kiizete");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );
	factions.setFaction( self, factions.FACTION_REBEL );
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );
	factions.setFaction( self, factions.FACTION_REBEL );
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	
	faceTo( self, player );

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.naboo_rebel_tier1_kiizete");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (!naboo_rebel_tier1_kiizete_condition_hasSpaceExpansion (player, npc))
	{
		doAnimationAction (npc, "check_wrist_device");

		//-- NPC: This droid is going to be the death of me! I'm sorry... I'm getting nowhere with this thing and I really need to get back home.
		string_id message = new string_id (c_stringFile, "s_933941d0");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Where's home?
		boolean hasResponse0 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What's wrong with it?
		boolean hasResponse1 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ee55c9ed");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7fa15433");

			utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 1);

			npcStartConversation (player, npc, "naboo_rebel_tier1_kiizete", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_tier1_kiizete_condition_imp_isasking (player, npc))
	{
		doAnimationAction (npc, "stop");

		//-- NPC: I'm serious. I don't have time for you right now!
		string_id message = new string_id (c_stringFile, "s_7a0420ae");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_tier1_kiizete_condition_isImperialPilot (player, npc))
	{
		doAnimationAction (npc, "shake_head_disgust");

		naboo_rebel_tier1_kiizete_action_an_imp_is_asking (player, npc);

		//-- NPC: Sorry. I really don't have time to talk right now. I've been trying to get this droid reprogrammed all day. It's turning out to be a nightmare!
		string_id message = new string_id (c_stringFile, "s_bdc96417");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Why reprogram it?
		boolean hasResponse0 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What's wrong with it?
		boolean hasResponse1 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Is this your job?
		boolean hasResponse2 = false;
		if (naboo_rebel_tier1_kiizete_condition_imp_isasking (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9ddf5e8a");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7fa15433");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5e15545");

			utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 7);

			npcStartConversation (player, npc, "naboo_rebel_tier1_kiizete", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_tier1_kiizete_condition_q1_has_discussed (player, npc))
	{
		doAnimationAction (npc, "pound_fist_palm");

		//-- NPC: Look. Go to the starport. Find a terminal and launch into space. The war isn't going to wait for you!
		string_id message = new string_id (c_stringFile, "s_b78a2c76");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_tier1_kiizete_condition_hasVeryFirstQuest (player, npc))
	{
		doAnimationAction (npc, "greet");

		naboo_rebel_tier1_kiizete_action_q1_set_discussed (player, npc);

		//-- NPC: So you're part of the Alliance now. How does it feel?
		string_id message = new string_id (c_stringFile, "s_65e8c460");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Meh.
		boolean hasResponse0 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I don't know what to do next!
		boolean hasResponse1 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: It is my destiny.
		boolean hasResponse2 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b41e5c00");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8bf64edf");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1162efff");

			utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 14);

			npcStartConversation (player, npc, "naboo_rebel_tier1_kiizete", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_tier1_kiizete_condition_hasFailedQuestOne (player, npc))
	{
		doAnimationAction (npc, "slit_throat");

		//-- NPC: Stinks to fail missions doesn't it?
		string_id message = new string_id (c_stringFile, "s_17eec115");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What do you mean?
		boolean hasResponse0 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes. It does.
		boolean hasResponse1 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_77e48d5b");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5df1433d");

			utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 29);

			npcStartConversation (player, npc, "naboo_rebel_tier1_kiizete", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_tier1_kiizete_condition_hasCompletedQuestOne (player, npc))
	{
		//-- NPC: Hey... so tell me. How are things going?
		string_id message = new string_id (c_stringFile, "s_27b64c88");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Couldn't be better.
		boolean hasResponse0 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Just doing my duty.
		boolean hasResponse1 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: It's all falling apart.
		boolean hasResponse2 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4bd66142");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ce894b84");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_93f420a0");

			utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 34);

			npcStartConversation (player, npc, "naboo_rebel_tier1_kiizete", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: Who are you?
		string_id message = new string_id (c_stringFile, "s_ca776e30");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Give Kiizete your name)
		boolean hasResponse0 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: First, who are you?
		boolean hasResponse1 = false;
		if (naboo_rebel_tier1_kiizete_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e518378e");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_17edec66");

			utils.setScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId", 42);

			npcStartConversation (player, npc, "naboo_rebel_tier1_kiizete", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "naboo_rebel_tier1_kiizete")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

	if (branchId == 1 && naboo_rebel_tier1_kiizete_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && naboo_rebel_tier1_kiizete_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && naboo_rebel_tier1_kiizete_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && naboo_rebel_tier1_kiizete_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && naboo_rebel_tier1_kiizete_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && naboo_rebel_tier1_kiizete_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && naboo_rebel_tier1_kiizete_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && naboo_rebel_tier1_kiizete_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && naboo_rebel_tier1_kiizete_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && naboo_rebel_tier1_kiizete_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && naboo_rebel_tier1_kiizete_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && naboo_rebel_tier1_kiizete_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && naboo_rebel_tier1_kiizete_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && naboo_rebel_tier1_kiizete_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && naboo_rebel_tier1_kiizete_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && naboo_rebel_tier1_kiizete_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && naboo_rebel_tier1_kiizete_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && naboo_rebel_tier1_kiizete_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && naboo_rebel_tier1_kiizete_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && naboo_rebel_tier1_kiizete_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && naboo_rebel_tier1_kiizete_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && naboo_rebel_tier1_kiizete_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && naboo_rebel_tier1_kiizete_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 46 && naboo_rebel_tier1_kiizete_handleBranch46 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && naboo_rebel_tier1_kiizete_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 51 && naboo_rebel_tier1_kiizete_handleBranch51 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && naboo_rebel_tier1_kiizete_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && naboo_rebel_tier1_kiizete_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 59 && naboo_rebel_tier1_kiizete_handleBranch59 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 60 && naboo_rebel_tier1_kiizete_handleBranch60 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && naboo_rebel_tier1_kiizete_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 63 && naboo_rebel_tier1_kiizete_handleBranch63 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && naboo_rebel_tier1_kiizete_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 65 && naboo_rebel_tier1_kiizete_handleBranch65 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 69 && naboo_rebel_tier1_kiizete_handleBranch69 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 70 && naboo_rebel_tier1_kiizete_handleBranch70 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 72 && naboo_rebel_tier1_kiizete_handleBranch72 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.naboo_rebel_tier1_kiizete.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

