// ======================================================================
//
// ep3_forest_npc_greeter.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.30 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/ep3_forest_npc_greeter";

// ======================================================================
// Script Conditions
// ======================================================================

boolean ep3_forest_npc_greeter_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ======================================================================
// Script Actions
// ======================================================================

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int ep3_forest_npc_greeter_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: [Shaey nods to you.] Looks like you're a little lost...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah. I'm new to this area.
	if (response == "s_6")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ah, I see. Let me introduce myself. [Shaey bows.] I am Shaey Kayr, Kashyyyk Explorer extraordinaire! I know everything about Kashyyyk... and more! You have questions? Shaey Kayr has answers!
			string_id message = new string_id (c_stringFile, "s_8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, Shaey Kayr. Tell me about this place.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, I see. Let me introduce myself. [Shaey bows.] I am Shaey Kayr, Kashyyyk Explorer extraordinaire! I know everything about Kashyyyk... and more! You have questions? Shaey Kayr has answers!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, Shaey Kayr. Tell me about this place.
	if (response == "s_10")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: There's certain areas of the Kkowir Forest that make up its cold charm. Which area do you want to know about? There's the Dead Forest, the Webweaver Caves, the Kerritamba Village, the Great Tree, and the Myyydril Caverns.
			string_id message = new string_id (c_stringFile, "s_78");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There's certain areas of the Kkowir Forest that make up its cold charm. Which area do you want to know about? There's the Dead Forest, the Webweaver Caves, the Kerritamba Village, the Great Tree, and the Myyydril Caverns.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Dead Forest.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Dead Forest is a dangerous place. I don't recommend going there. [Shaey shrugs.] The Sayormi live there and revel in the dead forest around them. They practice their voodoo, cursing anything that lives in this place. Additionally, you'll find the Mouf gentle creatures that are better left alone. The Sayormi own them. No one dares enter the Dead Forest anymore.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Webweaver Caves.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: As expected, webweaver spiders live there, guarded by their Outcast friends. The Outcasts are people who have been shunned from the Kerritamba way of life. They retreated into the caves and live there. Even today, the Kerritamba people and the Outcasts are at war. It would be best to leave their rivalry be.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Is anything pleasant here?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Kerritamba Village.
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ahh... the Kerritamba village. The Kerritamba village is led by Chief Kerritamba who comes from a long, strong line of Wookiee warriors. The Kerritamba people go about their lives, distrusting most and turning away the rest. The only way to get them to warm up is through their leader. Speak with him if you wish to earn their trust.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Great Tree.
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Great Tree, also known to the Kerritamba people as Nyenthi'Oris, lives on the island to the north, past the Kerritamba village and on the Chenataa river. The Great Tree is diseased and dying. The Chief of the Kerritamba village is trying to find a cure even to this day. It is a sad tale.
			string_id message = new string_id (c_stringFile, "s_112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I wonder if I can help somehow.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Myyydril Caverns.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril Caverns is a mystery to those not living within its cold, damp walls. You'll have to travel there yourself. However, it is a dangerous place. Far more dangerous than even the Kkowir Forest.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I get there?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know about the areas. Where do I go next?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, you have several options. You can visit the Kerritamba village and speak with Chief Kerritamba or you can find the Myyydril Caverns. Do you need any information about these areas?
			string_id message = new string_id (c_stringFile, "s_175");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for the information. I'm going to explore.
	if (response == "s_176")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope you'll be safe. [Shaey bows and waves.]
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Dead Forest is a dangerous place. I don't recommend going there. [Shaey shrugs.] The Sayormi live there and revel in the dead forest around them. They practice their voodoo, cursing anything that lives in this place. Additionally, you'll find the Mouf gentle creatures that are better left alone. The Sayormi own them. No one dares enter the Dead Forest anymore.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hmm.
	if (response == "s_107")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: What other area do you want to learn about?
			string_id message = new string_id (c_stringFile, "s_108");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What other area do you want to learn about?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Dead Forest.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Dead Forest is a dangerous place. I don't recommend going there. [Shaey shrugs.] The Sayormi live there and revel in the dead forest around them. They practice their voodoo, cursing anything that lives in this place. Additionally, you'll find the Mouf gentle creatures that are better left alone. The Sayormi own them. No one dares enter the Dead Forest anymore.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Webweaver Caves.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: As expected, webweaver spiders live there, guarded by their Outcast friends. The Outcasts are people who have been shunned from the Kerritamba way of life. They retreated into the caves and live there. Even today, the Kerritamba people and the Outcasts are at war. It would be best to leave their rivalry be.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Is anything pleasant here?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Kerritamba Village.
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ahh... the Kerritamba village. The Kerritamba village is led by Chief Kerritamba who comes from a long, strong line of Wookiee warriors. The Kerritamba people go about their lives, distrusting most and turning away the rest. The only way to get them to warm up is through their leader. Speak with him if you wish to earn their trust.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Great Tree.
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Great Tree, also known to the Kerritamba people as Nyenthi'Oris, lives on the island to the north, past the Kerritamba village and on the Chenataa river. The Great Tree is diseased and dying. The Chief of the Kerritamba village is trying to find a cure even to this day. It is a sad tale.
			string_id message = new string_id (c_stringFile, "s_112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I wonder if I can help somehow.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Myyydril Caverns.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril Caverns is a mystery to those not living within its cold, damp walls. You'll have to travel there yourself. However, it is a dangerous place. Far more dangerous than even the Kkowir Forest.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I get there?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know about the areas. Where do I go next?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, you have several options. You can visit the Kerritamba village and speak with Chief Kerritamba or you can find the Myyydril Caverns. Do you need any information about these areas?
			string_id message = new string_id (c_stringFile, "s_175");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for the information. I'm going to explore.
	if (response == "s_176")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope you'll be safe. [Shaey bows and waves.]
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: As expected, webweaver spiders live there, guarded by their Outcast friends. The Outcasts are people who have been shunned from the Kerritamba way of life. They retreated into the caves and live there. Even today, the Kerritamba people and the Outcasts are at war. It would be best to leave their rivalry be.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is anything pleasant here?
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not likely. What other area do you want to learn about?
			string_id message = new string_id (c_stringFile, "s_111");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not likely. What other area do you want to learn about?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Dead Forest.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Dead Forest is a dangerous place. I don't recommend going there. [Shaey shrugs.] The Sayormi live there and revel in the dead forest around them. They practice their voodoo, cursing anything that lives in this place. Additionally, you'll find the Mouf gentle creatures that are better left alone. The Sayormi own them. No one dares enter the Dead Forest anymore.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Webweaver Caves.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: As expected, webweaver spiders live there, guarded by their Outcast friends. The Outcasts are people who have been shunned from the Kerritamba way of life. They retreated into the caves and live there. Even today, the Kerritamba people and the Outcasts are at war. It would be best to leave their rivalry be.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Is anything pleasant here?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Kerritamba Village.
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ahh... the Kerritamba village. The Kerritamba village is led by Chief Kerritamba who comes from a long, strong line of Wookiee warriors. The Kerritamba people go about their lives, distrusting most and turning away the rest. The only way to get them to warm up is through their leader. Speak with him if you wish to earn their trust.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Great Tree.
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Great Tree, also known to the Kerritamba people as Nyenthi'Oris, lives on the island to the north, past the Kerritamba village and on the Chenataa river. The Great Tree is diseased and dying. The Chief of the Kerritamba village is trying to find a cure even to this day. It is a sad tale.
			string_id message = new string_id (c_stringFile, "s_112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I wonder if I can help somehow.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Myyydril Caverns.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril Caverns is a mystery to those not living within its cold, damp walls. You'll have to travel there yourself. However, it is a dangerous place. Far more dangerous than even the Kkowir Forest.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I get there?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know about the areas. Where do I go next?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, you have several options. You can visit the Kerritamba village and speak with Chief Kerritamba or you can find the Myyydril Caverns. Do you need any information about these areas?
			string_id message = new string_id (c_stringFile, "s_175");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for the information. I'm going to explore.
	if (response == "s_176")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope you'll be safe. [Shaey bows and waves.]
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ahh... the Kerritamba village. The Kerritamba village is led by Chief Kerritamba who comes from a long, strong line of Wookiee warriors. The Kerritamba people go about their lives, distrusting most and turning away the rest. The only way to get them to warm up is through their leader. Speak with him if you wish to earn their trust.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see. Thank you for the information.
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed. What other area do you want to learn about?
			string_id message = new string_id (c_stringFile, "s_105");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed. What other area do you want to learn about?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Dead Forest.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Dead Forest is a dangerous place. I don't recommend going there. [Shaey shrugs.] The Sayormi live there and revel in the dead forest around them. They practice their voodoo, cursing anything that lives in this place. Additionally, you'll find the Mouf gentle creatures that are better left alone. The Sayormi own them. No one dares enter the Dead Forest anymore.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Webweaver Caves.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: As expected, webweaver spiders live there, guarded by their Outcast friends. The Outcasts are people who have been shunned from the Kerritamba way of life. They retreated into the caves and live there. Even today, the Kerritamba people and the Outcasts are at war. It would be best to leave their rivalry be.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Is anything pleasant here?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Kerritamba Village.
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ahh... the Kerritamba village. The Kerritamba village is led by Chief Kerritamba who comes from a long, strong line of Wookiee warriors. The Kerritamba people go about their lives, distrusting most and turning away the rest. The only way to get them to warm up is through their leader. Speak with him if you wish to earn their trust.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Great Tree.
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Great Tree, also known to the Kerritamba people as Nyenthi'Oris, lives on the island to the north, past the Kerritamba village and on the Chenataa river. The Great Tree is diseased and dying. The Chief of the Kerritamba village is trying to find a cure even to this day. It is a sad tale.
			string_id message = new string_id (c_stringFile, "s_112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I wonder if I can help somehow.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Myyydril Caverns.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril Caverns is a mystery to those not living within its cold, damp walls. You'll have to travel there yourself. However, it is a dangerous place. Far more dangerous than even the Kkowir Forest.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I get there?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know about the areas. Where do I go next?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, you have several options. You can visit the Kerritamba village and speak with Chief Kerritamba or you can find the Myyydril Caverns. Do you need any information about these areas?
			string_id message = new string_id (c_stringFile, "s_175");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for the information. I'm going to explore.
	if (response == "s_176")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope you'll be safe. [Shaey bows and waves.]
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Great Tree, also known to the Kerritamba people as Nyenthi'Oris, lives on the island to the north, past the Kerritamba village and on the Chenataa river. The Great Tree is diseased and dying. The Chief of the Kerritamba village is trying to find a cure even to this day. It is a sad tale.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I wonder if I can help somehow.
	if (response == "s_168")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: You'll have to speak with him about that. What other area do you want to learn about?
			string_id message = new string_id (c_stringFile, "s_169");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You'll have to speak with him about that. What other area do you want to learn about?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Dead Forest.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Dead Forest is a dangerous place. I don't recommend going there. [Shaey shrugs.] The Sayormi live there and revel in the dead forest around them. They practice their voodoo, cursing anything that lives in this place. Additionally, you'll find the Mouf gentle creatures that are better left alone. The Sayormi own them. No one dares enter the Dead Forest anymore.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Webweaver Caves.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: As expected, webweaver spiders live there, guarded by their Outcast friends. The Outcasts are people who have been shunned from the Kerritamba way of life. They retreated into the caves and live there. Even today, the Kerritamba people and the Outcasts are at war. It would be best to leave their rivalry be.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Is anything pleasant here?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Kerritamba Village.
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ahh... the Kerritamba village. The Kerritamba village is led by Chief Kerritamba who comes from a long, strong line of Wookiee warriors. The Kerritamba people go about their lives, distrusting most and turning away the rest. The only way to get them to warm up is through their leader. Speak with him if you wish to earn their trust.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Great Tree.
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Great Tree, also known to the Kerritamba people as Nyenthi'Oris, lives on the island to the north, past the Kerritamba village and on the Chenataa river. The Great Tree is diseased and dying. The Chief of the Kerritamba village is trying to find a cure even to this day. It is a sad tale.
			string_id message = new string_id (c_stringFile, "s_112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I wonder if I can help somehow.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Myyydril Caverns.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril Caverns is a mystery to those not living within its cold, damp walls. You'll have to travel there yourself. However, it is a dangerous place. Far more dangerous than even the Kkowir Forest.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I get there?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know about the areas. Where do I go next?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, you have several options. You can visit the Kerritamba village and speak with Chief Kerritamba or you can find the Myyydril Caverns. Do you need any information about these areas?
			string_id message = new string_id (c_stringFile, "s_175");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for the information. I'm going to explore.
	if (response == "s_176")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope you'll be safe. [Shaey bows and waves.]
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Myyydril Caverns is a mystery to those not living within its cold, damp walls. You'll have to travel there yourself. However, it is a dangerous place. Far more dangerous than even the Kkowir Forest.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get there?
	if (response == "s_171")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must go north, past the Kerritamba village, to the Chenataa river. Follow it west until you see a small path covered with foliage and trees. Follow the path and you'll soon find the Myyydril Caverns. I suggest bringing friends before venturing inward.
			string_id message = new string_id (c_stringFile, "s_172");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_173");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You must go north, past the Kerritamba village, to the Chenataa river. Follow it west until you see a small path covered with foliage and trees. Follow the path and you'll soon find the Myyydril Caverns. I suggest bringing friends before venturing inward.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see.
	if (response == "s_173")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: What other area do you want to learn about?
			string_id message = new string_id (c_stringFile, "s_174");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What other area do you want to learn about?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Dead Forest.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Dead Forest is a dangerous place. I don't recommend going there. [Shaey shrugs.] The Sayormi live there and revel in the dead forest around them. They practice their voodoo, cursing anything that lives in this place. Additionally, you'll find the Mouf gentle creatures that are better left alone. The Sayormi own them. No one dares enter the Dead Forest anymore.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Webweaver Caves.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: As expected, webweaver spiders live there, guarded by their Outcast friends. The Outcasts are people who have been shunned from the Kerritamba way of life. They retreated into the caves and live there. Even today, the Kerritamba people and the Outcasts are at war. It would be best to leave their rivalry be.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Is anything pleasant here?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Kerritamba Village.
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ahh... the Kerritamba village. The Kerritamba village is led by Chief Kerritamba who comes from a long, strong line of Wookiee warriors. The Kerritamba people go about their lives, distrusting most and turning away the rest. The only way to get them to warm up is through their leader. Speak with him if you wish to earn their trust.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Great Tree.
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Great Tree, also known to the Kerritamba people as Nyenthi'Oris, lives on the island to the north, past the Kerritamba village and on the Chenataa river. The Great Tree is diseased and dying. The Chief of the Kerritamba village is trying to find a cure even to this day. It is a sad tale.
			string_id message = new string_id (c_stringFile, "s_112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I wonder if I can help somehow.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Myyydril Caverns.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril Caverns is a mystery to those not living within its cold, damp walls. You'll have to travel there yourself. However, it is a dangerous place. Far more dangerous than even the Kkowir Forest.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I get there?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know about the areas. Where do I go next?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, you have several options. You can visit the Kerritamba village and speak with Chief Kerritamba or you can find the Myyydril Caverns. Do you need any information about these areas?
			string_id message = new string_id (c_stringFile, "s_175");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for the information. I'm going to explore.
	if (response == "s_176")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope you'll be safe. [Shaey bows and waves.]
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_forest_npc_greeter_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, you have several options. You can visit the Kerritamba village and speak with Chief Kerritamba or you can find the Myyydril Caverns. Do you need any information about these areas?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Dead Forest.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Dead Forest is a dangerous place. I don't recommend going there. [Shaey shrugs.] The Sayormi live there and revel in the dead forest around them. They practice their voodoo, cursing anything that lives in this place. Additionally, you'll find the Mouf gentle creatures that are better left alone. The Sayormi own them. No one dares enter the Dead Forest anymore.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Webweaver Caves.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: As expected, webweaver spiders live there, guarded by their Outcast friends. The Outcasts are people who have been shunned from the Kerritamba way of life. They retreated into the caves and live there. Even today, the Kerritamba people and the Outcasts are at war. It would be best to leave their rivalry be.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Is anything pleasant here?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Kerritamba Village.
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ahh... the Kerritamba village. The Kerritamba village is led by Chief Kerritamba who comes from a long, strong line of Wookiee warriors. The Kerritamba people go about their lives, distrusting most and turning away the rest. The only way to get them to warm up is through their leader. Speak with him if you wish to earn their trust.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Great Tree.
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Great Tree, also known to the Kerritamba people as Nyenthi'Oris, lives on the island to the north, past the Kerritamba village and on the Chenataa river. The Great Tree is diseased and dying. The Chief of the Kerritamba village is trying to find a cure even to this day. It is a sad tale.
			string_id message = new string_id (c_stringFile, "s_112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I wonder if I can help somehow.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Myyydril Caverns.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril Caverns is a mystery to those not living within its cold, damp walls. You'll have to travel there yourself. However, it is a dangerous place. Far more dangerous than even the Kkowir Forest.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I get there?
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know about the areas. Where do I go next?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, you have several options. You can visit the Kerritamba village and speak with Chief Kerritamba or you can find the Myyydril Caverns. Do you need any information about these areas?
			string_id message = new string_id (c_stringFile, "s_175");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Dead Forest.
			boolean hasResponse0 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Webweaver Caves.
			boolean hasResponse1 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the Kerritamba Village.
			boolean hasResponse2 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Tell me about the Great Tree.
			boolean hasResponse3 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Tell me about the Myyydril Caverns.
			boolean hasResponse4 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: I know about the areas. Where do I go next?
			boolean hasResponse5 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you for the information. I'm going to explore.
			boolean hasResponse6 = false;
			if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for the information. I'm going to explore.
	if (response == "s_176")
	{
		//-- [NOTE] 
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I hope you'll be safe. [Shaey bows and waves.]
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.ep3_forest_npc_greeter");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable(self, true);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable(self, true);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.ep3_forest_npc_greeter");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
	{
		//-- NPC: [Shaey nods to you.] Looks like you're a little lost...
		string_id message = new string_id (c_stringFile, "s_4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yeah. I'm new to this area.
		boolean hasResponse0 = false;
		if (ep3_forest_npc_greeter_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6");

			utils.setScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId", 1);

			npcStartConversation (player, npc, "ep3_forest_npc_greeter", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "ep3_forest_npc_greeter")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

	if (branchId == 1 && ep3_forest_npc_greeter_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && ep3_forest_npc_greeter_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && ep3_forest_npc_greeter_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && ep3_forest_npc_greeter_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && ep3_forest_npc_greeter_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && ep3_forest_npc_greeter_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && ep3_forest_npc_greeter_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && ep3_forest_npc_greeter_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && ep3_forest_npc_greeter_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && ep3_forest_npc_greeter_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && ep3_forest_npc_greeter_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && ep3_forest_npc_greeter_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && ep3_forest_npc_greeter_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && ep3_forest_npc_greeter_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && ep3_forest_npc_greeter_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.ep3_forest_npc_greeter.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

