// ======================================================================
//
// tat_icon_missingdroid_lyrissa.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.34 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/tat_icon_missingdroid_lyrissa";

// ======================================================================
// Script Conditions
// ======================================================================

boolean tat_icon_missingdroid_lyrissa_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_stage9 (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_stage8 (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_fixingJawa_e5ore6 (obj_id player, obj_id npc)
{	
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_lyrissa_to_tosche_e5 = groundquests.getTaskId(questId1, "tat_icon_lyrissa_to_tosche_e5"); 
	int tat_icon_tosche_to_lyrissa_e6 = groundquests.getTaskId(questId1, "tat_icon_tosche_to_lyrissa_e6"); 

	boolean onTask1 = questIsTaskActive(questId1, tat_icon_lyrissa_to_tosche_e5, player);
	boolean onTask2 = questIsTaskActive(questId1, tat_icon_tosche_to_lyrissa_e6, player);
	
	boolean onTask = false;	
	
	if (onTask1 || onTask2)
	  onTask = true;	
	
	return onTask;

}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_fixingJawa_e8_droid2Dren (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_droid_to_dren_e8 = groundquests.getTaskId(questId1, "tat_icon_droid_to_dren_e8"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_droid_to_dren_e8, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_fixingJawa_e7_lyrissa2Droid (obj_id player, obj_id npc)
{	
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_lyrissa_to_droid_e7 = groundquests.getTaskId(questId1, "tat_icon_lyrissa_to_droid_e7"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_lyrissa_to_droid_e7, player);

	
	return onTask;

}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_fixingJawa_e6_tosche2Lyrissa (obj_id player, obj_id npc)
{	
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_tosche_to_lyrissa_e6 = groundquests.getTaskId(questId1, "tat_icon_tosche_to_lyrissa_e6"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_tosche_to_lyrissa_e6, player);
	
	return onTask;

}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_fixingJawa_e5_lyrissa2Tosche (obj_id player, obj_id npc)
{	
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_lyrissa_to_tosche_e5 = groundquests.getTaskId(questId1, "tat_icon_lyrissa_to_tosche_e5"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_lyrissa_to_tosche_e5, player);
	
	return onTask;

}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_fixingJawa_e4_dren2Lyrissa (obj_id player, obj_id npc)
{	
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_dren_to_lyrissa_e4 = groundquests.getTaskId(questId1, "tat_icon_dren_to_lyrissa_e4"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_dren_to_lyrissa_e4, player);
	
	return onTask;

}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_lyrissa_condition_fixingjJawa_e3_droid2Dren (obj_id player, obj_id npc)
{	
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_droid_to_dren_e3 = groundquests.getTaskId(questId1, "tat_icon_droid_to_dren_e3"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_droid_to_dren_e3, player);
	
	return onTask;

}

// ======================================================================
// Script Actions
// ======================================================================

void tat_icon_missingdroid_lyrissa_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ----------------------------------------------------------------------

void tat_icon_missingdroid_lyrissa_action_giveQuest_missingHarvest (obj_id player, obj_id npc)
{
	int questId = questGetQuestId("quest/tatooine_fixingjawa_missingharvest1");

	groundquests.grantQuest(questId, player, npc, true);
}

// ----------------------------------------------------------------------

void tat_icon_missingdroid_lyrissa_action_signal_gotoTosche_e4 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_icon_fixingjawa_dren_to_lyrissa_e4");
}

// ----------------------------------------------------------------------

void tat_icon_missingdroid_lyrissa_action_signal_gotoLyrissa_e5 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_icon_fixingjawa_lyrissa_to_tosche_e5");
}

// ----------------------------------------------------------------------

void tat_icon_missingdroid_lyrissa_action_signal_gotoDroid_e6 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_icon_fixingjawa_tosche_to_lyrissa_e6");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int tat_icon_missingdroid_lyrissa_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Did you get a chance to talk to that offworlder about the Pod Racer?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I haven't
	if (response == "s_54")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: You might want to check in with him. I heard he's paying pretty well.
			string_id message = new string_id (c_stringFile, "s_56");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Could you give me directions?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I did.
	if (response == "s_55")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: So how did it go? Did you find him any old republic pod racer parts?
			string_id message = new string_id (c_stringFile, "s_59");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not yet, still searching though.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yea. Got it right here. Thought I'd see if you wanted it first.
			boolean hasResponse1 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_61");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You might want to check in with him. I heard he's paying pretty well.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Could you give me directions?
	if (response == "s_57")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sure, Here ya go.
			string_id message = new string_id (c_stringFile, "s_58");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So how did it go? Did you find him any old republic pod racer parts?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet, still searching though.
	if (response == "s_60")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: I heard a story a long time ago about some kid 
			string_id message = new string_id (c_stringFile, "s_67");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yea. Got it right here. Thought I'd see if you wanted it first.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Well now! That's very flattering, but I don't have anywhere near the credits he has. If I were you I'd finish that deal as soon as I could.
			string_id message = new string_id (c_stringFile, "s_63");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just thought I would check.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well now! That's very flattering, but I don't have anywhere near the credits he has. If I were you I'd finish that deal as soon as I could.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just thought I would check.
	if (response == "s_65")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Well, after you get rich, and you play your cards right, I might let you buy me some stabilizers. Mine broke loose in the last race.
			string_id message = new string_id (c_stringFile, "s_66");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hey again! That DR-42 power converter is working out great. I raced a modified XP-38 the other day and I dusted him easy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nice! I'd love to see you race sometime.
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well the races are a bit impromtu since the empire made them illegal, but I'll let you know if one is coming up.
			string_id message = new string_id (c_stringFile, "s_49");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks, I'll take you up on that.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_51");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks. I was wondering if you knew of anyone else that needed some jobs done. I'm pretty resourceful.
	if (response == "s_78")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: I can tell. You sure handeled that demolitionmech. Yea, I've got just the job for ya.
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are the details?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well the races are a bit impromtu since the empire made them illegal, but I'll let you know if one is coming up.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks, I'll take you up on that.
	if (response == "s_51")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition_fixingJawa_e8_droid2Dren (player, npc))
		{
			//-- NPC: Listen, you did really good in handling Dren's droid problem. would you be interested in helping on another job?
			string_id message = new string_id (c_stringFile, "s_53");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure, what's the job?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_64");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!tat_icon_missingdroid_lyrissa_condition_fixingJawa_e8_droid2Dren (player, npc))
		{
			//-- NPC: I sometimes know about work around here. Why don't you check back with me and I'll let you know if anything good comes up.
			string_id message = new string_id (c_stringFile, "s_52");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Listen, you did really good in handling Dren's droid problem. would you be interested in helping on another job?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure, what's the job?
	if (response == "s_64")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: I know a collector that is looking for old Pod Racer artifacts. He's got a possible location on one and needs someone to check it out. If you think this is something you can handle I could have him meet you.
			string_id message = new string_id (c_stringFile, "s_76");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I can tell. You sure handeled that demolitionmech. Yea, I've got just the job for ya.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are the details?
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: I know a collector that is looking for old Pod Racer artifacts. He's got a possible location on one and needs someone to check it out. If you think this is something you can handle I could have him meet you.
			string_id message = new string_id (c_stringFile, "s_76");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Were you able to find Tosche Station?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I wasn't.
	if (response == "s_86")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: Here let me give you directions.
			string_id message = new string_id (c_stringFile, "s_88");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I did.
	if (response == "s_90")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did you get the power converter?
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, I wasn't able to find the dealer.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I did. Here you go.
			boolean hasResponse1 = false;
			if (tat_icon_missingdroid_lyrissa_condition_fixingJawa_e6_tosche2Lyrissa (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Did you get the power converter?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I wasn't able to find the dealer.
	if (response == "s_94")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: [Directions to dealer]
			string_id message = new string_id (c_stringFile, "s_96");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I did. Here you go.
	if (response == "s_98")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			tat_icon_missingdroid_lyrissa_action_signal_gotoDroid_e6 (player, npc);

			//-- NPC: Hey hey! This is a DR-42 series. You've got a good eye for performance! Here's the slice card as promised. All you have to do is plug it into the demolitionmech's data port and it should do the rest.
			string_id message = new string_id (c_stringFile, "s_100");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Way busy, speak quickly.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was told to talk to you about a rogue droid.
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: Who told you to come to me?
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Dren Feelu, a moisture farmer. Says he knows you.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Who told you to come to me?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Dren Feelu, a moisture farmer. Says he knows you.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, ok. Dren, what does he need me to do?
			string_id message = new string_id (c_stringFile, "s_110");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: His harvester droid has gone rogue.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yea, ok. Dren, what does he need me to do?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: His harvester droid has gone rogue.
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "belly_laugh");

			//-- NPC: I told him not to buy that military droid! Those things have some of the strictest code available in a droid. I knew those bug-eaters were selling him a zero. Look I can help ya, but I really need to get my chores done or my pa will have my hide.
			string_id message = new string_id (c_stringFile, "s_114");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can I do to help?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I told him not to buy that military droid! Those things have some of the strictest code available in a droid. I knew those bug-eaters were selling him a zero. Look I can help ya, but I really need to get my chores done or my pa will have my hide.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can I do to help?
	if (response == "s_116")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			//-- NPC: I tell you what, if you can go into Tosche Station and pick me up a power converter for my speeder I can write you a slice card that will have that droid back to harvesting in no time. What do you say?
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll do it. Where is Tosche Station?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_120");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_lyrissa_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I tell you what, if you can go into Tosche Station and pick me up a power converter for my speeder I can write you a slice card that will have that droid back to harvesting in no time. What do you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll do it. Where is Tosche Station?
	if (response == "s_120")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			tat_icon_missingdroid_lyrissa_action_signal_gotoTosche_e4 (player, npc);

			//-- NPC: Great! Just go to Anchorhead and it should be just up a hill from there.
			string_id message = new string_id (c_stringFile, "s_122");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.tat_icon_missingdroid_lyrissa");

    setCondition (self, CONDITION_CONVERSABLE);
    setCondition (self, CONDITION_INTERESTING);
    setInvulnerable (self, true);

    setName (self, "Lyrissa");

    ai_lib.setDefaultCalmBehavior( self, ai_lib.BEHAVIOR_SENTINEL );

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
    setCondition (self, CONDITION_CONVERSABLE);
    setCondition (self, CONDITION_INTERESTING);
    
    setInvulnerable (self, true);
    setName (self, "Lyrissa");
    
    ai_lib.setDefaultCalmBehavior( self, ai_lib.BEHAVIOR_SENTINEL );
    
    return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.tat_icon_missingdroid_lyrissa");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (tat_icon_missingdroid_lyrissa_condition_fixingJawa_e8_droid2Dren (player, npc))
	{
		tat_icon_missingdroid_lyrissa_action_facePlayer (player, npc);

		//-- NPC: Did you get a chance to talk to that offworlder about the Pod Racer?
		string_id message = new string_id (c_stringFile, "s_33");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No, I haven't
		boolean hasResponse0 = false;
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes, I did.
		boolean hasResponse1 = false;
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_54");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_55");

			utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 1);

			npcStartConversation (player, npc, "tat_icon_missingdroid_lyrissa", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tat_icon_missingdroid_lyrissa_condition_fixingJawa_e7_lyrissa2Droid (player, npc))
	{
		tat_icon_missingdroid_lyrissa_action_facePlayer (player, npc);

		//-- NPC: Hey again! That DR-42 power converter is working out great. I raced a modified XP-38 the other day and I dusted him easy.
		string_id message = new string_id (c_stringFile, "s_45");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Nice! I'd love to see you race sometime.
		boolean hasResponse0 = false;
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Thanks. I was wondering if you knew of anyone else that needed some jobs done. I'm pretty resourceful.
		boolean hasResponse1 = false;
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_47");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_78");

			utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 8);

			npcStartConversation (player, npc, "tat_icon_missingdroid_lyrissa", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tat_icon_missingdroid_lyrissa_condition_fixingJawa_e5ore6 (player, npc))
	{
		tat_icon_missingdroid_lyrissa_action_facePlayer (player, npc);

		//-- NPC: Were you able to find Tosche Station?
		string_id message = new string_id (c_stringFile, "s_84");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No, I wasn't.
		boolean hasResponse0 = false;
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes, I did.
		boolean hasResponse1 = false;
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_86");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_90");

			utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 14);

			npcStartConversation (player, npc, "tat_icon_missingdroid_lyrissa", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tat_icon_missingdroid_lyrissa_condition_fixingJawa_e4_dren2Lyrissa (player, npc))
	{
		tat_icon_missingdroid_lyrissa_action_facePlayer (player, npc);

		//-- NPC: Way busy, speak quickly.
		string_id message = new string_id (c_stringFile, "s_102");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I was told to talk to you about a rogue droid.
		boolean hasResponse0 = false;
		if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_104");

			utils.setScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId", 19);

			npcStartConversation (player, npc, "tat_icon_missingdroid_lyrissa", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tat_icon_missingdroid_lyrissa_condition__defaultCondition (player, npc))
	{
		//-- NPC: Way busy, no time to talk.
		string_id message = new string_id (c_stringFile, "s_124");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "tat_icon_missingdroid_lyrissa")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

	if (branchId == 1 && tat_icon_missingdroid_lyrissa_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && tat_icon_missingdroid_lyrissa_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && tat_icon_missingdroid_lyrissa_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && tat_icon_missingdroid_lyrissa_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && tat_icon_missingdroid_lyrissa_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && tat_icon_missingdroid_lyrissa_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && tat_icon_missingdroid_lyrissa_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && tat_icon_missingdroid_lyrissa_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && tat_icon_missingdroid_lyrissa_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && tat_icon_missingdroid_lyrissa_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && tat_icon_missingdroid_lyrissa_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && tat_icon_missingdroid_lyrissa_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && tat_icon_missingdroid_lyrissa_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && tat_icon_missingdroid_lyrissa_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && tat_icon_missingdroid_lyrissa_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_lyrissa.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

