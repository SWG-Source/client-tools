// ======================================================================
//
// computer_console_level_02.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.37 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.conversation;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/computer_console_level_02";

// ======================================================================
// Script Conditions
// ======================================================================

boolean computer_console_level_02_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean computer_console_level_02_condition_hasKey (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "outbreak_quest_facility_02", "searchJinOffice"); 
}

// ----------------------------------------------------------------------

boolean computer_console_level_02_condition_hasAlreadyCompleted (obj_id player, obj_id npc)
{
	return groundquests.hasCompletedQuest(player, "outbreak_quest_facility_02");
}

// ----------------------------------------------------------------------

boolean computer_console_level_02_condition_hasKilledScientist (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "outbreak_quest_facility_02", "useScienceConsole")
	|| computer_console_level_02_condition_hasAlreadyCompleted(player, npc); 
}

// ----------------------------------------------------------------------

boolean computer_console_level_02_condition_hasDeletedQuest (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedQuest(player, "outbreak_quest_facility_02")
	&& !groundquests.isQuestActiveOrComplete(player, "outbreak_quest_facility_02_neutral")
	&& !groundquests.isQuestActiveOrComplete(player, "outbreak_quest_facility_02_rebel")
	&& !groundquests.isQuestActiveOrComplete(player, "outbreak_quest_facility_02_imperial"));
}

// ======================================================================
// Script Actions
// ======================================================================

void computer_console_level_02_action_completeQuest (obj_id player, obj_id npc)
{
	CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player has found the unmutated virus.");

	if(!groundquests.hasCompletedQuest(player, "outbreak_quest_facility_02"))
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player has found the unmutated virus for the first time. Sending signal to player: "+player);	
		groundquests.sendSignal(player, "scienceConsoleUsed");
		if(groundquests.hasCompletedQuest(player, "outbreak_quest_02_imperial"))
		{
			CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the imperial faction quest: outbreak_quest_facility_02_imperial. Player: "+player);	
			groundquests.sendSignal(player, "imperialPlayerSignal");
		}
		else if(groundquests.hasCompletedQuest(player, "outbreak_quest_02_rebel"))
		{
			CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the rebel faction quest: outbreak_quest_facility_02_rebel. Player: "+player);	
			groundquests.sendSignal(player, "rebelPlayerSignal");
		}
		else if(groundquests.hasCompletedQuest(player, "outbreak_quest_02_neutral"))
		{
			CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the neutral faction quest: outbreak_quest_facility_02_neutral. Player: "+player);			
			groundquests.sendSignal(player, "neutralPlayerSignal");	
		}
		
	}
	
	/*
	if(!groundquests.isQuestActiveOrComplete(player, "outbreak_quest_facility_02_imperial") 
	&& !groundquests.isQuestActiveOrComplete(player, "outbreak_quest_facility_02_rebel") 
	&& !groundquests.isQuestActiveOrComplete(player, "outbreak_quest_facility_02_neutral"))
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player has found never received and not completed the next factional mission. Giving quest to player: "+player);	

		string faction = "";
		string questPrefix = "outbreak_quest_facility_02_";
		if(groundquests.hasCompletedQuest(player, "outbreak_quest_02_imperial"))
		{
			CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the imperial faction quest: outbreak_quest_facility_02_imperial. Player: "+player);	
			faction = "imperial";
		}
		else if (groundquests.hasCompletedQuest(player, "outbreak_quest_02_rebel"))
		{
			CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the rebel faction quest: outbreak_quest_facility_02_rebel. Player: "+player);	
			faction = "rebel";
		}
		else
		{
			CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the neutral faction quest: outbreak_quest_facility_02_neutral. Player: "+player);			
			faction = "neutral";	
		}
		if(faction == null || faction.length() <= 0)
		{
			CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() Previous faction quest was not found for player: "
			+player+" ("+getPlayerName(player)+"). This may be due to the player somehow having a previous quest removed.");
			faction = "neutral";
		}
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() a message handler is being sent to reward the player with the correct quest. Player: "+player);			
		string questString = questPrefix + faction;
		dictionary params = new dictionary();
		params.put("faction_quest", questString);
		params.put("player", player);	
		messageTo(npc, "rewardPlayerFactionalQuest", params, 1, true);
	}
	else
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() Player has already received or completed quest. No update or quest needed. Player: "+player);			
	
	}
	*/
}

// ----------------------------------------------------------------------

void computer_console_level_02_action_signalKillScientist (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "hasFoundComputerSystem");

}

// ----------------------------------------------------------------------

void computer_console_level_02_action_fixDeletedQuest (obj_id player, obj_id npc)
{
	
	CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player has found never received and not completed the next factional mission. Giving quest to player: "+player);	
	
	string faction = "";
	string questPrefix = "outbreak_quest_facility_02_";
	if(groundquests.hasCompletedQuest(player, "outbreak_quest_02_imperial"))
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the imperial faction quest: outbreak_quest_facility_02_imperial. Player: "+player);	
		faction = "imperial";
	}
	else if (groundquests.hasCompletedQuest(player, "outbreak_quest_02_rebel"))
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the rebel faction quest: outbreak_quest_facility_02_rebel. Player: "+player);	
		faction = "rebel";
	}
	else
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() the player is receiving the neutral faction quest: outbreak_quest_facility_02_neutral. Player: "+player);			
		faction = "neutral";	
	}
	if(faction == null || faction.length() <= 0)
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() Previous faction quest was not found for player: "
		+player+" ("+getPlayerName(player)+"). This may be due to the player somehow having a previous quest removed.");
		faction = "neutral";
	}
	CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.completeQuest() a message handler is being sent to reward the player with the correct quest. Player: "+player);			
	string questString = questPrefix + faction;
	dictionary params = new dictionary();
	params.put("faction_quest", questString);
	params.put("player", player);	
	messageTo(npc, "rewardPlayerFactionalQuest", params, 1, true);
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int computer_console_level_02_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Reissue 'Look for Clues' quest?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes please!
	if (response == "s_51")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			computer_console_level_02_action_fixDeletedQuest (player, npc);

			//-- NPC: Done.
			string_id message = new string_id (c_stringFile, "s_52");
			utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Cryogenics Laboratory Records

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Current Status
	if (response == "s_105")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Store breached, virus lost.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
			boolean hasResponse1 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
			boolean hasResponse2 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
			boolean hasResponse3 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
			boolean hasResponse4 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
			boolean hasResponse5 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
			boolean hasResponse6 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Store breached, virus lost.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Current Status
	if (response == "s_103")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Store breached, virus lost.
			string_id message = new string_id (c_stringFile, "s_104");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
			boolean hasResponse1 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
			boolean hasResponse2 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
			boolean hasResponse3 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
			boolean hasResponse4 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
			boolean hasResponse5 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
			boolean hasResponse6 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Store breached, virus lost.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Current Status
	if (response == "s_101")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Store breached, virus lost.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
			boolean hasResponse1 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
			boolean hasResponse2 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
			boolean hasResponse3 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
			boolean hasResponse4 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
			boolean hasResponse5 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
			boolean hasResponse6 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Store breached, virus lost.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Current Status
	if (response == "s_99")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Store breached, virus lost.
			string_id message = new string_id (c_stringFile, "s_100");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
			boolean hasResponse1 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
			boolean hasResponse2 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
			boolean hasResponse3 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
			boolean hasResponse4 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
			boolean hasResponse5 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
			boolean hasResponse6 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Store breached, virus lost.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Current Status
	if (response == "s_97")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Store breached, virus lost.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
			boolean hasResponse1 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
			boolean hasResponse2 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
			boolean hasResponse3 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
			boolean hasResponse4 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
			boolean hasResponse5 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
			boolean hasResponse6 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Store breached, virus lost.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Current Status
	if (response == "s_95")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Store breached, virus lost.
			string_id message = new string_id (c_stringFile, "s_96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
			boolean hasResponse1 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
			boolean hasResponse2 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
			boolean hasResponse3 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
			boolean hasResponse4 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
			boolean hasResponse5 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
			boolean hasResponse6 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Store breached, virus lost.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Current Status
	if (response == "s_93")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			computer_console_level_02_action_completeQuest (player, npc);

			//-- NPC: Removed by Dr. Jin
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
			boolean hasResponse1 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
			boolean hasResponse2 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
			boolean hasResponse3 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
			boolean hasResponse4 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
			boolean hasResponse5 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
			boolean hasResponse6 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Removed by Dr. Jin

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 1, located in the main cryogenics laboratory facility, contains the mutated strain 1.04. 
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 2, located in the main cryogenics laboratory facility, contains the mutated strain 1.08. This strain has had an unmutated Berserker Virus introduced to study if there are any favorable results. 
			string_id message = new string_id (c_stringFile, "s_91");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 3, located in the main cryogenics laboratory facility, contains the mutated strain 1.02. This strain was slightly mutated using ultraviolet light. The virus created a less favorable mutation that handicapped the virus radius of infectious influence.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 4, located in the main cryogenics laboratory facility, contains the mutated strain 1.05. This strain was introduced to Virus 43-115, otherwise known as the waterborne strain of the Blue Shadow Virus. This sample will eventually be used in hypermutation experiments in an attempt to make a more viable airborne Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain was introduced to the Krytos virus which led to undesirable results. The Krytos virus mutation is too influential on the Blackwing nudentide. The host rapidly loses important tissue and quickly becomes nonoperational. 
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Cryogenic storage unit 6, located in the main cryogenics laboratory facility, contains the mutated strain 1.20. This strain's RNA was mutated to the point in which the surface proteins altered their shape to make it past the immunity of the Rancor. Ongoing experiments with live subjects continue in the lowest level.
			string_id message = new string_id (c_stringFile, "s_79");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: The restricted cryogenic storage unit, located in Doctor Jin's office, contains the only available unmutated strain of the Blackwing virus.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Current Status
			boolean hasResponse0 = false;
			if (computer_console_level_02_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Cryogenics Laboratory Authorization Required.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Log off.
	if (response == "s_34")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Keep operational security in mind at all times. Thank you.
			string_id message = new string_id (c_stringFile, "s_37");
			utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int computer_console_level_02_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Unauthorized access detected.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Log off.
	if (response == "s_42")
	{
		//-- [NOTE] 
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			//-- NPC: Facility security notified.
			string_id message = new string_id (c_stringFile, "s_48");
			utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isTangible (self)) || (isPlayer (self)))
		detachScript(self, "conversation.computer_console_level_02");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.computer_console_level_02");

	return SCRIPT_CONTINUE;
}

//--------------------------------
//--------------------------------
messageHandler rewardPlayerFactionalQuest()
{	
	if ((params == null) || (params.isEmpty()))
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.rewardPlayerFactionalQuest() messageHandler received no data. Catestrophic error.");
		return SCRIPT_CONTINUE;
	}
	if(!params.containsKey("player"))
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.rewardPlayerFactionalQuest() messageHandler received data but without critical PLAYER key. Catestrophic error.");
		return SCRIPT_CONTINUE;		
	}
	obj_id player = params.getObjId("player");
	if(!isValidId(player) || !exists(player))
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.rewardPlayerFactionalQuest() messageHandler received data but Player is no longer valid or no longer exists. Catestrophic error for player: "+player+".");
		return SCRIPT_CONTINUE;		
	}
	if(!params.containsKey("faction_quest"))
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.rewardPlayerFactionalQuest() messageHandler received data but without critical faction key. Catestrophic error for player: "+player+".");
		return SCRIPT_CONTINUE;		
	}		
	string factionQuest = params.getString("faction_quest");
	if(factionQuest == null || factionQuest.length() <= 0)
	{
		CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.rewardPlayerFactionalQuest() messageHandler received data but faction quest is not valid. Catestrophic error for player: "+player);
		return SCRIPT_CONTINUE;		
	}
	groundquests.requestGrantQuest(player, factionQuest);
	CustomerServiceLog("outbreak_themepark", "computer_console_level_02 conversation script.rewardPlayerFactionalQuest() messageHandler requesting that player: "+player+" be granted quest: "+factionQuest);		
	return SCRIPT_CONTINUE;	
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (computer_console_level_02_condition_hasDeletedQuest (player, npc))
	{
		//-- NPC: Reissue 'Look for Clues' quest?
		string_id message = new string_id (c_stringFile, "s_50");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes please!
		boolean hasResponse0 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_51");

			utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 1);

			npcStartConversation (player, npc, "computer_console_level_02", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (computer_console_level_02_condition_hasKilledScientist (player, npc))
	{
		//-- NPC: Cryogenics Laboratory Records
		string_id message = new string_id (c_stringFile, "s_38");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Cryogenic Storage Unit 1 - Mutation Strain 1.04
		boolean hasResponse0 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Cryogenic Storage Unit 2 - Mutation Strain 1.08
		boolean hasResponse1 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Cryogenic Storage Unit 3 - Mutation Strain 1.02
		boolean hasResponse2 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Cryogenic Storage Unit 4 - Mutation Strain 1.05
		boolean hasResponse3 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: Cryogenic Storage Unit 5 - Mutation Strain 1.12
		boolean hasResponse4 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		//-- PLAYER: Cryogenic Storage Unit 6 - Mutation Strain 1.20
		boolean hasResponse5 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse5 = true;
		}

		//-- PLAYER: Restricted Cryogenic Storage - Unmutated Strain
		boolean hasResponse6 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse6 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_43");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_44");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_45");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_47");

			if (hasResponse5)
				responses [responseIndex++] = new string_id (c_stringFile, "s_49");

			if (hasResponse6)
				responses [responseIndex++] = new string_id (c_stringFile, "s_74");

			utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 3);

			npcStartConversation (player, npc, "computer_console_level_02", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (computer_console_level_02_condition_hasKey (player, npc))
	{
		computer_console_level_02_action_signalKillScientist (player, npc);

		//-- NPC: Cryogenics Laboratory Authorization Required.
		string_id message = new string_id (c_stringFile, "s_40");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Log off.
		boolean hasResponse0 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_34");

			utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 18);

			npcStartConversation (player, npc, "computer_console_level_02", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (computer_console_level_02_condition__defaultCondition (player, npc))
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- NPC: Unauthorized access detected.
		string_id message = new string_id (c_stringFile, "s_65");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Log off.
		boolean hasResponse0 = false;
		if (computer_console_level_02_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_42");

			utils.setScriptVar (player, "conversation.computer_console_level_02.branchId", 20);

			npcStartConversation (player, npc, "computer_console_level_02", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "computer_console_level_02")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.computer_console_level_02.branchId");

	if (branchId == 1 && computer_console_level_02_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && computer_console_level_02_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && computer_console_level_02_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && computer_console_level_02_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && computer_console_level_02_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && computer_console_level_02_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && computer_console_level_02_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && computer_console_level_02_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && computer_console_level_02_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && computer_console_level_02_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && computer_console_level_02_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && computer_console_level_02_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && computer_console_level_02_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && computer_console_level_02_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && computer_console_level_02_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && computer_console_level_02_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && computer_console_level_02_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && computer_console_level_02_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.computer_console_level_02.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

