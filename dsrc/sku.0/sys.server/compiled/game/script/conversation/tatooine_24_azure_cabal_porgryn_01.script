// ======================================================================
//
// tatooine_24_azure_cabal_porgryn_01.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/tatooine_24_azure_cabal_porgryn_01";

// ======================================================================
// Script Conditions
// ======================================================================

boolean tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPorgryn (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_porgryn");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasCompletedPorgrynEndorsementQuestHoleFiller (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_porgryn") &&
	groundquests.hasCompletedQuest(player, "tatooine_24_azure_cabal_top_world_01"));
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasNotCompletedPorgrynEndorsementQuest (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "tatooine_24_azure_cabal_top_world_01");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskReturnToPorgryn (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "tatooine_24_azure_cabal_top_world_01", "tatooine_24_azure_cabal_top_world_01_waitforsignal2_north_tower");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskMeetPorgryn (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_meet_porgryn");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskWorkingForPorgryn (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_porgryn");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_needsPorgrynEndorsementQuest (obj_id player, obj_id npc)
{
	return !groundquests.isQuestActive(player, "tatooine_24_azure_cabal_top_world_01");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasCompletedPorgrynEndorsementQuest (obj_id player, obj_id npc)
{
	return groundquests.hasCompletedQuest(player, "tatooine_24_azure_cabal_top_world_01");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_propti");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_tweego");
}

// ----------------------------------------------------------------------

boolean tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_broma") ||
		groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_broma_working"));
}

// ======================================================================
// Script Actions
// ======================================================================

void tatooine_24_azure_cabal_porgryn_01_action_signalPorgrynEndorsement (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "tatooine_24_azure_cabal_top_world_01_return_to_porgryn");
}

// ----------------------------------------------------------------------

void tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "quest/tatooine_24_azure_cabal_top_world_01");
	groundquests.sendSignal(player, "workingForProgryn");
}

// ----------------------------------------------------------------------

void tatooine_24_azure_cabal_porgryn_01_action_signalReturnedToPorgryn (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tatooine_24_azure_cabal_top_world_01_return_to_porgryn");
}

// ----------------------------------------------------------------------

void tatooine_24_azure_cabal_porgryn_01_action_signalWorkingForPorgryn (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "workingForProgryn");
}

// ----------------------------------------------------------------------

void tatooine_24_azure_cabal_porgryn_01_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int tatooine_24_azure_cabal_porgryn_01_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Loruna will compile a list of assignments and forward them soon. Check back later.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Loruna Scathe?
	if (response == "s_107")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna likes to stay in Theed on Naboo, to monitor the politics in the Palace. When you see her, would you ask about my -- you know -- my debt? Only if it comes up, of course.
			string_id message = new string_id (c_stringFile, "s_114");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Tweego?
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Tweego is near the spaceport in Mos Entha to track the movements of some Mystril Shadow Guard operatives.
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Propti Xaroon?
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Propti Xaroon is usually near the cantina in Mos Espa. Her background as an entertainer lets her blend in and hear things that others cannot.
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is Broma Lak on Tatooine?
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You will find Broma in Theed on Naboo. Is there anything else you need?
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have what I need. Farewell.
	if (response == "s_121")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember to tell Loruna that -- that I was helpful, would you?
			string_id message = new string_id (c_stringFile, "s_122");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Loruna likes to stay in Theed on Naboo, to monitor the politics in the Palace. When you see her, would you ask about my -- you know -- my debt? Only if it comes up, of course.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Loruna Scathe?
	if (response == "s_107")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna likes to stay in Theed on Naboo, to monitor the politics in the Palace. When you see her, would you ask about my -- you know -- my debt? Only if it comes up, of course.
			string_id message = new string_id (c_stringFile, "s_114");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Tweego?
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Tweego is near the spaceport in Mos Entha to track the movements of some Mystril Shadow Guard operatives.
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Propti Xaroon?
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Propti Xaroon is usually near the cantina in Mos Espa. Her background as an entertainer lets her blend in and hear things that others cannot.
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is Broma Lak on Tatooine?
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You will find Broma in Theed on Naboo. Is there anything else you need?
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have what I need. Farewell.
	if (response == "s_121")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember to tell Loruna that -- that I was helpful, would you?
			string_id message = new string_id (c_stringFile, "s_122");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Tweego is near the spaceport in Mos Entha to track the movements of some Mystril Shadow Guard operatives.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Loruna Scathe?
	if (response == "s_107")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna likes to stay in Theed on Naboo, to monitor the politics in the Palace. When you see her, would you ask about my -- you know -- my debt? Only if it comes up, of course.
			string_id message = new string_id (c_stringFile, "s_114");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Tweego?
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Tweego is near the spaceport in Mos Entha to track the movements of some Mystril Shadow Guard operatives.
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Propti Xaroon?
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Propti Xaroon is usually near the cantina in Mos Espa. Her background as an entertainer lets her blend in and hear things that others cannot.
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is Broma Lak on Tatooine?
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You will find Broma in Theed on Naboo. Is there anything else you need?
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have what I need. Farewell.
	if (response == "s_121")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember to tell Loruna that -- that I was helpful, would you?
			string_id message = new string_id (c_stringFile, "s_122");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Propti Xaroon is usually near the cantina in Mos Espa. Her background as an entertainer lets her blend in and hear things that others cannot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Loruna Scathe?
	if (response == "s_107")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna likes to stay in Theed on Naboo, to monitor the politics in the Palace. When you see her, would you ask about my -- you know -- my debt? Only if it comes up, of course.
			string_id message = new string_id (c_stringFile, "s_114");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Tweego?
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Tweego is near the spaceport in Mos Entha to track the movements of some Mystril Shadow Guard operatives.
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Propti Xaroon?
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Propti Xaroon is usually near the cantina in Mos Espa. Her background as an entertainer lets her blend in and hear things that others cannot.
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is Broma Lak on Tatooine?
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You will find Broma in Theed on Naboo. Is there anything else you need?
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have what I need. Farewell.
	if (response == "s_121")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember to tell Loruna that -- that I was helpful, would you?
			string_id message = new string_id (c_stringFile, "s_122");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You will find Broma in Theed on Naboo. Is there anything else you need?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Loruna Scathe?
	if (response == "s_107")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna likes to stay in Theed on Naboo, to monitor the politics in the Palace. When you see her, would you ask about my -- you know -- my debt? Only if it comes up, of course.
			string_id message = new string_id (c_stringFile, "s_114");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Tweego?
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Tweego is near the spaceport in Mos Entha to track the movements of some Mystril Shadow Guard operatives.
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is Propti Xaroon?
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Propti Xaroon is usually near the cantina in Mos Espa. Her background as an entertainer lets her blend in and hear things that others cannot.
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Is Broma Lak on Tatooine?
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You will find Broma in Theed on Naboo. Is there anything else you need?
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is Loruna Scathe?
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is Tweego?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is Propti Xaroon?
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Is Broma Lak on Tatooine?
			boolean hasResponse3 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I have what I need. Farewell.
			boolean hasResponse4 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_107");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have what I need. Farewell.
	if (response == "s_121")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember to tell Loruna that -- that I was helpful, would you?
			string_id message = new string_id (c_stringFile, "s_122");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have performed well. The comms from Jabba's Palace have revealed some interesting details in his pursuit of someone named Solo. We'll see if this is the sort of thing our client wants.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_17")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: We will meet again.
			string_id message = new string_id (c_stringFile, "s_19");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well done. I have just submitted my endorsement to Loruna.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you.
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I look forward to working with you once you become a full member of the Cabal.
			string_id message = new string_id (c_stringFile, "s_89");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You seem reluctant to complete the task I have assigned you. Are you belligerent or merely forgetful?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want more information first.
	if (response == "s_26")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of our clients is interested in some secret communications coming into Jabba's Palace. Not just random signals, but the top-security files.
			string_id message = new string_id (c_stringFile, "s_57");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll figure it out. Just upload the data.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You have a plan to do that?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm not interested in crossing Jabba.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_63");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Neither! I was just checking in.
	if (response == "s_28")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Timing is critical. Do not delay.
			string_id message = new string_id (c_stringFile, "s_54");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I will not endorse anyone who does not complete their assignments! 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just give me another comm-interceptor.
	if (response == "s_94")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What was I supposed to do again?
	if (response == "s_95")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of our clients is interested in some secret communications coming into Jabba's Palace. Not just random signals, but the top-security files.
			string_id message = new string_id (c_stringFile, "s_57");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll figure it out. Just upload the data.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You have a plan to do that?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm not interested in crossing Jabba.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_63");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Okay, you located me.  That's the first step.  Loruna said you have a solid combat background, but I'll form my own opinion.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Upload the assignment and I'll get it done.
	if (response == "s_36")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How did you become part of the Azure Cabal?
	if (response == "s_38")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Like many of my kind, I was a warrior at heart. As a child I trained daily to gain the skills that would make me great.
			string_id message = new string_id (c_stringFile, "s_40");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So...about that assignment...
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No offense, but you don't look like a warrior.
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_42");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Like many of my kind, I was a warrior at heart. As a child I trained daily to gain the skills that would make me great.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So...about that assignment...
	if (response == "s_42")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No offense, but you don't look like a warrior.
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: One day I heard combat and ran to watch. A bounty hunter lobbed a grenade filled with some sort of gas at his opponent. He missed and it landed upwind of me.
			string_id message = new string_id (c_stringFile, "s_46");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Now about that endorsement...
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: At least you weren't killed.
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm not ready. I'll come back later.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_128");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: One day I heard combat and ran to watch. A bounty hunter lobbed a grenade filled with some sort of gas at his opponent. He missed and it landed upwind of me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see. Now about that endorsement...
	if (response == "s_48")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: At least you weren't killed.
	if (response == "s_50")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I wished that I had been. The gas seared my lungs, making every breath agony...  but that was long ago and you probably want to get started. Are you ready to begin your task?
			string_id message = new string_id (c_stringFile, "s_52");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Not yet. How did you become a member of the Azure Cabal?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Actually, no.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_126");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not ready. I'll come back later.
	if (response == "s_128")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Membership in the Azure Cabal is not a casual commitment. Return when you are ready.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I wished that I had been. The gas seared my lungs, making every breath agony...  but that was long ago and you probably want to get started. Are you ready to begin your task?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_55")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of our clients is interested in some secret communications coming into Jabba's Palace. Not just random signals, but the top-security files.
			string_id message = new string_id (c_stringFile, "s_57");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll figure it out. Just upload the data.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You have a plan to do that?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm not interested in crossing Jabba.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_63");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet. How did you become a member of the Azure Cabal?
	if (response == "s_109")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: How refreshing. A candidate who is actually interested in me. I'm flattered! 
			string_id message = new string_id (c_stringFile, "s_111");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ah. I see. Well, I'm ready to begin now.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Service to the Azure Cabal?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_113");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_115");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Actually, no.
	if (response == "s_126")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Membership in the Azure Cabal is not a casual commitment. Return when you are ready.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: One of our clients is interested in some secret communications coming into Jabba's Palace. Not just random signals, but the top-security files.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll figure it out. Just upload the data.
	if (response == "s_59")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You have a plan to do that?
	if (response == "s_63")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Loruna has given me a large number of comm-interceptors. They are attached to the comm-relay towers and wait for high-level encrypted signals. Then they relay them back here.
			string_id message = new string_id (c_stringFile, "s_65");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've heard enough. Upload the rest.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: And Jabba hasn't figured this out?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No way. I'm not crossing Jabba.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_69");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested in crossing Jabba.
	if (response == "s_102")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Membership in the Azure Cabal is not a casual commitment. Return when you are ready.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course. Loruna has given me a large number of comm-interceptors. They are attached to the comm-relay towers and wait for high-level encrypted signals. Then they relay them back here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've heard enough. Upload the rest.
	if (response == "s_67")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And Jabba hasn't figured this out?
	if (response == "s_69")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not so far. Each time a signal is relayed, that comm-interceptor self-destructs, leaving little evidence. That's why Loruna gave me so many units and why I must continually replace them.
			string_id message = new string_id (c_stringFile, "s_71");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll figure the rest out as I go. Upload the details.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: So I just plug them in?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Not for me. I'm out.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_73");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_75");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No way. I'm not crossing Jabba.
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Membership in the Azure Cabal is not a casual commitment. Return when you are ready.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not so far. Each time a signal is relayed, that comm-interceptor self-destructs, leaving little evidence. That's why Loruna gave me so many units and why I must continually replace them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll figure the rest out as I go. Upload the details.
	if (response == "s_73")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So I just plug them in?
	if (response == "s_75")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: No. Once you have connected a unit, I will send a test signal that should be intercepted. You must remain nearby in order to reset the destruct sequence on the comm-interceptor. If you do not, it will self-destruct and you'll have to start over.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Got it, upload the locations.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How many towers?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Count me out.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not for me. I'm out.
	if (response == "s_97")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Membership in the Azure Cabal is not a casual commitment. Return when you are ready.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No. Once you have connected a unit, I will send a test signal that should be intercepted. You must remain nearby in order to reset the destruct sequence on the comm-interceptor. If you do not, it will self-destruct and you'll have to start over.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Got it, upload the locations.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How many towers?
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Only one. Are you ready to begin?
			string_id message = new string_id (c_stringFile, "s_83");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Not yet.
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Count me out.
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Membership in the Azure Cabal is not a casual commitment. Return when you are ready.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Only one. Are you ready to begin?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_85")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			tatooine_24_azure_cabal_porgryn_01_action_grantTopOfWorld (player, npc);

			//-- NPC: Upload complete. Report back when the comm-interceptor has been successfully tested.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet.
	if (response == "s_90")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Membership in the Azure Cabal is not a casual commitment. Return when you are ready.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How refreshing. A candidate who is actually interested in me. I'm flattered! 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ah. I see. Well, I'm ready to begin now.
	if (response == "s_113")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of our clients is interested in some secret communications coming into Jabba's Palace. Not just random signals, but the top-security files.
			string_id message = new string_id (c_stringFile, "s_57");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll figure it out. Just upload the data.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You have a plan to do that?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm not interested in crossing Jabba.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_63");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Service to the Azure Cabal?
	if (response == "s_115")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes. We didn't discuss terms or duration of the service, but I would have sold my horns to be able to breathe again. Loruna is fair, and eventually I believe she'll waive my debt. Until then, I work for a small stipend. I can't complain. Compared with the pain of blistered lungs, this is bliss.
			string_id message = new string_id (c_stringFile, "s_119");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Interesting. Well, I'm ready to begin my assignment.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_124");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_24_azure_cabal_porgryn_01_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes. We didn't discuss terms or duration of the service, but I would have sold my horns to be able to breathe again. Loruna is fair, and eventually I believe she'll waive my debt. Until then, I work for a small stipend. I can't complain. Compared with the pain of blistered lungs, this is bliss.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Interesting. Well, I'm ready to begin my assignment.
	if (response == "s_124")
	{
		//-- [NOTE] 
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of our clients is interested in some secret communications coming into Jabba's Palace. Not just random signals, but the top-security files.
			string_id message = new string_id (c_stringFile, "s_57");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll figure it out. Just upload the data.
			boolean hasResponse0 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You have a plan to do that?
			boolean hasResponse1 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm not interested in crossing Jabba.
			boolean hasResponse2 = false;
			if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_63");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isTangible (self)) || (isPlayer (self)))
		detachScript(self, "conversation.tatooine_24_azure_cabal_porgryn_01");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.tatooine_24_azure_cabal_porgryn_01");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_porgryn_01_condition_hasCompletedPorgrynEndorsementQuest (player, npc))
	{
		//-- NPC: Loruna will compile a list of assignments and forward them soon. Check back later.
		string_id message = new string_id (c_stringFile, "s_99");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Where is Loruna Scathe?
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Where is Tweego?
		boolean hasResponse1 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementTweego (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Where is Propti Xaroon?
		boolean hasResponse2 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementPropti (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Is Broma Lak on Tatooine?
		boolean hasResponse3 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskEndorsementBroma (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: I have what I need. Farewell.
		boolean hasResponse4 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_107");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_108");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_110");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_112");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_121");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 1);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_porgryn_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_porgryn_01_condition_hasCompletedPorgrynEndorsementQuestHoleFiller (player, npc))
	{
		tatooine_24_azure_cabal_porgryn_01_action_signalPorgrynEndorsement (player, npc);

		//-- NPC: You have performed well. The comms from Jabba's Palace have revealed some interesting details in his pursuit of someone named Solo. We'll see if this is the sort of thing our client wants.
		string_id message = new string_id (c_stringFile, "s_15");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Thanks.
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_17");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 7);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_porgryn_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskReturnToPorgryn (player, npc))
	{
		tatooine_24_azure_cabal_porgryn_01_action_signalReturnedToPorgryn (player, npc);

		//-- NPC: Well done. I have just submitted my endorsement to Loruna.
		string_id message = new string_id (c_stringFile, "s_87");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Thank you.
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_88");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 9);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_porgryn_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_porgryn_01_condition_hasNotCompletedPorgrynEndorsementQuest (player, npc))
	{
		//-- NPC: You seem reluctant to complete the task I have assigned you. Are you belligerent or merely forgetful?
		string_id message = new string_id (c_stringFile, "s_24");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I want more information first.
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Neither! I was just checking in.
		boolean hasResponse1 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_26");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_28");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 11);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_porgryn_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskWorkingForPorgryn (player, npc))
	{
		//-- NPC: I will not endorse anyone who does not complete their assignments! 
		string_id message = new string_id (c_stringFile, "s_93");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Just give me another comm-interceptor.
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition_needsPorgrynEndorsementQuest (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What was I supposed to do again?
		boolean hasResponse1 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_94");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_95");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 13);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_porgryn_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_porgryn_01_condition_hasActiveTaskMeetPorgryn (player, npc))
	{
		//-- NPC: Okay, you located me.  That's the first step.  Loruna said you have a solid combat background, but I'll form my own opinion.
		string_id message = new string_id (c_stringFile, "s_34");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Upload the assignment and I'll get it done.
		boolean hasResponse0 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: How did you become part of the Azure Cabal?
		boolean hasResponse1 = false;
		if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_36");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_38");

			utils.setScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId", 14);

			npcStartConversation (player, npc, "tatooine_24_azure_cabal_porgryn_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_24_azure_cabal_porgryn_01_condition__defaultCondition (player, npc))
	{
		//-- NPC: Nothing to see here.  Move along.
		string_id message = new string_id (c_stringFile, "s_132");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "tatooine_24_azure_cabal_porgryn_01")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

	if (branchId == 1 && tatooine_24_azure_cabal_porgryn_01_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && tatooine_24_azure_cabal_porgryn_01_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && tatooine_24_azure_cabal_porgryn_01_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && tatooine_24_azure_cabal_porgryn_01_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && tatooine_24_azure_cabal_porgryn_01_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && tatooine_24_azure_cabal_porgryn_01_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && tatooine_24_azure_cabal_porgryn_01_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && tatooine_24_azure_cabal_porgryn_01_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && tatooine_24_azure_cabal_porgryn_01_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && tatooine_24_azure_cabal_porgryn_01_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && tatooine_24_azure_cabal_porgryn_01_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && tatooine_24_azure_cabal_porgryn_01_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && tatooine_24_azure_cabal_porgryn_01_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && tatooine_24_azure_cabal_porgryn_01_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && tatooine_24_azure_cabal_porgryn_01_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && tatooine_24_azure_cabal_porgryn_01_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && tatooine_24_azure_cabal_porgryn_01_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && tatooine_24_azure_cabal_porgryn_01_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && tatooine_24_azure_cabal_porgryn_01_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && tatooine_24_azure_cabal_porgryn_01_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.tatooine_24_azure_cabal_porgryn_01.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

