// ======================================================================
//
// ep3_clone_relics_clone_trooper_morkov.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/ep3_clone_relics_clone_trooper_morkov";

// ======================================================================
// Script Conditions
// ======================================================================

boolean ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_clone_trooper_morkov_condition_onTaskTalkToMorkov (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ep3_clone_relics_clone_trooper_mort_1", "talkToMorkov"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_clone_trooper_morkov_condition_goToNaboo (obj_id player, obj_id npc)
{
	return (groundquests.hasCompletedTask(player, "ep3_clone_relics_clone_trooper_mort_1", "talkToMorkov"));
}

// ----------------------------------------------------------------------

boolean ep3_clone_relics_clone_trooper_morkov_condition_playerWantToKillMorkov (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive(player, "ep3_clone_relics_clone_trooper_mort_imperial", "killMorkov") || groundquests.isTaskActive(player, "ep3_clone_relics_clone_trooper_mort_neutral", "morkovDies") || groundquests.isTaskActive(player, "ep3_clone_relics_clone_trooper_mort_rebel", "morkovDies"))
		return true;
	else
		return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void ep3_clone_relics_clone_trooper_morkov_action_signalMorkov (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "talkedToMorkov");
}

// ----------------------------------------------------------------------

void ep3_clone_relics_clone_trooper_morkov_action_attackPlayer (obj_id player, obj_id npc)
{
	setInvulnerable (npc, false);
	clearCondition(npc, CONDITION_CONVERSABLE);
	startCombat(npc, player);
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int ep3_clone_relics_clone_trooper_morkov_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Was there something else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Dee'Ja Peak, opposite hotel, upstairs, right?
	if (response == "s_54")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: You got it. Now if you'll excuse me.
			string_id message = new string_id (c_stringFile, "s_55");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How may I assist?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm here on behalf of my master, Jabba the Hutt.
	if (response == "s_24")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Another one? Can you please tell your 'master' to stick to the same person doing his business? It's getting a little tiresome doing background checks on all of you.
			string_id message = new string_id (c_stringFile, "s_27");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: A lot of people in my masters service have a short life span.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Background check? No one told me about that...
			boolean hasResponse1 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_28");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've heard you're the man to go to for if I'm looking for a weapon.
	if (response == "s_65")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm afraid you heard wrong. There's a gun store up the street though I believe. Just be careful with those things, they are dangerous.
			string_id message = new string_id (c_stringFile, "s_67");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Another one? Can you please tell your 'master' to stick to the same person doing his business? It's getting a little tiresome doing background checks on all of you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A lot of people in my masters service have a short life span.
	if (response == "s_28")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: < Morkov chuckles > Yeah I suppose that would be true. Never mind on the background check, what does the hutt want this time?
			string_id message = new string_id (c_stringFile, "s_30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My master needs 5 new E-11's.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: My master require one standard TIE and 5 crates of E-11's.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: My master wants a walker model AT-AT.
			boolean hasResponse2 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_35");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_36");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Background check? No one told me about that...
	if (response == "s_57")
	{
		doAnimationAction (player, "huh");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well they should have. Now what's your name?
			string_id message = new string_id (c_stringFile, "s_59");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My name? Well it's... Xfawqo Qreop
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_61");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: < Morkov chuckles > Yeah I suppose that would be true. Never mind on the background check, what does the hutt want this time?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My master needs 5 new E-11's.
	if (response == "s_34")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: What?! He can send his thugs out to beat up 5 troopers and get that. I don't deal in quantities like that, is this a joke?
			string_id message = new string_id (c_stringFile, "s_37");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My apologies, I of course meant 5 crates.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My master require one standard TIE and 5 crates of E-11's.
	if (response == "s_35")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hmm, the TIE will take a little work, going to need a week for that one. The crates I can have ready for you when you arrive at the usual location.
			string_id message = new string_id (c_stringFile, "s_40");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Usual location..?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My master wants a walker model AT-AT.
	if (response == "s_36")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Has he gone mad?! I can't get that...
			string_id message = new string_id (c_stringFile, "s_41");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My master was under the impression you can get anything.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_42");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?! He can send his thugs out to beat up 5 troopers and get that. I don't deal in quantities like that, is this a joke?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My apologies, I of course meant 5 crates.
	if (response == "s_38")
	{
		doAnimationAction (player, "apologize");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Oh, why didn't you say so? Yeah that's not a problem, I'll have that ready for you when you arrive at the usual location.
			string_id message = new string_id (c_stringFile, "s_39");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Usual location..?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, why didn't you say so? Yeah that's not a problem, I'll have that ready for you when you arrive at the usual location.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Usual location..?
	if (response == "s_49")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did they not tell you anything?
			string_id message = new string_id (c_stringFile, "s_50");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My information was very sparse...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_51");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Did they not tell you anything?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My information was very sparse...
	if (response == "s_51")
	{
		ep3_clone_relics_clone_trooper_morkov_action_signalMorkov (player, npc);

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'd say... very well, there's an empty building located across from the hotel in Dee'Ja peak. My courier will meet you upstairs in that building.
			string_id message = new string_id (c_stringFile, "s_52");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hmm, the TIE will take a little work, going to need a week for that one. The crates I can have ready for you when you arrive at the usual location.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Usual location..?
	if (response == "s_49")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did they not tell you anything?
			string_id message = new string_id (c_stringFile, "s_50");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My information was very sparse...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_51");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Has he gone mad?! I can't get that...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My master was under the impression you can get anything.
	if (response == "s_42")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well he better think again. An AT-ST shouldn't be too hard but an AT-AT would just not be possible.
			string_id message = new string_id (c_stringFile, "s_43");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My master will be most disappointed.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_44");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well he better think again. An AT-ST shouldn't be too hard but an AT-AT would just not be possible.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My master will be most disappointed.
	if (response == "s_44")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't care if he starts to cry, it's just not possible.
			string_id message = new string_id (c_stringFile, "s_45");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Very well, he also requires 5 crates of E-11's.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_46");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't care if he starts to cry, it's just not possible.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very well, he also requires 5 crates of E-11's.
	if (response == "s_46")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Now that's not a problem. I'll have that ready for you when you arrive at usual location.
			string_id message = new string_id (c_stringFile, "s_48");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Usual location..?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Now that's not a problem. I'll have that ready for you when you arrive at usual location.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Usual location..?
	if (response == "s_49")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did they not tell you anything?
			string_id message = new string_id (c_stringFile, "s_50");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My information was very sparse...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_51");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well they should have. Now what's your name?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My name? Well it's... Xfawqo Qreop
	if (response == "s_61")
	{
		doAnimationAction (player, "nervous");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: How the heck do you spell that? You know what, never mind. What does the hutt want this time?
			string_id message = new string_id (c_stringFile, "s_63");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My master needs 5 new E-11's.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: My master require one standard TIE and 5 crates of E-11's.
			boolean hasResponse1 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: My master wants a walker model AT-AT.
			boolean hasResponse2 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_35");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_36");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How the heck do you spell that? You know what, never mind. What does the hutt want this time?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My master needs 5 new E-11's.
	if (response == "s_34")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: What?! He can send his thugs out to beat up 5 troopers and get that. I don't deal in quantities like that, is this a joke?
			string_id message = new string_id (c_stringFile, "s_37");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My apologies, I of course meant 5 crates.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My master require one standard TIE and 5 crates of E-11's.
	if (response == "s_35")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hmm, the TIE will take a little work, going to need a week for that one. The crates I can have ready for you when you arrive at the usual location.
			string_id message = new string_id (c_stringFile, "s_40");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Usual location..?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: My master wants a walker model AT-AT.
	if (response == "s_36")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Has he gone mad?! I can't get that...
			string_id message = new string_id (c_stringFile, "s_41");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: My master was under the impression you can get anything.
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_42");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you do here?
	if (response == "s_70")
	{
		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: I live here, how about you?
			string_id message = new string_id (c_stringFile, "s_72");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Umm... I'm just passing through, what about the droids?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I live here, how about you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Umm... I'm just passing through, what about the droids?
	if (response == "s_74")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: What about them?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are they yours?
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_78");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What about them?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are they yours?
	if (response == "s_78")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes. Now I'm a busy man I'm afraid. Do you need to know anything else?
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No I suppose not...
			boolean hasResponse0 = false;
			if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_clone_relics_clone_trooper_morkov_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes. Now I'm a busy man I'm afraid. Do you need to know anything else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No I suppose not...
	if (response == "s_82")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			//-- NPC: Very well then. Good day to you.
			string_id message = new string_id (c_stringFile, "s_84");
			utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.ep3_clone_relics_clone_trooper_morkov");

	setCondition (self, CONDITION_CONVERSABLE);
	setName(self, new string_id( "ep3/npc_names", "clone_relics_morkov" ));
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setName(self, new string_id( "ep3/npc_names", "clone_relics_morkov" ));
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.ep3_clone_relics_clone_trooper_morkov");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (ep3_clone_relics_clone_trooper_morkov_condition_playerWantToKillMorkov (player, npc))
	{
		ep3_clone_relics_clone_trooper_morkov_action_attackPlayer (player, npc);

		//-- NPC: I don't know who you are or what your deal is but you will pay with your life for what you did!
		string_id message = new string_id (c_stringFile, "s_53");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_clone_trooper_morkov_condition_goToNaboo (player, npc))
	{
		//-- NPC: Was there something else?
		string_id message = new string_id (c_stringFile, "s_21");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Dee'Ja Peak, opposite hotel, upstairs, right?
		boolean hasResponse0 = false;
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_54");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 2);

			npcStartConversation (player, npc, "ep3_clone_relics_clone_trooper_morkov", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_clone_trooper_morkov_condition_onTaskTalkToMorkov (player, npc))
	{
		//-- NPC: How may I assist?
		string_id message = new string_id (c_stringFile, "s_22");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm here on behalf of my master, Jabba the Hutt.
		boolean hasResponse0 = false;
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I've heard you're the man to go to for if I'm looking for a weapon.
		boolean hasResponse1 = false;
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_24");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_65");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 4);

			npcStartConversation (player, npc, "ep3_clone_relics_clone_trooper_morkov", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
	{
		//-- NPC: Yes?
		string_id message = new string_id (c_stringFile, "s_1554");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What do you do here?
		boolean hasResponse0 = false;
		if (ep3_clone_relics_clone_trooper_morkov_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_70");

			utils.setScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId", 19);

			npcStartConversation (player, npc, "ep3_clone_relics_clone_trooper_morkov", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "ep3_clone_relics_clone_trooper_morkov")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

	if (branchId == 2 && ep3_clone_relics_clone_trooper_morkov_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && ep3_clone_relics_clone_trooper_morkov_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && ep3_clone_relics_clone_trooper_morkov_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && ep3_clone_relics_clone_trooper_morkov_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && ep3_clone_relics_clone_trooper_morkov_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && ep3_clone_relics_clone_trooper_morkov_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && ep3_clone_relics_clone_trooper_morkov_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && ep3_clone_relics_clone_trooper_morkov_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && ep3_clone_relics_clone_trooper_morkov_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && ep3_clone_relics_clone_trooper_morkov_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && ep3_clone_relics_clone_trooper_morkov_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && ep3_clone_relics_clone_trooper_morkov_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && ep3_clone_relics_clone_trooper_morkov_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && ep3_clone_relics_clone_trooper_morkov_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && ep3_clone_relics_clone_trooper_morkov_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && ep3_clone_relics_clone_trooper_morkov_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && ep3_clone_relics_clone_trooper_morkov_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && ep3_clone_relics_clone_trooper_morkov_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.ep3_clone_relics_clone_trooper_morkov.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

