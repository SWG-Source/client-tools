// ======================================================================
//
// ep3_myyydril_cantina_girl.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/ep3_myyydril_cantina_girl";

// ======================================================================
// Script Conditions
// ======================================================================

boolean ep3_myyydril_cantina_girl_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean ep3_myyydril_cantina_girl_condition_hasCompletedTaskOne (obj_id player, obj_id npc)
{	
	return (groundquests.hasCompletedTask(player, "ep3_myyydril_kirirr_gather_1", 0) &&
		   !groundquests.hasCompletedQuest(player, "ep3_myyydril_kirirr_gather_1"));
}

// ----------------------------------------------------------------------

boolean ep3_myyydril_cantina_girl_condition_hasCompletedQuestTwo (obj_id player, obj_id npc)
{
	return groundquests.hasCompletedQuest(player, "ep3_myyydril_kirrir_talkto_4");
}

// ----------------------------------------------------------------------

boolean ep3_myyydril_cantina_girl_condition_hasCompletedQuestOther (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive(player, "ep3_myyydril_talaoree_talkto_3", 0) ||
	    groundquests.hasCompletedTask(player, "ep3_myyydril_talaoree_talkto_3", 0))
	    
	    return true;
	else
		return false; 
}

// ----------------------------------------------------------------------

boolean ep3_myyydril_cantina_girl_condition_isActiveTaskOne (obj_id player, obj_id npc)
{	
	if (groundquests.isTaskActive(player, "ep3_myyydril_kirirr_gather_1", 0) ||
	    groundquests.hasCompletedTask(player, "ep3_myyydril_kirirr_gather_1", 0))
	    
	    return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean ep3_myyydril_cantina_girl_condition_hasCompletedQuestOne (obj_id player, obj_id npc)
{
	return groundquests.hasCompletedQuest(player, "ep3_myyydril_kirirr_gather_1");	
}

// ----------------------------------------------------------------------

boolean ep3_myyydril_cantina_girl_condition_isTaskActiveTwo (obj_id player, obj_id npc)
{	
	if (groundquests.isTaskActive(player, "ep3_myyydril_kirrir_talkto_4", 0) ||
	    groundquests.hasCompletedTask(player, "ep3_myyydril_kirrir_talkto_4", 0))
	    
	    return true;
	else
		return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void ep3_myyydril_cantina_girl_action_giveQuestOne (obj_id player, obj_id npc)
{
	// Apply the first quest to the player here
	groundquests.grantQuest(player, "ep3_myyydril_kirirr_gather_1");
	groundquests.sendSignal(player, "talktokirrir");
}

// ----------------------------------------------------------------------

void ep3_myyydril_cantina_girl_action_giveQuestTwo (obj_id player, obj_id npc)
{
	// Apply the first quest to the player here
	groundquests.grantQuest(player, "ep3_myyydril_kirrir_talkto_4");
}

// ----------------------------------------------------------------------

void ep3_myyydril_cantina_girl_action_giveSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "giveReward");
}

// ----------------------------------------------------------------------

void ep3_myyydril_cantina_girl_action_giveSignal2 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "talktokirrir");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int ep3_myyydril_cantina_girl_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ye know. We've got a lot ta do around here. The next person you should visit is me girl, Nawika here in the cantina. She's been needin' some help.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll go speak with her. Thank you.
	if (response == "s_334")
	{
		ep3_myyydril_cantina_girl_action_giveQuestTwo (player, npc);

		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ye be safe now.
			string_id message = new string_id (c_stringFile, "s_335");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I smell Warl nectar all over ye! Thank ye fer gettin' it. Now, I can make me favorite drink. Let me stirr up a few. I knew me patrons been waitin' fer it fer awhile.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll wait right here.
	if (response == "s_347")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ye do that. Ye should try me 'Warl Surprise'. I bet ye'll like it. Talk with me after I made me drinks.
			string_id message = new string_id (c_stringFile, "s_348");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What can I--oh? A task I be needin' help with? Who sent ye? Was it tha sweetheart Tala'oree? She's always lookin' out fer me. Let me see what I need from ye.. oh yes! I'm runnin' out o' nectar fer me specialty, 'Warl Surprise'. Ye can get the nectar from the leaves of Warl plants. I need 10 leaves. What ya say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll go take a look. What do they look like?
	if (response == "s_342")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Welp, they're small, purple plants. Ye can find them where there be hostile uwari beetles... right in the entrance o' the cavern. They be pretty hard ta miss!
			string_id message = new string_id (c_stringFile, "s_345");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll be sure to find them. Thanks.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_365");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now.
	if (response == "s_343")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: All right. Ye let me know, k?
			string_id message = new string_id (c_stringFile, "s_344");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welp, they're small, purple plants. Ye can find them where there be hostile uwari beetles... right in the entrance o' the cavern. They be pretty hard ta miss!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll be sure to find them. Thanks.
	if (response == "s_365")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			ep3_myyydril_cantina_girl_action_giveQuestOne (player, npc);

			//-- NPC: No problem at'all! Come back when ye gots 'em!
			string_id message = new string_id (c_stringFile, "s_366");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What can I get fer ya?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing, really.
	if (response == "s_301")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: All right, darlin'. Let me know.
			string_id message = new string_id (c_stringFile, "s_302");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Know any rumors?
	if (response == "s_303")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ye know. Rumors are me speciality. But unfortunately, there be none 'ere. Ain't that a hoot?
			string_id message = new string_id (c_stringFile, "s_304");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to ask you some questions, if I can.
	if (response == "s_305")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: All right. Ye can go ahead. I'll answer yer questions to the the best o'me ability.
			string_id message = new string_id (c_stringFile, "s_306");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to know about the Myyydril.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are the Urnsor'is?
			boolean hasResponse1 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What can I expect from this place?
			boolean hasResponse2 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_307");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: All right. Ye can go ahead. I'll answer yer questions to the the best o'me ability.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to know about the Myyydril.
	if (response == "s_307")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril, huh? Welp. Let me see. There's not much to say, really. The Myyydril were thrown out o'the Kerritamba village a long time ago. Somethin' about a greedy leader not agreeing with policies of the Kerritamba chief. I dunno. Not too good with politicin', meself. But I know a good drink when I see one!
			string_id message = new string_id (c_stringFile, "s_310");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are the Urnsor'is?
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What can I expect from this place?
			boolean hasResponse1 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_308");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_309");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are the Urnsor'is?
	if (response == "s_87")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Them be ugly boogers. Got a brain as a body, with sharp, pointy claws. Scares the poodoo right outta ya. I suggest ye be stayin' away from them Urnies.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to know about the Myyydril.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What can I expect from this place?
			boolean hasResponse1 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can I expect from this place?
	if (response == "s_102")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Iffin' yer not careful, I'd say a bit'o death. Urnies be bad things. Stay away from them.
			string_id message = new string_id (c_stringFile, "s_104");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are Urnies??
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'd like to know about the Myyydril.
			boolean hasResponse1 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Myyydril, huh? Welp. Let me see. There's not much to say, really. The Myyydril were thrown out o'the Kerritamba village a long time ago. Somethin' about a greedy leader not agreeing with policies of the Kerritamba chief. I dunno. Not too good with politicin', meself. But I know a good drink when I see one!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are the Urnsor'is?
	if (response == "s_308")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Them be ugly boogers. Got a brain as a body, with sharp, pointy claws. Scares the poodoo right outta ya. I suggest ye be stayin' away from them Urnies.
			string_id message = new string_id (c_stringFile, "s_311");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They don't sound very nice.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_153");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can I expect from this place?
	if (response == "s_309")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Iffin' yer not careful, I'd say a bit'o death. Urnies be bad things. Stay away from them.
			string_id message = new string_id (c_stringFile, "s_312");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Them be ugly boogers. Got a brain as a body, with sharp, pointy claws. Scares the poodoo right outta ya. I suggest ye be stayin' away from them Urnies.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They don't sound very nice.
	if (response == "s_153")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ain't tha' the truth. Ye be needin' anathin' else?
			string_id message = new string_id (c_stringFile, "s_154");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've got all the information I need.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What can I expect from this place?
			boolean hasResponse1 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_155");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_157");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ain't tha' the truth. Ye be needin' anathin' else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've got all the information I need.
	if (response == "s_155")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: [Kirrir nods.] Aye. Ye best be careful out there.
			string_id message = new string_id (c_stringFile, "s_161");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can I expect from this place?
	if (response == "s_157")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Iffin' yer not careful, I'd say a bit'o death. Urnies be bad things. Stay away from them. I wish I had more ta say.
			string_id message = new string_id (c_stringFile, "s_159");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You've been a great help.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_160");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Iffin' yer not careful, I'd say a bit'o death. Urnies be bad things. Stay away from them. I wish I had more ta say.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You've been a great help.
	if (response == "s_160")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: [Kirrir blushes visibly.] Aww, yer a sweet one.
			string_id message = new string_id (c_stringFile, "s_162");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Them be ugly boogers. Got a brain as a body, with sharp, pointy claws. Scares the poodoo right outta ya. I suggest ye be stayin' away from them Urnies.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to know about the Myyydril.
	if (response == "s_94")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril, huh? Welp. Let me see. There's not much to say, really. The Myyydril were thrown out o'the Kerritamba village a long time ago. Somethin' about a greedy leader not agreeing with policies of the Kerritamba chief. I dunno. Not too good with politicin', meself. But I know a good drink when I see one!
			string_id message = new string_id (c_stringFile, "s_96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_163");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can I expect from this place?
	if (response == "s_98")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Iffin' yer not careful, I'd say a bit'o death. Urnies be bad things. Stay away from them.
			string_id message = new string_id (c_stringFile, "s_100");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Myyydril, huh? Welp. Let me see. There's not much to say, really. The Myyydril were thrown out o'the Kerritamba village a long time ago. Somethin' about a greedy leader not agreeing with policies of the Kerritamba chief. I dunno. Not too good with politicin', meself. But I know a good drink when I see one!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see. Thank you for the information.
	if (response == "s_163")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yer welcome. Ye be needin' anathin' else?
			string_id message = new string_id (c_stringFile, "s_164");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What can I expect from this place?
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, I don't need anything else. Thanks.
			boolean hasResponse1 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_166");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_169");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yer welcome. Ye be needin' anathin' else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What can I expect from this place?
	if (response == "s_166")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Iffin' yer not careful, I'd say a bit'o death. Urnies be bad things. Stay away from them.
			string_id message = new string_id (c_stringFile, "s_168");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds... unpleasant.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I don't need anything else. Thanks.
	if (response == "s_169")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ye best be on yer way, then. I gotta make drinks.
			string_id message = new string_id (c_stringFile, "s_170");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Iffin' yer not careful, I'd say a bit'o death. Urnies be bad things. Stay away from them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sounds... unpleasant.
	if (response == "s_171")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ta say the least. Ye be needin' anathin' else?
			string_id message = new string_id (c_stringFile, "s_172");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, thank you.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_173");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ta say the least. Ye be needin' anathin' else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, thank you.
	if (response == "s_173")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ye best be on yer way, then. Busy, busy, busy!
			string_id message = new string_id (c_stringFile, "s_174");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Iffin' yer not careful, I'd say a bit'o death. Urnies be bad things. Stay away from them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are Urnies??
	if (response == "s_106")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Urnsor'is. Them be ugly boogers. Got a brain as a body, with sharp, pointy claws. Scares the poodoo right outta ya. I suggest ye be stayin' away from them Urnies.
			string_id message = new string_id (c_stringFile, "s_108");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [You wince.]
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_175");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to know about the Myyydril.
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril, huh? Welp. Let me see. There's not much to say, really. The Myyydril were thrown out o'the Kerritamba village a long time ago. Somethin' about a greedy leader not agreeing with policies of the Kerritamba chief. I dunno. Not too good with politicin', meself. But I know a good drink when I see one!
			string_id message = new string_id (c_stringFile, "s_112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks for the help.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_185");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Urnsor'is. Them be ugly boogers. Got a brain as a body, with sharp, pointy claws. Scares the poodoo right outta ya. I suggest ye be stayin' away from them Urnies.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [You wince.]
	if (response == "s_175")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: That's what I thought too. Ye be needin' anathin' else?
			string_id message = new string_id (c_stringFile, "s_176");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to know about the Myyydril.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_178");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 33);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's what I thought too. Ye be needin' anathin' else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to know about the Myyydril.
	if (response == "s_178")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Myyydril, huh? Welp. Let me see. There's not much to say, really. The Myyydril were thrown out o'the Kerritamba village a long time ago. Somethin' about a greedy leader not agreeing with policies of the Kerritamba chief. I dunno. Not too good with politicin', meself. But I know a good drink when I see one!
			string_id message = new string_id (c_stringFile, "s_180");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I bet you do. Thank you for the information.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_181");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Myyydril, huh? Welp. Let me see. There's not much to say, really. The Myyydril were thrown out o'the Kerritamba village a long time ago. Somethin' about a greedy leader not agreeing with policies of the Kerritamba chief. I dunno. Not too good with politicin', meself. But I know a good drink when I see one!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I bet you do. Thank you for the information.
	if (response == "s_181")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Anathin' else?
			string_id message = new string_id (c_stringFile, "s_182");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, you've been a great help.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Anathin' else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, you've been a great help.
	if (response == "s_183")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Thank ye. Welp. I best be off ta me work.
			string_id message = new string_id (c_stringFile, "s_184");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Myyydril, huh? Welp. Let me see. There's not much to say, really. The Myyydril were thrown out o'the Kerritamba village a long time ago. Somethin' about a greedy leader not agreeing with policies of the Kerritamba chief. I dunno. Not too good with politicin', meself. But I know a good drink when I see one!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks for the help.
	if (response == "s_185")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: What else ye be needin' ta know?
			string_id message = new string_id (c_stringFile, "s_186");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are Urnies??
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_188");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What else ye be needin' ta know?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are Urnies??
	if (response == "s_188")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Urnsor'is. Them be ugly boogers. Got a brain as a body, with sharp, pointy claws. Scares the poodoo right outta ya. I suggest ye be stayin' away from them Urnies.
			string_id message = new string_id (c_stringFile, "s_190");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [You wince.]
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_192");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Urnsor'is. Them be ugly boogers. Got a brain as a body, with sharp, pointy claws. Scares the poodoo right outta ya. I suggest ye be stayin' away from them Urnies.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [You wince.]
	if (response == "s_192")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: That's what I thought too. Ye be needin' anathin' else?
			string_id message = new string_id (c_stringFile, "s_194");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, you've been a great help.
			boolean hasResponse0 = false;
			if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_208");

				utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_myyydril_cantina_girl_handleBranch40 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's what I thought too. Ye be needin' anathin' else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, you've been a great help.
	if (response == "s_208")
	{
		//-- [NOTE] 
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			//-- NPC: Thank ye. Welp. I best be off ta me work.
			string_id message = new string_id (c_stringFile, "s_210");
			utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.ep3_myyydril_cantina_girl");

	setCondition (self, CONDITION_CONVERSABLE);
	setName (self, new string_id( "ep3/npc_names", "kirrir" ));
	setInvulnerable(self, true);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setName (self, new string_id( "ep3/npc_names", "kirrir" ));
	setInvulnerable(self, true);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.ep3_myyydril_cantina_girl");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (ep3_myyydril_cantina_girl_condition_hasCompletedQuestTwo (player, npc))
	{
		//-- NPC: Ah! Welcome back, me friend. There ain't much goin' on these days. Bah! I gotta get back ta work.
		string_id message = new string_id (c_stringFile, "s_350");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_myyydril_cantina_girl_condition_isTaskActiveTwo (player, npc))
	{
		//-- NPC: Have ye talked to me girl Nawika yet? What are ye waitin' fer? Don't be shy!
		string_id message = new string_id (c_stringFile, "s_326");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_myyydril_cantina_girl_condition_hasCompletedQuestOne (player, npc))
	{
		//-- NPC: Ye know. We've got a lot ta do around here. The next person you should visit is me girl, Nawika here in the cantina. She's been needin' some help.
		string_id message = new string_id (c_stringFile, "s_333");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'll go speak with her. Thank you.
		boolean hasResponse0 = false;
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_334");

			utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 3);

			npcStartConversation (player, npc, "ep3_myyydril_cantina_girl", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_myyydril_cantina_girl_condition_hasCompletedTaskOne (player, npc))
	{
		ep3_myyydril_cantina_girl_action_giveSignal (player, npc);

		//-- NPC: I smell Warl nectar all over ye! Thank ye fer gettin' it. Now, I can make me favorite drink. Let me stirr up a few. I knew me patrons been waitin' fer it fer awhile.
		string_id message = new string_id (c_stringFile, "s_346");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'll wait right here.
		boolean hasResponse0 = false;
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_347");

			utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 5);

			npcStartConversation (player, npc, "ep3_myyydril_cantina_girl", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_myyydril_cantina_girl_condition_isActiveTaskOne (player, npc))
	{
		//-- NPC: Ye got me Warl leaves yet? No? Ye best get out there, then, afore the storm comes. Warl leaves don't last too long in heavy rainfall.
		string_id message = new string_id (c_stringFile, "s_349");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_myyydril_cantina_girl_condition_hasCompletedQuestOther (player, npc))
	{
		//-- NPC: What can I--oh? A task I be needin' help with? Who sent ye? Was it tha sweetheart Tala'oree? She's always lookin' out fer me. Let me see what I need from ye.. oh yes! I'm runnin' out o' nectar fer me specialty, 'Warl Surprise'. Ye can get the nectar from the leaves of Warl plants. I need 10 leaves. What ya say?
		string_id message = new string_id (c_stringFile, "s_341");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'll go take a look. What do they look like?
		boolean hasResponse0 = false;
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not right now.
		boolean hasResponse1 = false;
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_342");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_343");

			utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 8);

			npcStartConversation (player, npc, "ep3_myyydril_cantina_girl", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
	{
		//-- NPC: What can I get fer ya?
		string_id message = new string_id (c_stringFile, "s_300");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Nothing, really.
		boolean hasResponse0 = false;
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Know any rumors?
		boolean hasResponse1 = false;
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to ask you some questions, if I can.
		boolean hasResponse2 = false;
		if (ep3_myyydril_cantina_girl_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_301");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_303");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_305");

			utils.setScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId", 12);

			npcStartConversation (player, npc, "ep3_myyydril_cantina_girl", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "ep3_myyydril_cantina_girl")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

	if (branchId == 3 && ep3_myyydril_cantina_girl_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && ep3_myyydril_cantina_girl_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && ep3_myyydril_cantina_girl_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && ep3_myyydril_cantina_girl_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && ep3_myyydril_cantina_girl_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && ep3_myyydril_cantina_girl_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && ep3_myyydril_cantina_girl_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && ep3_myyydril_cantina_girl_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && ep3_myyydril_cantina_girl_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && ep3_myyydril_cantina_girl_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && ep3_myyydril_cantina_girl_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && ep3_myyydril_cantina_girl_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && ep3_myyydril_cantina_girl_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && ep3_myyydril_cantina_girl_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && ep3_myyydril_cantina_girl_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && ep3_myyydril_cantina_girl_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && ep3_myyydril_cantina_girl_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && ep3_myyydril_cantina_girl_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && ep3_myyydril_cantina_girl_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && ep3_myyydril_cantina_girl_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && ep3_myyydril_cantina_girl_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && ep3_myyydril_cantina_girl_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && ep3_myyydril_cantina_girl_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 40 && ep3_myyydril_cantina_girl_handleBranch40 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.ep3_myyydril_cantina_girl.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

