// ======================================================================
//
// naboo_imperial_trainer_2.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.skill;
include library.space_flags;
include library.space_quest;
include library.utils;
include library.xp;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/naboo_imperial_trainer_2";

// ======================================================================
// Script Conditions
// ======================================================================

boolean naboo_imperial_trainer_2_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasAllSkills (obj_id player, obj_id npc)
{
	/*
	if (hasSkill( player, "pilot_imperial_navy_starships_02" ) &&
		hasSkill( player, "pilot_imperial_navy_weapons_02" ) &&
		hasSkill( player, "pilot_imperial_navy_procedures_02" ) &&
		hasSkill( player, "pilot_imperial_navy_droid_02" ) )
		 return true;
	else
		return false;
	*/
	
	return space_flags.hasCompletedTierTwo(player);
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_onMyTrack (obj_id player, obj_id npc)
{
	return space_flags.isSpaceTrack(player, space_flags.IMPERIAL_NABOO);
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_canFlyNonTrackDuty (obj_id player, obj_id npc)
{
	if(space_flags.hasCompletedTierOne(player) && !space_quest.hasQuest(player))
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isReadyForMission1 (obj_id player, obj_id npc)
{
	/*
	if ( hasObjVar( player, "inquisitorialAgent" ) )
		return true;
	else
		return false;
	*/
	
	return space_flags.hasSpaceFlag(player, "inquisitorialAgent");
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isPrivateer (obj_id player, obj_id npc)
{
	if ( hasSkill( player, "pilot_neutral_novice" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isRebel (obj_id player, obj_id npc)
{
	if ( factions.getFaction(player) == factions.FACTION_REBEL )
		return true;

	if ( hasSkill( player, "pilot_rebel_navy_novice" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasFailedQuest1 (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest(  player, "inspect", "naboo_imperial_tier2_1" ) ||
		     space_quest.hasAbortedQuest( player, "inspect", "naboo_imperial_tier2_1" ) );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasWonQuest1 (obj_id player, obj_id npc)
{
	return space_quest.hasWonQuest( player, "inspect", "naboo_imperial_tier2_1" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isReadyForDutyBlock1 (obj_id player, obj_id npc)
{
	return space_quest.hasReceivedReward( player, "survival", "naboo_imperial_tier2_2" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasWonQuest2 (obj_id player, obj_id npc)
{
	return space_quest.hasWonQuest( player, "survival", "naboo_imperial_tier2_2" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasFailedQuest2 (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest(  player, "survival", "naboo_imperial_tier2_2" ) ||
		     space_quest.hasAbortedQuest( player, "survival", "naboo_imperial_tier2_2" ) );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isReadyForMission2 (obj_id player, obj_id npc)
{
	return space_quest.hasReceivedReward( player, "inspect", "naboo_imperial_tier2_1" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_canBuySkill1 (obj_id player, obj_id npc)
{
	if ( space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_starships_02" ) ||
		 space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_weapons_02" ) ||
		 space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_procedures_02" ) ||
		 space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_droid_02" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_canBuySkill1A (obj_id player, obj_id npc)
{
	return space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_starships_02" ) && !hasSkill( player, "pilot_imperial_navy_starships_02" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_canBuySkill1B (obj_id player, obj_id npc)
{
	return space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_weapons_02" ) && !hasSkill( player, "pilot_imperial_navy_weapons_02" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_canBuySkill1C (obj_id player, obj_id npc)
{
	return space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_procedures_02" ) && !hasSkill( player, "pilot_imperial_navy_procedures_02" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_canBuySkill1D (obj_id player, obj_id npc)
{
	return space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_droid_02" ) && !hasSkill( player, "pilot_imperial_navy_droid_02" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isReadyForMission3 (obj_id player, obj_id npc)
{
	if ( hasSkill( player, "pilot_imperial_navy_starships_02" ) ||
		 hasSkill( player, "pilot_imperial_navy_weapons_02" ) ||
		 hasSkill( player, "pilot_imperial_navy_procedures_02" ) ||
		 hasSkill( player, "pilot_imperial_navy_droid_02" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isOnMission (obj_id player, obj_id npc)
{
	return space_quest.hasQuest( player );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isWorkingForTrent (obj_id player, obj_id npc)
{
	/*
	if (!space_quest.hasReceivedReward( player, "assassinate", "naboo_imperial_tier2_4" ))
		return false;
	
	if (hasSkill( player, "pilot_imperial_navy_starships_02" ) &&
		hasSkill( player, "pilot_imperial_navy_weapons_02" ) &&
		hasSkill( player, "pilot_imperial_navy_procedures_02" ) &&
		hasSkill( player, "pilot_imperial_navy_droid_02" ) )
		 return true;
	else
		return false;
	*/
	
	if(space_flags.hasCompletedTierTwo(player) && space_quest.hasReceivedReward( player, "assassinate", "naboo_imperial_tier2_4" ))
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isReadyForMission4 (obj_id player, obj_id npc)
{
	/*
	if ( hasSkill( player, "pilot_imperial_navy_starships_02" ) &&
		 hasSkill( player, "pilot_imperial_navy_weapons_02" ) &&
		 hasSkill( player, "pilot_imperial_navy_procedures_02" ) &&
		 hasSkill( player, "pilot_imperial_navy_droid_02" ) )
		return true;
	else
		return false;
	*/
	
	return space_flags.hasCompletedTierTwo(player);
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isPilot (obj_id player, obj_id npc)
{
	return space_flags.hasAnyPilotSkill(player);
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isReadyForTraining (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "survival", "naboo_imperial_tier2_2" ))
		return false;
		
	if ( space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_starships_02" ) ||
		 space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_weapons_02" ) ||
		 space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_procedures_02" ) ||
		 space_quest.isPlayerQualifiedForSkill( player, "pilot_imperial_navy_droid_02" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_rewardedForClaw (obj_id player, obj_id npc)
{
	return space_quest.hasReceivedReward( player, "assassinate", "imperial_ss_3" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_isHereToSignUp (obj_id player, obj_id npc)
{
	return space_quest.hasCompletedQuestSeries( player, "naboo_imperial_1" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasFailedQuest3 (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest(  player, "recovery", "naboo_imperial_tier2_3" ) ||
		     space_quest.hasAbortedQuest( player, "recovery", "naboo_imperial_tier2_3" ) );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasWonQuest3 (obj_id player, obj_id npc)
{
	return space_quest.hasWonQuest( player, "recovery", "naboo_imperial_tier2_3" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasFailedQuest4 (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest(  player, "assassinate", "naboo_imperial_tier2_4" ) ||
		     space_quest.hasAbortedQuest( player, "assassinate", "naboo_imperial_tier2_4" ) );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasWonQuest4 (obj_id player, obj_id npc)
{
	return space_quest.hasWonQuest( player, "assassinate", "naboo_imperial_tier2_4" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasWonQuest3A (obj_id player, obj_id npc)
{
	return space_quest.hasReceivedReward( player, "recovery", "naboo_imperial_tier2_3" );
}

// ----------------------------------------------------------------------

boolean naboo_imperial_trainer_2_condition_hasNegativeFaction (obj_id player, obj_id npc)
{
	return ( factions.getFactionStanding(player, factions.FACTION_IMPERIAL) < 0.0f );
}

// ======================================================================
// Script Actions
// ======================================================================

void naboo_imperial_trainer_2_action_makeInquisitorialAgent (obj_id player, obj_id npc)
{
	space_flags.setSpaceFlag(player, "inquisitorialAgent", 1);
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveQuest1 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "inspect", "naboo_imperial_tier2_1" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveReward1 (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "inspect", "naboo_imperial_tier2_1") )
	{
		space_quest.giveReward( player, "inspect", "naboo_imperial_tier2_1", 5000, "object/tangible/ship/components/weapon/wpn_mission_reward_imperial_cygnus_starblaster.iff" );
		factions.addFactionStanding( player, factions.FACTION_IMPERIAL, 75.0f );
	}	
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveQuest2 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "survival", "naboo_imperial_tier2_2" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveReward2 (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "survival", "naboo_imperial_tier2_2") )
	{
		space_quest.giveReward( player, "survival", "naboo_imperial_tier2_2", 5000, "object/tangible/ship/components/armor/arm_mission_reward_imperial_sfs_light_military.iff" );
		factions.addFactionStanding( player, factions.FACTION_IMPERIAL, 75.0f );
	}
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_grantSkill1A (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_imperial_navy_starships_02" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_grantSkill1B (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_imperial_navy_weapons_02" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_grantSkill1C (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_imperial_navy_procedures_02" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_grantSkill1D (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_imperial_navy_droid_02" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveReward4 (obj_id player, obj_id npc)
{	
	if (!space_quest.hasReceivedReward( player, "assassinate", "naboo_imperial_tier2_4") )
	{
		space_quest.giveReward( player, "assassinate", "naboo_imperial_tier2_4", 5000, "object/tangible/ship/components/shield_generator/shd_mission_reward_imperial_rendili_dual_projector.iff" );
		factions.addFactionStanding( player, factions.FACTION_IMPERIAL, 75.0f );
	}
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveDuty1A (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "recovery_duty", "naboo_imperial_tier2_recoveryduty" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveDuty1B (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "escort_duty", "naboo_imperial_tier2_escortduty" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveDuty1C (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy_duty", "naboo_imperial_tier2_destroyduty" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveQuest3 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "recovery", "naboo_imperial_tier2_3" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveQuest4 (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "assassinate", "naboo_imperial_tier2_4" );
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_removeQuest3ObjVar (obj_id player, obj_id npc)
{
	space_flags.removeSpaceFlag(player, "ss.finishedQuest3");
}

// ----------------------------------------------------------------------

void naboo_imperial_trainer_2_action_giveReward3 (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "recovery", "naboo_imperial_tier2_3") )
	{
		space_quest.giveReward( player, "recovery", "naboo_imperial_tier2_3", 5000, "object/tangible/ship/components/booster/bst_mission_reward_imperial_rss_ion_booster.iff" );
		factions.addFactionStanding( player, factions.FACTION_IMPERIAL, 75.0f );
	}
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int naboo_imperial_trainer_2_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What is the matter with you, pilot?!  You dare show your face around here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are you talking about?
	if (response == "s_2313ac9e")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: I believe you know exactly what I'm talking about.  You're lucky you aren't shot on sight!  Naval command has issued orders throughout the galaxy... %TU is NOT to be trusted.  That's YOU, isn't it?
			string_id message = new string_id (c_stringFile, "s_c5e2a04a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Wait a minute... what happened?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97222fa4");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 5);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I believe you know exactly what I'm talking about.  You're lucky you aren't shot on sight!  Naval command has issued orders throughout the galaxy... %TU is NOT to be trusted.  That's YOU, isn't it?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Wait a minute... what happened?
	if (response == "s_97222fa4")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slit_throat");

			//-- NPC: I hope you're trying to tell me this was all a misunderstanding.  You have NOT defected to the Rebel Alliance?  Because if you think we're not on to you... you will not like the consequences of your desertion.
			string_id message = new string_id (c_stringFile, "s_3ce9c229");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, I am loyal to the Empire!
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a7cceea9");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I hope you're trying to tell me this was all a misunderstanding.  You have NOT defected to the Rebel Alliance?  Because if you think we're not on to you... you will not like the consequences of your desertion.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I am loyal to the Empire!
	if (response == "s_a7cceea9")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: You have a peculiar definition of loyalty.  Nevertheless, you might have a chance to redeem yourself.  If you can improve your standing in the eyes of the Empire... you MIGHT be allowed to fly again.
			string_id message = new string_id (c_stringFile, "s_34d641c9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f8b02865");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have a peculiar definition of loyalty.  Nevertheless, you might have a chance to redeem yourself.  If you can improve your standing in the eyes of the Empire... you MIGHT be allowed to fly again.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I do?
	if (response == "s_f8b02865")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "snap_finger2");

			//-- NPC: Two things, %TU.  One, you must perform missions for the Empire until your standing has improved enough that we might consider you trustworthy again.
			string_id message = new string_id (c_stringFile, "s_2a77f19e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's the second thing?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1ad5c0e0");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 8);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Two things, %TU.  One, you must perform missions for the Empire until your standing has improved enough that we might consider you trustworthy again.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the second thing?
	if (response == "s_1ad5c0e0")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: The second thing is, you must go away from here, and don't show your face until you are no longer considered an enemy of the Empire.
			string_id message = new string_id (c_stringFile, "s_9930b065");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, you're a long way from your squadron pilot. What brings you out this way?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would like to request a mission.
	if (response == "s_4854758d")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canFlyNonTrackDuty (player, npc))
		{
			//-- NPC: There's not very much, but I do have some elective duty missions that you can fly. I have a briefing prepared if you'd like to hear it.
			string_id message = new string_id (c_stringFile, "s_3ead1387");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like a briefing.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm ready to take an assignment.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2a924ab");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ac71da57");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm sorry. I can only assign missions to pilots in my own unit. You should return and speak to your commanding officer.
			string_id message = new string_id (c_stringFile, "s_a2cfa58e");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There's not very much, but I do have some elective duty missions that you can fly. I have a briefing prepared if you'd like to hear it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like a briefing.
	if (response == "s_b2a924ab")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: As I said, there are several areas that the local command needs assistance in. The first is strictly a simple police matter...if we could rely on the local authorities. A number of ships have been attempting to leave the system without filing the proper flight plans. You are to escort them back to port, by force if necessary.
			string_id message = new string_id (c_stringFile, "s_6a6fe80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Nod and listen.)
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5102164f");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to take an assignment.
	if (response == "s_ac71da57")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You have operational discretion to choose an assignment from the areas outlined by Imperial Intelligence in your briefing. I can also instruct you in the use of new technology if you think you're ready.
			string_id message = new string_id (c_stringFile, "s_5b4ccb84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to assist in the police action.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like to serve the Emperor and escort the freighters.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: It would be an honor to assist you in your quest to stamp out the insurgents.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready for training.
			boolean hasResponse3 = false;
			if (naboo_imperial_trainer_2_condition_onMyTrack (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e84f3a62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e6765c30");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f0a7a4ef");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0d3b474");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You need to report to Inquisitor Vrke inside the Emperor's Retreat. He will be handling your assignments for the time being.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't you want to say good bye to me?
	if (response == "s_3ba5368f")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You mean, wish you well...tell you what a good job you have done for me and how much I will miss having you around?
			string_id message = new string_id (c_stringFile, "s_f693949d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, that sort of thing.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_959d8d9b");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ok, well thanks for your guidance.
	if (response == "s_f2cf68b3")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes I know that I really went above and beyond the call of duty to help you. You were very lucky to have me as your commanding officer. Now don't keep Inquisitor Vrke waiting.
			string_id message = new string_id (c_stringFile, "s_b0e9edf4");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You mean, wish you well...tell you what a good job you have done for me and how much I will miss having you around?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, that sort of thing.
	if (response == "s_959d8d9b")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: No.
			string_id message = new string_id (c_stringFile, "s_4c695dbd");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Have you accomplished the task that I gave to you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir. I have taught the traitor the error of his ways.
	if (response == "s_97d51490")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveReward4 (player, npc);

			//-- NPC: Excellent. I think that you might have a very bright future with the Imperial Inquisition. I have decided to transfer you to Inquisitor Vrke. He is one of the finest field operatives within the Inquisition and I believe that you will be an asset to his operations.
			string_id message = new string_id (c_stringFile, "s_49be19d2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you sir. It has been an honor to serve under you.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1bac802d");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir. I think I need more training though.
	if (response == "s_c104d444")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: I agree with you. Let's get you better prepared for future operations.
			string_id message = new string_id (c_stringFile, "s_7be3e248");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: In what am I to be trained?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me think about this for a bit.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_213");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ae26c22");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent. I think that you might have a very bright future with the Imperial Inquisition. I have decided to transfer you to Inquisitor Vrke. He is one of the finest field operatives within the Inquisition and I believe that you will be an asset to his operations.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you sir. It has been an honor to serve under you.
	if (response == "s_1bac802d")
	{
		doAnimationAction (player, "salute1");

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (player, "salute1");

			//-- NPC: The honor has been entirely mine. I have a feeling that one day you will be a shining example of an Inquisition officer. Listen and learn well from Vrke. He is currently stationed at the Emperor's Retreat and is expecting you. Farewell, until we meet again.
			string_id message = new string_id (c_stringFile, "s_bc64aa40");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I agree with you. Let's get you better prepared for future operations.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: In what am I to be trained?
	if (response == "s_213")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1A (player, npc))
		{
			//-- NPC: You are authorized for training in advanced TIE combat operations.
			string_id message = new string_id (c_stringFile, "s_214");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_218");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1B (player, npc))
		{
			//-- NPC: You are authorized for training in advanced equipment technology.
			string_id message = new string_id (c_stringFile, "s_215");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_220");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1C (player, npc))
		{
			//-- NPC: You are authorized for training in pilot combat tactics.
			string_id message = new string_id (c_stringFile, "s_216");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_222");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are authorized for training in advanced reactor engineering algorithms.
			string_id message = new string_id (c_stringFile, "s_217");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_224");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let me think about this for a bit.
	if (response == "s_ae26c22")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're cleared for training, so let me know when you are ready to begin.
			string_id message = new string_id (c_stringFile, "s_7b25fbc3");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Have you accomplished what I have asked of you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, sir. I am having difficulties with the colonel.
	if (response == "s_9c394eb4")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: I told you once that I do not tolerate failure. Return to the Dantooine system and destroy that traitor.
			string_id message = new string_id (c_stringFile, "s_306da215");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir! I will not fail you again.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_846f2737");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I told you once that I do not tolerate failure. Return to the Dantooine system and destroy that traitor.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir! I will not fail you again.
	if (response == "s_846f2737")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveQuest4 (player, npc);

			//-- NPC: For your sake I hope that you are right.
			string_id message = new string_id (c_stringFile, "s_164b059e");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I was wondering when you would finally arrive. We have just finished our calculations on Colonel Hend's jump position and it is clear he has traveled to the Dantooine system. The time has come for the Colonel to be punished for his crimes against the Emperor.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am ready and willing to make the traitor pay.
	if (response == "s_1f033c46")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: As I knew you would be. Travel to the Dantooine system and scan for the colonel's ship. Once you have found it pass judgement on the colonel without mercy.
			string_id message = new string_id (c_stringFile, "s_d1fa21c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, my lord. But won't the colonel be expecting us?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_20b3aa70");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: As I knew you would be. Travel to the Dantooine system and scan for the colonel's ship. Once you have found it pass judgement on the colonel without mercy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, my lord. But won't the colonel be expecting us?
	if (response == "s_20b3aa70")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveQuest4 (player, npc);

			//-- NPC: No one expects the Imperial Inquisition. He obviously thinks that he has gotten away with his crimes. Now go before we lose him again.
			string_id message = new string_id (c_stringFile, "s_e17af774");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Naval Command wants us to continue to assist the local command. Their assessment of the situation is positive, but they feel that inflicting further damage is necessary.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like a briefing.
	if (response == "s_b2a924ab")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: As I said, there are several areas that the local command needs assistance in. The first is strictly a simple police matter...if we could rely on the local authorities. A number of ships have been attempting to leave the system without filing the proper flight plans. You are to escort them back to port, by force if necessary.
			string_id message = new string_id (c_stringFile, "s_6a6fe80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Nod and listen.)
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5102164f");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to take an assignment.
	if (response == "s_ac71da57")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You have operational discretion to choose an assignment from the areas outlined by Imperial Intelligence in your briefing. I can also instruct you in the use of new technology if you think you're ready.
			string_id message = new string_id (c_stringFile, "s_5b4ccb84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to assist in the police action.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like to serve the Emperor and escort the freighters.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: It would be an honor to assist you in your quest to stamp out the insurgents.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready for training.
			boolean hasResponse3 = false;
			if (naboo_imperial_trainer_2_condition_onMyTrack (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e84f3a62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e6765c30");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f0a7a4ef");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0d3b474");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good you're back. We have discovered a serious problem. We managed to decipher the remainder of the message that you intercepted. It would seem that Colonel Hend was the one who was leaking the information to the insurgents.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What? And we assisted him on his escape.
	if (response == "s_96db4fc7")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: That is what it would appear. This transgression must never be allowed to go unpunished. It is time for Colonel Hend to be punished for his treachery. We are in the process of tracking down where the colonel went. Once we have found his location it will be up to you to pass sentence on the traitor.
			string_id message = new string_id (c_stringFile, "s_196d2fe6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, my lord. What should I do until you have his location?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_369ec9fc");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That is what it would appear. This transgression must never be allowed to go unpunished. It is time for Colonel Hend to be punished for his treachery. We are in the process of tracking down where the colonel went. Once we have found his location it will be up to you to pass sentence on the traitor.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, my lord. What should I do until you have his location?
	if (response == "s_369ec9fc")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveReward3 (player, npc);

			//-- NPC: For now we will act as if nothing is amiss. Until we can figure out where he is, continue your operation helping the local forces.
			string_id message = new string_id (c_stringFile, "s_85fe2519");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like a briefing.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm ready to take an assignment.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2a924ab");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ac71da57");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: For now we will act as if nothing is amiss. Until we can figure out where he is, continue your operation helping the local forces.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like a briefing.
	if (response == "s_b2a924ab")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: As I said, there are several areas that the local command needs assistance in. The first is strictly a simple police matter...if we could rely on the local authorities. A number of ships have been attempting to leave the system without filing the proper flight plans. You are to escort them back to port, by force if necessary.
			string_id message = new string_id (c_stringFile, "s_6a6fe80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Nod and listen.)
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5102164f");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to take an assignment.
	if (response == "s_ac71da57")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You have operational discretion to choose an assignment from the areas outlined by Imperial Intelligence in your briefing. I can also instruct you in the use of new technology if you think you're ready.
			string_id message = new string_id (c_stringFile, "s_5b4ccb84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to assist in the police action.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like to serve the Emperor and escort the freighters.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: It would be an honor to assist you in your quest to stamp out the insurgents.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready for training.
			boolean hasResponse3 = false;
			if (naboo_imperial_trainer_2_condition_onMyTrack (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e84f3a62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e6765c30");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f0a7a4ef");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0d3b474");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What are you doing back here, pilot? I haven't received a report that you were successful in your mission.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am sorry but I seem to be having some difficulties...
	if (response == "s_f6d4476d")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: I do not care for your difficulties. Get back out there and finish what you started. If you are unable or unwilling to perform this task, perhaps you would care to answer some questions that I have?
			string_id message = new string_id (c_stringFile, "s_825fac62");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That will not be necessary sir. I will handle itl.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_91e4b620");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 33);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I do not care for your difficulties. Get back out there and finish what you started. If you are unable or unwilling to perform this task, perhaps you would care to answer some questions that I have?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That will not be necessary sir. I will handle itl.
	if (response == "s_91e4b620")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveQuest3 (player, npc);

			//-- NPC: I knew I could count on you. Report back to me when you have finished the job. Don't disappoint me again, pilot.
			string_id message = new string_id (c_stringFile, "s_172871ff");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good to see you, Flying Officer %NU. You've been making significant impact with the other branches of the Imperial High Command. You have certainly done the Inquisition proud. But the time has come for you to resume your duties in rooting out traitors.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Point me in the proper direction and I will do as you command.
	if (response == "s_51fe08f5")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: The transmission that you managed to intercept for us has been partially deciphered and it indicated plans to abduct Colonel Hend of the Imperial Navy. Unfortunately, we received this information too late to stop the abduction.
			string_id message = new string_id (c_stringFile, "s_de870f1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are there any leads on where they took him?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c9b80ee");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The transmission that you managed to intercept for us has been partially deciphered and it indicated plans to abduct Colonel Hend of the Imperial Navy. Unfortunately, we received this information too late to stop the abduction.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are there any leads on where they took him?
	if (response == "s_3c9b80ee")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: There are several leads and the Navy is sending out patrols trying to recover Hend. The Inquisition has volunteered to assist the Navy in their search for the lost colonel. Of course, we have information at our disposal that the Navy is not privy to...such as the jump location of the transport that Hend is on.
			string_id message = new string_id (c_stringFile, "s_26408ea");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Shouldn't we inform the Imperial Navy of this location?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6e39f51b");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There are several leads and the Navy is sending out patrols trying to recover Hend. The Inquisition has volunteered to assist the Navy in their search for the lost colonel. Of course, we have information at our disposal that the Navy is not privy to...such as the jump location of the transport that Hend is on.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Shouldn't we inform the Imperial Navy of this location?
	if (response == "s_6e39f51b")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Why would we want to do something like that? It would be a major black eye to the Navy if the Inquisition managed to find and rescue their lost man before they could.
			string_id message = new string_id (c_stringFile, "s_b257ddf8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course. I wasn't thinking, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fcff7b8e");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why would we want to do something like that? It would be a major black eye to the Navy if the Inquisition managed to find and rescue their lost man before they could.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course. I wasn't thinking, sir.
	if (response == "s_fcff7b8e")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You still have much to learn about what it means to be a member of the Inquisition. Regardless, you are to enter the Naboo system and recover the colonel. His transport will most likely be heavily guarded and I doubt they will be willing to give up their prize without a fight. Once you have his ship secured protect it from counter-attack until it can hyperspace back into safe space.
			string_id message = new string_id (c_stringFile, "s_1d63bf5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b43a7ee");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You still have much to learn about what it means to be a member of the Inquisition. Regardless, you are to enter the Naboo system and recover the colonel. His transport will most likely be heavily guarded and I doubt they will be willing to give up their prize without a fight. Once you have his ship secured protect it from counter-attack until it can hyperspace back into safe space.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes sir.
	if (response == "s_9b43a7ee")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveQuest3 (player, npc);

			//-- NPC: Remember, it will be worthless to the Inquisition if we are unable to recover the colonel alive. While you are recovering Hend we will continue our decoding of the transmission that you recovered.
			string_id message = new string_id (c_stringFile, "s_e1771eb6");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Pilot, it is time for you to learn some new skills. It is important that you keep sharp and keep your skills at the top of their game in order to succeed as an Imperial pilot. Are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am ready for some training.
	if (response == "s_c1bc23b4")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You've shown considerable improvement in ability since joining the Inquisition. It's time to allow you access to the next level of training and technology. I remind you that anything you learn is highly confidential, security rating Gamma Three. You may not share your knowledge with fellow officers of any rank at or below your own.
			string_id message = new string_id (c_stringFile, "s_9ac35c60");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: In what am I to be trained?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me think about this for a bit.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_213");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ae26c22");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You've shown considerable improvement in ability since joining the Inquisition. It's time to allow you access to the next level of training and technology. I remind you that anything you learn is highly confidential, security rating Gamma Three. You may not share your knowledge with fellow officers of any rank at or below your own.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: In what am I to be trained?
	if (response == "s_213")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1A (player, npc))
		{
			//-- NPC: You are authorized for training in advanced TIE combat operations.
			string_id message = new string_id (c_stringFile, "s_214");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_218");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1B (player, npc))
		{
			//-- NPC: You are authorized for training in advanced equipment technology.
			string_id message = new string_id (c_stringFile, "s_215");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_220");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1C (player, npc))
		{
			//-- NPC: You are authorized for training in pilot combat tactics.
			string_id message = new string_id (c_stringFile, "s_216");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_222");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are authorized for training in advanced reactor engineering algorithms.
			string_id message = new string_id (c_stringFile, "s_217");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_224");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let me think about this for a bit.
	if (response == "s_ae26c22")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're cleared for training, so let me know when you are ready to begin.
			string_id message = new string_id (c_stringFile, "s_7b25fbc3");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome back, Pilot. Things are rather calm for the moment, so you will be flying some duty missions.I have a briefing prepared. Or if you've already examined the briefing material, you can select a type of operation to begin.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like a briefing.
	if (response == "s_b2a924ab")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: As I said, there are several areas that the local command needs assistance in. The first is strictly a simple police matter...if we could rely on the local authorities. A number of ships have been attempting to leave the system without filing the proper flight plans. You are to escort them back to port, by force if necessary.
			string_id message = new string_id (c_stringFile, "s_6a6fe80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Nod and listen.)
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5102164f");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to take an assignment.
	if (response == "s_ac71da57")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You have operational discretion to choose an assignment from the areas outlined by Imperial Intelligence in your briefing. I can also instruct you in the use of new technology if you think you're ready.
			string_id message = new string_id (c_stringFile, "s_5b4ccb84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to assist in the police action.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like to serve the Emperor and escort the freighters.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: It would be an honor to assist you in your quest to stamp out the insurgents.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready for training.
			boolean hasResponse3 = false;
			if (naboo_imperial_trainer_2_condition_onMyTrack (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e84f3a62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e6765c30");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f0a7a4ef");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0d3b474");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch44 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: As I said, there are several areas that the local command needs assistance in. The first is strictly a simple police matter...if we could rely on the local authorities. A number of ships have been attempting to leave the system without filing the proper flight plans. You are to escort them back to port, by force if necessary.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Nod and listen.)
	if (response == "s_5102164f")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: The second matter is one that the Emperor himself took great interest in. Supply freighters will be leaving the Naboo system on their way to a small out of the way place called Endor. The Emperor very much wants these freighters to make it to their jump locations safe and sound.
			string_id message = new string_id (c_stringFile, "s_b8791e16");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Grin and listen.)
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45fa29cc");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The second matter is one that the Emperor himself took great interest in. Supply freighters will be leaving the Naboo system on their way to a small out of the way place called Endor. The Emperor very much wants these freighters to make it to their jump locations safe and sound.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Grin and listen.)
	if (response == "s_45fa29cc")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: The third and final task is one that I personally would like taken care of. I did a little investigation into the Dantooine system and have located small patrols of insurgents in the region. The Navy has no wish to begin operations in that system at this time but that doesn't stop me from sending one of my own in to clear out that little hornets nest.
			string_id message = new string_id (c_stringFile, "s_b2634e3d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir! I'm ready to take an assignment.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e71434ff");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 46);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch46 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The third and final task is one that I personally would like taken care of. I did a little investigation into the Dantooine system and have located small patrols of insurgents in the region. The Navy has no wish to begin operations in that system at this time but that doesn't stop me from sending one of my own in to clear out that little hornets nest.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir! I'm ready to take an assignment.
	if (response == "s_e71434ff")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You have operational discretion to choose an assignment from the areas outlined by Imperial Intelligence in your briefing. I can also instruct you in the use of new technology if you think you're ready.
			string_id message = new string_id (c_stringFile, "s_5b4ccb84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to assist in the police action.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like to serve the Emperor and escort the freighters.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: It would be an honor to assist you in your quest to stamp out the insurgents.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready for training.
			boolean hasResponse3 = false;
			if (naboo_imperial_trainer_2_condition_onMyTrack (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e84f3a62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e6765c30");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f0a7a4ef");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0d3b474");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have operational discretion to choose an assignment from the areas outlined by Imperial Intelligence in your briefing. I can also instruct you in the use of new technology if you think you're ready.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to assist in the police action.
	if (response == "s_e84f3a62")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveDuty1A (player, npc);

			//-- NPC: Very well. Patrol the Naboo system and we will direct you to any unauthorized flights. They are to be escorted back to port. If they resist, you are authorized to use any force necessary, with one exception: do not destroy any of those ships.
			string_id message = new string_id (c_stringFile, "s_4a2282c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8efde2ae");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would like to serve the Emperor and escort the freighters.
	if (response == "s_e6765c30")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveDuty1B (player, npc);

			//-- NPC: Everything we do serves the Emperor. Meet the freighters in the Naboo system and escort them to their hyperspace locations.
			string_id message = new string_id (c_stringFile, "s_dfdec194");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_89");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 50);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It would be an honor to assist you in your quest to stamp out the insurgents.
	if (response == "s_f0a7a4ef")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveDuty1C (player, npc);

			//-- NPC: That is what I wanted to hear. Travel to Dantooine and begin your search for the insurgents. Where ever you might find them wipe them out. Let the justice of the Inquisition be swift and merciless.
			string_id message = new string_id (c_stringFile, "s_79d26fde");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir!
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b6b25f13");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 52);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready for training.
	if (response == "s_b0d3b474")
	{
		//-- [NOTE] 
		if (!naboo_imperial_trainer_2_condition_canBuySkill1 (player, npc))
		{
			//-- NPC: Flying Officer %NU, I have the highest level of respect for you, but I want to see more results in the operations available to you before you are ready for advancement.
			string_id message = new string_id (c_stringFile, "s_d2f8399b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Very well, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_89bc583c");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 54);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You've shown considerable improvement in ability since joining the Inquisition. It's time to allow you access to the next level of training and technology. I remind you that anything you learn is highly confidential, security rating Gamma Three. You may not share your knowledge with fellow officers of any rank at or below your own.
			string_id message = new string_id (c_stringFile, "s_100");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: In what am I to be trained?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me think about this for a bit.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_213");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ae26c22");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch48 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well. Patrol the Naboo system and we will direct you to any unauthorized flights. They are to be escorted back to port. If they resist, you are authorized to use any force necessary, with one exception: do not destroy any of those ships.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir.
	if (response == "s_8efde2ae")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Be on your guard. Any one of those ships could be in league with the insurgents.
			string_id message = new string_id (c_stringFile, "s_205f33ca");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch50 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Everything we do serves the Emperor. Meet the freighters in the Naboo system and escort them to their hyperspace locations.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir.
	if (response == "s_89")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Be warned with the suspected traitor inside the Empire it is possible that our enemies have gotten word of these transports.
			string_id message = new string_id (c_stringFile, "s_fb6ae829");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That is what I wanted to hear. Travel to Dantooine and begin your search for the insurgents. Where ever you might find them wipe them out. Let the justice of the Inquisition be swift and merciless.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir!
	if (response == "s_b6b25f13")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: May the Emperor's blessing be on you, pilot.
			string_id message = new string_id (c_stringFile, "s_4682fc3c");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Flying Officer %NU, I have the highest level of respect for you, but I want to see more results in the operations available to you before you are ready for advancement.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very well, sir.
	if (response == "s_89bc583c")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Report back to me when you're ready for an assignment.
			string_id message = new string_id (c_stringFile, "s_49f3270b");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch56 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You've shown considerable improvement in ability since joining the Inquisition. It's time to allow you access to the next level of training and technology. I remind you that anything you learn is highly confidential, security rating Gamma Three. You may not share your knowledge with fellow officers of any rank at or below your own.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: In what am I to be trained?
	if (response == "s_213")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1A (player, npc))
		{
			//-- NPC: You are authorized for training in advanced TIE combat operations.
			string_id message = new string_id (c_stringFile, "s_214");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_218");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1B (player, npc))
		{
			//-- NPC: You are authorized for training in advanced equipment technology.
			string_id message = new string_id (c_stringFile, "s_215");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_220");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_canBuySkill1C (player, npc))
		{
			//-- NPC: You are authorized for training in pilot combat tactics.
			string_id message = new string_id (c_stringFile, "s_216");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_222");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are authorized for training in advanced reactor engineering algorithms.
			string_id message = new string_id (c_stringFile, "s_217");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_224");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let me think about this for a bit.
	if (response == "s_ae26c22")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're cleared for training, so let me know when you are ready to begin.
			string_id message = new string_id (c_stringFile, "s_7b25fbc3");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch57 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are authorized for training in advanced TIE combat operations.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir.
	if (response == "s_218")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_grantSkill1A (player, npc);

			//-- NPC: Your continued success will rely on your ability to make maximum use of all new Imperial technology. Use these study programs and destroy them when you have internalized the information.
			string_id message = new string_id (c_stringFile, "s_244");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Understood.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_246");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your continued success will rely on your ability to make maximum use of all new Imperial technology. Use these study programs and destroy them when you have internalized the information.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Understood.
	if (response == "s_246")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Report back to me when you are ready for a new assignment. The Inquisition is in need of someone with your talents yet again.
			string_id message = new string_id (c_stringFile, "s_248");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch60 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are authorized for training in advanced equipment technology.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir.
	if (response == "s_220")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_grantSkill1B (player, npc);

			//-- NPC: Mastering proper use of heavy equipment technology is key. Use these study programs and destroy them when you have internalized the information.
			string_id message = new string_id (c_stringFile, "s_238");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Understood.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Mastering proper use of heavy equipment technology is key. Use these study programs and destroy them when you have internalized the information.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Understood.
	if (response == "s_240")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Report back to me when you are ready for a new assignment. The Inquisition is in need of someone with your talents yet again.
			string_id message = new string_id (c_stringFile, "s_242");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch63 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are authorized for training in pilot combat tactics.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir.
	if (response == "s_222")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_grantSkill1C (player, npc);

			//-- NPC: Developing new levels of tactical and physical excellence will be critical in realizing your true potential. Use these study programs and destroy them when you have internalized the information.
			string_id message = new string_id (c_stringFile, "s_232");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Understood.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_234");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 64);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Developing new levels of tactical and physical excellence will be critical in realizing your true potential. Use these study programs and destroy them when you have internalized the information.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Understood.
	if (response == "s_234")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Report back to me when you are ready for a new assignment. The Inquisition is in need of someone with your talents yet again.
			string_id message = new string_id (c_stringFile, "s_236");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch66 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are authorized for training in advanced reactor engineering algorithms.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir.
	if (response == "s_224")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_grantSkill1D (player, npc);

			//-- NPC: Understanding how to rapidly and efficiently manage the complex systems on board a fighter is a necessary step in your development as a pilot. Use these study programs and destroy them when you have internalized the information.
			string_id message = new string_id (c_stringFile, "s_226");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Understood.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_228");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch67 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Understanding how to rapidly and efficiently manage the complex systems on board a fighter is a necessary step in your development as a pilot. Use these study programs and destroy them when you have internalized the information.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Understood.
	if (response == "s_228")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Report back to me when you are ready for a new assignment. The Inquisition is in need of someone with your talents yet again.
			string_id message = new string_id (c_stringFile, "s_230");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch70 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We have received the transmission successfully and have begun the process of deciphering their code. There will be no error in the data this time to protect the traitor. Yet again, you have proven yourself an asset to the Inquisition.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, sir.
	if (response == "s_987445c9")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Pilot Officer %NU, it's time that you were elevated to full operational status within the Inquisition. You are hereby granted the rank of Flying Officer. Congratulations Flying Officer %NU. You are now a full member of the Imperial Inquisition. Fly with honor and fight with the Emperor's own fury.
			string_id message = new string_id (c_stringFile, "s_78c7dc33");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir!
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_130");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 71);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch71 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Pilot Officer %NU, it's time that you were elevated to full operational status within the Inquisition. You are hereby granted the rank of Flying Officer. Congratulations Flying Officer %NU. You are now a full member of the Imperial Inquisition. Fly with honor and fight with the Emperor's own fury.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir!
	if (response == "s_130")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveReward2 (player, npc);

			//-- NPC: The Inquisition, in all its wisdom, has authorized a duty pay increase for your exceptional performance and new rank. Since you are now a Flying Officer, you may select your own assignment.
			string_id message = new string_id (c_stringFile, "s_b784db84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 72);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch72 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Inquisition, in all its wisdom, has authorized a duty pay increase for your exceptional performance and new rank. Since you are now a Flying Officer, you may select your own assignment.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir.
	if (response == "s_133")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: For the time being, the Inquisition is assisting the local command in its regional duties. It would seem that some of their pilots are less than trustworthy. Perhaps at some later date some of them need to be put to the question. But for now you will act as a normal member of the Imperial Navy.
			string_id message = new string_id (c_stringFile, "s_2865fd30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: As the Emperor commands.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d8b8f3ac");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch73 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: For the time being, the Inquisition is assisting the local command in its regional duties. It would seem that some of their pilots are less than trustworthy. Perhaps at some later date some of them need to be put to the question. But for now you will act as a normal member of the Imperial Navy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: As the Emperor commands.
	if (response == "s_d8b8f3ac")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes, as the Emperor commands.
			string_id message = new string_id (c_stringFile, "s_4df61c15");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is it you wish me to do?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fa3398f3");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch74 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, as the Emperor commands.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is it you wish me to do?
	if (response == "s_fa3398f3")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Since you have complete operational discretion, it will be up to you to decide how to assist the local command. They are having several issues that require someone with your particular skills.
			string_id message = new string_id (c_stringFile, "s_960be58b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready to begin immediately, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_91223b5c");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch75 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Since you have complete operational discretion, it will be up to you to decide how to assist the local command. They are having several issues that require someone with your particular skills.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to begin immediately, sir.
	if (response == "s_91223b5c")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: I have a briefing prepared. Or if you've already examined the briefing material, you can select a type of operation to begin.
			string_id message = new string_id (c_stringFile, "s_f85fd827");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like a briefing.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm ready to take an assignment.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2a924ab");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ac71da57");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch76 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I have a briefing prepared. Or if you've already examined the briefing material, you can select a type of operation to begin.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like a briefing.
	if (response == "s_b2a924ab")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: As I said, there are several areas that the local command needs assistance in. The first is strictly a simple police matter...if we could rely on the local authorities. A number of ships have been attempting to leave the system without filing the proper flight plans. You are to escort them back to port, by force if necessary.
			string_id message = new string_id (c_stringFile, "s_6a6fe80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Nod and listen.)
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5102164f");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to take an assignment.
	if (response == "s_ac71da57")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: You have operational discretion to choose an assignment from the areas outlined by Imperial Intelligence in your briefing. I can also instruct you in the use of new technology if you think you're ready.
			string_id message = new string_id (c_stringFile, "s_5b4ccb84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like to assist in the police action.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like to serve the Emperor and escort the freighters.
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: It would be an honor to assist you in your quest to stamp out the insurgents.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I'm ready for training.
			boolean hasResponse3 = false;
			if (naboo_imperial_trainer_2_condition_onMyTrack (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e84f3a62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e6765c30");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f0a7a4ef");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0d3b474");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch77 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Pilot, you're supposed to be on your ship! Get up there and intercept that transmission immediately!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir!
	if (response == "s_143")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveQuest2 (player, npc);

			//-- NPC: Return to me once you've successfully intercepted that transmission.
			string_id message = new string_id (c_stringFile, "s_964c5567");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch79 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Get suited up, you're being scrambled immediately.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir!
	if (response == "s_147")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: The information you retrieved from the courier has been decrypted. 
			string_id message = new string_id (c_stringFile, "s_6ca30e9d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_150");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 80);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch80 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The information you retrieved from the courier has been decrypted. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir.
	if (response == "s_150")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: It would seem that the encryption of the message was a bit more advanced than initially indicated and part of the message was lost. But we did manage to recover some very important details regardless.
			string_id message = new string_id (c_stringFile, "s_a5dff683");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I am ready to serve.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c0996c6b");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch81 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It would seem that the encryption of the message was a bit more advanced than initially indicated and part of the message was lost. But we did manage to recover some very important details regardless.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am ready to serve.
	if (response == "s_c0996c6b")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Your zeal is most appreciated. Although much was lost we did manage to uncover the location where the pirates will be meeting the insurgents. It would seem that they will be exchanging some important information.
			string_id message = new string_id (c_stringFile, "s_2dcd5da9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Do you want me to destroy the conspirators?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_19fe9586");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 82);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch82 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your zeal is most appreciated. Although much was lost we did manage to uncover the location where the pirates will be meeting the insurgents. It would seem that they will be exchanging some important information.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you want me to destroy the conspirators?
	if (response == "s_19fe9586")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: No, of course not. We must find out what is in that transmission. You are to travel to the meeting place and intercept that transmission. Once you have intercepted the signal immediately transmit the information back here.
			string_id message = new string_id (c_stringFile, "s_3252d1a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is the enemy planning on meeting?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_de5b2183");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 83);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch83 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No, of course not. We must find out what is in that transmission. You are to travel to the meeting place and intercept that transmission. Once you have intercepted the signal immediately transmit the information back here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is the enemy planning on meeting?
	if (response == "s_de5b2183")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: The meeting is going to be taking place inside of the Dantooine system. It would seem that the courier you captured was not the only ship involved in supplying data to the Rebellion.
			string_id message = new string_id (c_stringFile, "s_2e7df33d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I understand, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1980f233");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch84 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The meeting is going to be taking place inside of the Dantooine system. It would seem that the courier you captured was not the only ship involved in supplying data to the Rebellion.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I understand, sir.
	if (response == "s_1980f233")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once you have started your transmission there is a very high chance that you will be noticed by the enemy. You must complete the transmission at all costs. 
			string_id message = new string_id (c_stringFile, "s_3b69b91e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will not fail the Empire.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c602907a");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 85);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch85 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Once you have started your transmission there is a very high chance that you will be noticed by the enemy. You must complete the transmission at all costs. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will not fail the Empire.
	if (response == "s_c602907a")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveQuest2 (player, npc);

			//-- NPC: That is correct. You will not fail the Empire. Now to your ship, pilot. Get me that transmission.
			string_id message = new string_id (c_stringFile, "s_576f96b8");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch87 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What have you learned so far, %NU?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Transfer the information to Inquisitor Fa'Zoll.)
	if (response == "s_1d5aba18")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is most interesting. The data seems to be encrypted. Only the guilty need to hide what they are doing.
			string_id message = new string_id (c_stringFile, "s_5f0459fc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Perhaps Imperial Intelligence can crack it.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22a1fc52");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 88);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch88 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This is most interesting. The data seems to be encrypted. Only the guilty need to hide what they are doing.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Perhaps Imperial Intelligence can crack it.
	if (response == "s_22a1fc52")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveReward1 (player, npc);

			//-- NPC: Do not presume in matters that concern you not. Never the less, you have done very well, pilot. Here is a consignment fee for completing the job. Stay close, though. Once this data is cracked we might have need of someone with your particular skills.
			string_id message = new string_id (c_stringFile, "s_ad754");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you, sir.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 89);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch89 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Do not presume in matters that concern you not. Never the less, you have done very well, pilot. Here is a consignment fee for completing the job. Stay close, though. Once this data is cracked we might have need of someone with your particular skills.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, sir.
	if (response == "s_168")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go grab yourself a quick bite to eat and then report back. I have the utmost faith that the code will not keep us from the truth for very long.
			string_id message = new string_id (c_stringFile, "s_61eac8c");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch91 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Have you managed to track down the information that I requested, Pilot?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've run into some trouble so far.
	if (response == "s_29d0a2db")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: That is most unfortunate. But luck is with us this day since another meeting has taken place. Again the pilot is leaving the Naboo system for Dantooine. I find this most interesting, don't you? Capture and scan this ship...oh, and pilot, do not fail me again.
			string_id message = new string_id (c_stringFile, "s_4eef704f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, sir!
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_174");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 92);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch92 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That is most unfortunate. But luck is with us this day since another meeting has taken place. Again the pilot is leaving the Naboo system for Dantooine. I find this most interesting, don't you? Capture and scan this ship...oh, and pilot, do not fail me again.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, sir!
	if (response == "s_174")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveQuest1 (player, npc);

			//-- NPC: Good luck, pilot.
			string_id message = new string_id (c_stringFile, "s_aceff31e");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch94 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, yes, excellent. You are ready to begin our work, then?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am.
	if (response == "s_c8f39347")
	{
		doAnimationAction (player, "salute1");

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Then let us begin. It is a sad truth that many of the Emperor's own have chosen to betray his willful rule. The Imperial Inquisition must always practice vigilance against the traitor. It is often difficult to determine where these foul conspiracies take root. Indeed, the most dedicated traitor is also the most careful.
			string_id message = new string_id (c_stringFile, "s_41786376");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do you stop these crimes?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7c7a7681");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch95 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then let us begin. It is a sad truth that many of the Emperor's own have chosen to betray his willful rule. The Imperial Inquisition must always practice vigilance against the traitor. It is often difficult to determine where these foul conspiracies take root. Indeed, the most dedicated traitor is also the most careful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do you stop these crimes?
	if (response == "s_7c7a7681")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It is grim. Grim. We must often pluck up the brightest of our own commanding officers and examine their minds for any sign of conspiracy. The process is intrusive. Some do not live. As such, we only do this occasionally.
			string_id message = new string_id (c_stringFile, "s_5960dfeb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You scan their minds?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d4948205");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 96);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch96 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It is grim. Grim. We must often pluck up the brightest of our own commanding officers and examine their minds for any sign of conspiracy. The process is intrusive. Some do not live. As such, we only do this occasionally.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You scan their minds?
	if (response == "s_d4948205")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: To the best of our ability. Sometimes more traditional means are necessary. The Emperor has taught us much. More often, though, the traitor is undone by their own carelessness. His eminence, the High Inquisitor, has granted me the privileged task of bringing this traitor under the watchful eye of the Inquisition.
			string_id message = new string_id (c_stringFile, "s_a2137dae");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What part in this do I play?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f1be3213");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch97 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: To the best of our ability. Sometimes more traditional means are necessary. The Emperor has taught us much. More often, though, the traitor is undone by their own carelessness. His eminence, the High Inquisitor, has granted me the privileged task of bringing this traitor under the watchful eye of the Inquisition.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What part in this do I play?
	if (response == "s_f1be3213")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: I am an old man. I have worked with His Eminence the High Inquisitor for many years. He is older than I. We make use of able pilots to carry out our judgements and commands.
			string_id message = new string_id (c_stringFile, "s_f283cf35");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. What task do you have for me?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_daf7bb7d");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch98 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am an old man. I have worked with His Eminence the High Inquisitor for many years. He is older than I. We make use of able pilots to carry out our judgements and commands.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see. What task do you have for me?
	if (response == "s_daf7bb7d")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: For several months now the Inquisition has been monitoring activities between certain members of our military and suspected Rebel sympathizers. One such meeting took place not long ago and it is time that the Inquisition discovered the truth behind these suspicious activities.
			string_id message = new string_id (c_stringFile, "s_199bd27f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why don't you just arrest and question the parties involved?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_483fa8ea");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch99 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: For several months now the Inquisition has been monitoring activities between certain members of our military and suspected Rebel sympathizers. One such meeting took place not long ago and it is time that the Inquisition discovered the truth behind these suspicious activities.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why don't you just arrest and question the parties involved?
	if (response == "s_483fa8ea")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: I think that you will fit in well with the Inquisition. But you must learn patience. The officer involved has friends in very high places and without evidence we could leave ourselves open to scrutiny. The key to every good investigation is evidence.
			string_id message = new string_id (c_stringFile, "s_5bb1e2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do we gather the evidence that we need?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fed6017c");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 100);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch100 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I think that you will fit in well with the Inquisition. But you must learn patience. The officer involved has friends in very high places and without evidence we could leave ourselves open to scrutiny. The key to every good investigation is evidence.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do we gather the evidence that we need?
	if (response == "s_fed6017c")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: The suspected Rebel sympathizer is being overly cautious about not being stopped by any Imperial patrol and has filed a flight plan leaving the Naboo system in route to Dantooine. You are to stop his ship and inspect its navcomputer for any information that it might contain.
			string_id message = new string_id (c_stringFile, "s_d2fa677d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will do as you command, Fa'Zoll.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_32a6fe1c");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 101);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch101 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The suspected Rebel sympathizer is being overly cautious about not being stopped by any Imperial patrol and has filed a flight plan leaving the Naboo system in route to Dantooine. You are to stop his ship and inspect its navcomputer for any information that it might contain.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will do as you command, Fa'Zoll.
	if (response == "s_32a6fe1c")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_giveQuest1 (player, npc);

			//-- NPC: Yes. Yes, I do think that you will have a brilliant future with the Inquisition. Report back to me with your findings.
			string_id message = new string_id (c_stringFile, "s_486a2be");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch103 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, how may I help you? Have you come to report a suspicious activity? You really should speak to my secretary. I only handle heresies and other critical cases.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the Imperial Inquisition?
	if (response == "s_6396820e")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Inquisition is a unique branch of the Imperial Intelligence service. Our job is to maintain discipline and security within the Empire as a whole. We are the ever watchful eye against heresy and betrayal. The children of the Empire sleep safely each night, knowing that the Inquisition never rests.
			string_id message = new string_id (c_stringFile, "s_bb972bed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, but what do you do?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2101d5b");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 104);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you think of the Rebellion?
	if (response == "s_3ce84ec1")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: We do not honor the insurgents with a name within these halls. They are a collection of foul aliens and wayward children of the Emperor. Strange outworld philosophies have twisted their understanding and have driven them to betrayal. It is a sad time we live in when the light of the Empire cannot reach all living things.
			string_id message = new string_id (c_stringFile, "s_5f0835ab");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the Imperial Inquisition?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think of the Rebellion?
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I've been asked to report to you for duty.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6396820e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3ce84ec1");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_696a6a00");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've been asked to report to you for duty.
	if (response == "s_696a6a00")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_isHereToSignUp (player, npc))
		{
			//-- NPC: Ah, so you are the fine pilot that I have heard so much about. Your ruthlessness and efficiency are known among the Naval High Command and it is even said that the Emperor himself read your name in an activity report.
			string_id message = new string_id (c_stringFile, "s_7c0ff63d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I do what I can for the Empire.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95dcb85a");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_imperial_trainer_2_condition_isHereToSignUp (player, npc))
		{
			//-- NPC: The Imperial Inquisition does not recruit and does not outsource work. If we have need of you, we will contact you.
			string_id message = new string_id (c_stringFile, "s_ecb5e1e2");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch104 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Inquisition is a unique branch of the Imperial Intelligence service. Our job is to maintain discipline and security within the Empire as a whole. We are the ever watchful eye against heresy and betrayal. The children of the Empire sleep safely each night, knowing that the Inquisition never rests.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, but what do you do?
	if (response == "s_b2101d5b")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Investigations, judgements, and punishments. Each agent of the Inquisition is fully empowered to carry out any of these most important activities. The Emperor will suffer not for a moment the thought of a traitor to live. We must all be willing to give of ourselves that the Emperor's great work might continue.
			string_id message = new string_id (c_stringFile, "s_e2cb470f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the Imperial Inquisition?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think of the Rebellion?
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I've been asked to report to you for duty.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6396820e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3ce84ec1");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_696a6a00");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch105 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Investigations, judgements, and punishments. Each agent of the Inquisition is fully empowered to carry out any of these most important activities. The Emperor will suffer not for a moment the thought of a traitor to live. We must all be willing to give of ourselves that the Emperor's great work might continue.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the Imperial Inquisition?
	if (response == "s_6396820e")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Inquisition is a unique branch of the Imperial Intelligence service. Our job is to maintain discipline and security within the Empire as a whole. We are the ever watchful eye against heresy and betrayal. The children of the Empire sleep safely each night, knowing that the Inquisition never rests.
			string_id message = new string_id (c_stringFile, "s_bb972bed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, but what do you do?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2101d5b");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 104);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you think of the Rebellion?
	if (response == "s_3ce84ec1")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: We do not honor the insurgents with a name within these halls. They are a collection of foul aliens and wayward children of the Emperor. Strange outworld philosophies have twisted their understanding and have driven them to betrayal. It is a sad time we live in when the light of the Empire cannot reach all living things.
			string_id message = new string_id (c_stringFile, "s_5f0835ab");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the Imperial Inquisition?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think of the Rebellion?
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I've been asked to report to you for duty.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6396820e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3ce84ec1");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_696a6a00");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've been asked to report to you for duty.
	if (response == "s_696a6a00")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_isHereToSignUp (player, npc))
		{
			//-- NPC: Ah, so you are the fine pilot that I have heard so much about. Your ruthlessness and efficiency are known among the Naval High Command and it is even said that the Emperor himself read your name in an activity report.
			string_id message = new string_id (c_stringFile, "s_7c0ff63d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I do what I can for the Empire.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95dcb85a");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_imperial_trainer_2_condition_isHereToSignUp (player, npc))
		{
			//-- NPC: The Imperial Inquisition does not recruit and does not outsource work. If we have need of you, we will contact you.
			string_id message = new string_id (c_stringFile, "s_ecb5e1e2");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch106 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We do not honor the insurgents with a name within these halls. They are a collection of foul aliens and wayward children of the Emperor. Strange outworld philosophies have twisted their understanding and have driven them to betrayal. It is a sad time we live in when the light of the Empire cannot reach all living things.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the Imperial Inquisition?
	if (response == "s_6396820e")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: The Inquisition is a unique branch of the Imperial Intelligence service. Our job is to maintain discipline and security within the Empire as a whole. We are the ever watchful eye against heresy and betrayal. The children of the Empire sleep safely each night, knowing that the Inquisition never rests.
			string_id message = new string_id (c_stringFile, "s_bb972bed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, but what do you do?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2101d5b");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 104);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you think of the Rebellion?
	if (response == "s_3ce84ec1")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: We do not honor the insurgents with a name within these halls. They are a collection of foul aliens and wayward children of the Emperor. Strange outworld philosophies have twisted their understanding and have driven them to betrayal. It is a sad time we live in when the light of the Empire cannot reach all living things.
			string_id message = new string_id (c_stringFile, "s_5f0835ab");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the Imperial Inquisition?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you think of the Rebellion?
			boolean hasResponse1 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I've been asked to report to you for duty.
			boolean hasResponse2 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6396820e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3ce84ec1");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_696a6a00");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've been asked to report to you for duty.
	if (response == "s_696a6a00")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition_isHereToSignUp (player, npc))
		{
			//-- NPC: Ah, so you are the fine pilot that I have heard so much about. Your ruthlessness and efficiency are known among the Naval High Command and it is even said that the Emperor himself read your name in an activity report.
			string_id message = new string_id (c_stringFile, "s_7c0ff63d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I do what I can for the Empire.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95dcb85a");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_imperial_trainer_2_condition_isHereToSignUp (player, npc))
		{
			//-- NPC: The Imperial Inquisition does not recruit and does not outsource work. If we have need of you, we will contact you.
			string_id message = new string_id (c_stringFile, "s_ecb5e1e2");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch107 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, so you are the fine pilot that I have heard so much about. Your ruthlessness and efficiency are known among the Naval High Command and it is even said that the Emperor himself read your name in an activity report.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I do what I can for the Empire.
	if (response == "s_95dcb85a")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Such modesty! How refreshing. So many pilots believe they are the only thing keeping the Empire together. They do not realize that they are but a single thread in the grand tapestry. They do not realize that they could be cut and the whole would remain.
			string_id message = new string_id (c_stringFile, "s_8e68e9b0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How may I serve the Inquisition?
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4d0198f9");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 108);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch108 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Such modesty! How refreshing. So many pilots believe they are the only thing keeping the Empire together. They do not realize that they are but a single thread in the grand tapestry. They do not realize that they could be cut and the whole would remain.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How may I serve the Inquisition?
	if (response == "s_4d0198f9")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Right to the point as well. Yes, yes, I can see that I am going to enjoy our time together. Most exciting indeed! Well, now where to begin? Most members of the Inquisition spend several years at a seminary camp, learning the values of Imperial justice. You are being recruited on an accelerated time frame. This means you are exempt from seminary, but it also means you will be closely monitored for deviant activities.
			string_id message = new string_id (c_stringFile, "s_860b7132");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would never betray the Empire.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8f913d8b");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch109 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Right to the point as well. Yes, yes, I can see that I am going to enjoy our time together. Most exciting indeed! Well, now where to begin? Most members of the Inquisition spend several years at a seminary camp, learning the values of Imperial justice. You are being recruited on an accelerated time frame. This means you are exempt from seminary, but it also means you will be closely monitored for deviant activities.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would never betray the Empire.
	if (response == "s_8f913d8b")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed, I am sure you would not. Nonetheless, we must watch our agents twice as closely as we watch the other members of the Empire. Any violation of Imperial doctrine by an Inquisitorial agent is punishable by summary execution. Do not stray from the righteous path I will set for you.
			string_id message = new string_id (c_stringFile, "s_872eaec4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course not, Lord Fa'Zoll.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_53d26b00");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 110);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch110 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed, I am sure you would not. Nonetheless, we must watch our agents twice as closely as we watch the other members of the Empire. Any violation of Imperial doctrine by an Inquisitorial agent is punishable by summary execution. Do not stray from the righteous path I will set for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course not, Lord Fa'Zoll.
	if (response == "s_53d26b00")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			naboo_imperial_trainer_2_action_makeInquisitorialAgent (player, npc);

			//-- NPC: Very well, yes, yes. Very well. Now then, I have an initial assignment for you. Why don't you familiarize yourself with the facilities and then return when you are ready?
			string_id message = new string_id (c_stringFile, "s_131def5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, Lord Fa'Zoll.
			boolean hasResponse0 = false;
			if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a9a7be2");

				utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 111);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_imperial_trainer_2_handleBranch111 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well, yes, yes. Very well. Now then, I have an initial assignment for you. Why don't you familiarize yourself with the facilities and then return when you are ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, Lord Fa'Zoll.
	if (response == "s_6a9a7be2")
	{
		//-- [NOTE] 
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good, very good.
			string_id message = new string_id (c_stringFile, "s_50f84d04");
			utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.naboo_imperial_trainer_2");

	setCondition (self, CONDITION_CONVERSABLE);
	setName(self, "Inquisitor Holfheim");
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );
	factions.setFaction( self, factions.FACTION_IMPERIAL );

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setName(self, "Inquisitor Holfheim");
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );
	factions.setFaction( self, factions.FACTION_IMPERIAL );


	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.naboo_imperial_trainer_2");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (!naboo_imperial_trainer_2_condition_isPilot (player, npc))
	{
		//-- NPC: I'm sorry, I can't help you right now.
		string_id message = new string_id (c_stringFile, "s_21772e5a");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isRebel (player, npc))
	{
		//-- NPC: I'm sorry, I can't help you now.
		string_id message = new string_id (c_stringFile, "s_4");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isPrivateer (player, npc))
	{
		//-- NPC: I'm sorry, I can't help you.
		string_id message = new string_id (c_stringFile, "s_6");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasNegativeFaction (player, npc))
	{
		doAnimationAction (npc, "gesticulate_wildly");

		//-- NPC: What is the matter with you, pilot?!  You dare show your face around here?
		string_id message = new string_id (c_stringFile, "s_245945cb");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What are you talking about?
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2313ac9e");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 4);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_imperial_trainer_2_condition_onMyTrack (player, npc))
	{
		//-- NPC: Ah, you're a long way from your squadron pilot. What brings you out this way?
		string_id message = new string_id (c_stringFile, "s_1c006c1c");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I would like to request a mission.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4854758d");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 10);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isOnMission (player, npc))
	{
		//-- NPC: It looks like you're busy with an assignment already. Come talk to me when you're done.
		string_id message = new string_id (c_stringFile, "s_a59c7bd7");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isWorkingForTrent (player, npc))
	{
		//-- NPC: You need to report to Inquisitor Vrke inside the Emperor's Retreat. He will be handling your assignments for the time being.
		string_id message = new string_id (c_stringFile, "s_69ea2753");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Don't you want to say good bye to me?
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Ok, well thanks for your guidance.
		boolean hasResponse1 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3ba5368f");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f2cf68b3");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 14);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasWonQuest4 (player, npc))
	{
		//-- NPC: Have you accomplished the task that I gave to you?
		string_id message = new string_id (c_stringFile, "s_a08c6dc4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, sir. I have taught the traitor the error of his ways.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition_hasAllSkills (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes, sir. I think I need more training though.
		boolean hasResponse1 = false;
		if (!naboo_imperial_trainer_2_condition_hasAllSkills (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_97d51490");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c104d444");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 18);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasFailedQuest4 (player, npc))
	{
		//-- NPC: Have you accomplished what I have asked of you?
		string_id message = new string_id (c_stringFile, "s_681fb295");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No, sir. I am having difficulties with the colonel.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9c394eb4");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 22);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isReadyForMission4 (player, npc))
	{
		naboo_imperial_trainer_2_action_removeQuest3ObjVar (player, npc);

		//-- NPC: I was wondering when you would finally arrive. We have just finished our calculations on Colonel Hend's jump position and it is clear he has traveled to the Dantooine system. The time has come for the Colonel to be punished for his crimes against the Emperor.
		string_id message = new string_id (c_stringFile, "s_e1b07878");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I am ready and willing to make the traitor pay.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1f033c46");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 25);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasWonQuest3A (player, npc))
	{
		//-- NPC: Naval Command wants us to continue to assist the local command. Their assessment of the situation is positive, but they feel that inflicting further damage is necessary.
		string_id message = new string_id (c_stringFile, "s_9962665d");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like a briefing.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to take an assignment.
		boolean hasResponse1 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b2a924ab");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ac71da57");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 43);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasWonQuest3 (player, npc))
	{
		//-- NPC: Good you're back. We have discovered a serious problem. We managed to decipher the remainder of the message that you intercepted. It would seem that Colonel Hend was the one who was leaking the information to the insurgents.
		string_id message = new string_id (c_stringFile, "s_2ccef947");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What? And we assisted him on his escape.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_96db4fc7");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 29);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasFailedQuest3 (player, npc))
	{
		//-- NPC: What are you doing back here, pilot? I haven't received a report that you were successful in your mission.
		string_id message = new string_id (c_stringFile, "s_ff5a78b2");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I am sorry but I seem to be having some difficulties...
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f6d4476d");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 32);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isReadyForMission3 (player, npc))
	{
		//-- NPC: Good to see you, Flying Officer %NU. You've been making significant impact with the other branches of the Imperial High Command. You have certainly done the Inquisition proud. But the time has come for you to resume your duties in rooting out traitors.
		string_id message = new string_id (c_stringFile, "s_6309f032");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Point me in the proper direction and I will do as you command.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_51fe08f5");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 35);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isReadyForTraining (player, npc))
	{
		//-- NPC: Pilot, it is time for you to learn some new skills. It is important that you keep sharp and keep your skills at the top of their game in order to succeed as an Imperial pilot. Are you ready?
		string_id message = new string_id (c_stringFile, "s_883a28d9");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I am ready for some training.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c1bc23b4");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 41);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isReadyForDutyBlock1 (player, npc))
	{
		//-- NPC: Welcome back, Pilot. Things are rather calm for the moment, so you will be flying some duty missions.I have a briefing prepared. Or if you've already examined the briefing material, you can select a type of operation to begin.
		string_id message = new string_id (c_stringFile, "s_be89481");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like a briefing.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to take an assignment.
		boolean hasResponse1 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b2a924ab");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ac71da57");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 43);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasWonQuest2 (player, npc))
	{
		//-- NPC: We have received the transmission successfully and have begun the process of deciphering their code. There will be no error in the data this time to protect the traitor. Yet again, you have proven yourself an asset to the Inquisition.
		string_id message = new string_id (c_stringFile, "s_32701355");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Thank you, sir.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_987445c9");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 70);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasFailedQuest2 (player, npc))
	{
		//-- NPC: Pilot, you're supposed to be on your ship! Get up there and intercept that transmission immediately!
		string_id message = new string_id (c_stringFile, "s_c50d6541");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, sir!
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_143");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 77);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isReadyForMission2 (player, npc))
	{
		doAnimationAction (npc, "point_forward");

		//-- NPC: Get suited up, you're being scrambled immediately.
		string_id message = new string_id (c_stringFile, "s_7f95f0a5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, sir!
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_147");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 79);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasWonQuest1 (player, npc))
	{
		//-- NPC: What have you learned so far, %NU?
		string_id message = new string_id (c_stringFile, "s_f91e446d");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Transfer the information to Inquisitor Fa'Zoll.)
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1d5aba18");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 87);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_hasFailedQuest1 (player, npc))
	{
		//-- NPC: Have you managed to track down the information that I requested, Pilot?
		string_id message = new string_id (c_stringFile, "s_6e9c09c4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I've run into some trouble so far.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_29d0a2db");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 91);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition_isReadyForMission1 (player, npc))
	{
		doAnimationAction (npc, "salute1");

		//-- NPC: Ah, yes, excellent. You are ready to begin our work, then?
		string_id message = new string_id (c_stringFile, "s_1f0f791f");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I am.
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c8f39347");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 94);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
	{
		//-- NPC: Yes, how may I help you? Have you come to report a suspicious activity? You really should speak to my secretary. I only handle heresies and other critical cases.
		string_id message = new string_id (c_stringFile, "s_26cb713a");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What is the Imperial Inquisition?
		boolean hasResponse0 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What do you think of the Rebellion?
		boolean hasResponse1 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I've been asked to report to you for duty.
		boolean hasResponse2 = false;
		if (naboo_imperial_trainer_2_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6396820e");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3ce84ec1");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_696a6a00");

			utils.setScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId", 103);

			npcStartConversation (player, npc, "naboo_imperial_trainer_2", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "naboo_imperial_trainer_2")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

	if (branchId == 4 && naboo_imperial_trainer_2_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && naboo_imperial_trainer_2_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && naboo_imperial_trainer_2_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && naboo_imperial_trainer_2_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && naboo_imperial_trainer_2_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && naboo_imperial_trainer_2_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && naboo_imperial_trainer_2_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && naboo_imperial_trainer_2_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && naboo_imperial_trainer_2_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && naboo_imperial_trainer_2_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && naboo_imperial_trainer_2_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && naboo_imperial_trainer_2_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && naboo_imperial_trainer_2_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && naboo_imperial_trainer_2_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && naboo_imperial_trainer_2_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && naboo_imperial_trainer_2_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && naboo_imperial_trainer_2_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && naboo_imperial_trainer_2_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && naboo_imperial_trainer_2_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && naboo_imperial_trainer_2_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && naboo_imperial_trainer_2_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && naboo_imperial_trainer_2_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && naboo_imperial_trainer_2_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && naboo_imperial_trainer_2_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && naboo_imperial_trainer_2_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && naboo_imperial_trainer_2_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && naboo_imperial_trainer_2_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && naboo_imperial_trainer_2_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && naboo_imperial_trainer_2_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && naboo_imperial_trainer_2_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 44 && naboo_imperial_trainer_2_handleBranch44 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && naboo_imperial_trainer_2_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 46 && naboo_imperial_trainer_2_handleBranch46 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && naboo_imperial_trainer_2_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 48 && naboo_imperial_trainer_2_handleBranch48 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 50 && naboo_imperial_trainer_2_handleBranch50 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && naboo_imperial_trainer_2_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && naboo_imperial_trainer_2_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 56 && naboo_imperial_trainer_2_handleBranch56 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 57 && naboo_imperial_trainer_2_handleBranch57 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && naboo_imperial_trainer_2_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 60 && naboo_imperial_trainer_2_handleBranch60 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && naboo_imperial_trainer_2_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 63 && naboo_imperial_trainer_2_handleBranch63 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && naboo_imperial_trainer_2_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 66 && naboo_imperial_trainer_2_handleBranch66 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 67 && naboo_imperial_trainer_2_handleBranch67 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 70 && naboo_imperial_trainer_2_handleBranch70 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 71 && naboo_imperial_trainer_2_handleBranch71 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 72 && naboo_imperial_trainer_2_handleBranch72 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 73 && naboo_imperial_trainer_2_handleBranch73 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 74 && naboo_imperial_trainer_2_handleBranch74 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 75 && naboo_imperial_trainer_2_handleBranch75 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 76 && naboo_imperial_trainer_2_handleBranch76 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 77 && naboo_imperial_trainer_2_handleBranch77 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 79 && naboo_imperial_trainer_2_handleBranch79 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 80 && naboo_imperial_trainer_2_handleBranch80 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 81 && naboo_imperial_trainer_2_handleBranch81 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 82 && naboo_imperial_trainer_2_handleBranch82 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 83 && naboo_imperial_trainer_2_handleBranch83 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 84 && naboo_imperial_trainer_2_handleBranch84 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 85 && naboo_imperial_trainer_2_handleBranch85 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 87 && naboo_imperial_trainer_2_handleBranch87 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 88 && naboo_imperial_trainer_2_handleBranch88 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 89 && naboo_imperial_trainer_2_handleBranch89 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 91 && naboo_imperial_trainer_2_handleBranch91 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 92 && naboo_imperial_trainer_2_handleBranch92 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 94 && naboo_imperial_trainer_2_handleBranch94 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 95 && naboo_imperial_trainer_2_handleBranch95 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 96 && naboo_imperial_trainer_2_handleBranch96 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 97 && naboo_imperial_trainer_2_handleBranch97 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 98 && naboo_imperial_trainer_2_handleBranch98 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 99 && naboo_imperial_trainer_2_handleBranch99 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 100 && naboo_imperial_trainer_2_handleBranch100 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 101 && naboo_imperial_trainer_2_handleBranch101 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 103 && naboo_imperial_trainer_2_handleBranch103 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 104 && naboo_imperial_trainer_2_handleBranch104 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 105 && naboo_imperial_trainer_2_handleBranch105 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 106 && naboo_imperial_trainer_2_handleBranch106 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 107 && naboo_imperial_trainer_2_handleBranch107 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 108 && naboo_imperial_trainer_2_handleBranch108 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 109 && naboo_imperial_trainer_2_handleBranch109 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 110 && naboo_imperial_trainer_2_handleBranch110 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 111 && naboo_imperial_trainer_2_handleBranch111 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.naboo_imperial_trainer_2.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

