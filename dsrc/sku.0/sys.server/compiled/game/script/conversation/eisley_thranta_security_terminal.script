// ======================================================================
//
// eisley_thranta_security_terminal.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/eisley_thranta_security_terminal";

// ======================================================================
// Script Conditions
// ======================================================================

boolean eisley_thranta_security_terminal_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean eisley_thranta_security_terminal_condition_onVirusSecurity (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "tatooine_eisley_digital_infection_v2", "tat_eisley_infection_e2") ||
			groundquests.hasCompletedTask(player, "tatooine_eisley_digital_infection_v2", "tat_eisley_infection_e2"));

}

// ----------------------------------------------------------------------

boolean eisley_thranta_security_terminal_condition_onDrenSlice (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "tatooine_eisley_drensbounty_v2", "tat_eisley_drensbounty_e3") ||
			groundquests.hasCompletedTask(player, "tatooine_eisley_drensbounty_v2", "tat_eisley_drensbounty_e3"));
}

// ----------------------------------------------------------------------

boolean eisley_thranta_security_terminal_condition_onBrokSlice (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "tatooine_eisley_drensbounty_v2", "tat_eisley_drensbounty_e4") ||
			groundquests.hasCompletedTask(player, "tatooine_eisley_drensbounty_v2", "tat_eisley_drensbounty_e4"));

}

// ======================================================================
// Script Actions
// ======================================================================

void eisley_thranta_security_terminal_action_signalVirusSecurity (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_eisley_infection_e2");
}

// ----------------------------------------------------------------------

void eisley_thranta_security_terminal_action_signalDrenSlice (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_eisley_drensbounty_e3");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int eisley_thranta_security_terminal_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: WARNING!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Enter Password]
	if (response == "s_6")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Welcome to White Thranta Shipping's security network.
			string_id message = new string_id (c_stringFile, "s_8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Review Security Measures]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Review Personnel Records]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [High Security Files]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_28");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_58");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Enter False Password]
	if (response == "s_76")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Access Denied
			string_id message = new string_id (c_stringFile, "s_78");
			utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome to White Thranta Shipping's security network.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Review Security Measures]
	if (response == "s_10")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter security section you wish to review:
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Medical Facility Status]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Armory Status]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Repair Bay Status]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse3 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_17");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Review Personnel Records]
	if (response == "s_28")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter personnel ID code:
			string_id message = new string_id (c_stringFile, "s_30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [8747 - Brok Ziam]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition_onBrokSlice (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [1644 - Durr Roddack]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition_onDrenSlice (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Enter false ID code]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse3 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_35");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_56");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [High Security Files]
	if (response == "s_58")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter restricted topic for review:
			string_id message = new string_id (c_stringFile, "s_60");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Genetic Research Data]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Orbital Traffic Surveillance]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please enter security section you wish to review:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Medical Facility Status]
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			eisley_thranta_security_terminal_action_signalVirusSecurity (player, npc);

			//-- NPC: Medical Facility Status:
			string_id message = new string_id (c_stringFile, "s_44");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Return to Previous Menu]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Armory Status]
	if (response == "s_17")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Security Armory Status:
			string_id message = new string_id (c_stringFile, "s_19");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Return to Previous Menu]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Repair Bay Status]
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Repair Bay Status:
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Return to Previous Menu]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_32");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Main Menu]
	if (response == "s_34")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Welcome to White Thranta Shipping's security network.
			string_id message = new string_id (c_stringFile, "s_8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Review Security Measures]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Review Personnel Records]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [High Security Files]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_28");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_58");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Medical Facility Status:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Return to Previous Menu]
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter security section you wish to review:
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Medical Facility Status]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Armory Status]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Repair Bay Status]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse3 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_17");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Security Armory Status:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Return to Previous Menu]
	if (response == "s_31")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter security section you wish to review:
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Medical Facility Status]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Armory Status]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Repair Bay Status]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse3 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_17");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Repair Bay Status:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Return to Previous Menu]
	if (response == "s_32")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter security section you wish to review:
			string_id message = new string_id (c_stringFile, "s_12");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Medical Facility Status]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Armory Status]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Repair Bay Status]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse3 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_17");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please enter personnel ID code:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [8747 - Brok Ziam]
	if (response == "s_35")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Brok Ziam:
			string_id message = new string_id (c_stringFile, "s_37");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Return to Previous Menu]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_39");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [1644 - Durr Roddack]
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			eisley_thranta_security_terminal_action_signalDrenSlice (player, npc);

			//-- NPC: Durr Roddack:
			string_id message = new string_id (c_stringFile, "s_46");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Return to Previous Menu]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Enter false ID code]
	if (response == "s_50")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Personnel ID not found.
			string_id message = new string_id (c_stringFile, "s_52");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Return to Previous Menu]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_54");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Main Menu]
	if (response == "s_56")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Welcome to White Thranta Shipping's security network.
			string_id message = new string_id (c_stringFile, "s_8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Review Security Measures]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Review Personnel Records]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [High Security Files]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_28");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_58");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Brok Ziam:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Return to Previous Menu]
	if (response == "s_39")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter personnel ID code:
			string_id message = new string_id (c_stringFile, "s_30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [8747 - Brok Ziam]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition_onBrokSlice (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [1644 - Durr Roddack]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition_onDrenSlice (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Enter false ID code]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse3 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_35");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_56");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Durr Roddack:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Return to Previous Menu]
	if (response == "s_48")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter personnel ID code:
			string_id message = new string_id (c_stringFile, "s_30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [8747 - Brok Ziam]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition_onBrokSlice (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [1644 - Durr Roddack]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition_onDrenSlice (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Enter false ID code]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse3 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_35");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_56");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Personnel ID not found.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Return to Previous Menu]
	if (response == "s_54")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter personnel ID code:
			string_id message = new string_id (c_stringFile, "s_30");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [8747 - Brok Ziam]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition_onBrokSlice (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [1644 - Durr Roddack]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition_onDrenSlice (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Enter false ID code]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse3 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_35");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_56");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please enter restricted topic for review:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Genetic Research Data]
	if (response == "s_62")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Latest progress entry from research staff:
			string_id message = new string_id (c_stringFile, "s_64");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Return to Previous Menu]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_66");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Orbital Traffic Surveillance]
	if (response == "s_68")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Latest progress entry from surveillance staff:
			string_id message = new string_id (c_stringFile, "s_70");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Return to Previous Menu]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_72");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 13);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Main Menu]
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Welcome to White Thranta Shipping's security network.
			string_id message = new string_id (c_stringFile, "s_8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Review Security Measures]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Review Personnel Records]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [High Security Files]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_10");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_28");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_58");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Latest progress entry from research staff:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Return to Previous Menu]
	if (response == "s_66")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter restricted topic for review:
			string_id message = new string_id (c_stringFile, "s_60");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Genetic Research Data]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Orbital Traffic Surveillance]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int eisley_thranta_security_terminal_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Latest progress entry from surveillance staff:

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Return to Previous Menu]
	if (response == "s_72")
	{
		//-- [NOTE] 
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please enter restricted topic for review:
			string_id message = new string_id (c_stringFile, "s_60");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [Genetic Research Data]
			boolean hasResponse0 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: [Orbital Traffic Surveillance]
			boolean hasResponse1 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: [Main Menu]
			boolean hasResponse2 = false;
			if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isTangible (self)) || (isPlayer (self)))
		detachScript(self, "conversation.eisley_thranta_security_terminal");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.eisley_thranta_security_terminal");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
	{
		//-- NPC: WARNING!
		string_id message = new string_id (c_stringFile, "s_4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: [Enter Password]
		boolean hasResponse0 = false;
		if (eisley_thranta_security_terminal_condition_onVirusSecurity (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: [Enter False Password]
		boolean hasResponse1 = false;
		if (eisley_thranta_security_terminal_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_76");

			utils.setScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId", 1);

			npcStartConversation (player, npc, "eisley_thranta_security_terminal", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "eisley_thranta_security_terminal")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

	if (branchId == 1 && eisley_thranta_security_terminal_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && eisley_thranta_security_terminal_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && eisley_thranta_security_terminal_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && eisley_thranta_security_terminal_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && eisley_thranta_security_terminal_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && eisley_thranta_security_terminal_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && eisley_thranta_security_terminal_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && eisley_thranta_security_terminal_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && eisley_thranta_security_terminal_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && eisley_thranta_security_terminal_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && eisley_thranta_security_terminal_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && eisley_thranta_security_terminal_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && eisley_thranta_security_terminal_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.eisley_thranta_security_terminal.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

