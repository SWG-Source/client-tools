// ======================================================================
//
// ep3_trandoshan_boshaz_zssik_01.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.space_quest;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/ep3_trandoshan_boshaz_zssik_01";

// ======================================================================
// Script Conditions
// ======================================================================

boolean ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean ep3_trandoshan_boshaz_zssik_01_condition_isOnSpaceMission (obj_id player, obj_id npc)
{
	return space_quest.hasQuest( player );
}

// ----------------------------------------------------------------------

boolean ep3_trandoshan_boshaz_zssik_01_condition_hasCompletedSpaceMission (obj_id player, obj_id npc)
{
	return space_quest.hasReceivedReward( player, "escort", "ep3_trando_boshaz_zssik_01" );
}

// ----------------------------------------------------------------------

boolean ep3_trandoshan_boshaz_zssik_01_condition_hasFailedSpaceMission (obj_id player, obj_id npc)
{
	if ( space_quest.hasFailedQuest( player, "escort", "ep3_trando_boshaz_zssik_01" ) ||
		space_quest.hasAbortedQuest( player, "escort", "ep3_trando_boshaz_zssik_01" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean ep3_trandoshan_boshaz_zssik_01_condition_hasWonSpaceMission (obj_id player, obj_id npc)
{
	if( space_quest.hasWonQuest( player, "escort", "ep3_trando_boshaz_zssik_01" ) &&
	!space_quest.hasReceivedReward( player, "escort", "ep3_trando_boshaz_zssik_01" ))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean ep3_trandoshan_boshaz_zssik_01_condition_isOnGroundQuest (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "ep3_trando_boshaz_zssik_02");
}

// ----------------------------------------------------------------------

boolean ep3_trandoshan_boshaz_zssik_01_condition_isOnTask01 (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ep3_trando_boshaz_zssik_02", "talkToBoshaz");
}

// ----------------------------------------------------------------------

boolean ep3_trandoshan_boshaz_zssik_01_condition_hasCompletedGroundQuest (obj_id player, obj_id npc)
{
	return groundquests.hasCompletedQuest(player, "ep3_trando_boshaz_zssik_02");
}

// ======================================================================
// Script Actions
// ======================================================================

void ep3_trandoshan_boshaz_zssik_01_action_giveSpaceMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "escort", "ep3_trando_boshaz_zssik_01" );
}

// ----------------------------------------------------------------------

void ep3_trandoshan_boshaz_zssik_01_action_giveRewardSpaceMission (obj_id player, obj_id npc)
{
	space_quest.giveReward( player, "escort", "ep3_trando_boshaz_zssik_01", 5000);
}

// ----------------------------------------------------------------------

void ep3_trandoshan_boshaz_zssik_01_action_giveGroundQuest01 (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_trando_boshaz_zssik_02");
}

// ----------------------------------------------------------------------

void ep3_trandoshan_boshaz_zssik_01_action_doSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "rewardBoshaz");
	groundquests.grantQuest(player, "ep3_trando_boshaz_transfer");
}

// ----------------------------------------------------------------------

void ep3_trandoshan_boshaz_zssik_01_action_clearGroundQuest (obj_id player, obj_id npc)
{
	groundquests.clearQuest(player, "ep3_trando_boshaz_zssik_02");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int ep3_trandoshan_boshaz_zssik_01_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How did your meeting with Chawroo go? I trust you managed to gain his...trust.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Chawroo is setting up the meeting.
	if (response == "s_603")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			//-- NPC: Good. I would have been hoping that was the case. So, just between you and me...did those Blackscale curs suffer before you eliminated them?
			string_id message = new string_id (c_stringFile, "s_605");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They have been dealt with if that is what you mean.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_607");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good. I would have been hoping that was the case. So, just between you and me...did those Blackscale curs suffer before you eliminated them?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They have been dealt with if that is what you mean.
	if (response == "s_607")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slump_head");

			ep3_trandoshan_boshaz_zssik_01_action_doSignal (player, npc);

			//-- NPC: Ah...and I was hoping for some gory details. You take all the fun out of things. Oh well, as long as you have accomplished the goals set out before you. And as promised here is payment for the completion of your first task for us. For your next task you need to go talk to Dakar. He is expecting you. Good bye, fledgling.
			string_id message = new string_id (c_stringFile, "s_609");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So, did you managed to rescue Chawroo and get him to set up a meeting between you and the resistance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet. But I am working on it.
	if (response == "s_613")
	{
		ep3_trandoshan_boshaz_zssik_01_action_clearGroundQuest (player, npc);

		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			ep3_trandoshan_boshaz_zssik_01_action_giveGroundQuest01 (player, npc);

			//-- NPC: You had better hurry. If the Blackscale spend to much time with him, I am afraid that Chawroo will be in no condition to set up anything.
			string_id message = new string_id (c_stringFile, "s_615");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So, my young fledgling has returned. Have you changed your mind about working for the Zssik?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more about what I need to do.
	if (response == "s_619")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It is true that I set you up. But you must realize it was simply a test to see if you had the right stuff. My clan has great need of a skilled warrior who is not in any way connected to us. I think that you will fit the bill quite nicely. Besides, we are not a poor clan and we always handsomely reward those who help us.
			string_id message = new string_id (c_stringFile, "s_631");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright. What is the deal?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_633");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet, maybe later.
	if (response == "s_621")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Suit yourself. If you happen to change your mind, you know where to find me.
			string_id message = new string_id (c_stringFile, "s_711");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am surprised. I honestly did not believe that you had even an inkling of skill in you. When I reported your position to the Wookiee resistance I was sure that you would be quickly destroyed. You have proven to be quite a little scrapper.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You told the Wookiee resistance about the escort?
	if (response == "s_625")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			ep3_trandoshan_boshaz_zssik_01_action_giveRewardSpaceMission (player, npc);

			//-- NPC: Of course I did. I couldn't very well test your prowess if you were not attacked. Allow me to compensate you for any damage that the attack might have caused to your vessel. My clan has no need for anyone who cannot handle themselves under fire. If you are interested I have a job for you to do. 
			string_id message = new string_id (c_stringFile, "s_627");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So why should I trust you after you set me up?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No thanks. I don't work for people who set me up.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_629");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_709");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course I did. I couldn't very well test your prowess if you were not attacked. Allow me to compensate you for any damage that the attack might have caused to your vessel. My clan has no need for anyone who cannot handle themselves under fire. If you are interested I have a job for you to do. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So why should I trust you after you set me up?
	if (response == "s_629")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It is true that I set you up. But you must realize it was simply a test to see if you had the right stuff. My clan has great need of a skilled warrior who is not in any way connected to us. I think that you will fit the bill quite nicely. Besides, we are not a poor clan and we always handsomely reward those who help us.
			string_id message = new string_id (c_stringFile, "s_631");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright. What is the deal?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_633");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks. I don't work for people who set me up.
	if (response == "s_709")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Suit yourself. If you happen to change your mind, you know where to find me.
			string_id message = new string_id (c_stringFile, "s_711");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It is true that I set you up. But you must realize it was simply a test to see if you had the right stuff. My clan has great need of a skilled warrior who is not in any way connected to us. I think that you will fit the bill quite nicely. Besides, we are not a poor clan and we always handsomely reward those who help us.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright. What is the deal?
	if (response == "s_633")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: I am a member of the Zssik clan. Perhaps you have heard of my clan? Or perhaps you haven't? It matters very little either way. All you really need to know is that we are tired of having to be subservient to those Blackscale weaklings. It is time for us to take our rightful place as rulers of the Avatar.
			string_id message = new string_id (c_stringFile, "s_635");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Blackscale weaklings?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Rulers of the Avatar?
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_637");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_703");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am a member of the Zssik clan. Perhaps you have heard of my clan? Or perhaps you haven't? It matters very little either way. All you really need to know is that we are tired of having to be subservient to those Blackscale weaklings. It is time for us to take our rightful place as rulers of the Avatar.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Blackscale weaklings?
	if (response == "s_637")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Blackscales are currently the dominant clan here on Kashyyyk. To be the dominate clan means that you are the first and last word on all operations here on Kashyyyk. They alone deal with the Empire directly, leaving the rest of us dealing with them in order to make any credits. They can take the prime choice of Wookiee captives for themselves, and have absolute control of the Avatar Space Platform.
			string_id message = new string_id (c_stringFile, "s_639");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the Avatar Space Platform?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_641");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Rulers of the Avatar?
	if (response == "s_703")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Blackscale are its current master. They are the dominant clan on Kashyyyk which gives them virtual control over all aspects of the slave trade. Needless to say this has allowed them to take credits that should belong to us and keep them for themselves.
			string_id message = new string_id (c_stringFile, "s_705");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the Avatar Space Platform?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_707");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Blackscales are currently the dominant clan here on Kashyyyk. To be the dominate clan means that you are the first and last word on all operations here on Kashyyyk. They alone deal with the Empire directly, leaving the rest of us dealing with them in order to make any credits. They can take the prime choice of Wookiee captives for themselves, and have absolute control of the Avatar Space Platform.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the Avatar Space Platform?
	if (response == "s_641")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: That is the heart of the operation here on Kashyyyk. All Wookiee who are captured here on the planet will be eventually brought there. There they are retrained and domesticated before being sold to the Empire. Since no other clan is allowed on the Avatar, the Blackscale are able to dictate terms of payment to every other clan working on Kashyyyk. Their time as rulers of the Trandoshans has come to an end.
			string_id message = new string_id (c_stringFile, "s_643");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why is it called the Avatar?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I take it you do not like the Blackscale.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_645");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That is the heart of the operation here on Kashyyyk. All Wookiee who are captured here on the planet will be eventually brought there. There they are retrained and domesticated before being sold to the Empire. Since no other clan is allowed on the Avatar, the Blackscale are able to dictate terms of payment to every other clan working on Kashyyyk. Their time as rulers of the Trandoshans has come to an end.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why is it called the Avatar?
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Because it is the Dosh destiny to rule over the weak. What better name to give the symbol of Trandoshan power over these primitives? Simply put, the space station is the Avatar of Dosh on Kashyyyk. And it sickens me that the Blackscale weaklings have control over something so powerful.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I take it that you do not like the Blackscale.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I take it you do not like the Blackscale.
	if (response == "s_645")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Blackscale are a bunch of cowards and weaklings. They have become corrupt, and even worse brazen about their corruption. We have heard that the Empire would not be upset if the Blackscale were removed from power. We Zssik have waited and fought a very long time to get into the position to dispose of the Blackscale.
			string_id message = new string_id (c_stringFile, "s_647");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And what is my role in all of this?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_649");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Because it is the Dosh destiny to rule over the weak. What better name to give the symbol of Trandoshan power over these primitives? Simply put, the space station is the Avatar of Dosh on Kashyyyk. And it sickens me that the Blackscale weaklings have control over something so powerful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I take it that you do not like the Blackscale.
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The Blackscale are a bunch of cowards and weaklings. They have become corrupt, and even worse brazen about their corruption. We have heard that the Empire would not be upset if the Blackscale were removed from power. We Zssik have waited and fought a very long time to get into the position to dispose of the Blackscale.
			string_id message = new string_id (c_stringFile, "s_647");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And what is my role in all of this?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_649");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Blackscale are a bunch of cowards and weaklings. They have become corrupt, and even worse brazen about their corruption. We have heard that the Empire would not be upset if the Blackscale were removed from power. We Zssik have waited and fought a very long time to get into the position to dispose of the Blackscale.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And what is my role in all of this?
	if (response == "s_649")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			//-- NPC: You, my young friend, will be the instrument of their ultimate downfall. We do not fear the Blackscale but an open confrontation with them would result in a weakening of both sides. Lesser clans might get some ideas...dangerous ideas. You are an outsider and no one will be able to connect you with us should things go badly.
			string_id message = new string_id (c_stringFile, "s_651");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: In other words, I am expendable.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds like you are looking for a patsy.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_653");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_699");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You, my young friend, will be the instrument of their ultimate downfall. We do not fear the Blackscale but an open confrontation with them would result in a weakening of both sides. Lesser clans might get some ideas...dangerous ideas. You are an outsider and no one will be able to connect you with us should things go badly.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: In other words, I am expendable.
	if (response == "s_653")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Oh yes. You are quite expendable. But the Zssik would rather want you to survive and accomplish the tasks we need done. A dead agent is a useless tool.
			string_id message = new string_id (c_stringFile, "s_655");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will take the job. What do I need to do?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks but no thanks. This sounds too dangerous.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_657");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_695");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like you are looking for a patsy.
	if (response == "s_699")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A patsy knows nothing. But a good agent must know what they are doing in order to be effective. We are not worried that you will talk if captured. The Blackscale do not take prisoners. If you fail they will probably eat you...for your sake I hope you are dead before they begin.
			string_id message = new string_id (c_stringFile, "s_701");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will take the job. What do I need to do?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks but no thanks. This sounds too dangerous.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_657");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_695");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh yes. You are quite expendable. But the Zssik would rather want you to survive and accomplish the tasks we need done. A dead agent is a useless tool.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will take the job. What do I need to do?
	if (response == "s_657")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: In order for us to take our rightful place as chief clan on Kashyyyk, the Blackscales must fall. We will disrupt their operations, kill their leaders, and embarrass them in the eyes of the Empire. But first...first we need an ally. An ally that can and is quite willing to cause all sorts of trouble for the Blackscale. We need to enlist the aid of the Wookiee resistance.
			string_id message = new string_id (c_stringFile, "s_659");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Wookiee resistance will never work for you.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_661");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks but no thanks. This sounds too dangerous.
	if (response == "s_695")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slit_throat");

			//-- NPC: Very well. But remember this before you leave...if you tell anyone about what has been said here you will find out why the wrath of the Zssik is legendary even amongst other Trandoshans.
			string_id message = new string_id (c_stringFile, "s_697");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: In order for us to take our rightful place as chief clan on Kashyyyk, the Blackscales must fall. We will disrupt their operations, kill their leaders, and embarrass them in the eyes of the Empire. But first...first we need an ally. An ally that can and is quite willing to cause all sorts of trouble for the Blackscale. We need to enlist the aid of the Wookiee resistance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Wookiee resistance will never work for you.
	if (response == "s_661")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			//-- NPC: Of that I have no doubt. But...perhaps, they will work for you and in doing so, unwittingly work for us. 
			string_id message = new string_id (c_stringFile, "s_663");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How am I supposed to get the resistance to work for me?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_665");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of that I have no doubt. But...perhaps, they will work for you and in doing so, unwittingly work for us. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How am I supposed to get the resistance to work for me?
	if (response == "s_665")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: By giving them what they want most...access codes to the Avatar Space Platform. You see the Wookiees all consider that to be the most evil place and the focal point of Trandoshan power on the planet. The resistance will do anything to be able to stage an attack on the platform. But they have no way to penetrate its defenses...you will supply them with that way.
			string_id message = new string_id (c_stringFile, "s_667");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How am I supposed to do that?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_669");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: By giving them what they want most...access codes to the Avatar Space Platform. You see the Wookiees all consider that to be the most evil place and the focal point of Trandoshan power on the planet. The resistance will do anything to be able to stage an attack on the platform. But they have no way to penetrate its defenses...you will supply them with that way.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How am I supposed to do that?
	if (response == "s_669")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: All in good time, my young friend. First you must establish contact with the resistance. Fortunately, the means to this end have simply fallen into our laps. Have you ever heard of a life debt?
			string_id message = new string_id (c_stringFile, "s_671");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I have.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, I haven't.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_673");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_689");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: All in good time, my young friend. First you must establish contact with the resistance. Fortunately, the means to this end have simply fallen into our laps. Have you ever heard of a life debt?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I have.
	if (response == "s_673")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Good...good. A short time ago one of the Blackscale slaver patrols was attacked by a group of Wookiee freedom fighters under the command of a Wookiee named Chawroo. The Blackscales have been turning this world upside down trying to find him...but you see we found him first. He is in hiding not far from here with his mate.
			string_id message = new string_id (c_stringFile, "s_675");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we going to do with him?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_677");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I haven't.
	if (response == "s_689")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A life debt is part of the Wookiees foolish sense of honor. If a Wookiee is saved from death by another being they basically believe that their life belongs to that other person. It is so strong and complete that I have seen Wookiees walk of their own free will into a slave shuttle after giving a life debt to a slaver who saved their life. Do you understand now?
			string_id message = new string_id (c_stringFile, "s_691");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. I understand.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_693");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good...good. A short time ago one of the Blackscale slaver patrols was attacked by a group of Wookiee freedom fighters under the command of a Wookiee named Chawroo. The Blackscales have been turning this world upside down trying to find him...but you see we found him first. He is in hiding not far from here with his mate.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we going to do with him?
	if (response == "s_677")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It is so simple. We are going to tell the Blackscale where he is. The Blackscale will send a patrol out to put Chawroo and his mate to a slow, painful death. But just in the nick of time you will arrive and rescue those poor Wookiees from certain doom. Naturally, the Wookiee's screwed up sense of honor will force him into a life debt to you.
			string_id message = new string_id (c_stringFile, "s_679");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And then what do I do with him?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_681");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It is so simple. We are going to tell the Blackscale where he is. The Blackscale will send a patrol out to put Chawroo and his mate to a slow, painful death. But just in the nick of time you will arrive and rescue those poor Wookiees from certain doom. Naturally, the Wookiee's screwed up sense of honor will force him into a life debt to you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And then what do I do with him?
	if (response == "s_681")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Oh, all we want him to do for now is to vouch for you and set up a meeting between you and the resistance leadership. Once a meeting has been set up he can leave word about the when and where with Orooroo.
			string_id message = new string_id (c_stringFile, "s_683");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is Orooroo?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_685");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, all we want him to do for now is to vouch for you and set up a meeting between you and the resistance leadership. Once a meeting has been set up he can leave word about the when and where with Orooroo.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who is Orooroo?
	if (response == "s_685")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "belly_laugh");

			//-- NPC: Oh, I am quite certain that Chawroo will know who Orooroo is. For now all you really should focus on is to rescue Chawroo from the Blackscale. Remember, kill all the Blackscale attackers and then get Chawroo to set up a meeting for you. Come back to me when you are finished.
			string_id message = new string_id (c_stringFile, "s_687");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, where can I find Chawroo?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_155");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, I am quite certain that Chawroo will know who Orooroo is. For now all you really should focus on is to rescue Chawroo from the Blackscale. Remember, kill all the Blackscale attackers and then get Chawroo to set up a meeting for you. Come back to me when you are finished.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, where can I find Chawroo?
	if (response == "s_155")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			ep3_trandoshan_boshaz_zssik_01_action_giveGroundQuest01 (player, npc);

			//-- NPC: Chawroo is hiding to the northwest of here. He has found a hiding place along a path, near where the bolotaur's like to sun themselves, across from a waterfall in that area. You shouldn't have any trouble finding it if you are half as good as I think you are. Now get moving. The Blackscale will not wait long to strike.
			string_id message = new string_id (c_stringFile, "s_157");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A life debt is part of the Wookiees foolish sense of honor. If a Wookiee is saved from death by another being they basically believe that their life belongs to that other person. It is so strong and complete that I have seen Wookiees walk of their own free will into a slave shuttle after giving a life debt to a slaver who saved their life. Do you understand now?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I understand.
	if (response == "s_693")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Good...good. A short time ago one of the Blackscale slaver patrols was attacked by a group of Wookiee freedom fighters under the command of a Wookiee named Chawroo. The Blackscales have been turning this world upside down trying to find him...but you see we found him first. He is in hiding not far from here with his mate.
			string_id message = new string_id (c_stringFile, "s_675");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we going to do with him?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_677");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A patsy knows nothing. But a good agent must know what they are doing in order to be effective. We are not worried that you will talk if captured. The Blackscale do not take prisoners. If you fail they will probably eat you...for your sake I hope you are dead before they begin.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will take the job. What do I need to do?
	if (response == "s_657")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: In order for us to take our rightful place as chief clan on Kashyyyk, the Blackscales must fall. We will disrupt their operations, kill their leaders, and embarrass them in the eyes of the Empire. But first...first we need an ally. An ally that can and is quite willing to cause all sorts of trouble for the Blackscale. We need to enlist the aid of the Wookiee resistance.
			string_id message = new string_id (c_stringFile, "s_659");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Wookiee resistance will never work for you.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_661");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks but no thanks. This sounds too dangerous.
	if (response == "s_695")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slit_throat");

			//-- NPC: Very well. But remember this before you leave...if you tell anyone about what has been said here you will find out why the wrath of the Zssik is legendary even amongst other Trandoshans.
			string_id message = new string_id (c_stringFile, "s_697");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Blackscale are its current master. They are the dominant clan on Kashyyyk which gives them virtual control over all aspects of the slave trade. Needless to say this has allowed them to take credits that should belong to us and keep them for themselves.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the Avatar Space Platform?
	if (response == "s_707")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: That is the heart of the operation here on Kashyyyk. All Wookiee who are captured here on the planet will be eventually brought there. There they are retrained and domesticated before being sold to the Empire. Since no other clan is allowed on the Avatar, the Blackscale are able to dictate terms of payment to every other clan working on Kashyyyk. Their time as rulers of the Trandoshans has come to an end.
			string_id message = new string_id (c_stringFile, "s_643");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why is it called the Avatar?
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I take it you do not like the Blackscale.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_645");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, is the fledgling nothing more then a braggart or did you simply have a bad day?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I just had an off day. I can do your task.
	if (response == "s_715")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			ep3_trandoshan_boshaz_zssik_01_action_giveSpaceMission (player, npc);

			//-- NPC: Really? Well it is time to put your money where your mouth is. I have another ship that is on its way to the Avatar. Maybe you can get this one through. Or maybe you are just wasting my time and the lives of my pilots. Well...what are you waiting for? Get going.
			string_id message = new string_id (c_stringFile, "s_717");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I cannot accomplish what you asked of me.
	if (response == "s_719")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Not surprising. I never thought that you would.
			string_id message = new string_id (c_stringFile, "s_721");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Greetings, offworlder. You have the look of someone who is in search of something. Whatever it is you seek, you should look elsewhere. I am not interested in holding the hand of a fledgling who has no business on this dangerous world.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I think you have me mistaken for someone else.
	if (response == "s_727")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Oh, so the fledgling thinks it has teeth. How interesting. I am not certain that you are worth my time but I am looking for someone who can perform some small...tasks for me. Perhaps you would wish to fill that role?
			string_id message = new string_id (c_stringFile, "s_729");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Keep talking scaly.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No thanks. I am not interested.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_731");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_741");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, so the fledgling thinks it has teeth. How interesting. I am not certain that you are worth my time but I am looking for someone who can perform some small...tasks for me. Perhaps you would wish to fill that role?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Keep talking scaly.
	if (response == "s_731")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It is a simple task. So simple that I think even a young whelp such as yourself should be able to perform it. You see, I have a slave ship that needs to be protected on its way to the Avatar Space Platform. If you believe that you have the skills, perhaps you would be so kind to make sure it reaches its destination in one piece.
			string_id message = new string_id (c_stringFile, "s_733");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: For me, that isn't even a challenge.
			boolean hasResponse0 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, I don't think I want to do that right now.
			boolean hasResponse1 = false;
			if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_735");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_739");

				utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks. I am not interested.
	if (response == "s_741")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Of course, fledgling. I would not want you to put yourself in any danger. Perhaps you will find work dancing in one of the local cantinas.
			string_id message = new string_id (c_stringFile, "s_743");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_trandoshan_boshaz_zssik_01_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It is a simple task. So simple that I think even a young whelp such as yourself should be able to perform it. You see, I have a slave ship that needs to be protected on its way to the Avatar Space Platform. If you believe that you have the skills, perhaps you would be so kind to make sure it reaches its destination in one piece.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For me, that isn't even a challenge.
	if (response == "s_735")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			ep3_trandoshan_boshaz_zssik_01_action_giveSpaceMission (player, npc);

			//-- NPC: Brave words. Very well...meet my ship in orbit above Kashyyyk. Escort it until it reaches the Avatar zone of control. When and if you finish, report back to me and perhaps I will have some more work for you to do.
			string_id message = new string_id (c_stringFile, "s_737");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I don't think I want to do that right now.
	if (response == "s_739")
	{
		//-- [NOTE] 
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Of course, fledgling. I would not want you to put yourself in any danger. Perhaps you will find work dancing in one of the local cantinas.
			string_id message = new string_id (c_stringFile, "s_743");
			utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.ep3_trandoshan_boshaz_zssik_01");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_INTERESTING );
	setName(self, "Boshaz");
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_INTERESTING );
	setName(self, "Boshaz");
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);
	faceTo(self, player);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.ep3_trandoshan_boshaz_zssik_01");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (ep3_trandoshan_boshaz_zssik_01_condition_hasCompletedGroundQuest (player, npc))
	{
		doAnimationAction (npc, "wave_on_dismissing");

		//-- NPC: I have already told you everything that I can about the situation. You need to speak with Dakar now.
		string_id message = new string_id (c_stringFile, "s_599");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_trandoshan_boshaz_zssik_01_condition_isOnTask01 (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: How did your meeting with Chawroo go? I trust you managed to gain his...trust.
		string_id message = new string_id (c_stringFile, "s_601");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Chawroo is setting up the meeting.
		boolean hasResponse0 = false;
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_603");

			utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 2);

			npcStartConversation (player, npc, "ep3_trandoshan_boshaz_zssik_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_trandoshan_boshaz_zssik_01_condition_isOnGroundQuest (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: So, did you managed to rescue Chawroo and get him to set up a meeting between you and the resistance.
		string_id message = new string_id (c_stringFile, "s_611");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Not yet. But I am working on it.
		boolean hasResponse0 = false;
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_613");

			utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 5);

			npcStartConversation (player, npc, "ep3_trandoshan_boshaz_zssik_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_trandoshan_boshaz_zssik_01_condition_hasCompletedSpaceMission (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: So, my young fledgling has returned. Have you changed your mind about working for the Zssik?
		string_id message = new string_id (c_stringFile, "s_617");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Tell me more about what I need to do.
		boolean hasResponse0 = false;
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not yet, maybe later.
		boolean hasResponse1 = false;
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_619");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_621");

			utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 7);

			npcStartConversation (player, npc, "ep3_trandoshan_boshaz_zssik_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_trandoshan_boshaz_zssik_01_condition_hasWonSpaceMission (player, npc))
	{
		doAnimationAction (npc, "nod_head_once");

		//-- NPC: I am surprised. I honestly did not believe that you had even an inkling of skill in you. When I reported your position to the Wookiee resistance I was sure that you would be quickly destroyed. You have proven to be quite a little scrapper.
		string_id message = new string_id (c_stringFile, "s_623");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: You told the Wookiee resistance about the escort?
		boolean hasResponse0 = false;
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_625");

			utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 8);

			npcStartConversation (player, npc, "ep3_trandoshan_boshaz_zssik_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_trandoshan_boshaz_zssik_01_condition_hasFailedSpaceMission (player, npc))
	{
		doAnimationAction (npc, "laugh_cackle");

		//-- NPC: Ah, is the fledgling nothing more then a braggart or did you simply have a bad day?
		string_id message = new string_id (c_stringFile, "s_713");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I just had an off day. I can do your task.
		boolean hasResponse0 = false;
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I cannot accomplish what you asked of me.
		boolean hasResponse1 = false;
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_715");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_719");

			utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 32);

			npcStartConversation (player, npc, "ep3_trandoshan_boshaz_zssik_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_trandoshan_boshaz_zssik_01_condition_isOnSpaceMission (player, npc))
	{
		doAnimationAction (npc, "wave_on_dismissing");

		//-- NPC: You are a busy little fledgling. I have no time for someone who already has work to be done. I do have a job that is in need of a good star pilot. Finish up with what you are tasked with and then...perhaps...I will talk with you.
		string_id message = new string_id (c_stringFile, "s_723");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "yawn");

		//-- NPC: Greetings, offworlder. You have the look of someone who is in search of something. Whatever it is you seek, you should look elsewhere. I am not interested in holding the hand of a fledgling who has no business on this dangerous world.
		string_id message = new string_id (c_stringFile, "s_725");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I think you have me mistaken for someone else.
		boolean hasResponse0 = false;
		if (ep3_trandoshan_boshaz_zssik_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_727");

			utils.setScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId", 36);

			npcStartConversation (player, npc, "ep3_trandoshan_boshaz_zssik_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "ep3_trandoshan_boshaz_zssik_01")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

	if (branchId == 2 && ep3_trandoshan_boshaz_zssik_01_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && ep3_trandoshan_boshaz_zssik_01_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && ep3_trandoshan_boshaz_zssik_01_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && ep3_trandoshan_boshaz_zssik_01_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && ep3_trandoshan_boshaz_zssik_01_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && ep3_trandoshan_boshaz_zssik_01_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && ep3_trandoshan_boshaz_zssik_01_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && ep3_trandoshan_boshaz_zssik_01_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && ep3_trandoshan_boshaz_zssik_01_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && ep3_trandoshan_boshaz_zssik_01_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && ep3_trandoshan_boshaz_zssik_01_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && ep3_trandoshan_boshaz_zssik_01_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && ep3_trandoshan_boshaz_zssik_01_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && ep3_trandoshan_boshaz_zssik_01_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && ep3_trandoshan_boshaz_zssik_01_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && ep3_trandoshan_boshaz_zssik_01_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && ep3_trandoshan_boshaz_zssik_01_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && ep3_trandoshan_boshaz_zssik_01_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && ep3_trandoshan_boshaz_zssik_01_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && ep3_trandoshan_boshaz_zssik_01_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && ep3_trandoshan_boshaz_zssik_01_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && ep3_trandoshan_boshaz_zssik_01_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && ep3_trandoshan_boshaz_zssik_01_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && ep3_trandoshan_boshaz_zssik_01_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && ep3_trandoshan_boshaz_zssik_01_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && ep3_trandoshan_boshaz_zssik_01_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && ep3_trandoshan_boshaz_zssik_01_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && ep3_trandoshan_boshaz_zssik_01_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && ep3_trandoshan_boshaz_zssik_01_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.ep3_trandoshan_boshaz_zssik_01.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

