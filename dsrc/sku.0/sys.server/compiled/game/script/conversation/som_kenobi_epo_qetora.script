// ======================================================================
//
// som_kenobi_epo_qetora.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.prose;
include library.static_item;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/som_kenobi_epo_qetora";

// ======================================================================
// Script Conditions
// ======================================================================

boolean som_kenobi_epo_qetora_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean som_kenobi_epo_qetora_condition_onQuest (obj_id player, obj_id npc)
{
	if (groundquests.isQuestActive(player, "som_kenobi_historian_1")||
		groundquests.isQuestActive(player, "som_kenobi_historian_smuggler"))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean som_kenobi_epo_qetora_condition_isSmuggler (obj_id player, obj_id npc)
{
	return hasSkill(player, "class_smuggler_phase1_novice");
}

// ----------------------------------------------------------------------

boolean som_kenobi_epo_qetora_condition_haveDisk (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive(player, "som_kenobi_historian_1", "success")||
		groundquests.isTaskActive(player, "som_kenobi_historian_smuggler", "success")||
		groundquests.isTaskActive(player, "som_kenobi_historian_1", "done")||
		groundquests.isTaskActive(player, "som_kenobi_historian_smuggler", "done"))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean som_kenobi_epo_qetora_condition_finishedQuestAll (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest(player, "som_kenobi_historian_1")||
		groundquests.hasCompletedQuest(player, "som_kenobi_historian_smuggler"))
		return groundquests.hasCompletedQuest(player, "som_kenobi_historian_2");
	else
		return false;
	
}

// ----------------------------------------------------------------------

boolean som_kenobi_epo_qetora_condition_finishedQuest1 (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest(player, "som_kenobi_historian_1")||
		groundquests.hasCompletedQuest(player, "som_kenobi_historian_smuggler"))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean som_kenobi_epo_qetora_condition_onQuest2 (obj_id player, obj_id npc)
{
	return (groundquests.isQuestActive(player, "som_kenobi_historian_2"));
}

// ----------------------------------------------------------------------

boolean som_kenobi_epo_qetora_condition_rewardQuest2 (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "som_kenobi_historian_2", "retrievedPieces");
}

// ======================================================================
// Script Actions
// ======================================================================

void som_kenobi_epo_qetora_action_giveQuestSmuggler (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "som_kenobi_historian_smuggler");
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_giveQuest (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "som_kenobi_historian_1");
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_reward (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "successDone");
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_giveQuest2 (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "som_kenobi_historian_2");
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_reward2 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "piecesRetrieved");
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_rewardBattle (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "battlePicked");
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_rewardRecon (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "reconPicked");
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_rewardAssault (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "assaultPicked");
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_rewardRifle (obj_id player, obj_id npc)
{
	obj_id item = static_item.createNewItemFunction("weapon_tow_rifle_03_02", player);
	string_id name = new string_id ( "static_item_n", "weapon_tow_rifle_03_02" );
	prose_package pp = new prose_package ();
	pp = prose.setStringId(pp, new string_id( "quest/ground/system_message", "placed_in_inventory" ));
	pp = prose.setTO(pp, name);
	sendQuestSystemMessage(player, pp);
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_rewardCarbine (obj_id player, obj_id npc)
{
	obj_id item = static_item.createNewItemFunction("weapon_tow_carbine_03_01", player);
	string_id name = new string_id ( "static_item_n", "weapon_tow_carbine_03_01" );
	prose_package pp = new prose_package ();
	pp = prose.setStringId(pp, new string_id( "quest/ground/system_message", "placed_in_inventory" ));
	pp = prose.setTO(pp, name);
	sendQuestSystemMessage(player, pp);
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_rewardPistol (obj_id player, obj_id npc)
{
	obj_id item = static_item.createNewItemFunction("weapon_tow_pistol_03_01", player);
	string_id name = new string_id ( "static_item_n", "weapon_tow_pistol_03_01" );
	prose_package pp = new prose_package ();
	pp = prose.setStringId(pp, new string_id( "quest/ground/system_message", "placed_in_inventory" ));
	pp = prose.setTO(pp, name);
	sendQuestSystemMessage(player, pp);
}

// ----------------------------------------------------------------------

void som_kenobi_epo_qetora_action_finishQuest1 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "completed");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int som_kenobi_epo_qetora_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: %TU! Please tell me you've had some luck. I've had nothing but misfortune lately.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sorry to hear that. Yes, I do believe I have all the pieces.
	if (response == "s_106")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: You know how to make an old man happy, my friend. Let me have a look. Yes, this is perfect. That's all of them. Very interesting. I will get you a copy of the translated text. Give me a minute.
			string_id message = new string_id (c_stringFile, "s_108");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks! I am curious as to what it will say.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_134");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, I care. Here's your tablets. Where's my reward?
	if (response == "s_107")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Your tough attitude really doesn't suit you, %TU. I know you better than that. Let me have a look at these. Ah yes, beautiful. They fit together perfectly. Let me get you a copy of the translated text. Give me a minute.
			string_id message = new string_id (c_stringFile, "s_109");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That better not be my only reward...
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_124");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 11);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You know how to make an old man happy, my friend. Let me have a look. Yes, this is perfect. That's all of them. Very interesting. I will get you a copy of the translated text. Give me a minute.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks! I am curious as to what it will say.
	if (response == "s_134")
	{
		doAnimationAction (player, "thumb_up");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: I bet, after all the trouble you've gone through. Alright, the translated text is done as well. Another job well done, my friend. I wouldn't have gotten anywhere without you and I'm most grateful. Now for your reward, I've come across a stash of weapons and was wondering about your preference: a rifle, carbine, or pistol?
			string_id message = new string_id (c_stringFile, "s_138");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Rifle sounds good.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Carbine sounds good.
			boolean hasResponse1 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Pistol sounds good.
			boolean hasResponse2 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_262");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_270");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_294");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I bet, after all the trouble you've gone through. Alright, the translated text is done as well. Another job well done, my friend. I wouldn't have gotten anywhere without you and I'm most grateful. Now for your reward, I've come across a stash of weapons and was wondering about your preference: a rifle, carbine, or pistol?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Rifle sounds good.
	if (response == "s_262")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.
			string_id message = new string_id (c_stringFile, "s_264");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you for everything. It's been very interesting.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_266");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Carbine sounds good.
	if (response == "s_270")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.
			string_id message = new string_id (c_stringFile, "s_272");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you for everything. It's been very interesting.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_274");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Pistol sounds good.
	if (response == "s_294")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.
			string_id message = new string_id (c_stringFile, "s_296");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you for everything. It's been very interesting.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_298");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for everything. It's been very interesting.
	if (response == "s_266")
	{
		som_kenobi_epo_qetora_action_reward2 (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			doAnimationAction (player, "nod_head_once");

			som_kenobi_epo_qetora_action_rewardRifle (player, npc);

			//-- NPC: I'm glad to hear that, %TU. Well, I'm afraid this concludes our business, for now at least. Take well care of yourself and thanks again.
			string_id message = new string_id (c_stringFile, "s_268");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for everything. It's been very interesting.
	if (response == "s_274")
	{
		som_kenobi_epo_qetora_action_reward2 (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			doAnimationAction (player, "nod_head_once");

			som_kenobi_epo_qetora_action_rewardCarbine (player, npc);

			//-- NPC: I'm glad to hear that, %TU. Well, I'm afraid this concludes our business, for now at least. Take well care of yourself and thanks again.
			string_id message = new string_id (c_stringFile, "s_276");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you for everything. It's been very interesting.
	if (response == "s_298")
	{
		som_kenobi_epo_qetora_action_reward2 (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			doAnimationAction (player, "nod_head_once");

			som_kenobi_epo_qetora_action_rewardPistol (player, npc);

			//-- NPC: I'm glad to hear that, %TU. Well, I'm afraid this concludes our business, for now at least. Take well care of yourself and thanks again.
			string_id message = new string_id (c_stringFile, "s_300");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your tough attitude really doesn't suit you, %TU. I know you better than that. Let me have a look at these. Ah yes, beautiful. They fit together perfectly. Let me get you a copy of the translated text. Give me a minute.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That better not be my only reward...
	if (response == "s_124")
	{
		doAnimationAction (player, "tap_foot");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Of course not, silly. I've come across a stash of weapons and just need to know what you would prefer: a rifle, carbine, or pistol?
			string_id message = new string_id (c_stringFile, "s_126");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Rifle sounds good.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Carbine sounds good.
			boolean hasResponse1 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Pistol sounds good.
			boolean hasResponse2 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_278");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_286");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_243");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course not, silly. I've come across a stash of weapons and just need to know what you would prefer: a rifle, carbine, or pistol?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Rifle sounds good.
	if (response == "s_278")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.
			string_id message = new string_id (c_stringFile, "s_280");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You could say that again. So I guess our business is concluded?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_282");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Carbine sounds good.
	if (response == "s_286")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.
			string_id message = new string_id (c_stringFile, "s_288");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You could say that again. So I guess our business is concluded?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_290");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Pistol sounds good.
	if (response == "s_243")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.
			string_id message = new string_id (c_stringFile, "s_256");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You could say that again. So I guess our business is concluded?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_258");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You could say that again. So I guess our business is concluded?
	if (response == "s_282")
	{
		som_kenobi_epo_qetora_action_reward2 (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			doAnimationAction (player, "nod_head_once");

			som_kenobi_epo_qetora_action_rewardRifle (player, npc);

			//-- NPC: Yes indeed, for now at least. Take care of yourself, %TU, and thanks again.
			string_id message = new string_id (c_stringFile, "s_284");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You could say that again. So I guess our business is concluded?
	if (response == "s_290")
	{
		som_kenobi_epo_qetora_action_reward2 (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			doAnimationAction (player, "nod_head_once");

			som_kenobi_epo_qetora_action_rewardCarbine (player, npc);

			//-- NPC: Yes indeed, for now at least. Take care of yourself, %TU, and thanks again.
			string_id message = new string_id (c_stringFile, "s_292");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not a problem. Here you go. I wouldn't have gotten anywhere without you and I'm most grateful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You could say that again. So I guess our business is concluded?
	if (response == "s_258")
	{
		som_kenobi_epo_qetora_action_reward2 (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			doAnimationAction (player, "nod_head_once");

			som_kenobi_epo_qetora_action_rewardPistol (player, npc);

			//-- NPC: Yes indeed, for now at least. Take care of yourself, %TU, and thanks again.
			string_id message = new string_id (c_stringFile, "s_260");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How can I help you, my friend?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just making sure there was nothing you forgot to tell me?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			doAnimationAction (player, "goodbye");

			//-- NPC: No, I think I covered everything. Be careful and good luck!
			string_id message = new string_id (c_stringFile, "s_86");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Great to see you again, %TU! I could use your assistance again if you have some time to spare?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was hoping you would say that. How can I help, Epo?
	if (response == "s_66")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: While you were up retrieving the data log from that old bunker for me, I was out doing a bit of exploring of my own. To the northeast of us, but south of the bunker you ventured to, I came across some old buildings of unknown origin. Among them I found this, a piece of an old tablet with some writing on it. Unfortunately, it's been shattered and this is but a small piece of the puzzle so to speak.
			string_id message = new string_id (c_stringFile, "s_72");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Did you try to find the other pieces?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_73");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why else would I be here? Your charming personality?
	if (response == "s_67")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Umm...anyway, while you were up retrieving the data log from that old bunker for me, I was out doing a bit of exploring of my own. To the northeast of us, but south of the bunker you ventured to, I came across some old buildings of unknown origin. Among them I found this, a piece of an old tablet with some writing on it. Unfortunately, it's been shattered and this is but a small piece of the puzzle so to speak.
			string_id message = new string_id (c_stringFile, "s_88");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Didn't occur to you to look for the other pieces while you were there?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sorry, Epo. Just stopped by to say hi. Have a lot to do.
	if (response == "s_68")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: Well, that's typical. If you free up some time later, make sure to stop by. I could really use your help, %TU.
			string_id message = new string_id (c_stringFile, "s_71");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For you, I have about 10 seconds...and they just ran out.
	if (response == "s_69")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Haha, always such a charmer, %TU. Well, you take care then! Stop by later, will you? Please?
			string_id message = new string_id (c_stringFile, "s_81");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: While you were up retrieving the data log from that old bunker for me, I was out doing a bit of exploring of my own. To the northeast of us, but south of the bunker you ventured to, I came across some old buildings of unknown origin. Among them I found this, a piece of an old tablet with some writing on it. Unfortunately, it's been shattered and this is but a small piece of the puzzle so to speak.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Did you try to find the other pieces?
	if (response == "s_73")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Indeed I did, but as I was looking around I noticed I was no longer alone. I spotted some skulking figures moving around the area, also apparently searching for something. I didn't like the way they looked and sneaked off before they could spot me. It's my hope that you could return there and see if you can find the other pieces of the tablet?
			string_id message = new string_id (c_stringFile, "s_74");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Probably just some scavengers. Shouldn't be a problem.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't have the time to venture out there right now, I'm afraid.
			boolean hasResponse1 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_75");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed I did, but as I was looking around I noticed I was no longer alone. I spotted some skulking figures moving around the area, also apparently searching for something. I didn't like the way they looked and sneaked off before they could spot me. It's my hope that you could return there and see if you can find the other pieces of the tablet?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Probably just some scavengers. Shouldn't be a problem.
	if (response == "s_75")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			//-- NPC: For you? I'm sure not. Please be careful still, though. They might be more than meets the eye.
			string_id message = new string_id (c_stringFile, "s_78");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't have the time to venture out there right now, I'm afraid.
	if (response == "s_76")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: That is too bad, my friend. Maybe you'll get some time later? If so, please come back.
			string_id message = new string_id (c_stringFile, "s_77");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: For you? I'm sure not. Please be careful still, though. They might be more than meets the eye.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't worry. I will be.
	if (response == "s_79")
	{
		doAnimationAction (player, "nod");

		som_kenobi_epo_qetora_action_giveQuest2 (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			//-- NPC: Good, I hope you can find the pieces. If the rest are about the same size as this one, my guess is there will be four more of them.
			string_id message = new string_id (c_stringFile, "s_80");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Umm...anyway, while you were up retrieving the data log from that old bunker for me, I was out doing a bit of exploring of my own. To the northeast of us, but south of the bunker you ventured to, I came across some old buildings of unknown origin. Among them I found this, a piece of an old tablet with some writing on it. Unfortunately, it's been shattered and this is but a small piece of the puzzle so to speak.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Didn't occur to you to look for the other pieces while you were there?
	if (response == "s_90")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Indeed it did, but as I was looking around, I noticed I was no longer alone. I spotted some skulking figures moving around the area, also apparently searching for something. I didn't like the way they looked and sneaked off before they could spot me. It's my hope that you could return there and see if you can find the other pieces of the tablet?
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Probably just some filthy scavengers. I'll handle them.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Right now, I have some more important things to do. Maybe later.
			boolean hasResponse1 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed it did, but as I was looking around, I noticed I was no longer alone. I spotted some skulking figures moving around the area, also apparently searching for something. I didn't like the way they looked and sneaked off before they could spot me. It's my hope that you could return there and see if you can find the other pieces of the tablet?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Probably just some filthy scavengers. I'll handle them.
	if (response == "s_94")
	{
		doAnimationAction (player, "point_to_self");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm sure you will... Don't forget about the tablet pieces, though. If the rest are about the same size as this one, my guess is there will be four more of them.
			string_id message = new string_id (c_stringFile, "s_96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll get them. Don't worry.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Right now, I have some more important things to do. Maybe later.
	if (response == "s_102")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: That is too bad, my friend. Maybe you'll get some time later? If so, please come back.
			string_id message = new string_id (c_stringFile, "s_104");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm sure you will... Don't forget about the tablet pieces, though. If the rest are about the same size as this one, my guess is there will be four more of them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll get them. Don't worry.
	if (response == "s_98")
	{
		doAnimationAction (player, "nod");

		som_kenobi_epo_qetora_action_giveQuest2 (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			//-- NPC: You haven't disappointed me so far, %TU. I'm not worried. Still, good luck out there!
			string_id message = new string_id (c_stringFile, "s_100");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: %TU, you are back! How did everything go?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: There wasn't much data left intact but I got what I could.
	if (response == "s_89")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Let me have a look at that please. Ohh, this is very interesting. I wish there was more, but it's all that was left, you say? Too bad, but at least this gives me some clues and shows that I'm on the right track. I'm most grateful, my friend.
			string_id message = new string_id (c_stringFile, "s_93");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't mention it. I'm glad it's of some help.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Piece of cake. I got out what was left in that dump.
	if (response == "s_122")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Let me have a look at that please. Ohh, this is very interesting. I wish there was more, but it's all that was left, you say? Too bad, but at least this gives me some clues and shows that I'm on the right track. I'm most grateful, my friend.
			string_id message = new string_id (c_stringFile, "s_125");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That's great. Now there was talk of a reward?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_128");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Let me have a look at that please. Ohh, this is very interesting. I wish there was more, but it's all that was left, you say? Too bad, but at least this gives me some clues and shows that I'm on the right track. I'm most grateful, my friend.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't mention it. I'm glad it's of some help.
	if (response == "s_97")
	{
		doAnimationAction (player, "refuse_offer_affection");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: It very much is. Now for your reward. Let's see...where did I put that thing... You can keep this disk, by the way. I've already copied it down on my datapad. Ah, here it is! This helmet should be useful for someone in your line of work. What type of armor would you like it to be modified for?
			string_id message = new string_id (c_stringFile, "s_101");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Assault, please.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Battle, please.
			boolean hasResponse1 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Recon, please.
			boolean hasResponse2 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_210");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_182");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It very much is. Now for your reward. Let's see...where did I put that thing... You can keep this disk, by the way. I've already copied it down on my datapad. Ah, here it is! This helmet should be useful for someone in your line of work. What type of armor would you like it to be modified for?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Assault, please.
	if (response == "s_210")
	{
		som_kenobi_epo_qetora_action_reward (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Certainly. Here we go. All yours!.
			string_id message = new string_id (c_stringFile, "s_212");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Very nice! It was a pleasure working with you, Epo.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_214");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Battle, please.
	if (response == "s_182")
	{
		som_kenobi_epo_qetora_action_reward (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Certainly. Here we go. All yours!.
			string_id message = new string_id (c_stringFile, "s_190");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Very nice! It was a pleasure working with you, Epo.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_192");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Recon, please.
	if (response == "s_183")
	{
		som_kenobi_epo_qetora_action_reward (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Certainly. Here we go. All yours!.
			string_id message = new string_id (c_stringFile, "s_196");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Very nice! It was a pleasure working with you, Epo.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_198");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 41);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certainly. Here we go. All yours!.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very nice! It was a pleasure working with you, Epo.
	if (response == "s_214")
	{
		doAnimationAction (player, "thumb_up");

		som_kenobi_epo_qetora_action_rewardAssault (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			som_kenobi_epo_qetora_action_finishQuest1 (player, npc);

			//-- NPC: The pleasure was all mine, %TU. If you find yourself looking for something else to do, come back and see me. I've got another lead I need help with.
			string_id message = new string_id (c_stringFile, "s_216");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certainly. Here we go. All yours!.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very nice! It was a pleasure working with you, Epo.
	if (response == "s_192")
	{
		doAnimationAction (player, "thumb_up");

		som_kenobi_epo_qetora_action_rewardBattle (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			som_kenobi_epo_qetora_action_finishQuest1 (player, npc);

			//-- NPC: The pleasure was all mine, %TU. If you find yourself looking for something else to do, come back and see me. I've got another lead I need help with.
			string_id message = new string_id (c_stringFile, "s_194");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certainly. Here we go. All yours!.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very nice! It was a pleasure working with you, Epo.
	if (response == "s_198")
	{
		doAnimationAction (player, "thumb_up");

		som_kenobi_epo_qetora_action_rewardRecon (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			som_kenobi_epo_qetora_action_finishQuest1 (player, npc);

			//-- NPC: The pleasure was all mine, %TU. If you find yourself looking for something else to do, come back and see me. I've got another lead I need help with.
			string_id message = new string_id (c_stringFile, "s_200");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Let me have a look at that please. Ohh, this is very interesting. I wish there was more, but it's all that was left, you say? Too bad, but at least this gives me some clues and shows that I'm on the right track. I'm most grateful, my friend.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's great. Now there was talk of a reward?
	if (response == "s_128")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			som_kenobi_epo_qetora_action_reward (player, npc);

			//-- NPC: Of course, of course. You can keep this disk, by the way. I've already copied it down on my datapad. Now for your reward. Let's see...where did I put that thing... Ah, here it is! This helmet should be useful for someone in your line of work. What type of armor would you like it to be modified for?
			string_id message = new string_id (c_stringFile, "s_130");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Assault.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Battle.
			boolean hasResponse1 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Recon.
			boolean hasResponse2 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_218");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_226");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_234");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch44 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course, of course. You can keep this disk, by the way. I've already copied it down on my datapad. Now for your reward. Let's see...where did I put that thing... Ah, here it is! This helmet should be useful for someone in your line of work. What type of armor would you like it to be modified for?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Assault.
	if (response == "s_218")
	{
		som_kenobi_epo_qetora_action_reward (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Certainly. Here we go. All yours!.
			string_id message = new string_id (c_stringFile, "s_220");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: This could come in handy. Well, good doing business with you.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_222");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Battle.
	if (response == "s_226")
	{
		som_kenobi_epo_qetora_action_reward (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Certainly. Here we go. All yours!.
			string_id message = new string_id (c_stringFile, "s_228");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: This could come in handy. Well, good doing business with you.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_230");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Recon.
	if (response == "s_234")
	{
		som_kenobi_epo_qetora_action_reward (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod");

			//-- NPC: Certainly. Here we go. All yours!.
			string_id message = new string_id (c_stringFile, "s_236");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: This could come in handy. Well, good doing business with you.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_238");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 49);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certainly. Here we go. All yours!.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: This could come in handy. Well, good doing business with you.
	if (response == "s_222")
	{
		doAnimationAction (player, "nod");

		som_kenobi_epo_qetora_action_rewardAssault (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			som_kenobi_epo_qetora_action_finishQuest1 (player, npc);

			//-- NPC: The pleasure was all mine, %TU. If you find yourself looking for something else to do, come back and see me. I've got another lead I need help with.
			string_id message = new string_id (c_stringFile, "s_224");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certainly. Here we go. All yours!.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: This could come in handy. Well, good doing business with you.
	if (response == "s_230")
	{
		doAnimationAction (player, "nod");

		som_kenobi_epo_qetora_action_rewardBattle (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			som_kenobi_epo_qetora_action_finishQuest1 (player, npc);

			//-- NPC: The pleasure was all mine, %TU. If you find yourself looking for something else to do, come back and see me. I've got another lead I need help with.
			string_id message = new string_id (c_stringFile, "s_232");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch49 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certainly. Here we go. All yours!.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: This could come in handy. Well, good doing business with you.
	if (response == "s_238")
	{
		doAnimationAction (player, "nod");

		som_kenobi_epo_qetora_action_rewardRecon (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			som_kenobi_epo_qetora_action_finishQuest1 (player, npc);

			//-- NPC: The pleasure was all mine, %TU. If you find yourself looking for something else to do, come back and see me. I've got another lead I need help with.
			string_id message = new string_id (c_stringFile, "s_240");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch51 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How can I help, my friend?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just making sure there was nothing you forgot to tell me?
	if (response == "s_148")
	{
		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			doAnimationAction (player, "goodbye");

			//-- NPC: No, I think I covered everything. Be careful and good luck!
			string_id message = new string_id (c_stringFile, "s_150");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch53 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, hello there. You look like you know your way around dangerous situations. Maybe you could help me out?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I might be able to. What do you need help with?
	if (response == "s_154")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "bow5");

			//-- NPC: Very kind, my friend. I am Epo Qetora...yes, the famed Twi'lek historian. I'm here trying to find out the history of this...rock. After scouting around and talking to some of its inhabitants, it's obvious that civilizations have been present here for thousands of years. Strangely enough though, apart from the last few hundred years, there are no records at all of who was here or where they are now.
			string_id message = new string_id (c_stringFile, "s_156");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Really? That's very...interesting.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_158");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sure of it, but if I want to is a different story.
	if (response == "s_180")
	{
		doAnimationAction (player, "pose_proudly");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "thumbs_up");

			//-- NPC: A confident one, eh? I like it! I am Epo Qetora...yes, the famed Twi'lek historian. I'm here trying to find out the history of this...rock. After scouting around and talking to some of its inhabitants, it's obvious that civilizations have been present here for thousands of years. Strangely enough though, apart from the last few hundred years, there are no records at all of who was here or where they are now.
			string_id message = new string_id (c_stringFile, "s_184");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Do I really look like I'm interested in this stuff?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_186");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I think you're mistaken, friend. I'm no fighter.
	if (response == "s_219")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "apologize");

			//-- NPC: My apologies for making such hasty assumptions. Have a splendid day!
			string_id message = new string_id (c_stringFile, "s_223");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Drop dead, trunk head.
	if (response == "s_227")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "taken_aback");

			//-- NPC: How rude! You...you rude person!
			string_id message = new string_id (c_stringFile, "s_231");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very kind, my friend. I am Epo Qetora...yes, the famed Twi'lek historian. I'm here trying to find out the history of this...rock. After scouting around and talking to some of its inhabitants, it's obvious that civilizations have been present here for thousands of years. Strangely enough though, apart from the last few hundred years, there are no records at all of who was here or where they are now.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Really? That's very...interesting.
	if (response == "s_158")
	{
		doAnimationAction (player, "yawn");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: I agree! Now I think I've come across a way to find out more, though. In the northeast part of this continent, an old facility has been discovered. I went there to see if I could find anything interesting, but it almost cost me my life! The inside of it was inhabited by ferocious animals and old droids that had gone haywire. This is where I need your help, my friend.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That could actually be something I'm interested in.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm sorry...something just came up. I've got to go.
			boolean hasResponse1 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_162");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 55);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch55 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I agree! Now I think I've come across a way to find out more, though. In the northeast part of this continent, an old facility has been discovered. I went there to see if I could find anything interesting, but it almost cost me my life! The inside of it was inhabited by ferocious animals and old droids that had gone haywire. This is where I need your help, my friend.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That could actually be something I'm interested in.
	if (response == "s_162")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition_isSmuggler (player, npc))
		{
			doAnimationAction (npc, "clap_rousing");

			//-- NPC: Excellent! Apparently, it belonged to the Old Republic and they should have kept detailed logs of events on the planet. What I need from you is to enter this old facility and see if their computer logs are still intact. If they are, download whatever information you can from it and get it back to me. If the information is useful, I will give a reward fit for a king!
			string_id message = new string_id (c_stringFile, "s_164");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good. How do I get there?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_166");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "clap_rousing");

			//-- NPC: Good! Apparently, it belonged to the Old Republic and they should have kept detailed logs of events on the planet. What I need from you is to enter this old facility and see if their computer logs are still intact. If they are, download whatever information you can from it and get it back to me. If the information is useful, I will give a reward fit for a king!
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good. How do I get there?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sorry...something just came up. I've got to go.
	if (response == "s_176")
	{
		doAnimationAction (player, "check_wrist_device");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Oh well. That's typical. Maybe you could come back later? I'll be here. Come see me! Please...?
			string_id message = new string_id (c_stringFile, "s_178");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch56 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent! Apparently, it belonged to the Old Republic and they should have kept detailed logs of events on the planet. What I need from you is to enter this old facility and see if their computer logs are still intact. If they are, download whatever information you can from it and get it back to me. If the information is useful, I will give a reward fit for a king!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good. How do I get there?
	if (response == "s_166")
	{
		doAnimationAction (player, "nod");

		som_kenobi_epo_qetora_action_giveQuestSmuggler (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: It's all the way up in the northeast corner of this continent. Good luck and be careful!
			string_id message = new string_id (c_stringFile, "s_168");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good! Apparently, it belonged to the Old Republic and they should have kept detailed logs of events on the planet. What I need from you is to enter this old facility and see if their computer logs are still intact. If they are, download whatever information you can from it and get it back to me. If the information is useful, I will give a reward fit for a king!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good. How do I get there?
	if (response == "s_172")
	{
		doAnimationAction (player, "nod");

		som_kenobi_epo_qetora_action_giveQuest (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: It's all the way up in the northeast corner of this continent. Good luck and be careful!
			string_id message = new string_id (c_stringFile, "s_174");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A confident one, eh? I like it! I am Epo Qetora...yes, the famed Twi'lek historian. I'm here trying to find out the history of this...rock. After scouting around and talking to some of its inhabitants, it's obvious that civilizations have been present here for thousands of years. Strangely enough though, apart from the last few hundred years, there are no records at all of who was here or where they are now.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do I really look like I'm interested in this stuff?
	if (response == "s_186")
	{
		doAnimationAction (player, "yawn");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "apologize");

			//-- NPC: Oh, I forgot that not everyone shares my passion. Just hear me out though, please. I think I've come across a way to find out more. In the northeast part of this continent, an old facility has been discovered. I went there to see if I could find anything interesting, but it almost cost me my life! The inside of it was inhabited by ferocious animals and old droids that had gone haywire. This is where I need your help.
			string_id message = new string_id (c_stringFile, "s_188");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That could actually be something I'm interested in.
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I really don't have time for this. Find someone else.
			boolean hasResponse1 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_191");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_211");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, I forgot that not everyone shares my passion. Just hear me out though, please. I think I've come across a way to find out more. In the northeast part of this continent, an old facility has been discovered. I went there to see if I could find anything interesting, but it almost cost me my life! The inside of it was inhabited by ferocious animals and old droids that had gone haywire. This is where I need your help.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That could actually be something I'm interested in.
	if (response == "s_191")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition_isSmuggler (player, npc))
		{
			doAnimationAction (npc, "clap_rousing");

			//-- NPC: Excellent! Apparently, it belonged to the Old Republic and they should have kept detailed logs of events on the planet. What I need from you is to enter this old facility and see if their computer logs are still intact. If they are, download whatever information you can from it and get it back to me. If the information is useful, I will give a reward fit for a king!
			string_id message = new string_id (c_stringFile, "s_195");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How fitting. How do I get there?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_199");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "clap_rousing");

			//-- NPC: Good! Apparently, it belonged to the Old Republic and they should have kept detailed logs of events on the planet. What I need from you is to enter this old facility and see if their computer logs are still intact. If they are, download whatever information you can from it and get it back to me. If the information is useful, I will give a reward fit for a king!
			string_id message = new string_id (c_stringFile, "s_204");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How fitting. How do I get there?
			boolean hasResponse0 = false;
			if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_206");

				utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I really don't have time for this. Find someone else.
	if (response == "s_211")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Oh well. That's typical. Maybe you could come back later, when you have time? I'll be here. Come see me! Please...?
			string_id message = new string_id (c_stringFile, "s_215");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch63 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent! Apparently, it belonged to the Old Republic and they should have kept detailed logs of events on the planet. What I need from you is to enter this old facility and see if their computer logs are still intact. If they are, download whatever information you can from it and get it back to me. If the information is useful, I will give a reward fit for a king!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How fitting. How do I get there?
	if (response == "s_199")
	{
		doAnimationAction (player, "nod");

		som_kenobi_epo_qetora_action_giveQuestSmuggler (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: It's all the way up in the northeast corner of this continent. Good luck and be careful!
			string_id message = new string_id (c_stringFile, "s_202");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_epo_qetora_handleBranch65 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good! Apparently, it belonged to the Old Republic and they should have kept detailed logs of events on the planet. What I need from you is to enter this old facility and see if their computer logs are still intact. If they are, download whatever information you can from it and get it back to me. If the information is useful, I will give a reward fit for a king!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How fitting. How do I get there?
	if (response == "s_206")
	{
		doAnimationAction (player, "pose_proudly");

		som_kenobi_epo_qetora_action_giveQuest (player, npc);

		//-- [NOTE] 
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: It's all the way up in the northeast corner of this continent. Good luck and be careful!
			string_id message = new string_id (c_stringFile, "s_208");
			utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.som_kenobi_epo_qetora");

	setCondition (self, CONDITION_INTERESTING);
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_INTERESTING);	
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.som_kenobi_epo_qetora");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (som_kenobi_epo_qetora_condition_finishedQuestAll (player, npc))
	{
		doAnimationAction (npc, "bow5");

		doAnimationAction (player, "greet");

		//-- NPC: Good to see you, %TU! Unfortunately, you've caught me at a bad time. I have some more leads I need to do research on. I will have to chat another day. Take care!
		string_id message = new string_id (c_stringFile, "s_55");
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_epo_qetora_condition_rewardQuest2 (player, npc))
	{
		doAnimationAction (npc, "bow5");

		doAnimationAction (player, "greet");

		//-- NPC: %TU! Please tell me you've had some luck. I've had nothing but misfortune lately.
		string_id message = new string_id (c_stringFile, "s_105");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sorry to hear that. Yes, I do believe I have all the pieces.
		boolean hasResponse0 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yeah, I care. Here's your tablets. Where's my reward?
		boolean hasResponse1 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_106");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_107");

			utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 2);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "som_kenobi_epo_qetora", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_epo_qetora_condition_onQuest2 (player, npc))
	{
		doAnimationAction (npc, "bow5");

		//-- NPC: How can I help you, my friend?
		string_id message = new string_id (c_stringFile, "s_82");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Just making sure there was nothing you forgot to tell me?
		boolean hasResponse0 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_84");

			utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 19);

			npcStartConversation (player, npc, "som_kenobi_epo_qetora", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_epo_qetora_condition_finishedQuest1 (player, npc))
	{
		doAnimationAction (npc, "bow5");

		doAnimationAction (player, "greet");

		//-- NPC: Great to see you again, %TU! I could use your assistance again if you have some time to spare?
		string_id message = new string_id (c_stringFile, "s_65");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I was hoping you would say that. How can I help, Epo?
		boolean hasResponse0 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Why else would I be here? Your charming personality?
		boolean hasResponse1 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm sorry, Epo. Just stopped by to say hi. Have a lot to do.
		boolean hasResponse2 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: For you, I have about 10 seconds...and they just ran out.
		boolean hasResponse3 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_66");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_67");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_68");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_69");

			utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 21);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "som_kenobi_epo_qetora", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_epo_qetora_condition_haveDisk (player, npc))
	{
		doAnimationAction (npc, "offer_affection");

		doAnimationAction (player, "greet");

		//-- NPC: %TU, you are back! How did everything go?
		string_id message = new string_id (c_stringFile, "s_85");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: There wasn't much data left intact but I got what I could.
		boolean hasResponse0 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Piece of cake. I got out what was left in that dump.
		boolean hasResponse1 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_89");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_122");

			utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 34);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "som_kenobi_epo_qetora", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_epo_qetora_condition_onQuest (player, npc))
	{
		doAnimationAction (npc, "bow5");

		//-- NPC: How can I help, my friend?
		string_id message = new string_id (c_stringFile, "s_146");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Just making sure there was nothing you forgot to tell me?
		boolean hasResponse0 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_148");

			utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 51);

			npcStartConversation (player, npc, "som_kenobi_epo_qetora", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "greet");

		doAnimationAction (player, "greet");

		//-- NPC: Well, hello there. You look like you know your way around dangerous situations. Maybe you could help me out?
		string_id message = new string_id (c_stringFile, "s_152");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I might be able to. What do you need help with?
		boolean hasResponse0 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm sure of it, but if I want to is a different story.
		boolean hasResponse1 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I think you're mistaken, friend. I'm no fighter.
		boolean hasResponse2 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Drop dead, trunk head.
		boolean hasResponse3 = false;
		if (som_kenobi_epo_qetora_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_154");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_180");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_219");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_227");

			utils.setScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId", 53);

			npcStartConversation (player, npc, "som_kenobi_epo_qetora", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "som_kenobi_epo_qetora")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

	if (branchId == 2 && som_kenobi_epo_qetora_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && som_kenobi_epo_qetora_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && som_kenobi_epo_qetora_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && som_kenobi_epo_qetora_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && som_kenobi_epo_qetora_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && som_kenobi_epo_qetora_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && som_kenobi_epo_qetora_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && som_kenobi_epo_qetora_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && som_kenobi_epo_qetora_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && som_kenobi_epo_qetora_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && som_kenobi_epo_qetora_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && som_kenobi_epo_qetora_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && som_kenobi_epo_qetora_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && som_kenobi_epo_qetora_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && som_kenobi_epo_qetora_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && som_kenobi_epo_qetora_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && som_kenobi_epo_qetora_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && som_kenobi_epo_qetora_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && som_kenobi_epo_qetora_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && som_kenobi_epo_qetora_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && som_kenobi_epo_qetora_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && som_kenobi_epo_qetora_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && som_kenobi_epo_qetora_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && som_kenobi_epo_qetora_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && som_kenobi_epo_qetora_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && som_kenobi_epo_qetora_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 44 && som_kenobi_epo_qetora_handleBranch44 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && som_kenobi_epo_qetora_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && som_kenobi_epo_qetora_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 49 && som_kenobi_epo_qetora_handleBranch49 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 51 && som_kenobi_epo_qetora_handleBranch51 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 53 && som_kenobi_epo_qetora_handleBranch53 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && som_kenobi_epo_qetora_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 55 && som_kenobi_epo_qetora_handleBranch55 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 56 && som_kenobi_epo_qetora_handleBranch56 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && som_kenobi_epo_qetora_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && som_kenobi_epo_qetora_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && som_kenobi_epo_qetora_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 63 && som_kenobi_epo_qetora_handleBranch63 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 65 && som_kenobi_epo_qetora_handleBranch65 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.som_kenobi_epo_qetora.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

