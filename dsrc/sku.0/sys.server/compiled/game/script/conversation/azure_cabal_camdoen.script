// ======================================================================
//
// azure_cabal_camdoen.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.combat;
include library.groundquests;
include library.money;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/azure_cabal_camdoen";

// ======================================================================
// Script Conditions
// ======================================================================

boolean azure_cabal_camdoen_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01", "ac_30_camdoen_01_dm2");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01", "ac_30_camdoen_01_dml1");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01", "ac_30_camdoen_01_dml2");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onTask_30ENF_Q1_Return (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01", "ac_30_camdoen_01_wfs_quest_complete");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onTask_30ENF_Repeater_Q1orQ2 (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30", "ac_repeater_loruna_enf_30_WFS_camdoen_01") ||
			groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30", "ac_repeater_loruna_enf_30_WFS_camdoen_02"));
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onQuest_30ENF_Q1 (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01"); 
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q2A (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02", "ac_30_camdoen_02_dm1");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q2B (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02", "ac_30_camdoen_02_dm2");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q2C (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02", "ac_30_camdoen_02_dm3") ||
			groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02", "ac_30_camdoen_02_dm3_retrieve"));
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02", "ac_30_camdoen_02_retrieve_01") ||
			groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02", "ac_30_camdoen_02_dml1"));
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01", "ac_30_camdoen_01_dm1");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onTask_30ENF_Q2_Return (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02", "ac_30_camdoen_02_wfs_quest_complete");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onQuest_30ENF_Q2 (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02"); 
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onTask_30ENF_Repeater_Q1 (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30", "ac_repeater_loruna_enf_30_WFS_camdoen_01");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onTask_30ENF_Repeater_Q2 (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30", "ac_repeater_loruna_enf_30_WFS_camdoen_02");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D_inv_use (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02", "ac_30_camdoen_02_retrieve_02");
}

// ----------------------------------------------------------------------

boolean azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01", "ac_30_camdoen_01_sacrifice_retrieve") ||
			groundquests.isTaskActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01", "ac_30_camdoen_01_sacrifice_enc"));
}

// ======================================================================
// Script Actions
// ======================================================================

void azure_cabal_camdoen_action_grant30Q1 (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.grantQuest(player, "quest/ac_repeater_loruna_enf_30_dnpc3_camdoen_01");
}

// ----------------------------------------------------------------------

void azure_cabal_camdoen_action_signal_30ENF_ClearRepeaterQuest (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "ac_repeater_loruna_enf_30_signal_clear_quest");
}

// ----------------------------------------------------------------------

void azure_cabal_camdoen_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ----------------------------------------------------------------------

void azure_cabal_camdoen_action_grant30Q2 (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.grantQuest(player, "quest/ac_repeater_loruna_enf_30_dnpc3_camdoen_02");
}

// ----------------------------------------------------------------------

void azure_cabal_camdoen_action_signal_30ENF_CompleteQuest (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "ac_quest_complete");
}

// ----------------------------------------------------------------------

void azure_cabal_camdoen_action_signal_30ENF_no_sacrifice (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.clearQuest(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01");
	groundquests.grantQuest(player, "quest/ac_ongoing_launchpad_loruna_repeater");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

string azure_cabal_camdoen_tokenTO_playerFirstName (obj_id player, obj_id npc)
{
    string name = getFirstName(player);
	return name;
}

// ----------------------------------------------------------------------

string azure_cabal_camdoen_tokenTO_playersProfession (obj_id player, obj_id npc)
{
	return new string();
}

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int azure_cabal_camdoen_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Loruna has sent you to me? Very good! She must trust you greatly to involve you in her dealings with the Mystril Shadow Guard.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I do.
	if (response == "s_170")
	{
		azure_cabal_camdoen_action_grant30Q1 (player, npc);

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good! I will upload all of the details. After you read your journal, contact me with questions you may have. Loruna trusts you with her deepest secrets, so I will honor her faith in you.
			string_id message = new string_id (c_stringFile, "s_172");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No.
	if (response == "s_171")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Better that you decide to betray Loruna before you learn her secrets, then. Go your way. If you wish to continue with the Azure Cabal, you must get her permission.
			string_id message = new string_id (c_stringFile, "s_181");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: On second thought, I will do it.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Fine. I want out.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_182");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Better that you decide to betray Loruna before you learn her secrets, then. Go your way. If you wish to continue with the Azure Cabal, you must get her permission.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: On second thought, I will do it.
	if (response == "s_182")
	{
		azure_cabal_camdoen_action_grant30Q1 (player, npc);

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good! I will upload all of the details. After you read your journal, contact me with questions you may have. Loruna trusts you with her deepest secrets, so I will honor her faith in you.
			string_id message = new string_id (c_stringFile, "s_172");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine. I want out.
	if (response == "s_183")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then just go.
			string_id message = new string_id (c_stringFile, "s_185");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Nikto are not bright and are easily manipulated. Are you willing to help me? If not, you must return to Loruna for further assignment.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll deal with the Nikto.
	if (response == "s_175")
	{
		azure_cabal_camdoen_action_grant30Q2 (player, npc);

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Together we can shape the Nikto blood-cult known as the M'dweshuui into a tool of enforcement for the Azure Cabal!
			string_id message = new string_id (c_stringFile, "s_180");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not interested.
	if (response == "s_176")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you sure? If you refuse this assignment, I will offer it not again. and you will have to contact Loruna directly for additional assignments.
			string_id message = new string_id (c_stringFile, "s_177");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: On second thought, I'll take it.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm not interested.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_178");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_179");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you sure? If you refuse this assignment, I will offer it not again. and you will have to contact Loruna directly for additional assignments.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: On second thought, I'll take it.
	if (response == "s_178")
	{
		azure_cabal_camdoen_action_grant30Q2 (player, npc);

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Together we can shape the Nikto blood-cult known as the M'dweshuui into a tool of enforcement for the Azure Cabal!
			string_id message = new string_id (c_stringFile, "s_180");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not interested.
	if (response == "s_179")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then go.
			string_id message = new string_id (c_stringFile, "s_186");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are doing very well.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Now what do I do?
	if (response == "s_196")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna will contact you when she has another assignment for you.
			string_id message = new string_id (c_stringFile, "s_198");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why are you still here? Do you need my help?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_193")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, just checking in.
	if (response == "s_194")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What would you know?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Mystril Shadow Guard.
	if (response == "s_216")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ah, yes. Loruna's sisters. They are a relentless mercenary group comprised entirely of Human females from the planet of Emberlene.
			string_id message = new string_id (c_stringFile, "s_218");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Wasn't Emberlene destroyed long ago?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Actually, I want more information about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_220");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_221");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Assignment specifics.
	if (response == "s_217")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I have learned that they are planning an attack on Loruna herself. We must stop it! Go to their safe house and thin their ranks.
			string_id message = new string_id (c_stringFile, "s_219");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How many do I kill?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I have other questions.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_228");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_227");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, yes. Loruna's sisters. They are a relentless mercenary group comprised entirely of Human females from the planet of Emberlene.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Wasn't Emberlene destroyed long ago?
	if (response == "s_220")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes, and most people believe the Empire was responsible for that. Loruna has reason to believe that this is not the case. Because of her beliefs, she left them long ago, but I know she misses them terribly.
			string_id message = new string_id (c_stringFile, "s_222");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we fight them, then?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'd like more information about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_223");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_224");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Actually, I want more information about my assignment.
	if (response == "s_221")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, and most people believe the Empire was responsible for that. Loruna has reason to believe that this is not the case. Because of her beliefs, she left them long ago, but I know she misses them terribly.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why do we fight them, then?
	if (response == "s_223")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna dearly wants to convince them of their error in blaming the Empire. Because of this, she would much rather talk than fight.
			string_id message = new string_id (c_stringFile, "s_225");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So what should I do?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_237");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like more information about my assignment.
	if (response == "s_224")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Loruna dearly wants to convince them of their error in blaming the Empire. Because of this, she would much rather talk than fight.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So what should I do?
	if (response == "s_237")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I have learned that they are planning an attack on Loruna herself. We must stop it! Go to their safe house and thin their ranks.
			string_id message = new string_id (c_stringFile, "s_219");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How many do I kill?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I have other questions.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_228");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_227");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I have learned that they are planning an attack on Loruna herself. We must stop it! Go to their safe house and thin their ranks.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How many do I kill?
	if (response == "s_228")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: We do not kill more of Loruna's sisters than we must. Kill only 10 and return to me.
			string_id message = new string_id (c_stringFile, "s_229");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Which Mystril Shadow Guards should I kill?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I have other questions, too.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Sounds good to me. I'm on my way.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_230");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_231");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_233");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have other questions.
	if (response == "s_227")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We do not kill more of Loruna's sisters than we must. Kill only 10 and return to me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Which Mystril Shadow Guards should I kill?
	if (response == "s_230")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Any except for a Team Prime. They are the senior-most warriors and there is a chance you will kill someone Loruna cares for personally.
			string_id message = new string_id (c_stringFile, "s_232");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll kill them... um... gently?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I had another question.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_235");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_236");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have other questions, too.
	if (response == "s_231")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good to me. I'm on my way.
	if (response == "s_233")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Any except for a Team Prime. They are the senior-most warriors and there is a chance you will kill someone Loruna cares for personally.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll kill them... um... gently?
	if (response == "s_235")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I had another question.
	if (response == "s_236")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why do we care?
	if (response == "s_239")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna's kind do not make mistakes. The Shadow Guard knew that we were defending that shipment. They either had a contract from a rival organization or were attacking to make us look bad. Either way, we must take prompt action to save face.
			string_id message = new string_id (c_stringFile, "s_241");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So I kill Skirmishers now?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more information about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm ready to do the job.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_243");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_252");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_244");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want more specifics about my assignment.
	if (response == "s_240")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I have calculated the appropriate level of response to this attack at 8.59 kills. That is to say, if we kill approximately 9 Skirmishers, we will have saved face without overdoing it and escalating this conflict beyond Loruna's wishes.
			string_id message = new string_id (c_stringFile, "s_242");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care about proportional response?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's all I need to know.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_245");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_246");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Loruna's kind do not make mistakes. The Shadow Guard knew that we were defending that shipment. They either had a contract from a rival organization or were attacking to make us look bad. Either way, we must take prompt action to save face.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So I kill Skirmishers now?
	if (response == "s_243")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes. Do you need operational specifics?
			string_id message = new string_id (c_stringFile, "s_253");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, I'm ready to go.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_254");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want more information about the assignment.
	if (response == "s_252")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I have calculated the appropriate level of response to this attack at 8.59 kills. That is to say, if we kill approximately 9 Skirmishers, we will have saved face without overdoing it and escalating this conflict beyond Loruna's wishes.
			string_id message = new string_id (c_stringFile, "s_242");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care about proportional response?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's all I need to know.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_245");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_246");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do the job.
	if (response == "s_244")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes. Do you need operational specifics?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes
	if (response == "s_254")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I have calculated the appropriate level of response to this attack at 8.59 kills. That is to say, if we kill approximately 9 Skirmishers, we will have saved face without overdoing it and escalating this conflict beyond Loruna's wishes.
			string_id message = new string_id (c_stringFile, "s_242");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care about proportional response?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's all I need to know.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_245");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_246");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I'm ready to go.
	if (response == "s_255")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I have calculated the appropriate level of response to this attack at 8.59 kills. That is to say, if we kill approximately 9 Skirmishers, we will have saved face without overdoing it and escalating this conflict beyond Loruna's wishes.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why do we care about proportional response?
	if (response == "s_245")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: As I may have said before, Loruna was once one of the Mystril Shadow Guard and regards them as lost sisters. We cannot back down from a direct attack without losing face, but Loruna would like us to exercise discretion where possible.
			string_id message = new string_id (c_stringFile, "s_247");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where can I find the Mystril Shadow Guard?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: As good as done.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_248");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_249");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's all I need to know.
	if (response == "s_246")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: As I may have said before, Loruna was once one of the Mystril Shadow Guard and regards them as lost sisters. We cannot back down from a direct attack without losing face, but Loruna would like us to exercise discretion where possible.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where can I find the Mystril Shadow Guard?
	if (response == "s_248")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: They have a safe house near Theed. You will certainly find enough of them in and around there.
			string_id message = new string_id (c_stringFile, "s_250");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll go now.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_251");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: As good as done.
	if (response == "s_249")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They have a safe house near Theed. You will certainly find enough of them in and around there.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll go now.
	if (response == "s_251")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why should we care?
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You know that Loruna was once a Mystril Shadow Guard. Despite the animosity between our two organizations, the presence of a spy in the Shadow Guard is something Loruna cannot abide. 
			string_id message = new string_id (c_stringFile, "s_110");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why doesn't she just tell them?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want details about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_111");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want more specifics about the assignment.
	if (response == "s_109")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel to the Mystril safe house and engage any Mystril Shadow Guards you encounter. As you dispatch each one, attempt to get a confession out of them.
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But what if she is not really the spy?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I have another question.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You know that Loruna was once a Mystril Shadow Guard. Despite the animosity between our two organizations, the presence of a spy in the Shadow Guard is something Loruna cannot abide. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why doesn't she just tell them?
	if (response == "s_111")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: She has, of course, but they think it's a ploy. Unless we can identify the spy, they will ignore her warnings.
			string_id message = new string_id (c_stringFile, "s_113");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay, I'm on my way
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_114");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_118");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want details about the assignment.
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel to the Mystril safe house and engage any Mystril Shadow Guards you encounter. As you dispatch each one, attempt to get a confession out of them.
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But what if she is not really the spy?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I have another question.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: She has, of course, but they think it's a ploy. Unless we can identify the spy, they will ignore her warnings.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, I'm on my way
	if (response == "s_114")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: If only they would listen...
			string_id message = new string_id (c_stringFile, "s_339");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more about the assignment.
	if (response == "s_118")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel to the Mystril safe house and engage any Mystril Shadow Guards you encounter. As you dispatch each one, attempt to get a confession out of them.
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But what if she is not really the spy?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I have another question.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Travel to the Mystril safe house and engage any Mystril Shadow Guards you encounter. As you dispatch each one, attempt to get a confession out of them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: But what if she is not really the spy?
	if (response == "s_121")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Who cares? A confession will make them at least look more closely at their organization and listen to Loruna's warnings. That's all I care about.
			string_id message = new string_id (c_stringFile, "s_122");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, but I have another question first.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_124");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_125");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have another question.
	if (response == "s_123")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Who cares? A confession will make them at least look more closely at their organization and listen to Loruna's warnings. That's all I care about.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay.
	if (response == "s_124")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: The sooner you can convince one of them that she's a spy, the sooner you can stop killing Loruna's sisters.
			string_id message = new string_id (c_stringFile, "s_126");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, but I have another question first.
	if (response == "s_125")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What can I answer for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about Loruna's past.
	if (response == "s_87")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Because she sent you to deal with the Mystril Shadow Guard on her behalf, I will share this. Many years ago, when Loruna was still young, she learned that the rumors of Imperial destruction of Emberlene were false.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How so?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Enough said. Tell me about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_91");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 33);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Clarify my orders for me.
	if (response == "s_106")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Some of the Mystril Shadow Guard Slicers have accessed the database of one of our clients! This makes us look very bad and Loruna is livid. So angry that she has lifted the injunction against killing Mystrils!
			string_id message = new string_id (c_stringFile, "s_127");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me ask about something else.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_132");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_340");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Because she sent you to deal with the Mystril Shadow Guard on her behalf, I will share this. Many years ago, when Loruna was still young, she learned that the rumors of Imperial destruction of Emberlene were false.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How so?
	if (response == "s_91")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: It was not the Empire at all. It was the Eleven! 
			string_id message = new string_id (c_stringFile, "s_93");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Eleven?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: So what am I supposed to do?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_128");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Enough said. Tell me about the assignment.
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Some of the Mystril Shadow Guard Slicers have accessed the database of one of our clients! This makes us look very bad and Loruna is livid. So angry that she has lifted the injunction against killing Mystrils!
			string_id message = new string_id (c_stringFile, "s_127");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me ask about something else.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_132");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_340");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It was not the Empire at all. It was the Eleven! 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Eleven?
	if (response == "s_128")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: The leaders of Emberlene! They had grand designs of creating a mini-empire for themselves. When their neighbors sensed that Emberlene was about to pounce on them, they hired mercenaries to raze the planet.
			string_id message = new string_id (c_stringFile, "s_129");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Can Loruna prove any of this?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_131");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So what am I supposed to do?
	if (response == "s_102")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Some of the Mystril Shadow Guard Slicers have accessed the database of one of our clients! This makes us look very bad and Loruna is livid. So angry that she has lifted the injunction against killing Mystrils!
			string_id message = new string_id (c_stringFile, "s_127");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me ask about something else.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_132");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_340");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The leaders of Emberlene! They had grand designs of creating a mini-empire for themselves. When their neighbors sensed that Emberlene was about to pounce on them, they hired mercenaries to raze the planet.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can Loruna prove any of this?
	if (response == "s_131")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sadly, no. She had some documents on a disk, but the Empire confiscated them. It would appear that the Emperor WANTS people to believe that he destroyed the planet.
			string_id message = new string_id (c_stringFile, "s_133");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about my assignment.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_139");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sadly, no. She had some documents on a disk, but the Empire confiscated them. It would appear that the Emperor WANTS people to believe that he destroyed the planet.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about my assignment.
	if (response == "s_139")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Some of the Mystril Shadow Guard Slicers have accessed the database of one of our clients! This makes us look very bad and Loruna is livid. So angry that she has lifted the injunction against killing Mystrils!
			string_id message = new string_id (c_stringFile, "s_127");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me ask about something else.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_132");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_340");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Some of the Mystril Shadow Guard Slicers have accessed the database of one of our clients! This makes us look very bad and Loruna is livid. So angry that she has lifted the injunction against killing Mystrils!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Go on.
	if (response == "s_132")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Locate the Slicers in the Mystril safe house and make them pay for their boldness. Once you recover the stolen datatables, return here for payment.
			string_id message = new string_id (c_stringFile, "s_136");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: On my way.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I have other questions.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_140");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_341");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let me ask about something else.
	if (response == "s_340")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Locate the Slicers in the Mystril safe house and make them pay for their boldness. Once you recover the stolen datatables, return here for payment.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: On my way.
	if (response == "s_140")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna is angry. Make sure her sisters remember what it means to push too far.
			string_id message = new string_id (c_stringFile, "s_142");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have other questions.
	if (response == "s_341")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1A (player, npc))
		{
			//-- NPC: What would you know?
			string_id message = new string_id (c_stringFile, "s_197");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Mystril Shadow Guard.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Assignment specifics.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_216");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_217");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1B (player, npc))
		{
			//-- NPC: I recently learned that a supply convoy that our agents were defending was destroyed by the Mystril Shadow Guard Skirmishers.
			string_id message = new string_id (c_stringFile, "s_199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_240");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1C (player, npc))
		{
			//-- NPC: It's a complicated situation. The Mystril Shadow Guard has an Imperial spy in their organization and Loruna needs your help in proving it.
			string_id message = new string_id (c_stringFile, "s_201");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why should we care?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want more specifics about the assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_109");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1D (player, npc))
		{
			//-- NPC: What can I answer for you?
			string_id message = new string_id (c_stringFile, "s_203");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about Loruna's past.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Clarify my orders for me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q1E (player, npc))
		{
			//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.
			string_id message = new string_id (c_stringFile, "s_299");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Forget it. I'm out of here.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_301");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_303");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should read your journal. Perhaps it holds the answers you seek.
			string_id message = new string_id (c_stringFile, "s_167");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch40 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You... I... I don't know how to begin. Loruna has asked a favor from you. Something special that will demonstrate your loyalty beyond anything I can imagine.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What?
	if (response == "s_301")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of our operatives went insane in the Mystril safe house and killed MANY of Loruna's sisters. They are outraged and are threatening to cut off all communication with Loruna if someone does not voluntarily pay the price.
			string_id message = new string_id (c_stringFile, "s_313");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why doesn't Loruna go?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am not interested in dying for the Loruna.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_315");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_320");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 41);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Forget it. I'm out of here.
	if (response == "s_303")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please! Loruna needs someone to do this and cannot do it herself. We will bear the cost of the cloning, but a member of the Cabal must volunteer for ritual assassination. 
			string_id message = new string_id (c_stringFile, "s_305");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, alright. But I expect to be WELL paid.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I said NO!
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_307");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_309");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: One of our operatives went insane in the Mystril safe house and killed MANY of Loruna's sisters. They are outraged and are threatening to cut off all communication with Loruna if someone does not voluntarily pay the price.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why doesn't Loruna go?
	if (response == "s_315")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: They were explicit. She is not to set foot in the safe house, where the assassination will take place.
			string_id message = new string_id (c_stringFile, "s_317");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So what do I do?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Get someone else.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_321");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_322");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 42);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am not interested in dying for the Loruna.
	if (response == "s_320")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please! Loruna needs someone to do this and cannot do it herself. We will bear the cost of the cloning, but a member of the Cabal must volunteer for ritual assassination. 
			string_id message = new string_id (c_stringFile, "s_305");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, alright. But I expect to be WELL paid.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I said NO!
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_307");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_309");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They were explicit. She is not to set foot in the safe house, where the assassination will take place.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So what do I do?
	if (response == "s_321")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel to the Mystril safe house outside of Theed. You will not be welcomed. that's part of the test. You must fight your way to the Emberlene Memorial to prove yourself a worthy sacrifice.
			string_id message = new string_id (c_stringFile, "s_323");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then what?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nope. Not me.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_324");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_325");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Get someone else.
	if (response == "s_322")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please! Loruna needs someone to do this and cannot do it herself. We will bear the cost of the cloning, but a member of the Cabal must volunteer for ritual assassination. 
			string_id message = new string_id (c_stringFile, "s_305");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, alright. But I expect to be WELL paid.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I said NO!
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_307");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_309");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Travel to the Mystril safe house outside of Theed. You will not be welcomed. that's part of the test. You must fight your way to the Emberlene Memorial to prove yourself a worthy sacrifice.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then what?
	if (response == "s_324")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must kill all of the Mystril Shadow Guards in the room and then touch the Memorial on behalf of Loruna.
			string_id message = new string_id (c_stringFile, "s_326");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And then?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nope. I'm out.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_327");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_329");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nope. Not me.
	if (response == "s_325")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please! Loruna needs someone to do this and cannot do it herself. We will bear the cost of the cloning, but a member of the Cabal must volunteer for ritual assassination. 
			string_id message = new string_id (c_stringFile, "s_305");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, alright. But I expect to be WELL paid.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I said NO!
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_307");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_309");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch44 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You must kill all of the Mystril Shadow Guards in the room and then touch the Memorial on behalf of Loruna.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And then?
	if (response == "s_327")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then... well... When more Mystril come, you just take the punishment and die. Not just pass out, you must be truly killed.
			string_id message = new string_id (c_stringFile, "s_331");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So I die. Then what?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You are crazy.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_332");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_333");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nope. I'm out.
	if (response == "s_329")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please! Loruna needs someone to do this and cannot do it herself. We will bear the cost of the cloning, but a member of the Cabal must volunteer for ritual assassination. 
			string_id message = new string_id (c_stringFile, "s_305");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, alright. But I expect to be WELL paid.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I said NO!
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_307");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_309");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then... well... When more Mystril come, you just take the punishment and die. Not just pass out, you must be truly killed.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So I die. Then what?
	if (response == "s_332")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: When you have been cloned, return here and I will give you the deepest thanks of the Azure Cabal.
			string_id message = new string_id (c_stringFile, "s_334");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And payment, of course.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nah. I'm not doing it.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_335");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_336");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 46);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You are crazy.
	if (response == "s_333")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please! Loruna needs someone to do this and cannot do it herself. We will bear the cost of the cloning, but a member of the Cabal must volunteer for ritual assassination. 
			string_id message = new string_id (c_stringFile, "s_305");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, alright. But I expect to be WELL paid.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I said NO!
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_307");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_309");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch46 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: When you have been cloned, return here and I will give you the deepest thanks of the Azure Cabal.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And payment, of course.
	if (response == "s_335")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Now, go with Loruna's blessings and my thanks, my friend.
			string_id message = new string_id (c_stringFile, "s_337");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nah. I'm not doing it.
	if (response == "s_336")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please! Loruna needs someone to do this and cannot do it herself. We will bear the cost of the cloning, but a member of the Cabal must volunteer for ritual assassination. 
			string_id message = new string_id (c_stringFile, "s_305");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, alright. But I expect to be WELL paid.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I said NO!
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_307");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_309");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch48 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please! Loruna needs someone to do this and cannot do it herself. We will bear the cost of the cloning, but a member of the Cabal must volunteer for ritual assassination. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, alright. But I expect to be WELL paid.
	if (response == "s_307")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: When you have been cloned, return here and I will give you the deepest thanks of the Azure Cabal.
			string_id message = new string_id (c_stringFile, "s_334");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And payment, of course.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nah. I'm not doing it.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_335");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_336");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 46);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I said NO!
	if (response == "s_309")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			azure_cabal_camdoen_action_signal_30ENF_no_sacrifice (player, npc);

			//-- NPC: As you wish. The sacrifice must go willingly. I will inform Loruna.
			string_id message = new string_id (c_stringFile, "s_311");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent working with you. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Anything else you need?
	if (response == "s_200")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Loruna will contact you when she has another assignment for you.
			string_id message = new string_id (c_stringFile, "s_198");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch53 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Do you need additional information about your assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_283")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2A (player, npc))
		{
			//-- NPC: Are you interested in information about the M'dweshuui or about your specific assignment?
			string_id message = new string_id (c_stringFile, "s_358");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the M'dweshuui
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_360");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_403");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2B (player, npc))
		{
			//-- NPC: You have been assigned a simple killing run. Do you need more information about the Nikto M'dweshuui blood cult?
			string_id message = new string_id (c_stringFile, "s_286");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, tell me more.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, but where do I go?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_288");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_312");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_401");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2C (player, npc))
		{
			//-- NPC: We have a chance to turn the M'dweshuui's fury against the Mon'Shallok terrorists, but you must act quickly!
			string_id message = new string_id (c_stringFile, "s_396");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me more.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_399");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_400");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_402");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D (player, npc))
		{
			//-- NPC: This is a most delicate assignment. How can I help you?
			string_id message = new string_id (c_stringFile, "s_398");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just tell me where to go.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm ready to begin.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_417");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_418");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_419");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D_inv_use (player, npc))
		{
			azure_cabal_camdoen_action_facePlayer (player, npc);

			//-- NPC: Just check your inventory for the Emberlene Essence and apply it. 
			string_id message = new string_id (c_stringFile, "s_352");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not now! We cannot be seen here. There are Mystril about.
			string_id message = new string_id (c_stringFile, "s_354");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, just checking in.
	if (response == "s_356")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you interested in information about the M'dweshuui or about your specific assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the M'dweshuui
	if (response == "s_360")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: The M'dweshuui are a blood-cult that has wreaked havoc on the history of the Nikto people, sometimes slaughtering entire continents of planets in which they gain a foothold.  
			string_id message = new string_id (c_stringFile, "s_362");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready to begin.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How so?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: True. Tell me more about my assignment.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_364");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_366");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_382");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 55);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more about my assignment.
	if (response == "s_384")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I recently observed a skirmish between the Nikto Fanatics and some Drennics. If we attack the M'dweshuui immediately, they will conclude that this is an escalation of that skirmish.
			string_id message = new string_id (c_stringFile, "s_386");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How many should I kill?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_388");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_390");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, that's all I needed.
	if (response == "s_403")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch55 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The M'dweshuui are a blood-cult that has wreaked havoc on the history of the Nikto people, sometimes slaughtering entire continents of planets in which they gain a foothold.  

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to begin.
	if (response == "s_364")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How so?
	if (response == "s_366")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: If we can mislead them into believing that one of our adversaries has attacked them, they will kill without pause to slake their bloodlust.
			string_id message = new string_id (c_stringFile, "s_368");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready to begin.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Isn't that a dangerous game?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_372");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_380");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: True. Tell me more about my assignment.
	if (response == "s_382")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I recently observed a skirmish between the Nikto Fanatics and some Drennics. If we attack the M'dweshuui immediately, they will conclude that this is an escalation of that skirmish.
			string_id message = new string_id (c_stringFile, "s_386");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How many should I kill?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_388");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_390");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch56 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If we can mislead them into believing that one of our adversaries has attacked them, they will kill without pause to slake their bloodlust.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to begin.
	if (response == "s_370")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Isn't that a dangerous game?
	if (response == "s_372")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course! If they learn that it is the Azure Cabal behind the attacks, you can be certain they will slaughter us all.
			string_id message = new string_id (c_stringFile, "s_374");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Understood. I'm ready to begin.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_376");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_378");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more about my assignment.
	if (response == "s_380")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I recently observed a skirmish between the Nikto Fanatics and some Drennics. If we attack the M'dweshuui immediately, they will conclude that this is an escalation of that skirmish.
			string_id message = new string_id (c_stringFile, "s_386");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How many should I kill?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_388");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_390");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch57 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course! If they learn that it is the Azure Cabal behind the attacks, you can be certain they will slaughter us all.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Understood. I'm ready to begin.
	if (response == "s_376")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more about my assignment.
	if (response == "s_378")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I recently observed a skirmish between the Nikto Fanatics and some Drennics. If we attack the M'dweshuui immediately, they will conclude that this is an escalation of that skirmish.
			string_id message = new string_id (c_stringFile, "s_386");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How many should I kill?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_388");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_390");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I recently observed a skirmish between the Nikto Fanatics and some Drennics. If we attack the M'dweshuui immediately, they will conclude that this is an escalation of that skirmish.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay.
	if (response == "s_388")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How many should I kill?
	if (response == "s_390")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: No more than 15, I'd say. My data show that the Drennic capabilities would not allow for a wholesale slaughter. Remember, we must avoid calling attention to ourselves at all. You've fought Drennics... use their tactics if you can.
			string_id message = new string_id (c_stringFile, "s_392");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay. Consider it done.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_394");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch59 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No more than 15, I'd say. My data show that the Drennic capabilities would not allow for a wholesale slaughter. Remember, we must avoid calling attention to ourselves at all. You've fought Drennics... use their tactics if you can.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay. Consider it done.
	if (response == "s_394")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch60 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have been assigned a simple killing run. Do you need more information about the Nikto M'dweshuui blood cult?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, tell me more.
	if (response == "s_288")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: We intend to prod them into unwitting serving for the Azure Cabal by exploiting other conflicts this group is involved in. They are moderately intelligent, but as soon as they become enraged, they lose the capacity for reason.
			string_id message = new string_id (c_stringFile, "s_290");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've heard enough.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do we use them?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me more about my specific assignment.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_292");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_294");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_310");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, but where do I go?
	if (response == "s_312")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel to the M'dweshuui Stronghold immediately. You must arrive before or during the Sleepless Brotherhood's assault and kill the first 20 you see. Don't be picky. The Sleepless Brotherhood would charge in blindly and I expect you to do so as well.
			string_id message = new string_id (c_stringFile, "s_314");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay, I'm going.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_316");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_318");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 64);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, that's all I needed.
	if (response == "s_401")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We intend to prod them into unwitting serving for the Azure Cabal by exploiting other conflicts this group is involved in. They are moderately intelligent, but as soon as they become enraged, they lose the capacity for reason.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've heard enough.
	if (response == "s_292")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do we use them?
	if (response == "s_294")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Each time we receive intelligence that they have engaged our adversaries and rival mercenary groups, we will strike quickly in ways intended to make the cultists escalate the conflict.
			string_id message = new string_id (c_stringFile, "s_296");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready to go.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Has there been an attack?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about my assignment.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_298");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_300");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_308");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more about my specific assignment.
	if (response == "s_310")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2A (player, npc))
		{
			//-- NPC: Are you interested in information about the M'dweshuui or about your specific assignment?
			string_id message = new string_id (c_stringFile, "s_358");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the M'dweshuui
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_360");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_403");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2B (player, npc))
		{
			//-- NPC: You have been assigned a simple killing run. Do you need more information about the Nikto M'dweshuui blood cult?
			string_id message = new string_id (c_stringFile, "s_286");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, tell me more.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, but where do I go?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_288");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_312");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_401");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2C (player, npc))
		{
			//-- NPC: We have a chance to turn the M'dweshuui's fury against the Mon'Shallok terrorists, but you must act quickly!
			string_id message = new string_id (c_stringFile, "s_396");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me more.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_399");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_400");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_402");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D (player, npc))
		{
			//-- NPC: This is a most delicate assignment. How can I help you?
			string_id message = new string_id (c_stringFile, "s_398");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just tell me where to go.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm ready to begin.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_417");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_418");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_419");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D_inv_use (player, npc))
		{
			azure_cabal_camdoen_action_facePlayer (player, npc);

			//-- NPC: Just check your inventory for the Emberlene Essence and apply it. 
			string_id message = new string_id (c_stringFile, "s_352");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not now! We cannot be seen here. There are Mystril about.
			string_id message = new string_id (c_stringFile, "s_354");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Each time we receive intelligence that they have engaged our adversaries and rival mercenary groups, we will strike quickly in ways intended to make the cultists escalate the conflict.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to go.
	if (response == "s_298")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Has there been an attack?
	if (response == "s_300")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not yet, but I intercepted a comm signal between two of their Overseers that discussed an imminent attack by the Sleepless Brotherhood. We will attack in a manner that will allow them to reach the logical conclusion that the Brotherhood should be the main target of their rage.
			string_id message = new string_id (c_stringFile, "s_302");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay, I'm ready to go.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: So what do I do?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_304");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_306");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about my assignment.
	if (response == "s_308")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2A (player, npc))
		{
			//-- NPC: Are you interested in information about the M'dweshuui or about your specific assignment?
			string_id message = new string_id (c_stringFile, "s_358");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the M'dweshuui
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_360");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_403");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2B (player, npc))
		{
			//-- NPC: You have been assigned a simple killing run. Do you need more information about the Nikto M'dweshuui blood cult?
			string_id message = new string_id (c_stringFile, "s_286");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, tell me more.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, but where do I go?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_288");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_312");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_401");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2C (player, npc))
		{
			//-- NPC: We have a chance to turn the M'dweshuui's fury against the Mon'Shallok terrorists, but you must act quickly!
			string_id message = new string_id (c_stringFile, "s_396");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me more.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_399");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_400");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_402");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D (player, npc))
		{
			//-- NPC: This is a most delicate assignment. How can I help you?
			string_id message = new string_id (c_stringFile, "s_398");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just tell me where to go.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm ready to begin.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_417");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_418");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_419");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D_inv_use (player, npc))
		{
			azure_cabal_camdoen_action_facePlayer (player, npc);

			//-- NPC: Just check your inventory for the Emberlene Essence and apply it. 
			string_id message = new string_id (c_stringFile, "s_352");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not now! We cannot be seen here. There are Mystril about.
			string_id message = new string_id (c_stringFile, "s_354");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch63 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not yet, but I intercepted a comm signal between two of their Overseers that discussed an imminent attack by the Sleepless Brotherhood. We will attack in a manner that will allow them to reach the logical conclusion that the Brotherhood should be the main target of their rage.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, I'm ready to go.
	if (response == "s_304")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So what do I do?
	if (response == "s_306")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2A (player, npc))
		{
			//-- NPC: Are you interested in information about the M'dweshuui or about your specific assignment?
			string_id message = new string_id (c_stringFile, "s_358");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the M'dweshuui
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_360");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_403");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2B (player, npc))
		{
			//-- NPC: You have been assigned a simple killing run. Do you need more information about the Nikto M'dweshuui blood cult?
			string_id message = new string_id (c_stringFile, "s_286");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, tell me more.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, but where do I go?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_288");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_312");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_401");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2C (player, npc))
		{
			//-- NPC: We have a chance to turn the M'dweshuui's fury against the Mon'Shallok terrorists, but you must act quickly!
			string_id message = new string_id (c_stringFile, "s_396");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me more.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, that's all I needed.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_399");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_400");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_402");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D (player, npc))
		{
			//-- NPC: This is a most delicate assignment. How can I help you?
			string_id message = new string_id (c_stringFile, "s_398");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just tell me where to go.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me more.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'm ready to begin.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_417");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_418");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_419");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition_onAssignment_30ENF_Q2D_inv_use (player, npc))
		{
			azure_cabal_camdoen_action_facePlayer (player, npc);

			//-- NPC: Just check your inventory for the Emberlene Essence and apply it. 
			string_id message = new string_id (c_stringFile, "s_352");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not now! We cannot be seen here. There are Mystril about.
			string_id message = new string_id (c_stringFile, "s_354");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Travel to the M'dweshuui Stronghold immediately. You must arrive before or during the Sleepless Brotherhood's assault and kill the first 20 you see. Don't be picky. The Sleepless Brotherhood would charge in blindly and I expect you to do so as well.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, I'm going.
	if (response == "s_316")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more.
	if (response == "s_318")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: We intend to prod them into unwitting serving for the Azure Cabal by exploiting other conflicts this group is involved in. They are moderately intelligent, but as soon as they become enraged, they lose the capacity for reason.
			string_id message = new string_id (c_stringFile, "s_290");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've heard enough.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do we use them?
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me more about my specific assignment.
			boolean hasResponse2 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_292");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_294");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_310");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch65 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We have a chance to turn the M'dweshuui's fury against the Mon'Shallok terrorists, but you must act quickly!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more.
	if (response == "s_399")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Recently a Mon'Shallok operative attempted to slice our database. They were so clumsy that we identified their data-trail instantly and stopped the incursion.
			string_id message = new string_id (c_stringFile, "s_404");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So how does this help me?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Fine. Tell me about my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_406");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_407");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more about my assignment.
	if (response == "s_400")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must fight your way into the area where the Overseers reside and kill at least 5 of them. 
			string_id message = new string_id (c_stringFile, "s_411");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then what?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, thanks.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_413");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_414");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, that's all I needed.
	if (response == "s_402")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch66 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Recently a Mon'Shallok operative attempted to slice our database. They were so clumsy that we identified their data-trail instantly and stopped the incursion.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So how does this help me?
	if (response == "s_406")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: We now have a clear data-trail signature! We can use this to frame the Mon'Shallok in the eyes of the M'dweshuui blood cultists.
			string_id message = new string_id (c_stringFile, "s_408");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me more about my assignment.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: If you say so... I'm ready to begin my assignment.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_410");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_409");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine. Tell me about my assignment.
	if (response == "s_407")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must fight your way into the area where the Overseers reside and kill at least 5 of them. 
			string_id message = new string_id (c_stringFile, "s_411");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then what?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, thanks.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_413");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_414");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch67 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We now have a clear data-trail signature! We can use this to frame the Mon'Shallok in the eyes of the M'dweshuui blood cultists.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more about my assignment.
	if (response == "s_410")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must fight your way into the area where the Overseers reside and kill at least 5 of them. 
			string_id message = new string_id (c_stringFile, "s_411");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then what?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay, thanks.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_413");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_414");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: If you say so... I'm ready to begin my assignment.
	if (response == "s_409")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch68 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You must fight your way into the area where the Overseers reside and kill at least 5 of them. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then what?
	if (response == "s_413")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then locate and deliberately botch an attempt to slice into their power grid control terminal. You must leave a clear data-trail that identifies the attacker as Mon'Shallok.
			string_id message = new string_id (c_stringFile, "s_415");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay, I'm on my way.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_416");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, thanks.
	if (response == "s_414")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch69 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then locate and deliberately botch an attempt to slice into their power grid control terminal. You must leave a clear data-trail that identifies the attacker as Mon'Shallok.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, I'm on my way.
	if (response == "s_416")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch70 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This is a most delicate assignment. How can I help you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just tell me where to go.
	if (response == "s_417")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: First you will go to the Mystril safe house. Then, after you have completed your tasks there, you must move immediately to the M'dweshuui Stronghold. I have uploaded the coordinates to both to your datapad.
			string_id message = new string_id (c_stringFile, "s_420");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are the specifics of my assignment?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm ready to go.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_421");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_422");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me more.
	if (response == "s_418")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: In the Mystril safe house there is a shrine with several jars on it. Take one of the jars of Emberlene Essence.
			string_id message = new string_id (c_stringFile, "s_423");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay. Go on.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's all I needed to know.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_424");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_425");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 72);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to begin.
	if (response == "s_419")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch71 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: First you will go to the Mystril safe house. Then, after you have completed your tasks there, you must move immediately to the M'dweshuui Stronghold. I have uploaded the coordinates to both to your datapad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are the specifics of my assignment?
	if (response == "s_421")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: In the Mystril safe house there is a shrine with several jars on it. Take one of the jars of Emberlene Essence.
			string_id message = new string_id (c_stringFile, "s_423");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay. Go on.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's all I needed to know.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_424");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_425");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 72);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to go.
	if (response == "s_422")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch72 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: In the Mystril safe house there is a shrine with several jars on it. Take one of the jars of Emberlene Essence.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay. Go on.
	if (response == "s_424")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Emberlene Essence in your inventory to mask your normal scent and assume the scent of a Mystril Shadow Guard.
			string_id message = new string_id (c_stringFile, "s_426");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay, I put on perfume. Then what.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Instill a sense of terror. That I can do!
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_428");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's all I needed to know.
	if (response == "s_425")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch73 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Use the Emberlene Essence in your inventory to mask your normal scent and assume the scent of a Mystril Shadow Guard.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, I put on perfume. Then what.
	if (response == "s_427")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel quickly to the M'dweshuui Stronghold and let yourself go. Kill as many of the Nikto there as you must to spread the scent of the Mystril throughout their lair.
			string_id message = new string_id (c_stringFile, "s_429");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How many should I kill?
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Okay. I've got it now.
			boolean hasResponse1 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_430");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_431");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Instill a sense of terror. That I can do!
	if (response == "s_428")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch74 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Travel quickly to the M'dweshuui Stronghold and let yourself go. Kill as many of the Nikto there as you must to spread the scent of the Mystril throughout their lair.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How many should I kill?
	if (response == "s_430")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: I will comm you when I begin to see comms from their stronghold warning others of a Mystril assault. Until then, you just keep killing.
			string_id message = new string_id (c_stringFile, "s_432");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Got it.
			boolean hasResponse0 = false;
			if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_433");

				utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay. I've got it now.
	if (response == "s_431")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int azure_cabal_camdoen_handleBranch75 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I will comm you when I begin to see comms from their stronghold warning others of a Mystril assault. Until then, you just keep killing.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Got it.
	if (response == "s_433")
	{
		//-- [NOTE] 
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			//-- NPC: Travel well, trusted one.
			string_id message = new string_id (c_stringFile, "s_195");
			utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isTangible (self)) || (isPlayer (self)))
		detachScript(self, "conversation.azure_cabal_camdoen");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.azure_cabal_camdoen");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (azure_cabal_camdoen_condition_onTask_30ENF_Repeater_Q1 (player, npc))
	{
		azure_cabal_camdoen_action_signal_30ENF_ClearRepeaterQuest (player, npc);

		//-- NPC: Loruna has sent you to me? Very good! She must trust you greatly to involve you in her dealings with the Mystril Shadow Guard.
		string_id message = new string_id (c_stringFile, "s_168");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, I do.
		boolean hasResponse0 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No.
		boolean hasResponse1 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_170");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_171");

			utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 1);

			npcStartConversation (player, npc, "azure_cabal_camdoen", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (azure_cabal_camdoen_condition_onTask_30ENF_Repeater_Q2 (player, npc))
	{
		azure_cabal_camdoen_action_signal_30ENF_ClearRepeaterQuest (player, npc);

		//-- NPC: The Nikto are not bright and are easily manipulated. Are you willing to help me? If not, you must return to Loruna for further assignment.
		string_id message = new string_id (c_stringFile, "s_174");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'll deal with the Nikto.
		boolean hasResponse0 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not interested.
		boolean hasResponse1 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_175");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_176");

			utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 5);

			npcStartConversation (player, npc, "azure_cabal_camdoen", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (azure_cabal_camdoen_condition_onTask_30ENF_Q1_Return (player, npc))
	{
		azure_cabal_camdoen_action_signal_30ENF_CompleteQuest (player, npc);

		//-- NPC: You are doing very well.
		string_id message = new string_id (c_stringFile, "s_189");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Now what do I do?
		boolean hasResponse0 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_196");

			utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 9);

			npcStartConversation (player, npc, "azure_cabal_camdoen", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (azure_cabal_camdoen_condition_onQuest_30ENF_Q1 (player, npc))
	{
		//-- NPC: Why are you still here? Do you need my help?
		string_id message = new string_id (c_stringFile, "s_192");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes.
		boolean hasResponse0 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No, just checking in.
		boolean hasResponse1 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_193");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_194");

			utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 11);

			npcStartConversation (player, npc, "azure_cabal_camdoen", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (azure_cabal_camdoen_condition_onTask_30ENF_Q2_Return (player, npc))
	{
		azure_cabal_camdoen_action_signal_30ENF_CompleteQuest (player, npc);

		//-- NPC: Excellent working with you. 
		string_id message = new string_id (c_stringFile, "s_191");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Anything else you need?
		boolean hasResponse0 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_200");

			utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 52);

			npcStartConversation (player, npc, "azure_cabal_camdoen", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (azure_cabal_camdoen_condition_onQuest_30ENF_Q2 (player, npc))
	{
		//-- NPC: Do you need additional information about your assignment?
		string_id message = new string_id (c_stringFile, "s_282");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes.
		boolean hasResponse0 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No, just checking in.
		boolean hasResponse1 = false;
		if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_283");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_356");

			utils.setScriptVar (player, "conversation.azure_cabal_camdoen.branchId", 53);

			npcStartConversation (player, npc, "azure_cabal_camdoen", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (azure_cabal_camdoen_condition__defaultCondition (player, npc))
	{
		//-- NPC: I cannot speak to you now. It is not safe. Come back later.
		string_id message = new string_id (c_stringFile, "s_359");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "azure_cabal_camdoen")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

	if (branchId == 1 && azure_cabal_camdoen_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && azure_cabal_camdoen_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && azure_cabal_camdoen_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && azure_cabal_camdoen_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && azure_cabal_camdoen_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && azure_cabal_camdoen_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && azure_cabal_camdoen_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && azure_cabal_camdoen_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && azure_cabal_camdoen_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && azure_cabal_camdoen_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && azure_cabal_camdoen_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && azure_cabal_camdoen_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && azure_cabal_camdoen_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && azure_cabal_camdoen_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && azure_cabal_camdoen_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && azure_cabal_camdoen_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && azure_cabal_camdoen_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && azure_cabal_camdoen_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && azure_cabal_camdoen_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && azure_cabal_camdoen_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && azure_cabal_camdoen_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && azure_cabal_camdoen_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && azure_cabal_camdoen_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && azure_cabal_camdoen_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && azure_cabal_camdoen_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && azure_cabal_camdoen_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && azure_cabal_camdoen_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && azure_cabal_camdoen_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && azure_cabal_camdoen_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && azure_cabal_camdoen_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && azure_cabal_camdoen_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 40 && azure_cabal_camdoen_handleBranch40 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && azure_cabal_camdoen_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && azure_cabal_camdoen_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && azure_cabal_camdoen_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 44 && azure_cabal_camdoen_handleBranch44 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && azure_cabal_camdoen_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 46 && azure_cabal_camdoen_handleBranch46 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 48 && azure_cabal_camdoen_handleBranch48 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && azure_cabal_camdoen_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 53 && azure_cabal_camdoen_handleBranch53 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && azure_cabal_camdoen_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 55 && azure_cabal_camdoen_handleBranch55 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 56 && azure_cabal_camdoen_handleBranch56 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 57 && azure_cabal_camdoen_handleBranch57 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && azure_cabal_camdoen_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 59 && azure_cabal_camdoen_handleBranch59 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 60 && azure_cabal_camdoen_handleBranch60 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && azure_cabal_camdoen_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && azure_cabal_camdoen_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 63 && azure_cabal_camdoen_handleBranch63 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && azure_cabal_camdoen_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 65 && azure_cabal_camdoen_handleBranch65 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 66 && azure_cabal_camdoen_handleBranch66 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 67 && azure_cabal_camdoen_handleBranch67 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 68 && azure_cabal_camdoen_handleBranch68 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 69 && azure_cabal_camdoen_handleBranch69 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 70 && azure_cabal_camdoen_handleBranch70 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 71 && azure_cabal_camdoen_handleBranch71 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 72 && azure_cabal_camdoen_handleBranch72 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 73 && azure_cabal_camdoen_handleBranch73 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 74 && azure_cabal_camdoen_handleBranch74 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 75 && azure_cabal_camdoen_handleBranch75 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.azure_cabal_camdoen.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

