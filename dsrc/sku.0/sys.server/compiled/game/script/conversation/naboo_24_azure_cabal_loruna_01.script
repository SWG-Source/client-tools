// ======================================================================
//
// naboo_24_azure_cabal_loruna_01.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.combat;
include library.groundquests;
include library.money;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/naboo_24_azure_cabal_loruna_01";

// ======================================================================
// Script Conditions
// ======================================================================

boolean naboo_24_azure_cabal_loruna_01_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_canTakeQuestAC_01 (obj_id player, obj_id npc)
{
	if ((combat.getLevel(player)) < 80)
		return false;	
	
	return !(groundquests.isQuestActive(player, "naboo_24_azure_cabal_loruna_01") ||
			groundquests.hasCompletedQuest(player, "naboo_24_azure_cabal_01")); 
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_OnQuestAC_01 (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "naboo_24_azure_cabal_01"); 
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_onTaskAC_01_Return (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "naboo_24_azure_cabal_01", "azure_cabal_loruna_endorsements_01_return");
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_onTaskRepeaterCooldown (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_ongoing_launchpad_loruna_repeater", "ac_ongoing_loruna_cooldown_waiting_cooldown");
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_onTaskRepeaterGetQuest (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_ongoing_launchpad_loruna_repeater", "ac_ongoing_loruna_ready_for_quest");
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_onTaskRepeaterCompleteQuest (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "ac_ongoing_launchpad_loruna_repeater", "ac_ongoing_loruna_has_quest");
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_hasCompletedAC_01_NotOnRepeater (obj_id player, obj_id npc)
{
return (groundquests.hasCompletedQuest(player, "naboo_24_azure_cabal_01") &&
		!(groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc1_haadj_01") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc1_haadj_02") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc2_hoolar_01") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc2_hoolar_02") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02")));
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_alwaysFalse (obj_id player, obj_id npc)
{
	return (1 == 0);
}

// ----------------------------------------------------------------------

boolean naboo_24_azure_cabal_loruna_01_condition_hasCompletedAC_01_OnRepeater (obj_id player, obj_id npc)
{
return (groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc1_haadj_01") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc1_haadj_02") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc2_hoolar_01") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc2_hoolar_02") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_01") ||
		groundquests.isQuestActive(player, "ac_repeater_loruna_enf_30_dnpc3_camdoen_02"));
}

// ======================================================================
// Script Actions
// ======================================================================

void naboo_24_azure_cabal_loruna_01_action_grantNaboo24AzureCabal01 (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.grantQuest(player, "quest/naboo_24_azure_cabal_01");
	
	if ( groundquests.isQuestActiveOrComplete(player, "naboo_24_gungan_power_grab_01") )
	{
		groundquests.sendSignal(player, "metBromaLak");
	}
	
	if ( groundquests.hasCompletedQuest(player, "naboo_24_gungan_power_grab_02") )
	{
		groundquests.sendSignal(player, "endorsedByBromaLak");
	}
	
	if ( groundquests.isQuestActiveOrComplete(player, "tatooine_24_azure_cabal_top_world_01") )
	{
		groundquests.sendSignal(player, "workingForProgryn");
	}
	
	if ( groundquests.hasCompletedQuest(player, "tatooine_24_azure_cabal_top_world_01") )
	{
		groundquests.sendSignal(player, "tatooine_24_azure_cabal_top_world_01_return_to_porgryn");
	}
	
	if ( groundquests.hasCompletedQuest(player, "tatooine_24_azure_cabal_hunters_path_02") )
	{
		groundquests.sendSignal(player, "endorsedByTweego");
	}
	
	if ( groundquests.hasCompletedQuest(player, "tatooine_24_azure_cabal_proptis_loss_01") )
	{
		groundquests.sendSignal(player, "endorsedByProptiXaroon");
	}
	
	return;
}

// ----------------------------------------------------------------------

void naboo_24_azure_cabal_loruna_01_action_signalCompleteAC01 (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "endorsedByAll");
}

// ----------------------------------------------------------------------

void naboo_24_azure_cabal_loruna_01_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ----------------------------------------------------------------------

void naboo_24_azure_cabal_loruna_01_action_grantLaunchpadRepeater (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.grantQuest(player, "quest/ac_ongoing_launchpad_loruna_repeater");
}

// ----------------------------------------------------------------------

void naboo_24_azure_cabal_loruna_01_action_signalRepeaterClearQuest (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "ac_ongoing_loruna_clear_quest");
}

// ----------------------------------------------------------------------

void naboo_24_azure_cabal_loruna_01_action_signalRepeaterNewQuestReceived (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "ac_ongoing_loruna_quest_received");
}

// ----------------------------------------------------------------------

void naboo_24_azure_cabal_loruna_01_action_signalRepeaterQuestComplete (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "ac_ongoing_loruna_quest_complete");
}

// ----------------------------------------------------------------------

void naboo_24_azure_cabal_loruna_01_action_grantRepeaterEnforcement (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "ac_ongoing_loruna_quest_received");
	groundquests.clearQuest(player, "quest/ac_ongoing_launchpad_loruna_repeater");
	groundquests.grantQuest(player, "quest/ac_repeater_loruna_enf_30");
}

// ----------------------------------------------------------------------

void naboo_24_azure_cabal_loruna_01_action_grantRepeaterAdmin (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	groundquests.sendSignal(player, "Loruna25ChoiceAdmin");
	groundquests.sendSignal(player, "ac_ongoing_loruna_quest_received");
	groundquests.grantQuest(player, "quest/ac_repeater_loruna_enf_30");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

string naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (obj_id player, obj_id npc)
{
    string name = getFirstName(player);
	return name;
}

// ----------------------------------------------------------------------

string naboo_24_azure_cabal_loruna_01_tokenTO_playersProfession (obj_id player, obj_id npc)
{
	return new string();
}

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int naboo_24_azure_cabal_loruna_01_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I have identified an assignment that you qualify for. We need someone to serve as Azure Cabal's enforcer immediately. Are you ready?

	//-- [RESPONSE NOTE] Eventually, I must change the action to include a switch to choose between multiple level tiers. For now, just assigning Level 30 enforcement quest.
	//-- PLAYER: Yes. 
	if (response == "s_164")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			naboo_24_azure_cabal_loruna_01_action_grantRepeaterEnforcement (player, npc);

			//-- NPC: Just meet my operative at these coordinates. You will receive more information at that time.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you mean by enforcement?
	if (response == "s_124")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certain groups have become jealous of our success and are moving against us. We need to deter them from thinking they can steal our business.
			string_id message = new string_id (c_stringFile, "s_127");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Got it. Upload the data.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do I need to do?
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_129");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Actually, I don't want to work for you anymore.
	if (response == "s_152")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you certain you wish to leave the Azure Cabal?
			string_id message = new string_id (c_stringFile, "s_165");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Actually, no.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_166");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_167");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certain groups have become jealous of our success and are moving against us. We need to deter them from thinking they can steal our business.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Got it. Upload the data.
	if (response == "s_129")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			naboo_24_azure_cabal_loruna_01_action_grantRepeaterEnforcement (player, npc);

			//-- NPC: Just meet my operative at these coordinates. You will receive more information at that time.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I need to do?
	if (response == "s_133")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I know that there is an operation in the works against us. Intel suggests that the rally point is at a specific location.
			string_id message = new string_id (c_stringFile, "s_136");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do when I get there?
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_138");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I know that there is an operation in the works against us. Intel suggests that the rally point is at a specific location.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I do when I get there?
	if (response == "s_138")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			naboo_24_azure_cabal_loruna_01_action_grantRepeaterEnforcement (player, npc);

			//-- NPC: Just meet my operative at these coordinates. You will receive more information at that time.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you certain you wish to leave the Azure Cabal?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_166")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			naboo_24_azure_cabal_loruna_01_action_signalRepeaterClearQuest (player, npc);

			//-- NPC: That's too bad. Perhaps you will change your mind.
			string_id message = new string_id (c_stringFile, "s_170");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Actually, no.
	if (response == "s_167")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Make up your mind and talk to me again later.
			string_id message = new string_id (c_stringFile, "s_168");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're Back? Excellent. I hope you're here to work.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_150")
	{
		naboo_24_azure_cabal_loruna_01_action_grantLaunchpadRepeater (player, npc);

		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I will begin searching for assignments that match your particular talents. I will notify you when I have found something.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why don't you have something for me now?
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me know when you find something.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet.
	if (response == "s_157")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Pity. Come back when you are.
			string_id message = new string_id (c_stringFile, "s_161");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well done, %TO. It appears that you have impressed each member of my inner circle. Do you wish to become a member of the Azure Cabal?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_101")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I will begin searching for assignments that match your particular talents. I will notify you when I have found something.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why don't you have something for me now?
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me know when you find something.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_105");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now.
	if (response == "s_102")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: The invitation stands. Return when you are ready.
			string_id message = new string_id (c_stringFile, "s_107");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I will begin searching for assignments that match your particular talents. I will notify you when I have found something.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why don't you have something for me now?
	if (response == "s_105")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: We maintain a 99% success rate by carefully matching the assignment to the operative. With your skills, it should not take long. I will contact you.
			string_id message = new string_id (c_stringFile, "s_108");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let me know when you find something.
	if (response == "s_106")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I will send a comm signal when I do.
			string_id message = new string_id (c_stringFile, "s_109");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I have much to do, %TO. Can't this wait?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need directions.
	if (response == "s_90")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Who are you trying to find?
			string_id message = new string_id (c_stringFile, "s_99");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Broma Lak
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Porgryn
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tweego
			boolean hasResponse2 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Propti Xaroon
			boolean hasResponse3 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_115");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_117");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_118");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine.
	if (response == "s_91")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Contact me when you have acquired endorsements from my friends.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Who are you trying to find?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Broma Lak
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I have Broma monitoring who arrives and departs from one of the shuttleports in Theed.
			string_id message = new string_id (c_stringFile, "s_120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm on my way.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_126");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_128");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Porgryn
	if (response == "s_115")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Porgryn is on assignment in Wayfar on Tatooine.
			string_id message = new string_id (c_stringFile, "s_121");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No, I'm on my way.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_131");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_132");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tweego
	if (response == "s_117")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Tweego is stationed near the starport in Mos Entha on Tatooine.
			string_id message = new string_id (c_stringFile, "s_122");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: That's all I needed.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_134");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Propti Xaroon
	if (response == "s_118")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Propti Xaroon is brokering information for me near a cantina in Mos Espa on Tatooine.
			string_id message = new string_id (c_stringFile, "s_123");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Not right now.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_137");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_139");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I have Broma monitoring who arrives and departs from one of the shuttleports in Theed.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_126")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Who are you trying to find?
			string_id message = new string_id (c_stringFile, "s_99");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Broma Lak
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Porgryn
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tweego
			boolean hasResponse2 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Propti Xaroon
			boolean hasResponse3 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_115");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_117");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_118");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way.
	if (response == "s_128")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Contact me when you have acquired endorsements from my friends.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Porgryn is on assignment in Wayfar on Tatooine.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_131")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Who are you trying to find?
			string_id message = new string_id (c_stringFile, "s_99");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Broma Lak
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Porgryn
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tweego
			boolean hasResponse2 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Propti Xaroon
			boolean hasResponse3 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_115");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_117");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_118");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I'm on my way.
	if (response == "s_132")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Contact me when you have acquired endorsements from my friends.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Tweego is stationed near the starport in Mos Entha on Tatooine.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_134")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Who are you trying to find?
			string_id message = new string_id (c_stringFile, "s_99");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Broma Lak
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Porgryn
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tweego
			boolean hasResponse2 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Propti Xaroon
			boolean hasResponse3 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_115");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_117");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_118");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's all I needed.
	if (response == "s_135")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Contact me when you have acquired endorsements from my friends.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Propti Xaroon is brokering information for me near a cantina in Mos Espa on Tatooine.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_137")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Who are you trying to find?
			string_id message = new string_id (c_stringFile, "s_99");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Broma Lak
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Porgryn
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tweego
			boolean hasResponse2 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Propti Xaroon
			boolean hasResponse3 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_115");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_117");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_118");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now.
	if (response == "s_139")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Contact me when you have acquired endorsements from my friends.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've seen you pass through here, %TO, and asked around. If you are serious about making credits and advancing your skills, I may be able to help you. Are you interested?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Less talk, more action. 
	if (response == "s_60")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			naboo_24_azure_cabal_loruna_01_action_grantNaboo24AzureCabal01 (player, npc);

			//-- NPC: I trust very few. There are four beings whose opinion I value. I will contact each of them and alert them that you are interested in joining the Azure Cabal. If you can convince them of your value, I will gladly add you to our rolls.
			string_id message = new string_id (c_stringFile, "s_73");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm always looking for ways to turn a fast credit. What do you need done?
	if (response == "s_63")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must prove yourself capable, persistent and adaptable. My associates will test your abilities and endorse you if you succeed.
			string_id message = new string_id (c_stringFile, "s_65");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I don't work for free. Ever.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: First things first. Who are you?
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: My name is no secret. I am Loruna Scathe, founder of the Azure Cabal.
			string_id message = new string_id (c_stringFile, "s_82");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So how can I become a member of this Cabal?
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is the Azure Cabal?
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: On second thought...I have other things to do.
			boolean hasResponse2 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_86");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_154");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not really interested.  Sorry.
	if (response == "s_158")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, then, we have nothing to discuss.  Pity.
			string_id message = new string_id (c_stringFile, "s_160");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You must prove yourself capable, persistent and adaptable. My associates will test your abilities and endorse you if you succeed.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't work for free. Ever.
	if (response == "s_67")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Nor should you. You will be paid for services rendered during the assessment process. Now, are you interested or wasting my time?
			string_id message = new string_id (c_stringFile, "s_69");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah. What do you need done?
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Not really, no.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_71");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_75");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Nor should you. You will be paid for services rendered during the assessment process. Now, are you interested or wasting my time?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah. What do you need done?
	if (response == "s_71")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			naboo_24_azure_cabal_loruna_01_action_grantNaboo24AzureCabal01 (player, npc);

			//-- NPC: I trust very few. There are four beings whose opinion I value. I will contact each of them and alert them that you are interested in joining the Azure Cabal. If you can convince them of your value, I will gladly add you to our rolls.
			string_id message = new string_id (c_stringFile, "s_73");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not really, no.
	if (response == "s_75")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I understand. Return if you change your mind. You have potential.
			string_id message = new string_id (c_stringFile, "s_77");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: My name is no secret. I am Loruna Scathe, founder of the Azure Cabal.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So how can I become a member of this Cabal?
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must prove yourself capable, persistent and adaptable. My associates will test your abilities and endorse you if you succeed.
			string_id message = new string_id (c_stringFile, "s_65");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I don't work for free. Ever.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the Azure Cabal?
	if (response == "s_86")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are mercenaries that specialize in search, locate and annihilate missions.
			string_id message = new string_id (c_stringFile, "s_88");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why haven't I heard of you before?
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've heard enough. How do I join?
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_148");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: On second thought...I have other things to do.
	if (response == "s_154")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, then, we have nothing to discuss.  Pity.
			string_id message = new string_id (c_stringFile, "s_156");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are mercenaries that specialize in search, locate and annihilate missions.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why haven't I heard of you before?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: We do not advertise. Most of our jobs come from references and repeat customers. With a 99.26% mission success rate, we maintain a waiting list of interested clients.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So why should I join you and not one of the other mercenary groups?
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've heard enough. How do I join?
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_146");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've heard enough. How do I join?
	if (response == "s_148")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition_onTaskRepeaterCooldown (player, npc))
		{
			naboo_24_azure_cabal_loruna_01_action_facePlayer (player, npc);

			//-- NPC: I believe I have identified an appropriate assignment for you, %TO, but I am still awaiting confirmation of the contract. Please wait just a bit longer.
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition_onTaskRepeaterGetQuest (player, npc))
		{
			//-- NPC: I have identified an assignment that you qualify for. We need someone to serve as Azure Cabal's enforcer immediately. Are you ready?
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes. 
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you mean by enforcement?
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Actually, I don't want to work for you anymore.
			boolean hasResponse2 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_164");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_124");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_152");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition_hasCompletedAC_01_OnRepeater (player, npc))
		{
			//-- NPC: Complete your assignment and report back when you are done for further orders.
			string_id message = new string_id (c_stringFile, "s_155");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition_hasCompletedAC_01_NotOnRepeater (player, npc))
		{
			//-- NPC: You're Back? Excellent. I hope you're here to work.
			string_id message = new string_id (c_stringFile, "s_145");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Not yet.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_150");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_157");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition_onTaskAC_01_Return (player, npc))
		{
			doAnimationAction (npc, "bow");

			naboo_24_azure_cabal_loruna_01_action_signalCompleteAC01 (player, npc);

			//-- NPC: Well done, %TO. It appears that you have impressed each member of my inner circle. Do you wish to become a member of the Azure Cabal?
			string_id message = new string_id (c_stringFile, "s_61");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Not right now.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_102");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 12);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);
				pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);
				pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition_OnQuestAC_01 (player, npc))
		{
			//-- NPC: I have much to do, %TO. Can't this wait?
			string_id message = new string_id (c_stringFile, "s_81");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I need directions.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Fine.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_91");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 17);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);
				pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);
				pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition_canTakeQuestAC_01 (player, npc))
		{
			//-- NPC: I've seen you pass through here, %TO, and asked around. If you are serious about making credits and advancing your skills, I may be able to help you. Are you interested?
			string_id message = new string_id (c_stringFile, "s_58");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Less talk, more action. 
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm always looking for ways to turn a fast credit. What do you need done?
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: First things first. Who are you?
			boolean hasResponse2 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not really interested.  Sorry.
			boolean hasResponse3 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_63");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_158");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 24);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);
				pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);
				pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hush, now. Not here. Too many ears!
			string_id message = new string_id (c_stringFile, "s_162");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We do not advertise. Most of our jobs come from references and repeat customers. With a 99.26% mission success rate, we maintain a waiting list of interested clients.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So why should I join you and not one of the other mercenary groups?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you are serious about your career and earn your membership, you will not have to scrounge jobs as often as you have been. Our members spend their days earning credits or spending them...not wandering around cities begging for jobs.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've heard enough. How do I join?
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nah. Not interested.
			boolean hasResponse1 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_111");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_113");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've heard enough. How do I join?
	if (response == "s_146")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must prove yourself capable, persistent and adaptable. My associates will test your abilities and endorse you if you succeed.
			string_id message = new string_id (c_stringFile, "s_65");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I don't work for free. Ever.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_24_azure_cabal_loruna_01_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If you are serious about your career and earn your membership, you will not have to scrounge jobs as often as you have been. Our members spend their days earning credits or spending them...not wandering around cities begging for jobs.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've heard enough. How do I join?
	if (response == "s_111")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must prove yourself capable, persistent and adaptable. My associates will test your abilities and endorse you if you succeed.
			string_id message = new string_id (c_stringFile, "s_65");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I don't work for free. Ever.
			boolean hasResponse0 = false;
			if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67");

				utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nah. Not interested.
	if (response == "s_113")
	{
		//-- [NOTE] 
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, then, we have nothing to discuss.  Pity.
			string_id message = new string_id (c_stringFile, "s_144");
			utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isTangible (self)) || (isPlayer (self)))
		detachScript(self, "conversation.naboo_24_azure_cabal_loruna_01");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.naboo_24_azure_cabal_loruna_01");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (naboo_24_azure_cabal_loruna_01_condition_onTaskRepeaterCooldown (player, npc))
	{
		naboo_24_azure_cabal_loruna_01_action_facePlayer (player, npc);

		//-- NPC: I believe I have identified an appropriate assignment for you, %TO, but I am still awaiting confirmation of the contract. Please wait just a bit longer.
		string_id message = new string_id (c_stringFile, "s_110");
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

			chat.chat (npc, player, null, null, pp);
		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_24_azure_cabal_loruna_01_condition_onTaskRepeaterGetQuest (player, npc))
	{
		//-- NPC: I have identified an assignment that you qualify for. We need someone to serve as Azure Cabal's enforcer immediately. Are you ready?
		string_id message = new string_id (c_stringFile, "s_116");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes. 
		boolean hasResponse0 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What do you mean by enforcement?
		boolean hasResponse1 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Actually, I don't want to work for you anymore.
		boolean hasResponse2 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_164");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_124");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_152");

			utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 2);

			npcStartConversation (player, npc, "naboo_24_azure_cabal_loruna_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_24_azure_cabal_loruna_01_condition_hasCompletedAC_01_OnRepeater (player, npc))
	{
		//-- NPC: Complete your assignment and report back when you are done for further orders.
		string_id message = new string_id (c_stringFile, "s_155");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_24_azure_cabal_loruna_01_condition_hasCompletedAC_01_NotOnRepeater (player, npc))
	{
		//-- NPC: You're Back? Excellent. I hope you're here to work.
		string_id message = new string_id (c_stringFile, "s_145");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes.
		boolean hasResponse0 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not yet.
		boolean hasResponse1 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_150");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_157");

			utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 10);

			npcStartConversation (player, npc, "naboo_24_azure_cabal_loruna_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_24_azure_cabal_loruna_01_condition_onTaskAC_01_Return (player, npc))
	{
		doAnimationAction (npc, "bow");

		naboo_24_azure_cabal_loruna_01_action_signalCompleteAC01 (player, npc);

		//-- NPC: Well done, %TO. It appears that you have impressed each member of my inner circle. Do you wish to become a member of the Azure Cabal?
		string_id message = new string_id (c_stringFile, "s_61");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes.
		boolean hasResponse0 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not right now.
		boolean hasResponse1 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_101");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_102");

			utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 12);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

			npcStartConversation (player, npc, "naboo_24_azure_cabal_loruna_01", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_24_azure_cabal_loruna_01_condition_OnQuestAC_01 (player, npc))
	{
		//-- NPC: I have much to do, %TO. Can't this wait?
		string_id message = new string_id (c_stringFile, "s_81");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I need directions.
		boolean hasResponse0 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Fine.
		boolean hasResponse1 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_90");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_91");

			utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 17);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

			npcStartConversation (player, npc, "naboo_24_azure_cabal_loruna_01", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_24_azure_cabal_loruna_01_condition_canTakeQuestAC_01 (player, npc))
	{
		//-- NPC: I've seen you pass through here, %TO, and asked around. If you are serious about making credits and advancing your skills, I may be able to help you. Are you interested?
		string_id message = new string_id (c_stringFile, "s_58");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Less talk, more action. 
		boolean hasResponse0 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm always looking for ways to turn a fast credit. What do you need done?
		boolean hasResponse1 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: First things first. Who are you?
		boolean hasResponse2 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Not really interested.  Sorry.
		boolean hasResponse3 = false;
		if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_60");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_63");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_79");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_158");

			utils.setScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId", 24);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

			npcStartConversation (player, npc, "naboo_24_azure_cabal_loruna_01", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (naboo_24_azure_cabal_loruna_01_tokenTO_playerFirstName (player, npc));

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_24_azure_cabal_loruna_01_condition__defaultCondition (player, npc))
	{
		//-- NPC: Hush, now. Not here. Too many ears!
		string_id message = new string_id (c_stringFile, "s_162");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "naboo_24_azure_cabal_loruna_01")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

	if (branchId == 2 && naboo_24_azure_cabal_loruna_01_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && naboo_24_azure_cabal_loruna_01_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && naboo_24_azure_cabal_loruna_01_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && naboo_24_azure_cabal_loruna_01_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && naboo_24_azure_cabal_loruna_01_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && naboo_24_azure_cabal_loruna_01_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && naboo_24_azure_cabal_loruna_01_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && naboo_24_azure_cabal_loruna_01_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && naboo_24_azure_cabal_loruna_01_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && naboo_24_azure_cabal_loruna_01_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && naboo_24_azure_cabal_loruna_01_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && naboo_24_azure_cabal_loruna_01_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && naboo_24_azure_cabal_loruna_01_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && naboo_24_azure_cabal_loruna_01_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && naboo_24_azure_cabal_loruna_01_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && naboo_24_azure_cabal_loruna_01_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && naboo_24_azure_cabal_loruna_01_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && naboo_24_azure_cabal_loruna_01_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && naboo_24_azure_cabal_loruna_01_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && naboo_24_azure_cabal_loruna_01_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.naboo_24_azure_cabal_loruna_01.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

