// ======================================================================
//
// static_quests_record_keeper_dantooine.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/static_quests_record_keeper_dantooine";

// ======================================================================
// Script Conditions
// ======================================================================

boolean static_quests_record_keeper_dantooine_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean static_quests_record_keeper_dantooine_condition_static_quests_on_player (obj_id player, obj_id npc)
{
	string datatable = "datatables/npc/static_quest/static_quest_records_dantooine.iff";
	string[] static_quest = dataTableGetStringColumn(datatable, "static_quest");
	
	//debugSpeakMsg (npc, "datatable is " + datatable);
	//debugSpeakMsg (npc, "static_quest is " + static_quest);

	for (int i = 0; i < static_quest.length; i++)
	{
		if(hasObjVar(player, "static." + static_quest[i]))
			return true;
	}
	
	return false;
}

// ----------------------------------------------------------------------

boolean static_quests_record_keeper_dantooine_condition_already_reset (obj_id player, obj_id npc)
{
	if(hasObjVar(player, "static_quests_reset.dantooine"))
	{
		return true;
	}
	
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void static_quests_record_keeper_dantooine_action_reset_gating (obj_id player, obj_id npc)
{
	setObjVar(player, "static_quests_reset.dantooine", true);

	string datatable = "datatables/npc/static_quest/static_quest_records_dantooine.iff";
	string[] static_quest = dataTableGetStringColumn(datatable, "static_quest");

	for (int i = 0; i < static_quest.length; i++)
	{
		if(hasObjVar(player, "static." + static_quest[i]))
		{
			removeObjVar(player, "static." + static_quest[i]);
		}
	}

	int gating = getIntObjVar(player, "static_quests_reset.dantooine");
	
	CustomerServiceLog("StaticQuestDantooine", "RESET OPTION: Static quests on Dantooine -- " + getName(player) + " (" + player + ") has opted to RESET his/her Dantooine static quest obj vars [" + gating + "] and START OVER");
}

// ----------------------------------------------------------------------

void static_quests_record_keeper_dantooine_action_set_redo (obj_id player, obj_id npc)
{
	setObjVar(player, "static_quests_reset.dantooine", true);
	
	CustomerServiceLog("StaticQuestDantooine", "RESET OPTION: Static quests on Dantooine -- " + getName(player) + " (" + player + ") has opted to NOT RESET his/her Dantooine static quest obj vars and CONTINUE");
}

// ----------------------------------------------------------------------

void static_quests_record_keeper_dantooine_action_face_to (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int static_quests_record_keeper_dantooine_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Greetings citizen. My records indicate that you have been assisting fellow citizens and officials around Tatooine. Unfortunately, our databank had a 'accident', and I need to ask you some questions to fill in the blanks.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What kind of questions?
	if (response == "s_f51b07ff")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: We believe the Rebel scum attacked our database with a virus. It erased a lot of the progress that adventurers like yourself have done for citizens on Tatooine.
			string_id message = new string_id (c_stringFile, "s_eef03120");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Go on.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've got to be going now.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edb238bc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't have time right now.
	if (response == "s_2e18346d")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should make time for a official of the Empire but I have a lot to do anyway so come back when you have time.
			string_id message = new string_id (c_stringFile, "s_4b10b47");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We believe the Rebel scum attacked our database with a virus. It erased a lot of the progress that adventurers like yourself have done for citizens on Tatooine.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Go on.
	if (response == "s_edb238bc")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: My job is to update these records to their former standard, if citizens so wish, or to blank them out completely and let them start over on a clean slate.
			string_id message = new string_id (c_stringFile, "s_cd1a9013");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see. How do we proceed?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've got to be going now.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12936cce");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_46");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've got to be going now.
	if (response == "s_50")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've finally met someone with less patience than me. Fine, move along.
			string_id message = new string_id (c_stringFile, "s_52");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: My job is to update these records to their former standard, if citizens so wish, or to blank them out completely and let them start over on a clean slate.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see. How do we proceed?
	if (response == "s_12936cce")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: I must recompile all of the data that was lost, if you so wish. For that reason, I must ask you some questions.
			string_id message = new string_id (c_stringFile, "s_a87d342e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will answer your questions.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've got to be going now.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9ef848f9");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_710595f4");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've got to be going now.
	if (response == "s_46")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've finally met someone with less patience than me. Fine, move along.
			string_id message = new string_id (c_stringFile, "s_48");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I must recompile all of the data that was lost, if you so wish. For that reason, I must ask you some questions.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will answer your questions.
	if (response == "s_9ef848f9")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you like, I can just erase your progress, you would save me a lot of time and you could go around and help citizens all over again, with them rewarding you appropriately. Otherwise, you can update my records with your old progress and go on as nothing happened. Remember that once you decide, you cannot change your mind. Shall I erase the information in your databank?
			string_id message = new string_id (c_stringFile, "s_884006f7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to erase all my data?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What if I decide to fill in the missing information in my file?
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, please reset my file.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: No, do not reset my file. I will update your records.
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I want to decide later.
			boolean hasResponse4 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b1f6ba02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e61fd069");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cc924cf1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_287908df");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6d50e348");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've got to be going now.
	if (response == "s_710595f4")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've finally met someone with less patience than me. Fine, move along.
			string_id message = new string_id (c_stringFile, "s_71a41175");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If you like, I can just erase your progress, you would save me a lot of time and you could go around and help citizens all over again, with them rewarding you appropriately. Otherwise, you can update my records with your old progress and go on as nothing happened. Remember that once you decide, you cannot change your mind. Shall I erase the information in your databank?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I decide to erase all my data?
	if (response == "s_b1f6ba02")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: If I reset your file, you will be able to go back and assist citizens on Tatooine again as you have done in the past. But don't worry, you will be rewarded, and it will in no way reduce your standing in the eyes of the public or any organizations you're assosciated with. Shall I reset your file?
			string_id message = new string_id (c_stringFile, "s_e8ad81f2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to continue my service?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, please reset my employment history.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, do not reset my history. I will continue my service.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Can I decide later?
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14693cf1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ff20b1f1");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e3c4a3d5");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bfd7fb6d");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I decide to fill in the missing information in my file?
	if (response == "s_e61fd069")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then you will just continue where you were. Any missions for citizens that you are currently doing will stay as they are and citizens you're already done helping, will have records of that. But remember that once you decide you cannot change your mind. Shall I reset your history?
			string_id message = new string_id (c_stringFile, "s_83c0df9e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to reset my history?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, please reset my employment history.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, do not reset my history. I will continue my service.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Can I decide later?
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a76f22bf");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_24");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_26");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, please reset my file.
	if (response == "s_cc924cf1")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Keep in mind that once I reset your file, it cannot be undone. Are you sure that you want me to blank out your databank?
			string_id message = new string_id (c_stringFile, "s_ab18ccf6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. It will be fun to help those citizens again.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0902f5e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1288f6ef");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, do not reset my file. I will update your records.
	if (response == "s_287908df")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember that once you decide to update your files and continue as normal, you cannot change your mind. Are you sure that you want me to update your record?
			string_id message = new string_id (c_stringFile, "s_3b67eeac");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. I want to continue where I left of.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5db2d1c9");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to decide later.
	if (response == "s_6d50e348")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are wasting my time citizen but fine, return to me when you have made your decision. Now move along.
			string_id message = new string_id (c_stringFile, "s_6de63882");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If I reset your file, you will be able to go back and assist citizens on Tatooine again as you have done in the past. But don't worry, you will be rewarded, and it will in no way reduce your standing in the eyes of the public or any organizations you're assosciated with. Shall I reset your file?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I decide to continue my service?
	if (response == "s_14693cf1")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then you will just continue where you were. Any missions for citizens that you are currently doing will stay as they are and citizens you're already done helping, will have records of that. But remember that once you decide you cannot change your mind. Shall I reset your history?
			string_id message = new string_id (c_stringFile, "s_83c0df9e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to reset my history?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, please reset my employment history.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, do not reset my history. I will continue my service.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Can I decide later?
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a76f22bf");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_24");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_26");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, please reset my employment history.
	if (response == "s_ff20b1f1")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Keep in mind that once I reset your file, it cannot be undone. Are you sure that you want me to blank out your databank?
			string_id message = new string_id (c_stringFile, "s_ab18ccf6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. It will be fun to help those citizens again.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0902f5e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1288f6ef");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, do not reset my history. I will continue my service.
	if (response == "s_e3c4a3d5")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember that once you decide to update your files and continue as normal, you cannot change your mind. Are you sure that you want me to update your record?
			string_id message = new string_id (c_stringFile, "s_3b67eeac");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. I want to continue where I left of.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5db2d1c9");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can I decide later?
	if (response == "s_bfd7fb6d")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are wasting my time citizen but fine, return to me when you have made your decision. Now move along.
			string_id message = new string_id (c_stringFile, "s_6de63882");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then you will just continue where you were. Any missions for citizens that you are currently doing will stay as they are and citizens you're already done helping, will have records of that. But remember that once you decide you cannot change your mind. Shall I reset your history?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I decide to reset my history?
	if (response == "s_a76f22bf")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: If I reset your file, you will be able to go back and assist citizens on Tatooine again as you have done in the past. But don't worry, you will be rewarded, and it will in no way reduce your standing in the eyes of the public or any organizations you're assosciated with. Shall I reset your file?
			string_id message = new string_id (c_stringFile, "s_e8ad81f2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to continue my service?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, please reset my employment history.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, do not reset my history. I will continue my service.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Can I decide later?
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14693cf1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ff20b1f1");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e3c4a3d5");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bfd7fb6d");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, please reset my employment history.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Keep in mind that once I reset your file, it cannot be undone. Are you sure that you want me to blank out your databank?
			string_id message = new string_id (c_stringFile, "s_ab18ccf6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. It will be fun to help those citizens again.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0902f5e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1288f6ef");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, do not reset my history. I will continue my service.
	if (response == "s_24")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember that once you decide to update your files and continue as normal, you cannot change your mind. Are you sure that you want me to update your record?
			string_id message = new string_id (c_stringFile, "s_3b67eeac");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. I want to continue where I left of.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5db2d1c9");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can I decide later?
	if (response == "s_26")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are wasting my time citizen but fine, return to me when you have made your decision. Now move along.
			string_id message = new string_id (c_stringFile, "s_6de63882");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Keep in mind that once I reset your file, it cannot be undone. Are you sure that you want me to blank out your databank?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: YES, I am sure. It will be fun to help those citizens again.
	if (response == "s_d0902f5e")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			static_quests_record_keeper_dantooine_action_reset_gating (player, npc);

			//-- NPC: Very well citizen, it is done. Thank you for saving me the time to update your records. Now move along.
			string_id message = new string_id (c_stringFile, "s_ae08bd89");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: NO, wait. Let me think about it for a second.
	if (response == "s_1288f6ef")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Very well. But do not take long, my time is valuable.
			string_id message = new string_id (c_stringFile, "s_3d27cb3d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to erase all my data?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What if I decide to fill in the missing information in my file?
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, please reset my file.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: No, do not reset my file. I will update your records.
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I want to decide later.
			boolean hasResponse4 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b1f6ba02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e61fd069");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cc924cf1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_287908df");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6d50e348");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well. But do not take long, my time is valuable.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I decide to erase all my data?
	if (response == "s_b1f6ba02")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: If I reset your file, you will be able to go back and assist citizens on Tatooine again as you have done in the past. But don't worry, you will be rewarded, and it will in no way reduce your standing in the eyes of the public or any organizations you're assosciated with. Shall I reset your file?
			string_id message = new string_id (c_stringFile, "s_e8ad81f2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to continue my service?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, please reset my employment history.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, do not reset my history. I will continue my service.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Can I decide later?
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14693cf1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ff20b1f1");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e3c4a3d5");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bfd7fb6d");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I decide to fill in the missing information in my file?
	if (response == "s_e61fd069")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then you will just continue where you were. Any missions for citizens that you are currently doing will stay as they are and citizens you're already done helping, will have records of that. But remember that once you decide you cannot change your mind. Shall I reset your history?
			string_id message = new string_id (c_stringFile, "s_83c0df9e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to reset my history?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, please reset my employment history.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, do not reset my history. I will continue my service.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Can I decide later?
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a76f22bf");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_24");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_26");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, please reset my file.
	if (response == "s_cc924cf1")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Keep in mind that once I reset your file, it cannot be undone. Are you sure that you want me to blank out your databank?
			string_id message = new string_id (c_stringFile, "s_ab18ccf6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. It will be fun to help those citizens again.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0902f5e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1288f6ef");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, do not reset my file. I will update your records.
	if (response == "s_287908df")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember that once you decide to update your files and continue as normal, you cannot change your mind. Are you sure that you want me to update your record?
			string_id message = new string_id (c_stringFile, "s_3b67eeac");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. I want to continue where I left of.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5db2d1c9");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to decide later.
	if (response == "s_6d50e348")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are wasting my time citizen but fine, return to me when you have made your decision. Now move along.
			string_id message = new string_id (c_stringFile, "s_6de63882");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Remember that once you decide to update your files and continue as normal, you cannot change your mind. Are you sure that you want me to update your record?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: YES, I am sure. I want to continue where I left of.
	if (response == "s_5db2d1c9")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			static_quests_record_keeper_dantooine_action_set_redo (player, npc);

			//-- NPC: Very well, it is done. Thank you for updating my records citizen. Now move along.
			string_id message = new string_id (c_stringFile, "s_b8ea8b67");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: NO, wait. Let me think about it for a second.
	if (response == "s_38")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Very well. But do not take long, my time is valuable.
			string_id message = new string_id (c_stringFile, "s_40");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to erase all my data?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What if I decide to fill in the missing information in my file?
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes, please reset my file.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: No, do not reset my file. I will update your records.
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: I want to decide later.
			boolean hasResponse4 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b1f6ba02");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e61fd069");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cc924cf1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_287908df");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6d50e348");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_quests_record_keeper_dantooine_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well. But do not take long, my time is valuable.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I decide to erase all my data?
	if (response == "s_b1f6ba02")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: If I reset your file, you will be able to go back and assist citizens on Tatooine again as you have done in the past. But don't worry, you will be rewarded, and it will in no way reduce your standing in the eyes of the public or any organizations you're assosciated with. Shall I reset your file?
			string_id message = new string_id (c_stringFile, "s_e8ad81f2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to continue my service?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, please reset my employment history.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, do not reset my history. I will continue my service.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Can I decide later?
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_14693cf1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ff20b1f1");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e3c4a3d5");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bfd7fb6d");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I decide to fill in the missing information in my file?
	if (response == "s_e61fd069")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then you will just continue where you were. Any missions for citizens that you are currently doing will stay as they are and citizens you're already done helping, will have records of that. But remember that once you decide you cannot change your mind. Shall I reset your history?
			string_id message = new string_id (c_stringFile, "s_83c0df9e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What if I decide to reset my history?
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, please reset my employment history.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, do not reset my history. I will continue my service.
			boolean hasResponse2 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Can I decide later?
			boolean hasResponse3 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a76f22bf");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_22");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_24");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_26");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, please reset my file.
	if (response == "s_cc924cf1")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Keep in mind that once I reset your file, it cannot be undone. Are you sure that you want me to blank out your databank?
			string_id message = new string_id (c_stringFile, "s_ab18ccf6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. It will be fun to help those citizens again.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0902f5e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1288f6ef");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, do not reset my file. I will update your records.
	if (response == "s_287908df")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: Remember that once you decide to update your files and continue as normal, you cannot change your mind. Are you sure that you want me to update your record?
			string_id message = new string_id (c_stringFile, "s_3b67eeac");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: YES, I am sure. I want to continue where I left of.
			boolean hasResponse0 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: NO, wait. Let me think about it for a second.
			boolean hasResponse1 = false;
			if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5db2d1c9");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_38");

				utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to decide later.
	if (response == "s_6d50e348")
	{
		//-- [NOTE] 
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			//-- NPC: You are wasting my time citizen but fine, return to me when you have made your decision. Now move along.
			string_id message = new string_id (c_stringFile, "s_6de63882");
			utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.static_quests_record_keeper_dantooine");
	
	setInvulnerable (self, true);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setInvulnerable (self, true);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.static_quests_record_keeper_dantooine");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (static_quests_record_keeper_dantooine_condition_already_reset (player, npc))
	{
		static_quests_record_keeper_dantooine_action_face_to (player, npc);

		//-- NPC: I think we are done with each other citizen. I explained that you couldn't change your mind. Now move along.
		string_id message = new string_id (c_stringFile, "s_32bfa453");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (static_quests_record_keeper_dantooine_condition_static_quests_on_player (player, npc))
	{
		static_quests_record_keeper_dantooine_action_face_to (player, npc);

		//-- NPC: Greetings citizen. My records indicate that you have been assisting fellow citizens and officials around Tatooine. Unfortunately, our databank had a 'accident', and I need to ask you some questions to fill in the blanks.
		string_id message = new string_id (c_stringFile, "s_5c12e6ea");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What kind of questions?
		boolean hasResponse0 = false;
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I don't have time right now.
		boolean hasResponse1 = false;
		if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f51b07ff");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2e18346d");

			utils.setScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId", 2);

			npcStartConversation (player, npc, "static_quests_record_keeper_dantooine", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (static_quests_record_keeper_dantooine_condition__defaultCondition (player, npc))
	{
		static_quests_record_keeper_dantooine_action_face_to (player, npc);

		//-- NPC: Citizen, I'm a very busy person. Please move on.
		string_id message = new string_id (c_stringFile, "s_9413f709");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "static_quests_record_keeper_dantooine")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

	if (branchId == 2 && static_quests_record_keeper_dantooine_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && static_quests_record_keeper_dantooine_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && static_quests_record_keeper_dantooine_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && static_quests_record_keeper_dantooine_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && static_quests_record_keeper_dantooine_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && static_quests_record_keeper_dantooine_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && static_quests_record_keeper_dantooine_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && static_quests_record_keeper_dantooine_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && static_quests_record_keeper_dantooine_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && static_quests_record_keeper_dantooine_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && static_quests_record_keeper_dantooine_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.static_quests_record_keeper_dantooine.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

