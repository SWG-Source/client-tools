// ======================================================================
//
// tatooine_eisley_tori.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.37 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.conversation;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/tatooine_eisley_tori";

// ======================================================================
// Script Conditions
// ======================================================================

boolean tatooine_eisley_tori_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_tori_condition_boneOnTask (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	int questId1 = questGetQuestId("quest/tatooine_eisley_bone_gnasher");
	int tat_eisley_bone_gnasher_e1 = groundquests.getTaskId(questId1, "tat_eisley_bone_gnasher_e1"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_eisley_bone_gnasher_e1, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_tori_condition_boneQuestComplete (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	return groundquests.hasCompletedQuest(player, "quest/tatooine_eisley_bone_gnasher");
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_tori_condition_boneTaskComplete (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	int questId1 = questGetQuestId("quest/tatooine_eisley_bone_gnasher");
	int tat_eisley_bone_gnasher_e2 = groundquests.getTaskId(questId1, "tat_eisley_bone_gnasher_e2"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_eisley_bone_gnasher_e2, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_tori_condition_diQuestComplete (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tatooine_eisley_digital_infection");
	
	boolean OnTask = 	(questIsQuestComplete(questId1, player));
	
	return OnTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_tori_condition_gotoToriOnTask (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	int questId1 = questGetQuestId("quest/tatooine_eisley_gototori");
	int tat_reimos_tori_e1 = groundquests.getTaskId(questId1, "tat_reimos_tori_e1"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_reimos_tori_e1, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_tori_condition_gotoToriComplete (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	int questId1 = questGetQuestId("quest/tatooine_eisley_gototori");
	
	boolean OnTask = 	(questIsQuestComplete(questId1, player));
	
	return OnTask;
}

// ----------------------------------------------------------------------

boolean tatooine_eisley_tori_condition_facePlayer2 (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	return true;
}

// ======================================================================
// Script Actions
// ======================================================================

void tatooine_eisley_tori_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ----------------------------------------------------------------------

void tatooine_eisley_tori_action_grantBoneQuest (obj_id player, obj_id npc)
{
    int questId = questGetQuestId("quest/tatooine_eisley_bone_gnasher");
    groundquests.grantQuest(questId, player, npc, true);

}

// ----------------------------------------------------------------------

void tatooine_eisley_tori_action_signalBoneReward (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_eisley_bone_gnasher_e2");
}

// ----------------------------------------------------------------------

void tatooine_eisley_tori_action_signalGotoTori (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_reimos_tori_e1");
}

// ----------------------------------------------------------------------

void tatooine_eisley_tori_action_grantGotoReimos (obj_id player, obj_id npc)
{
	if(!groundquests.isQuestActiveOrComplete(player, "quest/tatooine_eisley_returnreimos"))
    	groundquests.grantQuest(player, "quest/tatooine_eisley_returnreimos");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int tatooine_eisley_tori_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I really have no time to talk to you any more. If you really need work see Reimos. I heard he has been looking for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ok, on my way.
	if (response == "s_126")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_tori_action_grantGotoReimos (player, npc);

			//-- NPC: Bye.
			string_id message = new string_id (c_stringFile, "s_127");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, these are fantastic. What beautiful specimens. Look at the saxitoxin glisten on the injector nodes. This will be very potent.  

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm just interested in getting paid.
	if (response == "s_62")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_tori_action_signalBoneReward (player, npc);

			//-- NPC: Yes, yes. Here you go. Now please be off. Bib says Reimos is looking for you.
			string_id message = new string_id (c_stringFile, "s_64");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't kill anyone I know.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_66");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't want to hear about another tool for assassins.
	if (response == "s_120")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Why not? I look at it this way; the more they kill each other the less there will be to bother the rest of us.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure, and what about the innocent targets that get in the way?
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, yes. Here you go. Now please be off. Bib says Reimos is looking for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't kill anyone I know.
	if (response == "s_66")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Don't worry, they won't feel a thing.
			string_id message = new string_id (c_stringFile, "s_84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Very funny.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_86");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't worry, they won't feel a thing.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very funny.
	if (response == "s_86")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_tori_action_grantGotoReimos (player, npc);

			//-- NPC: Are you still here?
			string_id message = new string_id (c_stringFile, "s_88");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why not? I look at it this way; the more they kill each other the less there will be to bother the rest of us.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure, and what about the innocent targets that get in the way?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: What's the phrase? To cure a cancer a doctor must cut away good tissue?
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That's a harsh look on life.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What's the phrase? To cure a cancer a doctor must cut away good tissue?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's a harsh look on life.
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed. One day I may have to face the bitter end of that opinion, but not today.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm done with you.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed. One day I may have to face the bitter end of that opinion, but not today.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm done with you.
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_tori_action_signalBoneReward (player, npc);

			//-- NPC: Here's your money. Bib says you need to talk to Reimos again.
			string_id message = new string_id (c_stringFile, "s_107");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: On my way.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_40");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Here's your money. Bib says you need to talk to Reimos again.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: On my way.
	if (response == "s_40")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_tori_action_grantGotoReimos (player, npc);

			//-- NPC: Are you still here?
			string_id message = new string_id (c_stringFile, "s_41");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Have you secured those glands yet?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, not yet.
	if (response == "s_116")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please hurry, Bib is very interested in this.
			string_id message = new string_id (c_stringFile, "s_117");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm on my way.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_118");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please hurry, Bib is very interested in this.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way.
	if (response == "s_118")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Thank you.
			string_id message = new string_id (c_stringFile, "s_119");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What else do you need?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need the information on the Bone Gnashers again.
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: I need you to go into a cave nearby and find creatures called Bone Gnashers. 
			string_id message = new string_id (c_stringFile, "s_46");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you need those for?
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_75");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I need you to go into a cave nearby and find creatures called Bone Gnashers. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you need those for?
	if (response == "s_75")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: They are a transplanted breed of arachne from Endor. They have a type of poison I think I can synthesize for use by Jabba.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why does Jabba need poison?
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Arachne? What's that?
			boolean hasResponse1 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So Bib thinks you can get into the cave with those cultists and get me the poison glands I need?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Apparently. I wasn't told what I need to do for you.
	if (response == "s_71")
	{
		tatooine_eisley_tori_action_signalGotoTori (player, npc);

		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, I'm glad he sent me someone prepared. Look, I need you to go into a cave nearby and find creatures called Bone Gnashers. 
			string_id message = new string_id (c_stringFile, "s_73");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you need those for?
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_75");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, I'm glad he sent me someone prepared. Look, I need you to go into a cave nearby and find creatures called Bone Gnashers. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you need those for?
	if (response == "s_75")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: They are a transplanted breed of arachne from Endor. They have a type of poison I think I can synthesize for use by Jabba.
			string_id message = new string_id (c_stringFile, "s_77");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why does Jabba need poison?
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Arachne? What's that?
			boolean hasResponse1 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They are a transplanted breed of arachne from Endor. They have a type of poison I think I can synthesize for use by Jabba.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why does Jabba need poison?
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Jabba doesn't need it for himself. I am sure he can find a use for it though.
			string_id message = new string_id (c_stringFile, "s_81");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I can get them, but I expect to be well paid.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Arachne? What's that?
	if (response == "s_50")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Know what a spider is?
			string_id message = new string_id (c_stringFile, "s_51");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_53");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Jabba doesn't need it for himself. I am sure he can find a use for it though.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can get them, but I expect to be well paid.
	if (response == "s_55")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			tatooine_eisley_tori_action_grantBoneQuest (player, npc);

			//-- NPC: This is the cave location. Remember, I need six intact glands. Anything less will not be enough material for the synthesis.
			string_id message = new string_id (c_stringFile, "s_57");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Gotcha.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This is the cave location. Remember, I need six intact glands. Anything less will not be enough material for the synthesis.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Gotcha.
	if (response == "s_59")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Thanks.
			string_id message = new string_id (c_stringFile, "s_61");
			utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Know what a spider is?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah.
	if (response == "s_53")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Same thing.
			string_id message = new string_id (c_stringFile, "s_56");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why does Jabba need poison?
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tatooine_eisley_tori_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Same thing.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why does Jabba need poison?
	if (response == "s_60")
	{
		//-- [NOTE] 
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			//-- NPC: Jabba doesn't need it for himself. I am sure he can find a use for it though.
			string_id message = new string_id (c_stringFile, "s_81");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I can get them, but I expect to be well paid.
			boolean hasResponse0 = false;
			if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55");

				utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.tatooine_eisley_tori");

    setCondition (self, CONDITION_CONVERSABLE);
    setCondition (self, CONDITION_INTERESTING);
    setInvulnerable (self, true);

    setName (self, "Tori Radeen");

    ai_lib.setDefaultCalmBehavior( self, ai_lib.BEHAVIOR_SENTINEL );

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
    setCondition (self, CONDITION_CONVERSABLE);
    setCondition (self, CONDITION_INTERESTING);
    setInvulnerable (self, true);

    setName (self, "Tori Radeen");

    ai_lib.setDefaultCalmBehavior( self, ai_lib.BEHAVIOR_SENTINEL );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.tatooine_eisley_tori");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (tatooine_eisley_tori_condition_boneQuestComplete (player, npc))
	{
		tatooine_eisley_tori_action_facePlayer (player, npc);

		//-- NPC: I really have no time to talk to you any more. If you really need work see Reimos. I heard he has been looking for you.
		string_id message = new string_id (c_stringFile, "s_115");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Ok, on my way.
		boolean hasResponse0 = false;
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_126");

			utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 1);

			npcStartConversation (player, npc, "tatooine_eisley_tori", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_tori_condition_boneTaskComplete (player, npc))
	{
		tatooine_eisley_tori_action_facePlayer (player, npc);

		//-- NPC: Yes, these are fantastic. What beautiful specimens. Look at the saxitoxin glisten on the injector nodes. This will be very potent.  
		string_id message = new string_id (c_stringFile, "s_114");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm just interested in getting paid.
		boolean hasResponse0 = false;
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I don't want to hear about another tool for assassins.
		boolean hasResponse1 = false;
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_62");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_120");

			utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 3);

			npcStartConversation (player, npc, "tatooine_eisley_tori", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_tori_condition_boneOnTask (player, npc))
	{
		tatooine_eisley_tori_action_facePlayer (player, npc);

		//-- NPC: Have you secured those glands yet?
		string_id message = new string_id (c_stringFile, "s_113");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No, not yet.
		boolean hasResponse0 = false;
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_116");

			utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 12);

			npcStartConversation (player, npc, "tatooine_eisley_tori", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_tori_condition_gotoToriComplete (player, npc))
	{
		//-- NPC: What else do you need?
		string_id message = new string_id (c_stringFile, "s_44");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I need the information on the Bone Gnashers again.
		boolean hasResponse0 = false;
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_45");

			utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 15);

			npcStartConversation (player, npc, "tatooine_eisley_tori", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_tori_condition_gotoToriOnTask (player, npc))
	{
		tatooine_eisley_tori_action_facePlayer (player, npc);

		//-- NPC: So Bib thinks you can get into the cave with those cultists and get me the poison glands I need?
		string_id message = new string_id (c_stringFile, "s_32");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Apparently. I wasn't told what I need to do for you.
		boolean hasResponse0 = false;
		if (tatooine_eisley_tori_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_71");

			utils.setScriptVar (player, "conversation.tatooine_eisley_tori.branchId", 17);

			npcStartConversation (player, npc, "tatooine_eisley_tori", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tatooine_eisley_tori_condition_facePlayer2 (player, npc))
	{
		//-- NPC: I've got no reason to talk to you right now.
		string_id message = new string_id (c_stringFile, "s_65");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "tatooine_eisley_tori")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

	if (branchId == 1 && tatooine_eisley_tori_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && tatooine_eisley_tori_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && tatooine_eisley_tori_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && tatooine_eisley_tori_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && tatooine_eisley_tori_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && tatooine_eisley_tori_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && tatooine_eisley_tori_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && tatooine_eisley_tori_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && tatooine_eisley_tori_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && tatooine_eisley_tori_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && tatooine_eisley_tori_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && tatooine_eisley_tori_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && tatooine_eisley_tori_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && tatooine_eisley_tori_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && tatooine_eisley_tori_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && tatooine_eisley_tori_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && tatooine_eisley_tori_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && tatooine_eisley_tori_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && tatooine_eisley_tori_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.tatooine_eisley_tori.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

