// ======================================================================
//
// emp_day_imperial_commander.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.31 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/emp_day_imperial_commander";

// ======================================================================
// Script Conditions
// ======================================================================

boolean emp_day_imperial_commander_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_isRebel (obj_id player, obj_id npc)
{
	int factionHashCode = factions.pvpGetAlignedFaction(player);
	string whichFaction = factions.getFactionNameByHashCode(factionHashCode);

	if (whichFaction==null)
		return false;

	if (whichFaction == "Rebel")
		return true;

	return false;
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_notReadyForYou (obj_id player, obj_id npc)
{
	int questId = questGetQuestId("quest/emp_day_imperial");
	int task1 = groundquests.getTaskId(questId, "converseHistorian");
	int task2 = groundquests.getTaskId(questId, "killRebels");
	int task3a = groundquests.getTaskId(questId, "diskGuy1");
	int task3b = groundquests.getTaskId(questId, "diskGuy2");
	int task3c = groundquests.getTaskId(questId, "diskGuy3");
	int task4 = groundquests.getTaskId(questId, "disksToHistorian");
	int task5 = groundquests.getTaskId(questId, "toDataOfficer");

	return (questIsTaskActive(questId, task1, player) ||
	questIsTaskActive(questId, task2, player) ||
	questIsTaskActive(questId, task3a, player) || 
	questIsTaskActive(questId, task3b, player) || 
	questIsTaskActive(questId, task3c, player) || 
	questIsTaskActive(questId, task4, player) ||
	questIsTaskActive(questId, task5, player) ||
	!questIsQuestActive(questId, player) );
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_questCompleted (obj_id player, obj_id npc)
{
	int questId = questGetQuestId("quest/emp_day_imperial");
	return questIsQuestComplete(questId, player);
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_hasTask6 (obj_id player, obj_id npc)
{
	int questId = questGetQuestId("quest/emp_day_imperial");
	int task6 = groundquests.getTaskId(questId, "toCommander");
	int task7 = groundquests.getTaskId(questId, "toVader");

	int factionHashCode = factions.pvpGetAlignedFaction(player);
	string whichFaction = factions.getFactionNameByHashCode(factionHashCode);

	return (questIsTaskActive(questId, task6, player)	||
	(questIsTaskActive(questId, task7, player) && whichFaction == null) );
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_isNeutral (obj_id player, obj_id npc)
{
	int factionHashCode = factions.pvpGetAlignedFaction(player);
	string whichFaction = factions.getFactionNameByHashCode(factionHashCode);

	if (whichFaction==null)
		return true;

	return false;
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_hasTask8 (obj_id player, obj_id npc)
{
	int questId = questGetQuestId("quest/emp_day_imperial");
	int task8 = groundquests.getTaskId(questId, "empDayComplete");
	return questIsTaskActive(questId, task8, player);
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_isImperial (obj_id player, obj_id npc)
{
	int factionHashCode = factions.pvpGetAlignedFaction(player);
	string whichFaction = factions.getFactionNameByHashCode(factionHashCode);

	if (whichFaction==null)
		return false;

	if (whichFaction == "Imperial")
		return true;

	return false;
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_hasTask7 (obj_id player, obj_id npc)
{
	int questId = questGetQuestId("quest/emp_day_imperial");
	int task7 = groundquests.getTaskId(questId, "toVader");
	int factionHashCode = factions.pvpGetAlignedFaction(player);
	string whichFaction = factions.getFactionNameByHashCode(factionHashCode);

	return (questIsTaskActive(questId, task7, player) && whichFaction == "Imperial");
}

// ----------------------------------------------------------------------

boolean emp_day_imperial_commander_condition_noPrisonAccess (obj_id player, obj_id npc)
{
	return (!hasObjVar(player, "event.emp_day.prisoner") );
}

// ======================================================================
// Script Actions
// ======================================================================

void emp_day_imperial_commander_action_rewardAndEndNow (obj_id player, obj_id npc)
{

	obj_id playerInventory = utils.getInventoryContainer(player);
	obj_id createdObject = createObject("object/tangible/furniture/all/bestine_quest_imp_banner.iff", playerInventory, "");

	if(isIdValid(createdObject))
	{	
		groundquests.sendSignal(player, "to_vader");
		groundquests.sendSignal(player, "emp_day_complete");
		removeObjVar(player, "empire_day.waypoint");
		setObjVar(player, "event.emp_day.rewarded", 1);
	}
	else
	{
		sendSystemMessage(player, new string_id("quest/lifeday/lifeday", "full_inv"));	
	}

}

// ----------------------------------------------------------------------

void emp_day_imperial_commander_action_givePrisonerAccess (obj_id player, obj_id npc)
{
	setObjVar(player, "event.emp_day.prisoner", 0);
}

// ----------------------------------------------------------------------

void emp_day_imperial_commander_action_signal6 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "to_commander");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int emp_day_imperial_commander_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Thanks for taking the time to help us out. Your dedication as a citizen of the Empire serves as a great example that others would do well to notice.

	//-- [RESPONSE NOTE] 
	//-- PLAYER:  What is Lord Vader's interest in this Jedi? 
	if (response == "s_367")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_givePrisonerAccess (player, npc);

			//-- NPC: He's being questioned in that small detainment facility. You can go take a look if you like. He's not much of a threat anymore.
			string_id message = new string_id (c_stringFile, "s_369");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Did you speak to him? He's an inspiration, that's for sure. I'm hoping some day to serve aboard his flagship. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've delivered the historian's disks. My work here is done.
	if (response == "s_373")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yes, yes. We'll get that data uploaded immediately. We may even show it to Vader himself. See what he has to say. What a momentous day for our outpost!
			string_id message = new string_id (c_stringFile, "s_375");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Do you have any other assignments for me?
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_377");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, yes. We'll get that data uploaded immediately. We may even show it to Vader himself. See what he has to say. What a momentous day for our outpost!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you have any other assignments for me?
	if (response == "s_377")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_rewardAndEndNow (player, npc);

			//-- NPC: No, we have it all under control. Here. Take this memento of your Empire Day meeting with Darth Vader! Thank you for your help today. I hope we meet again sometime.
			string_id message = new string_id (c_stringFile, "s_379");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Disks? I don't have time to upload them right now because Lord Vader is here! What an auspicious occasion.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Vader is here? Why?
	if (response == "s_385")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			//-- NPC: We captured a Rebel Jedi and Vader wants to question him personally. This is a huge opportunity for us all. And on Empire Day of all days!
			string_id message = new string_id (c_stringFile, "s_387");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Do you think I might be able to meet Lord Vader?
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER:  What is Lord Vader's interest in this Jedi? 
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition_noPrisonAccess (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_389");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_419");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to go. Sorry. 
	if (response == "s_427")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			//-- NPC: There's no place I'd rather be than in the presence of Lord Vader, but to each their own. Talk to me again if you change your mind.
			string_id message = new string_id (c_stringFile, "s_429");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We captured a Rebel Jedi and Vader wants to question him personally. This is a huge opportunity for us all. And on Empire Day of all days!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you think I might be able to meet Lord Vader?
	if (response == "s_389")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition_isImperial (player, npc))
		{
			//-- NPC: He's not taking visitors, but perhaps you could deliver this report to him.
			string_id message = new string_id (c_stringFile, "s_391");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll do it.
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I think I'll wait.
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_393");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_397");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (emp_day_imperial_commander_condition_isNeutral (player, npc))
		{
			//-- NPC: I couldn't allow someone who is not a member of the Imperial military inside. If you'd like to join up, I can see what I can do.
			string_id message = new string_id (c_stringFile, "s_401");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like that. How do I sign up?
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: The life of the Empire just isn't for me.
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_403");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_407");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER:  What is Lord Vader's interest in this Jedi? 
	if (response == "s_419")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			//-- NPC: We thought it might be Skywalker, but he won't talk. I'm sure Vader will find the truth. 
			string_id message = new string_id (c_stringFile, "s_421");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is this Jedi now?
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_423");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He's not taking visitors, but perhaps you could deliver this report to him.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll do it.
	if (response == "s_393")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_signal6 (player, npc);

			//-- NPC: Excellent. Deliver this report on the captured Jedi to him. You should feel fortunate you came today of all days.
			string_id message = new string_id (c_stringFile, "s_395");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I think I'll wait.
	if (response == "s_397")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			//-- NPC: These opportunities don't come up often, but ok. Come find me if you change your mind
			string_id message = new string_id (c_stringFile, "s_399");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I couldn't allow someone who is not a member of the Imperial military inside. If you'd like to join up, I can see what I can do.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like that. How do I sign up?
	if (response == "s_403")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_signal6 (player, npc);

			//-- NPC: Just speak to the Outpost Recruiter then come back and speak to me. Hurry though, Vader won't be here for long.
			string_id message = new string_id (c_stringFile, "s_405");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The life of the Empire just isn't for me.
	if (response == "s_407")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_signal6 (player, npc);

			//-- NPC: Are you sure? If you decide to not join, you will not be able to meet Darth Vader.
			string_id message = new string_id (c_stringFile, "s_409");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm sure. I don't want to join the Empire.
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me think about it some more,
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_411");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_415");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you sure? If you decide to not join, you will not be able to meet Darth Vader.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sure. I don't want to join the Empire.
	if (response == "s_411")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_rewardAndEndNow (player, npc);

			//-- NPC: Very well. You're missing an opportunity, but I understand. Here. Take this. Perhaps a constant reminder of the Empire in your home will sway you. Happy Empire Day to you, citizen.
			string_id message = new string_id (c_stringFile, "s_413");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let me think about it some more,
	if (response == "s_415")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, don't take too long. Lord Vader will not be here very long, and then your opportunity will be gone.
			string_id message = new string_id (c_stringFile, "s_417");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like that. How do I sign up?
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: The life of the Empire just isn't for me.
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_403");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_407");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, don't take too long. Lord Vader will not be here very long, and then your opportunity will be gone.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like that. How do I sign up?
	if (response == "s_403")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_signal6 (player, npc);

			//-- NPC: Just speak to the Outpost Recruiter then come back and speak to me. Hurry though, Vader won't be here for long.
			string_id message = new string_id (c_stringFile, "s_405");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The life of the Empire just isn't for me.
	if (response == "s_407")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_signal6 (player, npc);

			//-- NPC: Are you sure? If you decide to not join, you will not be able to meet Darth Vader.
			string_id message = new string_id (c_stringFile, "s_409");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm sure. I don't want to join the Empire.
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let me think about it some more,
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_411");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_415");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We thought it might be Skywalker, but he won't talk. I'm sure Vader will find the truth. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is this Jedi now?
	if (response == "s_423")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_givePrisonerAccess (player, npc);

			//-- NPC: He's being questioned in that small detainment facility. You can go take a look if you like. He's not much of a threat anymore.
			string_id message = new string_id (c_stringFile, "s_425");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Do you think I might be able to meet Lord Vader?
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER:  What is Lord Vader's interest in this Jedi? 
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition_noPrisonAccess (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_389");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_419");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He's being questioned in that small detainment facility. You can go take a look if you like. He's not much of a threat anymore.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you think I might be able to meet Lord Vader?
	if (response == "s_389")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition_isImperial (player, npc))
		{
			//-- NPC: He's not taking visitors, but perhaps you could deliver this report to him.
			string_id message = new string_id (c_stringFile, "s_391");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll do it.
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I think I'll wait.
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_393");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_397");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (emp_day_imperial_commander_condition_isNeutral (player, npc))
		{
			//-- NPC: I couldn't allow someone who is not a member of the Imperial military inside. If you'd like to join up, I can see what I can do.
			string_id message = new string_id (c_stringFile, "s_401");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd like that. How do I sign up?
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: The life of the Empire just isn't for me.
			boolean hasResponse1 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_403");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_407");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER:  What is Lord Vader's interest in this Jedi? 
	if (response == "s_419")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			//-- NPC: We thought it might be Skywalker, but he won't talk. I'm sure Vader will find the truth. 
			string_id message = new string_id (c_stringFile, "s_421");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where is this Jedi now?
			boolean hasResponse0 = false;
			if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_423");

				utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int emp_day_imperial_commander_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What are you doing here? You'd better deliver that report to Darth Vader immediately!

	//-- [RESPONSE NOTE] 
	//-- PLAYER:  What is Lord Vader's interest in this Jedi? 
	if (response == "s_433")
	{
		//-- [NOTE] 
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			emp_day_imperial_commander_action_givePrisonerAccess (player, npc);

			//-- NPC: He's being questioned in that small detainment facility. You can go take a look if you like. He's not much of a threat anymore.
			string_id message = new string_id (c_stringFile, "s_435");
			utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.emp_day_imperial_commander");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.emp_day_imperial_commander");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (emp_day_imperial_commander_condition_isRebel (player, npc))
	{
		//-- NPC: I don't have time to talk to traitorous Rebels.
		string_id message = new string_id (c_stringFile, "s_363");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (emp_day_imperial_commander_condition_questCompleted (player, npc))
	{
		//-- NPC: Thanks for taking the time to help us out. Your dedication as a citizen of the Empire serves as a great example that others would do well to notice.
		string_id message = new string_id (c_stringFile, "s_365");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER:  What is Lord Vader's interest in this Jedi? 
		boolean hasResponse0 = false;
		if (emp_day_imperial_commander_condition_noPrisonAccess (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_367");

			utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 2);

			npcStartConversation (player, npc, "emp_day_imperial_commander", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (emp_day_imperial_commander_condition_hasTask8 (player, npc))
	{
		//-- NPC: Did you speak to him? He's an inspiration, that's for sure. I'm hoping some day to serve aboard his flagship. 
		string_id message = new string_id (c_stringFile, "s_371");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I've delivered the historian's disks. My work here is done.
		boolean hasResponse0 = false;
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_373");

			utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 4);

			npcStartConversation (player, npc, "emp_day_imperial_commander", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (emp_day_imperial_commander_condition_notReadyForYou (player, npc))
	{
		//-- NPC: I'm sorry, all requests must go thru our Public Relations Officers. 
		string_id message = new string_id (c_stringFile, "s_381");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (emp_day_imperial_commander_condition_hasTask6 (player, npc))
	{
		//-- NPC: Disks? I don't have time to upload them right now because Lord Vader is here! What an auspicious occasion.
		string_id message = new string_id (c_stringFile, "s_383");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Vader is here? Why?
		boolean hasResponse0 = false;
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I need to go. Sorry. 
		boolean hasResponse1 = false;
		if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_385");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_427");

			utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 8);

			npcStartConversation (player, npc, "emp_day_imperial_commander", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (emp_day_imperial_commander_condition_hasTask7 (player, npc))
	{
		//-- NPC: What are you doing here? You'd better deliver that report to Darth Vader immediately!
		string_id message = new string_id (c_stringFile, "s_431");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER:  What is Lord Vader's interest in this Jedi? 
		boolean hasResponse0 = false;
		if (emp_day_imperial_commander_condition_noPrisonAccess (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_433");

			utils.setScriptVar (player, "conversation.emp_day_imperial_commander.branchId", 21);

			npcStartConversation (player, npc, "emp_day_imperial_commander", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (emp_day_imperial_commander_condition__defaultCondition (player, npc))
	{
		//-- NPC: All requests must go thru our Public Relations Officers. 
		string_id message = new string_id (c_stringFile, "s_437");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "emp_day_imperial_commander")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

	if (branchId == 2 && emp_day_imperial_commander_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && emp_day_imperial_commander_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && emp_day_imperial_commander_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && emp_day_imperial_commander_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && emp_day_imperial_commander_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && emp_day_imperial_commander_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && emp_day_imperial_commander_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && emp_day_imperial_commander_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && emp_day_imperial_commander_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && emp_day_imperial_commander_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && emp_day_imperial_commander_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && emp_day_imperial_commander_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.emp_day_imperial_commander.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

