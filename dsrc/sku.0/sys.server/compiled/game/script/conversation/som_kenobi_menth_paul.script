// ======================================================================
//
// som_kenobi_menth_paul.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.jedi;
include library.money;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/som_kenobi_menth_paul";

// ======================================================================
// Script Conditions
// ======================================================================

boolean som_kenobi_menth_paul_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean som_kenobi_menth_paul_condition_playerJedi (obj_id player, obj_id npc)
{
	return jedi.isForceSensitive (player);
}

// ----------------------------------------------------------------------

boolean som_kenobi_menth_paul_condition_hermitChat2 (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "som_kenobi_main_quest_1", "talkHermit2");
}

// ----------------------------------------------------------------------

boolean som_kenobi_menth_paul_condition_dontHave500 (obj_id player, obj_id npc)
{
	return !money.hasFunds (player, money.MT_TOTAL, 500);
}

// ----------------------------------------------------------------------

boolean som_kenobi_menth_paul_condition_doneTalking (obj_id player, obj_id npc)
{
	if (groundquests.isQuestActive(player, "som_kenobi_cursed_shard_1")||
		groundquests.isQuestActive(player, "som_kenobi_cursed_shard_2")||
		groundquests.hasCompletedQuest(player, "som_kenobi_cursed_shard_2"))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean som_kenobi_menth_paul_condition_abortedQuest2 (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest(player, "som_kenobi_cursed_shard_1")&&
		!groundquests.hasCompletedQuest(player, "som_kenobi_cursed_shard_2")&&
		!groundquests.isQuestActive(player, "som_kenobi_cursed_shard_2"))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean som_kenobi_menth_paul_condition_tryingReturnShard (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "som_kenobi_cursed_shard_2", "givingUpShard");
}

// ----------------------------------------------------------------------

boolean som_kenobi_menth_paul_condition_dontHave1000 (obj_id player, obj_id npc)
{
	return !money.hasFunds (player, money.MT_TOTAL, 1000);
}

// ----------------------------------------------------------------------

boolean som_kenobi_menth_paul_condition_levelTooLow (obj_id player, obj_id npc)
{
	int playerLevel = getLevel(player);
	if (playerLevel > 60)
		return true;
	else
		return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void som_kenobi_menth_paul_action_giveQuest1000 (obj_id player, obj_id npc)
{
	money.pay(player, npc, 1000, "", null, true);
	groundquests.grantQuest(player, "som_kenobi_cursed_shard_1");
}

// ----------------------------------------------------------------------

void som_kenobi_menth_paul_action_pay500 (obj_id player, obj_id npc)
{
	money.pay(player, npc, 500, "", null, false);
}

// ----------------------------------------------------------------------

void som_kenobi_menth_paul_action_pay1000 (obj_id player, obj_id npc)
{
	money.pay(player, npc, 1000, "", null, true);
}

// ----------------------------------------------------------------------

void som_kenobi_menth_paul_action_giveQuestFree (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "som_kenobi_cursed_shard_1");
}

// ----------------------------------------------------------------------

void som_kenobi_menth_paul_action_giveQuest500 (obj_id player, obj_id npc)
{
	money.pay(player, npc, 500, "", null, true);
	groundquests.grantQuest(player, "som_kenobi_cursed_shard_1");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int som_kenobi_menth_paul_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What do you want..?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was wondering if you would like your crystal back?
	if (response == "s_137")
	{
		doAnimationAction (player, "thumb_up");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: No way! It's all yours now. I don't want that blasted thing back!
			string_id message = new string_id (c_stringFile, "s_139");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm here to give your crystal back.
	if (response == "s_138")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: I'm not taking that! The thing is evil!
			string_id message = new string_id (c_stringFile, "s_140");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please, help me... I need to get off this planet.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why don't you just take the shuttle then friend?
	if (response == "s_111")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've had a string of bad luck and lost everything. That's why I'm hoping that you would want to buy this crystal from me, for the cheap price of a thousand credits.
			string_id message = new string_id (c_stringFile, "s_161");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It looks very nice. Where did you find it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A thousand credits is a little steep...
			boolean hasResponse1 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_162");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_163");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And what do you want from me?
	if (response == "s_112")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "implore");

			//-- NPC: I need to sell this crystal. Please, only a thousand credits and this beautiful shard could be yours.
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Interesting. It's all black. I might want it. Where'd you find it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A thousand credits for a piece of glass? I don't think so.
			boolean hasResponse1 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_120");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sorry. I have some things I have to attend to right now.
	if (response == "s_113")
	{
		doAnimationAction (player, "refuse_offer_affection");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: Will you come back? I've lost everything. I need some help...
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, I might. You hang in there, friend.
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_117");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I look like, a shuttle ticket vendor?
	if (response == "s_114")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "heavy_cough_vomit");

			//-- NPC: Please... I've lost everything...
			string_id message = new string_id (c_stringFile, "s_115");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please, help me... I have to get off this planet.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why don't you just take the shuttle then friend?
	if (response == "s_111")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've had a string of bad luck and lost everything. That's why I'm hoping that you would want to buy this crystal from me, for the cheap price of a thousand credits.
			string_id message = new string_id (c_stringFile, "s_161");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It looks very nice. Where did you find it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A thousand credits is a little steep...
			boolean hasResponse1 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_162");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_163");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And what do you want from me?
	if (response == "s_112")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "implore");

			//-- NPC: I need to sell this crystal. Please, only a thousand credits and this beautiful shard could be yours.
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Interesting. It's all black. I might want it. Where'd you find it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A thousand credits for a piece of glass? I don't think so.
			boolean hasResponse1 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_120");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sorry. I have some things I have to attend to right now.
	if (response == "s_113")
	{
		doAnimationAction (player, "refuse_offer_affection");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: Will you come back? I've lost everything. I need some help...
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, I might. You hang in there, friend.
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_117");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I look like, a shuttle ticket vendor?
	if (response == "s_114")
	{
		doAnimationAction (player, "dismiss");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "heavy_cough_vomit");

			//-- NPC: Please... I've lost everything...
			string_id message = new string_id (c_stringFile, "s_115");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've had a string of bad luck and lost everything. That's why I'm hoping that you would want to buy this crystal from me, for the cheap price of a thousand credits.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It looks very nice. Where did you find it?
	if (response == "s_162")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: You wouldn't believe me if I told you.
			string_id message = new string_id (c_stringFile, "s_165");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm very open-minded.
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_167");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A thousand credits is a little steep...
	if (response == "s_163")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: Fine, fine. I'll go down to 500 credits.
			string_id message = new string_id (c_stringFile, "s_185");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hmm, maybe. Where did you find it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_187");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You wouldn't believe me if I told you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm very open-minded.
	if (response == "s_167")
	{
		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I came to this place in hopes of finding fortune. One evening, when I was out surveying, I saw the strangest thing. A man that was all covered in strange blue glowing dust was kneeling down, looking at something. He hadn't seen me, so I kept looking at what he was doing. After a while he stood back up and walked away. I ran up to the spot where I saw him, and there in between some rocks, I found this beautiful crystal.
			string_id message = new string_id (c_stringFile, "s_169");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And now you're trying to sell it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_171");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I came to this place in hopes of finding fortune. One evening, when I was out surveying, I saw the strangest thing. A man that was all covered in strange blue glowing dust was kneeling down, looking at something. He hadn't seen me, so I kept looking at what he was doing. After a while he stood back up and walked away. I ran up to the spot where I saw him, and there in between some rocks, I found this beautiful crystal.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And now you're trying to sell it?
	if (response == "s_171")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've had nothing but bad luck lately and I've lost everything. I need the credits to be able to get back home...
			string_id message = new string_id (c_stringFile, "s_173");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, I'll help you. We said a thousand credits, right?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A thousand credits is still a bit too much, though.
			boolean hasResponse1 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_175");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_181");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've had nothing but bad luck lately and I've lost everything. I need the credits to be able to get back home...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'll help you. We said a thousand credits, right?
	if (response == "s_175")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition_dontHave1000 (player, npc))
		{
			doAnimationAction (npc, "weeping");

			//-- NPC: You don't have the money on you? Please, go get it now!
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			som_kenobi_menth_paul_action_giveQuest1000 (player, npc);

			//-- NPC: Indeed we did. Here you go. All yours.
			string_id message = new string_id (c_stringFile, "s_179");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A thousand credits is still a bit too much, though.
	if (response == "s_181")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stamp_feet");

			som_kenobi_menth_paul_action_giveQuestFree (player, npc);

			//-- NPC: Then take it for free! Just take it, please!
			string_id message = new string_id (c_stringFile, "s_183");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fine, fine. I'll go down to 500 credits.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hmm, maybe. Where did you find it?
	if (response == "s_187")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: You wouldn't believe me if I told you.
			string_id message = new string_id (c_stringFile, "s_213");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm very open-minded.
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_215");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You wouldn't believe me if I told you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm very open-minded.
	if (response == "s_215")
	{
		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I came to this place in hopes of finding fortune. One evening, when I was out surveying, I saw the strangest thing. A man that was all covered in strange blue glowing dust was kneeling down, looking at something. He hadn't seen me, so I kept looking at what he was doing. After a while he stood back up and walked away. I ran up to the spot where I saw him, and there in between some rocks, I found this beautiful crystal.
			string_id message = new string_id (c_stringFile, "s_217");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And now you're trying to sell it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_219");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I came to this place in hopes of finding fortune. One evening, when I was out surveying, I saw the strangest thing. A man that was all covered in strange blue glowing dust was kneeling down, looking at something. He hadn't seen me, so I kept looking at what he was doing. After a while he stood back up and walked away. I ran up to the spot where I saw him, and there in between some rocks, I found this beautiful crystal.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And now you're trying to sell it?
	if (response == "s_219")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've had nothing but bad luck lately and I've lost everything. I need the credits to be able to get back home...
			string_id message = new string_id (c_stringFile, "s_221");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Very well. We said 500 right?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 500 credits is still a lot of money though...
			boolean hasResponse1 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_223");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've had nothing but bad luck lately and I've lost everything. I need the credits to be able to get back home...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Very well. We said 500 right?
	if (response == "s_223")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition_dontHave500 (player, npc))
		{
			doAnimationAction (npc, "weeping");

			//-- NPC: You don't have the money on you? Please, go get it now!
			string_id message = new string_id (c_stringFile, "s_225");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			som_kenobi_menth_paul_action_giveQuest500 (player, npc);

			//-- NPC: We sure did. Here you go, friend. Enjoy!
			string_id message = new string_id (c_stringFile, "s_227");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 500 credits is still a lot of money though...
	if (response == "s_229")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stamp_feet");

			som_kenobi_menth_paul_action_giveQuestFree (player, npc);

			//-- NPC: Then take it for free! Just take the darn thing!
			string_id message = new string_id (c_stringFile, "s_231");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I need to sell this crystal. Please, only a thousand credits and this beautiful shard could be yours.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Interesting. It's all black. I might want it. Where'd you find it?
	if (response == "s_119")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: You wouldn't believe me if I told you.
			string_id message = new string_id (c_stringFile, "s_142");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Try me.
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_144");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A thousand credits for a piece of glass? I don't think so.
	if (response == "s_120")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "weeping");

			//-- NPC: Please, I beg of you. I'll go with 500 credits, please. Just take it.
			string_id message = new string_id (c_stringFile, "s_121");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You're acting like it has the plague. Did you steal it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_122");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You wouldn't believe me if I told you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Try me.
	if (response == "s_144")
	{
		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I came to this place in hopes of finding fortune. One evening, when I was out surveying, I saw the strangest thing. A man that was all covered in strange blue glowing dust was kneeling down, looking at something. He hadn't seen me, so I kept looking at what he was doing. After a while he stood back up and walked away. I ran up to the spot where I saw him, and there in between some rocks, I found this beautiful crystal.
			string_id message = new string_id (c_stringFile, "s_146");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: If it's so beautiful, why are you trying to get rid of it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_148");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I came to this place in hopes of finding fortune. One evening, when I was out surveying, I saw the strangest thing. A man that was all covered in strange blue glowing dust was kneeling down, looking at something. He hadn't seen me, so I kept looking at what he was doing. After a while he stood back up and walked away. I ran up to the spot where I saw him, and there in between some rocks, I found this beautiful crystal.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: If it's so beautiful, why are you trying to get rid of it?
	if (response == "s_148")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've had nothing but bad luck lately and I've lost everything. I need the credits to be able to get back home...
			string_id message = new string_id (c_stringFile, "s_150");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Fine, we said a thousand credits right?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: A thousand credits is too much though.
			boolean hasResponse1 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_152");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_158");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've had nothing but bad luck lately and I've lost everything. I need the credits to be able to get back home...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine, we said a thousand credits right?
	if (response == "s_152")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition_dontHave1000 (player, npc))
		{
			doAnimationAction (npc, "weeping");

			//-- NPC: You don't have the money on you? Please, go get them now!
			string_id message = new string_id (c_stringFile, "s_154");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			som_kenobi_menth_paul_action_giveQuest1000 (player, npc);

			//-- NPC: Right, right. Here you go. Take it now. Please.
			string_id message = new string_id (c_stringFile, "s_156");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: A thousand credits is too much though.
	if (response == "s_158")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stamp_feet");

			som_kenobi_menth_paul_action_giveQuestFree (player, npc);

			//-- NPC: Then take it for free! Just take the darn thing!
			string_id message = new string_id (c_stringFile, "s_160");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please, I beg of you. I'll go with 500 credits, please. Just take it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You're acting like it has the plague. Did you steal it?
	if (response == "s_122")
	{
		doAnimationAction (player, "point_accusingly");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: No no, I found it...
			string_id message = new string_id (c_stringFile, "s_123");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_124");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No no, I found it...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where?
	if (response == "s_124")
	{
		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: You wouldn't believe me if I told you.
			string_id message = new string_id (c_stringFile, "s_125");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Try me.
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_126");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You wouldn't believe me if I told you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Try me.
	if (response == "s_126")
	{
		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I came to this place in hopes of finding fortune. One evening, when I was out surveying, I saw the strangest thing. A man that was all covered in strange blue glowing dust was kneeling down, looking at something. He hadn't seen me, so I kept looking at what he was doing. After a while he stood back up and walked away. I ran up to the spot where I saw him, and there in between some rocks, I found this beautiful crystal.
			string_id message = new string_id (c_stringFile, "s_127");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: If it's so beautiful, why are you trying to get rid of it?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_128");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I came to this place in hopes of finding fortune. One evening, when I was out surveying, I saw the strangest thing. A man that was all covered in strange blue glowing dust was kneeling down, looking at something. He hadn't seen me, so I kept looking at what he was doing. After a while he stood back up and walked away. I ran up to the spot where I saw him, and there in between some rocks, I found this beautiful crystal.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: If it's so beautiful, why are you trying to get rid of it?
	if (response == "s_128")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've had nothing but bad luck lately and I've lost everything. I need the credits to be able to get back home...
			string_id message = new string_id (c_stringFile, "s_129");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Fine, 500 credits we said right?
			boolean hasResponse0 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: 500 credits is too much though.
			boolean hasResponse1 = false;
			if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_130");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_131");

				utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've had nothing but bad luck lately and I've lost everything. I need the credits to be able to get back home...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine, 500 credits we said right?
	if (response == "s_130")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition_dontHave500 (player, npc))
		{
			doAnimationAction (npc, "weeping");

			//-- NPC: You don't have the money on you? Please, go get it now!
			string_id message = new string_id (c_stringFile, "s_134");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			som_kenobi_menth_paul_action_giveQuest500 (player, npc);

			//-- NPC: Right, right. Here you go. Take it now. Please.
			string_id message = new string_id (c_stringFile, "s_132");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: 500 credits is too much though.
	if (response == "s_131")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stamp_feet");

			som_kenobi_menth_paul_action_giveQuestFree (player, npc);

			//-- NPC: Then take it for free! Just take the darn thing!
			string_id message = new string_id (c_stringFile, "s_133");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int som_kenobi_menth_paul_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Will you come back? I've lost everything. I need some help...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, I might. You hang in there, friend.
	if (response == "s_117")
	{
		doAnimationAction (player, "nod");

		//-- [NOTE] 
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: I'm trying to...
			string_id message = new string_id (c_stringFile, "s_232");
			utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.som_kenobi_menth_paul");

	setCondition (self, CONDITION_INTERESTING);
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_INTERESTING);
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.som_kenobi_menth_paul");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (som_kenobi_menth_paul_condition_tryingReturnShard (player, npc))
	{
		doAnimationAction (npc, "nervous");

		//-- NPC: What do you want..?
		string_id message = new string_id (c_stringFile, "s_136");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I was wondering if you would like your crystal back?
		boolean hasResponse0 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm here to give your crystal back.
		boolean hasResponse1 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_137");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_138");

			utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 1);

			npcStartConversation (player, npc, "som_kenobi_menth_paul", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_menth_paul_condition_abortedQuest2 (player, npc))
	{
		doAnimationAction (npc, "implore");

		//-- NPC: Please, help me... I need to get off this planet.
		string_id message = new string_id (c_stringFile, "s_135");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Why don't you just take the shuttle then friend?
		boolean hasResponse0 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: And what do you want from me?
		boolean hasResponse1 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm sorry. I have some things I have to attend to right now.
		boolean hasResponse2 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: What do I look like, a shuttle ticket vendor?
		boolean hasResponse3 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_111");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_112");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_113");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_114");

			utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 6);

			npcStartConversation (player, npc, "som_kenobi_menth_paul", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_menth_paul_condition_doneTalking (player, npc))
	{
		doAnimationAction (npc, "greet");

		doAnimationAction (player, "nod");

		//-- NPC: Yeah, I know. I'm still here. Since you took the crystal, my luck sure has turned! I won a mining spot from a Mustafarian in a bet, and it turns out the resources I can get from it are worth millions! So I'll be around for a while...hehe.
		string_id message = new string_id (c_stringFile, "s_67");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_menth_paul_condition_levelTooLow (player, npc))
	{
		doAnimationAction (npc, "implore");

		//-- NPC: Please, help me... I have to get off this planet.
		string_id message = new string_id (c_stringFile, "s_110");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Why don't you just take the shuttle then friend?
		boolean hasResponse0 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: And what do you want from me?
		boolean hasResponse1 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm sorry. I have some things I have to attend to right now.
		boolean hasResponse2 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: What do I look like, a shuttle ticket vendor?
		boolean hasResponse3 = false;
		if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_111");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_112");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_113");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_114");

			utils.setScriptVar (player, "conversation.som_kenobi_menth_paul.branchId", 6);

			npcStartConversation (player, npc, "som_kenobi_menth_paul", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (som_kenobi_menth_paul_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "sigh_deeply");

		//-- NPC: I wish you were more experienced... I could really use some help.
		string_id message = new string_id (c_stringFile, "s_72");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "som_kenobi_menth_paul")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

	if (branchId == 1 && som_kenobi_menth_paul_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && som_kenobi_menth_paul_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && som_kenobi_menth_paul_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && som_kenobi_menth_paul_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && som_kenobi_menth_paul_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && som_kenobi_menth_paul_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && som_kenobi_menth_paul_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && som_kenobi_menth_paul_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && som_kenobi_menth_paul_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && som_kenobi_menth_paul_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && som_kenobi_menth_paul_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && som_kenobi_menth_paul_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && som_kenobi_menth_paul_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && som_kenobi_menth_paul_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && som_kenobi_menth_paul_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && som_kenobi_menth_paul_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && som_kenobi_menth_paul_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && som_kenobi_menth_paul_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && som_kenobi_menth_paul_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && som_kenobi_menth_paul_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && som_kenobi_menth_paul_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.som_kenobi_menth_paul.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

