// ======================================================================
//
// tat_icon_missingdroid_farmer.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.34 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/tat_icon_missingdroid_farmer";

// ======================================================================
// Script Conditions
// ======================================================================

boolean tat_icon_missingdroid_farmer_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_e6_dealer2Lyrissa (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_dealer_to_lyrissa_e6 = groundquests.getTaskId(questId1, "tat_icon_dealer_to_lyrissa_e6"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_dealer_to_lyrissa_e6, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_e5_lyrissa2Dealer (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_lyrissa_to_dealer_e5 = groundquests.getTaskId(questId1, "tat_icon_lyrissa_to_dealer_e5"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_lyrissa_to_dealer_e5, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_e4_dren2Lyrissa (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_dren_to_lyrissa_e4 = groundquests.getTaskId(questId1, "tat_icon_dren_to_lyrissa_e4"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_dren_to_lyrissa_e4, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_e3_droid2Dren (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_droid_to_dren_e3 = groundquests.getTaskId(questId1, "tat_icon_droid_to_dren_e3"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_droid_to_dren_e3, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_e2_jawa2Droid (obj_id player, obj_id npc)
{	
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_jawa_to_droid_e2 = groundquests.getTaskId(questId1, "tat_icon_jawa_to_droid_e2"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_jawa_to_droid_e2, player);
	
	return onTask;

}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_e1_dren2Jawa (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_dren_to_jawa_e1 = groundquests.getTaskId(questId1, "tat_icon_dren_to_jawa_e1"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_dren_to_jawa_e1, player);
	
	return onTask;
}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_e8_droid2Dren (obj_id player, obj_id npc)
{	
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_droid_to_dren_e8 = groundquests.getTaskId(questId1, "tat_icon_droid_to_dren_e8"); 
	
	boolean onTask = questIsTaskActive(questId1, tat_icon_droid_to_dren_e8, player);
	
	return onTask;

}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_complete (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	
	boolean OnTask = 	(questIsQuestComplete(questId1, player));
	
	return OnTask;

}

// ----------------------------------------------------------------------

boolean tat_icon_missingdroid_farmer_condition_fixingJawa_e1Plus_pastDren (obj_id player, obj_id npc)
{
	int questId1 = questGetQuestId("quest/tat_icon_fixingjawa_1");
	int tat_icon_dren_to_jawa_e1 = groundquests.getTaskId(questId1, "tat_icon_dren_to_jawa_e1"); 
	int tat_icon_jawa_to_droid_e2 = groundquests.getTaskId(questId1, "tat_icon_jawa_to_droid_e2"); 
	int tat_icon_droid_to_dren_e3 = groundquests.getTaskId(questId1, "tat_icon_droid_to_dren_e3"); 
	
	boolean onTask1 = questIsTaskActive(questId1, tat_icon_dren_to_jawa_e1, player);
	boolean onTask2 = questIsTaskActive(questId1, tat_icon_jawa_to_droid_e2, player);
	boolean onTask3 = questIsTaskActive(questId1, tat_icon_droid_to_dren_e3, player);
	
	boolean onTask = false;
	
	if (onTask1 || onTask2 || onTask3)
		onTask = true;
				
	return onTask;
}

// ======================================================================
// Script Actions
// ======================================================================

void tat_icon_missingdroid_farmer_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ----------------------------------------------------------------------

void tat_icon_missingdroid_farmer_action_signal_beginQuest (obj_id player, obj_id npc)
{
    int questId = questGetQuestId("quest/tat_icon_fixingjawa_1");
    groundquests.grantQuest(questId, player, npc, true);

}

// ----------------------------------------------------------------------

void tat_icon_missingdroid_farmer_action_signal_gotoLyrissa_e4 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_icon_fixingjawa_droid_to_dren_e3");
}

// ----------------------------------------------------------------------

void tat_icon_missingdroid_farmer_action_signal_fixingjawa_complete (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tat_icon_fixingjawa_complete");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int tat_icon_missingdroid_farmer_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome back. Have you been able to talk to Lyrissa yet?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, she wants me to pick up a power converter for her from Tosche Station.
	if (response == "s_97")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Yea, she is a really good pilot. Always doing something to her speeder. Are you going to get her the part?
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I just wanted to make sure that was ok with you.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 4);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I picked up a power converter for her from Tosche Station.
	if (response == "s_102")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Great, you should get it to her soon. There is no telling when that droid might decide it doesn't want to be in standby mode anymore.
			string_id message = new string_id (c_stringFile, "s_103");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Right, on my way.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I am on my way to see her now.
	if (response == "s_33")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please hurry! I hate to think what that droid might be doing under it's old military programming.
			string_id message = new string_id (c_stringFile, "s_34");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yea, she is a really good pilot. Always doing something to her speeder. Are you going to get her the part?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I just wanted to make sure that was ok with you.
	if (response == "s_99")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Sure, I don't see how it could hurt. For all her grease monkey bluster Lyrissa is a good friend.
			string_id message = new string_id (c_stringFile, "s_100");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Great, you should get it to her soon. There is no telling when that droid might decide it doesn't want to be in standby mode anymore.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Right, on my way.
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Safe travels.
			string_id message = new string_id (c_stringFile, "s_105");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hello again, did the Jawa have any information?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: He helped me find your droid.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Really? That's great! What did you find out?
			string_id message = new string_id (c_stringFile, "s_24");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Jawa's replacement programming failed.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_26");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've got a location for the droid. I am on my way there now.
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Excellent! I hope it's still ok. I really need that droid to collect my harvest.
			string_id message = new string_id (c_stringFile, "s_46");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am on my way to see him now.
	if (response == "s_48")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: I sure hope he can help. I can't imagine what happened to that droid.
			string_id message = new string_id (c_stringFile, "s_50");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Really? That's great! What did you find out?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Jawa's replacement programming failed.
	if (response == "s_26")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Failed? How?
			string_id message = new string_id (c_stringFile, "s_28");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It started reverting to it's old military programming.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_30");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Failed? How?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It started reverting to it's old military programming.
	if (response == "s_30")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Oh no! That used to be a demolition droid, it could cause a lot of damage if not repaired soon. 
			string_id message = new string_id (c_stringFile, "s_32");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I can't fix a droid like that.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_36");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh no! That used to be a demolition droid, it could cause a lot of damage if not repaired soon. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can't fix a droid like that.
	if (response == "s_36")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: That's ok, I know someone who can. Her name is Lyrissa. She is a moisture farmer too, but she is great with machines. I am sure she could reprogram this droid correctly.
			string_id message = new string_id (c_stringFile, "s_38");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Can you give directions to her?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_40");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's ok, I know someone who can. Her name is Lyrissa. She is a moisture farmer too, but she is great with machines. I am sure she could reprogram this droid correctly.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can you give directions to her?
	if (response == "s_40")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			tat_icon_missingdroid_farmer_action_signal_gotoLyrissa_e4 (player, npc);

			//-- NPC: Yes, this should be where she is.
			string_id message = new string_id (c_stringFile, "s_42");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hi, did Texe send you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes he did, how can I help?
	if (response == "s_54")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: My harvest droid has gone missing and my harvesters are being vandalized. If I need someone to try and get my droid back while I repair these harvesters.
			string_id message = new string_id (c_stringFile, "s_56");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How would I go about finding your droid?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_58");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know any Texe.
	if (response == "s_74")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Oh, sorry. I was expecting someone he sent to help me.
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you need help with?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well that's not me. Goodbye.
			boolean hasResponse1 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_78");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_110");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: My harvest droid has gone missing and my harvesters are being vandalized. If I need someone to try and get my droid back while I repair these harvesters.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How would I go about finding your droid?
	if (response == "s_58")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well I bought it from some traveling Jawa traders. They sometimes have tracking devices on them. It's possible this one does too.
			string_id message = new string_id (c_stringFile, "s_60");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do they put tracking devices on them?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where are these Jawas now?
			boolean hasResponse1 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_62");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_134");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well I bought it from some traveling Jawa traders. They sometimes have tracking devices on them. It's possible this one does too.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why do they put tracking devices on them?
	if (response == "s_62")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Some of the more unscrupulous jawas do it so they can get their droid back. You see, they put the tracker on it, and add a behavioral matrix modifier that makes them run away from their new master after a short period of time. Fire up the ol tracker and you got yourself the jawa version of the re-gift.
			string_id message = new string_id (c_stringFile, "s_123");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do you know these Jawas didn't do the same?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_125");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are these Jawas now?
	if (response == "s_134")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: They usually return to their trading post.
			string_id message = new string_id (c_stringFile, "s_88");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Can you provide directions?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, sorry. I was expecting someone he sent to help me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you need help with?
	if (response == "s_78")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: My harvest droid has gone missing and my harvesters are being vandalized. If I need someone to try and get my droid back while I repair these harvesters.
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How would I go about finding your droid?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sorry, can't help you. 
			boolean hasResponse1 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_82");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well that's not me. Goodbye.
	if (response == "s_110")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Goodbye.
			string_id message = new string_id (c_stringFile, "s_112");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: My harvest droid has gone missing and my harvesters are being vandalized. If I need someone to try and get my droid back while I repair these harvesters.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How would I go about finding your droid?
	if (response == "s_82")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well I bought it from some traveling Jawa traders. They sometimes have tracking devices on them. It's possible this one does too.
			string_id message = new string_id (c_stringFile, "s_84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why do they put tracking devices on them?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where are these Jawas now?
			boolean hasResponse1 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_86");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sorry, can't help you. 
	if (response == "s_106")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: I understand. Goodbye.
			string_id message = new string_id (c_stringFile, "s_108");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well I bought it from some traveling Jawa traders. They sometimes have tracking devices on them. It's possible this one does too.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why do they put tracking devices on them?
	if (response == "s_121")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Some of the more unscrupulous jawas do it so they can get their droid back. You see, they put the tracker on it, and add a behavioral matrix modifier that makes them run away from their new master after a short period of time. Fire up the ol tracker and you got yourself the jawa version of the re-gift.
			string_id message = new string_id (c_stringFile, "s_123");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do you know these Jawas didn't do the same?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_125");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are these Jawas now?
	if (response == "s_86")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: They usually return to their trading post.
			string_id message = new string_id (c_stringFile, "s_88");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Can you provide directions?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Some of the more unscrupulous jawas do it so they can get their droid back. You see, they put the tracker on it, and add a behavioral matrix modifier that makes them run away from their new master after a short period of time. Fire up the ol tracker and you got yourself the jawa version of the re-gift.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do you know these Jawas didn't do the same?
	if (response == "s_125")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't. That's why I need you to talk to them. You're new to them, they won't know if you're a farmer like me or a hired gun to come set things square.
			string_id message = new string_id (c_stringFile, "s_127");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Will they get violent?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_129");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't. That's why I need you to talk to them. You're new to them, they won't know if you're a farmer like me or a hired gun to come set things square.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will they get violent?
	if (response == "s_129")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not likely. Jawas aren't much of a threat to anything able to defend itself.
			string_id message = new string_id (c_stringFile, "s_131");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where are these jawas now?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not likely. Jawas aren't much of a threat to anything able to defend itself.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where are these jawas now?
	if (response == "s_133")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: They usually return to their trading post.
			string_id message = new string_id (c_stringFile, "s_88");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Can you provide directions?
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They usually return to their trading post.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can you provide directions?
	if (response == "s_90")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. The trading post can be found here.
			string_id message = new string_id (c_stringFile, "s_92");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ok, I'm on my way.
			boolean hasResponse0 = false;
			if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94");

				utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int tat_icon_missingdroid_farmer_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Certainly. The trading post can be found here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ok, I'm on my way.
	if (response == "s_94")
	{
		//-- [NOTE] 
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			tat_icon_missingdroid_farmer_action_signal_beginQuest (player, npc);

			//-- NPC: Thank you so much. This will be a great help.
			string_id message = new string_id (c_stringFile, "s_96");
			utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
    if ((!isMob (self)) || (isPlayer (self)))
    detachScript(self, "conversation.tat_icon_missingdroid_farmer");
    setCondition (self, CONDITION_CONVERSABLE);
    setCondition (self, CONDITION_INTERESTING);
    setInvulnerable (self, true);

    setName (self, "Dren Feddeks");

    ai_lib.setDefaultCalmBehavior( self, ai_lib.BEHAVIOR_SENTINEL );

    return SCRIPT_CONTINUE;
}
trigger OnAttach ()
{
    setCondition (self, CONDITION_CONVERSABLE);
    setCondition (self, CONDITION_INTERESTING);
    setInvulnerable (self, true);
    setName (self, "Dren Feddeks");
    ai_lib.setDefaultCalmBehavior( self, ai_lib.BEHAVIOR_SENTINEL );
    return SCRIPT_CONTINUE;
}
trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
    int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
    menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
    menuInfoData.setServerNotify (false);
    setCondition (self, CONDITION_CONVERSABLE);
    return SCRIPT_CONTINUE;
}
trigger OnIncapacitated (obj_id killer)
{
    clearCondition (self, CONDITION_CONVERSABLE);
    detachScript (self, "conversation.tat_icon_missingdroid_farmer");
    return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (tat_icon_missingdroid_farmer_condition_fixingJawa_complete (player, npc))
	{
		tat_icon_missingdroid_farmer_action_facePlayer (player, npc);

		//-- NPC: Thanks again! My droid is working even better than when I bought it. Lyrissa is such a wiz with droids.
		string_id message = new string_id (c_stringFile, "s_4");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tat_icon_missingdroid_farmer_condition_fixingJawa_e8_droid2Dren (player, npc))
	{
		tat_icon_missingdroid_farmer_action_signal_fixingjawa_complete (player, npc);

		//-- NPC: Hello! My droid has returned and went straight back to work. It's really amazing what you have done for me. Take this for you troubles.
		string_id message = new string_id (c_stringFile, "s_6");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tat_icon_missingdroid_farmer_condition_fixingJawa_e4_dren2Lyrissa (player, npc))
	{
		tat_icon_missingdroid_farmer_action_facePlayer (player, npc);

		//-- NPC: Welcome back. Have you been able to talk to Lyrissa yet?
		string_id message = new string_id (c_stringFile, "s_11");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, she wants me to pick up a power converter for her from Tosche Station.
		boolean hasResponse0 = false;
		if (tat_icon_missingdroid_farmer_condition_fixingJawa_e5_lyrissa2Dealer (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes, I picked up a power converter for her from Tosche Station.
		boolean hasResponse1 = false;
		if (tat_icon_missingdroid_farmer_condition_fixingJawa_e6_dealer2Lyrissa (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: No, I am on my way to see her now.
		boolean hasResponse2 = false;
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_97");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_102");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_33");

			utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 3);

			npcStartConversation (player, npc, "tat_icon_missingdroid_farmer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tat_icon_missingdroid_farmer_condition_fixingJawa_e1Plus_pastDren (player, npc))
	{
		tat_icon_missingdroid_farmer_action_facePlayer (player, npc);

		//-- NPC: Hello again, did the Jawa have any information?
		string_id message = new string_id (c_stringFile, "s_20");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: He helped me find your droid.
		boolean hasResponse0 = false;
		if (tat_icon_missingdroid_farmer_condition_fixingJawa_e3_droid2Dren (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I've got a location for the droid. I am on my way there now.
		boolean hasResponse1 = false;
		if (tat_icon_missingdroid_farmer_condition_fixingJawa_e2_jawa2Droid (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I am on my way to see him now.
		boolean hasResponse2 = false;
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_44");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_48");

			utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 9);

			npcStartConversation (player, npc, "tat_icon_missingdroid_farmer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
	{
		tat_icon_missingdroid_farmer_action_facePlayer (player, npc);

		//-- NPC: Hi, did Texe send you?
		string_id message = new string_id (c_stringFile, "s_52");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes he did, how can I help?
		boolean hasResponse0 = false;
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I don't know any Texe.
		boolean hasResponse1 = false;
		if (tat_icon_missingdroid_farmer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_54");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_74");

			utils.setScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId", 17);

			npcStartConversation (player, npc, "tat_icon_missingdroid_farmer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "tat_icon_missingdroid_farmer")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

	if (branchId == 3 && tat_icon_missingdroid_farmer_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && tat_icon_missingdroid_farmer_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && tat_icon_missingdroid_farmer_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && tat_icon_missingdroid_farmer_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && tat_icon_missingdroid_farmer_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && tat_icon_missingdroid_farmer_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && tat_icon_missingdroid_farmer_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && tat_icon_missingdroid_farmer_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && tat_icon_missingdroid_farmer_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && tat_icon_missingdroid_farmer_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && tat_icon_missingdroid_farmer_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && tat_icon_missingdroid_farmer_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && tat_icon_missingdroid_farmer_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && tat_icon_missingdroid_farmer_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && tat_icon_missingdroid_farmer_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && tat_icon_missingdroid_farmer_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && tat_icon_missingdroid_farmer_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && tat_icon_missingdroid_farmer_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && tat_icon_missingdroid_farmer_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.tat_icon_missingdroid_farmer.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

