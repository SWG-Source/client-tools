// ======================================================================
//
// corvette_bronell_neutral_assassin.script
// Copyright 2003, Sony Online Entertainment
// All Rights Reserved.
//
// DO NOT EDIT THIS AUTO-GENERATED FILE.  PLEASE USE THE CONVERATION EDITOR!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.money;
include library.space_dungeon;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/corvette_bronell_neutral_assassin";

// ======================================================================
// Script Conditions
// ======================================================================

boolean corvette_bronell_neutral_assassin_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_knowsLocation3 (obj_id player, obj_id npc)
{
	return utils.hasScriptVar(player, "corl_corvette.heardLocation3");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_knowsLocation2 (obj_id player, obj_id npc)
{
	return utils.hasScriptVar(player, "corl_corvette.heardLocation2");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_knowsLocation1 (obj_id player, obj_id npc)
{
	return utils.hasScriptVar(player, "corl_corvette.heardLocation1");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_knowsAllLocations (obj_id player, obj_id npc)
{
	return ( (utils.hasScriptVar(player, "corl_corvette.heardLocation1"))
			&& (utils.hasScriptVar(player, "corl_corvette.heardLocation2"))
			&& (utils.hasScriptVar(player, "corl_corvette.heardLocation3")) );
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_hasStuff (obj_id player, obj_id npc)
{
	return ( (utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_intel.iff"))
			|| (utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler01.iff"))
			|| (utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler02.iff")) );
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_hasObject01 (obj_id player, obj_id npc)
{
	return utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_intel.iff");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_hasObject02 (obj_id player, obj_id npc)
{
	return utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler01.iff");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_hasObject03 (obj_id player, obj_id npc)
{
	return utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler02.iff");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_isGm (obj_id player, obj_id npc)
{
	return hasObjVar(player, "gm");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_hasTravelTicket (obj_id player, obj_id npc)
{
	string ticketTemplate = "object/tangible/travel/travel_ticket/dungeon_ticket.iff";
	obj_id[] spaceTickets = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, ticketTemplate);
	if (spaceTickets != null && spaceTickets.length > 0 )
	{
		for (int i = 0; i < spaceTickets.length; i++)
		{
			obj_id ticket = spaceTickets[i];
			if ( isIdValid(ticket) )
			{
				if (getStringObjVar (ticket, "space_dungeon.ticket.dungeon").equals ("corvette_neutral"))
					return true;
			}
		}
	}
	return false;
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (obj_id player, obj_id npc)
{
	return ( (!corvette_bronell_neutral_assassin_condition_hasTravelTicket(player, npc)) && (!corvette_bronell_neutral_assassin_condition_hasStuff(player, npc)) );
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_isOnQuest (obj_id player, obj_id npc)
{
	return hasObjVar(player, "corl_corvette.neutral_assassin");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_dungeonInactive (obj_id player, obj_id npc)
{
	string isDungeonActive = getConfigSetting("Dungeon", "Corellian_Corvette_Neutral");
	if ( isDungeonActive == null || isDungeonActive == "false" || isDungeonActive == "0")
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_onDifferentCorvetteQuest (obj_id player, obj_id npc)
{
	if ( hasObjVar(player, "corl_corvette") )
	{
		if ( !hasObjVar(player, "corl_corvette.neutral_assassin") )
			return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_earnedCorvetteReward (obj_id player, obj_id npc)
{
	return hasObjVar(player, "corvette.neutral_assassin.finished");
}

// ----------------------------------------------------------------------

boolean corvette_bronell_neutral_assassin_condition_hasInventorySpace (obj_id player, obj_id npc)
{
	obj_id playerInv = utils.getInventoryContainer(player);
	if ( isIdValid(playerInv) )
	{
		int free_space = getVolumeFree(playerInv);
		if (free_space > 0 )
		{
			return true;
		}
	}
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void corvette_bronell_neutral_assassin_action__defaultAction (obj_id player, obj_id npc)
{
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_heardLocation1 (obj_id player, obj_id npc)
{
	utils.setScriptVar(player, "corl_corvette.heardLocation1", 1);
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_heardLocation2 (obj_id player, obj_id npc)
{
	utils.setScriptVar(player, "corl_corvette.heardLocation2", 1);
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_heardLocation3 (obj_id player, obj_id npc)
{
	utils.setScriptVar(player, "corl_corvette.heardLocation3", 1);
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_acceptsQuest (obj_id player, obj_id npc)
{
	setObjVar(player, "corl_corvette.neutral_assassin", 1);
	
	string custLogMsg = "*Corvette Ground Quest: Player %TU has started the neutral assassin quest.";
	CustomerServiceLog("DUNGEON_CorellianCorvette", custLogMsg, player);
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_clearLocations (obj_id player, obj_id npc)
{
	if ( utils.hasScriptVar(player, "corl_corvette.heardLocation1") )
		utils.removeScriptVar( player, "corl_corvette.heardLocation1");
		
	if ( utils.hasScriptVar(player, "corl_corvette.heardLocation2") )
		utils.removeScriptVar( player, "corl_corvette.heardLocation2");
		
	if ( utils.hasScriptVar(player, "corl_corvette.heardLocation3") )
		utils.removeScriptVar( player, "corl_corvette.heardLocation3");
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_giveTicket (obj_id player, obj_id npc)
{
	obj_id document = utils.getItemPlayerHasByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_intel.iff");
	if( isIdValid(document) )
	{
		destroyObject(document);
		
		obj_id ticket = space_dungeon.createTicket(player, "tatooine", "corvette_neutral_pilot", "corvette_neutral");
		setObjVar(ticket, "space_dungeon.ticket.quest_type", "neutral_assassin");
		setObjVar(ticket, "corl_corvette.ticket_owner", player);
		setObjVar(ticket, "noTrade", true);
		attachScript(ticket, "item.special.nomove");
		attachScript(ticket, "theme_park.dungeon.corvette.corvette_quest_cleanup");
		
		string custLogMsg = "*Corvette Ground Quest: Player %TU finished the neutral assassin quest and received a neutral corvette ticket.";
		CustomerServiceLog("DUNGEON_CorellianCorvette", custLogMsg, player);
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_giveDocuments (obj_id player, obj_id npc)
{
	obj_id playerInv = utils.getInventoryContainer(player);
	{
		createObject ("object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_intel.iff", playerInv, "");
		createObject ("object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler01.iff", playerInv, "");
		createObject ("object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler02.iff", playerInv, "");
	}
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_quitCorvetteQuest (obj_id player, obj_id npc)
{
	if ( hasObjVar(player, "corl_corvette") )
		removeObjVar (player, "corl_corvette");
		
	string custLogMsg = "*Corvette Ground Quest: Player %TU aborted the neutral assassin quest.";
	CustomerServiceLog("DUNGEON_CorellianCorvette", custLogMsg, player);
	
	obj_id document = null;
	string intelTemplate = "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_intel.iff";
	obj_id[] intelDocuments = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, intelTemplate);
	if ( intelDocuments != null && intelDocuments.length > 0 )
	{
		for ( int i = 0; i < intelDocuments.length; i++ )
		{
			document = intelDocuments[i];
			if ( isIdValid(document) )
				destroyObject(document);
		}
	}
	
	string filler01Template = "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler01.iff";
	obj_id[] filler01Documents = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, filler01Template);
	if ( filler01Documents != null && filler01Documents.length > 0 )
	{
		for ( int i = 0; i < filler01Documents.length; i++ )
		{
			document = filler01Documents[i];
			if ( isIdValid(document) )
				destroyObject(document);
		}
	}

	string filler02Template = "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler02.iff";
	obj_id[] filler02Documents = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, filler02Template);
	if ( filler02Documents != null && filler02Documents.length > 0 )
	{
		for ( int i = 0; i < filler02Documents.length; i++ )
		{
			document = filler02Documents[i];
			if ( isIdValid(document) )
				destroyObject(document);
		}
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_removeTicket (obj_id player, obj_id npc)
{
	corvette_bronell_neutral_assassin_action_quitCorvetteQuest(player, npc);
	
	string ticketTemplate = "object/tangible/travel/travel_ticket/dungeon_ticket.iff";
	obj_id[] spaceTickets = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, ticketTemplate);
	if ( spaceTickets != null && spaceTickets.length > 0 )
	{
		for (int i = 0; i < spaceTickets.length; i++)
		{
			obj_id ticket = spaceTickets[i];
			if ( isIdValid(ticket) )
			{
				if ( getStringObjVar(ticket, "space_dungeon.ticket.dungeon").equals("corvette_neutral") )
				{
					destroyObject(ticket);
					string custLogMsg = "*Corvette Ground Quest: Player %TU aborted the neutral assassin quest and the neutal corvette ticket was revoked.";
					CustomerServiceLog("DUNGEON_CorellianCorvette", custLogMsg, player);
				}
			}
		}
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_takeDoc2 (obj_id player, obj_id npc)
{
	obj_id document = utils.getItemPlayerHasByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler02.iff");
	if( isIdValid(document) )
	{
		destroyObject(document);
		
		int credits = 487;
		if (credits != 0)
		{
			money.bankTo(money.ACCT_JABBA, player, credits);
			sendSystemMessage (player, credits + " credits have been deposited in your bank account.", null);
		}
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_takeDoc1 (obj_id player, obj_id npc)
{
	obj_id document = utils.getItemPlayerHasByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/neutral_assassin_filler01.iff");
	if ( isIdValid(document) )
	{
		destroyObject(document);
		
		int credits = 487;
		if (credits != 0)
		{
			money.bankTo(money.ACCT_JABBA, player, credits);
			sendSystemMessage (player, credits + " credits have been deposited in your bank account.", null);
		}
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	return;
}

// ----------------------------------------------------------------------

void corvette_bronell_neutral_assassin_action_giveCorvetteReward (obj_id player, obj_id npc)
{
 	obj_id playerInv = getObjectInSlot(player, "inventory"); 
	if(isIdValid(playerInv))
	{
		createObject ("object/tangible/loot/loot_schematic/corellian_corvette_landspeeder_av21_schematic.iff", playerInv, "");
		removeObjVar(player, "corvette.neutral_assassin.finished");
		CustomerServiceLog ("DUNGEON_CorellianCorvette", "*Corvette Reward: Player %TU has been given an AV-21 schematic.", player);
	}
	return;
}

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "npc.conversation.corvette_bronell_neutral_assassin");
	
	setInvulnerable (self, true );
	
	setCondition (self, CONDITION_CONVERSABLE);
	setCondition (self, CONDITION_INTERESTING);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setCondition (self, CONDITION_INTERESTING);
	setName (self, "Bronell");
	setInvulnerable (self, true );
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "npc.conversation.corvette_bronell_neutral_assassin");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

trigger OnStartNpcConversation (obj_id player)
{
	if (ai_lib.isInCombat (self) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (corvette_bronell_neutral_assassin_condition_dungeonInactive (player, self))
	{
		corvette_bronell_neutral_assassin_action_facePlayer (player, self);

		//-- NPC: I have no business with you at this time... and by that I mean Lord Jabba the Hutt has no need for me to do business with you at this time. Perhaps in the future, but not now.
		string_id message = new string_id (c_stringFile, "s_382a91bd");
		chat.chat (self, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_bronell_neutral_assassin_condition_earnedCorvetteReward (player, self))
	{
		//-- NPC: Lord Jabba is pleased to hear that the CorSec officer has been eliminated. Let this be a lesson to all who fail to pay their debts to Lord Jabba the Hutt, yes? Are you here to receive your reward, yes?
		string_id message = new string_id (c_stringFile, "s_1779cf3b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I am, yes. Hand it over!
		boolean hasResponse0 = false;
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No, not just yet. I'll be back.
		boolean hasResponse1 = false;
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b5022ce5");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ffc988c3");

			setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 2);

			npcStartConversation (player, self, "corvette_bronell_neutral_assassin", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_bronell_neutral_assassin_condition_onDifferentCorvetteQuest (player, self))
	{
		corvette_bronell_neutral_assassin_action_facePlayer (player, self);

		//-- NPC: Oh, well I could use your help, but I see that you're already busy. Come back when you've finished your current task and maybe we can do business.
		string_id message = new string_id (c_stringFile, "s_dc53bb6d");
		chat.chat (self, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
	{
		corvette_bronell_neutral_assassin_action_facePlayer (player, self);

		//-- NPC: Your still here? I thought I made clear time was not to be wasted looking at the dancers in the palace, yes?
		string_id message = new string_id (c_stringFile, "s_6f1692bf");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Where was Klaatu again?
		boolean hasResponse0 = false;
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Sorry, you'll have to find someone else. I cannot continue with this mission.
		boolean hasResponse1 = false;
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I had some other documents I came across.
		boolean hasResponse2 = false;
		if (corvette_bronell_neutral_assassin_condition_hasStuff (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2972f7c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4a365e4b");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d27dcffe");

			setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 7);

			npcStartConversation (player, self, "corvette_bronell_neutral_assassin", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_bronell_neutral_assassin_condition_isOnQuest (player, self))
	{
		corvette_bronell_neutral_assassin_action_facePlayer (player, self);

		//-- NPC: You are back already, yes? You have the documents, yes?
		string_id message = new string_id (c_stringFile, "s_491ecf2b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I believe so.
		boolean hasResponse0 = false;
		if (corvette_bronell_neutral_assassin_condition_hasStuff (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Could you go over the planets and pirates part again?
		boolean hasResponse1 = false;
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Sorry, I have other things to do. Erase all knowledge of my progress.
		boolean hasResponse2 = false;
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_24dbc07d");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9b8c1f57");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3e2e8616");

			setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 13);

			npcStartConversation (player, self, "corvette_bronell_neutral_assassin", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
	{
		corvette_bronell_neutral_assassin_action_facePlayer (player, self);

		//-- NPC: You there. I must demand your assistance with this mission of utmost importance. You will help, yes? This mission is vital to Lord Jabba and I am only speaking on his behalf. I, of course, get nothing out of this myself. So you will help, yes yes?
		string_id message = new string_id (c_stringFile, "s_8c30ea7c");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Depends, tell me about this mission?
		boolean hasResponse0 = false;
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Sorry, I have other things to do.
		boolean hasResponse1 = false;
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d62f7d90");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_bd40a944");

			setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 23);

			npcStartConversation (player, self, "corvette_bronell_neutral_assassin", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (self, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "corvette_bronell_neutral_assassin")
		return SCRIPT_CONTINUE;

	int branchId = getIntObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

	//-- [BRANCH NOTE] 
	//-- NPC: Lord Jabba is pleased to hear that the CorSec officer has been eliminated. Let this be a lesson to all who fail to pay their debts to Lord Jabba the Hutt, yes? Are you here to receive your reward, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am, yes. Hand it over!
	if (branchId == 2 && response == "s_b5022ce5")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition_hasInventorySpace (player, self))
		{
			corvette_bronell_neutral_assassin_action_giveCorvetteReward (player, self);

			//-- NPC: Very well. It has been well earned, yes? Yes.
			string_id message = new string_id (c_stringFile, "s_249447d6");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corvette_bronell_neutral_assassin_condition_hasInventorySpace (player, self))
		{
			//-- NPC: Hmm... you have truly earned your reward, but you do not have room for it in your inventory, yes? Please come back and speak with me after clearing some space, yes?
			string_id message = new string_id (c_stringFile, "s_73f00073");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Lord Jabba is pleased to hear that the CorSec officer has been eliminated. Let this be a lesson to all who fail to pay their debts to Lord Jabba the Hutt, yes? Are you here to receive your reward, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Lord Jabba is pleased to hear that the CorSec officer has been eliminated. Let this be a lesson to all who fail to pay their debts to Lord Jabba the Hutt, yes? Are you here to receive your reward, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, not just yet. I'll be back.
	if (branchId == 2 && response == "s_ffc988c3")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: As you wish. Though I do not understand why you'd want to delay, yes?
			string_id message = new string_id (c_stringFile, "s_44fa5eb9");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Lord Jabba is pleased to hear that the CorSec officer has been eliminated. Let this be a lesson to all who fail to pay their debts to Lord Jabba the Hutt, yes? Are you here to receive your reward, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Your still here? I thought I made clear time was not to be wasted looking at the dancers in the palace, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where was Klaatu again?
	if (branchId == 7 && response == "s_2972f7c")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: He's outside with the shuttle... back behind this building, Lord Jabba's palace! If you succeed in your mission, return to me for your reward, yes? Now hurry!
			string_id message = new string_id (c_stringFile, "s_3ffc96c6");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Your still here? I thought I made clear time was not to be wasted looking at the dancers in the palace, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Your still here? I thought I made clear time was not to be wasted looking at the dancers in the palace, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sorry, you'll have to find someone else. I cannot continue with this mission.
	if (branchId == 7 && response == "s_4a365e4b")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Our bountiful Lord will be most disappointed by your attitude, maybe a bit too disappointed, yes? I'll be forced to take all documents from you that relate to this endeavor, yes?
			string_id message = new string_id (c_stringFile, "s_f8c79456");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm sorry, I just can't right now. Please erase all records of my progress.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You might be right. Fine, I'll get back to it.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79855d10");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdde1d3a");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Your still here? I thought I made clear time was not to be wasted looking at the dancers in the palace, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Your still here? I thought I made clear time was not to be wasted looking at the dancers in the palace, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I had some other documents I came across.
	if (branchId == 7 && response == "s_d27dcffe")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Oh? Let me see that, yes?
			string_id message = new string_id (c_stringFile, "s_dcf8e793");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Your still here? I thought I made clear time was not to be wasted looking at the dancers in the palace, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Our bountiful Lord will be most disappointed by your attitude, maybe a bit too disappointed, yes? I'll be forced to take all documents from you that relate to this endeavor, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sorry, I just can't right now. Please erase all records of my progress.
	if (branchId == 9 && response == "s_79855d10")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_removeTicket (player, self);

			//-- NPC: Fine, get your smelly self out of here, yes?
			string_id message = new string_id (c_stringFile, "s_969790d9");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Our bountiful Lord will be most disappointed by your attitude, maybe a bit too disappointed, yes? I'll be forced to take all documents from you that relate to this endeavor, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Our bountiful Lord will be most disappointed by your attitude, maybe a bit too disappointed, yes? I'll be forced to take all documents from you that relate to this endeavor, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: You might be right. Fine, I'll get back to it.
	if (branchId == 9 && response == "s_bdde1d3a")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Good, now hurry, yes?
			string_id message = new string_id (c_stringFile, "s_ce8f15d7");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Our bountiful Lord will be most disappointed by your attitude, maybe a bit too disappointed, yes? I'll be forced to take all documents from you that relate to this endeavor, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Gambling Debt List.
	if (branchId == 12 && response == "s_465d7ab2")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_giveTicket (player, self);

			//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
			string_id message = new string_id (c_stringFile, "s_c79b5a4b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Research Request.
	if (branchId == 12 && response == "s_3f04cefb")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc1 (player, self);

			//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
			string_id message = new string_id (c_stringFile, "s_e9499aa6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Purchase Request Form.
	if (branchId == 12 && response == "s_fecfb00f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc2 (player, self);

			//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
			string_id message = new string_id (c_stringFile, "s_8c845e29");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, but how do I get to the corvette?
	if (branchId == 12 && response == "s_3c2e92a")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Go talk to Klaatu outside behind the palace and give him the travel authorization I provided you with, yes? He will make all the arrangements. If you succeed in your mission, return to me for your reward. Now hurry, who knows when the corvette will move on, yes?
			string_id message = new string_id (c_stringFile, "s_c1d18ea0");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: On my way to check one of the other hideouts.
	if (branchId == 12 && response == "s_c5ae9239")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Splendid but hurry, yes? Our magnificent Lord is not known for his patience, yes?
			string_id message = new string_id (c_stringFile, "s_502c2a8d");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: You are back already, yes? You have the documents, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I believe so.
	if (branchId == 13 && response == "s_24dbc07d")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Let me see that, yes?
			string_id message = new string_id (c_stringFile, "s_6333f589");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'You are back already, yes? You have the documents, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: You are back already, yes? You have the documents, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Could you go over the planets and pirates part again?
	if (branchId == 13 && response == "s_9b8c1f57")
	{
		corvette_bronell_neutral_assassin_action_clearLocations (player, self);

		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Bah! Fine. Talus, Naboo, or Lok... which planet, yes?
			string_id message = new string_id (c_stringFile, "s_f9bdde1d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'You are back already, yes? You have the documents, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: You are back already, yes? You have the documents, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sorry, I have other things to do. Erase all knowledge of my progress.
	if (branchId == 13 && response == "s_3e2e8616")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_quitCorvetteQuest (player, self);

			//-- NPC: Our bountiful Lord will be most disappointed by your response, yes? Also, I must take all documents from you that are connected to this business, yes? Yes.
			string_id message = new string_id (c_stringFile, "s_88ef705e");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'You are back already, yes? You have the documents, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Gambling Debt List.
	if (branchId == 14 && response == "s_465d7ab2")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_giveTicket (player, self);

			//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
			string_id message = new string_id (c_stringFile, "s_c79b5a4b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Research Request.
	if (branchId == 14 && response == "s_3f04cefb")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc1 (player, self);

			//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
			string_id message = new string_id (c_stringFile, "s_e9499aa6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Purchase Request Form.
	if (branchId == 14 && response == "s_fecfb00f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc2 (player, self);

			//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
			string_id message = new string_id (c_stringFile, "s_8c845e29");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, but how do I get to the corvette?
	if (branchId == 14 && response == "s_3c2e92a")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Go talk to Klaatu outside behind the palace and give him the travel authorization I provided you with, yes? He will make all the arrangements. If you succeed in your mission, return to me for your reward. Now hurry, who knows when the corvette will move on, yes?
			string_id message = new string_id (c_stringFile, "s_c1d18ea0");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see that, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: On my way to check one of the other hideouts.
	if (branchId == 14 && response == "s_c5ae9239")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Splendid but hurry, yes? Our magnificent Lord is not known for his patience, yes?
			string_id message = new string_id (c_stringFile, "s_502c2a8d");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see that, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Gambling Debt List.
	if (branchId == 15 && response == "s_465d7ab2")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_giveTicket (player, self);

			//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
			string_id message = new string_id (c_stringFile, "s_c79b5a4b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Research Request.
	if (branchId == 15 && response == "s_3f04cefb")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc1 (player, self);

			//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
			string_id message = new string_id (c_stringFile, "s_e9499aa6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Purchase Request Form.
	if (branchId == 15 && response == "s_fecfb00f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc2 (player, self);

			//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
			string_id message = new string_id (c_stringFile, "s_8c845e29");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, but how do I get to the corvette?
	if (branchId == 15 && response == "s_3c2e92a")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Go talk to Klaatu outside behind the palace and give him the travel authorization I provided you with, yes? He will make all the arrangements. If you succeed in your mission, return to me for your reward. Now hurry, who knows when the corvette will move on, yes?
			string_id message = new string_id (c_stringFile, "s_c1d18ea0");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: On my way to check one of the other hideouts.
	if (branchId == 15 && response == "s_c5ae9239")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Splendid but hurry, yes? Our magnificent Lord is not known for his patience, yes?
			string_id message = new string_id (c_stringFile, "s_502c2a8d");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Gambling Debt List.
	if (branchId == 16 && response == "s_465d7ab2")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_giveTicket (player, self);

			//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
			string_id message = new string_id (c_stringFile, "s_c79b5a4b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Research Request.
	if (branchId == 16 && response == "s_3f04cefb")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc1 (player, self);

			//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
			string_id message = new string_id (c_stringFile, "s_e9499aa6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Purchase Request Form.
	if (branchId == 16 && response == "s_fecfb00f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc2 (player, self);

			//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
			string_id message = new string_id (c_stringFile, "s_8c845e29");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, but how do I get to the corvette?
	if (branchId == 16 && response == "s_3c2e92a")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Go talk to Klaatu outside behind the palace and give him the travel authorization I provided you with, yes? He will make all the arrangements. If you succeed in your mission, return to me for your reward. Now hurry, who knows when the corvette will move on, yes?
			string_id message = new string_id (c_stringFile, "s_c1d18ea0");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: On my way to check one of the other hideouts.
	if (branchId == 16 && response == "s_c5ae9239")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Splendid but hurry, yes? Our magnificent Lord is not known for his patience, yes?
			string_id message = new string_id (c_stringFile, "s_502c2a8d");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Gambling Debt List.
	if (branchId == 17 && response == "s_465d7ab2")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_giveTicket (player, self);

			//-- NPC: Splendid, let me see. Hmm, no, no...wait, yes! There he is! So that's where his ship is, yes? You must intercept this corvette and arrange his demise immediately, yes? If you succeed in this, Lord Jabba has allowed me to reward you generously, yes? Yes!
			string_id message = new string_id (c_stringFile, "s_c79b5a4b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Research Request.
	if (branchId == 17 && response == "s_3f04cefb")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc1 (player, self);

			//-- NPC: Let me see. Hmm, yes, yes...No! Did you even read this? Oh well, guess I have to compensate you for your time, yes? Now check the other hideouts!
			string_id message = new string_id (c_stringFile, "s_e9499aa6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Here, take a look at this Purchase Request Form.
	if (branchId == 17 && response == "s_fecfb00f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_takeDoc2 (player, self);

			//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
			string_id message = new string_id (c_stringFile, "s_8c845e29");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Here, take a look at this Gambling Debt List.
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Here, take a look at this Research Request.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Here, take a look at this Purchase Request Form.
			boolean hasResponse2 = false;
			if (corvette_bronell_neutral_assassin_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, but how do I get to the corvette?
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: On my way to check one of the other hideouts.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465d7ab2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f04cefb");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fecfb00f");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3c2e92a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c5ae9239");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, but how do I get to the corvette?
	if (branchId == 17 && response == "s_3c2e92a")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Go talk to Klaatu outside behind the palace and give him the travel authorization I provided you with, yes? He will make all the arrangements. If you succeed in your mission, return to me for your reward. Now hurry, who knows when the corvette will move on, yes?
			string_id message = new string_id (c_stringFile, "s_c1d18ea0");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: On my way to check one of the other hideouts.
	if (branchId == 17 && response == "s_c5ae9239")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Splendid but hurry, yes? Our magnificent Lord is not known for his patience, yes?
			string_id message = new string_id (c_stringFile, "s_502c2a8d");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Hmm, yes, yes, completely useless. Here's some credits as compensation for you time. Anything else, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Bah! Fine. Talus, Naboo, or Lok... which planet, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Talus, yes? I mean, Talus?
	if (branchId == 20 && response == "s_94bc6e8f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation1 (player, self);

			//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
			string_id message = new string_id (c_stringFile, "s_f7203ef1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Bah! Fine. Talus, Naboo, or Lok... which planet, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Bah! Fine. Talus, Naboo, or Lok... which planet, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Naboo, ok.
	if (branchId == 20 && response == "s_2ad476a3")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation2 (player, self);

			//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
			string_id message = new string_id (c_stringFile, "s_469c9070");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Bah! Fine. Talus, Naboo, or Lok... which planet, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Bah! Fine. Talus, Naboo, or Lok... which planet, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Lok, yes? Bah, look what you did to me.
	if (branchId == 20 && response == "s_ba696f5d")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation3 (player, self);

			//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
			string_id message = new string_id (c_stringFile, "s_e1341a5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Bah! Fine. Talus, Naboo, or Lok... which planet, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Bah! Fine. Talus, Naboo, or Lok... which planet, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'll try and get the information we need.
	if (branchId == 20 && response == "s_76e2b2f6")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_acceptsQuest (player, self);

			//-- NPC: Excellent! Now hurry my noble warrior, hurry for Bronell...I mean our magnificent Lord Hutt! Yes? Yes.
			string_id message = new string_id (c_stringFile, "s_b1e7cb8c");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Bah! Fine. Talus, Naboo, or Lok... which planet, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Bah! Fine. Talus, Naboo, or Lok... which planet, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes? Yes! I've had enough, goodbye.
	if (branchId == 20 && response == "s_12eb211c")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_quitCorvetteQuest (player, self);

			//-- NPC: Farewell, maybe I'll never see you again, yes?
			string_id message = new string_id (c_stringFile, "s_ecc1d177");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Bah! Fine. Talus, Naboo, or Lok... which planet, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: You there. I must demand your assistance with this mission of utmost importance. You will help, yes? This mission is vital to Lord Jabba and I am only speaking on his behalf. I, of course, get nothing out of this myself. So you will help, yes yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Depends, tell me about this mission?
	if (branchId == 23 && response == "s_d62f7d90")
	{
		corvette_bronell_neutral_assassin_action_clearLocations (player, self);

		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Yes, indeed. Lord Hutt has a problem with a person who owes more than he could possibly pay, yes? You know what our venerable Lord does to those who can't pay debts, yes?
			string_id message = new string_id (c_stringFile, "s_bef20f69");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Was that a question or a statement?
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds too much like something I'd want no part of, sorry.
			boolean hasResponse1 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f5dcf8ec");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85bcb083");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'You there. I must demand your assistance with this mission of utmost importance. You will help, yes? This mission is vital to Lord Jabba and I am only speaking on his behalf. I, of course, get nothing out of this myself. So you will help, yes yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: You there. I must demand your assistance with this mission of utmost importance. You will help, yes? This mission is vital to Lord Jabba and I am only speaking on his behalf. I, of course, get nothing out of this myself. So you will help, yes yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sorry, I have other things to do.
	if (branchId == 23 && response == "s_bd40a944")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Then maybe you could leave, yes?
			string_id message = new string_id (c_stringFile, "s_7d29adc4");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'You there. I must demand your assistance with this mission of utmost importance. You will help, yes? This mission is vital to Lord Jabba and I am only speaking on his behalf. I, of course, get nothing out of this myself. So you will help, yes yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Yes, indeed. Lord Hutt has a problem with a person who owes more than he could possibly pay, yes? You know what our venerable Lord does to those who can't pay debts, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Was that a question or a statement?
	if (branchId == 24 && response == "s_f5dcf8ec")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Quiet! I was not done yet, yes? This person is an officer in the Corellian Security Forces and thinks that will protect him, yes? He has incurred debts all over the galaxy, though, and I know a way to find him, yes?
			string_id message = new string_id (c_stringFile, "s_560a7d6e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I don't know... have you?
			boolean hasResponse0 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eece1fb8");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Yes, indeed. Lord Hutt has a problem with a person who owes more than he could possibly pay, yes? You know what our venerable Lord does to those who can't pay debts, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Yes, indeed. Lord Hutt has a problem with a person who owes more than he could possibly pay, yes? You know what our venerable Lord does to those who can't pay debts, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds too much like something I'd want no part of, sorry.
	if (branchId == 24 && response == "s_85bcb083")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Farewell, maybe I'll never see you again, yes?
			string_id message = new string_id (c_stringFile, "s_ecc1d177");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Yes, indeed. Lord Hutt has a problem with a person who owes more than he could possibly pay, yes? You know what our venerable Lord does to those who can't pay debts, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Quiet! I was not done yet, yes? This person is an officer in the Corellian Security Forces and thinks that will protect him, yes? He has incurred debts all over the galaxy, though, and I know a way to find him, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know... have you?
	if (branchId == 25 && response == "s_eece1fb8")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			//-- NPC: Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?
			string_id message = new string_id (c_stringFile, "s_832183d2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Quiet! I was not done yet, yes? This person is an officer in the Corellian Security Forces and thinks that will protect him, yes? He has incurred debts all over the galaxy, though, and I know a way to find him, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Talus, yes? I mean, Talus?
	if (branchId == 26 && response == "s_94bc6e8f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation1 (player, self);

			//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
			string_id message = new string_id (c_stringFile, "s_f7203ef1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Naboo, ok.
	if (branchId == 26 && response == "s_2ad476a3")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation2 (player, self);

			//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
			string_id message = new string_id (c_stringFile, "s_469c9070");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Lok, yes? Bah, look what you did to me.
	if (branchId == 26 && response == "s_ba696f5d")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation3 (player, self);

			//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
			string_id message = new string_id (c_stringFile, "s_e1341a5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'll try and get the information we need.
	if (branchId == 26 && response == "s_76e2b2f6")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_acceptsQuest (player, self);

			//-- NPC: Excellent! Now hurry my noble warrior, hurry for Bronell...I mean our magnificent Lord Hutt! Yes? Yes.
			string_id message = new string_id (c_stringFile, "s_b1e7cb8c");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes? Yes! I've had enough, goodbye.
	if (branchId == 26 && response == "s_12eb211c")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_quitCorvetteQuest (player, self);

			//-- NPC: Farewell, maybe I'll never see you again, yes?
			string_id message = new string_id (c_stringFile, "s_ecc1d177");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Didn't I just say I have, yes? There are three lowly gangs of pirates, and this CorSec officer owes money to each of them, yes? These pirates can be found on Talus, Naboo, and Lok. Any of them may have documented his whereabouts, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Talus, yes? I mean, Talus?
	if (branchId == 27 && response == "s_94bc6e8f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation1 (player, self);

			//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
			string_id message = new string_id (c_stringFile, "s_f7203ef1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Naboo, ok.
	if (branchId == 27 && response == "s_2ad476a3")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation2 (player, self);

			//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
			string_id message = new string_id (c_stringFile, "s_469c9070");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Lok, yes? Bah, look what you did to me.
	if (branchId == 27 && response == "s_ba696f5d")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation3 (player, self);

			//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
			string_id message = new string_id (c_stringFile, "s_e1341a5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'll try and get the information we need.
	if (branchId == 27 && response == "s_76e2b2f6")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_acceptsQuest (player, self);

			//-- NPC: Excellent! Now hurry my noble warrior, hurry for Bronell...I mean our magnificent Lord Hutt! Yes? Yes.
			string_id message = new string_id (c_stringFile, "s_b1e7cb8c");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes? Yes! I've had enough, goodbye.
	if (branchId == 27 && response == "s_12eb211c")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_quitCorvetteQuest (player, self);

			//-- NPC: Farewell, maybe I'll never see you again, yes?
			string_id message = new string_id (c_stringFile, "s_ecc1d177");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Talus, yes? I mean, Talus?
	if (branchId == 28 && response == "s_94bc6e8f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation1 (player, self);

			//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
			string_id message = new string_id (c_stringFile, "s_f7203ef1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Naboo, ok.
	if (branchId == 28 && response == "s_2ad476a3")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation2 (player, self);

			//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
			string_id message = new string_id (c_stringFile, "s_469c9070");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Lok, yes? Bah, look what you did to me.
	if (branchId == 28 && response == "s_ba696f5d")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation3 (player, self);

			//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
			string_id message = new string_id (c_stringFile, "s_e1341a5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'll try and get the information we need.
	if (branchId == 28 && response == "s_76e2b2f6")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_acceptsQuest (player, self);

			//-- NPC: Excellent! Now hurry my noble warrior, hurry for Bronell...I mean our magnificent Lord Hutt! Yes? Yes.
			string_id message = new string_id (c_stringFile, "s_b1e7cb8c");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes? Yes! I've had enough, goodbye.
	if (branchId == 28 && response == "s_12eb211c")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_quitCorvetteQuest (player, self);

			//-- NPC: Farewell, maybe I'll never see you again, yes?
			string_id message = new string_id (c_stringFile, "s_ecc1d177");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Talus, yes? I mean, Talus?
	if (branchId == 29 && response == "s_94bc6e8f")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation1 (player, self);

			//-- NPC: On Talus resides the despicable gang of pirates called Binayre. Scum they are, yes? Well below our notice normally, but this CorSec officer owes them a small amount of money. Perhaps they have information on CorSec operations that would tell us his location, yes?
			string_id message = new string_id (c_stringFile, "s_f7203ef1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Naboo, ok.
	if (branchId == 29 && response == "s_2ad476a3")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation2 (player, self);

			//-- NPC: A ragtag bunch of pirates has a hideout there, yes? They are mostly known for kidnapping Naboo royalty, but they indulge in gambling from time to time as well. This CorSec officer owes them money, yes? He has quite a gambling problem, yes?
			string_id message = new string_id (c_stringFile, "s_469c9070");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Lok, yes? Bah, look what you did to me.
	if (branchId == 29 && response == "s_ba696f5d")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_heardLocation3 (player, self);

			//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
			string_id message = new string_id (c_stringFile, "s_e1341a5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Talus, yes? I mean, Talus?
			boolean hasResponse0 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Naboo, ok.
			boolean hasResponse1 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Lok, yes? Bah, look what you did to me.
			boolean hasResponse2 = false;
			if (!corvette_bronell_neutral_assassin_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Alright, I'll try and get the information we need.
			boolean hasResponse3 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Yes? Yes! I've had enough, goodbye.
			boolean hasResponse4 = false;
			if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_94bc6e8f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ad476a3");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ba696f5d");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76e2b2f6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_12eb211c");

				setObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, I'll try and get the information we need.
	if (branchId == 29 && response == "s_76e2b2f6")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_acceptsQuest (player, self);

			//-- NPC: Excellent! Now hurry my noble warrior, hurry for Bronell...I mean our magnificent Lord Hutt! Yes? Yes.
			string_id message = new string_id (c_stringFile, "s_b1e7cb8c");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes? Yes! I've had enough, goodbye.
	if (branchId == 29 && response == "s_12eb211c")
	{
		//-- [NOTE] 
		if (corvette_bronell_neutral_assassin_condition__defaultCondition (player, self))
		{
			corvette_bronell_neutral_assassin_action_quitCorvetteQuest (player, self);

			//-- NPC: Farewell, maybe I'll never see you again, yes?
			string_id message = new string_id (c_stringFile, "s_ecc1d177");
			removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I learned of a third option from an informant... well, a former informant. While his tongue was still attached, yes? The Canyon Corsairs on Lok have also dealt with this gambling maniac, yes? They might have the correct information as well, yes?' were false.");

		return SCRIPT_CONTINUE;
	}

	chat.chat (self, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	removeObjVar (player, "conversation.corvette_bronell_neutral_assassin.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

