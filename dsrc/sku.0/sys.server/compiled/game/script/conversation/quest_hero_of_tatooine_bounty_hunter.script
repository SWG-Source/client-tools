// ======================================================================
//
// quest_hero_of_tatooine_bounty_hunter.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.sui;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/quest_hero_of_tatooine_bounty_hunter";

// ======================================================================
// Script Conditions
// ======================================================================

boolean quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean quest_hero_of_tatooine_bounty_hunter_condition_complete (obj_id player, obj_id npc)
{
	if(hasObjVar(player, "quest.hero_of_tatooine.intellect.complete"))
		return true;
		
	if(hasObjVar(player, "quest.hero_of_tatooine.complete"))
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean quest_hero_of_tatooine_bounty_hunter_condition_failed (obj_id player, obj_id npc)
{
	if(hasObjVar(player, "quest.hero_of_tatooine.intellect.failed")) {
	
		obj_id last_bh = getObjIdObjVar(player, "quest.hero_of_tatooine.intellect.failed");
		
		if(last_bh == npc) {
		
			return true;
		
		} else {
		
			removeObjVar(player, "quest.hero_of_tatooine.intellect.failed");
		}
			
	}
	
	return false;
}

// ----------------------------------------------------------------------

boolean quest_hero_of_tatooine_bounty_hunter_condition_in_progress (obj_id player, obj_id npc)
{
	if(utils.hasScriptVar(player, "quest.hero_of_tatooine.intellect.in_progress"))
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean quest_hero_of_tatooine_bounty_hunter_condition_quest_done (obj_id player, obj_id npc)
{
	if(hasObjVar(npc, "quest.hero_of_tatooine.intellect.spawner.complete"))
		return true;
	
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void quest_hero_of_tatooine_bounty_hunter_action_set_help (obj_id player, obj_id npc)
{
	utils.setScriptVar(player, "quest.hero_of_tatooine.intellect.in_progress", 1);
}

// ----------------------------------------------------------------------

void quest_hero_of_tatooine_bounty_hunter_action_point_him_out (obj_id player, obj_id npc)
{
	obj_id control = getObjIdObjVar(npc, "quest.hero_of_tatooine.intellect.spawner.controller");
	
	if(!isIdValid(control))
		return;
		
	resizeable obj_id[] liars = getResizeableObjIdArrayObjVar(control, "quest.hero_of_tatooine.intellect.spawner.active.liars");
	resizeable obj_id[] newLiars = new obj_id[0];
	
	int size = liars.size();

	while(size > 0) {

		int i = rand(0, (size - 1));
		
		newLiars = utils.addElement(newLiars, liars.get(i));
		liars = utils.removeElementAt(liars, i);
		size--;
	}
	
	if(newLiars == null || newLiars.length == 0)
		return;
		
	string[] names = new string[newLiars.length];
	
	for(int i = 0; i < newLiars.length; i++) {
	
		int num = getIntObjVar(newLiars[i], "quest.hero_of_tatooine.intellect.spawner.npc");
		if(num == 3)
			utils.setScriptVar(player, "quest.hero_of_tatooine.intellect.liar", i);
		
		string name = getEncodedName(newLiars[i]);
		names[i] = name;
	}
	
	string prompt = "@quest/hero_of_tatooine/intellect_liar:sui_prompt";
	string title = "@quest/hero_of_tatooine/intellect_liar:sui_title";
	string btn_ok = "@quest/hero_of_tatooine/intellect_liar:sui_btn_ok";
	string btn_cancel = "@quest/hero_of_tatooine/intellect_liar:sui_btn_cancel";
	
	int pid = sui.listbox(npc, player, prompt, sui.OK_CANCEL, title, names, "handleImplicationSui", false, false);
	
	if(pid > -1) {

		setSUIProperty(pid, sui.LISTBOX_BTN_OK, sui.PROP_TEXT, btn_ok);
		setSUIProperty(pid, sui.LISTBOX_BTN_CANCEL, sui.PROP_TEXT, btn_cancel);
		showSUIPage(pid);
	}
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

string quest_hero_of_tatooine_bounty_hunter_tokenTO_smuggler_leader (obj_id player, obj_id npc)
{
	obj_id control = getObjIdObjVar(npc, "quest.hero_of_tatooine.intellect.spawner.controller");
	
	obj_id[] liars = getObjIdArrayObjVar(npc, "quest.hero_of_tatooine.intellect.spawner.active.liars");
	
	obj_id leader = liars[0];
	
	string name = getEncodedName(leader);
	
	if(name == null)
		name = "";
	
	return name;
}

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int quest_hero_of_tatooine_bounty_hunter_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Did you find out who my mark is?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I think I know who it is.
	if (response == "s_1b0651f8")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			quest_hero_of_tatooine_bounty_hunter_action_point_him_out (player, npc);

			//-- NPC: That's great! Just point out who you think it is. If your reasoning is justified, I'll take care of the rest.
			string_id message = new string_id (c_stringFile, "s_fc5b59e0");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not yet. I'm still looking.
	if (response == "s_69cdbab1")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: OK. If you find out who it is, let me know.
			string_id message = new string_id (c_stringFile, "s_cd33dbbc");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You there. Do you know any of these smugglers here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, can't say as I do.
	if (response == "s_775eed34")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I figured as much. Blast it all. Now what do I do?
			string_id message = new string_id (c_stringFile, "s_7eb6abbe");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, that one guy does look kinda familiar.
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What seems to be the problem?
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I don't know, but good luck!
			boolean hasResponse2 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b8f4813d");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a49ad86c");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Smugglers? Why would I know any smugglers?
	if (response == "s_12afde00")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: No, I didn't think that you would. Curses. Now what do I do?
			string_id message = new string_id (c_stringFile, "s_60d63ec8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, that one guy does look kinda familiar.
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What seems to be the problem?
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I don't know, but good luck!
			boolean hasResponse2 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b8f4813d");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_59");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a49ad86c");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't have time to talk right now. Sorry.
	if (response == "s_3769e5f8")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Then leave me alone. I'm busy.
			string_id message = new string_id (c_stringFile, "s_54e633f8");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I figured as much. Blast it all. Now what do I do?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, that one guy does look kinda familiar.
	if (response == "s_b8f4813d")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've got no time for 'kinda familiar'. I need concrete information.
			string_id message = new string_id (c_stringFile, "s_dec94c0f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What seems to be the problem?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, so long then.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d3412303");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5fdaa4bb");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What seems to be the problem?
	if (response == "s_59")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of these smugglers is my mark. I think. I'm not sure.  I've been tracking him for some time and just now caught up with him here in this city.
			string_id message = new string_id (c_stringFile, "s_250df636");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why don't you arrest him?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You're not sure?
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Good luck with all that!
			boolean hasResponse2 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_922dd166");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eaa5c299");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dd6f7e");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know, but good luck!
	if (response == "s_a49ad86c")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: A lotta help you were. Get out of here.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've got no time for 'kinda familiar'. I need concrete information.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What seems to be the problem?
	if (response == "s_d3412303")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of these smugglers is my mark. I think. I'm not sure.  I've been tracking him for some time and just now caught up with him here in this city.
			string_id message = new string_id (c_stringFile, "s_250df636");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why don't you arrest him?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You're not sure?
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Good luck with all that!
			boolean hasResponse2 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_922dd166");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eaa5c299");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dd6f7e");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, so long then.
	if (response == "s_5fdaa4bb")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: A lotta help you were. Get out of here.
			string_id message = new string_id (c_stringFile, "s_86c02324");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: One of these smugglers is my mark. I think. I'm not sure.  I've been tracking him for some time and just now caught up with him here in this city.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then why don't you arrest him?
	if (response == "s_922dd166")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: 'Arrest him'... heh. Yea, I would 'arrest him' but the problem is, I don't know which one of these smugglers he is.
			string_id message = new string_id (c_stringFile, "s_d9ec1dc4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I thought you were tracking him for a long time?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You've got to be the worst bounty hunter ever.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Well, I hope you find him. Good-bye!
			boolean hasResponse2 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df981c5e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3ece0ca2");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You're not sure?
	if (response == "s_eaa5c299")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, that's the thing. I don't know which one of these smugglers is my mark.
			string_id message = new string_id (c_stringFile, "s_b8bf2aa9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I thought you were tracking him for a long time?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You've got to be the worst bounty hunter ever.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Well, I hope you find him. Good-bye!
			boolean hasResponse2 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_df981c5e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3ece0ca2");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Good luck with all that!
	if (response == "s_dd6f7e")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, yea. Get out of here.
			string_id message = new string_id (c_stringFile, "s_55");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: 'Arrest him'... heh. Yea, I would 'arrest him' but the problem is, I don't know which one of these smugglers he is.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I thought you were tracking him for a long time?
	if (response == "s_df981c5e")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The guy I'm looking for stole some equipment. Nobody got a good look at him, but the crate that he stole had a transponder signal attached to it.
			string_id message = new string_id (c_stringFile, "s_ffdc10ae");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why not find the one with the transponder?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've helped all I can. Good-bye.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1fa16fd3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e244342");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You've got to be the worst bounty hunter ever.
	if (response == "s_3ece0ca2")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Listen up, kid. This stuff isn't as easy as it sounds. Cut me some slack.
			string_id message = new string_id (c_stringFile, "s_659bb141");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I thought you were tracking him for a long time?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, I hope you find him. Good-bye!
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239b0909");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, I hope you find him. Good-bye!
	if (response == "s_48")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, yea. Get out of here.
			string_id message = new string_id (c_stringFile, "s_50");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The guy I'm looking for stole some equipment. Nobody got a good look at him, but the crate that he stole had a transponder signal attached to it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then why not find the one with the transponder?
	if (response == "s_1fa16fd3")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must think I'm stupid don't you? A little while after I tracked the signal to this city, it went dead. He must have found it and turned it off.
			string_id message = new string_id (c_stringFile, "s_bebac1d7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Have you questioned all the smugglers here?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds like you have it under control.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dce437b6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7cf16bce");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've helped all I can. Good-bye.
	if (response == "s_5e244342")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, yea. Get out of here.
			string_id message = new string_id (c_stringFile, "s_39");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You must think I'm stupid don't you? A little while after I tracked the signal to this city, it went dead. He must have found it and turned it off.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Have you questioned all the smugglers here?
	if (response == "s_dce437b6")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Questioned them? Yep, I found all the suspected smugglers in the area and brought them here. Questioned them, pushed them around, roughed them up, you name it.
			string_id message = new string_id (c_stringFile, "s_806d4263");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What did you find out?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Good for you. So long!
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47482ee2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1892fb83");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like you have it under control.
	if (response == "s_7cf16bce")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, yea. Get out of here.
			string_id message = new string_id (c_stringFile, "s_36");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Questioned them? Yep, I found all the suspected smugglers in the area and brought them here. Questioned them, pushed them around, roughed them up, you name it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What did you find out?
	if (response == "s_47482ee2")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: To be honest, I'm a little confused. I know the smuggler captain and I can trust him. But everyone else just blames each other, and I don't know what to do.
			string_id message = new string_id (c_stringFile, "s_254a92c6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Maybe I can find out who your mark is.
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Wow, I wouldn't want to be in your position.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_369a8bff");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_402e3e9a");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Good for you. So long!
	if (response == "s_1892fb83")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, yea. Get out of here.
			string_id message = new string_id (c_stringFile, "s_33");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: To be honest, I'm a little confused. I know the smuggler captain and I can trust him. But everyone else just blames each other, and I don't know what to do.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe I can find out who your mark is.
	if (response == "s_369a8bff")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			quest_hero_of_tatooine_bounty_hunter_action_set_help (player, npc);

			//-- NPC: You really do think I'm stupid. I can handle this myself... but, uh... if you want to, give it a shot. See what you can find out.
			string_id message = new string_id (c_stringFile, "s_44f4df");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Wow, I wouldn't want to be in your position.
	if (response == "s_402e3e9a")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, yea. Get out of here.
			string_id message = new string_id (c_stringFile, "s_624a9b6f");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Listen up, kid. This stuff isn't as easy as it sounds. Cut me some slack.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I thought you were tracking him for a long time?
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The guy I'm looking for stole some equipment. Nobody got a good look at him, but the crate that he stole had a transponder signal attached to it.
			string_id message = new string_id (c_stringFile, "s_ffdc10ae");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why not find the one with the transponder?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've helped all I can. Good-bye.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1fa16fd3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e244342");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, I hope you find him. Good-bye!
	if (response == "s_239b0909")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, yea. Get out of here.
			string_id message = new string_id (c_stringFile, "s_46");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yea, that's the thing. I don't know which one of these smugglers is my mark.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I thought you were tracking him for a long time?
	if (response == "s_df981c5e")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: The guy I'm looking for stole some equipment. Nobody got a good look at him, but the crate that he stole had a transponder signal attached to it.
			string_id message = new string_id (c_stringFile, "s_ffdc10ae");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why not find the one with the transponder?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I've helped all I can. Good-bye.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1fa16fd3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e244342");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You've got to be the worst bounty hunter ever.
	if (response == "s_3ece0ca2")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Listen up, kid. This stuff isn't as easy as it sounds. Cut me some slack.
			string_id message = new string_id (c_stringFile, "s_659bb141");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I thought you were tracking him for a long time?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, I hope you find him. Good-bye!
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239b0909");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, I hope you find him. Good-bye!
	if (response == "s_48")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yea, yea. Get out of here.
			string_id message = new string_id (c_stringFile, "s_50");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int quest_hero_of_tatooine_bounty_hunter_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No, I didn't think that you would. Curses. Now what do I do?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, that one guy does look kinda familiar.
	if (response == "s_b8f4813d")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've got no time for 'kinda familiar'. I need concrete information.
			string_id message = new string_id (c_stringFile, "s_dec94c0f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What seems to be the problem?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, so long then.
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d3412303");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5fdaa4bb");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What seems to be the problem?
	if (response == "s_59")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: One of these smugglers is my mark. I think. I'm not sure.  I've been tracking him for some time and just now caught up with him here in this city.
			string_id message = new string_id (c_stringFile, "s_250df636");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why don't you arrest him?
			boolean hasResponse0 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You're not sure?
			boolean hasResponse1 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Good luck with all that!
			boolean hasResponse2 = false;
			if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_922dd166");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eaa5c299");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_dd6f7e");

				utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't know, but good luck!
	if (response == "s_a49ad86c")
	{
		//-- [NOTE] 
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			//-- NPC: A lotta help you were. Get out of here.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.quest_hero_of_tatooine_bounty_hunter");

	setCondition (self, CONDITION_CONVERSABLE);
	
	if(!hasScript(self, "quest.hero_of_tatooine.intellect_bounty_hunter"))
		attachScript(self, "quest.hero_of_tatooine.intellect_bounty_hunter");

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	
	attachScript(self, "quest.hero_of_tatooine.intellect_bounty_hunter");

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.quest_hero_of_tatooine_bounty_hunter");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (quest_hero_of_tatooine_bounty_hunter_condition_complete (player, npc))
	{
		//-- NPC: Hey there! Thanks again for helping me find the right mark. You're OK in my book.
		string_id message = new string_id (c_stringFile, "s_b932a57a");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (quest_hero_of_tatooine_bounty_hunter_condition_failed (player, npc))
	{
		//-- NPC: You had your shot, kid. I'm not going to give you another chance. Get out of here.
		string_id message = new string_id (c_stringFile, "s_29ecd548");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (quest_hero_of_tatooine_bounty_hunter_condition_quest_done (player, npc))
	{
		//-- NPC: No time to talk now, sorry.
		string_id message = new string_id (c_stringFile, "s_df879e");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (quest_hero_of_tatooine_bounty_hunter_condition_in_progress (player, npc))
	{
		//-- NPC: Did you find out who my mark is?
		string_id message = new string_id (c_stringFile, "s_faaf7075");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, I think I know who it is.
		boolean hasResponse0 = false;
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not yet. I'm still looking.
		boolean hasResponse1 = false;
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1b0651f8");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_69cdbab1");

			utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 4);

			npcStartConversation (player, npc, "quest_hero_of_tatooine_bounty_hunter", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
	{
		//-- NPC: You there. Do you know any of these smugglers here?
		string_id message = new string_id (c_stringFile, "s_bd2c41cb");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No, can't say as I do.
		boolean hasResponse0 = false;
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Smugglers? Why would I know any smugglers?
		boolean hasResponse1 = false;
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I don't have time to talk right now. Sorry.
		boolean hasResponse2 = false;
		if (quest_hero_of_tatooine_bounty_hunter_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_775eed34");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_12afde00");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3769e5f8");

			utils.setScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId", 7);

			npcStartConversation (player, npc, "quest_hero_of_tatooine_bounty_hunter", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "quest_hero_of_tatooine_bounty_hunter")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

	if (branchId == 4 && quest_hero_of_tatooine_bounty_hunter_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && quest_hero_of_tatooine_bounty_hunter_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && quest_hero_of_tatooine_bounty_hunter_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && quest_hero_of_tatooine_bounty_hunter_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && quest_hero_of_tatooine_bounty_hunter_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && quest_hero_of_tatooine_bounty_hunter_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && quest_hero_of_tatooine_bounty_hunter_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && quest_hero_of_tatooine_bounty_hunter_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && quest_hero_of_tatooine_bounty_hunter_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && quest_hero_of_tatooine_bounty_hunter_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && quest_hero_of_tatooine_bounty_hunter_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && quest_hero_of_tatooine_bounty_hunter_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && quest_hero_of_tatooine_bounty_hunter_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.quest_hero_of_tatooine_bounty_hunter.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

