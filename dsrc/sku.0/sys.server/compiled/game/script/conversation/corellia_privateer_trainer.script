// ======================================================================
//
// corellia_privateer_trainer.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.features;
include library.skill;
include library.space_flags;
include library.space_quest;
include library.space_skill;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/corellia_privateer_trainer";

// ======================================================================
// Script Conditions
// ======================================================================

boolean corellia_privateer_trainer_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_remembersPlayer (obj_id player, obj_id npc)
{
	return ( utils.hasScriptVar( player, "metCorSec" ));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasNoviceSkillBox (obj_id player, obj_id npc)
{
	return ( space_quest.hasSkill( player, "pilot_neutral_novice" ) );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasCorSecQuest (obj_id player, obj_id npc)
{
	return ( space_quest.hasQuest( player ) && corellia_privateer_trainer_condition_hasNoviceSkillBox(player, npc ) );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_isImperialPilot (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_imperial_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_isRebelPilot (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_isPilotButNotCorSec (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_neutral_novice" ) && (!space_flags.isSpaceTrack( player, space_flags.PRIVATEER_CORELLIA )) );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasCompletedQuestOne (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuestRecursive( player, "patrol", "corellia_privateer_1") );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasCompletedQuestTwo (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "destroy", "corellia_privateer_2") );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasCompletedQuestThree (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuestRecursive( player, "patrol", "corellia_privateer_3") );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasCompletedQuestFour (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuestRecursive( player, "assassinate", "corellia_privateer_tier1_4a") );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_missionOneFailed (obj_id player, obj_id npc)
{
		
	return (space_quest.hasFailedQuestRecursive(player, "patrol", "corellia_privateer_1")) ||
			(space_quest.hasAbortedQuestRecursive(player, "patrol", "corellia_privateer_1"));

}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_isReadyForFirstTraining (obj_id player, obj_id npc)
{
	//You have completed all 4 missions is handled by previous convo conditions
	
	if ( 	hasSkill( player, "pilot_neutral_starships_01") 	|| 
			hasSkill( player, "pilot_neutral_procedures_01") 	||
			hasSkill( player, "pilot_neutral_weapons_01") 		||
			hasSkill( player, "pilot_neutral_droid_01" ) 	)
		return false;//You have already received your initial skill (or more!)
	else	
		return true;
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_isReadyForMoreTraining (obj_id player, obj_id npc)
{
	//You have completed all 4 missions - checked via previous convo options
	//but don't have all the tier-1 skills yet, - //checked via previous convo options
	
	//if you have enough xp for this skill, then you have enough for
	//any other skill at this tier:
	return ( space_quest.isPlayerQualifiedForSkill( player, "pilot_neutral_starships_01") );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasAllTierOneSkills (obj_id player, obj_id npc)
{
	/*
	if (!hasSkill( player, "pilot_neutral_starships_01") )
		return false;
	else if (!hasSkill( player, "pilot_neutral_procedures_01") )
		return false;
	else if (!hasSkill( player, "pilot_neutral_weapons_01") )
		return false;
	else if (!hasSkill( player, "pilot_neutral_droid_01" ) )
		return false;
	else	
		return true;
	*/
	
	return space_flags.hasCompletedTierOne(player);
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_missionTwoFailed (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "destroy", "corellia_privateer_2" ) );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_beenRewardedForQuestOne (obj_id player, obj_id npc)
{
	if (!space_quest.hasCompletedQuestRecursive( player, "patrol", "corellia_privateer_1"))
		return true;
		
	//this returns true if you have completed this quest but have not been rewarded for it
	return ( space_quest.hasReceivedReward( player, "patrol", "corellia_privateer_1") );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_beenRewardedForQuestTwo (obj_id player, obj_id npc)
{
	if (!space_quest.hasCompletedQuest( player, "destroy", "corellia_privateer_2"))
		return true;
		
	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuest( player, "destroy", "corellia_privateer_2")) && 
				( space_quest.hasReceivedReward( player, "destroy", "corellia_privateer_2")) );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_beenRewardedForQuestThree (obj_id player, obj_id npc)
{
	if ( !space_quest.hasCompletedQuestRecursive( player, "patrol", "corellia_privateer_3"))
		return true;
		
	//this returns true if you have completed this quest but have not been rewarded for it
	return ( space_quest.hasReceivedReward( player, "patrol", "corellia_privateer_3") );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_beenRewardedForQuestFour (obj_id player, obj_id npc)
{
	if (!space_quest.hasCompletedQuestRecursive( player, "assassinate", "corellia_privateer_tier1_4a"))
		return true;

	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuestRecursive( player, "assassinate", "corellia_privateer_tier1_4a")) && 
				( space_quest.hasReceivedReward( player, "assassinate", "corellia_privateer_tier1_4a")) );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_missionThreeFailed (obj_id player, obj_id npc)
{
		
	return (space_quest.hasFailedQuestRecursive(player, "patrol", "corellia_privateer_3")) ||
			(space_quest.hasAbortedQuestRecursive(player, "patrol", "corellia_privateer_3"));
		
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_missionFourFailed (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuestRecursive( player, "assassinate", "corellia_privateer_tier1_4a" ) )||
			(space_quest.hasAbortedQuestRecursive(player, "assassinate", "corellia_privateer_tier1_4a"));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasGoodCorSecFaction (obj_id player, obj_id npc)
{
	float corSecFaction = factions.getFactionStanding( player, "corsec" );
	if ( corSecFaction < 0 )
		return false;
	else	
		return true;
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasGreatCorSecFaction (obj_id player, obj_id npc)
{
	float corSecFaction = factions.getFactionStanding( player, "corsec" );
	if ( corSecFaction > 200 )
		return true;
	else	
		return false;
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasProcedureSkill1 (obj_id player, obj_id npc)
{
	return (hasSkill( player, "pilot_neutral_procedures_01"));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasWeaponsSkill1 (obj_id player, obj_id npc)
{
	return (hasSkill( player, "pilot_neutral_weapons_01"));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasDroidSkill1 (obj_id player, obj_id npc)
{
	return (hasSkill( player, "pilot_neutral_droid_01"));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasStarshipSkill1 (obj_id player, obj_id npc)
{
	return (hasSkill( player, "pilot_neutral_starships_01"));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasSpaceExpansion (obj_id player, obj_id npc)
{
	return ( features.isSpaceEdition( player ) );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasSpaceShip (obj_id player, obj_id npc)
{
	return (!space_quest.canGrantNewbieShip(player));
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_hasVeryFirstQuest (obj_id player, obj_id npc)
{
	//You've got your assignment, and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.

	if (!space_quest.hasCertifiedShip(player))
		return false; 
	else	
		return ( space_quest.hasQuest( player, "patrol", "corellia_privateer_1" ) ||
			  	 space_quest.hasQuest( player, "destroy_surpriseattack", "corellia_privateer_1" ) );
}

// ----------------------------------------------------------------------

boolean corellia_privateer_trainer_condition_canFlyNonTrackDuty (obj_id player, obj_id npc)
{
		if((hasSkill( player, "pilot_neutral_starships_01") || 
	    hasSkill( player, "pilot_neutral_procedures_01") ||
	    hasSkill( player, "pilot_neutral_weapons_01") ||
	    hasSkill( player, "pilot_neutral_droid_01" )) && !space_quest.hasQuest(player))
	    return true;
	    
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void corellia_privateer_trainer_action_rememberPlayer (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "metCorSec", true );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantNoviceSkillBox (obj_id player, obj_id npc)
{
	space_flags.setSpaceTrack( player, space_flags.PRIVATEER_CORELLIA );
	skill.noisyGrantSkill( player, "pilot_neutral_novice" );

	//remove when other convos stop looking for this:
	// setObjVar( player, "corSecMember", true );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantMissionOne (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "patrol", "corellia_privateer_1" );
	space_flags.restoreClientPath( player );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantMissionTwo (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy", "corellia_privateer_2" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantMissionThree (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "patrol", "corellia_privateer_3" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantMissionFour (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "assassinate", "corellia_privateer_tier1_4a" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_rewardForQuestOne (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "patrol", "corellia_privateer_1" ) )
		space_quest.giveReward( player, "patrol", "corellia_privateer_1", 100 );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_rewardForQuestTwo (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "destroy", "corellia_privateer_2" ) )
		space_quest.giveReward( player, "destroy", "corellia_privateer_2", 200 );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_rewardForQuestThree (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "patrol", "corellia_privateer_3" ) )
	{
		if ( getSpecies( player ) != SPECIES_ITHORIAN )
			space_quest.giveReward( player, "patrol", "corellia_privateer_3", 500, "object/tangible/wearables/bandolier/mercenary_bandolier.iff" );
		else
			space_quest.giveReward( player, "patrol", "corellia_privateer_3", 500, "object/tangible/wearables/bandolier/ith_mercenary_bandolier.iff" );
	}
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_rewardForQuestFour (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "assassinate", "corellia_privateer_tier1_4a" ) )
		space_quest.giveReward( player, "assassinate", "corellia_privateer_tier1_4a", 1000, "object/tangible/ship/components/weapon/wpn_mission_reward_neutral_mandal_light_blaster.iff" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantDroidSkill1 (obj_id player, obj_id npc)
{
	skill.noisyGrantSkill( player, "pilot_neutral_droid_01" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantProcedureSkill1 (obj_id player, obj_id npc)
{
	skill.noisyGrantSkill( player, "pilot_neutral_procedures_01" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantWeaponsSkill1 (obj_id player, obj_id npc)
{
	skill.noisyGrantSkill( player, "pilot_neutral_weapons_01" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantStarshipSkill1 (obj_id player, obj_id npc)
{
	skill.noisyGrantSkill( player, "pilot_neutral_starships_01" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_buyWeaponsSkill1 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_weapons_01" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_buyDroidSkill1 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_droid_01" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_buyStarshipSkill1 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_starships_01" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_buyProcedureSkill1 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_procedures_01" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantDestroyDutyMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy_duty", "corellia_privateer_6" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantPatrolDutyMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "patrol_duty", "corellia_privateer_5" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantQuestEight (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy", "corellia_privateer_8" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantEscortDutyMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "escort_duty", "corellia_privateer_7" );
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_grantSpaceShip (obj_id player, obj_id npc)
{
	space_quest.grantNewbieShip( player, "neutral");
}

// ----------------------------------------------------------------------

void corellia_privateer_trainer_action_eraseClientPath (obj_id player, obj_id npc)
{
	space_flags.clearClientPath( player );
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int corellia_privateer_trainer_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: An assignment?
	if (response == "s_95d0a1f6")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Yes. You have your first mission, now. You'll need to fly into space to complete it. Start by launching your starship at the starport.
			string_id message = new string_id (c_stringFile, "s_cda1bb94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What ship?
	if (response == "s_bdd269be")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I've given you the control codes to a basic ship. The codes are visible in your datapad. Open your personal datapad, and review your ship status before you launch.
			string_id message = new string_id (c_stringFile, "s_198b5a97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Go to the starport. Inside the starport you will find a number of 'starship terminals.' Interface with the terminals in order to launch your starship.
			string_id message = new string_id (c_stringFile, "s_4ffb02bd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And when I'm done?
	if (response == "s_5e4ebfca")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you're finished with your first mission - fly back to the 'launch waypoint' that we automatically add to your datapad. Communicate with the space station and indicate that you want to land.
			string_id message = new string_id (c_stringFile, "s_2bbff5e2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes. You have your first mission, now. You'll need to fly into space to complete it. Start by launching your starship at the starport.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: An assignment?
	if (response == "s_95d0a1f6")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Yes. You have your first mission, now. You'll need to fly into space to complete it. Start by launching your starship at the starport.
			string_id message = new string_id (c_stringFile, "s_cda1bb94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What ship?
	if (response == "s_bdd269be")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I've given you the control codes to a basic ship. The codes are visible in your datapad. Open your personal datapad, and review your ship status before you launch.
			string_id message = new string_id (c_stringFile, "s_198b5a97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Go to the starport. Inside the starport you will find a number of 'starship terminals.' Interface with the terminals in order to launch your starship.
			string_id message = new string_id (c_stringFile, "s_4ffb02bd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And when I'm done?
	if (response == "s_5e4ebfca")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you're finished with your first mission - fly back to the 'launch waypoint' that we automatically add to your datapad. Communicate with the space station and indicate that you want to land.
			string_id message = new string_id (c_stringFile, "s_2bbff5e2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've given you the control codes to a basic ship. The codes are visible in your datapad. Open your personal datapad, and review your ship status before you launch.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: An assignment?
	if (response == "s_95d0a1f6")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Yes. You have your first mission, now. You'll need to fly into space to complete it. Start by launching your starship at the starport.
			string_id message = new string_id (c_stringFile, "s_cda1bb94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What ship?
	if (response == "s_bdd269be")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I've given you the control codes to a basic ship. The codes are visible in your datapad. Open your personal datapad, and review your ship status before you launch.
			string_id message = new string_id (c_stringFile, "s_198b5a97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Go to the starport. Inside the starport you will find a number of 'starship terminals.' Interface with the terminals in order to launch your starship.
			string_id message = new string_id (c_stringFile, "s_4ffb02bd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And when I'm done?
	if (response == "s_5e4ebfca")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you're finished with your first mission - fly back to the 'launch waypoint' that we automatically add to your datapad. Communicate with the space station and indicate that you want to land.
			string_id message = new string_id (c_stringFile, "s_2bbff5e2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Go to the starport. Inside the starport you will find a number of 'starship terminals.' Interface with the terminals in order to launch your starship.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: An assignment?
	if (response == "s_95d0a1f6")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Yes. You have your first mission, now. You'll need to fly into space to complete it. Start by launching your starship at the starport.
			string_id message = new string_id (c_stringFile, "s_cda1bb94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What ship?
	if (response == "s_bdd269be")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I've given you the control codes to a basic ship. The codes are visible in your datapad. Open your personal datapad, and review your ship status before you launch.
			string_id message = new string_id (c_stringFile, "s_198b5a97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Go to the starport. Inside the starport you will find a number of 'starship terminals.' Interface with the terminals in order to launch your starship.
			string_id message = new string_id (c_stringFile, "s_4ffb02bd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And when I'm done?
	if (response == "s_5e4ebfca")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you're finished with your first mission - fly back to the 'launch waypoint' that we automatically add to your datapad. Communicate with the space station and indicate that you want to land.
			string_id message = new string_id (c_stringFile, "s_2bbff5e2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: When you're finished with your first mission - fly back to the 'launch waypoint' that we automatically add to your datapad. Communicate with the space station and indicate that you want to land.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: An assignment?
	if (response == "s_95d0a1f6")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Yes. You have your first mission, now. You'll need to fly into space to complete it. Start by launching your starship at the starport.
			string_id message = new string_id (c_stringFile, "s_cda1bb94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What ship?
	if (response == "s_bdd269be")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I've given you the control codes to a basic ship. The codes are visible in your datapad. Open your personal datapad, and review your ship status before you launch.
			string_id message = new string_id (c_stringFile, "s_198b5a97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: Go to the starport. Inside the starport you will find a number of 'starship terminals.' Interface with the terminals in order to launch your starship.
			string_id message = new string_id (c_stringFile, "s_4ffb02bd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And when I'm done?
	if (response == "s_5e4ebfca")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you're finished with your first mission - fly back to the 'launch waypoint' that we automatically add to your datapad. Communicate with the space station and indicate that you want to land.
			string_id message = new string_id (c_stringFile, "s_2bbff5e2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm looking for a mission. Do you have any?
	if (response == "s_c1ff5062")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_canFlyNonTrackDuty (player, npc))
		{
			//-- NPC: As a matter of fact, I do have some duty missions that you could perform. Who knows, maybe if you do well, you might be invited to join the CorSec. Are you ready for a duty mission?
			string_id message = new string_id (c_stringFile, "s_dadaabf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm sorry, unless you are a member of the CorSec, there's very little that I can do for you. You may want to try speaking to your own contacts if you're looking for a mission.
			string_id message = new string_id (c_stringFile, "s_5b8a384b");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: As a matter of fact, I do have some duty missions that you could perform. Who knows, maybe if you do well, you might be invited to join the CorSec. Are you ready for a duty mission?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Duty missions are a good way for you to get experience as a pilot. Only experienced pilots can receive the proper training to learn new skills and abilities.There's no real final objective so you can end the mission whenever you want. So, would you like to take a duty mission?
			string_id message = new string_id (c_stringFile, "s_d43b2932");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I am. What duty missions are available?
	if (response == "s_1a7f1a1b")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, merchant freighters are always in need of escorts, or if you like you can help in the fight against the pirate leaders. What interests you?
			string_id message = new string_id (c_stringFile, "s_d6a6a9fc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Pirate Leaders
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Escorting Merchants
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Nothing right now.
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9e417b37");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_221daf73");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c35e70d2");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 92);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, but how about some training?
	if (response == "s_6276c98")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're not ready for more training yet. But duty missions are a good way of getting good experience so that you'll be ready for training soon. What do you say? Are you ready to try a duty mission?
			string_id message = new string_id (c_stringFile, "s_e5c453e0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now, thanks.
	if (response == "s_2883b989")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Maybe next time then.
			string_id message = new string_id (c_stringFile, "s_228");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hello!  Come to join the ranks of CorSec?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes
	if (response == "s_370822d1")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: We don't let just anyone join.  There'll be a background check of course.
			string_id message = new string_id (c_stringFile, "s_63a61222");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you mean?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: That's ridiculous!
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1418de80");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77e48d5b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_64b308bc");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No
	if (response == "s_457a7010")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: In that case, move along.  We're very busy here.
			string_id message = new string_id (c_stringFile, "s_c8c21783");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is CorSec?
	if (response == "s_4906b993")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: CorSec is Corellia's local Security force.  As you know, Corellia is officially neutral in the Galactic Civil War... or at least as neutral as a world can be in this day and age.  We provide defense and law enforcement for the entire Corellian system.
			string_id message = new string_id (c_stringFile, "s_40061f60");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Oh.  No thanks
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I see...
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_deaabd84");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_614b71f0");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We don't let just anyone join.  There'll be a background check of course.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course
	if (response == "s_1418de80")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasGoodCorSecFaction (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: This will only take a moment... Ah... Well according to this you've run into a problem with the law.  Are you certain you want to join CorSec?
			string_id message = new string_id (c_stringFile, "s_be5a7539");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes I am
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No thanks
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e107077e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_360d8728");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasGreatCorSecFaction (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: This will only take a moment... Oh... Wow!  You've got an excellent track record.  I guess you've tangled with your share of criminals and thugs in your day. Are you ready to begin?
			string_id message = new string_id (c_stringFile, "s_7df39542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes I am
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No thanks
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_30");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_51");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: This will only take a moment... Ah... You've got a pretty clean record here.  I can assure you that it will improve once you're flying for CorSec! Are you ready to begin?
			string_id message = new string_id (c_stringFile, "s_ee2895df");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes I am
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No thanks
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_30");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_51");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you mean?
	if (response == "s_77e48d5b")
	{
		doAnimationAction (player, "cover_mouth");

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasGoodCorSecFaction (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: Don't worry, this will only take a moment.  We do need to ensure you're not some sort of criminal... Ah... Well according to this you haven't exactly been the most law abiding citizen on Corellia... Are you certain you want to join CorSec?
			string_id message = new string_id (c_stringFile, "s_b06e1df9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I guess so.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't think so
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af22b9b2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_984b22b");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasGreatCorSecFaction (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: Don't worry, this will only take a moment... Oh... Wow!  You've got an excellent track record.  I guess you've tangled with your share of criminals and thugs in your day. Are you ready to begin?
			string_id message = new string_id (c_stringFile, "s_ea3dcf32");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I guess so.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't think so
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 33);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: Don't worry, this will only take a moment... Ah... You've got a pretty clean record here.  I can assure you that it will improve once you're flying for CorSec! Are you ready to begin?
			string_id message = new string_id (c_stringFile, "s_898027d6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I guess so.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I don't think so
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 33);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's ridiculous!
	if (response == "s_64b308bc")
	{
		doAnimationAction (player, "shake_head_disgust");

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasGoodCorSecFaction (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: Not at all.  We can't have thugs and ne'er-do-wells joining CorSec.  We're here to provide security for the citizens of Corellia!  Now, this will only take a moment... Ah... Well according to this you've run into a problem with the law.  Are you certain you want to join CorSec?
			string_id message = new string_id (c_stringFile, "s_d72a0d47");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Maybe.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Maybe not.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2f9db7a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f42d051f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasGreatCorSecFaction (player, npc))
		{
			//-- NPC: Not at all.  We can't have thugs and ne'er-do-wells joining CorSec.  We're here to provide security for the citizens of Corellia!  Now, this will only take a moment... Oh... Wow!  You've got an excellent track record.  I guess you've tangled with your share of criminals and thugs in your day. Are you ready to begin?
			string_id message = new string_id (c_stringFile, "s_cec02f8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Maybe.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Maybe not.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_139");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Not at all.  We can't have thugs and ne'er-do-wells joining CorSec.  We're here to provide security for the citizens of Corellia!  Now, this will only take a moment... Ah... You've got a pretty clean record here.  I can assure you that it'd improve once you're flying for CorSec! Are you ready to begin?
			string_id message = new string_id (c_stringFile, "s_7396ebcf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Maybe.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Maybe not.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_139");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This will only take a moment... Ah... Well according to this you've run into a problem with the law.  Are you certain you want to join CorSec?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I am
	if (response == "s_e107077e")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: I'm sorry, but I'm afraid it's completely out of the question with your history.  You're going to have to clean up your act before you can join CorSec.
			string_id message = new string_id (c_stringFile, "s_2c6a1c50");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks
	if (response == "s_360d8728")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			//-- NPC: Perhaps you'll turn your life around, decide to be a part of the solution instead of a part of the problem.  When you're ready to help enforce the law instead of breaking it, come see me.
			string_id message = new string_id (c_stringFile, "s_2b418533");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This will only take a moment... Oh... Wow!  You've got an excellent track record.  I guess you've tangled with your share of criminals and thugs in your day. Are you ready to begin?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I am
	if (response == "s_30")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			corellia_privateer_trainer_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Indeed.  Do you have your own ship?
			string_id message = new string_id (c_stringFile, "s_40617d3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ah... no I don't.
			boolean hasResponse0 = false;
			if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, I do.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5091cb8e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90ec63e0");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks
	if (response == "s_51")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: What?  Are you sure?  You'd make an excellent candidate.
			string_id message = new string_id (c_stringFile, "s_36adcd36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Positive.  I don't want to join CorSec
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You're right.  Sign me up!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8acaf405");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e258dc9b");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This will only take a moment... Ah... You've got a pretty clean record here.  I can assure you that it will improve once you're flying for CorSec! Are you ready to begin?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I am
	if (response == "s_30")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			corellia_privateer_trainer_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Indeed.  Do you have your own ship?
			string_id message = new string_id (c_stringFile, "s_40617d3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ah... no I don't.
			boolean hasResponse0 = false;
			if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, I do.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5091cb8e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90ec63e0");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks
	if (response == "s_51")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: What?  Are you sure?  You'd make an excellent candidate.
			string_id message = new string_id (c_stringFile, "s_36adcd36");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Positive.  I don't want to join CorSec
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You're right.  Sign me up!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8acaf405");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e258dc9b");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed.  Do you have your own ship?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ah... no I don't.
	if (response == "s_5091cb8e")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_4e95069b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b9b27823");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I do.
	if (response == "s_90ec63e0")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well then. I think that about does it. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_ddfbd948");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I'm ready.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I need to take care of some things first.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_43");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you.
	if (response == "s_b9b27823")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well now, I think that does it. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_80a724aa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I'm ready.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I need to take care of some things first.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4a1d2431");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68660d24");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well now, I think that does it. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I'm ready.
	if (response == "s_4a1d2431")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_cc53ba8f");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to take care of some things first.
	if (response == "s_68660d24")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. Just come speak with me again when you're ready.
			string_id message = new string_id (c_stringFile, "s_c697d2d5");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well then. I think that about does it. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I'm ready.
	if (response == "s_43")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to take care of some things first.
	if (response == "s_47")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. Just come speak with me again when you're ready.
			string_id message = new string_id (c_stringFile, "s_49");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?  Are you sure?  You'd make an excellent candidate.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Positive.  I don't want to join CorSec
	if (response == "s_8acaf405")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Oh?  Well.  Ok.  Come see me if you change your mind.  We could really use you.
			string_id message = new string_id (c_stringFile, "s_f176d13");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You're right.  Sign me up!
	if (response == "s_e258dc9b")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			corellia_privateer_trainer_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Indeed.  Do you have your own ship?
			string_id message = new string_id (c_stringFile, "s_40617d3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ah... no I don't.
			boolean hasResponse0 = false;
			if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, I do.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5091cb8e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90ec63e0");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't worry, this will only take a moment.  We do need to ensure you're not some sort of criminal... Ah... Well according to this you haven't exactly been the most law abiding citizen on Corellia... Are you certain you want to join CorSec?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I guess so.
	if (response == "s_af22b9b2")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: Completely out of the question with your history.  You're going to have to clean up your act before you can join CorSec.
			string_id message = new string_id (c_stringFile, "s_1a1a36a3");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't think so
	if (response == "s_984b22b")
	{
		doAnimationAction (player, "wave_on_dismissing");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: All right.  But you'd better clean up your act if you ever change your mind.  We can't have CorSec members with your kind of history.
			string_id message = new string_id (c_stringFile, "s_8b451c3f");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't worry, this will only take a moment... Oh... Wow!  You've got an excellent track record.  I guess you've tangled with your share of criminals and thugs in your day. Are you ready to begin?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I guess so.
	if (response == "s_65")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			corellia_privateer_trainer_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Then I will add you to the roster.  Do you have your own ship?
			string_id message = new string_id (c_stringFile, "s_f3fdecfc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ah... no I don't.
			boolean hasResponse0 = false;
			if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, I do.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't think so
	if (response == "s_96")
	{
		doAnimationAction (player, "laugh_titter");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Very well.  Let me know if you change your mind.
			string_id message = new string_id (c_stringFile, "s_8358aeb7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't worry, this will only take a moment... Ah... You've got a pretty clean record here.  I can assure you that it will improve once you're flying for CorSec! Are you ready to begin?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I guess so.
	if (response == "s_65")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			corellia_privateer_trainer_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Then I will add you to the roster.  Do you have your own ship?
			string_id message = new string_id (c_stringFile, "s_f3fdecfc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ah... no I don't.
			boolean hasResponse0 = false;
			if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, I do.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't think so
	if (response == "s_96")
	{
		doAnimationAction (player, "laugh_titter");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Very well.  Let me know if you change your mind.
			string_id message = new string_id (c_stringFile, "s_8358aeb7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then I will add you to the roster.  Do you have your own ship?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ah... no I don't.
	if (response == "s_68")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_70");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_72");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I do.
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well then. I think that about does it. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_86");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I'm ready.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I need to take care of some things first.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you.
	if (response == "s_72")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well now, I think that does it. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_74");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I'm ready.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I need to take care of some things first.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well now, I think that does it. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I'm ready.
	if (response == "s_76")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_78");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to take care of some things first.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. Just come speak with me again when you're ready.
			string_id message = new string_id (c_stringFile, "s_82");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well then. I think that about does it. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I'm ready.
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_90");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to take care of some things first.
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. Just come speak with me again when you're ready.
			string_id message = new string_id (c_stringFile, "s_94");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not at all.  We can't have thugs and ne'er-do-wells joining CorSec.  We're here to provide security for the citizens of Corellia!  Now, this will only take a moment... Ah... Well according to this you've run into a problem with the law.  Are you certain you want to join CorSec?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe.
	if (response == "s_2f9db7a")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: It's completely out of the question with your history.  If you want to work for CorSec then you're going to have to clean up your act.
			string_id message = new string_id (c_stringFile, "s_342d1bdf");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe not.
	if (response == "s_f42d051f")
	{
		doAnimationAction (player, "wave_on_dismissing");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: It's completely out of the question with your history.  If you want to work for CorSec then you're going to have to clean up your act.
			string_id message = new string_id (c_stringFile, "s_104");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch46 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not at all.  We can't have thugs and ne'er-do-wells joining CorSec.  We're here to provide security for the citizens of Corellia!  Now, this will only take a moment... Oh... Wow!  You've got an excellent track record.  I guess you've tangled with your share of criminals and thugs in your day. Are you ready to begin?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe.
	if (response == "s_108")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Very well.  I'll go ahead and sign you up.  Do you have your own ship?
			string_id message = new string_id (c_stringFile, "s_e0dfe067");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ah... no I don't.
			boolean hasResponse0 = false;
			if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, I do.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_111");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_127");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe not.
	if (response == "s_139")
	{
		doAnimationAction (player, "slump_head");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Ok.  Bye.  Come see me if you change your mind.
			string_id message = new string_id (c_stringFile, "s_746a1bed");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not at all.  We can't have thugs and ne'er-do-wells joining CorSec.  We're here to provide security for the citizens of Corellia!  Now, this will only take a moment... Ah... You've got a pretty clean record here.  I can assure you that it'd improve once you're flying for CorSec! Are you ready to begin?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe.
	if (response == "s_108")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Very well.  I'll go ahead and sign you up.  Do you have your own ship?
			string_id message = new string_id (c_stringFile, "s_e0dfe067");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ah... no I don't.
			boolean hasResponse0 = false;
			if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, I do.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_111");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_127");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe not.
	if (response == "s_139")
	{
		doAnimationAction (player, "slump_head");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Ok.  Bye.  Come see me if you change your mind.
			string_id message = new string_id (c_stringFile, "s_746a1bed");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch48 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well.  I'll go ahead and sign you up.  Do you have your own ship?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ah... no I don't.
	if (response == "s_111")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_113");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_115");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 49);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I do.
	if (response == "s_127")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well then. I think that about does it. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_129");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I'm ready.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I need to take care of some things first.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_131");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 53);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch49 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you.
	if (response == "s_115")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well now, I think that does it. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_117");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I'm ready.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I need to take care of some things first.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 50);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch50 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well now, I think that does it. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I'm ready.
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_121");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to take care of some things first.
	if (response == "s_123")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. Just come speak with me again when you're ready.
			string_id message = new string_id (c_stringFile, "s_125");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch53 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well then. I think that about does it. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I'm ready.
	if (response == "s_131")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_133");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to take care of some things first.
	if (response == "s_135")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Certainly. Just come speak with me again when you're ready.
			string_id message = new string_id (c_stringFile, "s_137");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: CorSec is Corellia's local Security force.  As you know, Corellia is officially neutral in the Galactic Civil War... or at least as neutral as a world can be in this day and age.  We provide defense and law enforcement for the entire Corellian system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Oh.  No thanks
	if (response == "s_deaabd84")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "smack_self");

			//-- NPC: Very well, then please move along.  I am very busy.
			string_id message = new string_id (c_stringFile, "s_580337ba");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see...
	if (response == "s_614b71f0")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: We don't let just anyone join.  There'll be a background check of course.
			string_id message = new string_id (c_stringFile, "s_63a61222");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What do you mean?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: That's ridiculous!
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1418de80");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77e48d5b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_64b308bc");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch63 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you going to train me after this?
	if (response == "s_133a724d")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: Oh no, not quite so fast, %NU.  You complete a few more assignments and then we'll see about getting you some training!
			string_id message = new string_id (c_stringFile, "s_8e5b619a");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you going to train me after this?
	if (response == "s_168")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: Not just yet, %NU.  I have just a few more things for you to do before you get any training from me.
			string_id message = new string_id (c_stringFile, "s_c91ba06e");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch65 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you going to train me after this?
	if (response == "s_176")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Almost, %NU.  I have just a couple more things for you to take care of before you can be trained further.  Starting with that patrol...
			string_id message = new string_id (c_stringFile, "s_e926d5f0");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch66 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you going to train me after this?
	if (response == "s_183")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: Tell you what, %NU.  Kill that pirate leader and YES, I will train you.
			string_id message = new string_id (c_stringFile, "s_28435d2");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch67 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent work!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was attacked!
	if (response == "s_1c76874e")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_9e5f75be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Sorry, but you don't look like a pilot to me.
			string_id message = new string_id (c_stringFile, "s_698d80f7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
			string_id message = new string_id (c_stringFile, "s_c9f74b42");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
			string_id message = new string_id (c_stringFile, "s_15502961");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
			string_id message = new string_id (c_stringFile, "s_ff684aed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			corellia_privateer_trainer_action_eraseClientPath (player, npc);

			//-- NPC: Hello!  Come to join the ranks of CorSec?
			string_id message = new string_id (c_stringFile, "s_f518ba4f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What is CorSec?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
		{
			doAnimationAction (npc, "explain");

			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
			string_id message = new string_id (c_stringFile, "s_333f5da9");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
			string_id message = new string_id (c_stringFile, "s_cd44e585");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
			string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
			string_id message = new string_id (c_stringFile, "s_8b8a1542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
			string_id message = new string_id (c_stringFile, "s_844a124b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
			string_id message = new string_id (c_stringFile, "s_429077be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work!
			string_id message = new string_id (c_stringFile, "s_f053b088");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was attacked!
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nothing to it.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "salute1");

			corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

			//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
			string_id message = new string_id (c_stringFile, "s_926359dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It was a snap.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			doAnimationAction (player, "nod_head_multiple");

			//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
			string_id message = new string_id (c_stringFile, "s_4801be96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (npc, "bow");

			corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

			//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f8026f5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There are too many pirates in this system!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
			string_id message = new string_id (c_stringFile, "s_964fb39a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

			//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
			string_id message = new string_id (c_stringFile, "s_dc993cdb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do now?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I love being a starfighter pilot.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
			string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
			string_id message = new string_id (c_stringFile, "s_5e6c7400");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
			string_id message = new string_id (c_stringFile, "s_46ea46ad");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I agree.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
			string_id message = new string_id (c_stringFile, "s_a48c20c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing to it.
	if (response == "s_3557fc9f")
	{
		doAnimationAction (player, "wave_on_dismissing");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_9e5f75be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Sorry, but you don't look like a pilot to me.
			string_id message = new string_id (c_stringFile, "s_698d80f7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
			string_id message = new string_id (c_stringFile, "s_c9f74b42");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
			string_id message = new string_id (c_stringFile, "s_15502961");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
			string_id message = new string_id (c_stringFile, "s_ff684aed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			corellia_privateer_trainer_action_eraseClientPath (player, npc);

			//-- NPC: Hello!  Come to join the ranks of CorSec?
			string_id message = new string_id (c_stringFile, "s_f518ba4f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What is CorSec?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
		{
			doAnimationAction (npc, "explain");

			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
			string_id message = new string_id (c_stringFile, "s_333f5da9");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
			string_id message = new string_id (c_stringFile, "s_cd44e585");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
			string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
			string_id message = new string_id (c_stringFile, "s_8b8a1542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
			string_id message = new string_id (c_stringFile, "s_844a124b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
			string_id message = new string_id (c_stringFile, "s_429077be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work!
			string_id message = new string_id (c_stringFile, "s_f053b088");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was attacked!
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nothing to it.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "salute1");

			corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

			//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
			string_id message = new string_id (c_stringFile, "s_926359dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It was a snap.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			doAnimationAction (player, "nod_head_multiple");

			//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
			string_id message = new string_id (c_stringFile, "s_4801be96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (npc, "bow");

			corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

			//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f8026f5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There are too many pirates in this system!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
			string_id message = new string_id (c_stringFile, "s_964fb39a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

			//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
			string_id message = new string_id (c_stringFile, "s_dc993cdb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do now?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I love being a starfighter pilot.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
			string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
			string_id message = new string_id (c_stringFile, "s_5e6c7400");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
			string_id message = new string_id (c_stringFile, "s_46ea46ad");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I agree.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
			string_id message = new string_id (c_stringFile, "s_a48c20c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch68 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you going to train me after this?
	if (response == "s_133a724d")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: Oh no, not quite so fast, %NU.  You complete a few more assignments and then we'll see about getting you some training!
			string_id message = new string_id (c_stringFile, "s_8e5b619a");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch70 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's next?
	if (response == "s_a927c891")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_9e5f75be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Sorry, but you don't look like a pilot to me.
			string_id message = new string_id (c_stringFile, "s_698d80f7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
			string_id message = new string_id (c_stringFile, "s_c9f74b42");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
			string_id message = new string_id (c_stringFile, "s_15502961");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
			string_id message = new string_id (c_stringFile, "s_ff684aed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			corellia_privateer_trainer_action_eraseClientPath (player, npc);

			//-- NPC: Hello!  Come to join the ranks of CorSec?
			string_id message = new string_id (c_stringFile, "s_f518ba4f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What is CorSec?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
		{
			doAnimationAction (npc, "explain");

			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
			string_id message = new string_id (c_stringFile, "s_333f5da9");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
			string_id message = new string_id (c_stringFile, "s_cd44e585");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
			string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
			string_id message = new string_id (c_stringFile, "s_8b8a1542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
			string_id message = new string_id (c_stringFile, "s_844a124b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
			string_id message = new string_id (c_stringFile, "s_429077be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work!
			string_id message = new string_id (c_stringFile, "s_f053b088");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was attacked!
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nothing to it.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "salute1");

			corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

			//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
			string_id message = new string_id (c_stringFile, "s_926359dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It was a snap.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			doAnimationAction (player, "nod_head_multiple");

			//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
			string_id message = new string_id (c_stringFile, "s_4801be96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (npc, "bow");

			corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

			//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f8026f5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There are too many pirates in this system!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
			string_id message = new string_id (c_stringFile, "s_964fb39a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

			//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
			string_id message = new string_id (c_stringFile, "s_dc993cdb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do now?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I love being a starfighter pilot.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
			string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
			string_id message = new string_id (c_stringFile, "s_5e6c7400");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
			string_id message = new string_id (c_stringFile, "s_46ea46ad");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I agree.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
			string_id message = new string_id (c_stringFile, "s_a48c20c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It was a snap.
	if (response == "s_257f178d")
	{
		doAnimationAction (player, "snap_finger1");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_9e5f75be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Sorry, but you don't look like a pilot to me.
			string_id message = new string_id (c_stringFile, "s_698d80f7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
			string_id message = new string_id (c_stringFile, "s_c9f74b42");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
			string_id message = new string_id (c_stringFile, "s_15502961");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
			string_id message = new string_id (c_stringFile, "s_ff684aed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			corellia_privateer_trainer_action_eraseClientPath (player, npc);

			//-- NPC: Hello!  Come to join the ranks of CorSec?
			string_id message = new string_id (c_stringFile, "s_f518ba4f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What is CorSec?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
		{
			doAnimationAction (npc, "explain");

			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
			string_id message = new string_id (c_stringFile, "s_333f5da9");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
			string_id message = new string_id (c_stringFile, "s_cd44e585");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
			string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
			string_id message = new string_id (c_stringFile, "s_8b8a1542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
			string_id message = new string_id (c_stringFile, "s_844a124b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
			string_id message = new string_id (c_stringFile, "s_429077be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work!
			string_id message = new string_id (c_stringFile, "s_f053b088");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was attacked!
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nothing to it.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "salute1");

			corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

			//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
			string_id message = new string_id (c_stringFile, "s_926359dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It was a snap.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			doAnimationAction (player, "nod_head_multiple");

			//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
			string_id message = new string_id (c_stringFile, "s_4801be96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (npc, "bow");

			corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

			//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f8026f5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There are too many pirates in this system!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
			string_id message = new string_id (c_stringFile, "s_964fb39a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

			//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
			string_id message = new string_id (c_stringFile, "s_dc993cdb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do now?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I love being a starfighter pilot.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
			string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
			string_id message = new string_id (c_stringFile, "s_5e6c7400");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
			string_id message = new string_id (c_stringFile, "s_46ea46ad");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I agree.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
			string_id message = new string_id (c_stringFile, "s_a48c20c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch71 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you going to train me after this?
	if (response == "s_168")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: Not just yet, %NU.  I have just a few more things for you to do before you get any training from me.
			string_id message = new string_id (c_stringFile, "s_c91ba06e");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch73 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's next?
	if (response == "s_172")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_9e5f75be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Sorry, but you don't look like a pilot to me.
			string_id message = new string_id (c_stringFile, "s_698d80f7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
			string_id message = new string_id (c_stringFile, "s_c9f74b42");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
			string_id message = new string_id (c_stringFile, "s_15502961");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
			string_id message = new string_id (c_stringFile, "s_ff684aed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			corellia_privateer_trainer_action_eraseClientPath (player, npc);

			//-- NPC: Hello!  Come to join the ranks of CorSec?
			string_id message = new string_id (c_stringFile, "s_f518ba4f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What is CorSec?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
		{
			doAnimationAction (npc, "explain");

			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
			string_id message = new string_id (c_stringFile, "s_333f5da9");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
			string_id message = new string_id (c_stringFile, "s_cd44e585");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
			string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
			string_id message = new string_id (c_stringFile, "s_8b8a1542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
			string_id message = new string_id (c_stringFile, "s_844a124b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
			string_id message = new string_id (c_stringFile, "s_429077be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work!
			string_id message = new string_id (c_stringFile, "s_f053b088");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was attacked!
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nothing to it.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "salute1");

			corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

			//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
			string_id message = new string_id (c_stringFile, "s_926359dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It was a snap.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			doAnimationAction (player, "nod_head_multiple");

			//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
			string_id message = new string_id (c_stringFile, "s_4801be96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (npc, "bow");

			corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

			//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f8026f5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There are too many pirates in this system!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
			string_id message = new string_id (c_stringFile, "s_964fb39a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

			//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
			string_id message = new string_id (c_stringFile, "s_dc993cdb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do now?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I love being a starfighter pilot.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
			string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
			string_id message = new string_id (c_stringFile, "s_5e6c7400");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
			string_id message = new string_id (c_stringFile, "s_46ea46ad");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I agree.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
			string_id message = new string_id (c_stringFile, "s_a48c20c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: There are too many pirates in this system!
	if (response == "s_ebb99364")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_9e5f75be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Sorry, but you don't look like a pilot to me.
			string_id message = new string_id (c_stringFile, "s_698d80f7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
			string_id message = new string_id (c_stringFile, "s_c9f74b42");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
			string_id message = new string_id (c_stringFile, "s_15502961");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
			string_id message = new string_id (c_stringFile, "s_ff684aed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			corellia_privateer_trainer_action_eraseClientPath (player, npc);

			//-- NPC: Hello!  Come to join the ranks of CorSec?
			string_id message = new string_id (c_stringFile, "s_f518ba4f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What is CorSec?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
		{
			doAnimationAction (npc, "explain");

			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
			string_id message = new string_id (c_stringFile, "s_333f5da9");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
			string_id message = new string_id (c_stringFile, "s_cd44e585");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
			string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
			string_id message = new string_id (c_stringFile, "s_8b8a1542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
			string_id message = new string_id (c_stringFile, "s_844a124b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
			string_id message = new string_id (c_stringFile, "s_429077be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work!
			string_id message = new string_id (c_stringFile, "s_f053b088");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was attacked!
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nothing to it.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "salute1");

			corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

			//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
			string_id message = new string_id (c_stringFile, "s_926359dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It was a snap.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			doAnimationAction (player, "nod_head_multiple");

			//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
			string_id message = new string_id (c_stringFile, "s_4801be96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (npc, "bow");

			corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

			//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f8026f5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There are too many pirates in this system!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
			string_id message = new string_id (c_stringFile, "s_964fb39a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

			//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
			string_id message = new string_id (c_stringFile, "s_dc993cdb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do now?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I love being a starfighter pilot.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
			string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
			string_id message = new string_id (c_stringFile, "s_5e6c7400");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
			string_id message = new string_id (c_stringFile, "s_46ea46ad");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I agree.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
			string_id message = new string_id (c_stringFile, "s_a48c20c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch74 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you going to train me after this?
	if (response == "s_176")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Almost, %NU.  I have just a couple more things for you to take care of before you can be trained further.  Starting with that patrol...
			string_id message = new string_id (c_stringFile, "s_e926d5f0");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch76 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do I do now?
	if (response == "s_a78a4ac3")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_9e5f75be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Sorry, but you don't look like a pilot to me.
			string_id message = new string_id (c_stringFile, "s_698d80f7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
			string_id message = new string_id (c_stringFile, "s_c9f74b42");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
			string_id message = new string_id (c_stringFile, "s_15502961");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
			string_id message = new string_id (c_stringFile, "s_ff684aed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			corellia_privateer_trainer_action_eraseClientPath (player, npc);

			//-- NPC: Hello!  Come to join the ranks of CorSec?
			string_id message = new string_id (c_stringFile, "s_f518ba4f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What is CorSec?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
		{
			doAnimationAction (npc, "explain");

			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
			string_id message = new string_id (c_stringFile, "s_333f5da9");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
			string_id message = new string_id (c_stringFile, "s_cd44e585");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
			string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
			string_id message = new string_id (c_stringFile, "s_8b8a1542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
			string_id message = new string_id (c_stringFile, "s_844a124b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
			string_id message = new string_id (c_stringFile, "s_429077be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work!
			string_id message = new string_id (c_stringFile, "s_f053b088");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was attacked!
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nothing to it.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "salute1");

			corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

			//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
			string_id message = new string_id (c_stringFile, "s_926359dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It was a snap.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			doAnimationAction (player, "nod_head_multiple");

			//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
			string_id message = new string_id (c_stringFile, "s_4801be96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (npc, "bow");

			corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

			//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f8026f5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There are too many pirates in this system!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
			string_id message = new string_id (c_stringFile, "s_964fb39a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

			//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
			string_id message = new string_id (c_stringFile, "s_dc993cdb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do now?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I love being a starfighter pilot.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
			string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
			string_id message = new string_id (c_stringFile, "s_5e6c7400");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
			string_id message = new string_id (c_stringFile, "s_46ea46ad");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I agree.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
			string_id message = new string_id (c_stringFile, "s_a48c20c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I love being a starfighter pilot.
	if (response == "s_af264a19")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_9e5f75be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An assignment?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What ship?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where do I go?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: And when I'm done?
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Sorry, but you don't look like a pilot to me.
			string_id message = new string_id (c_stringFile, "s_698d80f7");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
			string_id message = new string_id (c_stringFile, "s_c9f74b42");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "standing_placate");

			//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
			string_id message = new string_id (c_stringFile, "s_15502961");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
			string_id message = new string_id (c_stringFile, "s_ff684aed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			corellia_privateer_trainer_action_eraseClientPath (player, npc);

			//-- NPC: Hello!  Come to join the ranks of CorSec?
			string_id message = new string_id (c_stringFile, "s_f518ba4f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What is CorSec?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
		{
			doAnimationAction (npc, "explain");

			corellia_privateer_trainer_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
			string_id message = new string_id (c_stringFile, "s_333f5da9");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
		{
			doAnimationAction (npc, "beckon");

			//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
			string_id message = new string_id (c_stringFile, "s_cd44e585");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			corellia_privateer_trainer_action_grantMissionOne (player, npc);

			//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
			string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantMissionTwo (player, npc);

			//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
			string_id message = new string_id (c_stringFile, "s_8b8a1542");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_privateer_trainer_action_grantMissionThree (player, npc);

			//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
			string_id message = new string_id (c_stringFile, "s_844a124b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
			string_id message = new string_id (c_stringFile, "s_429077be");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work!
			string_id message = new string_id (c_stringFile, "s_f053b088");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was attacked!
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Nothing to it.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "salute1");

			corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

			//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
			string_id message = new string_id (c_stringFile, "s_926359dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It was a snap.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			doAnimationAction (player, "nod_head_multiple");

			//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
			string_id message = new string_id (c_stringFile, "s_4801be96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (npc, "bow");

			corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

			//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_f8026f5d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's next?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: There are too many pirates in this system!
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
			string_id message = new string_id (c_stringFile, "s_964fb39a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_176");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

			//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
			string_id message = new string_id (c_stringFile, "s_dc993cdb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do I do now?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I love being a starfighter pilot.
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
			string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you going to train me after this?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_183");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
			string_id message = new string_id (c_stringFile, "s_5e6c7400");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
			string_id message = new string_id (c_stringFile, "s_46ea46ad");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I agree.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_229");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
			string_id message = new string_id (c_stringFile, "s_a48c20c8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch77 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you going to train me after this?
	if (response == "s_183")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_grantMissionFour (player, npc);

			//-- NPC: Tell you what, %NU.  Kill that pirate leader and YES, I will train you.
			string_id message = new string_id (c_stringFile, "s_28435d2");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch79 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sounds great.
	if (response == "s_c06c7aa9")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_grantStarshipSkill1 (player, npc);

			//-- NPC: Excellent.  Review this information regarding your new skills, and report back to me when you're ready for another assignment.
			string_id message = new string_id (c_stringFile, "s_db7653ed");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch81 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I agree.
	if (response == "s_229")
	{
		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasStarshipSkill1 (player, npc))
		{
			//-- NPC: Today we'll be learning about basic starfighters. How does that sound?
			string_id message = new string_id (c_stringFile, "s_230");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_235");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 82);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasWeaponsSkill1 (player, npc))
		{
			//-- NPC: Today we'll be learning about basic starship component use. How does that sound?
			string_id message = new string_id (c_stringFile, "s_231");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_privateer_trainer_condition_hasProcedureSkill1 (player, npc))
		{
			//-- NPC: Today we'll be learning about basic starfighter pilot training. How does that sound?
			string_id message = new string_id (c_stringFile, "s_232");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_243");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 86);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Today we'll be learning about droid interface basics. How does that sound?
			string_id message = new string_id (c_stringFile, "s_233");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That sounds great.
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_247");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 88);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch82 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Today we'll be learning about basic starfighters. How does that sound?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sounds great.
	if (response == "s_235")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_buyStarshipSkill1 (player, npc);

			//-- NPC: Excellent.  Review this information regarding your new skills, and report back to me when you're ready for another assignment.
			string_id message = new string_id (c_stringFile, "s_237");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch84 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Today we'll be learning about basic starship component use. How does that sound?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sounds great.
	if (response == "s_239")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_buyWeaponsSkill1 (player, npc);

			//-- NPC: Excellent.  Review this information regarding your new skills, and report back to me when you're ready for another assignment.
			string_id message = new string_id (c_stringFile, "s_241");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch86 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Today we'll be learning about basic starfighter pilot training. How does that sound?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sounds great.
	if (response == "s_243")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_buyProcedureSkill1 (player, npc);

			//-- NPC: Excellent.  Review this information regarding your new skills, and report back to me when you're ready for another assignment.
			string_id message = new string_id (c_stringFile, "s_245");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch88 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Today we'll be learning about droid interface basics. How does that sound?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That sounds great.
	if (response == "s_247")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			corellia_privateer_trainer_action_buyDroidSkill1 (player, npc);

			//-- NPC: Excellent.  Review this information regarding your new skills, and report back to me when you're ready for another assignment.
			string_id message = new string_id (c_stringFile, "s_249");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch90 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Duty missions are a good way for you to get experience as a pilot. Only experienced pilots can receive the proper training to learn new skills and abilities.There's no real final objective so you can end the mission whenever you want. So, would you like to take a duty mission?
			string_id message = new string_id (c_stringFile, "s_d43b2932");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I am. What duty missions are available?
	if (response == "s_1a7f1a1b")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, merchant freighters are always in need of escorts, or if you like you can help in the fight against the pirate leaders. What interests you?
			string_id message = new string_id (c_stringFile, "s_d6a6a9fc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Pirate Leaders
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Escorting Merchants
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Nothing right now.
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9e417b37");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_221daf73");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c35e70d2");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 92);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, but how about some training?
	if (response == "s_6276c98")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're not ready for more training yet. But duty missions are a good way of getting good experience so that you'll be ready for training soon. What do you say? Are you ready to try a duty mission?
			string_id message = new string_id (c_stringFile, "s_e5c453e0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now, thanks.
	if (response == "s_2883b989")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Maybe next time then.
			string_id message = new string_id (c_stringFile, "s_228");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch91 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Duty missions are a good way for you to get experience as a pilot. Only experienced pilots can receive the proper training to learn new skills and abilities.There's no real final objective so you can end the mission whenever you want. So, would you like to take a duty mission?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Duty missions are a good way for you to get experience as a pilot. Only experienced pilots can receive the proper training to learn new skills and abilities.There's no real final objective so you can end the mission whenever you want. So, would you like to take a duty mission?
			string_id message = new string_id (c_stringFile, "s_d43b2932");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I am. What duty missions are available?
	if (response == "s_1a7f1a1b")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, merchant freighters are always in need of escorts, or if you like you can help in the fight against the pirate leaders. What interests you?
			string_id message = new string_id (c_stringFile, "s_d6a6a9fc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Pirate Leaders
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Escorting Merchants
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Nothing right now.
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9e417b37");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_221daf73");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c35e70d2");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 92);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, but how about some training?
	if (response == "s_6276c98")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're not ready for more training yet. But duty missions are a good way of getting good experience so that you'll be ready for training soon. What do you say? Are you ready to try a duty mission?
			string_id message = new string_id (c_stringFile, "s_e5c453e0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now, thanks.
	if (response == "s_2883b989")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Maybe next time then.
			string_id message = new string_id (c_stringFile, "s_228");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch92 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, merchant freighters are always in need of escorts, or if you like you can help in the fight against the pirate leaders. What interests you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Pirate Leaders
	if (response == "s_9e417b37")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			corellia_privateer_trainer_action_grantDestroyDutyMission (player, npc);

			//-- NPC: You're right.  No sooner do we eliminate one leader than another shows up.  Take out the Corellian Space Pirates!
			string_id message = new string_id (c_stringFile, "s_cebaf0db");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Escorting Merchants
	if (response == "s_221daf73")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			corellia_privateer_trainer_action_grantEscortDutyMission (player, npc);

			//-- NPC: Right, those merchants rely on us to provide them with safe passage through the Corellia system.  Just keep escorting them through.
			string_id message = new string_id (c_stringFile, "s_3f62bf0");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing right now.
	if (response == "s_c35e70d2")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Maybe next time then.
			string_id message = new string_id (c_stringFile, "s_27d40413");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_privateer_trainer_handleBranch96 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're not ready for more training yet. But duty missions are a good way of getting good experience so that you'll be ready for training soon. What do you say? Are you ready to try a duty mission?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Duty missions are a good way for you to get experience as a pilot. Only experienced pilots can receive the proper training to learn new skills and abilities.There's no real final objective so you can end the mission whenever you want. So, would you like to take a duty mission?
			string_id message = new string_id (c_stringFile, "s_d43b2932");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes I am. What duty missions are available?
	if (response == "s_1a7f1a1b")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, merchant freighters are always in need of escorts, or if you like you can help in the fight against the pirate leaders. What interests you?
			string_id message = new string_id (c_stringFile, "s_d6a6a9fc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Pirate Leaders
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Escorting Merchants
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Nothing right now.
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9e417b37");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_221daf73");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c35e70d2");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 92);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, but how about some training?
	if (response == "s_6276c98")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're not ready for more training yet. But duty missions are a good way of getting good experience so that you'll be ready for training soon. What do you say? Are you ready to try a duty mission?
			string_id message = new string_id (c_stringFile, "s_e5c453e0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes I am. What duty missions are available?
			boolean hasResponse1 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: No, but how about some training?
			boolean hasResponse2 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now, thanks.
	if (response == "s_2883b989")
	{
		//-- [NOTE] 
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			//-- NPC: Maybe next time then.
			string_id message = new string_id (c_stringFile, "s_228");
			utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.corellia_privateer_trainer");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );


	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );
	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.corellia_privateer_trainer");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_hasVeryFirstQuest (player, npc))
	{
		doAnimationAction (npc, "point_away");

		//-- NPC: You've got your assignment and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
		string_id message = new string_id (c_stringFile, "s_9e5f75be");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: An assignment?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What ship?
		boolean hasResponse1 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Where do I go?
		boolean hasResponse2 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: And when I'm done?
		boolean hasResponse3 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_95d0a1f6");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_bdd269be");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5e4ebfca");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 1);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_hasSpaceExpansion (player, npc))
	{
		doAnimationAction (npc, "shake_head_no");

		//-- NPC: Sorry, but you don't look like a pilot to me.
		string_id message = new string_id (c_stringFile, "s_698d80f7");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_isRebelPilot (player, npc))
	{
		doAnimationAction (npc, "wave_on_dismissing");

		//-- NPC: Greetings, citizen.  Keep your nose clean and we won't have any trouble.
		string_id message = new string_id (c_stringFile, "s_c9f74b42");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_isImperialPilot (player, npc))
	{
		doAnimationAction (npc, "standing_placate");

		//-- NPC: Nothing to worry about here, sir.  CorSec has the area well secured.
		string_id message = new string_id (c_stringFile, "s_15502961");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_isPilotButNotCorSec (player, npc))
	{
		doAnimationAction (npc, "point_accusingly");

		//-- NPC: Ah, I've heard of you!  You're not a bad pilot from what I understand. What can the CorSec do for you?
		string_id message = new string_id (c_stringFile, "s_ff684aed");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm looking for a mission. Do you have any?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 9);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_hasNoviceSkillBox (player, npc))
	{
		doAnimationAction (npc, "greet");

		corellia_privateer_trainer_action_eraseClientPath (player, npc);

		//-- NPC: Hello!  Come to join the ranks of CorSec?
		string_id message = new string_id (c_stringFile, "s_f518ba4f");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No
		boolean hasResponse1 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: What is CorSec?
		boolean hasResponse2 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_370822d1");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_457a7010");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4906b993");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 12);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_hasSpaceShip (player, npc))
	{
		doAnimationAction (npc, "explain");

		corellia_privateer_trainer_action_grantSpaceShip (player, npc);

		//-- NPC: You're going to need a ship, if you're going to fly for CorSec.  I'll add the Ship Control Codes to your datapad for you.  It's not a great ship, but it will get you around.
		string_id message = new string_id (c_stringFile, "s_149");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_hasCorSecQuest (player, npc))
	{
		doAnimationAction (npc, "dismiss");

		//-- NPC: What are you doing talking to me?  Isn't there somewhere you're supposed to be?
		string_id message = new string_id (c_stringFile, "s_333f5da9");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_hasAllTierOneSkills (player, npc))
	{
		doAnimationAction (npc, "beckon");

		//-- NPC: Listen, something important has come up.  The captain wants to speak to you right away!  Go speak to Captain Rikkh in the next room.
		string_id message = new string_id (c_stringFile, "s_cd44e585");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_missionOneFailed (player, npc))
	{
		doAnimationAction (npc, "dismiss");

		corellia_privateer_trainer_action_grantMissionOne (player, npc);

		//-- NPC: What happened to you?  Never mind, never mind.  Get back out there and finish that patrol.
		string_id message = new string_id (c_stringFile, "s_5cdbbd1f");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you going to train me after this?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_missionTwoFailed (player, npc))
	{
		doAnimationAction (npc, "pound_fist_palm");

		corellia_privateer_trainer_action_grantMissionTwo (player, npc);

		//-- NPC: That's bad news, but this is just the reason why CorSec is needed!  Go back out there and take out those pirates.  Defending this system from pirates and killers is all up to us.
		string_id message = new string_id (c_stringFile, "s_8b8a1542");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you going to train me after this?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_168");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_missionThreeFailed (player, npc))
	{
		doAnimationAction (npc, "point_accusingly");

		corellia_privateer_trainer_action_grantMissionThree (player, npc);

		//-- NPC: Piracy in this system has increased 17 percent since you signed-up!  This is inexcusable!  Now get out there and complete that patrol!
		string_id message = new string_id (c_stringFile, "s_844a124b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you going to train me after this?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_176");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_missionFourFailed (player, npc))
	{
		doAnimationAction (npc, "shake_head_disgust");

		corellia_privateer_trainer_action_grantMissionFour (player, npc);

		//-- NPC: You're never going to make a dent in the frequency of piracy in the Corellia system until you take out their leader!  Now get back out there and destroy the leader's ship!
		string_id message = new string_id (c_stringFile, "s_429077be");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you going to train me after this?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_183");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_beenRewardedForQuestOne (player, npc))
	{
		doAnimationAction (npc, "applause_polite");

		corellia_privateer_trainer_action_rewardForQuestOne (player, npc);

		//-- NPC: Excellent work!
		string_id message = new string_id (c_stringFile, "s_f053b088");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I was attacked!
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Nothing to it.
		boolean hasResponse1 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1c76874e");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3557fc9f");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 67);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_hasCompletedQuestOne (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Alright, your first assignment is to run a simple patrol.  From time to time pirates move into the area and harass merchants in our system. Just run this simple patrol and make sure there's no criminal activity going on.
		string_id message = new string_id (c_stringFile, "s_160");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you going to train me after this?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_133a724d");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 68);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_beenRewardedForQuestTwo (player, npc))
	{
		doAnimationAction (npc, "salute1");

		corellia_privateer_trainer_action_rewardForQuestTwo (player, npc);

		//-- NPC: Excellent job taking out those pirates.  No doubt, this won't be the end of them.
		string_id message = new string_id (c_stringFile, "s_926359dd");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What's next?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: It was a snap.
		boolean hasResponse1 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a927c891");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_257f178d");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 70);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_hasCompletedQuestTwo (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		doAnimationAction (player, "nod_head_multiple");

		//-- NPC: So, you were ambushed as you ran that patrol.  We need to hit them hard to send a message... make an example out of them.  I'm going to send you in to run that scum out of the system. I am uploading some possible waypoints into your nav computer, destroy any Black Sun you find.
		string_id message = new string_id (c_stringFile, "s_4801be96");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you going to train me after this?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_168");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 71);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_beenRewardedForQuestThree (player, npc))
	{
		doAnimationAction (npc, "bow");

		corellia_privateer_trainer_action_rewardForQuestThree (player, npc);

		//-- NPC: Excellent work with that escort.  It's a good thing you were running that patrol, or there's no telling what might have happened to them.  I've got a little something extra for you, too.  It's a mercenary bandolier.  It'll help you keep your things organized.
		string_id message = new string_id (c_stringFile, "s_f8026f5d");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What's next?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: There are too many pirates in this system!
		boolean hasResponse1 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_172");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ebb99364");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 73);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_hasCompletedQuestThree (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Just to be sure that's the last of them, we need you to run another patrol.  It's a little longer than the last one, but you already know how crafty those criminals can be.
		string_id message = new string_id (c_stringFile, "s_964fb39a");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you going to train me after this?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_176");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 74);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_beenRewardedForQuestFour (player, npc))
	{
		doAnimationAction (npc, "point_forward");

		corellia_privateer_trainer_action_rewardForQuestFour (player, npc);

		//-- NPC: Ha!  That'll show those thugs that CorSec is not to be trifled with.  You're really going to go places in this organization, I can tell already.
		string_id message = new string_id (c_stringFile, "s_dc993cdb");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What do I do now?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I love being a starfighter pilot.
		boolean hasResponse1 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a78a4ac3");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_af264a19");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 76);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_privateer_trainer_condition_hasCompletedQuestFour (player, npc))
	{
		doAnimationAction (npc, "pound_fist_palm");

		//-- NPC: Enough of this!  Every time we send you out, you find more pirates.  We need to go straight to the source of the problem:  Their leader.  I know you're up to this:  Go find the pirate's leader and take him out.  Let the rest of them go to the outer rim if they want easy prey.
		string_id message = new string_id (c_stringFile, "s_8e1ed7f0");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are you going to train me after this?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_183");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 77);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_isReadyForFirstTraining (player, npc))
	{
		doAnimationAction (npc, "nod_head_multiple");

		//-- NPC: You are definitely ready to move up.  So I'll tell you what. I'll teach you about basic fighters.
		string_id message = new string_id (c_stringFile, "s_5e6c7400");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: That sounds great.
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c06c7aa9");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 79);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition_isReadyForMoreTraining (player, npc))
	{
		doAnimationAction (npc, "salute1");

		//-- NPC: We're quite satisfied with your level of performance.  I think it's time you learned a bit more, though.
		string_id message = new string_id (c_stringFile, "s_46ea46ad");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I agree.
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_229");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 81);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "salute1");

		doAnimationAction (player, "salute1");

		//-- NPC: What's on your mind, officer? Thinking about performing some duty missions?
		string_id message = new string_id (c_stringFile, "s_a48c20c8");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What is a duty mission?
		boolean hasResponse0 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes I am. What duty missions are available?
		boolean hasResponse1 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: No, but how about some training?
		boolean hasResponse2 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Not right now, thanks.
		boolean hasResponse3 = false;
		if (corellia_privateer_trainer_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1a7f1a1b");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6276c98");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

			utils.setScriptVar (player, "conversation.corellia_privateer_trainer.branchId", 90);

			npcStartConversation (player, npc, "corellia_privateer_trainer", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "corellia_privateer_trainer")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

	if (branchId == 1 && corellia_privateer_trainer_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && corellia_privateer_trainer_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && corellia_privateer_trainer_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && corellia_privateer_trainer_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && corellia_privateer_trainer_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && corellia_privateer_trainer_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && corellia_privateer_trainer_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && corellia_privateer_trainer_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && corellia_privateer_trainer_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && corellia_privateer_trainer_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && corellia_privateer_trainer_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && corellia_privateer_trainer_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && corellia_privateer_trainer_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && corellia_privateer_trainer_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && corellia_privateer_trainer_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && corellia_privateer_trainer_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && corellia_privateer_trainer_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && corellia_privateer_trainer_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && corellia_privateer_trainer_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && corellia_privateer_trainer_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && corellia_privateer_trainer_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && corellia_privateer_trainer_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && corellia_privateer_trainer_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && corellia_privateer_trainer_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && corellia_privateer_trainer_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 46 && corellia_privateer_trainer_handleBranch46 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && corellia_privateer_trainer_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 48 && corellia_privateer_trainer_handleBranch48 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 49 && corellia_privateer_trainer_handleBranch49 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 50 && corellia_privateer_trainer_handleBranch50 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 53 && corellia_privateer_trainer_handleBranch53 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && corellia_privateer_trainer_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 63 && corellia_privateer_trainer_handleBranch63 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && corellia_privateer_trainer_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 65 && corellia_privateer_trainer_handleBranch65 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 66 && corellia_privateer_trainer_handleBranch66 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 67 && corellia_privateer_trainer_handleBranch67 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 68 && corellia_privateer_trainer_handleBranch68 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 70 && corellia_privateer_trainer_handleBranch70 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 71 && corellia_privateer_trainer_handleBranch71 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 73 && corellia_privateer_trainer_handleBranch73 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 74 && corellia_privateer_trainer_handleBranch74 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 76 && corellia_privateer_trainer_handleBranch76 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 77 && corellia_privateer_trainer_handleBranch77 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 79 && corellia_privateer_trainer_handleBranch79 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 81 && corellia_privateer_trainer_handleBranch81 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 82 && corellia_privateer_trainer_handleBranch82 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 84 && corellia_privateer_trainer_handleBranch84 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 86 && corellia_privateer_trainer_handleBranch86 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 88 && corellia_privateer_trainer_handleBranch88 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 90 && corellia_privateer_trainer_handleBranch90 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 91 && corellia_privateer_trainer_handleBranch91 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 92 && corellia_privateer_trainer_handleBranch92 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 96 && corellia_privateer_trainer_handleBranch96 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.corellia_privateer_trainer.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

