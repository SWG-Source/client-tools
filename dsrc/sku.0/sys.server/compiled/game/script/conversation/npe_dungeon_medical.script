// ======================================================================
//
// npe_dungeon_medical.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/npe_dungeon_medical";

// ======================================================================
// Script Conditions
// ======================================================================

boolean npe_dungeon_medical_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean npe_dungeon_medical_condition_isQuestFrelkaActive (obj_id player, obj_id npc)
{
	return (groundquests.isTaskActive(player, "npe_frelka_transition", "droidWait") ||
			groundquests.isTaskActive(player, "npe_frelka_transition_2", "droidWait")||
			groundquests.isQuestActive (player, "npe_frelka_quest"));
}

// ----------------------------------------------------------------------

boolean npe_dungeon_medical_condition_isAphoticActive (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "npe_frelka_quest", "killDie");
}

// ----------------------------------------------------------------------

boolean npe_dungeon_medical_condition_hasCompletedQuestAphotic (obj_id player, obj_id npc)
{
	return groundquests.isTaskActive(player, "npe_frelka_quest", "done");
}

// ----------------------------------------------------------------------

boolean npe_dungeon_medical_condition_hasCompletedFrelka (obj_id player, obj_id npc)
{
	return groundquests.hasCompletedTask(player, "npe_frelka_quest", "done");
}

// ----------------------------------------------------------------------

boolean npe_dungeon_medical_condition_hasTemplate (obj_id player, obj_id npc)
{
	return (hasObjVar(player, "npe.finishedTemplate") && !groundquests.isQuestActiveOrComplete(player, "npe_frelka_quest"));
}

// ----------------------------------------------------------------------

boolean npe_dungeon_medical_condition_hasFrelkaDungeonQuest (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive (player, "npe_frelka_quest");
}

// ======================================================================
// Script Actions
// ======================================================================

void npe_dungeon_medical_action_faceToNoob (obj_id player, obj_id npc)
{
	faceTo(npc, player);
}

// ----------------------------------------------------------------------

void npe_dungeon_medical_action_giveSignalNoob (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "talkdroid");
}

// ----------------------------------------------------------------------

void npe_dungeon_medical_action_grantFrelkaQuest (obj_id player, obj_id npc)
{
	if (!groundquests.hasCompletedQuest(player, "npe_frelka_quest"))
	{
		groundquests.requestGrantQuest(player, "npe_frelka_quest");
		
	}
}

// ----------------------------------------------------------------------

void npe_dungeon_medical_action_grantFrelkaReturn (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "npe_pointer_gamma");
}

// ----------------------------------------------------------------------

void npe_dungeon_medical_action_sendSignalNoob2 (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "return");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int npe_dungeon_medical_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hello, young master. Did you tell master Frelka?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have some questions still.
	if (response == "s_109")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: I will do my best to answer them.
			string_id message = new string_id (c_stringFile, "s_152");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Thanks for the answers.
			boolean hasResponse5 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_154");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_158");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_162");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_166");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_170");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_174");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome back, young master.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Rak'qua Aphotic... he's gone.
	if (response == "s_147")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			npe_dungeon_medical_action_sendSignalNoob2 (player, npc);

			//-- NPC: Oh, wonderful news! You should go tell Master Frelka. He'll be most pleased.
			string_id message = new string_id (c_stringFile, "s_148");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll do so. Thank you.
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_149");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have some questions still.
	if (response == "s_150")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: I will do my best to answer them.
			string_id message = new string_id (c_stringFile, "s_152");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Thanks for the answers.
			boolean hasResponse5 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_154");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_158");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_162");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_166");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_170");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_174");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, wonderful news! You should go tell Master Frelka. He'll be most pleased.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll do so. Thank you.
	if (response == "s_149")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Be safe, young master.
			string_id message = new string_id (c_stringFile, "s_177");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I will do my best to answer them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_154")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_156");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_158")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_162")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_164");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_166")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_168");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_170")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_172");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks for the answers.
	if (response == "s_174")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_176");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma as well. My sensors have picked up other mechanical signals in the area. You should be careful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Use the Shuttle to return to Tansarii.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why have you returned? Did you not defeat the Aphotic for master Frelka?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Oh, not yet. I'll get back to it.
	if (response == "s_144")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Be safe, young master. Before you go, can I help you with any questions about this station?
			string_id message = new string_id (c_stringFile, "s_145");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Be safe, young master. Before you go, can I help you with any questions about this station?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, no. You shouldn't be here. Did Frelka send you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, he did. What is this place?
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: It's a very dangerous one. You should leave as soon as possible.
			string_id message = new string_id (c_stringFile, "s_43");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I can take care of myself.
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want to ask you a few questions.
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's a very dangerous one. You should leave as soon as possible.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can take care of myself.
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to ask you a few questions.
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: I will do my best to answer them.
			string_id message = new string_id (c_stringFile, "s_86");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Did he send you here to defeat the Aphotic?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who is that?
	if (response == "s_77")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: It's a long story. The Aphotic hurt Frelka. I'm surprised he didn't say anything. I guess he doesn't want others meddling in his affairs.
			string_id message = new string_id (c_stringFile, "s_78");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I want to help him.
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's a long story. The Aphotic hurt Frelka. I'm surprised he didn't say anything. I guess he doesn't want others meddling in his affairs.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to help him.
	if (response == "s_79")
	{
		npe_dungeon_medical_action_giveSignalNoob (player, npc);

		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't know... I guess it couldn't hurt, could it? Find the Rak'qua Aphotic and defeat him for Frelka. The Aphotic should be very close; perhaps in the very beginning of this place. He'll be hard to miss.
			string_id message = new string_id (c_stringFile, "s_80");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Easy!
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_81");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know... I guess it couldn't hurt, could it? Find the Rak'qua Aphotic and defeat him for Frelka. The Aphotic should be very close; perhaps in the very beginning of this place. He'll be hard to miss.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Easy!
	if (response == "s_81")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			npe_dungeon_medical_action_grantFrelkaQuest (player, npc);

			//-- NPC: I wish you luck, young master. 
			string_id message = new string_id (c_stringFile, "s_82");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I will do my best to answer them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Use the Shuttle to return to Tansarii.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why are you here, young master?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just want to check this place out.
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: It's very dangerous. Maybe it would be best if you left...
			string_id message = new string_id (c_stringFile, "s_113");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I can take care of myself.
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_114");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's very dangerous. Maybe it would be best if you left...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can take care of myself.
	if (response == "s_114")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't know...
			string_id message = new string_id (c_stringFile, "s_115");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Trust me. Can you answer some questions for me?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you need help with anything?
			boolean hasResponse1 = false;
			if (!npe_dungeon_medical_condition_hasCompletedFrelka (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Trust me. Can you answer some questions for me?
	if (response == "s_116")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: I will do my best.
			string_id message = new string_id (c_stringFile, "s_118");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: Thanks for the answers.
			boolean hasResponse5 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_120");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_124");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_128");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_132");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_136");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_140");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you need help with anything?
	if (response == "s_85")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Oh, indeed I do. My master, Frelka, was horribly battered by something named Aphotic...Rak'qua Aphotic, perhaps.
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: He wants his revenge, huh?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I will do my best.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_120")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_122");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_124")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_126");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_128")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_130");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_132")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_134");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_136")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_138");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks for the answers.
	if (response == "s_140")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_142");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Use the Shuttle to return to Tansarii.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is this place?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: This is Station Gamma. It was once a research facility for Car'das allies, but now it has been taken over by the Rak'qua and their creations. It is a tough place to explore on your own. You might consider finding a group of friends to go in with you.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What happens if I die?
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you meet an unfortunate demise, you will end up here. You're safe among the Car'das.
			string_id message = new string_id (c_stringFile, "s_94");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I look out for anything in particular?
	if (response == "s_96")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should be mindful of the Rak'qua Overlord. There are other powerful denizens within Station Gamma, as well. My sensors have picked up other mechanical signals in the area. You should be careful.
			string_id message = new string_id (c_stringFile, "s_98");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get out of here?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Use the Shuttle to return to Tansarii.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm. Are there things of value to acquire here?
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed there are. You can vanquish enemies to get them. Many explorers hoping to find a quick credit or two often come here.
			string_id message = new string_id (c_stringFile, "s_106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is this place?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What happens if I die?
			boolean hasResponse1 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Should I look out for anything in particular?
			boolean hasResponse2 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: How do I get out of here?
			boolean hasResponse3 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Hm. Are there things of value to acquire here?
			boolean hasResponse4 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			//-- PLAYER: What's Frelka's story?
			boolean hasResponse5 = false;
			if (!npe_dungeon_medical_condition_hasFrelkaDungeonQuest (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse5 = true;
			}

			//-- PLAYER: Thank you, but I think I'll leave now.
			boolean hasResponse6 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse6 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				if (hasResponse5)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				if (hasResponse6)
					responses [responseIndex++] = new string_id (c_stringFile, "s_108");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's Frelka's story?
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Did he send you here to defeat the Aphotic?
			string_id message = new string_id (c_stringFile, "s_76");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is that?
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, but I think I'll leave now.
	if (response == "s_108")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Just be careful out there!
			string_id message = new string_id (c_stringFile, "s_110");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, indeed I do. My master, Frelka, was horribly battered by something named Aphotic...Rak'qua Aphotic, perhaps.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: He wants his revenge, huh?
	if (response == "s_93")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed so, young master. Would you avenge him?
			string_id message = new string_id (c_stringFile, "s_97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I will.
			boolean hasResponse0 = false;
			if (npe_dungeon_medical_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_101");

				utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int npe_dungeon_medical_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed so, young master. Would you avenge him?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I will.
	if (response == "s_101")
	{
		//-- [NOTE] 
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			npe_dungeon_medical_action_grantFrelkaQuest (player, npc);

			//-- NPC: Oh, thank you! Please, do come back soon.
			string_id message = new string_id (c_stringFile, "s_107");
			utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.npe_dungeon_medical");

	setCondition (self, CONDITION_CONVERSABLE);
	setName( self, "E-5KL");
	//setCondition (self, CONDITION_INTERESTING);
		setInvulnerable (self, true);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setName( self, "E-5KL");
	//setCondition( self, CONDITION_INTERESTING);
		setInvulnerable (self, true);
	
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.npe_dungeon_medical");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (npe_dungeon_medical_condition_hasCompletedFrelka (player, npc))
	{
		//-- NPC: Hello, young master. Did you tell master Frelka?
		string_id message = new string_id (c_stringFile, "s_105");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I have some questions still.
		boolean hasResponse0 = false;
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_109");

			utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 1);

			npcStartConversation (player, npc, "npe_dungeon_medical", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (npe_dungeon_medical_condition_hasCompletedQuestAphotic (player, npc))
	{
		//-- NPC: Welcome back, young master.
		string_id message = new string_id (c_stringFile, "s_146");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: The Rak'qua Aphotic... he's gone.
		boolean hasResponse0 = false;
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I have some questions still.
		boolean hasResponse1 = false;
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_147");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_150");

			utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 2);

			npcStartConversation (player, npc, "npe_dungeon_medical", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (npe_dungeon_medical_condition_isAphoticActive (player, npc))
	{
		//-- NPC: Why have you returned? Did you not defeat the Aphotic for master Frelka?
		string_id message = new string_id (c_stringFile, "s_143");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Oh, not yet. I'll get back to it.
		boolean hasResponse0 = false;
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_144");

			utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 12);

			npcStartConversation (player, npc, "npe_dungeon_medical", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (npe_dungeon_medical_condition_isQuestFrelkaActive (player, npc))
	{
		npe_dungeon_medical_action_faceToNoob (player, npc);

		//-- NPC: Oh, no. You shouldn't be here. Did Frelka send you?
		string_id message = new string_id (c_stringFile, "s_28");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, he did. What is this place?
		boolean hasResponse0 = false;
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 14);

			npcStartConversation (player, npc, "npe_dungeon_medical", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (npe_dungeon_medical_condition_hasTemplate (player, npc))
	{
		//-- NPC: Why are you here, young master?
		string_id message = new string_id (c_stringFile, "s_111");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Just want to check this place out.
		boolean hasResponse0 = false;
		if (npe_dungeon_medical_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_112");

			utils.setScriptVar (player, "conversation.npe_dungeon_medical.branchId", 27);

			npcStartConversation (player, npc, "npe_dungeon_medical", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (npe_dungeon_medical_condition__defaultCondition (player, npc))
	{
		//-- NPC: You shouldn't be here. Finish your training before you venture too far, young master.
		string_id message = new string_id (c_stringFile, "s_121");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "npe_dungeon_medical")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.npe_dungeon_medical.branchId");

	if (branchId == 1 && npe_dungeon_medical_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && npe_dungeon_medical_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && npe_dungeon_medical_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && npe_dungeon_medical_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && npe_dungeon_medical_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && npe_dungeon_medical_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && npe_dungeon_medical_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && npe_dungeon_medical_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && npe_dungeon_medical_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && npe_dungeon_medical_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && npe_dungeon_medical_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && npe_dungeon_medical_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && npe_dungeon_medical_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && npe_dungeon_medical_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && npe_dungeon_medical_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && npe_dungeon_medical_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && npe_dungeon_medical_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && npe_dungeon_medical_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && npe_dungeon_medical_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && npe_dungeon_medical_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && npe_dungeon_medical_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && npe_dungeon_medical_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && npe_dungeon_medical_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && npe_dungeon_medical_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && npe_dungeon_medical_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && npe_dungeon_medical_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && npe_dungeon_medical_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && npe_dungeon_medical_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && npe_dungeon_medical_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && npe_dungeon_medical_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && npe_dungeon_medical_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && npe_dungeon_medical_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && npe_dungeon_medical_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.npe_dungeon_medical.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

