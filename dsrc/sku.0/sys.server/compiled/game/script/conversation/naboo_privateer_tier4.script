// ======================================================================
//
// naboo_privateer_tier4.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.skill;
include library.space_flags;
include library.space_quest;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/naboo_privateer_tier4";

// ======================================================================
// Script Conditions
// ======================================================================

boolean naboo_privateer_tier4_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isWrongFaction (obj_id player, obj_id npc)
{
	//check the player has the correct novice skill box for this trainer
	return (!hasSkill(player,"pilot_neutral_novice"));
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasCompletedTier3 (obj_id player, obj_id npc)
{
	/*
	if (!hasSkill( player, "pilot_neutral_starships_03" ))
		return false;
	else if (!hasSkill( player, "pilot_neutral_procedures_03" ))
		return false;
	else if (!hasSkill( player, "pilot_neutral_droid_03" ))
		return false;
	else if (!hasSkill( player, "pilot_neutral_weapons_03" ))
		return false;
	else
		return true;
	*/
	
	return space_flags.hasCompletedTierThree(player);
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isOnMission (obj_id player, obj_id npc)
{
	return ( space_quest.hasQuest(player) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_faliedMissionOne (obj_id player, obj_id npc)
{
	if ( space_quest.hasAbortedQuestRecursive( player, "escort", "naboo_privateer_tier4_1a" ) ||
		 space_quest.hasFailedQuestRecursive( player, "escort", "naboo_privateer_tier4_1a" ) )
		 return true;

	return false;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_failedMissionTwo (obj_id player, obj_id npc)
{
	if ( space_quest.hasAbortedQuestRecursive( player, "inspect", "naboo_privateer_tier4_2a" ) ||
		 space_quest.hasFailedQuestRecursive( player, "inspect", "naboo_privateer_tier4_2a" ) )
		 return true;

	return false;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_failedMissionThree (obj_id player, obj_id npc)
{
	if ( space_quest.hasAbortedQuestRecursive( player, "delivery", "naboo_privateer_tier4_3a" ) ||
		 space_quest.hasFailedQuestRecursive( player, "delivery", "naboo_privateer_tier4_3a" ) )
		 return true;

	return false;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_failedMissionFour (obj_id player, obj_id npc)
{
	if ( space_quest.hasAbortedQuestRecursive( player, "space_battle", "naboo_privateer_tier4_4a" ) )
		 return true;
		 
	if ( space_quest.hasFailedQuest(player, "space_battle", "naboo_privateer_tier4_4a") ||
		 space_quest.hasFailedQuest(player, "space_battle", "naboo_privateer_tier4_4c_win") ||
		 space_quest.hasFailedQuest(player, "space_battle", "naboo_privateer_tier4_4c_lose") )
		 return true;
		 
	if ( space_quest.hasAbortedQuest(player, "space_battle", "naboo_privateer_tier4_4a") ||
		 space_quest.hasAbortedQuest(player, "space_battle", "naboo_privateer_tier4_4c_win") ||
		 space_quest.hasAbortedQuest(player, "space_battle", "naboo_privateer_tier4_4c_lose") )
		 return true;

	return false;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_failedMasterMission (obj_id player, obj_id npc)
{
	if ( space_quest.hasAbortedQuestRecursive( player, "destroy", "naboo_privateer_master" ) ||
		 space_quest.hasFailedQuestRecursive( player, "destroy", "naboo_privateer_master" ) )
		 return true;

	return false;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isReadyForTraining (obj_id player, obj_id npc)
{
	//see if the player has NOT completed all of tier 4
	//AND has enough XP to buy a tier-4 skill
	if (naboo_privateer_tier4_condition_hasCompletedTier4(player,npc))
		return false;//you're already done
		
	return ( space_quest.isPlayerQualifiedForSkill( player, "pilot_neutral_starships_04" ) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasCompletedTier4 (obj_id player, obj_id npc)
{
	/*
	if (!hasSkill( player, "pilot_neutral_starships_04" ))
		return false;
	else if (!hasSkill( player, "pilot_neutral_procedures_04" ))
		return false;
	else if (!hasSkill( player, "pilot_neutral_droid_04" ))
		return false;
	else if (!hasSkill( player, "pilot_neutral_weapons_04" ))
		return false;
	else
		return true;
	*/
	
	return space_flags.hasCompletedTierFour(player);
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasMasterSkill (obj_id player, obj_id npc)
{
	return (hasSkill( player, "pilot_neutral_master" ));
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isGettingMissionOneReward (obj_id player, obj_id npc)
{
	if (!space_quest.hasCompletedQuestRecursive( player, "escort", "naboo_privateer_tier4_1a" ))
		return false;//haven't completed it, so no reward
	
	//you've completed it, so you must be here for the reward:
	if (!space_quest.hasReceivedReward( player, "escort", "naboo_privateer_tier4_1a" ))
		return true;
	else
		return false;	
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isGettingMissionTwoReward (obj_id player, obj_id npc)
{
	if (!space_quest.hasCompletedQuestRecursive( player, "inspect", "naboo_privateer_tier4_2a" ))
		return false;//haven't completed it, so no reward
	
	//you've completed it, so you must be here for the reward:
	if (!space_quest.hasReceivedReward( player, "inspect", "naboo_privateer_tier4_2a" ))
		return true;
	else
		return false;	
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isGettingMissionThreeReward (obj_id player, obj_id npc)
{
	if (!space_quest.hasCompletedQuestRecursive( player, "delivery", "naboo_privateer_tier4_3a" ))
		return false;//haven't completed it, so no reward
	
	//you've completed it, so you must be here for the reward:
	if (!space_quest.hasReceivedReward( player, "delivery", "naboo_privateer_tier4_3a" ))
		return true;
	else
		return false;	
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isGettingMissionFourReward (obj_id player, obj_id npc)
{
	if ((!space_quest.hasCompletedQuest( player, "space_battle", "naboo_privateer_tier4_4c_win" )) && 
		(!space_quest.hasCompletedQuest( player, "space_battle", "naboo_privateer_tier4_4a_lose")) )
		return false;//haven't completed it, so no reward
	
	//you've completed it, so you must be here for the reward:
	if (!space_quest.hasReceivedReward( player, "space_battle", "naboo_privateer_tier4_4a" ))
		return true;
	else
		return false;	
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isGettingMasterMissionReward (obj_id player, obj_id npc)
{
	if (!space_quest.hasCompletedQuestRecursive( player, "destroy", "naboo_privateer_master" ))
		return false;//haven't completed it, so no reward
	
	//you've completed it, so you must be here for the reward:
	if (!space_quest.hasReceivedReward( player, "destroy", "naboo_privateer_master" ))
		return true;
	else
		return false;	
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasProceduresSkill04 (obj_id player, obj_id npc)
{
	return 	hasSkill( player, "pilot_neutral_procedures_04");
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasStarshipsSkill04 (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_neutral_starships_04" );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasWeaponsSkill04 (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_neutral_weapons_04" );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hadDroidSkill04 (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_neutral_droid_04" );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_onMyTrack (obj_id player, obj_id npc)
{
	return space_flags.isSpaceTrack(player, space_flags.PRIVATEER_NABOO);
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_canFlyNonTrackDuty (obj_id player, obj_id npc)
{
	if(space_flags.hasCompletedTierThree(player) && !space_quest.hasQuest(player))
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isPilot (obj_id player, obj_id npc)
{
	return space_flags.hasAnyPilotSkill(player);
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasMetMe (obj_id player, obj_id npc)
{
	return space_flags.hasSpaceFlag(player, "metTier4");
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_isReadyForMission (obj_id player, obj_id npc)
{
	return space_flags.hasSpaceFlag(player, "readyForTier4Mission");
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasTwoSkills (obj_id player, obj_id npc)
{
	int skillCount = 0;
	if (hasSkill(player,"pilot_neutral_droid_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_procedures_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_weapons_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_starships_04"))
		++skillCount;
		
	if(skillCount == 2)
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasThreeSkills (obj_id player, obj_id npc)
{
	int skillCount = 0;
	if (hasSkill(player,"pilot_neutral_droid_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_procedures_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_weapons_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_starships_04"))
		++skillCount;
		
	if(skillCount == 3)
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_tier4_condition_hasFourSkills (obj_id player, obj_id npc)
{
	int skillCount = 0;
	if (hasSkill(player,"pilot_neutral_droid_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_procedures_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_weapons_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_starships_04"))
		++skillCount;
		
	if(skillCount == 4)
		return true;
		
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void naboo_privateer_tier4_action_grantMissionOne (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "escort", "naboo_privateer_tier4_1a" );
	
	if(space_flags.hasSpaceFlag(player, "readyForTier4Mission"))
		space_flags.removeSpaceFlag(player, "readyForTier4Mission");
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantMissionTwo (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "inspect", "naboo_privateer_tier4_2a" );
	
	if(space_flags.hasSpaceFlag(player, "readyForTier4Mission"))
		space_flags.removeSpaceFlag(player, "readyForTier4Mission");
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantMissionThree (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "delivery", "naboo_privateer_tier4_3a" );
	
	if(space_flags.hasSpaceFlag(player, "readyForTier4Mission"))
		space_flags.removeSpaceFlag(player, "readyForTier4Mission");
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantMissionFour (obj_id player, obj_id npc)
{
	space_quest.clearQuestFlags(player, "space_battle", "naboo_privateer_tier4_4c_win");
	space_quest.clearQuestFlags(player, "space_battle", "naboo_privateer_tier4_4c_lose");
	space_quest.grantQuest( player, "space_battle", "naboo_privateer_tier4_4a" );
	
	if(space_flags.hasSpaceFlag(player, "readyForTier4Mission"))
		space_flags.removeSpaceFlag(player, "readyForTier4Mission");
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantMasterMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy", "naboo_privateer_master" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantDutyMissionOneDestroy (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy_duty", "naboo_privateer_tier4_1" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantDutyMissionTwoEscort (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "escort_duty", "naboo_privateer_tier4_1" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantDutyMissionThreeRecovery (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "recovery_duty", "naboo_privateer_tier4_1" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantDutyMissionFourRescue (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "rescue_duty", "naboo_privateer_tier4_1" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_rewardForMissionOne (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "escort", "naboo_privateer_tier4_1a" ))
		space_quest.giveReward( player, "escort", "naboo_privateer_tier4_1a", 10000, "object/tangible/ship/components/booster/bst_mission_reward_neutral_mandal_q_series.iff" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_rewardForMissionTwo (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "inspect", "naboo_privateer_tier4_2a" ))
		space_quest.giveReward( player, "inspect", "naboo_privateer_tier4_2a", 10000, "object/tangible/ship/components/weapon/wpn_mission_reward_neutral_borstel_disruptor.iff" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_rewardForMissionThree (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "delivery", "naboo_privateer_tier4_3a" ))
		space_quest.giveReward( player, "delivery", "naboo_privateer_tier4_3a", 10000, "object/tangible/ship/components/shield_generator/shd_mission_reward_neutral_armek_plasma_web.iff" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_rewardForMissionFour (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "space_battle", "naboo_privateer_tier4_4a" ))
		space_quest.giveReward( player, "space_battle", "naboo_privateer_tier4_4a", 10000, "object/tangible/ship/components/engine/eng_mission_reward_neutral_haor_chall_military.iff" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_rewardForMasterMission (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "destroy", "naboo_privateer_master" ))
	{
		space_quest.giveReward( player, "destroy", "naboo_privateer_master", 5000 );
		grantSkill( player, "pilot_neutral_master" );
	}
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_buyDroidSkill04 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_droid_04" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_buyProceduresSkill04 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_procedures_04" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_buyStarshipsSkill04 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_starships_04" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_buyWeaponsSkill04 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_weapons_04" );
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_setMetMe (obj_id player, obj_id npc)
{
	space_flags.setSpaceFlag(player, "metTier4", true);
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_grantNextMission (obj_id player, obj_id npc)
{
	int skillCount = 0;
	if (hasSkill(player,"pilot_neutral_droid_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_procedures_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_weapons_04"))
		++skillCount;
	if (hasSkill(player,"pilot_neutral_starships_04"))
		++skillCount;

	switch ( skillCount )
	{
		case 1:
			naboo_privateer_tier4_action_grantMissionOne( player, npc );
			break;
		case 2:
			naboo_privateer_tier4_action_grantMissionTwo( player, npc );
			break;
		case 3:
			naboo_privateer_tier4_action_grantMissionThree( player, npc );
			break;
		case 4:
			naboo_privateer_tier4_action_grantMissionFour( player, npc );
			break;
	}
				
}

// ----------------------------------------------------------------------

void naboo_privateer_tier4_action_setReadyForMission (obj_id player, obj_id npc)
{
	space_flags.setSpaceFlag(player, "readyForTier4Mission", true);
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int naboo_privateer_tier4_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You're the wrong faction.
	//-- NPC: I don't think I've seen you before. Maybe you took a wrong turn somewhere.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm looking for Rebels.
	if (response == "s_c09ba3fa")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Won't find any here. See ya.
			string_id message = new string_id (c_stringFile, "s_35a5b893");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm looking for Imperials.
	if (response == "s_251ee0bd")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Won't find any here. See ya.
			string_id message = new string_id (c_stringFile, "s_9");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't think I've seen you before, but your name is familiar to me. What brings you here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm looking for a mission. Do you have any?
	if (response == "s_c1ff5062")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition_canFlyNonTrackDuty (player, npc))
		{
			//-- NPC: That's a nice attitude for a pilot to have. Actually, I do have some duty missions to assign. Take a look at what there is. Shall I give you a briefing?
			string_id message = new string_id (c_stringFile, "s_24231574");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, please.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c82e9a2f");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I like your attitude. A busy pilot is a good pilot. But unfortunately I don't have any missions right now. Try your own contacts. 
			string_id message = new string_id (c_stringFile, "s_aff98418");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's a nice attitude for a pilot to have. Actually, I do have some duty missions to assign. Take a look at what there is. Shall I give you a briefing?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, please.
	if (response == "s_c82e9a2f")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Now listen up and pay attention. I hate repeating myself. I need a pilot to escort a Naboo mining transport through Endor space. I'll warn you, Borvo the Hutt's men will probably be interested in that transport.
			string_id message = new string_id (c_stringFile, "s_5603b2a1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_416");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 170);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You're not through tier-3 yet
	//-- NPC: Tsk. Better get back to Commander Dulios. He hates it when people don't finish what they started.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm done with him!
	if (response == "s_a404eb10")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "cough_polite");

			//-- NPC: I don't think he's done with YOU, though.
			string_id message = new string_id (c_stringFile, "s_25cf4e47");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are you talking about?
	if (response == "s_2313ac9e")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Ask him. Bye bye.
			string_id message = new string_id (c_stringFile, "s_886cd421");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You're busy.  Already doing a mission.  Hup hup, go do it.
	//-- NPC: I don't have any patience for lightweights. Do you know how to do your job, or don't you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course I do!
	if (response == "s_eb4e939")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "snap_finger2");

			//-- NPC: Oh yeah? Well show me, then.
			string_id message = new string_id (c_stringFile, "s_3892e6da");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hey, I'm working on it.
	if (response == "s_23e8aa6d")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: I'd like to see a little more sweat, if you know what I mean.
			string_id message = new string_id (c_stringFile, "s_94654f90");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You failed mission 1, do it over.
	//-- NPC: I thought you said you were a good pilot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am. I'm just having a bad day.
	if (response == "s_5e1ad1a2")
	{
		doAnimationAction (player, "slump_head");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			naboo_privateer_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Well don't come to me looking for a shoulder to cry on. I want results.
			string_id message = new string_id (c_stringFile, "s_a77b8282");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: This job's not as easy as it looks, you know.
	if (response == "s_ff47ef5a")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "offer_affection");

			naboo_privateer_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Really? Because it looks pretty easy. Maybe you could give it another shot. Please? 
			string_id message = new string_id (c_stringFile, "s_87f301d7");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You failed mission 2, do it over.
	//-- NPC: Well THAT didn't go as planned.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No kidding!
	if (response == "s_eb72f32e")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: Now, now. Don't get upset. Just because you can't get the job done.
			string_id message = new string_id (c_stringFile, "s_c5b4e219");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Oh, I can DO it. Don't worry.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cfba6d32");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can do it.
	if (response == "s_c17d2691")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: Give it another shot. Those Black Suns aren't so tough.
			string_id message = new string_id (c_stringFile, "s_a9d8ef68");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Now, now. Don't get upset. Just because you can't get the job done.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Oh, I can DO it. Don't worry.
	if (response == "s_cfba6d32")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			//-- NPC: Oh! Well that's a relief. 
			string_id message = new string_id (c_stringFile, "s_b7bd69e9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I guess I should just...go and do it, then.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4fcfcf7");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh! Well that's a relief. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I guess I should just...go and do it, then.
	if (response == "s_c4fcfcf7")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			naboo_privateer_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: Sure! Why not.
			string_id message = new string_id (c_stringFile, "s_501c35bc");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You failed mission 3, do it over.
	//-- NPC: Our relationship with the Imperials is an important one.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Things don't always go as planned.
	if (response == "s_8bbbb2ce")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			//-- NPC: In this system, things DO go as planned. My pilots get the job done.
			string_id message = new string_id (c_stringFile, "s_5fff247b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes ma'am.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5c429d49");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't think one setback is going to destroy our alliance.
	if (response == "s_c5f46951")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_foot");

			//-- NPC: Then you don't know the Imperials. Are you talking back to me?
			string_id message = new string_id (c_stringFile, "s_fcd38a06");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No ma'am.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fc7e0e7a");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: In this system, things DO go as planned. My pilots get the job done.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes ma'am.
	if (response == "s_5c429d49")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			naboo_privateer_tier4_action_grantMissionThree (player, npc);

			//-- NPC: Get out there and try again.
			string_id message = new string_id (c_stringFile, "s_d858532");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then you don't know the Imperials. Are you talking back to me?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No ma'am.
	if (response == "s_fc7e0e7a")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			naboo_privateer_tier4_action_grantMissionThree (player, npc);

			//-- NPC: I didn't think so. I want you to get up there and make things right. Immediately!
			string_id message = new string_id (c_stringFile, "s_d767398d");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You failed mission 4, do it over.
	//-- NPC: Listen, if you need a break, just say so. I've got other pilots. And you've been through a lot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No. I'm OK.
	if (response == "s_e2dbfb72")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You sure?
			string_id message = new string_id (c_stringFile, "s_1f680e83");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm sure.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0da26cf");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nobody replaces me.
	if (response == "s_8fe73508")
	{
		doAnimationAction (player, "point_to_self");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: I won't think any less of you. You've proven yourself to me.
			string_id message = new string_id (c_stringFile, "s_bd3f3d63");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready to go.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d55f2579");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You sure?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sure.
	if (response == "s_b0da26cf")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_privateer_tier4_action_grantMissionFour (player, npc);

			//-- NPC: OK, then.
			string_id message = new string_id (c_stringFile, "s_a479efa4");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I won't think any less of you. You've proven yourself to me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to go.
	if (response == "s_d55f2579")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			naboo_privateer_tier4_action_grantMissionFour (player, npc);

			//-- NPC: OK, if you're sure.
			string_id message = new string_id (c_stringFile, "s_de697017");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have completed mission one and so here is your reward.
	//-- NPC: So. You did it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure did.
	if (response == "s_e0ce54d")
	{
		doAnimationAction (player, "pose_proudly");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: I guess I have to pay you then.
			string_id message = new string_id (c_stringFile, "s_4d30b2ce");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That would be nice.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e1e40ead");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 33);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It was easy.
	if (response == "s_9121b74a")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Weren't you just a little bit scared? Just once?
			string_id message = new string_id (c_stringFile, "s_4ff27acf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Never.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c8888bec");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 36);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I guess I have to pay you then.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That would be nice.
	if (response == "s_e1e40ead")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "offer_affection");

			//-- NPC: Do you REALLY need to get paid? You're not doing the job for the money! Isn't it enough to know you're serving the royal family?
			string_id message = new string_id (c_stringFile, "s_7b043f5c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not by a long shot. And yes, I AM doing it for the money.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_55dae7d2");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Do you REALLY need to get paid? You're not doing the job for the money! Isn't it enough to know you're serving the royal family?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not by a long shot. And yes, I AM doing it for the money.
	if (response == "s_55dae7d2")
	{
		doAnimationAction (player, "stamp_feet");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Fine. Here's your duty pay. Find me later when you want another mission.
			string_id message = new string_id (c_stringFile, "s_4f4c63b2");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Weren't you just a little bit scared? Just once?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Never.
	if (response == "s_c8888bec")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Hm. Maybe I should start sending you on more dangerous missions. How would you feel about that?
			string_id message = new string_id (c_stringFile, "s_e9b81b8b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'd feel great.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4cbd4a9d");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hm. Maybe I should start sending you on more dangerous missions. How would you feel about that?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd feel great.
	if (response == "s_4cbd4a9d")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Interesting. Here's your duty pay. Let's talk later.
			string_id message = new string_id (c_stringFile, "s_6140c0f4");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have completed mission two and so here is your reward.
	//-- NPC: I have somebody's duty pay.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That would be mine.
	if (response == "s_328cdc99")
	{
		doAnimationAction (player, "point_to_self");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Here you go. I'm putting together another mission for you. Come see me later.
			string_id message = new string_id (c_stringFile, "s_3ff13caa");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Aren't you going to thank me?
	if (response == "s_f1e2aed8")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: What? For doing your job?
			string_id message = new string_id (c_stringFile, "s_95fa2e6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d70dba34");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 41);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What? For doing your job?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_d70dba34")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: No. I'm not. Although...I'll admit that I didn't think you'd pull it off.
			string_id message = new string_id (c_stringFile, "s_76cd7167");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why did you send me?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_18a40c8c");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 42);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No. I'm not. Although...I'll admit that I didn't think you'd pull it off.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then why did you send me?
	if (response == "s_18a40c8c")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: For the laughs. I like watching you fight. At any rate, here's your duty pay. I'll see you around.
			string_id message = new string_id (c_stringFile, "s_c62300e0");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch44 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have completed mission three and so here is your reward.
	//-- NPC: Here's your duty pay. Maybe you can spend some of your money on ship repairs.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to. Those Suns nearly shredded me.
	if (response == "s_7977e8e2")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: I know. I was worried.
			string_id message = new string_id (c_stringFile, "s_ab583079");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I didn't know you cared.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af9cab7a");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you kidding? I'm going to the cantina.
	if (response == "s_953c410e")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Some firefight.
			string_id message = new string_id (c_stringFile, "s_96b5ddf3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Black Suns are animals.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ddf79f01");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I know. I was worried.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I didn't know you cared.
	if (response == "s_af9cab7a")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: What? Who? Me? I don't care. Get out of here.
			string_id message = new string_id (c_stringFile, "s_eb559766");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Some firefight.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Black Suns are animals.
	if (response == "s_ddf79f01")
	{
		doAnimationAction (player, "smack_self");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stretch");

			//-- NPC: I think we're going to have to deal with them. But not right now. Take a break. I'll see you later.
			string_id message = new string_id (c_stringFile, "s_c31a5da7");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch49 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have completed mission four and so here is your reward.
	//-- NPC: Look. Here comes my favorite pilot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's me, baby.
	if (response == "s_ad713f95")
	{
		doAnimationAction (player, "point_to_self");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Here's a little something for you. Bye now.
			string_id message = new string_id (c_stringFile, "s_9d566d78");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You're making me blush.
	if (response == "s_2e904f6c")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Good. That's idea. Here's your duty pay. Bye now.
			string_id message = new string_id (c_stringFile, "s_9b07df81");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hurry up. It's time to get back to work.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever you say.
	if (response == "s_39e697de")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition_hasFourSkills (player, npc))
		{
			//-- NPC: I think the Black Suns have declared war on the RSF.
			string_id message = new string_id (c_stringFile, "s_e4bbe441");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I think you're right. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Good. Because I want to go after them.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_453");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_473");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition_hasThreeSkills (player, npc))
		{
			//-- NPC: Those Black Suns didn't like the fact you found their secret cargo.
			string_id message = new string_id (c_stringFile, "s_ff6e1247");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who cares what they think?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: They're lowlifes. 
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I didn't like the fact they tried to kill me.
			boolean hasResponse2 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_491");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_539");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_587");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition_hasTwoSkills (player, npc))
		{
			//-- NPC: You did a nice job protecting that scan vessel. We took a look at the tapes.
			string_id message = new string_id (c_stringFile, "s_bdc28bb4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What did you find?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have a mission for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_625");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_709");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: So. How was life with Commander Dulios?
			string_id message = new string_id (c_stringFile, "s_9312a850");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Action packed.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Ridiculous.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_739");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_819");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 132);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch53 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I think the Black Suns have declared war on the RSF.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I think you're right. 
	if (response == "s_453")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're lucky you made it back in one piece.
			string_id message = new string_id (c_stringFile, "s_455");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Luck had nothing to do with it.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Good. Because I want to go after them.
	if (response == "s_473")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			//-- NPC: You're about to get your wish. But this time, you're not going alone. We need a show of force. You'll be joining a fleet of RSF pilots up around Yavin in a sortie against the Black Sun.
			string_id message = new string_id (c_stringFile, "s_475");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: These other pilots. Are they any good?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_477");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Those Black Suns didn't like the fact you found their secret cargo.

	//-- [RESPONSE NOTE] debrief branch
	//-- PLAYER: Who cares what they think?
	if (response == "s_491")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: I like your attitude. I was right about that freighter, wasn't I?
			string_id message = new string_id (c_stringFile, "s_493");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What was the cargo?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_495");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They're lowlifes. 
	if (response == "s_539")
	{
		doAnimationAction (player, "shake_head_disgust");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Lowlifes with guns.
			string_id message = new string_id (c_stringFile, "s_541");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not any more.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_543");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 82);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I didn't like the fact they tried to kill me.
	if (response == "s_587")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Haha! They sure did.
			string_id message = new string_id (c_stringFile, "s_589");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You were right. They were packed to the gills with contraband.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_591");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 94);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch55 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You did a nice job protecting that scan vessel. We took a look at the tapes.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What did you find?
	if (response == "s_625")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: Nothing definite. Black Suns know how to cover their tracks. But they're definitely active in this system. And that's a problem, considering the Imperial crackdown.
			string_id message = new string_id (c_stringFile, "s_627");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see what you mean.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What Imperial crackdown?
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_629");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_665");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 104);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you have a mission for me?
	if (response == "s_709")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Always. There's a heavy mining freighter passing through Endor space.
			string_id message = new string_id (c_stringFile, "s_711");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: This is another escort mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_713");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 125);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch56 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So. How was life with Commander Dulios?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Action packed.
	if (response == "s_739")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Well, you'll get more of the same here, if you're lucky. Are you a good pilot?
			string_id message = new string_id (c_stringFile, "s_741");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You bet.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You tell me.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_743");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_783");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 133);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ridiculous.
	if (response == "s_819")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: He didn't dazzle you with his charm?
			string_id message = new string_id (c_stringFile, "s_821");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not exactly. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_823");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 153);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch57 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You're ready for a new skill, which would you like?
	//-- NPC: I'm supposed to give you a skill. Hurry up. Let's get this over with.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What skill are you going to give me?
	if (response == "s_438")
	{
		//-- [NOTE] 
		if (!naboo_privateer_tier4_condition_hasStarshipsSkill04 (player, npc))
		{
			//-- NPC: It looks like you're ready to learn about exotic vessels.
			string_id message = new string_id (c_stringFile, "s_439");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good to me.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_443");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_tier4_condition_hasWeaponsSkill04 (player, npc))
		{
			//-- NPC: It looks like you're ready to learn about heavy starship components.
			string_id message = new string_id (c_stringFile, "s_440");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good to me.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_445");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 161);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_tier4_condition_hasProceduresSkill04 (player, npc))
		{
			//-- NPC: It looks like you're ready to learn about expert techniques.
			string_id message = new string_id (c_stringFile, "s_441");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good to me.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_447");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 162);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: It looks like you're ready to learn about droid intelligence theory.
			string_id message = new string_id (c_stringFile, "s_442");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good to me.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_449");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 163);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're ready to learn about exotic vessels.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good to me.
	if (response == "s_443")
	{
		naboo_privateer_tier4_action_buyStarshipsSkill04 (player, npc);

		//-- [NOTE] Now go do Mission Four
		if (naboo_privateer_tier4_condition_hasFourSkills (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: I think the Black Suns have declared war on the RSF.
			string_id message = new string_id (c_stringFile, "s_451");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I think you're right. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Good. Because I want to go after them.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_453");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_473");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission Three.
		if (naboo_privateer_tier4_condition_hasThreeSkills (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: Those Black Suns didn't like the fact you found their secret cargo.
			string_id message = new string_id (c_stringFile, "s_489");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who cares what they think?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: They're lowlifes. 
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I didn't like the fact they tried to kill me.
			boolean hasResponse2 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_491");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_539");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_587");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission Two.
		if (naboo_privateer_tier4_condition_hasTwoSkills (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: You did a nice job protecting that scan vessel. We took a look at the tapes.
			string_id message = new string_id (c_stringFile, "s_623");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What did you find?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have a mission for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_625");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_709");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission One.
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: So. How was life with Commander Dulios?
			string_id message = new string_id (c_stringFile, "s_737");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Action packed.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Ridiculous.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_739");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_819");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 132);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch59 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] Now go do Mission Four
	//-- NPC: I think the Black Suns have declared war on the RSF.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I think you're right. 
	if (response == "s_453")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're lucky you made it back in one piece.
			string_id message = new string_id (c_stringFile, "s_455");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Luck had nothing to do with it.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_457");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Good. Because I want to go after them.
	if (response == "s_473")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			//-- NPC: You're about to get your wish. But this time, you're not going alone. We need a show of force. You'll be joining a fleet of RSF pilots up around Yavin in a sortie against the Black Sun.
			string_id message = new string_id (c_stringFile, "s_475");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: These other pilots. Are they any good?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_477");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch60 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're lucky you made it back in one piece.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Luck had nothing to do with it.
	if (response == "s_457")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: Well, this is a new problem in Yavin. We'll just have to deal with it. I'm not going to wait for them to come to us.
			string_id message = new string_id (c_stringFile, "s_459");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Send me out there.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_461");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, this is a new problem in Yavin. We'll just have to deal with it. I'm not going to wait for them to come to us.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Send me out there.
	if (response == "s_461")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You read my mind. But this time, you're not going alone. We need a show of force. You'll be joining a fleet of RSF pilots in a sortie against the Black Sun.
			string_id message = new string_id (c_stringFile, "s_463");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: These other pilots. Are they any good?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_465");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You read my mind. But this time, you're not going alone. We need a show of force. You'll be joining a fleet of RSF pilots in a sortie against the Black Sun.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: These other pilots. Are they any good?
	if (response == "s_465")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: I think so. But you can be the judge of that. Ready to go?
			string_id message = new string_id (c_stringFile, "s_467");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Absolutely.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_469");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch63 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I think so. But you can be the judge of that. Ready to go?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Absolutely.
	if (response == "s_469")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			naboo_privateer_tier4_action_grantMissionFour (player, npc);

			//-- NPC: Good luck.
			string_id message = new string_id (c_stringFile, "s_471");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch65 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're about to get your wish. But this time, you're not going alone. We need a show of force. You'll be joining a fleet of RSF pilots up around Yavin in a sortie against the Black Sun.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: These other pilots. Are they any good?
	if (response == "s_477")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: I think so. But you can be the judge of that. 
			string_id message = new string_id (c_stringFile, "s_479");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What about the Imperials?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_481");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch66 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I think so. But you can be the judge of that. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What about the Imperials?
	if (response == "s_481")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: They're not going to get involved. They've got other things to worry about. You just take care of those Black Suns. Ready to go?
			string_id message = new string_id (c_stringFile, "s_483");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Absolutely.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_485");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch67 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're not going to get involved. They've got other things to worry about. You just take care of those Black Suns. Ready to go?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Absolutely.
	if (response == "s_485")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			doAnimationAction (player, "salute1");

			naboo_privateer_tier4_action_grantMissionFour (player, npc);

			//-- NPC: Good luck.
			string_id message = new string_id (c_stringFile, "s_487");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch69 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] Now go do Mission Three.
	//-- NPC: Those Black Suns didn't like the fact you found their secret cargo.

	//-- [RESPONSE NOTE] debrief branch
	//-- PLAYER: Who cares what they think?
	if (response == "s_491")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: I like your attitude. I was right about that freighter, wasn't I?
			string_id message = new string_id (c_stringFile, "s_493");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What was the cargo?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_495");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They're lowlifes. 
	if (response == "s_539")
	{
		doAnimationAction (player, "shake_head_disgust");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Lowlifes with guns.
			string_id message = new string_id (c_stringFile, "s_541");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not any more.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_543");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 82);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I didn't like the fact they tried to kill me.
	if (response == "s_587")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Haha! They sure did.
			string_id message = new string_id (c_stringFile, "s_589");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You were right. They were packed to the gills with contraband.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_591");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 94);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch70 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I like your attitude. I was right about that freighter, wasn't I?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What was the cargo?
	if (response == "s_495")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shiver");

			//-- NPC: 'Materials for an explosives plant. If they had moved them into this system undetected... well, they didn't. That's all that matters.
			string_id message = new string_id (c_stringFile, "s_497");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not this time. But they'll try again. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_499");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 71);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch71 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: 'Materials for an explosives plant. If they had moved them into this system undetected... well, they didn't. That's all that matters.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not this time. But they'll try again. 
	if (response == "s_499")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			//-- NPC: Let me worry about the Black Suns. I have a new mission for you. 
			string_id message = new string_id (c_stringFile, "s_501");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_503");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 72);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch72 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Let me worry about the Black Suns. I have a new mission for you. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready.
	if (response == "s_503")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A pair of Imperial freighters have entered Yavin space. They need a go-between for a supply transfer. And that go-between would be you. 
			string_id message = new string_id (c_stringFile, "s_505");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Imperials!
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_507");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_515");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch73 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A pair of Imperial freighters have entered Yavin space. They need a go-between for a supply transfer. And that go-between would be you. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK. 
	if (response == "s_507")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?
			string_id message = new string_id (c_stringFile, "s_509");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Whatever you say.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_511");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperials!
	if (response == "s_515")
	{
		doAnimationAction (player, "shake_head_disgust");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not your favorites, I take it?
			string_id message = new string_id (c_stringFile, "s_517");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not so much.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_519");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch74 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever you say.
	if (response == "s_511")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			naboo_privateer_tier4_action_grantMissionThree (player, npc);

			//-- NPC: I'll talk to you when you get back.
			string_id message = new string_id (c_stringFile, "s_513");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch76 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not your favorites, I take it?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not so much.
	if (response == "s_519")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Why not?
			string_id message = new string_id (c_stringFile, "s_521");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Because they're bloodthirsty lunatics, I guess.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_523");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch77 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why not?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Because they're bloodthirsty lunatics, I guess.
	if (response == "s_523")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well THAT'S not a good enough reason! You're pretty bloodthirsty, too.
			string_id message = new string_id (c_stringFile, "s_525");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, but...well...
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_527");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 78);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch78 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well THAT'S not a good enough reason! You're pretty bloodthirsty, too.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, but...well...
	if (response == "s_527")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Look, this isn't an ethics class. We're all just here to do our job. 
			string_id message = new string_id (c_stringFile, "s_529");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_531");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch79 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Look, this isn't an ethics class. We're all just here to do our job. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK. 
	if (response == "s_531")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?
			string_id message = new string_id (c_stringFile, "s_533");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Whatever you say.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_535");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 80);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch80 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever you say.
	if (response == "s_535")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			naboo_privateer_tier4_action_grantMissionThree (player, npc);

			//-- NPC: I'll talk to you when you get back.
			string_id message = new string_id (c_stringFile, "s_537");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch82 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Lowlifes with guns.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not any more.
	if (response == "s_543")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You did good. I have a new mission for you.
			string_id message = new string_id (c_stringFile, "s_545");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_547");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 83);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch83 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You did good. I have a new mission for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready.
	if (response == "s_547")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A pair of Imperial freighters have entered Yavin space. They need a go-between for a supply transfer. And that go-between would be you. 
			string_id message = new string_id (c_stringFile, "s_549");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Imperials!
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_551");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_563");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch84 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A pair of Imperial freighters have entered Yavin space. They need a go-between for a supply transfer. And that go-between would be you. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK. 
	if (response == "s_551")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?
			string_id message = new string_id (c_stringFile, "s_553");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What about the Black Suns?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_555");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 85);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperials!
	if (response == "s_563")
	{
		doAnimationAction (player, "shake_head_disgust");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not your favorites, I take it?
			string_id message = new string_id (c_stringFile, "s_565");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not so much.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_567");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 88);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch85 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What about the Black Suns?
	if (response == "s_555")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "snap_finger1");

			//-- NPC: What about  them? Look, they don't bother us, we don't bother them. Right now, Imperial happiness is your only concern. Get out there and take care of those captains. 
			string_id message = new string_id (c_stringFile, "s_557");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Whatever you say.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_559");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 86);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch86 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What about  them? Look, they don't bother us, we don't bother them. Right now, Imperial happiness is your only concern. Get out there and take care of those captains. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever you say.
	if (response == "s_559")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			naboo_privateer_tier4_action_grantMissionThree (player, npc);

			//-- NPC: Go on. I'm busy. I've got things to do.
			string_id message = new string_id (c_stringFile, "s_561");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch88 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not your favorites, I take it?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not so much.
	if (response == "s_567")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Why not?
			string_id message = new string_id (c_stringFile, "s_569");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Because they're bloodthirsty lunatics, I guess.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_571");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 89);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch89 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why not?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Because they're bloodthirsty lunatics, I guess.
	if (response == "s_571")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well THAT'S not a good enough reason! You're pretty bloodthirsty, too.
			string_id message = new string_id (c_stringFile, "s_573");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, but...well...
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_575");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch90 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well THAT'S not a good enough reason! You're pretty bloodthirsty, too.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, but...well...
	if (response == "s_575")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: Look, this isn't an ethics class. We're all just here to do our job. 
			string_id message = new string_id (c_stringFile, "s_577");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_579");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 91);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch91 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Look, this isn't an ethics class. We're all just here to do our job. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK. 
	if (response == "s_579")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?
			string_id message = new string_id (c_stringFile, "s_581");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Whatever you say.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_583");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 92);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch92 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever you say.
	if (response == "s_583")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			naboo_privateer_tier4_action_grantMissionThree (player, npc);

			//-- NPC: I'll talk to you when you get back.
			string_id message = new string_id (c_stringFile, "s_585");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch94 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Haha! They sure did.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You were right. They were packed to the gills with contraband.
	if (response == "s_591")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: What can I say? I've got a nose for these kinds of  things.
			string_id message = new string_id (c_stringFile, "s_593");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's going on with the Black Suns?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_595");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch95 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What can I say? I've got a nose for these kinds of  things.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's going on with the Black Suns?
	if (response == "s_595")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: What do you mean?
			string_id message = new string_id (c_stringFile, "s_597");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They're going after the RSF pretty hard.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_599");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 96);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch96 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What do you mean?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They're going after the RSF pretty hard.
	if (response == "s_599")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: No, my friend. They're going against YOU pretty hard. You're good at what you do. And that means you're getting in their way. Black Suns don't like people getting in their way.
			string_id message = new string_id (c_stringFile, "s_601");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well I'm not going to stop.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_603");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch97 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No, my friend. They're going against YOU pretty hard. You're good at what you do. And that means you're getting in their way. Black Suns don't like people getting in their way.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well I'm not going to stop.
	if (response == "s_603")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course not. Forget about them for now. I have a new mission for you.
			string_id message = new string_id (c_stringFile, "s_605");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_607");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch98 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course not. Forget about them for now. I have a new mission for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready.
	if (response == "s_607")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A pair of Imperial freighters have entered Yavin space. They need a go-between for a supply transfer. And that go-between would be you. 
			string_id message = new string_id (c_stringFile, "s_609");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_611");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch99 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A pair of Imperial freighters have entered Yavin space. They need a go-between for a supply transfer. And that go-between would be you. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK. 
	if (response == "s_611")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?
			string_id message = new string_id (c_stringFile, "s_613");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Imperials.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_615");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 100);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch100 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You know how testy Imperial freighter captains can be. They think they own the universe. Especially these two. They're a real couple of killjoys. But they're our friends, so make nice, OK?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Imperials.
	if (response == "s_615")
	{
		doAnimationAction (player, "shake_head_disgust");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: You don't have  to love 'em. You just have to keep 'em happy. Get out there and take care of those captains. 
			string_id message = new string_id (c_stringFile, "s_617");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Whatever you say.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_619");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 101);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch101 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You don't have  to love 'em. You just have to keep 'em happy. Get out there and take care of those captains. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever you say.
	if (response == "s_619")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			naboo_privateer_tier4_action_grantMissionThree (player, npc);

			//-- NPC: Go on. I'm busy. I've got things to do.
			string_id message = new string_id (c_stringFile, "s_621");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch103 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] Now go do Mission Two.
	//-- NPC: You did a nice job protecting that scan vessel. We took a look at the tapes.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What did you find?
	if (response == "s_625")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			//-- NPC: Nothing definite. Black Suns know how to cover their tracks. But they're definitely active in this system. And that's a problem, considering the Imperial crackdown.
			string_id message = new string_id (c_stringFile, "s_627");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see what you mean.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What Imperial crackdown?
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_629");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_665");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 104);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you have a mission for me?
	if (response == "s_709")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Always. There's a heavy mining freighter passing through Endor space.
			string_id message = new string_id (c_stringFile, "s_711");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: This is another escort mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_713");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 125);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch104 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Nothing definite. Black Suns know how to cover their tracks. But they're definitely active in this system. And that's a problem, considering the Imperial crackdown.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see what you mean.
	if (response == "s_629")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I mean, the Empire's number one problem is that prison, but their radar is up, if you know what I mean. They're looking for trouble across the board. So we're going  to watch our step. We're going to watch everybody ELSE'S steps, too.
			string_id message = new string_id (c_stringFile, "s_631");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_633");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 105);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What Imperial crackdown?
	if (response == "s_665")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "smack_self");

			//-- NPC: Boy, where have YOU been? The Empire's tightening the leash. 
			string_id message = new string_id (c_stringFile, "s_667");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_669");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 114);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch105 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I mean, the Empire's number one problem is that prison, but their radar is up, if you know what I mean. They're looking for trouble across the board. So we're going  to watch our step. We're going to watch everybody ELSE'S steps, too.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK.
	if (response == "s_633")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "sigh_deeply");

			//-- NPC: Listen. There's a heavy mining freighter passing through Endor space.
			string_id message = new string_id (c_stringFile, "s_635");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's it carrying?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_637");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 106);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch106 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Listen. There's a heavy mining freighter passing through Endor space.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's it carrying?
	if (response == "s_637")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Supplies, a few passengers, nothing special.
			string_id message = new string_id (c_stringFile, "s_639");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK...
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_641");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch107 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Supplies, a few passengers, nothing special.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK...
	if (response == "s_641")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: That's what they SAY they're carrying. But I'm not so sure. I'd like you to take a closer look. 
			string_id message = new string_id (c_stringFile, "s_643");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: An inspect mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_645");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 108);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch108 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's what they SAY they're carrying. But I'm not so sure. I'd like you to take a closer look. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: An inspect mission?
	if (response == "s_645")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You got it. So check it out. And I don't mean one quick pass, either. Take a GOOD look. Because if they're carrying any contraband, we better find it before the Imperials do. Otherwise, the Empire might think we're pirate-friendly. And that would be a problem.
			string_id message = new string_id (c_stringFile, "s_647");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Any idea what I should be looking for?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_649");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch109 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You got it. So check it out. And I don't mean one quick pass, either. Take a GOOD look. Because if they're carrying any contraband, we better find it before the Imperials do. Otherwise, the Empire might think we're pirate-friendly. And that would be a problem.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Any idea what I should be looking for?
	if (response == "s_649")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Spice? Sliced weapons? Who knows. Anything's possible. And I mean ANYTHING. So keep your eyes open and your blasters primed.
			string_id message = new string_id (c_stringFile, "s_651");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I always do.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_653");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 110);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch110 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Spice? Sliced weapons? Who knows. Anything's possible. And I mean ANYTHING. So keep your eyes open and your blasters primed.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I always do.
	if (response == "s_653")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: And listen. Don't be too gung-ho, okay? If you find some contraband, you know, tell me first.
			string_id message = new string_id (c_stringFile, "s_655");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Looking for a taste?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_657");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 111);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch111 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And listen. Don't be too gung-ho, okay? If you find some contraband, you know, tell me first.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looking for a taste?
	if (response == "s_657")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Well...
			string_id message = new string_id (c_stringFile, "s_659");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't think I can help you there.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_661");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch112 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't think I can help you there.
	if (response == "s_661")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			naboo_privateer_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: Go on. Your ship is waiting.
			string_id message = new string_id (c_stringFile, "s_663");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch114 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Boy, where have YOU been? The Empire's tightening the leash. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why?
	if (response == "s_669")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: Beats me! Something about trouble with the prison. And word is that the Vortex is gunning for the Inquisitors.
			string_id message = new string_id (c_stringFile, "s_671");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Vortex? Inquisitors?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_673");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 115);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch115 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Beats me! Something about trouble with the prison. And word is that the Vortex is gunning for the Inquisitors.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Vortex? Inquisitors?
	if (response == "s_673")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "snap_finger1");

			//-- NPC: Hey! You want to know more, ask somebody who cares. All I know is, They want everything in this system to be ship-shape. No illegal activity of any kind. And of course we are going to comply.
			string_id message = new string_id (c_stringFile, "s_675");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Because we love the Empire so much.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_677");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 116);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch116 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hey! You want to know more, ask somebody who cares. All I know is, They want everything in this system to be ship-shape. No illegal activity of any kind. And of course we are going to comply.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Because we love the Empire so much.
	if (response == "s_677")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Something like that.
			string_id message = new string_id (c_stringFile, "s_679");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So do you have a mission for me?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_681");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 117);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch117 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Something like that.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So do you have a mission for me?
	if (response == "s_681")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Always. There's a heavy mining freighter passing through Endor space.
			string_id message = new string_id (c_stringFile, "s_683");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: This is another escort mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_685");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 118);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch118 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Always. There's a heavy mining freighter passing through Endor space.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: This is another escort mission?
	if (response == "s_685")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Not exactly. Since the Black Suns are willing to fight RSF pilots, they must have something to hide. So we're going to start taking a closer look at all activity in this system.
			string_id message = new string_id (c_stringFile, "s_687");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So I should inspect the ship.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_689");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 119);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch119 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not exactly. Since the Black Suns are willing to fight RSF pilots, they must have something to hide. So we're going to start taking a closer look at all activity in this system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So I should inspect the ship.
	if (response == "s_689")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You got it. Go check it out. And I don't mean one quick pass, either. Take a GOOD look. Because if they're carrying any contraband, we better find it before the Imperials do. Otherwise, the Empire might think we're pirate-friendly. And that would be a problem.
			string_id message = new string_id (c_stringFile, "s_691");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Any idea what I should be looking for?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_693");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 120);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch120 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You got it. Go check it out. And I don't mean one quick pass, either. Take a GOOD look. Because if they're carrying any contraband, we better find it before the Imperials do. Otherwise, the Empire might think we're pirate-friendly. And that would be a problem.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Any idea what I should be looking for?
	if (response == "s_693")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Spice? Sliced weapons? Who knows. Anything's possible. And I mean ANYTHING. So keep your eyes open and your blasters primed.
			string_id message = new string_id (c_stringFile, "s_695");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I always do.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_697");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 121);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch121 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Spice? Sliced weapons? Who knows. Anything's possible. And I mean ANYTHING. So keep your eyes open and your blasters primed.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I always do.
	if (response == "s_697")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: And listen. Don't be too gung-ho, okay? If you find some contraband, you know, tell me first.
			string_id message = new string_id (c_stringFile, "s_699");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Looking for a taste?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_701");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 122);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch122 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And listen. Don't be too gung-ho, okay? If you find some contraband, you know, tell me first.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looking for a taste?
	if (response == "s_701")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Well...
			string_id message = new string_id (c_stringFile, "s_703");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't think I can help you there.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_705");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 123);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch123 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't think I can help you there.
	if (response == "s_705")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			naboo_privateer_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: Go on. Your ship is waiting.
			string_id message = new string_id (c_stringFile, "s_707");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch125 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Always. There's a heavy mining freighter passing through Endor space.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: This is another escort mission?
	if (response == "s_713")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Not exactly. Since the Black Suns are willing to fight RSF pilots, they must have something to hide. So we're going to start taking a closer look at all activity in this system.
			string_id message = new string_id (c_stringFile, "s_715");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So I should inspect the ship.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_717");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 126);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch126 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not exactly. Since the Black Suns are willing to fight RSF pilots, they must have something to hide. So we're going to start taking a closer look at all activity in this system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So I should inspect the ship.
	if (response == "s_717")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You got it. So check it out. And I don't mean one quick pass, either. Take a GOOD look. Because if they're carrying any contraband, we better find it before the Imperials do. Otherwise, the Empire might think we're pirate-friendly. And that would be a problem.
			string_id message = new string_id (c_stringFile, "s_719");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Any idea what I should be looking for?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_721");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch127 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You got it. So check it out. And I don't mean one quick pass, either. Take a GOOD look. Because if they're carrying any contraband, we better find it before the Imperials do. Otherwise, the Empire might think we're pirate-friendly. And that would be a problem.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Any idea what I should be looking for?
	if (response == "s_721")
	{
		//-- [NOTE] note to myself: come back to see if i can work in some of that personality material (she's looking out for herself, etc)
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Spice? Sliced weapons? Who knows. Anything's possible. And I mean ANYTHING. So keep your eyes open and your blasters primed.
			string_id message = new string_id (c_stringFile, "s_723");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I always do.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_725");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 128);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch128 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] note to myself: come back to see if i can work in some of that personality material (she's looking out for herself, etc)
	//-- NPC: Spice? Sliced weapons? Who knows. Anything's possible. And I mean ANYTHING. So keep your eyes open and your blasters primed.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I always do.
	if (response == "s_725")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: And listen. Don't be too gung-ho, okay? If you find some contraband, you know, tell me first.
			string_id message = new string_id (c_stringFile, "s_727");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Looking for a taste?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_729");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 129);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch129 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And listen. Don't be too gung-ho, okay? If you find some contraband, you know, tell me first.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looking for a taste?
	if (response == "s_729")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well...
			string_id message = new string_id (c_stringFile, "s_731");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't think I can help you there.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_733");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch130 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't think I can help you there.
	if (response == "s_733")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: Go on. Your ship is waiting.
			string_id message = new string_id (c_stringFile, "s_735");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch132 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] Now go do Mission One.
	//-- NPC: So. How was life with Commander Dulios?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Action packed.
	if (response == "s_739")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Well, you'll get more of the same here, if you're lucky. Are you a good pilot?
			string_id message = new string_id (c_stringFile, "s_741");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You bet.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You tell me.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_743");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_783");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 133);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ridiculous.
	if (response == "s_819")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: He didn't dazzle you with his charm?
			string_id message = new string_id (c_stringFile, "s_821");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not exactly. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_823");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 153);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch133 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, you'll get more of the same here, if you're lucky. Are you a good pilot?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You bet.
	if (response == "s_743")
	{
		doAnimationAction (player, "pose_proudly");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Oo. I can't wait to see you in action.
			string_id message = new string_id (c_stringFile, "s_745");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Just get me up there.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_747");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 134);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You tell me.
	if (response == "s_783")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Mm!  Good answer, %TU.I should warn you, though. I don't like telling people what I'm thinking. 
			string_id message = new string_id (c_stringFile, "s_785");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Maybe you'll make an exception for me.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_787");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 144);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch134 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oo. I can't wait to see you in action.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Just get me up there.
	if (response == "s_747")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slit_throat");

			//-- NPC: I've met pilots like you before. Raring to go. It's pilots like you that die first.
			string_id message = new string_id (c_stringFile, "s_749");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not THIS pilot.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_751");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 135);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch135 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've met pilots like you before. Raring to go. It's pilots like you that die first.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not THIS pilot.
	if (response == "s_751")
	{
		doAnimationAction (player, "point_to_self");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Mm. We'll see. Let's get you going on a new mission.
			string_id message = new string_id (c_stringFile, "s_753");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Absolutely.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_755");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 136);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch136 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Mm. We'll see. Let's get you going on a new mission.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Absolutely.
	if (response == "s_755")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Royal Security Forces are using a deep space scan vessel in Endor space. 
			string_id message = new string_id (c_stringFile, "s_757");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why? For what reason?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_759");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 137);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch137 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Royal Security Forces are using a deep space scan vessel in Endor space. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why? For what reason?
	if (response == "s_759")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: The head honchos like to know EVERYTHING happening in this system. You must have figured  that out by now.
			string_id message = new string_id (c_stringFile, "s_761");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They're paranoid, I know that.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_763");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 138);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch138 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The head honchos like to know EVERYTHING happening in this system. You must have figured  that out by now.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They're paranoid, I know that.
	if (response == "s_763")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You would be, too, if half the galaxy was gunning for you. And besides, the Imperials are awfully active down here. 
			string_id message = new string_id (c_stringFile, "s_765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've noticed.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_767");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 139);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch139 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You would be, too, if half the galaxy was gunning for you. And besides, the Imperials are awfully active down here. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've noticed.
	if (response == "s_767")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Then maybe you've noticed that the Imperials aren't exactly tolerant of troublemakers. They'll stay out of our hair, as long as we keep running a tight ship.
			string_id message = new string_id (c_stringFile, "s_769");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see what you're saying.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_771");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 140);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch140 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then maybe you've noticed that the Imperials aren't exactly tolerant of troublemakers. They'll stay out of our hair, as long as we keep running a tight ship.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see what you're saying.
	if (response == "s_771")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: So apparently there are some Black Suns in the area, and they'd prefer to keep their activities private. So they're launching an attack against our vessel. 
			string_id message = new string_id (c_stringFile, "s_773");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Any idea what the Black Suns are up to?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_775");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 141);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch141 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So apparently there are some Black Suns in the area, and they'd prefer to keep their activities private. So they're launching an attack against our vessel. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Any idea what the Black Suns are up to?
	if (response == "s_775")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't know, and right now, I don't care. The only thing that matters right now is that deep space scan. It's an expensive piece of equipment. We'd hate to lose it.
			string_id message = new string_id (c_stringFile, "s_777");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You won't. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_779");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 142);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch142 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know, and right now, I don't care. The only thing that matters right now is that deep space scan. It's an expensive piece of equipment. We'd hate to lose it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You won't. 
	if (response == "s_779")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			naboo_privateer_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Go get 'em, tiger.
			string_id message = new string_id (c_stringFile, "s_781");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch144 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Mm!  Good answer, %TU.I should warn you, though. I don't like telling people what I'm thinking. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe you'll make an exception for me.
	if (response == "s_787")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "snap_finger1");

			//-- NPC: Don't hold your breath. We're wasting time here. I have a mission for you.
			string_id message = new string_id (c_stringFile, "s_789");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_791");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 145);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch145 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't hold your breath. We're wasting time here. I have a mission for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready.
	if (response == "s_791")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Royal Security Forces are using a deep space scan vessel in Endor space. 
			string_id message = new string_id (c_stringFile, "s_793");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why? For what reason?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_795");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 146);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch146 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Royal Security Forces are using a deep space scan vessel in Endor space. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why? For what reason?
	if (response == "s_795")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: The head honchos like to know EVERYTHING that happens in this system. You must have figured  that out by now.
			string_id message = new string_id (c_stringFile, "s_797");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They're paranoid, I know that.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_799");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 147);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch147 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The head honchos like to know EVERYTHING that happens in this system. You must have figured  that out by now.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They're paranoid, I know that.
	if (response == "s_799")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You would be, too, if half the galaxy was gunning for you. And besides, the Imperials are awfully active down here. 
			string_id message = new string_id (c_stringFile, "s_801");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I've noticed.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_803");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 148);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch148 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You would be, too, if half the galaxy was gunning for you. And besides, the Imperials are awfully active down here. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've noticed.
	if (response == "s_803")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: Then maybe you've noticed that the Imperials aren't exactly tolerant of troublemakers. They'll stay out of our hair, as long as we keep running a tight ship.
			string_id message = new string_id (c_stringFile, "s_805");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I see what you're saying.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_807");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 149);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch149 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then maybe you've noticed that the Imperials aren't exactly tolerant of troublemakers. They'll stay out of our hair, as long as we keep running a tight ship.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I see what you're saying.
	if (response == "s_807")
	{
		doAnimationAction (player, "rub_chin_thoughtful");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: So apparently there are some Black Suns in the area, and they'd prefer to keep their activities private. So they're launching an attack against our vessel. 
			string_id message = new string_id (c_stringFile, "s_809");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Any idea what the Black Suns are up to?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_811");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 150);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch150 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So apparently there are some Black Suns in the area, and they'd prefer to keep their activities private. So they're launching an attack against our vessel. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Any idea what the Black Suns are up to?
	if (response == "s_811")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: I don't know, and right now, I don't care. The only thing that matters right now is that deep space scan. It's an expensive piece of equipment. We'd hate to lose it.
			string_id message = new string_id (c_stringFile, "s_813");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You won't. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_815");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 151);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch151 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know, and right now, I don't care. The only thing that matters right now is that deep space scan. It's an expensive piece of equipment. We'd hate to lose it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You won't. 
	if (response == "s_815")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			naboo_privateer_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Go get 'em, tiger.
			string_id message = new string_id (c_stringFile, "s_817");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch153 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: He didn't dazzle you with his charm?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not exactly. 
	if (response == "s_823")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "hair_flip");

			//-- NPC: Aw, come on. That guy's harmless. I like his negative attitude. It cheers me up.
			string_id message = new string_id (c_stringFile, "s_825");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: He'd like to start a one-man war against the Gungans.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_827");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 154);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch154 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Aw, come on. That guy's harmless. I like his negative attitude. It cheers me up.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: He'd like to start a one-man war against the Gungans.
	if (response == "s_827")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "yawn");

			//-- NPC: What do you expect? He's a lifer here on Naboo. It's all he knows. Maybe you're more enlightened.
			string_id message = new string_id (c_stringFile, "s_829");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm ready for my next mission.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_831");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 155);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch155 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What do you expect? He's a lifer here on Naboo. It's all he knows. Maybe you're more enlightened.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready for my next mission.
	if (response == "s_831")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: So I see. This one's an escort job.
			string_id message = new string_id (c_stringFile, "s_833");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What am I escorting?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_835");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 156);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch156 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So I see. This one's an escort job.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What am I escorting?
	if (response == "s_835")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: A deep space scan vessel. It's an RSF ship. We're using it to investigate Endor space. 
			string_id message = new string_id (c_stringFile, "s_837");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds easy enough.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_839");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 157);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch157 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A deep space scan vessel. It's an RSF ship. We're using it to investigate Endor space. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds easy enough.
	if (response == "s_839")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Well, apparently there are some Black Suns in the area, and they'd prefer to keep their activities private. So they're launching an attack against our vessel. 
			string_id message = new string_id (c_stringFile, "s_841");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Any idea what the Black Suns are up to?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_843");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 158);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch158 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, apparently there are some Black Suns in the area, and they'd prefer to keep their activities private. So they're launching an attack against our vessel. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Any idea what the Black Suns are up to?
	if (response == "s_843")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I don't know, and right now, I don't care. The only thing that matters right now is that deep space scan. It's an expensive piece of equipment. We'd hate to lose it.
			string_id message = new string_id (c_stringFile, "s_845");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You won't. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_847");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 159);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch159 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know, and right now, I don't care. The only thing that matters right now is that deep space scan. It's an expensive piece of equipment. We'd hate to lose it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You won't. 
	if (response == "s_847")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			naboo_privateer_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Go get 'em, tiger.
			string_id message = new string_id (c_stringFile, "s_849");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch161 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're ready to learn about heavy starship components.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good to me.
	if (response == "s_445")
	{
		naboo_privateer_tier4_action_buyWeaponsSkill04 (player, npc);

		//-- [NOTE] Now go do Mission Four
		if (naboo_privateer_tier4_condition_hasFourSkills (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: I think the Black Suns have declared war on the RSF.
			string_id message = new string_id (c_stringFile, "s_451");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I think you're right. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Good. Because I want to go after them.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_453");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_473");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission Three.
		if (naboo_privateer_tier4_condition_hasThreeSkills (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: Those Black Suns didn't like the fact you found their secret cargo.
			string_id message = new string_id (c_stringFile, "s_489");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who cares what they think?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: They're lowlifes. 
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I didn't like the fact they tried to kill me.
			boolean hasResponse2 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_491");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_539");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_587");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission Two.
		if (naboo_privateer_tier4_condition_hasTwoSkills (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: You did a nice job protecting that scan vessel. We took a look at the tapes.
			string_id message = new string_id (c_stringFile, "s_623");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What did you find?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have a mission for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_625");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_709");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission One.
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: So. How was life with Commander Dulios?
			string_id message = new string_id (c_stringFile, "s_737");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Action packed.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Ridiculous.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_739");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_819");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 132);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch162 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're ready to learn about expert techniques.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good to me.
	if (response == "s_447")
	{
		naboo_privateer_tier4_action_buyProceduresSkill04 (player, npc);

		//-- [NOTE] Now go do Mission Four
		if (naboo_privateer_tier4_condition_hasFourSkills (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: I think the Black Suns have declared war on the RSF.
			string_id message = new string_id (c_stringFile, "s_451");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I think you're right. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Good. Because I want to go after them.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_453");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_473");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission Three.
		if (naboo_privateer_tier4_condition_hasThreeSkills (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: Those Black Suns didn't like the fact you found their secret cargo.
			string_id message = new string_id (c_stringFile, "s_489");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who cares what they think?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: They're lowlifes. 
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I didn't like the fact they tried to kill me.
			boolean hasResponse2 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_491");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_539");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_587");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission Two.
		if (naboo_privateer_tier4_condition_hasTwoSkills (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: You did a nice job protecting that scan vessel. We took a look at the tapes.
			string_id message = new string_id (c_stringFile, "s_623");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What did you find?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have a mission for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_625");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_709");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission One.
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: So. How was life with Commander Dulios?
			string_id message = new string_id (c_stringFile, "s_737");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Action packed.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Ridiculous.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_739");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_819");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 132);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch163 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It looks like you're ready to learn about droid intelligence theory.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good to me.
	if (response == "s_449")
	{
		naboo_privateer_tier4_action_buyDroidSkill04 (player, npc);

		//-- [NOTE] Now go do Mission Four
		if (naboo_privateer_tier4_condition_hasFourSkills (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: I think the Black Suns have declared war on the RSF.
			string_id message = new string_id (c_stringFile, "s_451");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I think you're right. 
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Good. Because I want to go after them.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_453");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_473");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission Three.
		if (naboo_privateer_tier4_condition_hasThreeSkills (player, npc))
		{
			doAnimationAction (npc, "laugh_titter");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: Those Black Suns didn't like the fact you found their secret cargo.
			string_id message = new string_id (c_stringFile, "s_489");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who cares what they think?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: They're lowlifes. 
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I didn't like the fact they tried to kill me.
			boolean hasResponse2 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_491");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_539");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_587");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission Two.
		if (naboo_privateer_tier4_condition_hasTwoSkills (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: You did a nice job protecting that scan vessel. We took a look at the tapes.
			string_id message = new string_id (c_stringFile, "s_623");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What did you find?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Do you have a mission for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_625");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_709");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] Now go do Mission One.
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			naboo_privateer_tier4_action_setReadyForMission (player, npc);

			//-- NPC: So. How was life with Commander Dulios?
			string_id message = new string_id (c_stringFile, "s_737");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Action packed.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Ridiculous.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_739");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_819");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 132);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch164 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Tsk. You're late. That's not a good way to start your tour with me, pilot. Are you ready to begin? I have a briefing prepped for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course.
	if (response == "s_52917b0d")
	{
		naboo_privateer_tier4_action_setMetMe (player, npc);

		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Now listen up and pay attention. I hate repeating myself. I need a pilot to escort a Naboo mining transport through Endor space. I'll warn you, Borvo the Hutt's men will probably be interested in that transport.
			string_id message = new string_id (c_stringFile, "s_5603b2a1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_416");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 170);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch165 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have not completed tier-4, so do one of these duty missions until you xp your way to a new skill.
	//-- NPC: I like it when my pilots stay busy. Why don't you volunteer for something? Or I can give you the briefing again, if you like.

	//-- [RESPONSE NOTE] Destroy Duty
	//-- PLAYER: I can handle those Black Suns.
	if (response == "s_62c8be58")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionOneDestroy (player, npc);

			//-- NPC: Oh, you can, can you? I guess we'll see. Bye bye.
			string_id message = new string_id (c_stringFile, "s_221582e4");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Escort Duty
	//-- PLAYER: I'll escort that mining transport.
	if (response == "s_619658af")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionTwoEscort (player, npc);

			//-- NPC: Lucky miners! Have fun.
			string_id message = new string_id (c_stringFile, "s_9750cd6f");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Recovery Duty
	//-- PLAYER: What's wrong with that Ay'Nat ship?
	if (response == "s_7869de8a")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionThreeRecovery (player, npc);

			//-- NPC: Right now, the only problem is that it's not here, in my custody. But you could solve that problem. Get going.
			string_id message = new string_id (c_stringFile, "s_552b15d");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Rescue Duty
	//-- PLAYER: I want to help that mining craft.
	if (response == "s_413ac49a")
	{
		//-- [NOTE] Get to work
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionFourRescue (player, npc);

			//-- NPC: Help is the one thing they need. Just try not to get killed yourself, okay? I hate breaking in new pilots. 
			string_id message = new string_id (c_stringFile, "s_ee2b4c16");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Please, give me the briefing again. 
	if (response == "s_70d61202")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Now listen up and pay attention. I hate repeating myself. I need a pilot to escort a Naboo mining transport through Endor space. I'll warn you, Borvo the Hutt's men will probably be interested in that transport.
			string_id message = new string_id (c_stringFile, "s_5603b2a1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_416");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 170);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch170 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Now listen up and pay attention. I hate repeating myself. I need a pilot to escort a Naboo mining transport through Endor space. I'll warn you, Borvo the Hutt's men will probably be interested in that transport.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK.
	if (response == "s_416")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you're feeling like a hero today, you could rescue a Naboo mining craft from Ay'Nat pirates.
			string_id message = new string_id (c_stringFile, "s_1993702c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Hm, maybe.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aba1f75e");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 171);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch171 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If you're feeling like a hero today, you could rescue a Naboo mining craft from Ay'Nat pirates.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hm, maybe.
	if (response == "s_aba1f75e")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: If you have a thing against the Ay'Nat, you could help me out by capturing one of their private vessels.
			string_id message = new string_id (c_stringFile, "s_8ca0b65b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Anything else?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_362a48e0");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 172);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch172 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If you have a thing against the Ay'Nat, you could help me out by capturing one of their private vessels.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Anything else?
	if (response == "s_362a48e0")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: I've saved the best for last. I need a pilot willing to fly sorties against Black Sun pirates. Dangerous, but fun. Especially for a big strong pilot like you.
			string_id message = new string_id (c_stringFile, "s_1cf9a0f6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm flattered.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c782ced7");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 173);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch173 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've saved the best for last. I need a pilot willing to fly sorties against Black Sun pirates. Dangerous, but fun. Especially for a big strong pilot like you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm flattered.
	if (response == "s_c782ced7")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Don't be. I'm being sarcastic.
			string_id message = new string_id (c_stringFile, "s_9192ecb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I can handle those Black Suns.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'll escort that mining transport.
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: What's wrong with that Ay'Nat ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want to help that mining craft.
			boolean hasResponse3 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Please, give me the briefing again. 
			boolean hasResponse4 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_62c8be58");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_619658af");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7869de8a");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_413ac49a");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70d61202");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 165);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch174 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't be. I'm being sarcastic.

	//-- [RESPONSE NOTE] Destroy Duty
	//-- PLAYER: I can handle those Black Suns.
	if (response == "s_62c8be58")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionOneDestroy (player, npc);

			//-- NPC: Oh, you can, can you? I guess we'll see. Bye bye.
			string_id message = new string_id (c_stringFile, "s_221582e4");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Escort Duty
	//-- PLAYER: I'll escort that mining transport.
	if (response == "s_619658af")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionTwoEscort (player, npc);

			//-- NPC: Lucky miners! Have fun.
			string_id message = new string_id (c_stringFile, "s_9750cd6f");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Recovery Duty
	//-- PLAYER: What's wrong with that Ay'Nat ship?
	if (response == "s_7869de8a")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionThreeRecovery (player, npc);

			//-- NPC: Right now, the only problem is that it's not here, in my custody. But you could solve that problem. Get going.
			string_id message = new string_id (c_stringFile, "s_552b15d");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Rescue Duty
	//-- PLAYER: I want to help that mining craft.
	if (response == "s_413ac49a")
	{
		//-- [NOTE] Get to work
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionFourRescue (player, npc);

			//-- NPC: Help is the one thing they need. Just try not to get killed yourself, okay? I hate breaking in new pilots. 
			string_id message = new string_id (c_stringFile, "s_ee2b4c16");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Please, give me the briefing again. 
	if (response == "s_70d61202")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Now listen up and pay attention. I hate repeating myself. I need a pilot to escort a Naboo mining transport through Endor space. I'll warn you, Borvo the Hutt's men will probably be interested in that transport.
			string_id message = new string_id (c_stringFile, "s_5603b2a1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_416");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 170);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch175 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, there you are. Good news, %NU. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's that?
	if (response == "s_b2e340f6")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I have just been informed that Grand Admiral Nial Declann has ordered that you be transferred to his squadron, effective immediately. This is a very rare honor! RSF soldiers are rarely drafted into Imperial service so quickly. And they NEVER report directly to a Grand Admiral.
			string_id message = new string_id (c_stringFile, "s_92fa7352");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who is Grand Admiral Nial Declann?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am going to become an Imperial?
			boolean hasResponse1 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7177c3f2");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57f232d6");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 176);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch176 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I have just been informed that Grand Admiral Nial Declann has ordered that you be transferred to his squadron, effective immediately. This is a very rare honor! RSF soldiers are rarely drafted into Imperial service so quickly. And they NEVER report directly to a Grand Admiral.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who is Grand Admiral Nial Declann?
	if (response == "s_7177c3f2")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Well, let me put it this way. In the Imperial Navy, power is held by only a few men. And Grand Admiral Nial Declann is one of those men. He makes things happen. I've been told that he is a brilliant and inspirational leader. I think you're going to learn a lot.
			string_id message = new string_id (c_stringFile, "s_cf0a01fd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I report?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50d4081c");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 177);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am going to become an Imperial?
	if (response == "s_57f232d6")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Well, not officially. But  this is a big opportunity for you. You will be working under them until they no longer are in need of your services. You will retain all of your RSF rankings and privileges. According to these orders, you are to report to Grand Admiral Declann at the Theed Palace immediately.
			string_id message = new string_id (c_stringFile, "s_5d72fdfa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Does it say what they want from me?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9fe8c7e7");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 180);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch177 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, let me put it this way. In the Imperial Navy, power is held by only a few men. And Grand Admiral Nial Declann is one of those men. He makes things happen. I've been told that he is a brilliant and inspirational leader. I think you're going to learn a lot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I report?
	if (response == "s_50d4081c")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: According to this, you are to report directly to the Grand Admiral at the Theed Palace. Now you won't actually JOIN the Imperial Navy - but you'll be joining them for a highly-classified mission. Sounds exciting, doesn't it.
			string_id message = new string_id (c_stringFile, "s_cd140a4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_614d7ac4");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 178);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch178 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: According to this, you are to report directly to the Grand Admiral at the Theed Palace. Now you won't actually JOIN the Imperial Navy - but you'll be joining them for a highly-classified mission. Sounds exciting, doesn't it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the mission?
	if (response == "s_614d7ac4")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stretch");

			//-- NPC: I wish I knew! That's highly classified information. The Admiral will explain everything to you. Pack your bags! The Admiral is waiting. 
			string_id message = new string_id (c_stringFile, "s_492a501d");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch180 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, not officially. But  this is a big opportunity for you. You will be working under them until they no longer are in need of your services. You will retain all of your RSF rankings and privileges. According to these orders, you are to report to Grand Admiral Declann at the Theed Palace immediately.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Does it say what they want from me?
	if (response == "s_9fe8c7e7")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: That's classified, my dear. You can ask the Grand Admiral yourself. Now let me give you some free advice. There are a lot of things that a pilot like you could learn from the Empire. Pack your things; you ship out immediately. Don't make the Admiral wait!
			string_id message = new string_id (c_stringFile, "s_75f5450f");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_tier4_handleBranch182 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I see you made it back from serving with the Empire in one piece. I am very glad of that. I have some jobs for you if you are interested.

	//-- [RESPONSE NOTE] Destroy Duty
	//-- PLAYER: I can handle those Black Suns.
	if (response == "s_62c8be58")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionOneDestroy (player, npc);

			//-- NPC: Oh, you can, can you? I guess we'll see. Bye bye.
			string_id message = new string_id (c_stringFile, "s_221582e4");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Escort Duty
	//-- PLAYER: I'll escort that mining transport.
	if (response == "s_619658af")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionTwoEscort (player, npc);

			//-- NPC: Lucky miners! Have fun.
			string_id message = new string_id (c_stringFile, "s_9750cd6f");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Recovery Duty
	//-- PLAYER: What's wrong with that Ay'Nat ship?
	if (response == "s_7869de8a")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionThreeRecovery (player, npc);

			//-- NPC: Right now, the only problem is that it's not here, in my custody. But you could solve that problem. Get going.
			string_id message = new string_id (c_stringFile, "s_552b15d");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Rescue Duty
	//-- PLAYER: I want to help that mining craft.
	if (response == "s_413ac49a")
	{
		//-- [NOTE] Get to work
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			naboo_privateer_tier4_action_grantDutyMissionFourRescue (player, npc);

			//-- NPC: Help is the one thing they need. Just try not to get killed yourself, okay? I hate breaking in new pilots. 
			string_id message = new string_id (c_stringFile, "s_ee2b4c16");
			utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Please, give me the briefing again. 
	if (response == "s_70d61202")
	{
		//-- [NOTE] 
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Now listen up and pay attention. I hate repeating myself. I need a pilot to escort a Naboo mining transport through Endor space. I'll warn you, Borvo the Hutt's men will probably be interested in that transport.
			string_id message = new string_id (c_stringFile, "s_5603b2a1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK.
			boolean hasResponse0 = false;
			if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_416");

				utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 170);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.naboo_privateer_tier4");

	setCondition (self, CONDITION_CONVERSABLE);
	setCondition( self, CONDITION_SPACE_INTERESTING );
	setInvulnerable(self, true);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setCondition( self, CONDITION_SPACE_INTERESTING );
	setInvulnerable(self, true);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );
	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.naboo_privateer_tier4");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (!naboo_privateer_tier4_condition_isPilot (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: I don't think I've seen you before. Maybe you took a wrong turn.
		string_id message = new string_id (c_stringFile, "s_aa1834ec");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You're the wrong faction.
	if (naboo_privateer_tier4_condition_isWrongFaction (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: I don't think I've seen you before. Maybe you took a wrong turn somewhere.
		string_id message = new string_id (c_stringFile, "s_4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm looking for Rebels.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm looking for Imperials.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c09ba3fa");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_251ee0bd");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 2);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_tier4_condition_onMyTrack (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: I don't think I've seen you before, but your name is familiar to me. What brings you here?
		string_id message = new string_id (c_stringFile, "s_d6473733");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm looking for a mission. Do you have any?
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 5);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You're not through tier-3 yet
	if (!naboo_privateer_tier4_condition_hasCompletedTier3 (player, npc))
	{
		doAnimationAction (npc, "check_wrist_device");

		//-- NPC: Tsk. Better get back to Commander Dulios. He hates it when people don't finish what they started.
		string_id message = new string_id (c_stringFile, "s_9686f892");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm done with him!
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What are you talking about?
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a404eb10");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2313ac9e");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 8);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You're busy.  Already doing a mission.  Hup hup, go do it.
	if (naboo_privateer_tier4_condition_isOnMission (player, npc))
	{
		doAnimationAction (npc, "stamp_feet");

		//-- NPC: I don't have any patience for lightweights. Do you know how to do your job, or don't you?
		string_id message = new string_id (c_stringFile, "s_eaf3e39a");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Of course I do!
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Hey, I'm working on it.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_eb4e939");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_23e8aa6d");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 11);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You failed mission 1, do it over.
	if (naboo_privateer_tier4_condition_faliedMissionOne (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		//-- NPC: I thought you said you were a good pilot.
		string_id message = new string_id (c_stringFile, "s_c93f357b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I am. I'm just having a bad day.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: This job's not as easy as it looks, you know.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5e1ad1a2");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ff47ef5a");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 14);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You failed mission 2, do it over.
	if (naboo_privateer_tier4_condition_failedMissionTwo (player, npc))
	{
		//-- NPC: Well THAT didn't go as planned.
		string_id message = new string_id (c_stringFile, "s_b59b9d3e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No kidding!
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I can do it.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_eb72f32e");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c17d2691");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 17);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You failed mission 3, do it over.
	if (naboo_privateer_tier4_condition_failedMissionThree (player, npc))
	{
		//-- NPC: Our relationship with the Imperials is an important one.
		string_id message = new string_id (c_stringFile, "s_95fbf5d7");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Things don't always go as planned.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I don't think one setback is going to destroy our alliance.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8bbbb2ce");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c5f46951");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 22);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You failed mission 4, do it over.
	if (naboo_privateer_tier4_condition_failedMissionFour (player, npc))
	{
		doAnimationAction (npc, "shrug_shoulders");

		//-- NPC: Listen, if you need a break, just say so. I've got other pilots. And you've been through a lot.
		string_id message = new string_id (c_stringFile, "s_8705a395");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No. I'm OK.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Nobody replaces me.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e2dbfb72");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8fe73508");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 27);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have completed mission one and so here is your reward.
	if (naboo_privateer_tier4_condition_isGettingMissionOneReward (player, npc))
	{
		doAnimationAction (npc, "rub_chin_thoughtful");

		naboo_privateer_tier4_action_rewardForMissionOne (player, npc);

		//-- NPC: So. You did it.
		string_id message = new string_id (c_stringFile, "s_8180cd8a");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sure did.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: It was easy.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e0ce54d");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9121b74a");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 32);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have completed mission two and so here is your reward.
	if (naboo_privateer_tier4_condition_isGettingMissionTwoReward (player, npc))
	{
		naboo_privateer_tier4_action_rewardForMissionTwo (player, npc);

		//-- NPC: I have somebody's duty pay.
		string_id message = new string_id (c_stringFile, "s_191d50ad");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: That would be mine.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Aren't you going to thank me?
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_328cdc99");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f1e2aed8");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 39);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have completed mission three and so here is your reward.
	if (naboo_privateer_tier4_condition_isGettingMissionThreeReward (player, npc))
	{
		naboo_privateer_tier4_action_rewardForMissionThree (player, npc);

		//-- NPC: Here's your duty pay. Maybe you can spend some of your money on ship repairs.
		string_id message = new string_id (c_stringFile, "s_6362f5f4");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I need to. Those Suns nearly shredded me.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Are you kidding? I'm going to the cantina.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7977e8e2");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_953c410e");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 44);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have completed mission four and so here is your reward.
	if (naboo_privateer_tier4_condition_isGettingMissionFourReward (player, npc))
	{
		doAnimationAction (npc, "beckon");

		naboo_privateer_tier4_action_rewardForMissionFour (player, npc);

		//-- NPC: Look. Here comes my favorite pilot.
		string_id message = new string_id (c_stringFile, "s_60d0f37b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: That's me, baby.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: You're making me blush.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ad713f95");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2e904f6c");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 49);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_tier4_condition_isReadyForMission (player, npc))
	{
		//-- NPC: Hurry up. It's time to get back to work.
		string_id message = new string_id (c_stringFile, "s_67b2bdc1");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Whatever you say.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_39e697de");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 52);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You're ready for a new skill, which would you like?
	if (naboo_privateer_tier4_condition_isReadyForTraining (player, npc))
	{
		doAnimationAction (npc, "check_wrist_device");

		//-- NPC: I'm supposed to give you a skill. Hurry up. Let's get this over with.
		string_id message = new string_id (c_stringFile, "s_d91c04b2");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What skill are you going to give me?
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_438");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 57);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_tier4_condition_hasMetMe (player, npc))
	{
		//-- NPC: Tsk. You're late. That's not a good way to start your tour with me, pilot. Are you ready to begin? I have a briefing prepped for you.
		string_id message = new string_id (c_stringFile, "s_cdffba3e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Of course.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_52917b0d");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 164);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have not completed tier-4, so do one of these duty missions until you xp your way to a new skill.
	if (!naboo_privateer_tier4_condition_hasCompletedTier4 (player, npc))
	{
		//-- NPC: I like it when my pilots stay busy. Why don't you volunteer for something? Or I can give you the briefing again, if you like.
		string_id message = new string_id (c_stringFile, "s_3f76a413");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I can handle those Black Suns.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'll escort that mining transport.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: What's wrong with that Ay'Nat ship?
		boolean hasResponse2 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want to help that mining craft.
		boolean hasResponse3 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: Please, give me the briefing again. 
		boolean hasResponse4 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_62c8be58");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_619658af");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7869de8a");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_413ac49a");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_70d61202");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 165);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_tier4_condition_hasMasterSkill (player, npc))
	{
		doAnimationAction (npc, "beckon");

		//-- NPC: Ah, there you are. Good news, %NU. 
		string_id message = new string_id (c_stringFile, "s_256dbbde");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What's that?
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b2e340f6");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 175);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "naboo_privateer_tier4", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
	{
		//-- NPC: I see you made it back from serving with the Empire in one piece. I am very glad of that. I have some jobs for you if you are interested.
		string_id message = new string_id (c_stringFile, "s_bd35f50b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I can handle those Black Suns.
		boolean hasResponse0 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'll escort that mining transport.
		boolean hasResponse1 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: What's wrong with that Ay'Nat ship?
		boolean hasResponse2 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want to help that mining craft.
		boolean hasResponse3 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: Please, give me the briefing again. 
		boolean hasResponse4 = false;
		if (naboo_privateer_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_62c8be58");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_619658af");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7869de8a");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_413ac49a");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_70d61202");

			utils.setScriptVar (player, "conversation.naboo_privateer_tier4.branchId", 165);

			npcStartConversation (player, npc, "naboo_privateer_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "naboo_privateer_tier4")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

	if (branchId == 2 && naboo_privateer_tier4_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && naboo_privateer_tier4_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && naboo_privateer_tier4_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && naboo_privateer_tier4_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && naboo_privateer_tier4_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && naboo_privateer_tier4_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && naboo_privateer_tier4_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && naboo_privateer_tier4_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && naboo_privateer_tier4_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && naboo_privateer_tier4_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && naboo_privateer_tier4_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && naboo_privateer_tier4_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && naboo_privateer_tier4_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && naboo_privateer_tier4_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && naboo_privateer_tier4_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && naboo_privateer_tier4_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && naboo_privateer_tier4_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && naboo_privateer_tier4_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && naboo_privateer_tier4_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && naboo_privateer_tier4_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && naboo_privateer_tier4_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && naboo_privateer_tier4_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && naboo_privateer_tier4_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 44 && naboo_privateer_tier4_handleBranch44 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && naboo_privateer_tier4_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && naboo_privateer_tier4_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 49 && naboo_privateer_tier4_handleBranch49 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && naboo_privateer_tier4_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 53 && naboo_privateer_tier4_handleBranch53 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && naboo_privateer_tier4_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 55 && naboo_privateer_tier4_handleBranch55 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 56 && naboo_privateer_tier4_handleBranch56 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 57 && naboo_privateer_tier4_handleBranch57 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && naboo_privateer_tier4_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 59 && naboo_privateer_tier4_handleBranch59 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 60 && naboo_privateer_tier4_handleBranch60 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && naboo_privateer_tier4_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && naboo_privateer_tier4_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 63 && naboo_privateer_tier4_handleBranch63 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 65 && naboo_privateer_tier4_handleBranch65 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 66 && naboo_privateer_tier4_handleBranch66 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 67 && naboo_privateer_tier4_handleBranch67 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 69 && naboo_privateer_tier4_handleBranch69 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 70 && naboo_privateer_tier4_handleBranch70 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 71 && naboo_privateer_tier4_handleBranch71 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 72 && naboo_privateer_tier4_handleBranch72 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 73 && naboo_privateer_tier4_handleBranch73 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 74 && naboo_privateer_tier4_handleBranch74 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 76 && naboo_privateer_tier4_handleBranch76 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 77 && naboo_privateer_tier4_handleBranch77 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 78 && naboo_privateer_tier4_handleBranch78 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 79 && naboo_privateer_tier4_handleBranch79 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 80 && naboo_privateer_tier4_handleBranch80 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 82 && naboo_privateer_tier4_handleBranch82 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 83 && naboo_privateer_tier4_handleBranch83 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 84 && naboo_privateer_tier4_handleBranch84 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 85 && naboo_privateer_tier4_handleBranch85 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 86 && naboo_privateer_tier4_handleBranch86 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 88 && naboo_privateer_tier4_handleBranch88 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 89 && naboo_privateer_tier4_handleBranch89 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 90 && naboo_privateer_tier4_handleBranch90 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 91 && naboo_privateer_tier4_handleBranch91 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 92 && naboo_privateer_tier4_handleBranch92 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 94 && naboo_privateer_tier4_handleBranch94 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 95 && naboo_privateer_tier4_handleBranch95 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 96 && naboo_privateer_tier4_handleBranch96 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 97 && naboo_privateer_tier4_handleBranch97 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 98 && naboo_privateer_tier4_handleBranch98 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 99 && naboo_privateer_tier4_handleBranch99 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 100 && naboo_privateer_tier4_handleBranch100 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 101 && naboo_privateer_tier4_handleBranch101 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 103 && naboo_privateer_tier4_handleBranch103 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 104 && naboo_privateer_tier4_handleBranch104 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 105 && naboo_privateer_tier4_handleBranch105 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 106 && naboo_privateer_tier4_handleBranch106 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 107 && naboo_privateer_tier4_handleBranch107 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 108 && naboo_privateer_tier4_handleBranch108 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 109 && naboo_privateer_tier4_handleBranch109 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 110 && naboo_privateer_tier4_handleBranch110 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 111 && naboo_privateer_tier4_handleBranch111 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 112 && naboo_privateer_tier4_handleBranch112 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 114 && naboo_privateer_tier4_handleBranch114 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 115 && naboo_privateer_tier4_handleBranch115 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 116 && naboo_privateer_tier4_handleBranch116 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 117 && naboo_privateer_tier4_handleBranch117 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 118 && naboo_privateer_tier4_handleBranch118 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 119 && naboo_privateer_tier4_handleBranch119 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 120 && naboo_privateer_tier4_handleBranch120 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 121 && naboo_privateer_tier4_handleBranch121 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 122 && naboo_privateer_tier4_handleBranch122 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 123 && naboo_privateer_tier4_handleBranch123 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 125 && naboo_privateer_tier4_handleBranch125 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 126 && naboo_privateer_tier4_handleBranch126 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 127 && naboo_privateer_tier4_handleBranch127 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 128 && naboo_privateer_tier4_handleBranch128 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 129 && naboo_privateer_tier4_handleBranch129 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 130 && naboo_privateer_tier4_handleBranch130 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 132 && naboo_privateer_tier4_handleBranch132 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 133 && naboo_privateer_tier4_handleBranch133 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 134 && naboo_privateer_tier4_handleBranch134 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 135 && naboo_privateer_tier4_handleBranch135 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 136 && naboo_privateer_tier4_handleBranch136 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 137 && naboo_privateer_tier4_handleBranch137 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 138 && naboo_privateer_tier4_handleBranch138 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 139 && naboo_privateer_tier4_handleBranch139 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 140 && naboo_privateer_tier4_handleBranch140 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 141 && naboo_privateer_tier4_handleBranch141 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 142 && naboo_privateer_tier4_handleBranch142 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 144 && naboo_privateer_tier4_handleBranch144 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 145 && naboo_privateer_tier4_handleBranch145 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 146 && naboo_privateer_tier4_handleBranch146 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 147 && naboo_privateer_tier4_handleBranch147 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 148 && naboo_privateer_tier4_handleBranch148 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 149 && naboo_privateer_tier4_handleBranch149 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 150 && naboo_privateer_tier4_handleBranch150 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 151 && naboo_privateer_tier4_handleBranch151 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 153 && naboo_privateer_tier4_handleBranch153 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 154 && naboo_privateer_tier4_handleBranch154 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 155 && naboo_privateer_tier4_handleBranch155 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 156 && naboo_privateer_tier4_handleBranch156 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 157 && naboo_privateer_tier4_handleBranch157 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 158 && naboo_privateer_tier4_handleBranch158 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 159 && naboo_privateer_tier4_handleBranch159 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 161 && naboo_privateer_tier4_handleBranch161 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 162 && naboo_privateer_tier4_handleBranch162 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 163 && naboo_privateer_tier4_handleBranch163 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 164 && naboo_privateer_tier4_handleBranch164 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 165 && naboo_privateer_tier4_handleBranch165 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 170 && naboo_privateer_tier4_handleBranch170 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 171 && naboo_privateer_tier4_handleBranch171 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 172 && naboo_privateer_tier4_handleBranch172 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 173 && naboo_privateer_tier4_handleBranch173 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 174 && naboo_privateer_tier4_handleBranch174 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 175 && naboo_privateer_tier4_handleBranch175 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 176 && naboo_privateer_tier4_handleBranch176 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 177 && naboo_privateer_tier4_handleBranch177 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 178 && naboo_privateer_tier4_handleBranch178 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 180 && naboo_privateer_tier4_handleBranch180 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 182 && naboo_privateer_tier4_handleBranch182 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.naboo_privateer_tier4.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

