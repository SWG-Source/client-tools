// ======================================================================
//
// ep3_etyyy_tuwezz_vol.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.groundquests;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/ep3_etyyy_tuwezz_vol";

// ======================================================================
// Script Conditions
// ======================================================================

boolean ep3_etyyy_tuwezz_vol_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean ep3_etyyy_tuwezz_vol_condition_hasCompletedTuwezzQuests (obj_id player, obj_id npc)
{
	// Returns TRUE of these quests are complete
	return ( groundquests.hasCompletedQuest(player, "ep3_hunt_tuwezz_collect_uller_horns") );
}

// ----------------------------------------------------------------------

boolean ep3_etyyy_tuwezz_vol_condition_killedSpiketop (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive(player, "ep3_hunt_loot_spiketop_killed");
}

// ----------------------------------------------------------------------

boolean ep3_etyyy_tuwezz_vol_condition_finishedHuntingDiseasedUller (obj_id player, obj_id npc)
{
	return ( groundquests.isTaskActive(player, "ep3_hunt_tuwezz_kill_diseased_ullers", "tuwezz_diseasedUllersDone") ||
				groundquests.hasCompletedQuest(player, "ep3_hunt_tuwezz_kill_diseased_ullers") );
}

// ----------------------------------------------------------------------

boolean ep3_etyyy_tuwezz_vol_condition_isHuntingDiseasedUller (obj_id player, obj_id npc)
{
	// Returns TRUE if the quest is active on the player
	return ( groundquests.isTaskActive(player, "ep3_hunt_tuwezz_kill_diseased_ullers", "tuwezz_huntingDiseasedUllers") );
}

// ----------------------------------------------------------------------

boolean ep3_etyyy_tuwezz_vol_condition_isCollectingUllerHorns (obj_id player, obj_id npc)
{
	// Returns TRUE if the quest is active on the player
	return ( groundquests.isTaskActive(player, "ep3_hunt_tuwezz_collect_uller_horns", "tuwezz_collectUllerHorns") );
}

// ----------------------------------------------------------------------

boolean ep3_etyyy_tuwezz_vol_condition_hasToSpeakToTuwezz (obj_id player, obj_id npc)
{
	return ( groundquests.isTaskActive(player, "ep3_hunt_tuwezz_kill_diseased_ullers", "tuwezz_talkToTuwezz") );
}

// ----------------------------------------------------------------------

boolean ep3_etyyy_tuwezz_vol_condition_finishedCollectingUllerHorns (obj_id player, obj_id npc)
{
	return ( groundquests.isTaskActive(player, "ep3_hunt_tuwezz_collect_uller_horns", "tuwezz_elderUllerHorns") );
}

// ----------------------------------------------------------------------

boolean ep3_etyyy_tuwezz_vol_condition_killedSpiketopPlusAll (obj_id player, obj_id npc)
{
	// Previously defeated all other named Etyyy creatures except spiketop.
	return groundquests.hasCompletedQuest(player, "ep3_hunt_loot_brightclaw_killed") &&
			groundquests.hasCompletedQuest(player, "ep3_hunt_loot_silkthrower_killed") &&
			groundquests.hasCompletedQuest(player, "ep3_hunt_loot_stoneleg_killed") &&
			groundquests.hasCompletedQuest(player, "ep3_hunt_loot_paleclaw_killed") &&
			groundquests.hasCompletedQuest(player, "ep3_hunt_loot_greyclimber_killed");
}

// ======================================================================
// Script Actions
// ======================================================================

void ep3_etyyy_tuwezz_vol_action_spiketopReward (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "lootQuest_defeatedSpiketop");
}

// ----------------------------------------------------------------------

void ep3_etyyy_tuwezz_vol_action_doneHuntingDiseasedUller (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tuwezz_diseasedUllersDone");
}

// ----------------------------------------------------------------------

void ep3_etyyy_tuwezz_vol_action_huntDiseasedUller (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tuwezz_talkToTuwezz");
}

// ----------------------------------------------------------------------

void ep3_etyyy_tuwezz_vol_action_collectUllerHorns (obj_id player, obj_id npc)
{
	groundquests.grantQuest(player, "ep3_hunt_tuwezz_collect_uller_horns");
}

// ----------------------------------------------------------------------

void ep3_etyyy_tuwezz_vol_action_speakWithSordaan (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "tuwezz_elderUllerHorns");
	
	groundquests.grantQuest(player, "ep3_hunt_sordaan_seek_sordaan");
	groundquests.sendSignal(player, "sordaan_tuwezzSendsYou");
}

// ----------------------------------------------------------------------

void ep3_etyyy_tuwezz_vol_action_spiketopRewardPlusAll (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "lootQuest_defeatedSpiketop");
	groundquests.grantQuest(player, "ep3_hunt_loot_completed_all");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int ep3_etyyy_tuwezz_vol_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome, hunter. I trust the hunt goes well for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
	if (response == "s_1623")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketopPlusAll (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopRewardPlusAll (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance. It would appear that you've also defeated Brightclaw, Paleclaw, Stoneleg, Silkthrower, and Greyclimber. That's an impressive list of hunted creatures. I should let Sordaan know of this. I think he'll want to recognize such an accomplishment.
			string_id message = new string_id (c_stringFile, "s_1689");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll go speak with Sordaan.
			boolean hasResponse0 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1691");

				utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopReward (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance.
			string_id message = new string_id (c_stringFile, "s_1695");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_etyyy_tuwezz_vol_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: These horns are exactly what I wanted. Well done. I'm very happy with your hunting abilities. Enough so that I think it's time for you to go talk to Sordaan Xris.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay. I'll go speak with him.
	if (response == "s_1627")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_speakWithSordaan (player, npc);

			//-- NPC: You've shown yourself to be a capable hunter. Sordaan will probably want to offer a hunting wager. It's usually something along the lines of a race to see who can hunt and kill more of a given creature, you or his hunter. If you can beat his hunters, you'll not only win some credits, but you'll make him look bad. So be sure and really beat him badly!
			string_id message = new string_id (c_stringFile, "s_1629");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll do that later.
	if (response == "s_1631")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course. Return to me when you're ready.
			string_id message = new string_id (c_stringFile, "s_1633");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
	if (response == "s_1635")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketopPlusAll (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopRewardPlusAll (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance. It would appear that you've also defeated Brightclaw, Paleclaw, Stoneleg, Silkthrower, and Greyclimber. That's an impressive list of hunted creatures. I should let Sordaan know of this. I think he'll want to recognize such an accomplishment.
			string_id message = new string_id (c_stringFile, "s_1689");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll go speak with Sordaan.
			boolean hasResponse0 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1691");

				utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopReward (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance.
			string_id message = new string_id (c_stringFile, "s_1695");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_etyyy_tuwezz_vol_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Unmarred uller horns. 11 of them. And remember, the elder ullers are the ones to target. None of the others will have good enough horns.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You're right. I'm on my way.
	if (response == "s_1639")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			//-- NPC: Thanks!
			string_id message = new string_id (c_stringFile, "s_1641");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
	if (response == "s_1643")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketopPlusAll (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopRewardPlusAll (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance. It would appear that you've also defeated Brightclaw, Paleclaw, Stoneleg, Silkthrower, and Greyclimber. That's an impressive list of hunted creatures. I should let Sordaan know of this. I think he'll want to recognize such an accomplishment.
			string_id message = new string_id (c_stringFile, "s_1689");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll go speak with Sordaan.
			boolean hasResponse0 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1691");

				utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopReward (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance.
			string_id message = new string_id (c_stringFile, "s_1695");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_etyyy_tuwezz_vol_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You did quite well hunting those diseased ullers. Quite well indeed. I can see excellent potential in you as a hunter. I have one other hunting task for you. I want you to gather 11 unmarred uller horns. You'll find these on elder ullers. The younger and more aggressive uller tend to have damaged or undersized horns, but the elders often have very good ones.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let the hunt begin!
	if (response == "s_1647")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_collectUllerHorns (player, npc);

			//-- NPC: The elder ullers are a good bit tougher than the diseased ones. Be ready for that. Return to me when you've collected 11 unmarred uller horns.
			string_id message = new string_id (c_stringFile, "s_1649");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now. I'll try that later.
	if (response == "s_1651")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ah. Okay then.
			string_id message = new string_id (c_stringFile, "s_1653");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
	if (response == "s_1655")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketopPlusAll (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopRewardPlusAll (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance. It would appear that you've also defeated Brightclaw, Paleclaw, Stoneleg, Silkthrower, and Greyclimber. That's an impressive list of hunted creatures. I should let Sordaan know of this. I think he'll want to recognize such an accomplishment.
			string_id message = new string_id (c_stringFile, "s_1689");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll go speak with Sordaan.
			boolean hasResponse0 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1691");

				utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopReward (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance.
			string_id message = new string_id (c_stringFile, "s_1695");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_etyyy_tuwezz_vol_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Go hunt those diseased ullers. Return to me when you're done.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Right. I'm on my way.
	if (response == "s_1659")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good to hear.
			string_id message = new string_id (c_stringFile, "s_1661");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
	if (response == "s_1663")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketopPlusAll (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopRewardPlusAll (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance. It would appear that you've also defeated Brightclaw, Paleclaw, Stoneleg, Silkthrower, and Greyclimber. That's an impressive list of hunted creatures. I should let Sordaan know of this. I think he'll want to recognize such an accomplishment.
			string_id message = new string_id (c_stringFile, "s_1689");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll go speak with Sordaan.
			boolean hasResponse0 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1691");

				utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopReward (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance.
			string_id message = new string_id (c_stringFile, "s_1695");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_etyyy_tuwezz_vol_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ziven sent you to me? Interesting. Well, I suppose he wants to see what kind of hunter you are. So let's find out, shall we?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay, what do you want me to do?
	if (response == "s_1667")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, let's see. Oh, I've got it. We've had a problem lately with some diseased ullers. We're worried that the disease that afflicts them could spread to other ullers or even other creatures. That would not be a good thing. What I need you to do is hunt and kill some of them. Kill 17 diseased ullers. That should help thin their population and reduce the chance of their disease spreading.
			string_id message = new string_id (c_stringFile, "s_1669");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Right, kill diseased ullers. I'm on it.
			boolean hasResponse0 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Maybe later.
			boolean hasResponse1 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1671");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1675");

				utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks. Not right now.
	if (response == "s_1679")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ah. Okay then.
			string_id message = new string_id (c_stringFile, "s_1681");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
	if (response == "s_1683")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketopPlusAll (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopRewardPlusAll (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance. It would appear that you've also defeated Brightclaw, Paleclaw, Stoneleg, Silkthrower, and Greyclimber. That's an impressive list of hunted creatures. I should let Sordaan know of this. I think he'll want to recognize such an accomplishment.
			string_id message = new string_id (c_stringFile, "s_1689");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll go speak with Sordaan.
			boolean hasResponse0 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1691");

				utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopReward (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance.
			string_id message = new string_id (c_stringFile, "s_1695");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_etyyy_tuwezz_vol_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, let's see. Oh, I've got it. We've had a problem lately with some diseased ullers. We're worried that the disease that afflicts them could spread to other ullers or even other creatures. That would not be a good thing. What I need you to do is hunt and kill some of them. Kill 17 diseased ullers. That should help thin their population and reduce the chance of their disease spreading.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Right, kill diseased ullers. I'm on it.
	if (response == "s_1671")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_huntDiseasedUller (player, npc);

			//-- NPC: Head due east from here, and you'll find an area full of ullers. That's their primary habitat in this region. Good hunting to you. Oh, and return to me when you're done.
			string_id message = new string_id (c_stringFile, "s_1673");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Maybe later.
	if (response == "s_1675")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			//-- NPC: Ah. Okay then.
			string_id message = new string_id (c_stringFile, "s_1677");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_etyyy_tuwezz_vol_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Be careful. There are many dangerous creatures in Etyyy, the hunting grounds.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
	if (response == "s_1687")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketopPlusAll (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopRewardPlusAll (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance. It would appear that you've also defeated Brightclaw, Paleclaw, Stoneleg, Silkthrower, and Greyclimber. That's an impressive list of hunted creatures. I should let Sordaan know of this. I think he'll want to recognize such an accomplishment.
			string_id message = new string_id (c_stringFile, "s_1689");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll go speak with Sordaan.
			boolean hasResponse0 = false;
			if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1691");

				utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			ep3_etyyy_tuwezz_vol_action_spiketopReward (player, npc);

			//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance.
			string_id message = new string_id (c_stringFile, "s_1695");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int ep3_etyyy_tuwezz_vol_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ah, you've managed to defeat Spiketop. I wondered when someone would finally do so. To be honest, I'd hoped to hunt him myself, but had not gotten the chance. It would appear that you've also defeated Brightclaw, Paleclaw, Stoneleg, Silkthrower, and Greyclimber. That's an impressive list of hunted creatures. I should let Sordaan know of this. I think he'll want to recognize such an accomplishment.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll go speak with Sordaan.
	if (response == "s_1691")
	{
		//-- [NOTE] 
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good. He might even give you one of those Excellence in Hunting awards. Though so far, he's only awarded those to himself.
			string_id message = new string_id (c_stringFile, "s_1693");
			utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.ep3_etyyy_tuwezz_vol");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable (self, true);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.ep3_etyyy_tuwezz_vol");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (ep3_etyyy_tuwezz_vol_condition_hasCompletedTuwezzQuests (player, npc))
	{
		//-- NPC: Welcome, hunter. I trust the hunt goes well for you.
		string_id message = new string_id (c_stringFile, "s_1621");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
		boolean hasResponse0 = false;
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketop (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1623");

			utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 1);

			npcStartConversation (player, npc, "ep3_etyyy_tuwezz_vol", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_etyyy_tuwezz_vol_condition_finishedCollectingUllerHorns (player, npc))
	{
		//-- NPC: These horns are exactly what I wanted. Well done. I'm very happy with your hunting abilities. Enough so that I think it's time for you to go talk to Sordaan Xris.
		string_id message = new string_id (c_stringFile, "s_1625");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Okay. I'll go speak with him.
		boolean hasResponse0 = false;
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'll do that later.
		boolean hasResponse1 = false;
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
		boolean hasResponse2 = false;
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketop (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1627");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1631");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1635");

			utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 2);

			npcStartConversation (player, npc, "ep3_etyyy_tuwezz_vol", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_etyyy_tuwezz_vol_condition_isCollectingUllerHorns (player, npc))
	{
		//-- NPC: Unmarred uller horns. 11 of them. And remember, the elder ullers are the ones to target. None of the others will have good enough horns.
		string_id message = new string_id (c_stringFile, "s_1637");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: You're right. I'm on my way.
		boolean hasResponse0 = false;
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
		boolean hasResponse1 = false;
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketop (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1639");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1643");

			utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 5);

			npcStartConversation (player, npc, "ep3_etyyy_tuwezz_vol", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_etyyy_tuwezz_vol_condition_finishedHuntingDiseasedUller (player, npc))
	{
		ep3_etyyy_tuwezz_vol_action_doneHuntingDiseasedUller (player, npc);

		//-- NPC: You did quite well hunting those diseased ullers. Quite well indeed. I can see excellent potential in you as a hunter. I have one other hunting task for you. I want you to gather 11 unmarred uller horns. You'll find these on elder ullers. The younger and more aggressive uller tend to have damaged or undersized horns, but the elders often have very good ones.
		string_id message = new string_id (c_stringFile, "s_1645");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Let the hunt begin!
		boolean hasResponse0 = false;
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not right now. I'll try that later.
		boolean hasResponse1 = false;
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
		boolean hasResponse2 = false;
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketop (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1647");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1651");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1655");

			utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 7);

			npcStartConversation (player, npc, "ep3_etyyy_tuwezz_vol", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_etyyy_tuwezz_vol_condition_isHuntingDiseasedUller (player, npc))
	{
		//-- NPC: Go hunt those diseased ullers. Return to me when you're done.
		string_id message = new string_id (c_stringFile, "s_1657");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Right. I'm on my way.
		boolean hasResponse0 = false;
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
		boolean hasResponse1 = false;
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketop (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1659");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1663");

			utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 10);

			npcStartConversation (player, npc, "ep3_etyyy_tuwezz_vol", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_etyyy_tuwezz_vol_condition_hasToSpeakToTuwezz (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: Ziven sent you to me? Interesting. Well, I suppose he wants to see what kind of hunter you are. So let's find out, shall we?
		string_id message = new string_id (c_stringFile, "s_1665");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Okay, what do you want me to do?
		boolean hasResponse0 = false;
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No thanks. Not right now.
		boolean hasResponse1 = false;
		if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
		boolean hasResponse2 = false;
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketop (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1667");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1679");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1683");

			utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 12);

			npcStartConversation (player, npc, "ep3_etyyy_tuwezz_vol", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (ep3_etyyy_tuwezz_vol_condition__defaultCondition (player, npc))
	{
		//-- NPC: Be careful. There are many dangerous creatures in Etyyy, the hunting grounds.
		string_id message = new string_id (c_stringFile, "s_1685");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: [Show Spiketop's Horn to Tuwezz]
		boolean hasResponse0 = false;
		if (ep3_etyyy_tuwezz_vol_condition_killedSpiketop (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1687");

			utils.setScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId", 17);

			npcStartConversation (player, npc, "ep3_etyyy_tuwezz_vol", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "ep3_etyyy_tuwezz_vol")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

	if (branchId == 1 && ep3_etyyy_tuwezz_vol_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && ep3_etyyy_tuwezz_vol_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && ep3_etyyy_tuwezz_vol_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && ep3_etyyy_tuwezz_vol_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && ep3_etyyy_tuwezz_vol_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && ep3_etyyy_tuwezz_vol_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && ep3_etyyy_tuwezz_vol_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && ep3_etyyy_tuwezz_vol_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && ep3_etyyy_tuwezz_vol_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.ep3_etyyy_tuwezz_vol.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

