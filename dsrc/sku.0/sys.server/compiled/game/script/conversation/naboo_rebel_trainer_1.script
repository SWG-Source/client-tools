// ======================================================================
//
// naboo_rebel_trainer_1.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.37 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.conversation;
include library.factions;
include library.features;
include library.skill;
include library.space_flags;
include library.space_quest;
include library.space_skill;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/naboo_rebel_trainer_1";

// ======================================================================
// Script Conditions
// ======================================================================

boolean naboo_rebel_trainer_1_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_isNotRebel (obj_id player, obj_id npc)
{
	return !factions.isRebel(player);
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasNoviceSkillBox (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_novice" ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasRebelQuest (obj_id player, obj_id npc)
{
	return ( space_quest.hasQuest( player ) && naboo_rebel_trainer_1_condition_hasNoviceSkillBox(player, npc ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_isImperialPilot (obj_id player, obj_id npc)
{
	int playerFactionID = pvpGetAlignedFaction(player);
	if (playerFactionID == ##"imperial")
		return true;
	else
		return (hasSkill(player, "pilot_imperial_navy_novice"));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_isGenderMale (obj_id player, obj_id npc)
{
	return ( getGender( player ) == GENDER_MALE );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasCompletedQuestOne (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "destroy_surpriseattack", "naboo_rebel_1") );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasCompletedQuestTwo (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "destroy", "naboo_rebel_2") );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasCompletedQuestThree (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "escort", "naboo_rebel_3") );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasCompletedQuestFour (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "assassinate", "naboo_rebel_4") );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasFailedQuestOne (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "destroy_surpriseattack", "naboo_rebel_1_surprise" ) ||
			 space_quest.hasAbortedQuest( player, "destroy_surpriseattack", "naboo_rebel_1_surprise" ) ||
			 space_quest.hasFailedQuest ( player, "patrol", "naboo_rebel_1" ) ||
			 space_quest.hasAbortedQuest( player, "patrol", "naboo_rebel_1" ));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_isReadyForFirstTraining (obj_id player, obj_id npc)
{
	//You have completed all 4 missions is handled by previous convo conditions
	
	if ( 	hasSkill( player, "pilot_rebel_navy_starships_01") 	|| 
			hasSkill( player, "pilot_rebel_navy_procedures_01") 	||
			hasSkill( player, "pilot_rebel_navy_weapons_01") 		||
			hasSkill( player, "pilot_rebel_navy_droid_01" ) 	)
		return false;//You have already received your initial skill (or more!)
	else	
		return true;
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_isReadyForMoreTraining (obj_id player, obj_id npc)
{
	//You have completed all 4 missions - checked via previous convo options
	//but don't have all the tier-1 skills yet, - //checked via previous convo options
	
	//if you have enough xp for this skill, then you have enough for
	//any other skill at this tier:
	return ( space_quest.isPlayerQualifiedForSkill( player, "pilot_rebel_navy_starships_01") );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasAllTierOneSkills (obj_id player, obj_id npc)
{
	/*
	if (!hasSkill( player, "pilot_rebel_navy_starships_01") )
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_procedures_01") )
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_weapons_01") )
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_droid_01" ) )
		return false;
	else	
		return true;
	*/
	
	return space_flags.hasCompletedTierOne(player);
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasFailedQuestTwo (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "destroy", "naboo_rebel_2" ) ||
			 space_quest.hasAbortedQuest( player, "destroy", "naboo_rebel_2" ));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_beenRewardedForQuestOne (obj_id player, obj_id npc)
{
	//this returns true if you have completed this quest but have not been rewarded for it
	return 
		space_quest.hasCompletedQuest( player, "destroy_surpriseattack", "naboo_rebel_1") && 
	 	space_quest.hasReceivedReward( player, "destroy_surpriseattack", "naboo_rebel_1");
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_beenRewardedForQuestTwo (obj_id player, obj_id npc)
{
	//this returns true if you have completed this quest but have not been rewarded for it
	return
		space_quest.hasCompletedQuest( player, "destroy", "naboo_rebel_2") && 
		space_quest.hasReceivedReward( player, "destroy", "naboo_rebel_2");
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_beenRewardedForQuestThree (obj_id player, obj_id npc)
{
	//this returns true if you have completed this quest but have not been rewarded for it
	return
		space_quest.hasCompletedQuest( player, "escort", "naboo_rebel_3") && 
		space_quest.hasReceivedReward( player, "escort", "naboo_rebel_3");
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_beenRewardedForQuestFour (obj_id player, obj_id npc)
{
	//this returns true if you have completed this quest but have not been rewarded for it
	return
		space_quest.hasCompletedQuest( player, "assassinate", "naboo_rebel_4") &&
		space_quest.hasReceivedReward( player, "assassinate", "naboo_rebel_4");
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasFailedQuestThree (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "escort", "naboo_rebel_3" ) ||
			 space_quest.hasAbortedQuest( player, "escort", "naboo_rebel_3" ) ||
			 space_quest.hasFailedQuest( player, "patrol", "naboo_rebel_3" ) ||
			 space_quest.hasAbortedQuest( player, "patrol", "naboo_rebel_3" ));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasFailedQuestFour (obj_id player, obj_id npc)
{
	return ( space_quest.hasFailedQuest( player, "assassinate", "naboo_rebel_4" ) ||
			 space_quest.hasAbortedQuest(player, "assassinate", "naboo_rebel_4" ));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_isPrivateerPilot (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_neutral_novice" );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_readyForSecondSkill (obj_id player, obj_id npc)
{
	if ( space_quest.hasCompletedQuest( player, "assassinate", "naboo_rebel_4" ) &&
		 !hasSkill( player, "pilot_rebel_navy_starships_01" ) &&
		 !hasSkill( player, "pilot_rebel_navy_weapons_01" ) &&
		 !hasSkill( player, "pilot_rebel_navy_procedures_01" ) &&
		 !hasSkill( player, "pilot_rebel_navy_droid_01" ) )
		 return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasSTARSHIPS1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_starships_01" ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasWEAPONS1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_weapons_01" ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasPROCEDURES1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_procedures_01" ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasSpaceExpansion (obj_id player, obj_id npc)
{
	return ( features.isSpaceEdition( player ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasSpaceShip (obj_id player, obj_id npc)
{
	return (!space_quest.canGrantNewbieShip(player));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_has_GRADUATED_goto_DANTOOINE (obj_id player, obj_id npc)
{
	return (space_flags.hasSpaceFlag(player, "vortexPilot"));
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_hasVeryFirstQuest (obj_id player, obj_id npc)
{
	//You've got your assignment, and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
	if (!space_quest.hasCertifiedShip(player))
		return false; 
	else	
		return ( space_quest.hasQuest( player, "patrol", "naboo_rebel_1" ) ||
			  	 space_quest.hasQuest( player, "destroy_surpriseattack", "naboo_rebel_1" ) );
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_onMyTrack (obj_id player, obj_id npc)
{
	//this is a temporary thing to convert BETA players to the
	//new flagging system.
	if ( hasSkill( player, "pilot_rebel_navy_novice" ))
	{
		string currentTrack = space_flags.getSpaceTrack( player );
		if ( currentTrack == null )
			space_flags.setSpaceTrack( player, space_flags.REBEL_NABOO);
	}

	return space_flags.isSpaceTrack(player, space_flags.REBEL_NABOO);
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_canFlyNonTrackDuty (obj_id player, obj_id npc)
{
	if((hasSkill( player, "pilot_rebel_navy_starships_01") || 
	    hasSkill( player, "pilot_rebel_navy_procedures_01") ||
	    hasSkill( player, "pilot_rebel_navy_weapons_01") ||
	    hasSkill( player, "pilot_rebel_navy_droid_01" )) && !space_quest.hasQuest(player))
	    return true;
	    
	return false;
}

// ----------------------------------------------------------------------

boolean naboo_rebel_trainer_1_condition_isWookieeOrIthorian (obj_id player, obj_id npc)
{
	int plrSpecies = getSpecies( player );
	return ( plrSpecies == SPECIES_WOOKIEE || plrSpecies == SPECIES_ITHORIAN ) ;
}

// ======================================================================
// Script Actions
// ======================================================================

void naboo_rebel_trainer_1_action_grantNoviceSkillBox (obj_id player, obj_id npc)
{
	space_flags.setSpaceTrack(player, space_flags.REBEL_NABOO);
	skill.noisyGrantSkill( player, "pilot_rebel_navy_novice" );
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantQuestOne (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "patrol", "naboo_rebel_1");
	space_flags.restoreClientPath( player );
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantQuestTwo (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "destroy", "naboo_rebel_2");
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantQuestThree (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "patrol", "naboo_rebel_3");
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantQuestFour (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "assassinate", "naboo_rebel_4");
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_anim_greet (obj_id player, obj_id npc)
{
	doAnimationAction( npc, "greet" );
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_anim_explode (obj_id player, obj_id npc)
{
	//playClientEffectObj(player, "clienteffect/lair_damage_light.cef", npc, "");
	playClientEffectLoc( player, "clienteffect/lair_damage_light.cef", getLocation(npc), 1.52f );
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantSkill1 (obj_id player, obj_id npc)
{
	doAnimationAction( npc, "salute1" );
	
	if ( naboo_rebel_trainer_1_condition_readyForSecondSkill( player, npc ) )
	{
		skill.noisyGrantSkill( player, "pilot_rebel_navy_starships_01" );
	}
	else
	{
		skill.purchaseSkill( player, "pilot_rebel_navy_starships_01" );
	}	
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantSkill2 (obj_id player, obj_id npc)
{
	doAnimationAction( npc, "salute1" );
	
	if ( naboo_rebel_trainer_1_condition_readyForSecondSkill( player, npc ) )
	{
		skill.noisyGrantSkill( player, "pilot_rebel_navy_weapons_01" );
	}
	else
	{
		skill.purchaseSkill( player, "pilot_rebel_navy_weapons_01" );
	}	
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantSkill3 (obj_id player, obj_id npc)
{
	doAnimationAction( npc, "salute1" );
	
	if ( naboo_rebel_trainer_1_condition_readyForSecondSkill( player, npc ) )
	{
		skill.noisyGrantSkill( player, "pilot_rebel_navy_procedures_01" );
	}
	else
	{
		skill.purchaseSkill( player, "pilot_rebel_navy_procedures_01" );
	}	
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantSkill4 (obj_id player, obj_id npc)
{
	doAnimationAction( npc, "salute1" );
	
	if ( naboo_rebel_trainer_1_condition_readyForSecondSkill( player, npc ) )
	{
		skill.noisyGrantSkill( player, "pilot_rebel_navy_droid_01" );
	}
	else
	{
		skill.purchaseSkill( player, "pilot_rebel_navy_droid_01" );
	}	
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantDUTY_Destroy1 (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "destroy_duty", "naboo_rebel_6");
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantDUTY_Escort1 (obj_id player, obj_id npc)
{
	space_quest.grantQuest ( player, "escort_duty", "naboo_rebel_7");
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_reward_quest_1 (obj_id player, obj_id npc)
{
	if( !space_quest.hasReceivedReward( player, "destroy_surpriseattack", "naboo_rebel_1") )
	{
		factions.addFactionStanding( player, factions.FACTION_REBEL, 25.0f );
		space_quest.giveReward( player, "destroy_surpriseattack", "naboo_rebel_1", 100 );
	}
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_reward_quest_2 (obj_id player, obj_id npc)
{
	if( !space_quest.hasReceivedReward( player, "destroy", "naboo_rebel_2") )
	{
		factions.addFactionStanding( player, factions.FACTION_REBEL, 50.0f );
	 	space_quest.giveReward( player, "destroy", "naboo_rebel_2", 200 );
	 }
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_reward_quest_3 (obj_id player, obj_id npc)
{
	if( !space_quest.hasReceivedReward( player, "escort", "naboo_rebel_3") )
	{
		factions.addFactionStanding( player, factions.FACTION_REBEL, 50.0f );
		if (!naboo_rebel_trainer_1_condition_isWookieeOrIthorian( player, npc ))
			space_quest.giveReward( player, "escort", "naboo_rebel_3", 500, "object/tangible/wearables/bodysuit/rebel_bodysuit_s14.iff");
		else if ( getSpecies(player)  != SPECIES_ITHORIAN )
			space_quest.giveReward( player, "escort", "naboo_rebel_3", 500, "object/tangible/wearables/bandolier/multipocket_bandolier.iff");
		else
			space_quest.giveReward( player, "escort", "naboo_rebel_3", 500, "object/tangible/wearables/bandolier/ith_multipocket_bandolier.iff");
	}
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_reward_quest_4 (obj_id player, obj_id npc)
{
	if( !space_quest.hasReceivedReward( player, "assassinate", "naboo_rebel_4") )
	{
		factions.addFactionStanding( player, factions.FACTION_REBEL, 75.0f );
		space_quest.giveReward( player, "assassinate", "naboo_rebel_4", 1000, "object/tangible/ship/components/armor/arm_mission_reward_rebel_incom_ultralight.iff");
	}
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_grantSpaceShip (obj_id player, obj_id npc)
{
	space_quest.grantNewbieShip( player, "rebel");
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_set_GRADUATED_goto_DANTOOINE (obj_id player, obj_id npc)
{
	space_flags.setSpaceFlag(player, "vortexPilot", true);
}

// ----------------------------------------------------------------------

void naboo_rebel_trainer_1_action_clearNewClientPaths (obj_id player, obj_id npc)
{
	space_flags.clearClientPath( player );
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int naboo_rebel_trainer_1_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How can I be of service? I have added your first assignment to your log... and I have uploaded codes for a ship control device in your datapad.  Go to the Starport and access the terminal to launch into space... unless I forgot something?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			//-- NPC: You must go to the starport in order to begin your assignment. It's just over there - on the other side of the pond. You can see it from here, actually. Go inside and look for a 'starship terminal.' Use the terminal to launch your ship into space.
			string_id message = new string_id (c_stringFile, "s_e6a9899d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back?
	if (response == "s_85dd7d6c")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you launch, your ship computer will create a 'launch waypoint' near the Naboo traffic control station. At the conclusion of your mission - return to this point. Communicate with the Naboo space station and request landing clearance.
			string_id message = new string_id (c_stringFile, "s_b952209b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is my ship?
	if (response == "s_4358efe9")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A basic Z95 starship is being held for you at the starport. I have uploaded the control codes for this ship to your personal datapad. Any time you wish to review the ship status, open your datapad and examine the ship control device.
			string_id message = new string_id (c_stringFile, "s_26bc1385");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You must go to the starport in order to begin your assignment. It's just over there - on the other side of the pond. You can see it from here, actually. Go inside and look for a 'starship terminal.' Use the terminal to launch your ship into space.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			//-- NPC: You must go to the starport in order to begin your assignment. It's just over there - on the other side of the pond. You can see it from here, actually. Go inside and look for a 'starship terminal.' Use the terminal to launch your ship into space.
			string_id message = new string_id (c_stringFile, "s_e6a9899d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back?
	if (response == "s_85dd7d6c")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you launch, your ship computer will create a 'launch waypoint' near the Naboo traffic control station. At the conclusion of your mission - return to this point. Communicate with the Naboo space station and request landing clearance.
			string_id message = new string_id (c_stringFile, "s_b952209b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is my ship?
	if (response == "s_4358efe9")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A basic Z95 starship is being held for you at the starport. I have uploaded the control codes for this ship to your personal datapad. Any time you wish to review the ship status, open your datapad and examine the ship control device.
			string_id message = new string_id (c_stringFile, "s_26bc1385");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: When you launch, your ship computer will create a 'launch waypoint' near the Naboo traffic control station. At the conclusion of your mission - return to this point. Communicate with the Naboo space station and request landing clearance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			//-- NPC: You must go to the starport in order to begin your assignment. It's just over there - on the other side of the pond. You can see it from here, actually. Go inside and look for a 'starship terminal.' Use the terminal to launch your ship into space.
			string_id message = new string_id (c_stringFile, "s_e6a9899d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back?
	if (response == "s_85dd7d6c")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you launch, your ship computer will create a 'launch waypoint' near the Naboo traffic control station. At the conclusion of your mission - return to this point. Communicate with the Naboo space station and request landing clearance.
			string_id message = new string_id (c_stringFile, "s_b952209b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is my ship?
	if (response == "s_4358efe9")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A basic Z95 starship is being held for you at the starport. I have uploaded the control codes for this ship to your personal datapad. Any time you wish to review the ship status, open your datapad and examine the ship control device.
			string_id message = new string_id (c_stringFile, "s_26bc1385");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A basic Z95 starship is being held for you at the starport. I have uploaded the control codes for this ship to your personal datapad. Any time you wish to review the ship status, open your datapad and examine the ship control device.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			//-- NPC: You must go to the starport in order to begin your assignment. It's just over there - on the other side of the pond. You can see it from here, actually. Go inside and look for a 'starship terminal.' Use the terminal to launch your ship into space.
			string_id message = new string_id (c_stringFile, "s_e6a9899d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back?
	if (response == "s_85dd7d6c")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: When you launch, your ship computer will create a 'launch waypoint' near the Naboo traffic control station. At the conclusion of your mission - return to this point. Communicate with the Naboo space station and request landing clearance.
			string_id message = new string_id (c_stringFile, "s_b952209b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is my ship?
	if (response == "s_4358efe9")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A basic Z95 starship is being held for you at the starport. I have uploaded the control codes for this ship to your personal datapad. Any time you wish to review the ship status, open your datapad and examine the ship control device.
			string_id message = new string_id (c_stringFile, "s_26bc1385");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh. Excuse me. I didn't see you there.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are you doing here?
	if (response == "s_b26194cb")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: I am not doing anything here.
			string_id message = new string_id (c_stringFile, "s_e9b471e7");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who are you?
	if (response == "s_ca776e30")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: I am V3-FX... Alliance Pilot Relations.
			string_id message = new string_id (c_stringFile, "s_2e4fedc2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am looking for pilot training.
	if (response == "s_1a53f653")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			naboo_rebel_trainer_1_action_anim_explode (player, npc);

			//-- NPC: I ***CRACKLE*** excellent pilot trainer!
			string_id message = new string_id (c_stringFile, "s_54a7239a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Train me to fly starships!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f60c8a5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_900f01d1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I ***CRACKLE*** excellent pilot trainer!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Train me to fly starships!
	if (response == "s_f60c8a5")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: ...smuggler...does not...compute...
			string_id message = new string_id (c_stringFile, "s_706c5683");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What?
	if (response == "s_900f01d1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: I... forget...
			string_id message = new string_id (c_stringFile, "s_72d418bf");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Greetings. Can I be of service?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I have no use for you.
	if (response == "s_a425f33d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good day to you, then.
			string_id message = new string_id (c_stringFile, "s_41e49ae6");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need answers, droid.
	if (response == "s_c16bab26")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			naboo_rebel_trainer_1_action_anim_explode (player, npc);

			//-- NPC: **zzt** What sort of answers?
			string_id message = new string_id (c_stringFile, "s_67d49232");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Glare in silent hatred.)
	if (response == "s_9f6cf52")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: Oh dear. Oh no! I hope I have not offended you.
			string_id message = new string_id (c_stringFile, "s_babb9df9");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] NABOO Rebel Trainer - V3-FX
	//-- NPC: Greetings. I am V3-FX, Alliance Pilot Relations. May I interest you in our starfighter training program?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like fun. Where do I sign?
	if (response == "s_78679b31")
	{
		naboo_rebel_trainer_1_action_clearNewClientPaths (player, npc);

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Oh, there is nothing to sign. You simply need to apply yourself to the flight lessons I have to teach. Are you interested?
			string_id message = new string_id (c_stringFile, "s_48d7182e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Interested? Is there money in it?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: You're going to teach ME?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99dfe276");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0643db5");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 18);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to fly for the Alliance!
	if (response == "s_1a0322d9")
	{
		naboo_rebel_trainer_1_action_clearNewClientPaths (player, npc);

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			//-- NPC: What a fortunate coincidence! I am able to induct you to the Alliance pilot training course immediately - provided that you agree to certain obligations.
			string_id message = new string_id (c_stringFile, "s_cca63822");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What sort of obligations?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_546aa4f5");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f91d81c7");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 38);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready to train.
	if (response == "s_616cd1dd")
	{
		naboo_rebel_trainer_1_action_clearNewClientPaths (player, npc);

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			//-- NPC: Wonderful! I will teach you to fly starfighters in exchange for your loyalty to the Alliance, and your full willingness to support the rebellion in Naboo space and beyond.
			string_id message = new string_id (c_stringFile, "s_a9a1aa2a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Alliance has my eternal faith.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_31d45080");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh, there is nothing to sign. You simply need to apply yourself to the flight lessons I have to teach. Are you interested?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Interested? Is there money in it?
	if (response == "s_99dfe276")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: There is a measure of monetary compensation for your efforts. Honestly, most pilots fly because they feel it is important to their survival. Do you not agree?
			string_id message = new string_id (c_stringFile, "s_9ba2e5e1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, sure. Why not.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What sort of money are we talking about?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2bf47615");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1d2430a3");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You're going to teach ME?
	if (response == "s_b0643db5")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: But of course. It is the standard starfighter training curriculum. I am a very skilled teacher, I assure you.
			string_id message = new string_id (c_stringFile, "s_16642b72");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds wizard. Let's go.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want to be part of the Alliance.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e096316d");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7ed5fec3");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There is a measure of monetary compensation for your efforts. Honestly, most pilots fly because they feel it is important to their survival. Do you not agree?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, sure. Why not.
	if (response == "s_2bf47615")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Splendid! You are now part of the Alliance pilot training program.
			string_id message = new string_id (c_stringFile, "s_65a3e672");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will need a starship.
			boolean hasResponse0 = false;
			if (!naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a42932d7");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What sort of money are we talking about?
	if (response == "s_1d2430a3")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: I cannot say. Being a droid, I have no use for such things.
			string_id message = new string_id (c_stringFile, "s_4c08239e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Well, I'll join anyway.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104b565a");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Splendid! You are now part of the Alliance pilot training program.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will need a starship.
	if (response == "s_a42932d7")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			naboo_rebel_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.
			string_id message = new string_id (c_stringFile, "s_efd8333f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fc27931b");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_43");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_fc27931b")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_52fb0136");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I cannot say. Being a droid, I have no use for such things.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, I'll join anyway.
	if (response == "s_104b565a")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			naboo_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Glorious! Welcome to the Alliance pilot training system. I will be your tutor.
			string_id message = new string_id (c_stringFile, "s_3be54b2c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will need a starship.
			boolean hasResponse0 = false;
			if (!naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Glorious! Welcome to the Alliance pilot training system. I will be your tutor.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will need a starship.
	if (response == "s_49")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.
			string_id message = new string_id (c_stringFile, "s_51");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_53");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_57")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_59");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_53")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_55");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: But of course. It is the standard starfighter training curriculum. I am a very skilled teacher, I assure you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds wizard. Let's go.
	if (response == "s_e096316d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: You have made an excellent choice. You are now part of the Alliance flight training program.
			string_id message = new string_id (c_stringFile, "s_de7d9c3a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will need a starship.
			boolean hasResponse0 = false;
			if (!naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_73");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to be part of the Alliance.
	if (response == "s_7ed5fec3")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Welcome to the Alliance.
			string_id message = new string_id (c_stringFile, "s_11e3b199");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will need a starship.
			boolean hasResponse0 = false;
			if (!naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_79");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 34);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have made an excellent choice. You are now part of the Alliance flight training program.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will need a starship.
	if (response == "s_65")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.
			string_id message = new string_id (c_stringFile, "s_67");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_69");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_73")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_75");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_69")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_71");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch34 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Welcome to the Alliance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will need a starship.
	if (response == "s_79")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_rebel_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.
			string_id message = new string_id (c_stringFile, "s_81");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_83");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_87")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_83")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_85");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch38 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What a fortunate coincidence! I am able to induct you to the Alliance pilot training course immediately - provided that you agree to certain obligations.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You got it!
	if (response == "s_546aa4f5")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: This is a great day! Welcome to the Alliance pilot training program.
			string_id message = new string_id (c_stringFile, "s_e93422bf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will need a starship.
			boolean hasResponse0 = false;
			if (!naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_103");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What sort of obligations?
	if (response == "s_f91d81c7")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: In order to qualify for continued training you must participate in Alliance military operations in Naboo space - and beyond.
			string_id message = new string_id (c_stringFile, "s_71a4b693");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds great!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d002385f");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This is a great day! Welcome to the Alliance pilot training program.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will need a starship.
	if (response == "s_95")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.
			string_id message = new string_id (c_stringFile, "s_97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 40);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_103")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_105");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch40 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_99")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_101");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: In order to qualify for continued training you must participate in Alliance military operations in Naboo space - and beyond.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds great!
	if (response == "s_d002385f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: In that case, I welcome you to the Rebel Alliance pilot training program.
			string_id message = new string_id (c_stringFile, "s_6cc94470");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will need a starship.
			boolean hasResponse0 = false;
			if (!naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_111");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_119");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch44 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: In that case, I welcome you to the Rebel Alliance pilot training program.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will need a starship.
	if (response == "s_111")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.
			string_id message = new string_id (c_stringFile, "s_113");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_115");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_119")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_121");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_115")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_117");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch46 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch48 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Wonderful! I will teach you to fly starfighters in exchange for your loyalty to the Alliance, and your full willingness to support the rebellion in Naboo space and beyond.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Alliance has my eternal faith.
	if (response == "s_31d45080")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Then it is my pleasure to welcome you to the Alliance space navy.
			string_id message = new string_id (c_stringFile, "s_7d2cd92d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I will need a starship.
			boolean hasResponse0 = false;
			if (!naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Thanks.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_127");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 49);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch49 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then it is my pleasure to welcome you to the Alliance space navy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will need a starship.
	if (response == "s_127")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.
			string_id message = new string_id (c_stringFile, "s_129");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_131");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 50);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_135")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_137");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch50 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are in luck. The Alliance has stowed a number of restored fighter craft at starports here on Naboo. I will upload the security codes for one of these vessels to your datapad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_131")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are most welcome. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_133");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch51 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You are most welcome. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch53 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh. You are an Alliance pilot, but I do not recognize you. How may I be of service?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are there any missions I can fly?
	if (response == "s_ac05bfba")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_canFlyNonTrackDuty (player, npc))
		{
			//-- NPC: Of course. I am authorized to assign a selection of combat duty missions to you. Would you like to perform a duty mission?
			string_id message = new string_id (c_stringFile, "s_dfc3b1e1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I want to enlist with the ground forces
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_761039fa");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 151);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Forgive me. I am not authorized to assign missions to you. Perhaps you should speak with your commanding officer.
			string_id message = new string_id (c_stringFile, "s_466afe42");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course. I am authorized to assign a selection of combat duty missions to you. Would you like to perform a duty mission?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is an excellent way of earning experience towards your next training session. No good trainer would teach a pilot with no experience. Duty missions have no real end to them, you simply finish whenever you are ready. Would you like a duty mission?
			string_id message = new string_id (c_stringFile, "s_96184c1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I want to enlist with the ground forces
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_761039fa");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 151);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am interested in a duty mission.
	if (response == "s_4ffd4b9d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: That is most excellent to hear. We have two mission types available. You may choose to patrol the system and destroy any Imperials that you find, or you may choose to help escort our supply ships. Which would you like?
			string_id message = new string_id (c_stringFile, "s_614176fe");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would like to patrol Naboo space.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Our transports could use my help.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf63ee80");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a66ff602");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 153);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to enlist with the ground forces
	if (response == "s_761039fa")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_cackle");

			//-- NPC: Finally decided to become a REAL Rebel, huh?  Well it's about time.  You do realize that enlisting with the ground forces means you could be attacked when you least expect it... even if you are a covert operative, there's still a danger!
			string_id message = new string_id (c_stringFile, "s_81eaed4e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not afraid
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5f13c4");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 157);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hello there! It is good to see you, although I believe you are supposed to be on planet Dantooine... correct?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where?
	if (response == "s_741df7ed")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Planet Dantooine. You are supposed to meet with the Vortex group leader, Brother Vrovel, at the 'abandoned' Rebel base there. Due to the secretive nature of the Vortex you won't be let in to the base until you are a full member of the Alliance.
			string_id message = new string_id (c_stringFile, "s_a26aaa8b");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Requesting more info, sir!
	if (response == "s_2be1e8b")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You must go to planet Dantooine and meet with Brother Vrovel, leader of the Vortex organization. He's located in the North West at what the Empire thinks is an abandoned Rebel base. Due to the secretive nature of the Vortex you won't be let in to the base until you are a full member of the Alliance.
			string_id message = new string_id (c_stringFile, "s_4063c2da");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get there?
	if (response == "s_b3bebc5e")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Get into your ship, Pilot, and FLY there.
			string_id message = new string_id (c_stringFile, "s_a3f21650");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have all the tier-1 skills, so would you like some tier-2 content?
	//-- NPC: Congratulations, pilot! You have successfully completed the first tier of the Alliance starfighter training program. Your next assignment will take you to planet Dantooine and place you under the guidance of Brother Vrovel... leader of the Vortex organization.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You got it, metal head!
	if (response == "s_f75b511f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Splendid! Please convey my admiration to the Alliance pilots on Dantooine. You will find Brother Vrovel in the 'abandoned' Rebel base in the North West. Be aware, you need to be a full member of the Alliance faction to be allowed entrance in to this secret base. Good luck!
			string_id message = new string_id (c_stringFile, "s_fedae3f2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is 'Vortex?'
	if (response == "s_4efbdbfa")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Unfortunately, I have very little information on the Vortex group. Alliance HQ indicates that they are a friendly organization dedicated to locating Light-side Jedi and recruiting them to the Rebel cause. They are a very secretive part of the Alliance which is why you need to be a full faction member of the Alliance before you can join them.
			string_id message = new string_id (c_stringFile, "s_7a4f9992");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who is Brother Vrovel?
	if (response == "s_b89c30f1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: According to the Alliance main computer, Brother Vrovel is an eccentric Bith who has helped the Rebels on numerous occasions. He himself is not a pilot... for that matter, he has almost no pilots in his ranks. That is why you have been re-assigned.
			string_id message = new string_id (c_stringFile, "s_6be218d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Goodbye, V3.
	if (response == "s_1df40ac6")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isGenderMale (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good bye, sir! And good luck on Dantooine!
			string_id message = new string_id (c_stringFile, "s_a6661be0");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good bye, ma'am! And good luck to you on Dantooine!
			string_id message = new string_id (c_stringFile, "s_9cb26d6a");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find this Brother Vrovel?
	if (response == "s_8334632")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Searching my extensive database I see that he is in what the Empire think is an 'abandoned' Rebel base on Dantooine. The Alliance is so clever it's almost scary.
			string_id message = new string_id (c_stringFile, "s_ae8026b4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Unfortunately, I have very little information on the Vortex group. Alliance HQ indicates that they are a friendly organization dedicated to locating Light-side Jedi and recruiting them to the Rebel cause. They are a very secretive part of the Alliance which is why you need to be a full faction member of the Alliance before you can join them.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You got it, metal head!
	if (response == "s_f75b511f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Splendid! Please convey my admiration to the Alliance pilots on Dantooine. You will find Brother Vrovel in the 'abandoned' Rebel base in the North West. Be aware, you need to be a full member of the Alliance faction to be allowed entrance in to this secret base. Good luck!
			string_id message = new string_id (c_stringFile, "s_fedae3f2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is 'Vortex?'
	if (response == "s_4efbdbfa")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Unfortunately, I have very little information on the Vortex group. Alliance HQ indicates that they are a friendly organization dedicated to locating Light-side Jedi and recruiting them to the Rebel cause. They are a very secretive part of the Alliance which is why you need to be a full faction member of the Alliance before you can join them.
			string_id message = new string_id (c_stringFile, "s_7a4f9992");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who is Brother Vrovel?
	if (response == "s_b89c30f1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: According to the Alliance main computer, Brother Vrovel is an eccentric Bith who has helped the Rebels on numerous occasions. He himself is not a pilot... for that matter, he has almost no pilots in his ranks. That is why you have been re-assigned.
			string_id message = new string_id (c_stringFile, "s_6be218d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Goodbye, V3.
	if (response == "s_1df40ac6")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isGenderMale (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good bye, sir! And good luck on Dantooine!
			string_id message = new string_id (c_stringFile, "s_a6661be0");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good bye, ma'am! And good luck to you on Dantooine!
			string_id message = new string_id (c_stringFile, "s_9cb26d6a");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find this Brother Vrovel?
	if (response == "s_8334632")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Searching my extensive database I see that he is in what the Empire think is an 'abandoned' Rebel base on Dantooine. The Alliance is so clever it's almost scary.
			string_id message = new string_id (c_stringFile, "s_ae8026b4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch65 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: According to the Alliance main computer, Brother Vrovel is an eccentric Bith who has helped the Rebels on numerous occasions. He himself is not a pilot... for that matter, he has almost no pilots in his ranks. That is why you have been re-assigned.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You got it, metal head!
	if (response == "s_f75b511f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Splendid! Please convey my admiration to the Alliance pilots on Dantooine. You will find Brother Vrovel in the 'abandoned' Rebel base in the North West. Be aware, you need to be a full member of the Alliance faction to be allowed entrance in to this secret base. Good luck!
			string_id message = new string_id (c_stringFile, "s_fedae3f2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is 'Vortex?'
	if (response == "s_4efbdbfa")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Unfortunately, I have very little information on the Vortex group. Alliance HQ indicates that they are a friendly organization dedicated to locating Light-side Jedi and recruiting them to the Rebel cause. They are a very secretive part of the Alliance which is why you need to be a full faction member of the Alliance before you can join them.
			string_id message = new string_id (c_stringFile, "s_7a4f9992");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who is Brother Vrovel?
	if (response == "s_b89c30f1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: According to the Alliance main computer, Brother Vrovel is an eccentric Bith who has helped the Rebels on numerous occasions. He himself is not a pilot... for that matter, he has almost no pilots in his ranks. That is why you have been re-assigned.
			string_id message = new string_id (c_stringFile, "s_6be218d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Goodbye, V3.
	if (response == "s_1df40ac6")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isGenderMale (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good bye, sir! And good luck on Dantooine!
			string_id message = new string_id (c_stringFile, "s_a6661be0");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good bye, ma'am! And good luck to you on Dantooine!
			string_id message = new string_id (c_stringFile, "s_9cb26d6a");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find this Brother Vrovel?
	if (response == "s_8334632")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Searching my extensive database I see that he is in what the Empire think is an 'abandoned' Rebel base on Dantooine. The Alliance is so clever it's almost scary.
			string_id message = new string_id (c_stringFile, "s_ae8026b4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch68 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Searching my extensive database I see that he is in what the Empire think is an 'abandoned' Rebel base on Dantooine. The Alliance is so clever it's almost scary.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You got it, metal head!
	if (response == "s_f75b511f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute2");

			//-- NPC: Splendid! Please convey my admiration to the Alliance pilots on Dantooine. You will find Brother Vrovel in the 'abandoned' Rebel base in the North West. Be aware, you need to be a full member of the Alliance faction to be allowed entrance in to this secret base. Good luck!
			string_id message = new string_id (c_stringFile, "s_fedae3f2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is 'Vortex?'
	if (response == "s_4efbdbfa")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Unfortunately, I have very little information on the Vortex group. Alliance HQ indicates that they are a friendly organization dedicated to locating Light-side Jedi and recruiting them to the Rebel cause. They are a very secretive part of the Alliance which is why you need to be a full faction member of the Alliance before you can join them.
			string_id message = new string_id (c_stringFile, "s_7a4f9992");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who is Brother Vrovel?
	if (response == "s_b89c30f1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: According to the Alliance main computer, Brother Vrovel is an eccentric Bith who has helped the Rebels on numerous occasions. He himself is not a pilot... for that matter, he has almost no pilots in his ranks. That is why you have been re-assigned.
			string_id message = new string_id (c_stringFile, "s_6be218d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Goodbye, V3.
	if (response == "s_1df40ac6")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isGenderMale (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good bye, sir! And good luck on Dantooine!
			string_id message = new string_id (c_stringFile, "s_a6661be0");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			//-- NPC: Good bye, ma'am! And good luck to you on Dantooine!
			string_id message = new string_id (c_stringFile, "s_9cb26d6a");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find this Brother Vrovel?
	if (response == "s_8334632")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Searching my extensive database I see that he is in what the Empire think is an 'abandoned' Rebel base on Dantooine. The Alliance is so clever it's almost scary.
			string_id message = new string_id (c_stringFile, "s_ae8026b4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You got it, metal head!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is 'Vortex?'
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Who is Brother Vrovel?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Goodbye, V3.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Where do I find this Brother Vrovel?
			boolean hasResponse4 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch69 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alliance HQ has requested that I assign a new mission to you... since you have failed the previous. Good luck!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch70 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Alliance HQ computer system is rather disappointed with your performance. I have been instructed to renew your mission assignment. Good luck!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure. Why not.
	if (response == "s_c92d07b4")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Why not indeed! Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_64308f9f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, sir!
	if (response == "s_b2d473c6")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Um... yes... um... Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_2582d5e6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am more than ready.
	if (response == "s_6fb5a316")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: That is good to hear. Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_314a123f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_241")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, %TU.  There'll be time for training later.  Right now we need you to do another assignment.  I hope you are ready for it.
			string_id message = new string_id (c_stringFile, "s_fb2ee4e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure. Why not.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Yes, sir!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I am more than ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c92d07b4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2d473c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6fb5a316");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_241");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 94);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch71 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I have been ordered to reassign a mission to you. The mission you most recently failed. My apologies. And... good luck!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, yeah.
	if (response == "s_3f1a842c")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Thank the maker!
			string_id message = new string_id (c_stringFile, "s_a30d25ad");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, sir!
	if (response == "s_255")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Good luck!
			string_id message = new string_id (c_stringFile, "s_a80d9308");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course. Those supplies are critical!
	if (response == "s_1e626caa")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Critical to say the least! We must resume supply flights as soon as possible. To accomplish this, we need you to sweep a five-point area. Once again, your ship sensor data will be used to plot an effective route for our larger transports.
			string_id message = new string_id (c_stringFile, "s_b5caf1b6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Force be with us.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0233b4b");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 115);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_262")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not yet, this is important!  Now are you ready?
			string_id message = new string_id (c_stringFile, "s_5f65d2ef");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, yeah.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Yes, sir!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Of course. Those supplies are critical!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f1a842c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1e626caa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_262");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch72 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alliance HQ demands that you rejoin battle against the TIE Veteran so that our campaign may move forward. Good luck!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I got nothing better to do.
	if (response == "s_ebc0661b")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate1");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: Splendid!
			string_id message = new string_id (c_stringFile, "s_c010439f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So tell me about the job.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let's have some details.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bf1a0f3e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a150013");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 124);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Affirmative!
	if (response == "s_6b490480")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: We have located a rally point from which the Empire has staged their recent aggressions. Go to this point and bring down a veteran TIE fighter pilot.
			string_id message = new string_id (c_stringFile, "s_47ce8fc2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes sir!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a30c1a53");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For the Alliance!
	if (response == "s_a994ca63")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: Yes... We have reliable information about the location of Imperial TIE forces. They are being led by an experienced TIE veteran. Eliminate this TIE veteran pilot with extreme prejudice. This should halt the Empire's plans rather effectively.
			string_id message = new string_id (c_stringFile, "s_da803d9b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Yes, yes, you have my word.  Complete this last assignment for me and I will train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_ca62728c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I got nothing better to do.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebc0661b");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6b490480");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a994ca63");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 123);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch73 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] grantQuest 1
	//-- NPC: Salutations, pilot! I hope you are well-rested and ready for your first assignment for the Alliance training system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch74 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent! I see you are in perfect health!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let's get on with it, buddy.
	if (response == "s_3a8fb4dd")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: As you wish! Your first assignment is a four-point patrol along the Naboo frontier. Be careful of local pirates and be kind to local merchants. They are friends to us all, you know.
			string_id message = new string_id (c_stringFile, "s_2518a758");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Wave dismissively.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Pirates? Nobody mentioned pirates before...
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: You say there are merchants to save?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116bd775");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afdd3bc5");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c713935d");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch75 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: As you wish! Your first assignment is a four-point patrol along the Naboo frontier. Be careful of local pirates and be kind to local merchants. They are friends to us all, you know.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Wave dismissively.)
	if (response == "s_116bd775")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Uh... yes, and good day to you too!
			string_id message = new string_id (c_stringFile, "s_ae7a2a24");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Pirates? Nobody mentioned pirates before...
	if (response == "s_afdd3bc5")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			//-- NPC: Oh absolutely! Naboo space is teeming with deadly gangs of pirates and horrid nebula that will melt your ship in mere seconds. Do be careful out there!
			string_id message = new string_id (c_stringFile, "s_eceb89e1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Wave dismissively.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Pirates? Nobody mentioned pirates before...
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: You say there are merchants to save?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116bd775");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afdd3bc5");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c713935d");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You say there are merchants to save?
	if (response == "s_c713935d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: They're quite safe on their own, I assure you. We only ask that you stay out of sight. Starfighter patrols make the local business interests somewhat... timid, if you get my meaning.
			string_id message = new string_id (c_stringFile, "s_81c3ab31");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Wave dismissively.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Pirates? Nobody mentioned pirates before...
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: You say there are merchants to save?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116bd775");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afdd3bc5");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c713935d");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch77 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh absolutely! Naboo space is teeming with deadly gangs of pirates and horrid nebula that will melt your ship in mere seconds. Do be careful out there!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Wave dismissively.)
	if (response == "s_116bd775")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Uh... yes, and good day to you too!
			string_id message = new string_id (c_stringFile, "s_ae7a2a24");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Pirates? Nobody mentioned pirates before...
	if (response == "s_afdd3bc5")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			//-- NPC: Oh absolutely! Naboo space is teeming with deadly gangs of pirates and horrid nebula that will melt your ship in mere seconds. Do be careful out there!
			string_id message = new string_id (c_stringFile, "s_eceb89e1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Wave dismissively.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Pirates? Nobody mentioned pirates before...
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: You say there are merchants to save?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116bd775");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afdd3bc5");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c713935d");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You say there are merchants to save?
	if (response == "s_c713935d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: They're quite safe on their own, I assure you. We only ask that you stay out of sight. Starfighter patrols make the local business interests somewhat... timid, if you get my meaning.
			string_id message = new string_id (c_stringFile, "s_81c3ab31");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Wave dismissively.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Pirates? Nobody mentioned pirates before...
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: You say there are merchants to save?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116bd775");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afdd3bc5");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c713935d");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch78 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're quite safe on their own, I assure you. We only ask that you stay out of sight. Starfighter patrols make the local business interests somewhat... timid, if you get my meaning.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Wave dismissively.)
	if (response == "s_116bd775")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Uh... yes, and good day to you too!
			string_id message = new string_id (c_stringFile, "s_ae7a2a24");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Pirates? Nobody mentioned pirates before...
	if (response == "s_afdd3bc5")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			//-- NPC: Oh absolutely! Naboo space is teeming with deadly gangs of pirates and horrid nebula that will melt your ship in mere seconds. Do be careful out there!
			string_id message = new string_id (c_stringFile, "s_eceb89e1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Wave dismissively.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Pirates? Nobody mentioned pirates before...
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: You say there are merchants to save?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116bd775");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afdd3bc5");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c713935d");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You say there are merchants to save?
	if (response == "s_c713935d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: They're quite safe on their own, I assure you. We only ask that you stay out of sight. Starfighter patrols make the local business interests somewhat... timid, if you get my meaning.
			string_id message = new string_id (c_stringFile, "s_81c3ab31");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Wave dismissively.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Pirates? Nobody mentioned pirates before...
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: You say there are merchants to save?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_116bd775");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afdd3bc5");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c713935d");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch79 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we looking for?
	if (response == "s_c7939f2f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.
			string_id message = new string_id (c_stringFile, "s_ce6b43af");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where am I patrolling?
	if (response == "s_2adb435")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.
			string_id message = new string_id (c_stringFile, "s_9757e73a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll blast any Imperial crosses my path!
	if (response == "s_7a27e11")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Oh dear! Please just stay out of trouble. Come back in one piece!
			string_id message = new string_id (c_stringFile, "s_c89c913b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) May the Force be with you.
	if (response == "s_123652f8")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: That sounds delightful.
			string_id message = new string_id (c_stringFile, "s_25572e24");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch80 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we looking for?
	if (response == "s_c7939f2f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.
			string_id message = new string_id (c_stringFile, "s_ce6b43af");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where am I patrolling?
	if (response == "s_2adb435")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.
			string_id message = new string_id (c_stringFile, "s_9757e73a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll blast any Imperial crosses my path!
	if (response == "s_7a27e11")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Oh dear! Please just stay out of trouble. Come back in one piece!
			string_id message = new string_id (c_stringFile, "s_c89c913b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) May the Force be with you.
	if (response == "s_123652f8")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: That sounds delightful.
			string_id message = new string_id (c_stringFile, "s_25572e24");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch81 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we looking for?
	if (response == "s_c7939f2f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.
			string_id message = new string_id (c_stringFile, "s_ce6b43af");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where am I patrolling?
	if (response == "s_2adb435")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.
			string_id message = new string_id (c_stringFile, "s_9757e73a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll blast any Imperial crosses my path!
	if (response == "s_7a27e11")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Oh dear! Please just stay out of trouble. Come back in one piece!
			string_id message = new string_id (c_stringFile, "s_c89c913b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) May the Force be with you.
	if (response == "s_123652f8")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: That sounds delightful.
			string_id message = new string_id (c_stringFile, "s_25572e24");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch82 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh dear! Please just stay out of trouble. Come back in one piece!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are we looking for?
	if (response == "s_c7939f2f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We have reason to believe that the Empire is moving a wing of TIE fighters directly into an Alliance supply route. We need to know more.
			string_id message = new string_id (c_stringFile, "s_ce6b43af");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where am I patrolling?
	if (response == "s_2adb435")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You'll be covering a little-used Alliance supply route. It's become more important in recent days because of Imperial activity outside the asteroid belt. We have plans for this area, and need to know that we'll remain unseen.
			string_id message = new string_id (c_stringFile, "s_9757e73a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll blast any Imperial crosses my path!
	if (response == "s_7a27e11")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Oh dear! Please just stay out of trouble. Come back in one piece!
			string_id message = new string_id (c_stringFile, "s_c89c913b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) May the Force be with you.
	if (response == "s_123652f8")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: That sounds delightful.
			string_id message = new string_id (c_stringFile, "s_25572e24");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch84 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't worry. I will be fine.
	if (response == "s_92e1301f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Of that I am certain.
			string_id message = new string_id (c_stringFile, "s_35657a01");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What am I looking for?
	if (response == "s_658f174d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Alliance intelligence has plans for this route. I don't know precisely what these plans are - but I assure you that I'll tell you as soon as I know.
			string_id message = new string_id (c_stringFile, "s_85cea100");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How should I stay hidden?
	if (response == "s_93470245")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There are a number of pirates in the area. The Empire is interested in removing these pirate elements, so I suspect your route will be clouded with peril... unfortunately.
			string_id message = new string_id (c_stringFile, "s_1c2c03ae");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch86 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alliance intelligence has plans for this route. I don't know precisely what these plans are - but I assure you that I'll tell you as soon as I know.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't worry. I will be fine.
	if (response == "s_92e1301f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Of that I am certain.
			string_id message = new string_id (c_stringFile, "s_35657a01");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What am I looking for?
	if (response == "s_658f174d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Alliance intelligence has plans for this route. I don't know precisely what these plans are - but I assure you that I'll tell you as soon as I know.
			string_id message = new string_id (c_stringFile, "s_85cea100");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How should I stay hidden?
	if (response == "s_93470245")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There are a number of pirates in the area. The Empire is interested in removing these pirate elements, so I suspect your route will be clouded with peril... unfortunately.
			string_id message = new string_id (c_stringFile, "s_1c2c03ae");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch87 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There are a number of pirates in the area. The Empire is interested in removing these pirate elements, so I suspect your route will be clouded with peril... unfortunately.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't worry. I will be fine.
	if (response == "s_92e1301f")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Of that I am certain.
			string_id message = new string_id (c_stringFile, "s_35657a01");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What am I looking for?
	if (response == "s_658f174d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Alliance intelligence has plans for this route. I don't know precisely what these plans are - but I assure you that I'll tell you as soon as I know.
			string_id message = new string_id (c_stringFile, "s_85cea100");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How should I stay hidden?
	if (response == "s_93470245")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There are a number of pirates in the area. The Empire is interested in removing these pirate elements, so I suspect your route will be clouded with peril... unfortunately.
			string_id message = new string_id (c_stringFile, "s_1c2c03ae");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch88 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Shrug and smile.)
	if (response == "s_d731f764")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Excellent! I see you are in perfect health!
			string_id message = new string_id (c_stringFile, "s_b4155765");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Let's get on with it, buddy.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3a8fb4dd");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 74);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_90377ed4")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: Glorious! Your first mission is a short-range, four-point patrol. When you return, your ship's sensor data will be downloaded to the Alliance 'net for Intel to review.
			string_id message = new string_id (c_stringFile, "s_748d90dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What are we looking for?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where am I patrolling?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I'll blast any Imperial crosses my path!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: (Salute) May the Force be with you.
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c7939f2f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2adb435");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7a27e11");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_123652f8");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I am ready.
	if (response == "s_b04f2893")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

			//-- NPC: You will fly a four-point patrol above Naboo. We will need your ship's sensor data when you are finished.
			string_id message = new string_id (c_stringFile, "s_4d3c3acd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Don't worry. I will be fine.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What am I looking for?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How should I stay hidden?
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92e1301f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_658f174d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_93470245");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 84);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: After you have completed a few assignments for me, then I will train you.  So, are you ready?
			string_id message = new string_id (c_stringFile, "s_8b2a5c11");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: (Shrug and smile.)
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Yes. I am ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch89 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Thank goodness you've returned in one piece! How was your first mission?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No sweat, droid.
	if (response == "s_1f6a762c")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			//-- NPC: You are clearly a superior pilot! Congratulations.
			string_id message = new string_id (c_stringFile, "s_45e27f53");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fighters attacked me!
	if (response == "s_85fcdc55")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: Oh dear! What did you do?
			string_id message = new string_id (c_stringFile, "s_db851c67");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Blasted 'em.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: They fought well, but I fought better.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7d4a858e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_73edb45");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 91);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch91 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh dear! What did you do?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Blasted 'em.
	if (response == "s_7d4a858e")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			//-- NPC: Sounds very exciting! Congratulations!
			string_id message = new string_id (c_stringFile, "s_9f83d209");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They fought well, but I fought better.
	if (response == "s_73edb45")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			//-- NPC: This is good to hear, pilot! Congratulations.
			string_id message = new string_id (c_stringFile, "s_98a1c60d");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch94 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We will need a short while to decode and interpret your starship sensor data. In the meantime, there is more work to be done... if you are ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure. Why not.
	if (response == "s_c92d07b4")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Why not indeed! Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_64308f9f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, sir!
	if (response == "s_b2d473c6")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Um... yes... um... Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_2582d5e6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am more than ready.
	if (response == "s_6fb5a316")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: That is good to hear. Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_314a123f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_241")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, %TU.  There'll be time for training later.  Right now we need you to do another assignment.  I hope you are ready for it.
			string_id message = new string_id (c_stringFile, "s_fb2ee4e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure. Why not.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Yes, sir!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I am more than ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c92d07b4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2d473c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6fb5a316");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_241");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 94);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch95 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why not indeed! Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How thin are we talking?
	if (response == "s_1728f71a")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Four or more will do quite nicely. Focus on the TIE fighters. They are critical to the Empire's operations in Naboo. If you focus on these craft, the Empire will know that their intelligence is compromised and that their operations are in jeopardy. This will buy us some time...
			string_id message = new string_id (c_stringFile, "s_d9c6f8a5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't they travel in big groups though?
	if (response == "s_6c620e99")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We cannot say at this time. Alliance pilots in the Naboo system have reported a wide variety of Imperial activity. You should be able to find some smaller support wings...
			string_id message = new string_id (c_stringFile, "s_1137ccd7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Piece o'cake, metal-mouth!
	if (response == "s_8b4893d6")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			//-- NPC: You're irrepressible! Best of luck!
			string_id message = new string_id (c_stringFile, "s_85c109c3");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch96 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Four or more will do quite nicely. Focus on the TIE fighters. They are critical to the Empire's operations in Naboo. If you focus on these craft, the Empire will know that their intelligence is compromised and that their operations are in jeopardy. This will buy us some time...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How thin are we talking?
	if (response == "s_1728f71a")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Four or more will do quite nicely. Focus on the TIE fighters. They are critical to the Empire's operations in Naboo. If you focus on these craft, the Empire will know that their intelligence is compromised and that their operations are in jeopardy. This will buy us some time...
			string_id message = new string_id (c_stringFile, "s_d9c6f8a5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't they travel in big groups though?
	if (response == "s_6c620e99")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We cannot say at this time. Alliance pilots in the Naboo system have reported a wide variety of Imperial activity. You should be able to find some smaller support wings...
			string_id message = new string_id (c_stringFile, "s_1137ccd7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Piece o'cake, metal-mouth!
	if (response == "s_8b4893d6")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			//-- NPC: You're irrepressible! Best of luck!
			string_id message = new string_id (c_stringFile, "s_85c109c3");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch97 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We cannot say at this time. Alliance pilots in the Naboo system have reported a wide variety of Imperial activity. You should be able to find some smaller support wings...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How thin are we talking?
	if (response == "s_1728f71a")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Four or more will do quite nicely. Focus on the TIE fighters. They are critical to the Empire's operations in Naboo. If you focus on these craft, the Empire will know that their intelligence is compromised and that their operations are in jeopardy. This will buy us some time...
			string_id message = new string_id (c_stringFile, "s_d9c6f8a5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't they travel in big groups though?
	if (response == "s_6c620e99")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We cannot say at this time. Alliance pilots in the Naboo system have reported a wide variety of Imperial activity. You should be able to find some smaller support wings...
			string_id message = new string_id (c_stringFile, "s_1137ccd7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Piece o'cake, metal-mouth!
	if (response == "s_8b4893d6")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			//-- NPC: You're irrepressible! Best of luck!
			string_id message = new string_id (c_stringFile, "s_85c109c3");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch99 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Um... yes... um... Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Only the TIE fighters?
	if (response == "s_cbe69427")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, please. Alliance intelligence 'net is quite clever - you see, the Empire does not yet know that their fleet info has been compromised. Strike their TIE fighters with extreme prejudice, and they will have no choice but to halt their greater strategy.
			string_id message = new string_id (c_stringFile, "s_cc07efa5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is the rally point?
	if (response == "s_97e129cd")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There will be none. You have to search the Naboo system until you find some good targets.
			string_id message = new string_id (c_stringFile, "s_1aaecbcb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) For the Alliance!
	if (response == "s_49bfaf2")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Yes! Please perform this task in the name of the Alliance!
			string_id message = new string_id (c_stringFile, "s_81770bc2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch100 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, please. Alliance intelligence 'net is quite clever - you see, the Empire does not yet know that their fleet info has been compromised. Strike their TIE fighters with extreme prejudice, and they will have no choice but to halt their greater strategy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Only the TIE fighters?
	if (response == "s_cbe69427")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, please. Alliance intelligence 'net is quite clever - you see, the Empire does not yet know that their fleet info has been compromised. Strike their TIE fighters with extreme prejudice, and they will have no choice but to halt their greater strategy.
			string_id message = new string_id (c_stringFile, "s_cc07efa5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is the rally point?
	if (response == "s_97e129cd")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There will be none. You have to search the Naboo system until you find some good targets.
			string_id message = new string_id (c_stringFile, "s_1aaecbcb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) For the Alliance!
	if (response == "s_49bfaf2")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Yes! Please perform this task in the name of the Alliance!
			string_id message = new string_id (c_stringFile, "s_81770bc2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch101 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There will be none. You have to search the Naboo system until you find some good targets.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Only the TIE fighters?
	if (response == "s_cbe69427")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes, please. Alliance intelligence 'net is quite clever - you see, the Empire does not yet know that their fleet info has been compromised. Strike their TIE fighters with extreme prejudice, and they will have no choice but to halt their greater strategy.
			string_id message = new string_id (c_stringFile, "s_cc07efa5");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is the rally point?
	if (response == "s_97e129cd")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There will be none. You have to search the Naboo system until you find some good targets.
			string_id message = new string_id (c_stringFile, "s_1aaecbcb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) For the Alliance!
	if (response == "s_49bfaf2")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Yes! Please perform this task in the name of the Alliance!
			string_id message = new string_id (c_stringFile, "s_81770bc2");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch103 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That is good to hear. Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the location?
	if (response == "s_1809c6fc")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There's none specified in the data I received from our esteemed superiors. You have to search the Naboo system until you find some good targets.
			string_id message = new string_id (c_stringFile, "s_c267cd40");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are my targets?
	if (response == "s_8995f223")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: TIE fighters only. There are other Imperial targets above Naboo, these TIE fighters are special. They are part of the Empire's occupation plans. Once you eliminate these craft - and only these craft - the Empire will know that their secrets are compromised.
			string_id message = new string_id (c_stringFile, "s_576d5b2a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: May the Force be with you.
	if (response == "s_2d4bb338")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Thank you.
			string_id message = new string_id (c_stringFile, "s_b9b27823");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch104 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There's none specified in the data I received from our esteemed superiors. You have to search the Naboo system until you find some good targets.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the location?
	if (response == "s_1809c6fc")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There's none specified in the data I received from our esteemed superiors. You have to search the Naboo system until you find some good targets.
			string_id message = new string_id (c_stringFile, "s_c267cd40");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are my targets?
	if (response == "s_8995f223")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: TIE fighters only. There are other Imperial targets above Naboo, these TIE fighters are special. They are part of the Empire's occupation plans. Once you eliminate these craft - and only these craft - the Empire will know that their secrets are compromised.
			string_id message = new string_id (c_stringFile, "s_576d5b2a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: May the Force be with you.
	if (response == "s_2d4bb338")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Thank you.
			string_id message = new string_id (c_stringFile, "s_b9b27823");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch105 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: TIE fighters only. There are other Imperial targets above Naboo, these TIE fighters are special. They are part of the Empire's occupation plans. Once you eliminate these craft - and only these craft - the Empire will know that their secrets are compromised.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the location?
	if (response == "s_1809c6fc")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: There's none specified in the data I received from our esteemed superiors. You have to search the Naboo system until you find some good targets.
			string_id message = new string_id (c_stringFile, "s_c267cd40");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What are my targets?
	if (response == "s_8995f223")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: TIE fighters only. There are other Imperial targets above Naboo, these TIE fighters are special. They are part of the Empire's occupation plans. Once you eliminate these craft - and only these craft - the Empire will know that their secrets are compromised.
			string_id message = new string_id (c_stringFile, "s_576d5b2a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: May the Force be with you.
	if (response == "s_2d4bb338")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Thank you.
			string_id message = new string_id (c_stringFile, "s_b9b27823");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch107 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not so fast, %TU.  There'll be time for training later.  Right now we need you to do another assignment.  I hope you are ready for it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure. Why not.
	if (response == "s_c92d07b4")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Why not indeed! Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_64308f9f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How thin are we talking?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't they travel in big groups though?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Piece o'cake, metal-mouth!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1728f71a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6c620e99");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8b4893d6");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, sir!
	if (response == "s_b2d473c6")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: Um... yes... um... Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_2582d5e6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Only the TIE fighters?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Where is the rally point?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: (Salute) For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cbe69427");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_97e129cd");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_49bfaf2");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am more than ready.
	if (response == "s_6fb5a316")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

			//-- NPC: That is good to hear. Let's have some revenge on the Empire. Find four of their puny TIE fighters in the Naboo system and destroy them!
			string_id message = new string_id (c_stringFile, "s_314a123f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is the location?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What are my targets?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: May the Force be with you.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1809c6fc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8995f223");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2d4bb338");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_241")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not so fast, %TU.  There'll be time for training later.  Right now we need you to do another assignment.  I hope you are ready for it.
			string_id message = new string_id (c_stringFile, "s_fb2ee4e7");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure. Why not.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Yes, sir!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I am more than ready.
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c92d07b4");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b2d473c6");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6fb5a316");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_241");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 94);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch108 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alliance HQ has spread word of your victory! Congratulations, pilot!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever.
	if (response == "s_c1013aa7")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Yes! Yes indeed!
			string_id message = new string_id (c_stringFile, "s_e50bc223");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Affirmative!
	if (response == "s_247")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			//-- NPC: Affirmative, pilot! Congratulations.
			string_id message = new string_id (c_stringFile, "s_83cb5e35");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you, V3.
	if (response == "s_f0b6493f")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate1");

			//-- NPC: You are welcome, pilot! Congratulations!
			string_id message = new string_id (c_stringFile, "s_d7d27ed7");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch112 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We have decoded your starship sensor data... and we have bad news. The planned Alliance supply route has been thoroughly compromised. We need you to find us another route. Are you ready to take flight again?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, yeah.
	if (response == "s_3f1a842c")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Thank the maker!
			string_id message = new string_id (c_stringFile, "s_a30d25ad");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, sir!
	if (response == "s_255")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Good luck!
			string_id message = new string_id (c_stringFile, "s_a80d9308");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course. Those supplies are critical!
	if (response == "s_1e626caa")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Critical to say the least! We must resume supply flights as soon as possible. To accomplish this, we need you to sweep a five-point area. Once again, your ship sensor data will be used to plot an effective route for our larger transports.
			string_id message = new string_id (c_stringFile, "s_b5caf1b6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Force be with us.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0233b4b");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 115);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_262")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not yet, this is important!  Now are you ready?
			string_id message = new string_id (c_stringFile, "s_5f65d2ef");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, yeah.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Yes, sir!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Of course. Those supplies are critical!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f1a842c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1e626caa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_262");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch115 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Critical to say the least! We must resume supply flights as soon as possible. To accomplish this, we need you to sweep a five-point area. Once again, your ship sensor data will be used to plot an effective route for our larger transports.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Force be with us.
	if (response == "s_d0233b4b")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Agreed!
			string_id message = new string_id (c_stringFile, "s_2099a39c");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch117 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not yet, this is important!  Now are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yeah, yeah.
	if (response == "s_3f1a842c")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Thank the maker!
			string_id message = new string_id (c_stringFile, "s_a30d25ad");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Yes, sir!
	if (response == "s_255")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave2");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Good luck!
			string_id message = new string_id (c_stringFile, "s_a80d9308");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course. Those supplies are critical!
	if (response == "s_1e626caa")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

			//-- NPC: Critical to say the least! We must resume supply flights as soon as possible. To accomplish this, we need you to sweep a five-point area. Once again, your ship sensor data will be used to plot an effective route for our larger transports.
			string_id message = new string_id (c_stringFile, "s_b5caf1b6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Force be with us.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d0233b4b");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 115);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_262")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Not yet, this is important!  Now are you ready?
			string_id message = new string_id (c_stringFile, "s_5f65d2ef");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yeah, yeah.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Yes, sir!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Of course. Those supplies are critical!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want training!
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f1a842c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_255");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1e626caa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_262");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 112);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch118 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alliance command is impressed with your courage, pilot! The main computer specifically mentioned your ability to improvise in a crisis situation. Very good work!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Thank you, sir!
	if (response == "s_9561a5a9")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isWookieeOrIthorian (player, npc))
		{
			//-- NPC: You are welcome, pilot!  As a bonus:  I've had a bandolier made for you.
			string_id message = new string_id (c_stringFile, "s_fbd6b3e6");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: You are welcome, pilot!  As a bonus:  I've had a flightsuit made for you.
			string_id message = new string_id (c_stringFile, "s_cd4363b6");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For the Alliance, my friend.
	if (response == "s_27a8c796")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isWookieeOrIthorian (player, npc))
		{
			//-- NPC: Yes. The Alliance thanks you.  As a little bonus, I've had this bandolier made for you.
			string_id message = new string_id (c_stringFile, "s_9490adec");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yes. The Alliance thanks you.  As a little bonus, I've had this flightsuit made for you.
			string_id message = new string_id (c_stringFile, "s_1fb7129c");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch123 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: While you were away, we discovered some information that will break the back of the Empire's fleet movements here in Naboo. Are you ready to fly again?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I got nothing better to do.
	if (response == "s_ebc0661b")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate1");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: Splendid!
			string_id message = new string_id (c_stringFile, "s_c010439f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So tell me about the job.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let's have some details.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bf1a0f3e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a150013");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 124);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Affirmative!
	if (response == "s_6b490480")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: We have located a rally point from which the Empire has staged their recent aggressions. Go to this point and bring down a veteran TIE fighter pilot.
			string_id message = new string_id (c_stringFile, "s_47ce8fc2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes sir!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a30c1a53");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For the Alliance!
	if (response == "s_a994ca63")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: Yes... We have reliable information about the location of Imperial TIE forces. They are being led by an experienced TIE veteran. Eliminate this TIE veteran pilot with extreme prejudice. This should halt the Empire's plans rather effectively.
			string_id message = new string_id (c_stringFile, "s_da803d9b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Yes, yes, you have my word.  Complete this last assignment for me and I will train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_ca62728c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I got nothing better to do.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebc0661b");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6b490480");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a994ca63");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 123);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch124 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Splendid!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So tell me about the job.
	if (response == "s_bf1a0f3e")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Destroy the veteran TIE pilot leader at a specific rally point. You will receive the waypoint data once you are in space.
			string_id message = new string_id (c_stringFile, "s_74163bd7");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Let's have some details.
	if (response == "s_6a150013")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: The recent Imperial activity is being led by a veteran TIE fighter pilot. We know of his location. Fly to the nav point, and eliminate this pilot with extreme prejudice.
			string_id message = new string_id (c_stringFile, "s_a20f8416");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch127 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We have located a rally point from which the Empire has staged their recent aggressions. Go to this point and bring down a veteran TIE fighter pilot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes sir!
	if (response == "s_a30c1a53")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			//-- NPC: Good luck!
			string_id message = new string_id (c_stringFile, "s_282");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I know this pilot?
	if (response == "s_87a8cca7")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It should not be difficult. He should be one of very few veteran TIE fighter pilots in this vicinity.
			string_id message = new string_id (c_stringFile, "s_ee82c4c3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes sir!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a30c1a53");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch129 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It should not be difficult. He should be one of very few veteran TIE fighter pilots in this vicinity.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes sir!
	if (response == "s_a30c1a53")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			//-- NPC: Good luck!
			string_id message = new string_id (c_stringFile, "s_282");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How will I know this pilot?
	if (response == "s_87a8cca7")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: It should not be difficult. He should be one of very few veteran TIE fighter pilots in this vicinity.
			string_id message = new string_id (c_stringFile, "s_ee82c4c3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes sir!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a30c1a53");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch130 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes... We have reliable information about the location of Imperial TIE forces. They are being led by an experienced TIE veteran. Eliminate this TIE veteran pilot with extreme prejudice. This should halt the Empire's plans rather effectively.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What more do we know of this TIE veteran?
	if (response == "s_cf04e474")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I, myself? Not very much. You may find more than one in the general area - but Alliance intelligence indicates that the Empire has charged one veteran TIE pilot with coordinating aggressions in this system.
			string_id message = new string_id (c_stringFile, "s_4bbbb3ee");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other targets are in the area?
	if (response == "s_d779178a")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Expect the rally point to be patrolled by junior TIE cadets. They will be quick to respond if they see you first - so be very careful. Keep your eye out for the veteran TIE fighter pilot, and eliminate him.
			string_id message = new string_id (c_stringFile, "s_2b74415f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will bring honor to the Alliance!
	if (response == "s_6aadaf18")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: I am quite certain the Alliance appreciates it, as well!
			string_id message = new string_id (c_stringFile, "s_f1c5717b");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch131 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I, myself? Not very much. You may find more than one in the general area - but Alliance intelligence indicates that the Empire has charged one veteran TIE pilot with coordinating aggressions in this system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What more do we know of this TIE veteran?
	if (response == "s_cf04e474")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I, myself? Not very much. You may find more than one in the general area - but Alliance intelligence indicates that the Empire has charged one veteran TIE pilot with coordinating aggressions in this system.
			string_id message = new string_id (c_stringFile, "s_4bbbb3ee");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other targets are in the area?
	if (response == "s_d779178a")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Expect the rally point to be patrolled by junior TIE cadets. They will be quick to respond if they see you first - so be very careful. Keep your eye out for the veteran TIE fighter pilot, and eliminate him.
			string_id message = new string_id (c_stringFile, "s_2b74415f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will bring honor to the Alliance!
	if (response == "s_6aadaf18")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: I am quite certain the Alliance appreciates it, as well!
			string_id message = new string_id (c_stringFile, "s_f1c5717b");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch132 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Expect the rally point to be patrolled by junior TIE cadets. They will be quick to respond if they see you first - so be very careful. Keep your eye out for the veteran TIE fighter pilot, and eliminate him.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What more do we know of this TIE veteran?
	if (response == "s_cf04e474")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I, myself? Not very much. You may find more than one in the general area - but Alliance intelligence indicates that the Empire has charged one veteran TIE pilot with coordinating aggressions in this system.
			string_id message = new string_id (c_stringFile, "s_4bbbb3ee");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What other targets are in the area?
	if (response == "s_d779178a")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Expect the rally point to be patrolled by junior TIE cadets. They will be quick to respond if they see you first - so be very careful. Keep your eye out for the veteran TIE fighter pilot, and eliminate him.
			string_id message = new string_id (c_stringFile, "s_2b74415f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I will bring honor to the Alliance!
	if (response == "s_6aadaf18")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: I am quite certain the Alliance appreciates it, as well!
			string_id message = new string_id (c_stringFile, "s_f1c5717b");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch134 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, yes, you have my word.  Complete this last assignment for me and I will train you.  Are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I got nothing better to do.
	if (response == "s_ebc0661b")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate1");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: Splendid!
			string_id message = new string_id (c_stringFile, "s_c010439f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: So tell me about the job.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Let's have some details.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bf1a0f3e");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6a150013");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 124);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: (Salute) Affirmative!
	if (response == "s_6b490480")
	{
		doAnimationAction (player, "salute2");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: We have located a rally point from which the Empire has staged their recent aggressions. Go to this point and bring down a veteran TIE fighter pilot.
			string_id message = new string_id (c_stringFile, "s_47ce8fc2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes sir!
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How will I know this pilot?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a30c1a53");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_87a8cca7");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: For the Alliance!
	if (response == "s_a994ca63")
	{
		doAnimationAction (player, "bow");

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

			//-- NPC: Yes... We have reliable information about the location of Imperial TIE forces. They are being led by an experienced TIE veteran. Eliminate this TIE veteran pilot with extreme prejudice. This should halt the Empire's plans rather effectively.
			string_id message = new string_id (c_stringFile, "s_da803d9b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What more do we know of this TIE veteran?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What other targets are in the area?
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I will bring honor to the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf04e474");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d779178a");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6aadaf18");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: Yes, yes, you have my word.  Complete this last assignment for me and I will train you.  Are you ready?
			string_id message = new string_id (c_stringFile, "s_ca62728c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I got nothing better to do.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: (Salute) Affirmative!
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: For the Alliance!
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse3 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ebc0661b");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6b490480");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a994ca63");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 123);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch136 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have performed wonderfully! Alliance HQ has authorized me to provide your first starship training module.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Training module?
	if (response == "s_342")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Indeed. It will instruct you in the use of basic starfighters. Are you adequately prepared?
			string_id message = new string_id (c_stringFile, "s_343");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_344");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 137);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch137 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Indeed. It will instruct you in the use of basic starfighters. Are you adequately prepared?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_344")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isGenderMale (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantSkill1 (player, npc);

			//-- NPC: Congratulations. Good work, sir!
			string_id message = new string_id (c_stringFile, "s_346");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate1");

			naboo_rebel_trainer_1_action_grantSkill1 (player, npc);

			//-- NPC: Congratulations. Excellent work, ma'am.
			string_id message = new string_id (c_stringFile, "s_348");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch140 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Greetings, pilot. You are performing exceptionally well! It is my pleasure to provide you with your next training module.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What sort of training module?
	if (response == "s_349")
	{
		//-- [NOTE] 
		if (!naboo_rebel_trainer_1_condition_hasSTARSHIPS1 (player, npc))
		{
			//-- NPC: This module will instruct you in the use of basic starfighters. Are you adequately prepared?
			string_id message = new string_id (c_stringFile, "s_350");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_354");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 141);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_rebel_trainer_1_condition_hasWEAPONS1 (player, npc))
		{
			//-- NPC: This module will instruct you in the use of basic starship components. Are you adequately prepared?
			string_id message = new string_id (c_stringFile, "s_351");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_356");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 144);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_rebel_trainer_1_condition_hasPROCEDURES1 (player, npc))
		{
			//-- NPC: This module will instruct you in the use of starfighter survival tactics. Are you adequately prepared?
			string_id message = new string_id (c_stringFile, "s_352");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_358");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 146);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: This module will instruct you in the use of droid interface basics. Are you adequately prepared?
			string_id message = new string_id (c_stringFile, "s_353");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_360");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 148);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch141 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This module will instruct you in the use of basic starfighters. Are you adequately prepared?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_354")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isGenderMale (player, npc))
		{
			doAnimationAction (npc, "celebrate");

			naboo_rebel_trainer_1_action_grantSkill1 (player, npc);

			//-- NPC: Congratulations. Good work, sir!
			string_id message = new string_id (c_stringFile, "s_362");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "celebrate1");

			naboo_rebel_trainer_1_action_grantSkill1 (player, npc);

			//-- NPC: Congratulations. Excellent work, ma'am.
			string_id message = new string_id (c_stringFile, "s_364");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch144 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This module will instruct you in the use of basic starship components. Are you adequately prepared?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_356")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			naboo_rebel_trainer_1_action_grantSkill2 (player, npc);

			//-- NPC: Splendid! You are now rated for more powerful components. Use them well!
			string_id message = new string_id (c_stringFile, "s_366");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch146 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This module will instruct you in the use of starfighter survival tactics. Are you adequately prepared?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_358")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			naboo_rebel_trainer_1_action_grantSkill3 (player, npc);

			//-- NPC: I have removed the code-lock on your procedures manual. Secret techniques are now available to you.
			string_id message = new string_id (c_stringFile, "s_368");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch148 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: This module will instruct you in the use of droid interface basics. Are you adequately prepared?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_360")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isGenderMale (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			naboo_rebel_trainer_1_action_grantSkill4 (player, npc);

			//-- NPC: How noble of you, sir! Droid programming is a critical skill for the Alliance pilot.
			string_id message = new string_id (c_stringFile, "s_370");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			naboo_rebel_trainer_1_action_grantSkill4 (player, npc);

			//-- NPC: How noble of you, ma'am! Droid programming is a most respectable vocation.
			string_id message = new string_id (c_stringFile, "s_372");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch151 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Greetings, my friend. You will be pleased to know that Alliance High Command has authorized me to offer you a selection of combat duty missions. Are you interested in a duty mission?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is an excellent way of earning experience towards your next training session. No good trainer would teach a pilot with no experience. Duty missions have no real end to them, you simply finish whenever you are ready. Would you like a duty mission?
			string_id message = new string_id (c_stringFile, "s_96184c1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I want to enlist with the ground forces
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_761039fa");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 151);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am interested in a duty mission.
	if (response == "s_4ffd4b9d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: That is most excellent to hear. We have two mission types available. You may choose to patrol the system and destroy any Imperials that you find, or you may choose to help escort our supply ships. Which would you like?
			string_id message = new string_id (c_stringFile, "s_614176fe");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would like to patrol Naboo space.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Our transports could use my help.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf63ee80");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a66ff602");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 153);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to enlist with the ground forces
	if (response == "s_761039fa")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_cackle");

			//-- NPC: Finally decided to become a REAL Rebel, huh?  Well it's about time.  You do realize that enlisting with the ground forces means you could be attacked when you least expect it... even if you are a covert operative, there's still a danger!
			string_id message = new string_id (c_stringFile, "s_81eaed4e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not afraid
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5f13c4");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 157);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch152 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A duty mission is an excellent way of earning experience towards your next training session. No good trainer would teach a pilot with no experience. Duty missions have no real end to them, you simply finish whenever you are ready. Would you like a duty mission?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is an excellent way of earning experience towards your next training session. No good trainer would teach a pilot with no experience. Duty missions have no real end to them, you simply finish whenever you are ready. Would you like a duty mission?
			string_id message = new string_id (c_stringFile, "s_96184c1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I am interested in a duty mission.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: I want to enlist with the ground forces
			boolean hasResponse2 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_761039fa");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 151);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am interested in a duty mission.
	if (response == "s_4ffd4b9d")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: That is most excellent to hear. We have two mission types available. You may choose to patrol the system and destroy any Imperials that you find, or you may choose to help escort our supply ships. Which would you like?
			string_id message = new string_id (c_stringFile, "s_614176fe");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would like to patrol Naboo space.
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Our transports could use my help.
			boolean hasResponse1 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cf63ee80");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a66ff602");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 153);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want to enlist with the ground forces
	if (response == "s_761039fa")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_cackle");

			//-- NPC: Finally decided to become a REAL Rebel, huh?  Well it's about time.  You do realize that enlisting with the ground forces means you could be attacked when you least expect it... even if you are a covert operative, there's still a danger!
			string_id message = new string_id (c_stringFile, "s_81eaed4e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not afraid
			boolean hasResponse0 = false;
			if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e5f13c4");

				utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 157);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch153 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That is most excellent to hear. We have two mission types available. You may choose to patrol the system and destroy any Imperials that you find, or you may choose to help escort our supply ships. Which would you like?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would like to patrol Naboo space.
	if (response == "s_cf63ee80")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition_isGenderMale (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantDUTY_Destroy1 (player, npc);

			//-- NPC: Affirmative, sir. I have placed your ID on the duty roster. You are free to sweep and clear Imperial warships in the area. You can return to the ground at any time when you feel that you have learned enough.
			string_id message = new string_id (c_stringFile, "s_ca5e8b8b");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantDUTY_Destroy1 (player, npc);

			//-- NPC: Yes, ma'am! I have uploaded your ID to the system duty roster. You are cleared to sweep Naboo space for Imperial starships. You can return to the ground at any time when you feel that you have learned enough. Good luck!
			string_id message = new string_id (c_stringFile, "s_284dc5d3");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Our transports could use my help.
	if (response == "s_a66ff602")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			naboo_rebel_trainer_1_action_grantDUTY_Escort1 (player, npc);

			//-- NPC: Agreed! I will put your ID on the escort duty roster. You can return to the ground at any time when you feel that you have learned enough. Take flight whenever you wish.
			string_id message = new string_id (c_stringFile, "s_6c399378");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_rebel_trainer_1_handleBranch157 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Finally decided to become a REAL Rebel, huh?  Well it's about time.  You do realize that enlisting with the ground forces means you could be attacked when you least expect it... even if you are a covert operative, there's still a danger!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not afraid
	if (response == "s_e5f13c4")
	{
		//-- [NOTE] 
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "applause_polite");

			//-- NPC: Good for you!  Alliance command will be happy to have you as an asset both in space and planetside! You can change your level of commitment to the Rebellion using the /pvp command. This will allow you to switch between being a combatant and in special forces.
			string_id message = new string_id (c_stringFile, "s_2ff8ef01");
			utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.naboo_rebel_trainer_1");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );
	factions.setFaction( self, factions.FACTION_REBEL );

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );
	factions.setFaction( self, factions.FACTION_REBEL );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );
	
	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.naboo_rebel_trainer_1");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_hasVeryFirstQuest (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: How can I be of service? I have added your first assignment to your log... and I have uploaded codes for a ship control device in your datapad.  Go to the Starport and access the terminal to launch into space... unless I forgot something?
		string_id message = new string_id (c_stringFile, "s_1323cdc1");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Where do I go?
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: How do I get back?
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Where is my ship?
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 1);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_hasSpaceExpansion (player, npc))
	{
		doAnimationAction (npc, "greet");

		naboo_rebel_trainer_1_action_anim_explode (player, npc);

		//-- NPC: Greeting, Sir.  Ma'am?  Pardon me.  I'm terribly sorry.  I cannot help you with that.  *bzzt*
		string_id message = new string_id (c_stringFile, "s_eedd7284");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_isPrivateerPilot (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: Oh. Excuse me. I didn't see you there.
		string_id message = new string_id (c_stringFile, "s_968b4bae");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What are you doing here?
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Who are you?
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I am looking for pilot training.
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b26194cb");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ca776e30");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1a53f653");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 6);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_isImperialPilot (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: Greetings. Can I be of service?
		string_id message = new string_id (c_stringFile, "s_85e2e26d");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I have no use for you.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I need answers, droid.
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: (Glare in silent hatred.)
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a425f33d");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c16bab26");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9f6cf52");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 12);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_isNotRebel (player, npc))
	{
		//-- NPC: Excuse me. I cannot locate any record of you registering yourself with the Alliance. Please visit a Rebel recruiter to correct this issue.
		string_id message = new string_id (c_stringFile, "s_326");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] NABOO Rebel Trainer - V3-FX
	if (!naboo_rebel_trainer_1_condition_hasNoviceSkillBox (player, npc))
	{
		doAnimationAction (npc, "greet");

		naboo_rebel_trainer_1_action_anim_greet (player, npc);

		//-- NPC: Greetings. I am V3-FX, Alliance Pilot Relations. May I interest you in our starfighter training program?
		string_id message = new string_id (c_stringFile, "s_cd448c20");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sounds like fun. Where do I sign?
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I want to fly for the Alliance!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Yes. I am ready to train.
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_78679b31");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1a0322d9");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_616cd1dd");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 17);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_onMyTrack (player, npc))
	{
		//-- NPC: Oh. You are an Alliance pilot, but I do not recognize you. How may I be of service?
		string_id message = new string_id (c_stringFile, "s_8c6a4b66");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are there any missions I can fly?
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ac05bfba");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 53);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_hasSpaceShip (player, npc))
	{
		doAnimationAction (npc, "explain");

		naboo_rebel_trainer_1_action_grantSpaceShip (player, npc);

		//-- NPC: You will need a ship, in order to be a pilot.  I will add the control codes to your datapad for a simple Z95.  I hope that it serves you well.
		string_id message = new string_id (c_stringFile, "s_909a1feb");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You are a member of the Smuggler's Alliance and you already have a quest.  Go do it!
	if (naboo_rebel_trainer_1_condition_hasRebelQuest (player, npc))
	{
		doAnimationAction (npc, "shake_head_no");

		//-- NPC: The duty logs indicate that you are in mid-mission. We have nothing to discuss until you are finished.
		string_id message = new string_id (c_stringFile, "s_e31d65b7");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_has_GRADUATED_goto_DANTOOINE (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Hello there! It is good to see you, although I believe you are supposed to be on planet Dantooine... correct?
		string_id message = new string_id (c_stringFile, "s_b7b55289");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Where?
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Requesting more info, sir!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: How do I get there?
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_741df7ed");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2be1e8b");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b3bebc5e");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 58);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have all the tier-1 skills, so would you like some tier-2 content?
	if (naboo_rebel_trainer_1_condition_hasAllTierOneSkills (player, npc))
	{
		doAnimationAction (npc, "celebrate1");

		naboo_rebel_trainer_1_action_set_GRADUATED_goto_DANTOOINE (player, npc);

		//-- NPC: Congratulations, pilot! You have successfully completed the first tier of the Alliance starfighter training program. Your next assignment will take you to planet Dantooine and place you under the guidance of Brother Vrovel... leader of the Vortex organization.
		string_id message = new string_id (c_stringFile, "s_2e5c9032");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: You got it, metal head!
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What is 'Vortex?'
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Who is Brother Vrovel?
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Goodbye, V3.
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: Where do I find this Brother Vrovel?
		boolean hasResponse4 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f75b511f");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4efbdbfa");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b89c30f1");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1df40ac6");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8334632");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 62);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_hasFailedQuestOne (player, npc))
	{
		doAnimationAction (npc, "explain");

		naboo_rebel_trainer_1_action_grantQuestOne (player, npc);

		//-- NPC: Alliance HQ has requested that I assign a new mission to you... since you have failed the previous. Good luck!
		string_id message = new string_id (c_stringFile, "s_b1ab42f0");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Shrug and smile.)
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Affirmative!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Yes. I am ready.
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want training!
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_hasFailedQuestTwo (player, npc))
	{
		doAnimationAction (npc, "explain");

		naboo_rebel_trainer_1_action_grantQuestTwo (player, npc);

		//-- NPC: The Alliance HQ computer system is rather disappointed with your performance. I have been instructed to renew your mission assignment. Good luck!
		string_id message = new string_id (c_stringFile, "s_f4d0e976");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sure. Why not.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Yes, sir!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I am more than ready.
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want training!
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c92d07b4");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b2d473c6");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6fb5a316");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_241");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 94);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_hasFailedQuestThree (player, npc))
	{
		doAnimationAction (npc, "explain");

		naboo_rebel_trainer_1_action_grantQuestThree (player, npc);

		//-- NPC: I have been ordered to reassign a mission to you. The mission you most recently failed. My apologies. And... good luck!
		string_id message = new string_id (c_stringFile, "s_c7626ea8");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yeah, yeah.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Yes, sir!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Of course. Those supplies are critical!
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want training!
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3f1a842c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_255");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1e626caa");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_262");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 112);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_hasFailedQuestFour (player, npc))
	{
		doAnimationAction (npc, "explain");

		naboo_rebel_trainer_1_action_grantQuestFour (player, npc);

		//-- NPC: Alliance HQ demands that you rejoin battle against the TIE Veteran so that our campaign may move forward. Good luck!
		string_id message = new string_id (c_stringFile, "s_54d18017");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I got nothing better to do.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Affirmative!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: For the Alliance!
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Will you train me after this?
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ebc0661b");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6b490480");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a994ca63");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 123);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] grantQuest 1
	if (!naboo_rebel_trainer_1_condition_hasCompletedQuestOne (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Salutations, pilot! I hope you are well-rested and ready for your first assignment for the Alliance training system.
		string_id message = new string_id (c_stringFile, "s_79a06a16");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Shrug and smile.)
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Affirmative!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Yes. I am ready.
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want training!
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d731f764");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_90377ed4");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b04f2893");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 73);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_beenRewardedForQuestOne (player, npc))
	{
		doAnimationAction (npc, "greet");

		naboo_rebel_trainer_1_action_reward_quest_1 (player, npc);

		//-- NPC: Thank goodness you've returned in one piece! How was your first mission?
		string_id message = new string_id (c_stringFile, "s_f14340ef");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No sweat, droid.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Fighters attacked me!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1f6a762c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_85fcdc55");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 89);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_hasCompletedQuestTwo (player, npc))
	{
		//-- NPC: We will need a short while to decode and interpret your starship sensor data. In the meantime, there is more work to be done... if you are ready?
		string_id message = new string_id (c_stringFile, "s_36fa01ee");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sure. Why not.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Yes, sir!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I am more than ready.
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want training!
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c92d07b4");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b2d473c6");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6fb5a316");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_241");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 94);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_beenRewardedForQuestTwo (player, npc))
	{
		doAnimationAction (npc, "celebrate");

		naboo_rebel_trainer_1_action_reward_quest_2 (player, npc);

		//-- NPC: Alliance HQ has spread word of your victory! Congratulations, pilot!
		string_id message = new string_id (c_stringFile, "s_76fb5d49");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Whatever.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Affirmative!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Thank you, V3.
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c1013aa7");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_247");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f0b6493f");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 108);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_hasCompletedQuestThree (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: We have decoded your starship sensor data... and we have bad news. The planned Alliance supply route has been thoroughly compromised. We need you to find us another route. Are you ready to take flight again?
		string_id message = new string_id (c_stringFile, "s_b0047226");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yeah, yeah.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Yes, sir!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Of course. Those supplies are critical!
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want training!
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3f1a842c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_255");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1e626caa");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_262");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 112);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_beenRewardedForQuestThree (player, npc))
	{
		doAnimationAction (npc, "applause_polite");

		naboo_rebel_trainer_1_action_reward_quest_3 (player, npc);

		//-- NPC: Alliance command is impressed with your courage, pilot! The main computer specifically mentioned your ability to improvise in a crisis situation. Very good work!
		string_id message = new string_id (c_stringFile, "s_7ad400bc");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: (Salute) Thank you, sir!
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: For the Alliance, my friend.
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9561a5a9");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_27a8c796");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 118);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_hasCompletedQuestFour (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: While you were away, we discovered some information that will break the back of the Empire's fleet movements here in Naboo. Are you ready to fly again?
		string_id message = new string_id (c_stringFile, "s_999f2284");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I got nothing better to do.
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: (Salute) Affirmative!
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: For the Alliance!
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Will you train me after this?
		boolean hasResponse3 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ebc0661b");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6b490480");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a994ca63");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 123);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_rebel_trainer_1_condition_beenRewardedForQuestFour (player, npc))
	{
		doAnimationAction (npc, "applause_polite");

		naboo_rebel_trainer_1_action_reward_quest_4 (player, npc);

		//-- NPC: The Alliance HQ computer system has informed me that the TIE Veteran pilot has been defeated. Very good work, my friend. Very good, indeed!
		string_id message = new string_id (c_stringFile, "s_61836246");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_isReadyForFirstTraining (player, npc))
	{
		doAnimationAction (npc, "applause_excited");

		//-- NPC: You have performed wonderfully! Alliance HQ has authorized me to provide your first starship training module.
		string_id message = new string_id (c_stringFile, "s_6f3e9d4b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Training module?
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_342");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 136);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition_isReadyForMoreTraining (player, npc))
	{
		doAnimationAction (npc, "nod_head_once");

		//-- NPC: Greetings, pilot. You are performing exceptionally well! It is my pleasure to provide you with your next training module.
		string_id message = new string_id (c_stringFile, "s_838cca30");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What sort of training module?
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_349");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 140);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Greetings, my friend. You will be pleased to know that Alliance High Command has authorized me to offer you a selection of combat duty missions. Are you interested in a duty mission?
		string_id message = new string_id (c_stringFile, "s_400d7391");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What is a duty mission?
		boolean hasResponse0 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I am interested in a duty mission.
		boolean hasResponse1 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I want to enlist with the ground forces
		boolean hasResponse2 = false;
		if (naboo_rebel_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4ffd4b9d");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_761039fa");

			utils.setScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId", 151);

			npcStartConversation (player, npc, "naboo_rebel_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "naboo_rebel_trainer_1")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

	if (branchId == 1 && naboo_rebel_trainer_1_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && naboo_rebel_trainer_1_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && naboo_rebel_trainer_1_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && naboo_rebel_trainer_1_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && naboo_rebel_trainer_1_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && naboo_rebel_trainer_1_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && naboo_rebel_trainer_1_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && naboo_rebel_trainer_1_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && naboo_rebel_trainer_1_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && naboo_rebel_trainer_1_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && naboo_rebel_trainer_1_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && naboo_rebel_trainer_1_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && naboo_rebel_trainer_1_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && naboo_rebel_trainer_1_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && naboo_rebel_trainer_1_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && naboo_rebel_trainer_1_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && naboo_rebel_trainer_1_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && naboo_rebel_trainer_1_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && naboo_rebel_trainer_1_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && naboo_rebel_trainer_1_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && naboo_rebel_trainer_1_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && naboo_rebel_trainer_1_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && naboo_rebel_trainer_1_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && naboo_rebel_trainer_1_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 34 && naboo_rebel_trainer_1_handleBranch34 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && naboo_rebel_trainer_1_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && naboo_rebel_trainer_1_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && naboo_rebel_trainer_1_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 38 && naboo_rebel_trainer_1_handleBranch38 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && naboo_rebel_trainer_1_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 40 && naboo_rebel_trainer_1_handleBranch40 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && naboo_rebel_trainer_1_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && naboo_rebel_trainer_1_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && naboo_rebel_trainer_1_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 44 && naboo_rebel_trainer_1_handleBranch44 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && naboo_rebel_trainer_1_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 46 && naboo_rebel_trainer_1_handleBranch46 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && naboo_rebel_trainer_1_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 48 && naboo_rebel_trainer_1_handleBranch48 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 49 && naboo_rebel_trainer_1_handleBranch49 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 50 && naboo_rebel_trainer_1_handleBranch50 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 51 && naboo_rebel_trainer_1_handleBranch51 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && naboo_rebel_trainer_1_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 53 && naboo_rebel_trainer_1_handleBranch53 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && naboo_rebel_trainer_1_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && naboo_rebel_trainer_1_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && naboo_rebel_trainer_1_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && naboo_rebel_trainer_1_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 65 && naboo_rebel_trainer_1_handleBranch65 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 68 && naboo_rebel_trainer_1_handleBranch68 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 69 && naboo_rebel_trainer_1_handleBranch69 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 70 && naboo_rebel_trainer_1_handleBranch70 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 71 && naboo_rebel_trainer_1_handleBranch71 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 72 && naboo_rebel_trainer_1_handleBranch72 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 73 && naboo_rebel_trainer_1_handleBranch73 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 74 && naboo_rebel_trainer_1_handleBranch74 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 75 && naboo_rebel_trainer_1_handleBranch75 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 77 && naboo_rebel_trainer_1_handleBranch77 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 78 && naboo_rebel_trainer_1_handleBranch78 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 79 && naboo_rebel_trainer_1_handleBranch79 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 80 && naboo_rebel_trainer_1_handleBranch80 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 81 && naboo_rebel_trainer_1_handleBranch81 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 82 && naboo_rebel_trainer_1_handleBranch82 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 84 && naboo_rebel_trainer_1_handleBranch84 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 86 && naboo_rebel_trainer_1_handleBranch86 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 87 && naboo_rebel_trainer_1_handleBranch87 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 88 && naboo_rebel_trainer_1_handleBranch88 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 89 && naboo_rebel_trainer_1_handleBranch89 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 91 && naboo_rebel_trainer_1_handleBranch91 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 94 && naboo_rebel_trainer_1_handleBranch94 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 95 && naboo_rebel_trainer_1_handleBranch95 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 96 && naboo_rebel_trainer_1_handleBranch96 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 97 && naboo_rebel_trainer_1_handleBranch97 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 99 && naboo_rebel_trainer_1_handleBranch99 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 100 && naboo_rebel_trainer_1_handleBranch100 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 101 && naboo_rebel_trainer_1_handleBranch101 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 103 && naboo_rebel_trainer_1_handleBranch103 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 104 && naboo_rebel_trainer_1_handleBranch104 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 105 && naboo_rebel_trainer_1_handleBranch105 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 107 && naboo_rebel_trainer_1_handleBranch107 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 108 && naboo_rebel_trainer_1_handleBranch108 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 112 && naboo_rebel_trainer_1_handleBranch112 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 115 && naboo_rebel_trainer_1_handleBranch115 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 117 && naboo_rebel_trainer_1_handleBranch117 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 118 && naboo_rebel_trainer_1_handleBranch118 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 123 && naboo_rebel_trainer_1_handleBranch123 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 124 && naboo_rebel_trainer_1_handleBranch124 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 127 && naboo_rebel_trainer_1_handleBranch127 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 129 && naboo_rebel_trainer_1_handleBranch129 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 130 && naboo_rebel_trainer_1_handleBranch130 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 131 && naboo_rebel_trainer_1_handleBranch131 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 132 && naboo_rebel_trainer_1_handleBranch132 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 134 && naboo_rebel_trainer_1_handleBranch134 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 136 && naboo_rebel_trainer_1_handleBranch136 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 137 && naboo_rebel_trainer_1_handleBranch137 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 140 && naboo_rebel_trainer_1_handleBranch140 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 141 && naboo_rebel_trainer_1_handleBranch141 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 144 && naboo_rebel_trainer_1_handleBranch144 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 146 && naboo_rebel_trainer_1_handleBranch146 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 148 && naboo_rebel_trainer_1_handleBranch148 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 151 && naboo_rebel_trainer_1_handleBranch151 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 152 && naboo_rebel_trainer_1_handleBranch152 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 153 && naboo_rebel_trainer_1_handleBranch153 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 157 && naboo_rebel_trainer_1_handleBranch157 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.naboo_rebel_trainer_1.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

