// ======================================================================
//
// corvette_tallon_rebel1.script
// Copyright 2003, Sony Online Entertainment
// All Rights Reserved.
//
// DO NOT EDIT THIS AUTO-GENERATED FILE.  PLEASE USE THE CONVERATION EDITOR!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.space_dungeon;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/corvette_tallon_rebel1";

// ======================================================================
// Script Conditions
// ======================================================================

boolean corvette_tallon_rebel1_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_knowsLocation3 (obj_id player, obj_id npc)
{
	return utils.hasScriptVar(player, "corl_corvette.heardLocation3");
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_knowsLocation2 (obj_id player, obj_id npc)
{
	return utils.hasScriptVar(player, "corl_corvette.heardLocation2");
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_knowsLocation1 (obj_id player, obj_id npc)
{
	return utils.hasScriptVar(player, "corl_corvette.heardLocation1");
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_knowsAllLocations (obj_id player, obj_id npc)
{
	return ( (utils.hasScriptVar(player, "corl_corvette.heardLocation1"))
			&& (utils.hasScriptVar(player, "corl_corvette.heardLocation2"))
			&& (utils.hasScriptVar(player, "corl_corvette.heardLocation3")) );
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_hasStuff (obj_id player, obj_id npc)
{
	return ( (utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_intel.iff"))
			|| (utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_filler01.iff"))
			|| (utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_filler02.iff")) );
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_hasObject01 (obj_id player, obj_id npc)
{
	return utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_intel.iff");
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_hasObject02 (obj_id player, obj_id npc)
{
	return utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_filler01.iff");
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_hasObject03 (obj_id player, obj_id npc)
{
	return utils.playerHasItemByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_filler02.iff");
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_notRebel (obj_id player, obj_id npc)
{
	string playerFaction = factions.getFaction(player);
	if ( playerFaction == null || !playerFaction.equals("Rebel") )
		return true;
	
	return false;
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_hasTravelTicket (obj_id player, obj_id npc)
{
	string ticketTemplate = "object/tangible/travel/travel_ticket/dungeon_ticket.iff";
	obj_id[] spaceTickets = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, ticketTemplate);
	if (spaceTickets != null && spaceTickets.length > 0 )
	{
		for (int i = 0; i < spaceTickets.length; i++)
		{
			obj_id ticket = spaceTickets[i];
			if ( isIdValid(ticket) )
			{
				if (getStringObjVar (ticket, "space_dungeon.ticket.dungeon").equals ("corvette_rebel"))
					return true;
			}
		}
	}
	return false;
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (obj_id player, obj_id npc)
{
	return ( (!corvette_tallon_rebel1_condition_hasTravelTicket(player, npc)) && (!corvette_tallon_rebel1_condition_hasStuff(player, npc)) );
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_isOnQuest (obj_id player, obj_id npc)
{
	return hasObjVar(player, "corl_corvette.rebel_rescue");
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_dungeonInactive (obj_id player, obj_id npc)
{
	string isDungeonActive = getConfigSetting("Dungeon", "Corellian_Corvette_Rebel");
	if ( isDungeonActive == null || isDungeonActive == "false" || isDungeonActive == "0")
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_onDifferentCorvetteQuest (obj_id player, obj_id npc)
{
	if ( hasObjVar(player, "corl_corvette") )
	{
		if ( !hasObjVar(player, "corl_corvette.rebel_rescue") )
			return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_earnedCorvetteReward (obj_id player, obj_id npc)
{
	return hasObjVar(player, "corvette.rebel_rescue.finished");
}

// ----------------------------------------------------------------------

boolean corvette_tallon_rebel1_condition_hasInventorySpace (obj_id player, obj_id npc)
{
	obj_id playerInv = utils.getInventoryContainer(player);
	if ( isIdValid(playerInv) )
	{
		int free_space = getVolumeFree(playerInv);
		if (free_space > 0 )
		{
			return true;
		}
	}
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void corvette_tallon_rebel1_action__defaultAction (obj_id player, obj_id npc)
{
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_heardLocation1 (obj_id player, obj_id npc)
{
	utils.setScriptVar(player, "corl_corvette.heardLocation1", 1);
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_heardLocation2 (obj_id player, obj_id npc)
{
	utils.setScriptVar(player, "corl_corvette.heardLocation2", 1);
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_heardLocation3 (obj_id player, obj_id npc)
{
	utils.setScriptVar(player, "corl_corvette.heardLocation3", 1);
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_acceptsQuest (obj_id player, obj_id npc)
{
	setObjVar(player, "corl_corvette.rebel_rescue", 1);
	
	string custLogMsg = "*Corvette Ground Quest: Player %TU has started the rebel rescue quest.";
	CustomerServiceLog("DUNGEON_CorellianCorvette", custLogMsg, player);
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_clearLocations (obj_id player, obj_id npc)
{
	if ( utils.hasScriptVar(player, "corl_corvette.heardLocation1") )
		utils.removeScriptVar( player, "corl_corvette.heardLocation1");
		
	if ( utils.hasScriptVar(player, "corl_corvette.heardLocation2") )
		utils.removeScriptVar( player, "corl_corvette.heardLocation2");
		
	if ( utils.hasScriptVar(player, "corl_corvette.heardLocation3") )
		utils.removeScriptVar( player, "corl_corvette.heardLocation3");
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_giveTicket (obj_id player, obj_id npc)
{
	obj_id document = utils.getItemPlayerHasByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_intel.iff");
	if ( isIdValid(document) )
	{
		destroyObject(document);
		
		obj_id ticket = space_dungeon.createTicket (player, "corellia", "corvette_rebel_pilot", "corvette_rebel");
		setObjVar(ticket, "space_dungeon.ticket.quest_type", "rebel_rescue");
		setObjVar(ticket, "corl_corvette.ticket_owner", player);
		setObjVar(ticket, "noTrade", true);
		attachScript(ticket, "item.special.nomove");
		attachScript(ticket, "theme_park.dungeon.corvette.corvette_quest_cleanup");

		string custLogMsg = "*Corvette Ground Quest: Player %TU finished the rebel rescue quest and received a rebel corvette ticket.";
		CustomerServiceLog("DUNGEON_CorellianCorvette", custLogMsg, player);
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_quitCorvetteQuest (obj_id player, obj_id npc)
{
	if ( hasObjVar (player, "corl_corvette") )
		removeObjVar (player, "corl_corvette");
		
	string custLogMsg = "*Corvette Ground Quest: Player %TU aborted the rebel rescue quest.";
	CustomerServiceLog("DUNGEON_CorellianCorvette", custLogMsg, player);
	
	obj_id document = null;
	string intelTemplate = "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_intel.iff";
	obj_id[] intelDocuments = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, intelTemplate);
	if ( intelDocuments != null && intelDocuments.length > 0 )
	{
		for ( int i = 0; i < intelDocuments.length; i++ )
		{
			document = intelDocuments[i];
			if ( isIdValid(document) )
				destroyObject(document);
		}
	}
	
	string filler01Template = "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_filler01.iff";
	obj_id[] filler01Documents = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, filler01Template);
	if ( filler01Documents != null && filler01Documents.length > 0 )
	{
		for ( int i = 0; i < filler01Documents.length; i++ )
		{
			document = filler01Documents[i];
			if ( isIdValid(document) )
				destroyObject(document);
		}
	}

	string filler02Template = "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_filler02.iff";
	obj_id[] filler02Documents = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, filler02Template);
	if ( filler02Documents != null && filler02Documents.length > 0 )
	{
		for ( int i = 0; i < filler02Documents.length; i++ )
		{
			document = filler02Documents[i];
			if ( isIdValid(document) )
				destroyObject(document);
		}
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_takeObject2 (obj_id player, obj_id npc)
{
	obj_id document = utils.getItemPlayerHasByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_filler01.iff");
	if ( isIdValid(document) )
	{
		destroyObject(document);
			
		string factionReward = "Rebel";
		if (factionReward != "none")
		{
			int factionAmt = 100;
			if (factionAmt != 0)
			{
				factions.addFactionStanding(player,factionReward,factionAmt);
			}
		}
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_takeObject3 (obj_id player, obj_id npc)
{
	obj_id document = utils.getItemPlayerHasByTemplate(player, "object/tangible/loot/dungeon/corellian_corvette/rebel_rescue_filler02.iff");
	if( isIdValid(document) )
	{
		destroyObject(document);
		
		string factionReward = "Rebel";
		if (factionReward != "none")
		{
			int factionAmt = 100;
			if (factionAmt != 0)
			{
				factions.addFactionStanding(player,factionReward,factionAmt);
			}
		}
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_removeTicket (obj_id player, obj_id npc)
{
	corvette_tallon_rebel1_action_quitCorvetteQuest(player, npc);
	
	string ticketTemplate = "object/tangible/travel/travel_ticket/dungeon_ticket.iff";
	obj_id[] spaceTickets = utils.getAllItemsPlayerHasByTemplateInBankAndInventory(player, ticketTemplate);
	if (spaceTickets != null && spaceTickets.length > 0 )
	{
		for (int i = 0; i < spaceTickets.length; i++)
		{
			obj_id ticket = spaceTickets[i];
			if ( isIdValid(ticket) )
			{
				if ( getStringObjVar(ticket, "space_dungeon.ticket.quest_type").equals("rebel_rescue") )
				{
					destroyObject(ticket);
					string custLogMsg = "*Corvette Ground Quest: Player %TU aborted the rebel rescue quest and the rebel corvette ticket was revoked.";
					CustomerServiceLog("DUNGEON_CorellianCorvette", custLogMsg, player);
				}
			}
		}
	}
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_facePlayer (obj_id player, obj_id npc)
{
	faceTo(npc, player);
	return;
}

// ----------------------------------------------------------------------

void corvette_tallon_rebel1_action_giveCorvetteReward (obj_id player, obj_id npc)
{
 	obj_id playerInv = getObjectInSlot(player, "inventory"); 
	if(isIdValid(playerInv))
	{
		createObject("object/tangible/loot/loot_schematic/corellian_corvette_landspeeder_av21_schematic.iff", playerInv, "");
		removeObjVar(player, "corvette.rebel_rescue.finished");
		CustomerServiceLog("DUNGEON_CorellianCorvette", "*Corvette Reward: Player %TU has been given an AV-21 schematic.", player);
	}
	return;
}

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "npc.conversation.corvette_tallon_rebel1");

	setCondition (self, CONDITION_CONVERSABLE);
	setCondition (self, CONDITION_INTERESTING);
	
	setInvulnerable (self, true);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setCondition (self, CONDITION_INTERESTING);
	
	setName(self, "Adar Tallon");
	setInvulnerable (self, true);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "npc.conversation.corvette_tallon_rebel1");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

trigger OnStartNpcConversation (obj_id player)
{
	if (ai_lib.isInCombat (self) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (corvette_tallon_rebel1_condition_dungeonInactive (player, self))
	{
		corvette_tallon_rebel1_action_facePlayer (player, self);

		//-- NPC: He's still missing. We have no word at all since losing communication with him. The Empire's behind this... I just know it. One of our best pilots too.
		string_id message = new string_id (c_stringFile, "s_71f656a9");
		chat.chat (self, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_tallon_rebel1_condition_notRebel (player, self))
	{
		corvette_tallon_rebel1_action_facePlayer (player, self);

		//-- NPC: No... no. This can't be allowed to... Oh, my apologies. I didn't see you there. More apologies, because another matter urgently demands my attention. Good bye.
		string_id message = new string_id (c_stringFile, "s_3e144812");
		chat.chat (self, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_tallon_rebel1_condition_earnedCorvetteReward (player, self))
	{
		//-- NPC: I cannot thank you enough for your actions. You have not only rescued a pilot of the Alliance, but also a friend. I am pleased to be able to reward you.
		string_id message = new string_id (c_stringFile, "s_5f61380e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Great. Let's see this reward then.
		boolean hasResponse0 = false;
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Reward? Not now. I'll return later.
		boolean hasResponse1 = false;
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d938179c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_60bce77f");

			setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 3);

			npcStartConversation (player, self, "corvette_tallon_rebel1", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_tallon_rebel1_condition_onDifferentCorvetteQuest (player, self))
	{
		corvette_tallon_rebel1_action_facePlayer (player, self);

		//-- NPC: Ah, you're here, but I see that you're already busy. I could've used your help. Maybe come back when you've finished your current task.
		string_id message = new string_id (c_stringFile, "s_2dd3860f");
		chat.chat (self, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
	{
		corvette_tallon_rebel1_action_facePlayer (player, self);

		//-- NPC: I am curious as why you have returned. Is there some sort of problem?
		string_id message = new string_id (c_stringFile, "s_7860ffca");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I forgot the location of our contact.
		boolean hasResponse0 = false;
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I can't continue. Please erase all records of my progress on this mission.
		boolean hasResponse1 = false;
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I still have documents for you.
		boolean hasResponse2 = false;
		if (corvette_tallon_rebel1_condition_hasStuff (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3925fbc0");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e70aed96");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2f26b4c4");

			setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 8);

			npcStartConversation (player, self, "corvette_tallon_rebel1", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_tallon_rebel1_condition_isOnQuest (player, self))
	{
		corvette_tallon_rebel1_action_facePlayer (player, self);

		//-- NPC: I see you have returned. Have you found the codes I'm looking for? Time is critical.
		string_id message = new string_id (c_stringFile, "s_953e3f2c");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I found several interesting documents and codes.
		boolean hasResponse0 = false;
		if (corvette_tallon_rebel1_condition_hasStuff (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Where do I go?
		boolean hasResponse1 = false;
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I can't continue. Please erase all records of my progress on this mission.
		boolean hasResponse2 = false;
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4d865d46");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e70aed96");

			setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 14);

			npcStartConversation (player, self, "corvette_tallon_rebel1", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
	{
		corvette_tallon_rebel1_action_facePlayer (player, self);

		//-- NPC: Arrested... treason... Dathomir prison. It truly is a pity. One of our most trusted and well-decorated pilots has been taken prisoner by the Empire. He was arrested for a crime that was not his doing. The Empire even went as far as planting false evidence to destroy his innocence. He needs to be rescued. He has been taken to a Corellian corvette which is en route to Dathomir. I need security codes in order to assemble a team to rescue him. Will you get those keys for us?
		string_id message = new string_id (c_stringFile, "s_f85a3545");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes, I will.
		boolean hasResponse0 = false;
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Not interested.
		boolean hasResponse1 = false;
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1944f1e6");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_df8ea53");

			setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 23);

			npcStartConversation (player, self, "corvette_tallon_rebel1", message, responses);
		}
		else
		{
			chat.chat (self, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (self, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "corvette_tallon_rebel1")
		return SCRIPT_CONTINUE;

	int branchId = getIntObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

	//-- [BRANCH NOTE] 
	//-- NPC: I cannot thank you enough for your actions. You have not only rescued a pilot of the Alliance, but also a friend. I am pleased to be able to reward you.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Great. Let's see this reward then.
	if (branchId == 3 && response == "s_d938179c")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition_hasInventorySpace (player, self))
		{
			corvette_tallon_rebel1_action_giveCorvetteReward (player, self);

			//-- NPC: [smiling] Eager as always. You've earned this... and the gratitude of the Alliance.
			string_id message = new string_id (c_stringFile, "s_ae9868f4");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corvette_tallon_rebel1_condition_hasInventorySpace (player, self))
		{
			//-- NPC: Ah. I'd like to give this reward to you, but you don't have room to carry it. You'll need to make some space and then speak with me again.
			string_id message = new string_id (c_stringFile, "s_5f449362");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I cannot thank you enough for your actions. You have not only rescued a pilot of the Alliance, but also a friend. I am pleased to be able to reward you.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I cannot thank you enough for your actions. You have not only rescued a pilot of the Alliance, but also a friend. I am pleased to be able to reward you.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Reward? Not now. I'll return later.
	if (branchId == 3 && response == "s_60bce77f")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: I see. Later it shall be then. Return whenever you are ready.
			string_id message = new string_id (c_stringFile, "s_80747fa2");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I cannot thank you enough for your actions. You have not only rescued a pilot of the Alliance, but also a friend. I am pleased to be able to reward you.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I am curious as why you have returned. Is there some sort of problem?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I forgot the location of our contact.
	if (branchId == 8 && response == "s_3925fbc0")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Lt. Lance is at the Rebel hideout on Corellia. He will provide you transportation to the Corellian corvette which houses our well-decorated pilot. We need that pilot returned to us as soon as possible. Once your mission has been successfully completed, return to me for a reward.
			string_id message = new string_id (c_stringFile, "s_49139b6");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I am curious as why you have returned. Is there some sort of problem?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I am curious as why you have returned. Is there some sort of problem?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can't continue. Please erase all records of my progress on this mission.
	if (branchId == 8 && response == "s_e70aed96")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Are you sure you wish to quit? I will have to revoke the travel authorization. It is for special operatives only. And I'll have to take any remaining documents you may have relating to this mission.
			string_id message = new string_id (c_stringFile, "s_b0f80f89");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm sure.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I changed my mind.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0da26cf");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4440ce87");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I am curious as why you have returned. Is there some sort of problem?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I am curious as why you have returned. Is there some sort of problem?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I still have documents for you.
	if (branchId == 8 && response == "s_2f26b4c4")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: I see. Which ones do you have?
			string_id message = new string_id (c_stringFile, "s_8b512dfa");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I am curious as why you have returned. Is there some sort of problem?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Are you sure you wish to quit? I will have to revoke the travel authorization. It is for special operatives only. And I'll have to take any remaining documents you may have relating to this mission.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sure.
	if (branchId == 10 && response == "s_b0da26cf")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_removeTicket (player, self);

			//-- NPC: I'm sorry to hear of this. I must find someone else to replace you. Good bye.
			string_id message = new string_id (c_stringFile, "s_303462a1");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Are you sure you wish to quit? I will have to revoke the travel authorization. It is for special operatives only. And I'll have to take any remaining documents you may have relating to this mission.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Are you sure you wish to quit? I will have to revoke the travel authorization. It is for special operatives only. And I'll have to take any remaining documents you may have relating to this mission.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I changed my mind.
	if (branchId == 10 && response == "s_4440ce87")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Good. Be swift in dealing with this situation.
			string_id message = new string_id (c_stringFile, "s_d50be1f6");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Are you sure you wish to quit? I will have to revoke the travel authorization. It is for special operatives only. And I'll have to take any remaining documents you may have relating to this mission.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I see. Which ones do you have?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Security Override Codes.
	if (branchId == 13 && response == "s_135bf1fb")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_giveTicket (player, self);

			//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
			string_id message = new string_id (c_stringFile, "s_62379ba");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I see. Which ones do you have?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I see. Which ones do you have?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Resignation Form.
	if (branchId == 13 && response == "s_9b6fd578")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject2 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_b2503658");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I see. Which ones do you have?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I see. Which ones do you have?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Purchase Order.
	if (branchId == 13 && response == "s_b0e6356e")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject3 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_581c3ef2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I see. Which ones do you have?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I see. Which ones do you have?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am leaving for the Corellian corvette.
	if (branchId == 13 && response == "s_67499366")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Excellent! I wish you luck and haste in your journey. Succeed in your mission and then return to me for your reward. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_7cea4aed");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I see. Which ones do you have?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I see. Which ones do you have?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looks like I have more to do.
	if (branchId == 13 && response == "s_a128e067")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Indeed. We need those codes as soon as possible.
			string_id message = new string_id (c_stringFile, "s_daf0c91a");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I see. Which ones do you have?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I see you have returned. Have you found the codes I'm looking for? Time is critical.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I found several interesting documents and codes.
	if (branchId == 14 && response == "s_4d865d46")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Oh? What have you found?
			string_id message = new string_id (c_stringFile, "s_75b09ea1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I see you have returned. Have you found the codes I'm looking for? Time is critical.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I see you have returned. Have you found the codes I'm looking for? Time is critical.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (branchId == 14 && response == "s_60c4f974")
	{
		corvette_tallon_rebel1_action_clearLocations (player, self);

		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?
			string_id message = new string_id (c_stringFile, "s_57504441");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I see you have returned. Have you found the codes I'm looking for? Time is critical.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I see you have returned. Have you found the codes I'm looking for? Time is critical.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I can't continue. Please erase all records of my progress on this mission.
	if (branchId == 14 && response == "s_e70aed96")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_quitCorvetteQuest (player, self);

			//-- NPC: I'm sorry to learn of this unexpected turn of events. I suppose I'll have to find someone else. Oh, I'll need to take any documents you've found while on this mission. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_abd76541");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I see you have returned. Have you found the codes I'm looking for? Time is critical.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? What have you found?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Security Override Codes.
	if (branchId == 15 && response == "s_135bf1fb")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_giveTicket (player, self);

			//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
			string_id message = new string_id (c_stringFile, "s_62379ba");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? What have you found?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? What have you found?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Resignation Form.
	if (branchId == 15 && response == "s_9b6fd578")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject2 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_b2503658");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? What have you found?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? What have you found?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Purchase Order.
	if (branchId == 15 && response == "s_b0e6356e")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject3 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_581c3ef2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? What have you found?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? What have you found?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am leaving for the Corellian corvette.
	if (branchId == 15 && response == "s_67499366")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Excellent! I wish you luck and haste in your journey. Succeed in your mission and then return to me for your reward. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_7cea4aed");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? What have you found?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Oh? What have you found?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looks like I have more to do.
	if (branchId == 15 && response == "s_a128e067")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Indeed. We need those codes as soon as possible.
			string_id message = new string_id (c_stringFile, "s_daf0c91a");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Oh? What have you found?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Security Override Codes.
	if (branchId == 16 && response == "s_135bf1fb")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_giveTicket (player, self);

			//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
			string_id message = new string_id (c_stringFile, "s_62379ba");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Resignation Form.
	if (branchId == 16 && response == "s_9b6fd578")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject2 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_b2503658");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Purchase Order.
	if (branchId == 16 && response == "s_b0e6356e")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject3 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_581c3ef2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am leaving for the Corellian corvette.
	if (branchId == 16 && response == "s_67499366")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Excellent! I wish you luck and haste in your journey. Succeed in your mission and then return to me for your reward. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_7cea4aed");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looks like I have more to do.
	if (branchId == 16 && response == "s_a128e067")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Indeed. We need those codes as soon as possible.
			string_id message = new string_id (c_stringFile, "s_daf0c91a");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Security Override Codes.
	if (branchId == 17 && response == "s_135bf1fb")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_giveTicket (player, self);

			//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
			string_id message = new string_id (c_stringFile, "s_62379ba");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Resignation Form.
	if (branchId == 17 && response == "s_9b6fd578")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject2 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_b2503658");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Purchase Order.
	if (branchId == 17 && response == "s_b0e6356e")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject3 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_581c3ef2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am leaving for the Corellian corvette.
	if (branchId == 17 && response == "s_67499366")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Excellent! I wish you luck and haste in your journey. Succeed in your mission and then return to me for your reward. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_7cea4aed");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looks like I have more to do.
	if (branchId == 17 && response == "s_a128e067")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Indeed. We need those codes as soon as possible.
			string_id message = new string_id (c_stringFile, "s_daf0c91a");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Security Override Codes.
	if (branchId == 18 && response == "s_135bf1fb")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_giveTicket (player, self);

			//-- NPC: Ahh, the codes! We now have the exact location of the Corellian corvette he is on. Here, take this travel authorization to the Rebel hideout on Corellia and meet with Lt. Lance there. He will provide you transport on a starship which is capable of intercepting the Corellian corvette in deep space. Once your mission has been successfully completed, return to me for a reward. I wish you haste and luck on your journey.
			string_id message = new string_id (c_stringFile, "s_62379ba");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Resignation Form.
	if (branchId == 18 && response == "s_9b6fd578")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject2 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Return to the locations I gave you and find those codes!  Arrgh... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_b2503658");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Show the Imperial Purchase Order.
	if (branchId == 18 && response == "s_b0e6356e")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_takeObject3 (player, self);

			//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
			string_id message = new string_id (c_stringFile, "s_581c3ef2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Show the Security Override Codes.
			boolean hasResponse0 = false;
			if (corvette_tallon_rebel1_condition_hasObject01 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Show the Imperial Resignation Form.
			boolean hasResponse1 = false;
			if (corvette_tallon_rebel1_condition_hasObject02 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Show the Imperial Purchase Order.
			boolean hasResponse2 = false;
			if (corvette_tallon_rebel1_condition_hasObject03 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I am leaving for the Corellian corvette.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition_hasTravelTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Looks like I have more to do.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition_hasNoObjectsAndNoTicket (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_135bf1fb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9b6fd578");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b0e6356e");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67499366");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a128e067");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am leaving for the Corellian corvette.
	if (branchId == 18 && response == "s_67499366")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Excellent! I wish you luck and haste in your journey. Succeed in your mission and then return to me for your reward. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_7cea4aed");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Looks like I have more to do.
	if (branchId == 18 && response == "s_a128e067")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Indeed. We need those codes as soon as possible.
			string_id message = new string_id (c_stringFile, "s_daf0c91a");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'This isn't what I'm looking for. I need the security codes as soon as possible! Bah... well... okay, this is potentially useful information. I'll reward you for finding it. But then you must go find those security codes.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Warren.
	if (branchId == 21 && response == "s_3410977a")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation1 (player, self);

			//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
			string_id message = new string_id (c_stringFile, "s_439048cf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Imperial Prison.
	if (branchId == 21 && response == "s_57214562")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation2 (player, self);

			//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
			string_id message = new string_id (c_stringFile, "s_e1aa98dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the downed Bloodrazor vessel.
	if (branchId == 21 && response == "s_151358aa")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation3 (player, self);

			//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
			string_id message = new string_id (c_stringFile, "s_a073eacd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I accept this mission.
	if (branchId == 21 && response == "s_a5591c27")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_acceptsQuest (player, self);

			//-- NPC: You're our only hope in this situation. You have your orders, soldier.
			string_id message = new string_id (c_stringFile, "s_90b2e0c5");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks.
	if (branchId == 21 && response == "s_d6695e83")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Your answer is unfortunate. But it is your choice. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_93fcbaae");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'I have several locations for you... places that, we think, may have the codes. Which location do you need more information on?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Arrested... treason... Dathomir prison. It truly is a pity. One of our most trusted and well-decorated pilots has been taken prisoner by the Empire. He was arrested for a crime that was not his doing. The Empire even went as far as planting false evidence to destroy his innocence. He needs to be rescued. He has been taken to a Corellian corvette which is en route to Dathomir. I need security codes in order to assemble a team to rescue him. Will you get those keys for us?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I will.
	if (branchId == 23 && response == "s_1944f1e6")
	{
		corvette_tallon_rebel1_action_clearLocations (player, self);

		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!
			string_id message = new string_id (c_stringFile, "s_94007006");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Arrested... treason... Dathomir prison. It truly is a pity. One of our most trusted and well-decorated pilots has been taken prisoner by the Empire. He was arrested for a crime that was not his doing. The Empire even went as far as planting false evidence to destroy his innocence. He needs to be rescued. He has been taken to a Corellian corvette which is en route to Dathomir. I need security codes in order to assemble a team to rescue him. Will you get those keys for us?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Arrested... treason... Dathomir prison. It truly is a pity. One of our most trusted and well-decorated pilots has been taken prisoner by the Empire. He was arrested for a crime that was not his doing. The Empire even went as far as planting false evidence to destroy his innocence. He needs to be rescued. He has been taken to a Corellian corvette which is en route to Dathomir. I need security codes in order to assemble a team to rescue him. Will you get those keys for us?
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not interested.
	if (branchId == 23 && response == "s_df8ea53")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Your answer troubles me. Return if you change your mind. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_a24d8ba0");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Arrested... treason... Dathomir prison. It truly is a pity. One of our most trusted and well-decorated pilots has been taken prisoner by the Empire. He was arrested for a crime that was not his doing. The Empire even went as far as planting false evidence to destroy his innocence. He needs to be rescued. He has been taken to a Corellian corvette which is en route to Dathomir. I need security codes in order to assemble a team to rescue him. Will you get those keys for us?' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Warren.
	if (branchId == 24 && response == "s_3410977a")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation1 (player, self);

			//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
			string_id message = new string_id (c_stringFile, "s_439048cf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Imperial Prison.
	if (branchId == 24 && response == "s_57214562")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation2 (player, self);

			//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
			string_id message = new string_id (c_stringFile, "s_e1aa98dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the downed Bloodrazor vessel.
	if (branchId == 24 && response == "s_151358aa")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation3 (player, self);

			//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
			string_id message = new string_id (c_stringFile, "s_a073eacd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I accept this mission.
	if (branchId == 24 && response == "s_a5591c27")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_acceptsQuest (player, self);

			//-- NPC: You're our only hope in this situation. You have your orders, soldier.
			string_id message = new string_id (c_stringFile, "s_90b2e0c5");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!
	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks.
	if (branchId == 24 && response == "s_d6695e83")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Your answer is unfortunate. But it is your choice. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_93fcbaae");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'There is hope yet! I have been informed that the security override codes I need could be in one of three locations. Based on that information, the places to seek out are a downed Bloodrazor smuggling vessel on Lok, the Imperial Warren on Dantooine and the Imperial Prison itself on Dathomir.  It will be a dangerous journey, filled with obstacles, but justice must be done. We must rescue the officer before he reaches that prison!' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Warren.
	if (branchId == 25 && response == "s_3410977a")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation1 (player, self);

			//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
			string_id message = new string_id (c_stringFile, "s_439048cf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Imperial Prison.
	if (branchId == 25 && response == "s_57214562")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation2 (player, self);

			//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
			string_id message = new string_id (c_stringFile, "s_e1aa98dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the downed Bloodrazor vessel.
	if (branchId == 25 && response == "s_151358aa")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation3 (player, self);

			//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
			string_id message = new string_id (c_stringFile, "s_a073eacd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I accept this mission.
	if (branchId == 25 && response == "s_a5591c27")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_acceptsQuest (player, self);

			//-- NPC: You're our only hope in this situation. You have your orders, soldier.
			string_id message = new string_id (c_stringFile, "s_90b2e0c5");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks.
	if (branchId == 25 && response == "s_d6695e83")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Your answer is unfortunate. But it is your choice. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_93fcbaae");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Warren.
	if (branchId == 26 && response == "s_3410977a")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation1 (player, self);

			//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
			string_id message = new string_id (c_stringFile, "s_439048cf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Imperial Prison.
	if (branchId == 26 && response == "s_57214562")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation2 (player, self);

			//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
			string_id message = new string_id (c_stringFile, "s_e1aa98dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the downed Bloodrazor vessel.
	if (branchId == 26 && response == "s_151358aa")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation3 (player, self);

			//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
			string_id message = new string_id (c_stringFile, "s_a073eacd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I accept this mission.
	if (branchId == 26 && response == "s_a5591c27")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_acceptsQuest (player, self);

			//-- NPC: You're our only hope in this situation. You have your orders, soldier.
			string_id message = new string_id (c_stringFile, "s_90b2e0c5");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks.
	if (branchId == 26 && response == "s_d6695e83")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Your answer is unfortunate. But it is your choice. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_93fcbaae");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Warren.
	if (branchId == 27 && response == "s_3410977a")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation1 (player, self);

			//-- NPC: The Empire has conducted many top secret operations at this location. Unfortunately, it has been very difficult to penetrate the defenses of the Warren on our side. We'll need a cunning Alliance operative to enter the establishment and find those codes. The Warren is on Dantooine.
			string_id message = new string_id (c_stringFile, "s_439048cf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the Imperial Prison.
	if (branchId == 27 && response == "s_57214562")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation2 (player, self);

			//-- NPC: Ah the Imperial Prison of Dathomir. Many who are arrested for their 'sins' against the Empire are taken to this location. Most are never seen again. We must not allow our officer to meet the same fate. We've lost too many men as it is! Since the Imperial Prison is where they are taking him, it wouldn't be surprising to find information about his current location there.
			string_id message = new string_id (c_stringFile, "s_e1aa98dd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: Tell me about the downed Bloodrazor vessel.
	if (branchId == 27 && response == "s_151358aa")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_heardLocation3 (player, self);

			//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
			string_id message = new string_id (c_stringFile, "s_a073eacd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Tell me about the Warren.
			boolean hasResponse0 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation1 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Tell me about the Imperial Prison.
			boolean hasResponse1 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation2 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Tell me about the downed Bloodrazor vessel.
			boolean hasResponse2 = false;
			if (!corvette_tallon_rebel1_condition_knowsLocation3 (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I accept this mission.
			boolean hasResponse3 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: No thanks.
			boolean hasResponse4 = false;
			if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3410977a");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_57214562");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151358aa");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a5591c27");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d6695e83");

				setObjVar (player, "conversation.corvette_tallon_rebel1.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

				npcSpeak (player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: I accept this mission.
	if (branchId == 27 && response == "s_a5591c27")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			corvette_tallon_rebel1_action_acceptsQuest (player, self);

			//-- NPC: You're our only hope in this situation. You have your orders, soldier.
			string_id message = new string_id (c_stringFile, "s_90b2e0c5");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.' were false.");

		return SCRIPT_CONTINUE;
	}

	//-- [BRANCH NOTE] 
	//-- NPC: The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.
	//-- [RESPONSE NOTE] 
	//-- PLAYER: No thanks.
	if (branchId == 27 && response == "s_d6695e83")
	{
		//-- [NOTE] 
		if (corvette_tallon_rebel1_condition__defaultCondition (player, self))
		{
			//-- NPC: Your answer is unfortunate. But it is your choice. May the Force be with you.
			string_id message = new string_id (c_stringFile, "s_93fcbaae");
			removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

			npcSpeak (player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

		chat.chat (self, "Error:  All conditions for OnNpcConversationResponse for branch 'The Bloodrazors are a mysterious band of pirates and criminals. It is said they have access to radar technology that can detect certain unique codes used by ships that approach within their range. This is my hope. The downed Bloodrazor vessel is on Lok though the exact location is unknown.' were false.");

		return SCRIPT_CONTINUE;
	}

	chat.chat (self, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	removeObjVar (player, "conversation.corvette_tallon_rebel1.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

