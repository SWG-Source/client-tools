// ======================================================================
//
// naboo_privateer_trainer_1.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.features;
include library.skill;
include library.space_flags;
include library.space_quest;
include library.space_skill;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/naboo_privateer_trainer_1";

// ======================================================================
// Script Conditions
// ======================================================================

boolean naboo_privateer_trainer_1_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_remembersPlayer (obj_id player, obj_id npc)
{
	return ( utils.hasScriptVar( player, "metRsfTrainer" ));
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasNoviceSkillBox (obj_id player, obj_id npc)
{
	return ( space_quest.hasSkill( player, "pilot_neutral_novice" ) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_alreadyHasAQuest (obj_id player, obj_id npc)
{
	return ( space_quest.hasQuest( player ) && naboo_privateer_trainer_1_condition_hasNoviceSkillBox(player, npc ) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_isImperialPilot (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_imperial_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_isRebelPilot (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_rebel_navy_novice" ));
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_isPilotButNotRsfMember (obj_id player, obj_id npc)
{
	return (hasSkill(player, "pilot_neutral_novice") && (!space_flags.isSpaceTrack(player, space_flags.PRIVATEER_NABOO)));
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasCompletedQuestOne (obj_id player, obj_id npc)
{
	//note that for quest one, we're checking vs. completion of the surprise attack:
	return ( space_quest.hasCompletedQuestRecursive( player, "patrol", "naboo_privateer_1" ) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasCompletedQuestTwo (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "destroy", "naboo_privateer_2") );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasCompletedQuestThree (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuestRecursive( player, "patrol", "naboo_privateer_3") );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasCompletedQuestFour (obj_id player, obj_id npc)
{
	return ( space_quest.hasCompletedQuest( player, "assassinate", "naboo_privateer_tier1_4a") );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_missionOneFailed (obj_id player, obj_id npc)
{
	//note that we are checking vs. the surprise attack in mission 1
	return ( space_quest.hasFailedQuestRecursive( player, "patrol", "naboo_privateer_1" ) ||
			 space_quest.hasAbortedQuestRecursive( player, "patrol", "naboo_privateer_1" ) );
		 
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_isReadyForFirstTraining (obj_id player, obj_id npc)
{
	//You have completed all 4 missions is handled by previous convo conditions
	
	if ( 	hasSkill( player, "pilot_neutral_starships_01") 	|| 
			hasSkill( player, "pilot_neutral_procedures_01") 	||
			hasSkill( player, "pilot_neutral_weapons_01") 		||
			hasSkill( player, "pilot_neutral_droid_01" ) 	)
		return false;//You have already received your initial skill (or more!)
	else	
		return true;
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_isReadyForMoreTraining (obj_id player, obj_id npc)
{
	//You have completed all 4 missions - checked via previous convo options
	//but don't have all the tier-1 skills yet, - //checked via previous convo options
	
	//if you have enough xp for this skill, then you have enough for
	//any other skill at this tier:
	return ( space_quest.isPlayerQualifiedForSkill( player, "pilot_neutral_starships_01") );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasAllTierOneSkills (obj_id player, obj_id npc)
{
	/*
	if (!hasSkill( player, "pilot_neutral_starships_01") )
		return false;
	else if (!hasSkill( player, "pilot_neutral_procedures_01") )
		return false;
	else if (!hasSkill( player, "pilot_neutral_weapons_01") )
		return false;
	else if (!hasSkill( player, "pilot_neutral_droid_01" ) )
		return false;
	else	
		return true;
	*/
	
	return space_flags.hasCompletedTierOne(player);
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_missionTwoFailed (obj_id player, obj_id npc)
{
	if ( space_quest.hasFailedQuest( player, "destroy", "naboo_privateer_2" ) ||
		 space_quest.hasAbortedQuest( player, "destroy", "naboo_privateer_2" ) )
		 return true;
		 
	return false;	 
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_beenRewardedForQuestOne (obj_id player, obj_id npc)
{
	//if the player hasn't completed this quest, then we don't want to give
	//the reward!  So return true here.
	
	//note that we're checking vs. completion of the surpise attack:
	if (!space_quest.hasCompletedQuestRecursive( player, "patrol", "naboo_privateer_1"))
		return true;

	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuestRecursive( player, "patrol", "naboo_privateer_1")) && 
				( !space_quest.hasReceivedReward( player, "patrol", "naboo_privateer_1")) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_beenRewardedForQuestTwo (obj_id player, obj_id npc)
{
	//if the player hasn't completed this quest, then we don't want to give
	//the reward!  So return true here.
	if (!space_quest.hasCompletedQuest( player, "destroy", "naboo_privateer_1"))
		return true;

	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuest( player, "destroy", "naboo_privateer_1")) && 
				( space_quest.hasReceivedReward( player, "destroy", "naboo_privateer_1")) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_beenRewardedForQuestThree (obj_id player, obj_id npc)
{
	//if the player hasn't completed this quest, then we don't want to give
	//the reward!  So return true here.
	if (!space_quest.hasCompletedQuestRecursive( player, "patrol", "naboo_privateer_3"))
		return true;

	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuestRecursive( player, "patrol", "naboo_privateer_3")) && 
				( space_quest.hasReceivedReward( player, "patrol", "naboo_privateer_3")) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_beenRewardedForQuestFour (obj_id player, obj_id npc)
{
	//if the player hasn't completed this quest, then we don't want to give
	//the reward!  So return true here.
	if (!space_quest.hasCompletedQuest( player, "assassinate", "naboo_privateer_tier1_4a"))
		return true;
		
	//this returns true if you have completed this quest but have not been rewarded for it
	return (	( space_quest.hasCompletedQuest( player, "assassinate", "naboo_privateer_tier1_4a")) && 
				( space_quest.hasReceivedReward( player, "assassinate", "naboo_privateer_tier1_4a")) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_missionThreeFailed (obj_id player, obj_id npc)
{

	return ( space_quest.hasFailedQuestRecursive( player, "patrol", "naboo_privateer_3" ) )||
			( space_quest.hasAbortedQuestRecursive( player, "patrol", "naboo_privateer_3" ) );
		
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_missionFourFailed (obj_id player, obj_id npc)
{
	if ( space_quest.hasFailedQuest( player, "assassinate", "naboo_privateer_tier1_4a" ) ||
		 space_quest.hasAbortedQuest( player, "assassinate", "naboo_privateer_tier1_4a" ) )
		 return true;
		 
	return false;	 
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasStarshipSkill1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_neutral_starships_01") );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasDroidSkill1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_neutral_droid_01") );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasProceduresSkill1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_neutral_procedures_01") );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasWeaponsSkill1 (obj_id player, obj_id npc)
{
	return ( hasSkill( player, "pilot_neutral_weapons_01") );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasSpaceExpansion (obj_id player, obj_id npc)
{
	return ( features.isSpaceEdition( player ) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasSpaceShip (obj_id player, obj_id npc)
{
	return (!space_quest.canGrantNewbieShip(player));
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_hasVeryFirstQuest (obj_id player, obj_id npc)
{
	//You've got your assignment, and the control device for your ship in your datapad.  Go to the Starport and access the terminal to launch into space.
	if (!space_quest.hasCertifiedShip(player))
		return false; 
	else	
		return ( space_quest.hasQuest( player, "patrol", "naboo_privateer_1" ) ||
			  	 space_quest.hasQuest( player, "destroy_surpriseattack", "naboo_privateer_1" ) );
}

// ----------------------------------------------------------------------

boolean naboo_privateer_trainer_1_condition_canFlyNonTrackDuty (obj_id player, obj_id npc)
{
		if((hasSkill( player, "pilot_neutral_starships_01") || 
	    hasSkill( player, "pilot_neutral_procedures_01") ||
	    hasSkill( player, "pilot_neutral_weapons_01") ||
	    hasSkill( player, "pilot_neutral_droid_01" )) && !space_quest.hasQuest(player))
	    return true;
	    
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void naboo_privateer_trainer_1_action_rememberPlayer (obj_id player, obj_id npc)
{
	utils.setScriptVar( player, "metRsfTrainer", true );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantNoviceSkillBox (obj_id player, obj_id npc)
{
	space_flags.setSpaceTrack( player, space_flags.PRIVATEER_NABOO );
	skill.noisyGrantSkill( player, "pilot_neutral_novice" );

	//remove this when the other convos stop looking for it:
	//setObjVar( player, "rsfMember", true );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_signUpForRsf (obj_id player, obj_id npc)
{
	playMusic( player, "sound/music_themequest_acc_general.snd");
	// setObjVar( player, "rsfMember", true ); // We probably shouldn't be doing this
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantMissionOne (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "patrol", "naboo_privateer_1" );
	space_flags.restoreClientPath( player );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantMissionTwo (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy", "naboo_privateer_2" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantMissionThree (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "patrol", "naboo_privateer_3" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantMissionFour (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "assassinate", "naboo_privateer_tier1_4a" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_rewardForQuestOne (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "patrol", "naboo_privateer_1") )
		space_quest.giveReward( player, "patrol", "naboo_privateer_1", 100 );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_rewardForQuestTwo (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "destroy", "naboo_privateer_2") )
		space_quest.giveReward( player, "destroy", "naboo_privateer_2", 200 );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_rewardForQuestThree (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "patrol", "naboo_privateer_3") )
	{
		if ( getSpecies( player ) != SPECIES_ITHORIAN )
			space_quest.giveReward( player, "patrol", "naboo_privateer_3", 500, "object/tangible/wearables/bandolier/mercenary_bandolier.iff" );
		else
			space_quest.giveReward( player, "patrol", "naboo_privateer_3", 500, "object/tangible/wearables/bandolier/ith_mercenary_bandolier.iff" );
	}
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_rewardForQuestFour (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "assassinate", "naboo_privateer_tier1_4a") )
		space_quest.giveReward( player, "assassinate", "naboo_privateer_tier1_4a", 1000, "object/tangible/ship/components/weapon/wpn_mission_reward_neutral_mandal_light_blaster.iff" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantDroidSkill (obj_id player, obj_id npc)
{
	skill.noisyGrantSkill( player, "pilot_neutral_droid_01" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantStarshipSkill (obj_id player, obj_id npc)
{
	skill.noisyGrantSkill( player, "pilot_neutral_starships_01" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantProceduresSkill (obj_id player, obj_id npc)
{
	skill.noisyGrantSkill( player, "pilot_neutral_procedures_01" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantWeaponsSkill (obj_id player, obj_id npc)
{
	skill.noisyGrantSkill( player, "pilot_neutral_weapons_01" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_buyWeaponsSkill1 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_weapons_01" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_buyDroidSkill1 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_droid_01" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_buyProcedureSkill1 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_procedures_01" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_buyStarshipSkill1 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_neutral_starships_01" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantPatrolDutyMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "patrol_duty", "naboo_privateer_5" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantDestroyDutyMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy_duty", "naboo_privateer_6" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantEscortDutyMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "escort_duty", "naboo_privateer_7" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantQuestEight (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy", "naboo_privateer_8" );
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_grantSpaceShip (obj_id player, obj_id npc)
{
	space_quest.grantNewbieShip( player, "neutral");
}

// ----------------------------------------------------------------------

void naboo_privateer_trainer_1_action_eraseClientPaths (obj_id player, obj_id npc)
{
	space_flags.clearClientPath( player );
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int naboo_privateer_trainer_1_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You've got your assignment, and the control device for your ship in your datapad. Go to Theed Hangar and access the terminal to launch into space.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Theed Hangar. Leave the palace the way you came. Take a left when you hit the plaza. Then follow the roads until you reach the power complex. Theed Hangar is on the opposite side of this building. Go inside the hangar and look for the 'starship terminals.' Use one of these terminals to launch your starship.
			string_id message = new string_id (c_stringFile, "s_41c478b2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back?
	if (response == "s_85dd7d6c")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must communicate with the Naboo space station and request permission to land. Fortunately all of the new starship navicomputers automatically add a 'launch waypoint' to your datapad when you take off. Find your way back to this point, fly up to the space station, communicate with them, and ask for landing clearance.
			string_id message = new string_id (c_stringFile, "s_1664d807");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is my ship?
	if (response == "s_4358efe9")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Your ship is parked at the starport. I've uploaded clearance codes to your datapad. Check your datapad to review the status of your ship.
			string_id message = new string_id (c_stringFile, "s_a49a3700");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Theed Hangar. Leave the palace the way you came. Take a left when you hit the plaza. Then follow the roads until you reach the power complex. Theed Hangar is on the opposite side of this building. Go inside the hangar and look for the 'starship terminals.' Use one of these terminals to launch your starship.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Theed Hangar. Leave the palace the way you came. Take a left when you hit the plaza. Then follow the roads until you reach the power complex. Theed Hangar is on the opposite side of this building. Go inside the hangar and look for the 'starship terminals.' Use one of these terminals to launch your starship.
			string_id message = new string_id (c_stringFile, "s_41c478b2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back?
	if (response == "s_85dd7d6c")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must communicate with the Naboo space station and request permission to land. Fortunately all of the new starship navicomputers automatically add a 'launch waypoint' to your datapad when you take off. Find your way back to this point, fly up to the space station, communicate with them, and ask for landing clearance.
			string_id message = new string_id (c_stringFile, "s_1664d807");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is my ship?
	if (response == "s_4358efe9")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Your ship is parked at the starport. I've uploaded clearance codes to your datapad. Check your datapad to review the status of your ship.
			string_id message = new string_id (c_stringFile, "s_a49a3700");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You must communicate with the Naboo space station and request permission to land. Fortunately all of the new starship navicomputers automatically add a 'launch waypoint' to your datapad when you take off. Find your way back to this point, fly up to the space station, communicate with them, and ask for landing clearance.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Theed Hangar. Leave the palace the way you came. Take a left when you hit the plaza. Then follow the roads until you reach the power complex. Theed Hangar is on the opposite side of this building. Go inside the hangar and look for the 'starship terminals.' Use one of these terminals to launch your starship.
			string_id message = new string_id (c_stringFile, "s_41c478b2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back?
	if (response == "s_85dd7d6c")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must communicate with the Naboo space station and request permission to land. Fortunately all of the new starship navicomputers automatically add a 'launch waypoint' to your datapad when you take off. Find your way back to this point, fly up to the space station, communicate with them, and ask for landing clearance.
			string_id message = new string_id (c_stringFile, "s_1664d807");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is my ship?
	if (response == "s_4358efe9")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Your ship is parked at the starport. I've uploaded clearance codes to your datapad. Check your datapad to review the status of your ship.
			string_id message = new string_id (c_stringFile, "s_a49a3700");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Your ship is parked at the starport. I've uploaded clearance codes to your datapad. Check your datapad to review the status of your ship.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I go?
	if (response == "s_60c4f974")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Theed Hangar. Leave the palace the way you came. Take a left when you hit the plaza. Then follow the roads until you reach the power complex. Theed Hangar is on the opposite side of this building. Go inside the hangar and look for the 'starship terminals.' Use one of these terminals to launch your starship.
			string_id message = new string_id (c_stringFile, "s_41c478b2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I get back?
	if (response == "s_85dd7d6c")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: You must communicate with the Naboo space station and request permission to land. Fortunately all of the new starship navicomputers automatically add a 'launch waypoint' to your datapad when you take off. Find your way back to this point, fly up to the space station, communicate with them, and ask for landing clearance.
			string_id message = new string_id (c_stringFile, "s_1664d807");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where is my ship?
	if (response == "s_4358efe9")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Your ship is parked at the starport. I've uploaded clearance codes to your datapad. Check your datapad to review the status of your ship.
			string_id message = new string_id (c_stringFile, "s_a49a3700");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I've heard you're quite a pilot. What brings you to speak with the Royal Security Forces?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm looking for a mission. Do you have any?
	if (response == "s_c1ff5062")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_canFlyNonTrackDuty (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Well, fortunately for you, I do have an opening for some duty missions today. There is escort duty and there is destroy duty. What would you like?
			string_id message = new string_id (c_stringFile, "s_1e029d90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: I appreciate your enthusiasm, but unless you're a member of the RSF, I'm afraid I cannot help you. Talk to your own contacts if you're looking for a mission.
			string_id message = new string_id (c_stringFile, "s_38d8b2c4");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, fortunately for you, I do have an opening for some duty missions today. There is escort duty and there is destroy duty. What would you like?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is an open-ended task. That means that there is no true end to it, you just finish whenever you're ready. It's a good way to earn experience to prepare yourself for training. So, what do you say?
			string_id message = new string_id (c_stringFile, "s_a8cdf3dc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would like a duty mission.
	if (response == "s_e814cf37")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good, good. We have a couple to choose from. You can either fight the Black Sun menace in a Destroy Duty, or you can assist the merchant freighters with an Escort Duty.
			string_id message = new string_id (c_stringFile, "s_e38f886b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I want Destroy Duty
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want Escort Duty
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Eh, I changed my mind.
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_40759c25");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_629c7d23");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d36a0e4");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How about some training?
	if (response == "s_db74ddc6")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: There's nothing in which I can train you right now. But a duty mission is a great way to earn some experience for your next training session. What do you say?
			string_id message = new string_id (c_stringFile, "s_f4245779");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now, thanks.
	if (response == "s_2883b989")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: OK, OK. Come back when you're ready.
			string_id message = new string_id (c_stringFile, "s_138");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] This is what the NPC says when he talks to a player who is not currently signed-up with any pilot profession.
	//-- NPC: Greetings citizen. Are you here to join the Royal Security Forces?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_d70dba34")
	{
		doAnimationAction (player, "nod_head_once");

		naboo_privateer_trainer_1_action_eraseClientPaths (player, npc);

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			naboo_privateer_trainer_1_action_grantNoviceSkillBox (player, npc);

			//-- NPC: Excellent. Consider yourself enlisted. Do you have a starship suitable for service?
			string_id message = new string_id (c_stringFile, "s_c19e20c9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, I don't.
			boolean hasResponse0 = false;
			if (!naboo_privateer_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Yes, of course.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition_hasSpaceShip (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9817b9bb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_5c522568");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No.
	if (response == "s_4c695dbd")
	{
		doAnimationAction (player, "shake_head_no");

		naboo_privateer_trainer_1_action_eraseClientPaths (player, npc);

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Stop wasting my time.
			string_id message = new string_id (c_stringFile, "s_789f76ee");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent. Consider yourself enlisted. Do you have a starship suitable for service?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I don't.
	if (response == "s_9817b9bb")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			naboo_privateer_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: Well, you're going to need a ship. I'll add these control codes for a basic one to your datapad. You should consider upgrading sooner or later, though.
			string_id message = new string_id (c_stringFile, "s_e3245432");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thank you.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b9b27823");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, of course.
	if (response == "s_5c522568")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Excellent. Sounds like you're all set then. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_5ed33504");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I am.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I need to take care of some things first.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_30");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_34");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, you're going to need a ship. I'll add these control codes for a basic one to your datapad. You should consider upgrading sooner or later, though.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thank you.
	if (response == "s_b9b27823")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Think nothing of it. Are you ready for your first assignment?
			string_id message = new string_id (c_stringFile, "s_eed2fe3e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, I am.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I need to take care of some things first.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_36a4e374");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68660d24");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Think nothing of it. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I am.
	if (response == "s_36a4e374")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Very well. Listen up. We've had reports of pirates in the system and we want you to run a patrol. Just make sure everything's on the up and up. This ought to be a piece of cake.
			string_id message = new string_id (c_stringFile, "s_97a2f8dc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm off!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's in it for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1db37a0f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48566ef9");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to take care of some things first.
	if (response == "s_68660d24")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Very well. Come back and speak with me when you're ready.
			string_id message = new string_id (c_stringFile, "s_f5f16210");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well. Listen up. We've had reports of pirates in the system and we want you to run a patrol. Just make sure everything's on the up and up. This ought to be a piece of cake.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm off!
	if (response == "s_1db37a0f")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			naboo_privateer_trainer_1_action_grantMissionOne (player, npc);

			//-- NPC: That's the spirit. Good luck!
			string_id message = new string_id (c_stringFile, "s_b02e116");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's in it for me?
	if (response == "s_48566ef9")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Whadya mean what's in it for you?! Pride in a job well done, you dope! RSF will pay you, of course, but that's not why we do this job! We do it because we're the ROYAL FREAKING SECURITY FORCES!! Now get out of here and complete your assignment.
			string_id message = new string_id (c_stringFile, "s_bbbfb8db");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, RSF better pay me
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65d63b3c");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I'll train you... AFTER you complete a few assignments for me. Now about that patrol...
			string_id message = new string_id (c_stringFile, "s_8fb73c4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm off!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's in it for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1db37a0f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48566ef9");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent. Sounds like you're all set then. Are you ready for your first assignment?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I am.
	if (response == "s_30")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Very well. Listen up. We've had reports of pirates in the system and we want you to run a patrol. Just make sure everything's on the up and up. This ought to be a piece of cake.
			string_id message = new string_id (c_stringFile, "s_32");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm off!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's in it for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1db37a0f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48566ef9");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to take care of some things first.
	if (response == "s_34")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Very well. Come back and speak with me when you're ready.
			string_id message = new string_id (c_stringFile, "s_36");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Very well. Listen up. We've had reports of pirates in the system and we want you to run a patrol. Just make sure everything's on the up and up. This ought to be a piece of cake.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm off!
	if (response == "s_1db37a0f")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			naboo_privateer_trainer_1_action_grantMissionOne (player, npc);

			//-- NPC: That's the spirit. Good luck!
			string_id message = new string_id (c_stringFile, "s_b02e116");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's in it for me?
	if (response == "s_48566ef9")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Whadya mean what's in it for you?! Pride in a job well done, you dope! RSF will pay you, of course, but that's not why we do this job! We do it because we're the ROYAL FREAKING SECURITY FORCES!! Now get out of here and complete your assignment.
			string_id message = new string_id (c_stringFile, "s_bbbfb8db");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, RSF better pay me
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65d63b3c");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I'll train you... AFTER you complete a few assignments for me. Now about that patrol...
			string_id message = new string_id (c_stringFile, "s_8fb73c4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm off!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's in it for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1db37a0f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48566ef9");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have accomplished all the tasks I have set for you. Are you ready for some more advanced work?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure!
	if (response == "s_b3a9fd05")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Go see Captain Kaydine. He is in one of the large meeting rooms upstairs here in the palace. Should be easy to find, just follow the ego.
			string_id message = new string_id (c_stringFile, "s_919029b");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: All I asked was for you to complete one simple patrol. What's the malfunction? Get back out there and secure the area!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It wasn't my fault. I was attacked!
	if (response == "s_aec5c3dd")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "cover_ears_mocking");

			naboo_privateer_trainer_1_action_grantMissionOne (player, npc);

			//-- NPC: I don't want to hear your excuses! Now get out there and complete the assignment!
			string_id message = new string_id (c_stringFile, "s_7af03aab");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will do!
	if (response == "s_ea059f38")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			naboo_privateer_trainer_1_action_grantMissionOne (player, npc);

			//-- NPC: That's what I want to hear. Now go!
			string_id message = new string_id (c_stringFile, "s_b9c77549");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please tell me you're not going to make excuses... It's a simple patrol mission and you're standing here jabbering at me?! Go complete your assignment!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will do!
	if (response == "s_52")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			naboo_privateer_trainer_1_action_grantMissionThree (player, npc);

			//-- NPC: That's what I want to hear. Now go!
			string_id message = new string_id (c_stringFile, "s_54");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It wasn't my fault. I was attacked!
	if (response == "s_56")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "cover_ears_mocking");

			naboo_privateer_trainer_1_action_grantMissionThree (player, npc);

			//-- NPC: I don't want to hear your excuses! Now get out there and complete the assignment!
			string_id message = new string_id (c_stringFile, "s_58");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Unacceptable! Unless you take out the Black Sun leader, they'll never stop harassing our transports and our citizens. You're a better pilot than that! Now go destroy that leader's ship!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine.
	if (response == "s_7eb4d42")
	{
		doAnimationAction (player, "wave_on_dismissing");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			naboo_privateer_trainer_1_action_grantMissionFour (player, npc);

			//-- NPC: I don't like your attitude! Just go complete your assignment before I shoot you myself.
			string_id message = new string_id (c_stringFile, "s_983440a3");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Don't worry.
	if (response == "s_48b8131c")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh_cackle");

			naboo_privateer_trainer_1_action_grantMissionFour (player, npc);

			//-- NPC: Don't worry?! Don't worry!? The captain's all over me about this and you tell me not to worry? Tell you what... go kill the Black Sun Leader and THEN I won't worry!
			string_id message = new string_id (c_stringFile, "s_80744015");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Now it's personal.
	if (response == "s_7736f08b")
	{
		doAnimationAction (player, "pound_fist_palm");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			naboo_privateer_trainer_1_action_grantMissionFour (player, npc);

			//-- NPC: Now hold on, there... there's no reason to fly off the handle. You've got a job to do, that's all. Now get out there and do it. And... good luck!
			string_id message = new string_id (c_stringFile, "s_73f727e7");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ok! Ready for an assignment already, eh? Well listen up! We've had reports of pirates in the system and we want you to run a patrol. Just make sure everything's on the up and up. This ought to be a piece of cake.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm off!
	if (response == "s_1db37a0f")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			naboo_privateer_trainer_1_action_grantMissionOne (player, npc);

			//-- NPC: That's the spirit. Good luck!
			string_id message = new string_id (c_stringFile, "s_b02e116");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's in it for me?
	if (response == "s_48566ef9")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Whadya mean what's in it for you?! Pride in a job well done, you dope! RSF will pay you, of course, but that's not why we do this job! We do it because we're the ROYAL FREAKING SECURITY FORCES!! Now get out of here and complete your assignment.
			string_id message = new string_id (c_stringFile, "s_bbbfb8db");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, RSF better pay me
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65d63b3c");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I'll train you... AFTER you complete a few assignments for me. Now about that patrol...
			string_id message = new string_id (c_stringFile, "s_8fb73c4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm off!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's in it for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1db37a0f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48566ef9");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Whadya mean what's in it for you?! Pride in a job well done, you dope! RSF will pay you, of course, but that's not why we do this job! We do it because we're the ROYAL FREAKING SECURITY FORCES!! Now get out of here and complete your assignment.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No problem.
	if (response == "s_c261cfae")
	{
		doAnimationAction (player, "snap_finger1");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_away");

			naboo_privateer_trainer_1_action_grantMissionOne (player, npc);

			//-- NPC: Go!
			string_id message = new string_id (c_stringFile, "s_7b3b28bf");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Well, RSF better pay me
	if (response == "s_65d63b3c")
	{
		doAnimationAction (player, "point_to_self");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			naboo_privateer_trainer_1_action_grantMissionOne (player, npc);

			//-- NPC: DIDN'T I JUST SAY WE'D PAY YOU?! NOW GO DO THE PATROL ALREADY! GO! GET OUT OF MY FACE BEFORE I HAVE YOU ARRESTED!
			string_id message = new string_id (c_stringFile, "s_f07f26ad");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'll train you... AFTER you complete a few assignments for me. Now about that patrol...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm off!
	if (response == "s_1db37a0f")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "goodbye");

			naboo_privateer_trainer_1_action_grantMissionOne (player, npc);

			//-- NPC: That's the spirit. Good luck!
			string_id message = new string_id (c_stringFile, "s_b02e116");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's in it for me?
	if (response == "s_48566ef9")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Whadya mean what's in it for you?! Pride in a job well done, you dope! RSF will pay you, of course, but that's not why we do this job! We do it because we're the ROYAL FREAKING SECURITY FORCES!! Now get out of here and complete your assignment.
			string_id message = new string_id (c_stringFile, "s_bbbfb8db");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No problem.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Well, RSF better pay me
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c261cfae");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_65d63b3c");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_7478cd28")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I'll train you... AFTER you complete a few assignments for me. Now about that patrol...
			string_id message = new string_id (c_stringFile, "s_8fb73c4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm off!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's in it for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1db37a0f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48566ef9");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch44 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ready for your next assignment? Good! Obviously we can't allow that random attack on you to stand, so now we need to send a message to those pirates. Go destroy four of the Black Sun pirates... That should send a clear message that we're not going to tolerate their activities.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want training!
	if (response == "s_fb1c19c1")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			//-- NPC: Not yet, %NU. I need you to complete a few assignments before I can train you any further.
			string_id message = new string_id (c_stringFile, "s_d487641c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But I'm experienced enough to learn more now!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b5b6e634");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 45);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not yet, %NU. I need you to complete a few assignments before I can train you any further.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: But I'm experienced enough to learn more now!
	if (response == "s_b5b6e634")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			naboo_privateer_trainer_1_action_grantMissionTwo (player, npc);

			//-- NPC: It's not all about experience, %TU. I need you to complete a few assignments before I train you.
			string_id message = new string_id (c_stringFile, "s_d07cf233");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That was a quick reaction to pick up the escort. It is a good thing we sent you on that patrol. The pilot sent over some money for you to cover any repairs you might have, and here's something to help you organize your things.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks!
	if (response == "s_c4682ea6")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: It's a mercenary bandolier. Pretty popular item among the freelance pilot crowd. Really handy to help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_d05d7112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Great!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f49e786");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is it?
	if (response == "s_1c8bddbb")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: It's a mercenary bandolier. Pretty popular item among the freelance pilot crowd. Really handy to help you keep your things organized.
			string_id message = new string_id (c_stringFile, "s_d05d7112");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Great!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3f49e786");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch48 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's a mercenary bandolier. Pretty popular item among the freelance pilot crowd. Really handy to help you keep your things organized.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Great!
	if (response == "s_3f49e786")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_hasVeryFirstQuest (player, npc))
		{
			//-- NPC: You've got your assignment, and the control device for your ship in your datapad. Go to Theed Hangar and access the terminal to launch into space.
			string_id message = new string_id (c_stringFile, "s_8853a92c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I go?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: How do I get back?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Where is my ship?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasSpaceExpansion (player, npc))
		{
			//-- NPC: Sorry, but I can't chit-chat with you. I'm busy doing important Royal Security work here.
			string_id message = new string_id (c_stringFile, "s_7c1fe68f");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_isRebelPilot (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: I think you're a pilot for the Rebel Alliance! Look, the Royal Security Forces don't have any use for the likes of you. I think you should leave now.
			string_id message = new string_id (c_stringFile, "s_72f94b8e");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_isImperialPilot (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: Greetings officer! Nothing to worry about here, we've got everything under control.
			string_id message = new string_id (c_stringFile, "s_7217d77");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_isPilotButNotRsfMember (player, npc))
		{
			//-- NPC: I've heard you're quite a pilot. What brings you to speak with the Royal Security Forces?
			string_id message = new string_id (c_stringFile, "s_6b25cc31");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm looking for a mission. Do you have any?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] This is what the NPC says when he talks to a player who is not currently signed-up with any pilot profession.
		if (!naboo_privateer_trainer_1_condition_hasNoviceSkillBox (player, npc))
		{
			doAnimationAction (npc, "greet");

			//-- NPC: Greetings citizen. Are you here to join the Royal Security Forces?
			string_id message = new string_id (c_stringFile, "s_465e6eeb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: No.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d70dba34");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4c695dbd");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasSpaceShip (player, npc))
		{
			naboo_privateer_trainer_1_action_grantSpaceShip (player, npc);

			//-- NPC: You're going to need a ship, of course. I'll add these control codes for a basic one to your datapad. You should consider upgrading to a better ship soon, though.
			string_id message = new string_id (c_stringFile, "s_b24af40a");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_alreadyHasAQuest (player, npc))
		{
			doAnimationAction (npc, "point_away");

			//-- NPC: You've got your orders! Stop standing around and get back on the job.
			string_id message = new string_id (c_stringFile, "s_61036267");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_hasAllTierOneSkills (player, npc))
		{
			//-- NPC: You have accomplished all the tasks I have set for you. Are you ready for some more advanced work?
			string_id message = new string_id (c_stringFile, "s_7c4368e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b3a9fd05");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_missionOneFailed (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: All I asked was for you to complete one simple patrol. What's the malfunction? Get back out there and secure the area!
			string_id message = new string_id (c_stringFile, "s_443c036b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It wasn't my fault. I was attacked!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Will do!
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_aec5c3dd");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ea059f38");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_missionTwoFailed (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			naboo_privateer_trainer_1_action_grantMissionTwo (player, npc);

			//-- NPC: You're a loose cannon! Do you want to be a member of Royal Security or not? Now get out there and complete your assignment! Black Sun isn't going to take over this system on *my* watch!
			string_id message = new string_id (c_stringFile, "s_659e0fa0");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_missionThreeFailed (player, npc))
		{
			doAnimationAction (npc, "cover_eyes");

			//-- NPC: Please tell me you're not going to make excuses... It's a simple patrol mission and you're standing here jabbering at me?! Go complete your assignment!
			string_id message = new string_id (c_stringFile, "s_652689c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Will do!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: It wasn't my fault. I was attacked!
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_56");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_missionFourFailed (player, npc))
		{
			doAnimationAction (npc, "smack_self");

			//-- NPC: Unacceptable! Unless you take out the Black Sun leader, they'll never stop harassing our transports and our citizens. You're a better pilot than that! Now go destroy that leader's ship!
			string_id message = new string_id (c_stringFile, "s_f742d1e8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Fine.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Don't worry.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Now it's personal.
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7eb4d42");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48b8131c");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7736f08b");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_beenRewardedForQuestOne (player, npc))
		{
			doAnimationAction (npc, "greet");

			naboo_privateer_trainer_1_action_rewardForQuestOne (player, npc);

			//-- NPC: Excellent work! It's a good thing we sent you on that patrol. Take this as your payment. If you want another assignment, then just come talk to me.
			string_id message = new string_id (c_stringFile, "s_5df0371b");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasCompletedQuestOne (player, npc))
		{
			//-- NPC: Ok! Ready for an assignment already, eh? Well listen up! We've had reports of pirates in the system and we want you to run a patrol. Just make sure everything's on the up and up. This ought to be a piece of cake.
			string_id message = new string_id (c_stringFile, "s_904d71cf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm off!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What's in it for me?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Will you train me after this?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1db37a0f");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48566ef9");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_beenRewardedForQuestTwo (player, npc))
		{
			doAnimationAction (npc, "greet");

			naboo_privateer_trainer_1_action_rewardForQuestTwo (player, npc);

			//-- NPC: Good work, %NU. Here's your payment.
			string_id message = new string_id (c_stringFile, "s_3d6c25c7");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasCompletedQuestTwo (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Ready for your next assignment? Good! Obviously we can't allow that random attack on you to stand, so now we need to send a message to those pirates. Go destroy four of the Black Sun pirates... That should send a clear message that we're not going to tolerate their activities.
			string_id message = new string_id (c_stringFile, "s_bcdb501b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I want training!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 44);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_beenRewardedForQuestThree (player, npc))
		{
			doAnimationAction (player, "pound_fist_palm");

			naboo_privateer_trainer_1_action_rewardForQuestThree (player, npc);

			//-- NPC: That was a quick reaction to pick up the escort. It is a good thing we sent you on that patrol. The pilot sent over some money for you to cover any repairs you might have, and here's something to help you organize your things.
			string_id message = new string_id (c_stringFile, "s_cd397d97");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks!
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: What is it?
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c4682ea6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1c8bddbb");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasCompletedQuestThree (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Right, back to work! Now that you've eliminated some of the riffraff out there, we want you to run a patrol and make sure there are no more of these criminals in the area.
			string_id message = new string_id (c_stringFile, "s_c29502");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What about training?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9237617f");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 49);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_beenRewardedForQuestFour (player, npc))
		{
			doAnimationAction (npc, "applause_excited");

			naboo_privateer_trainer_1_action_rewardForQuestFour (player, npc);

			//-- NPC: That's great work! With their leader destroyed, they're sure to be on the run. Let 'em go to Tatooine if they want to be criminals, but they're not going to aggress merchants while the RSF is on the job. Take these credits and get your ship repaired, if it needs it.
			string_id message = new string_id (c_stringFile, "s_6ea4aa1e");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: I don't know about you, but I've had about enough of these criminals operating in our system! We want you to take out their leader this time, and use all necessary force to defeat any wingmen that happen to be with him, too.
			string_id message = new string_id (c_stringFile, "s_966bc2b6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Will you train me after this?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_95");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 52);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_isReadyForFirstTraining (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You have demonstrated your competence to a very satisfactory degree. I think you're ready for some training.
			string_id message = new string_id (c_stringFile, "s_5e36283d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What kind of training?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_139");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_isReadyForMoreTraining (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: You're really coming along as a pilot, %TU. I think you're ready for some more training.
			string_id message = new string_id (c_stringFile, "s_122b0165");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What kind of training?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_143");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 57);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition_hasCompletedQuestFour (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			//-- NPC: Ready for an assignment? We have some duty missions available.
			string_id message = new string_id (c_stringFile, "s_63336624");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch49 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Right, back to work! Now that you've eliminated some of the riffraff out there, we want you to run a patrol and make sure there are no more of these criminals in the area.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What about training?
	if (response == "s_9237617f")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			naboo_privateer_trainer_1_action_grantMissionThree (player, npc);

			//-- NPC: There will be time for that later. Right now, you've got a job to do.
			string_id message = new string_id (c_stringFile, "s_e6c0bf4f");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know about you, but I've had about enough of these criminals operating in our system! We want you to take out their leader this time, and use all necessary force to defeat any wingmen that happen to be with him, too.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Will you train me after this?
	if (response == "s_95")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			naboo_privateer_trainer_1_action_grantMissionFour (player, npc);

			//-- NPC: Yes, yes, you have my word. Take out that leader and I will train you to be a better pilot.
			string_id message = new string_id (c_stringFile, "s_3175b359");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You have demonstrated your competence to a very satisfactory degree. I think you're ready for some training.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What kind of training?
	if (response == "s_139")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: I am going to train you in the use of basic starfighters. Are you ready?
			string_id message = new string_id (c_stringFile, "s_140");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I am.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_141");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 55);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch55 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am going to train you in the use of basic starfighters. Are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am.
	if (response == "s_141")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			naboo_privateer_trainer_1_action_grantStarshipSkill (player, npc);

			//-- NPC: Good.
			string_id message = new string_id (c_stringFile, "s_142");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch57 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're really coming along as a pilot, %TU. I think you're ready for some more training.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What kind of training?
	if (response == "s_143")
	{
		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasStarshipSkill1 (player, npc))
		{
			//-- NPC: I am going to train you in the use of basic starfighters. Are you ready?
			string_id message = new string_id (c_stringFile, "s_144");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I am.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_148");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 58);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasWeaponsSkill1 (player, npc))
		{
			//-- NPC: I am going to train you in the use of basic starship components. Are you ready?
			string_id message = new string_id (c_stringFile, "s_145");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I am.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_151");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!naboo_privateer_trainer_1_condition_hasProceduresSkill1 (player, npc))
		{
			//-- NPC: I am going to train you in basic starfighter pilot procedures. Are you ready?
			string_id message = new string_id (c_stringFile, "s_146");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I am.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_155");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: I am going to train you in droid interface basics. Are you ready?
			string_id message = new string_id (c_stringFile, "s_147");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I am.
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_159");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 64);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch58 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am going to train you in the use of basic starfighters. Are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am.
	if (response == "s_148")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			naboo_privateer_trainer_1_action_buyStarshipSkill1 (player, npc);

			//-- NPC: Good.
			string_id message = new string_id (c_stringFile, "s_149");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch60 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am going to train you in the use of basic starship components. Are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am.
	if (response == "s_151")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			naboo_privateer_trainer_1_action_buyWeaponsSkill1 (player, npc);

			//-- NPC: Good.
			string_id message = new string_id (c_stringFile, "s_153");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am going to train you in basic starfighter pilot procedures. Are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am.
	if (response == "s_155")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			naboo_privateer_trainer_1_action_buyProcedureSkill1 (player, npc);

			//-- NPC: Good.
			string_id message = new string_id (c_stringFile, "s_157");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am going to train you in droid interface basics. Are you ready?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I am.
	if (response == "s_159")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			naboo_privateer_trainer_1_action_buyDroidSkill1 (player, npc);

			//-- NPC: Good.
			string_id message = new string_id (c_stringFile, "s_161");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch66 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Ready for an assignment? We have some duty missions available.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is an open-ended task. That means that there is no true end to it, you just finish whenever you're ready. It's a good way to earn experience to prepare yourself for training. So, what do you say?
			string_id message = new string_id (c_stringFile, "s_a8cdf3dc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would like a duty mission.
	if (response == "s_e814cf37")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good, good. We have a couple to choose from. You can either fight the Black Sun menace in a Destroy Duty, or you can assist the merchant freighters with an Escort Duty.
			string_id message = new string_id (c_stringFile, "s_e38f886b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I want Destroy Duty
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want Escort Duty
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Eh, I changed my mind.
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_40759c25");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_629c7d23");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d36a0e4");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How about some training?
	if (response == "s_db74ddc6")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: There's nothing in which I can train you right now. But a duty mission is a great way to earn some experience for your next training session. What do you say?
			string_id message = new string_id (c_stringFile, "s_f4245779");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now, thanks.
	if (response == "s_2883b989")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: OK, OK. Come back when you're ready.
			string_id message = new string_id (c_stringFile, "s_138");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch67 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A duty mission is an open-ended task. That means that there is no true end to it, you just finish whenever you're ready. It's a good way to earn experience to prepare yourself for training. So, what do you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is an open-ended task. That means that there is no true end to it, you just finish whenever you're ready. It's a good way to earn experience to prepare yourself for training. So, what do you say?
			string_id message = new string_id (c_stringFile, "s_a8cdf3dc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would like a duty mission.
	if (response == "s_e814cf37")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good, good. We have a couple to choose from. You can either fight the Black Sun menace in a Destroy Duty, or you can assist the merchant freighters with an Escort Duty.
			string_id message = new string_id (c_stringFile, "s_e38f886b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I want Destroy Duty
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want Escort Duty
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Eh, I changed my mind.
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_40759c25");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_629c7d23");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d36a0e4");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How about some training?
	if (response == "s_db74ddc6")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: There's nothing in which I can train you right now. But a duty mission is a great way to earn some experience for your next training session. What do you say?
			string_id message = new string_id (c_stringFile, "s_f4245779");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now, thanks.
	if (response == "s_2883b989")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: OK, OK. Come back when you're ready.
			string_id message = new string_id (c_stringFile, "s_138");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch68 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good, good. We have a couple to choose from. You can either fight the Black Sun menace in a Destroy Duty, or you can assist the merchant freighters with an Escort Duty.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want Destroy Duty
	if (response == "s_40759c25")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			naboo_privateer_trainer_1_action_grantDestroyDutyMission (player, npc);

			//-- NPC: Get up there and put those Black Sun in their place.
			string_id message = new string_id (c_stringFile, "s_6575e5d0");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I want Escort Duty
	if (response == "s_629c7d23")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			naboo_privateer_trainer_1_action_grantEscortDutyMission (player, npc);

			//-- NPC: We'll pay for every transport that makes it through.
			string_id message = new string_id (c_stringFile, "s_90b16aa7");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Eh, I changed my mind.
	if (response == "s_3d36a0e4")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: OK, OK. Come back when you're ready.
			string_id message = new string_id (c_stringFile, "s_792ad840");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int naboo_privateer_trainer_1_handleBranch72 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: There's nothing in which I can train you right now. But a duty mission is a great way to earn some experience for your next training session. What do you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is a duty mission?
	if (response == "s_6106187c")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: A duty mission is an open-ended task. That means that there is no true end to it, you just finish whenever you're ready. It's a good way to earn experience to prepare yourself for training. So, what do you say?
			string_id message = new string_id (c_stringFile, "s_a8cdf3dc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would like a duty mission.
	if (response == "s_e814cf37")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: Good, good. We have a couple to choose from. You can either fight the Black Sun menace in a Destroy Duty, or you can assist the merchant freighters with an Escort Duty.
			string_id message = new string_id (c_stringFile, "s_e38f886b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I want Destroy Duty
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I want Escort Duty
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Eh, I changed my mind.
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_40759c25");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_629c7d23");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3d36a0e4");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How about some training?
	if (response == "s_db74ddc6")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: There's nothing in which I can train you right now. But a duty mission is a great way to earn some experience for your next training session. What do you say?
			string_id message = new string_id (c_stringFile, "s_f4245779");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What is a duty mission?
			boolean hasResponse0 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I would like a duty mission.
			boolean hasResponse1 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: How about some training?
			boolean hasResponse2 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: Not right now, thanks.
			boolean hasResponse3 = false;
			if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

				utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not right now, thanks.
	if (response == "s_2883b989")
	{
		//-- [NOTE] 
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			//-- NPC: OK, OK. Come back when you're ready.
			string_id message = new string_id (c_stringFile, "s_138");
			utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.naboo_privateer_trainer_1");

	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );


	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.naboo_privateer_trainer_1");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_hasVeryFirstQuest (player, npc))
	{
		//-- NPC: You've got your assignment, and the control device for your ship in your datapad. Go to Theed Hangar and access the terminal to launch into space.
		string_id message = new string_id (c_stringFile, "s_8853a92c");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Where do I go?
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: How do I get back?
		boolean hasResponse1 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Where is my ship?
		boolean hasResponse2 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_60c4f974");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_85dd7d6c");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4358efe9");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 1);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_hasSpaceExpansion (player, npc))
	{
		//-- NPC: Sorry, but I can't chit-chat with you. I'm busy doing important Royal Security work here.
		string_id message = new string_id (c_stringFile, "s_7c1fe68f");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_isRebelPilot (player, npc))
	{
		doAnimationAction (npc, "slow_down");

		//-- NPC: I think you're a pilot for the Rebel Alliance! Look, the Royal Security Forces don't have any use for the likes of you. I think you should leave now.
		string_id message = new string_id (c_stringFile, "s_72f94b8e");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_isImperialPilot (player, npc))
	{
		doAnimationAction (npc, "salute1");

		//-- NPC: Greetings officer! Nothing to worry about here, we've got everything under control.
		string_id message = new string_id (c_stringFile, "s_7217d77");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_isPilotButNotRsfMember (player, npc))
	{
		//-- NPC: I've heard you're quite a pilot. What brings you to speak with the Royal Security Forces?
		string_id message = new string_id (c_stringFile, "s_6b25cc31");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm looking for a mission. Do you have any?
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c1ff5062");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 8);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] This is what the NPC says when he talks to a player who is not currently signed-up with any pilot profession.
	if (!naboo_privateer_trainer_1_condition_hasNoviceSkillBox (player, npc))
	{
		doAnimationAction (npc, "greet");

		//-- NPC: Greetings citizen. Are you here to join the Royal Security Forces?
		string_id message = new string_id (c_stringFile, "s_465e6eeb");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes.
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No.
		boolean hasResponse1 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d70dba34");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4c695dbd");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 11);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_hasSpaceShip (player, npc))
	{
		naboo_privateer_trainer_1_action_grantSpaceShip (player, npc);

		//-- NPC: You're going to need a ship, of course. I'll add these control codes for a basic one to your datapad. You should consider upgrading to a better ship soon, though.
		string_id message = new string_id (c_stringFile, "s_b24af40a");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_alreadyHasAQuest (player, npc))
	{
		doAnimationAction (npc, "point_away");

		//-- NPC: You've got your orders! Stop standing around and get back on the job.
		string_id message = new string_id (c_stringFile, "s_61036267");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_hasAllTierOneSkills (player, npc))
	{
		//-- NPC: You have accomplished all the tasks I have set for you. Are you ready for some more advanced work?
		string_id message = new string_id (c_stringFile, "s_7c4368e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Sure!
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b3a9fd05");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 23);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_missionOneFailed (player, npc))
	{
		doAnimationAction (npc, "shake_head_disgust");

		//-- NPC: All I asked was for you to complete one simple patrol. What's the malfunction? Get back out there and secure the area!
		string_id message = new string_id (c_stringFile, "s_443c036b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: It wasn't my fault. I was attacked!
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Will do!
		boolean hasResponse1 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_aec5c3dd");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ea059f38");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 25);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_missionTwoFailed (player, npc))
	{
		doAnimationAction (npc, "point_accusingly");

		naboo_privateer_trainer_1_action_grantMissionTwo (player, npc);

		//-- NPC: You're a loose cannon! Do you want to be a member of Royal Security or not? Now get out there and complete your assignment! Black Sun isn't going to take over this system on *my* watch!
		string_id message = new string_id (c_stringFile, "s_659e0fa0");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_missionThreeFailed (player, npc))
	{
		doAnimationAction (npc, "cover_eyes");

		//-- NPC: Please tell me you're not going to make excuses... It's a simple patrol mission and you're standing here jabbering at me?! Go complete your assignment!
		string_id message = new string_id (c_stringFile, "s_652689c");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Will do!
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: It wasn't my fault. I was attacked!
		boolean hasResponse1 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_52");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_56");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 29);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_missionFourFailed (player, npc))
	{
		doAnimationAction (npc, "smack_self");

		//-- NPC: Unacceptable! Unless you take out the Black Sun leader, they'll never stop harassing our transports and our citizens. You're a better pilot than that! Now go destroy that leader's ship!
		string_id message = new string_id (c_stringFile, "s_f742d1e8");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Fine.
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Don't worry.
		boolean hasResponse1 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Now it's personal.
		boolean hasResponse2 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7eb4d42");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_48b8131c");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7736f08b");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 32);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_beenRewardedForQuestOne (player, npc))
	{
		doAnimationAction (npc, "greet");

		naboo_privateer_trainer_1_action_rewardForQuestOne (player, npc);

		//-- NPC: Excellent work! It's a good thing we sent you on that patrol. Take this as your payment. If you want another assignment, then just come talk to me.
		string_id message = new string_id (c_stringFile, "s_5df0371b");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_hasCompletedQuestOne (player, npc))
	{
		//-- NPC: Ok! Ready for an assignment already, eh? Well listen up! We've had reports of pirates in the system and we want you to run a patrol. Just make sure everything's on the up and up. This ought to be a piece of cake.
		string_id message = new string_id (c_stringFile, "s_904d71cf");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm off!
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What's in it for me?
		boolean hasResponse1 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Will you train me after this?
		boolean hasResponse2 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1db37a0f");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_48566ef9");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7478cd28");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 37);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_beenRewardedForQuestTwo (player, npc))
	{
		doAnimationAction (npc, "greet");

		naboo_privateer_trainer_1_action_rewardForQuestTwo (player, npc);

		//-- NPC: Good work, %NU. Here's your payment.
		string_id message = new string_id (c_stringFile, "s_3d6c25c7");
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_hasCompletedQuestTwo (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Ready for your next assignment? Good! Obviously we can't allow that random attack on you to stand, so now we need to send a message to those pirates. Go destroy four of the Black Sun pirates... That should send a clear message that we're not going to tolerate their activities.
		string_id message = new string_id (c_stringFile, "s_bcdb501b");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I want training!
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_fb1c19c1");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 44);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_beenRewardedForQuestThree (player, npc))
	{
		doAnimationAction (player, "pound_fist_palm");

		naboo_privateer_trainer_1_action_rewardForQuestThree (player, npc);

		//-- NPC: That was a quick reaction to pick up the escort. It is a good thing we sent you on that patrol. The pilot sent over some money for you to cover any repairs you might have, and here's something to help you organize your things.
		string_id message = new string_id (c_stringFile, "s_cd397d97");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Thanks!
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What is it?
		boolean hasResponse1 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c4682ea6");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1c8bddbb");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 47);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_hasCompletedQuestThree (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: Right, back to work! Now that you've eliminated some of the riffraff out there, we want you to run a patrol and make sure there are no more of these criminals in the area.
		string_id message = new string_id (c_stringFile, "s_c29502");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What about training?
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9237617f");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 49);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_beenRewardedForQuestFour (player, npc))
	{
		doAnimationAction (npc, "applause_excited");

		naboo_privateer_trainer_1_action_rewardForQuestFour (player, npc);

		//-- NPC: That's great work! With their leader destroyed, they're sure to be on the run. Let 'em go to Tatooine if they want to be criminals, but they're not going to aggress merchants while the RSF is on the job. Take these credits and get your ship repaired, if it needs it.
		string_id message = new string_id (c_stringFile, "s_6ea4aa1e");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!naboo_privateer_trainer_1_condition_hasCompletedQuestFour (player, npc))
	{
		doAnimationAction (npc, "pound_fist_palm");

		//-- NPC: I don't know about you, but I've had about enough of these criminals operating in our system! We want you to take out their leader this time, and use all necessary force to defeat any wingmen that happen to be with him, too.
		string_id message = new string_id (c_stringFile, "s_966bc2b6");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Will you train me after this?
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_95");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 52);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_isReadyForFirstTraining (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: You have demonstrated your competence to a very satisfactory degree. I think you're ready for some training.
		string_id message = new string_id (c_stringFile, "s_5e36283d");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What kind of training?
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_139");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 54);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_isReadyForMoreTraining (player, npc))
	{
		doAnimationAction (npc, "explain");

		//-- NPC: You're really coming along as a pilot, %TU. I think you're ready for some more training.
		string_id message = new string_id (c_stringFile, "s_122b0165");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What kind of training?
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_143");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 57);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (naboo_privateer_trainer_1_condition_hasCompletedQuestFour (player, npc))
	{
		doAnimationAction (npc, "pose_proudly");

		//-- NPC: Ready for an assignment? We have some duty missions available.
		string_id message = new string_id (c_stringFile, "s_63336624");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What is a duty mission?
		boolean hasResponse0 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I would like a duty mission.
		boolean hasResponse1 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: How about some training?
		boolean hasResponse2 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Not right now, thanks.
		boolean hasResponse3 = false;
		if (naboo_privateer_trainer_1_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6106187c");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e814cf37");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_db74ddc6");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_2883b989");

			utils.setScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId", 66);

			npcStartConversation (player, npc, "naboo_privateer_trainer_1", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "naboo_privateer_trainer_1")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

	if (branchId == 1 && naboo_privateer_trainer_1_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && naboo_privateer_trainer_1_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && naboo_privateer_trainer_1_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && naboo_privateer_trainer_1_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && naboo_privateer_trainer_1_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && naboo_privateer_trainer_1_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && naboo_privateer_trainer_1_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && naboo_privateer_trainer_1_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && naboo_privateer_trainer_1_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && naboo_privateer_trainer_1_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && naboo_privateer_trainer_1_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && naboo_privateer_trainer_1_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && naboo_privateer_trainer_1_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && naboo_privateer_trainer_1_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && naboo_privateer_trainer_1_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && naboo_privateer_trainer_1_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && naboo_privateer_trainer_1_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && naboo_privateer_trainer_1_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && naboo_privateer_trainer_1_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && naboo_privateer_trainer_1_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 44 && naboo_privateer_trainer_1_handleBranch44 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && naboo_privateer_trainer_1_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && naboo_privateer_trainer_1_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 48 && naboo_privateer_trainer_1_handleBranch48 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 49 && naboo_privateer_trainer_1_handleBranch49 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && naboo_privateer_trainer_1_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && naboo_privateer_trainer_1_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 55 && naboo_privateer_trainer_1_handleBranch55 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 57 && naboo_privateer_trainer_1_handleBranch57 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 58 && naboo_privateer_trainer_1_handleBranch58 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 60 && naboo_privateer_trainer_1_handleBranch60 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && naboo_privateer_trainer_1_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && naboo_privateer_trainer_1_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 66 && naboo_privateer_trainer_1_handleBranch66 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 67 && naboo_privateer_trainer_1_handleBranch67 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 68 && naboo_privateer_trainer_1_handleBranch68 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 72 && naboo_privateer_trainer_1_handleBranch72 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.naboo_privateer_trainer_1.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

