// ======================================================================
//
// lok_rebel_trainer_2_smuggler.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.space_flags;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/lok_rebel_trainer_2_smuggler";

// ======================================================================
// Script Conditions
// ======================================================================

boolean lok_rebel_trainer_2_smuggler_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean lok_rebel_trainer_2_smuggler_condition_isRightPlayer1 (obj_id player, obj_id npc)
{
	if (space_flags.getIntSpaceFlag(player, "viopaSmuggler") == 1)
		return true;
	
	return false;
}

// ----------------------------------------------------------------------

boolean lok_rebel_trainer_2_smuggler_condition_isRightPlayer2 (obj_id player, obj_id npc)
{
	if (space_flags.getIntSpaceFlag(player, "viopaSmuggler") == 3)
		return true;
		
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void lok_rebel_trainer_2_smuggler_action_face (obj_id player, obj_id npc)
{
	faceTo (npc, player);
}

// ----------------------------------------------------------------------

void lok_rebel_trainer_2_smuggler_action_addSmuggler2 (obj_id player, obj_id npc)
{
	space_flags.setSpaceFlag(player, "viopaSmuggler", 4);
}

// ----------------------------------------------------------------------

void lok_rebel_trainer_2_smuggler_action_addSmuggler1 (obj_id player, obj_id npc)
{
	space_flags.setSpaceFlag(player, "viopaSmuggler", 2);
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int lok_rebel_trainer_2_smuggler_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you the pilot?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes. I was told that you could help me.
	if (response == "s_fca23d59")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			//-- NPC: Yes maybe so. You have a, uh, 'package' that you can't open?
			string_id message = new string_id (c_stringFile, "s_a3c154e6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Correct.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6bb0cb94");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 2);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, I'm with the CorSec anti smuggling department.
	if (response == "s_a46f0bb8")
	{
		doAnimationAction (player, "point_accusingly");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "implore");

			//-- NPC: Wh.. what do you want? I haven't done anything, I swear!
			string_id message = new string_id (c_stringFile, "s_ed7e2471");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Calm down silly, I'm just kidding. I am the pilot.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b9280ab0");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes maybe so. You have a, uh, 'package' that you can't open?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Correct.
	if (response == "s_6bb0cb94")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: I know a man that specializes in opening 'packages'. He's a bit paranoid though but when I was contacted by Nym, I made some arrangements. He's agreed to meet you in the Dantooine system... he won't land. You need to meet with him there and transfer the 'package' that you need him to open.
			string_id message = new string_id (c_stringFile, "s_8dc05a21");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good, thank you Vrak.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50e93f2d");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I know a man that specializes in opening 'packages'. He's a bit paranoid though but when I was contacted by Nym, I made some arrangements. He's agreed to meet you in the Dantooine system... he won't land. You need to meet with him there and transfer the 'package' that you need him to open.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good, thank you Vrak.
	if (response == "s_50e93f2d")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nervous");

			lok_rebel_trainer_2_smuggler_action_addSmuggler1 (player, npc);

			//-- NPC: Sure sure, now go back to your commanding officer and get that data to my friend so he can 'open' it.
			string_id message = new string_id (c_stringFile, "s_871202ae");
			utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Wh.. what do you want? I haven't done anything, I swear!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Calm down silly, I'm just kidding. I am the pilot.
	if (response == "s_b9280ab0")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nervous");

			//-- NPC: What are you trying to do? Get me killed? Now what did you need help with?
			string_id message = new string_id (c_stringFile, "s_d36158bb");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You are supposed to know a good 'translator'?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d5132e66");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What are you trying to do? Get me killed? Now what did you need help with?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You are supposed to know a good 'translator'?
	if (response == "s_d5132e66")
	{
		doAnimationAction (player, "whisper");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "whisper");

			//-- NPC: The best. He's a bit paranoid though but when I was contacted by Nym, I made some arrangements. He's agreed to meet you in the Dantooine system... he won't land. You need to meet with him there and transfer the information that you need him to 'translate'.
			string_id message = new string_id (c_stringFile, "s_53a57295");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds good, is there anything else I should know?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6541ff8b");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The best. He's a bit paranoid though but when I was contacted by Nym, I made some arrangements. He's agreed to meet you in the Dantooine system... he won't land. You need to meet with him there and transfer the information that you need him to 'translate'.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds good, is there anything else I should know?
	if (response == "s_6541ff8b")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nervous");

			//-- NPC: What do you mean..?
			string_id message = new string_id (c_stringFile, "s_5dd70a2c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I was just asking, calm down. Well, see you around.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3103c633");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What do you mean..?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I was just asking, calm down. Well, see you around.
	if (response == "s_3103c633")
	{
		doAnimationAction (player, "slow_down");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			lok_rebel_trainer_2_smuggler_action_addSmuggler1 (player, npc);

			//-- NPC: Sure sure, now go back to your commanding officer and get that data to my friend so he can 'translate' it.
			string_id message = new string_id (c_stringFile, "s_1106d288");
			utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Wh..what are you doing here?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: We have some unfinished business I believe.
	if (response == "s_88fb9bcb")
	{
		doAnimationAction (player, "point_accusingly");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "implore");

			//-- NPC: Please, I didn't mean to do it, I had to! Don't hurt me, I'll tell you everything I know!
			string_id message = new string_id (c_stringFile, "s_bf37655b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm waiting.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: So I don't even have to break any of your bones? Pity.
			boolean hasResponse1 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_73e609b7");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f911cddf");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Oh I'm just here to check if you're blaster-proof.
	if (response == "s_e28286fc")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "implore");

			//-- NPC: Please, I didn't mean to do it, I had to! Don't hurt me, I'll tell you everything I know!
			string_id message = new string_id (c_stringFile, "s_50");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm waiting.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: So I don't even have to break any of your bones? Pity.
			boolean hasResponse1 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_64");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please, I didn't mean to do it, I had to! Don't hurt me, I'll tell you everything I know!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm waiting.
	if (response == "s_73e609b7")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: A while ago I was contacted by someone named Kud'ar Mub'at, he's the type of 'person' that you don't say no to. He offered a large sum of money for any information on some stolen Imperial data.
			string_id message = new string_id (c_stringFile, "s_9da8a554");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Kud'ar Mub'at?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Go on.
			boolean hasResponse1 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d7fe1a24");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_edb238bc");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 12);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So I don't even have to break any of your bones? Pity.
	if (response == "s_f911cddf")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			//-- NPC: I'm not a fighter, I'll tell you all I know. A while ago I was contacted by someone named Kud'ar Mub'at, he's the type of 'person' that you don't say no to. He offered a large sum of money for any information on some stolen Imperial data.
			string_id message = new string_id (c_stringFile, "s_99dcc230");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Kud'ar Mub'at?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Go on.
			boolean hasResponse1 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_41");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_47");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A while ago I was contacted by someone named Kud'ar Mub'at, he's the type of 'person' that you don't say no to. He offered a large sum of money for any information on some stolen Imperial data.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Kud'ar Mub'at?
	if (response == "s_d7fe1a24")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I don't know who he really is, not many have seen him. I only know that he's some sort of shady information broker but he has a lot of power.
			string_id message = new string_id (c_stringFile, "s_4e307ac2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, go on.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2387d3d7");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Go on.
	if (response == "s_edb238bc")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			lok_rebel_trainer_2_smuggler_action_addSmuggler2 (player, npc);

			//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.
			string_id message = new string_id (c_stringFile, "s_f7deed3b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What else?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6789e90e");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know who he really is, not many have seen him. I only know that he's some sort of shady information broker but he has a lot of power.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, go on.
	if (response == "s_2387d3d7")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			lok_rebel_trainer_2_smuggler_action_addSmuggler2 (player, npc);

			//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.
			string_id message = new string_id (c_stringFile, "s_f7deed3b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What else?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6789e90e");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What else?
	if (response == "s_6789e90e")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "weeping");

			//-- NPC: That's all I know, I swear! Please don't hurt me, my life is just starting. I'm nothing but a pawn, please...
			string_id message = new string_id (c_stringFile, "s_cf66be5a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You're lucky I'm in a hurry. I'm sure Nym will take care of you.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I think you're in the wrong business. Pathetic.
			boolean hasResponse1 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b5dd2ed8");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_29b10239");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's all I know, I swear! Please don't hurt me, my life is just starting. I'm nothing but a pawn, please...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You're lucky I'm in a hurry. I'm sure Nym will take care of you.
	if (response == "s_b5dd2ed8")
	{
		doAnimationAction (player, "slit_throat");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: I need to get out of here!
			string_id message = new string_id (c_stringFile, "s_d0098db3");
			utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I think you're in the wrong business. Pathetic.
	if (response == "s_29b10239")
	{
		doAnimationAction (player, "belly_laugh");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "weeping");

			//-- NPC: I will change, I promise..
			string_id message = new string_id (c_stringFile, "s_f39484e1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nym will probably make sure of that.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6b133072");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I will change, I promise..

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nym will probably make sure of that.
	if (response == "s_6b133072")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: I need to get out of here!
			string_id message = new string_id (c_stringFile, "s_37");
			utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm not a fighter, I'll tell you all I know. A while ago I was contacted by someone named Kud'ar Mub'at, he's the type of 'person' that you don't say no to. He offered a large sum of money for any information on some stolen Imperial data.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Kud'ar Mub'at?
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I don't know who he really is, not many have seen him. I only know that he's some sort of shady information broker but he has a lot of power.
			string_id message = new string_id (c_stringFile, "s_43");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, go on.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_45");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Go on.
	if (response == "s_47")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			lok_rebel_trainer_2_smuggler_action_addSmuggler2 (player, npc);

			//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.
			string_id message = new string_id (c_stringFile, "s_f7deed3b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What else?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6789e90e");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know who he really is, not many have seen him. I only know that he's some sort of shady information broker but he has a lot of power.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, go on.
	if (response == "s_45")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			lok_rebel_trainer_2_smuggler_action_addSmuggler2 (player, npc);

			//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.
			string_id message = new string_id (c_stringFile, "s_f7deed3b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What else?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6789e90e");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Please, I didn't mean to do it, I had to! Don't hurt me, I'll tell you everything I know!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm waiting.
	if (response == "s_52")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			//-- NPC: A while ago I was contacted by someone named Kud'ar Mub'at, he's the type of 'person' that you don't say no to. He offered a large sum of money for any information on some stolen Imperial data.
			string_id message = new string_id (c_stringFile, "s_54");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Kud'ar Mub'at?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Go on.
			boolean hasResponse1 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_56");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_62");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: So I don't even have to break any of your bones? Pity.
	if (response == "s_64")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "fishing_cast");

			//-- NPC: I'm not a fighter, I'll tell you all I know. A while ago I was contacted by someone named Kud'ar Mub'at, he's the type of 'person' that you don't say no to. He offered a large sum of money for any information on some stolen Imperial data.
			string_id message = new string_id (c_stringFile, "s_66");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Kud'ar Mub'at?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Go on.
			boolean hasResponse1 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_74");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A while ago I was contacted by someone named Kud'ar Mub'at, he's the type of 'person' that you don't say no to. He offered a large sum of money for any information on some stolen Imperial data.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Kud'ar Mub'at?
	if (response == "s_56")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I don't know who he really is, not many have seen him. I only know that he's some sort of shady information broker but he has a lot of power.
			string_id message = new string_id (c_stringFile, "s_58");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, go on.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_60");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Go on.
	if (response == "s_62")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			lok_rebel_trainer_2_smuggler_action_addSmuggler2 (player, npc);

			//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.
			string_id message = new string_id (c_stringFile, "s_f7deed3b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What else?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6789e90e");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know who he really is, not many have seen him. I only know that he's some sort of shady information broker but he has a lot of power.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, go on.
	if (response == "s_60")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			lok_rebel_trainer_2_smuggler_action_addSmuggler2 (player, npc);

			//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.
			string_id message = new string_id (c_stringFile, "s_f7deed3b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What else?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6789e90e");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm not a fighter, I'll tell you all I know. A while ago I was contacted by someone named Kud'ar Mub'at, he's the type of 'person' that you don't say no to. He offered a large sum of money for any information on some stolen Imperial data.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Kud'ar Mub'at?
	if (response == "s_68")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_hands");

			//-- NPC: I don't know who he really is, not many have seen him. I only know that he's some sort of shady information broker but he has a lot of power.
			string_id message = new string_id (c_stringFile, "s_70");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, go on.
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_72");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Go on.
	if (response == "s_74")
	{
		doAnimationAction (player, "nod_head_multiple");

		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			lok_rebel_trainer_2_smuggler_action_addSmuggler2 (player, npc);

			//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.
			string_id message = new string_id (c_stringFile, "s_f7deed3b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What else?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6789e90e");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int lok_rebel_trainer_2_smuggler_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't know who he really is, not many have seen him. I only know that he's some sort of shady information broker but he has a lot of power.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, go on.
	if (response == "s_72")
	{
		//-- [NOTE] 
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			lok_rebel_trainer_2_smuggler_action_addSmuggler2 (player, npc);

			//-- NPC: When I was contacted by one of Nym's people about the data that they couldn't encrypt, I put two and two together. I sent Kud'ar what he needed to know to intercept you and the hacker.
			string_id message = new string_id (c_stringFile, "s_f7deed3b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What else?
			boolean hasResponse0 = false;
			if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6789e90e");

				utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 14);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.lok_rebel_trainer_2_smuggler");

	setCondition (self, CONDITION_CONVERSABLE);
	setName (self, "Vrak");
	ai_lib.setDefaultCalmMood (self, "nervous");
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );


	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setName (self, "Vrak");
	ai_lib.setDefaultCalmMood (self, "nervous");
	setInvulnerable( self, true );
	setCondition( self, CONDITION_SPACE_INTERESTING );

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.lok_rebel_trainer_2_smuggler");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (lok_rebel_trainer_2_smuggler_condition_isRightPlayer1 (player, npc))
	{
		lok_rebel_trainer_2_smuggler_action_face (player, npc);

		//-- NPC: Are you the pilot?
		string_id message = new string_id (c_stringFile, "s_c5fc3d7a");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Yes. I was told that you could help me.
		boolean hasResponse0 = false;
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: No, I'm with the CorSec anti smuggling department.
		boolean hasResponse1 = false;
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_fca23d59");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_a46f0bb8");

			utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 1);

			npcStartConversation (player, npc, "lok_rebel_trainer_2_smuggler", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (lok_rebel_trainer_2_smuggler_condition_isRightPlayer2 (player, npc))
	{
		doAnimationAction (npc, "nervous");

		lok_rebel_trainer_2_smuggler_action_face (player, npc);

		//-- NPC: Wh..what are you doing here?!
		string_id message = new string_id (c_stringFile, "s_9b75f1f");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: We have some unfinished business I believe.
		boolean hasResponse0 = false;
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Oh I'm just here to check if you're blaster-proof.
		boolean hasResponse1 = false;
		if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_88fb9bcb");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e28286fc");

			utils.setScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId", 10);

			npcStartConversation (player, npc, "lok_rebel_trainer_2_smuggler", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (lok_rebel_trainer_2_smuggler_condition__defaultCondition (player, npc))
	{
		doAnimationAction (npc, "dismiss");

		lok_rebel_trainer_2_smuggler_action_face (player, npc);

		//-- NPC: What do you want? I'm busy!
		string_id message = new string_id (c_stringFile, "s_b02a22c2");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "lok_rebel_trainer_2_smuggler")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

	if (branchId == 1 && lok_rebel_trainer_2_smuggler_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && lok_rebel_trainer_2_smuggler_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && lok_rebel_trainer_2_smuggler_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && lok_rebel_trainer_2_smuggler_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && lok_rebel_trainer_2_smuggler_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && lok_rebel_trainer_2_smuggler_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && lok_rebel_trainer_2_smuggler_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && lok_rebel_trainer_2_smuggler_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && lok_rebel_trainer_2_smuggler_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && lok_rebel_trainer_2_smuggler_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && lok_rebel_trainer_2_smuggler_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && lok_rebel_trainer_2_smuggler_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && lok_rebel_trainer_2_smuggler_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && lok_rebel_trainer_2_smuggler_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && lok_rebel_trainer_2_smuggler_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && lok_rebel_trainer_2_smuggler_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && lok_rebel_trainer_2_smuggler_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && lok_rebel_trainer_2_smuggler_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && lok_rebel_trainer_2_smuggler_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && lok_rebel_trainer_2_smuggler_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && lok_rebel_trainer_2_smuggler_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.lok_rebel_trainer_2_smuggler.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

