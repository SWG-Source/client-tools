// ======================================================================
//
// legacy_naboo_lt_col_typho.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.badge;
include library.chat;
include library.content;
include library.groundquests;
include library.space_quest;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/legacy_naboo_lt_col_typho";

// ======================================================================
// Script Conditions
// ======================================================================

boolean legacy_naboo_lt_col_typho_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest (player, "quest/typho_police_one"))
	{
		return true;
	}
	return false;		
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_didMarineWorkOne (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest (player, "quest/typho_marine_one"))
	{
		return true;
	}
	return false;				
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_didPilotWorkOne (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest (player, "quest/typho_pilot_one"))
	{
		return true;
	}
	return false;			
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_worksForTypho (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest (player, "quest/typho_police_one")||
	(groundquests.hasCompletedQuest (player, "quest/typho_marine_one"))||
	(groundquests.hasCompletedQuest(player, "quest/typho_pilot_one")))
	{
		return true;
	}
	return false;				
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_donePiloting (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest(player, "quest/typho_pilot_two"))
	{
		return true;
	}
	return false;			
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_donePolicing (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest (player, "quest/typho_police_two"))
	{
		return true;
	}
	return false;			
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_doneMarining (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest (player, "quest/typho_marine_two"))
	{
		return true;
	}
	return false;		
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_doneAll (obj_id player, obj_id npc)
{
	if (groundquests.hasCompletedQuest (player, "quest/typho_police_two")
	&& groundquests.hasCompletedQuest (player, "quest/typho_marine_two")
	&& groundquests.hasCompletedQuest(player, "quest/typho_pilot_two"))
	{
		return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_finishingPoliceOne (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive (player, "quest/typho_police_one", "waitForTypho"))
	{
		return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_finishingPoliceTwo (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive (player, "quest/typho_police_two", "killedDoogal"))
	{
		return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_finishingPilotOne (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive (player, "quest/typho_pilot_one", "finishingPilotOne"))
	{
		return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_finishingPilotTwo (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive (player, "quest/typho_pilot_two", "starwingEliminated"))
	{
		return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_finishingMarineOne (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive (player, "quest/typho_marine_one", "weaponsCacheDestroyed"))
	{
		return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_finishingMarineTwo (obj_id player, obj_id npc)
{
	if (groundquests.isTaskActive (player, "quest/typho_marine_two", "killedMaulerLeaders"))
	{
		return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_isWorkingForTypho (obj_id player, obj_id npc)
{
	if (groundquests.isQuestActive (player, "quest/typho_police_one") ||
	groundquests.isQuestActive (player, "quest/typho_marine_one") || 
	groundquests.isQuestActive (player, "quest/typho_pilot_one") ||
	groundquests.isQuestActive (player, "quest/typho_police_two") || 
	groundquests.isQuestActive (player, "quest/typho_marine_two") || 
	groundquests.isQuestActive (player, "quest/typho_pilot_two"))
	{
		return true;
	}
	return false;		
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_readyForTypho (obj_id player, obj_id npc)
{
	return groundquests.hasCompletedQuest(player, "naboo_kadaraa_tipping_the_balance_2");
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_readyForPooja (obj_id player, obj_id npc)
{
	if ( badge.hasBadge (player, "bdg_content_rsf_clearance_4") && !badge.hasBadge (player, "bdg_content_rsf_clearance_5") )
	{
		return true;
	}
	return false;
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_failedPilotOne (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive (player, "typho_pilot_one_failed");
}

// ----------------------------------------------------------------------

boolean legacy_naboo_lt_col_typho_condition_failedPilotTwo (obj_id player, obj_id npc)
{
	return groundquests.isQuestActive (player, "typho_pilot_two_failed");
}

// ======================================================================
// Script Actions
// ======================================================================

void legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (obj_id player, obj_id npc)
{
	groundquests.requestGrantQuest(player, "quest/typho_police_one");
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (obj_id player, obj_id npc)
{
	groundquests.requestGrantQuest(player, "quest/typho_police_two");
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_grantMarineOneQuest (obj_id player, obj_id npc)
{
	groundquests.requestGrantQuest(player, "quest/typho_marine_one");
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (obj_id player, obj_id npc)
{
	groundquests.requestGrantQuest(player, "quest/typho_marine_two");
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_grantPilotOneQuest (obj_id player, obj_id npc)
{
	groundquests.requestGrantQuest(player, "quest/typho_pilot_one");
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (obj_id player, obj_id npc)
{
	groundquests.requestGrantQuest(player, "quest/typho_pilot_two");
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendPoliceOneSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "completedPoliceOne");
	if (!badge.hasBadge (player, "bdg_content_rsf_clearance_4"))
	{
		content.grantRsfSecurityClearance(player);
	}
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendPoliceTwoSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "killedDoogal");
	if (!badge.hasBadge (player, "bdg_content_rsf_clearance_4"))
	{
		content.grantRsfSecurityClearance(player);
	}
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendMarineOneSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "destroyedMaulerWeapons");
	if (!badge.hasBadge (player, "bdg_content_rsf_clearance_4"))
	{
		content.grantRsfSecurityClearance(player);
	}	
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendMarineTwoSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "killedMaulerLeaders");
	if (!badge.hasBadge (player, "bdg_content_rsf_clearance_4"))
	{
		content.grantRsfSecurityClearance(player);
	}	
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendPilotOneSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "completedPatrolMission");
	if (!badge.hasBadge (player, "bdg_content_rsf_clearance_4"))
	{
		content.grantRsfSecurityClearance(player);
	}	
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendPilotTwoSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "completedAssassinMission");
	if (!badge.hasBadge (player, "bdg_content_rsf_clearance_4"))
	{
		content.grantRsfSecurityClearance(player);
	}
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_endPointerQuestToTypho (obj_id player, obj_id npc)
{
	if ( groundquests.isQuestActive(player, "naboo_keren_goto_typho")  )
	{
		groundquests.sendSignal(player, "keren_goto_typho");
	}
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendToPooja (obj_id player, obj_id npc)
{
	groundquests.requestGrantQuest (player, "quest/naboo_theed_goto_pooja");
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendFailedPilotOneSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "failedTyphoPilotOne");
}

// ----------------------------------------------------------------------

void legacy_naboo_lt_col_typho_action_sendFailedPilotTwoSignal (obj_id player, obj_id npc)
{
	groundquests.sendSignal(player, "failedTyphoPilotTwo");
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int legacy_naboo_lt_col_typho_handleBranch1 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I really don't have anything else for you to do at the moment.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I need to increase my standing with the RSF.
	if (response == "s_65")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good work with Rezunin. He was a thorn in our side. What can you do for us now?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Good work on that patrol. What are you interested in doing for us right now?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Doogal is finished, that's good. We may have to update our most wanted list now, see who's next in line for our attention. What's next for you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: The Maulers will know you on sight, and hopefully show you a little respect. You did excellent work.  What would you like to do now?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Excellent work against the Mauler higher-ups. I wonder if you're still looking for more work...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Great work with those explosives. Are you ready to do some more work for me?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Starwing got away?! That's bad news. With luck, we can take another shot at him before he decides to go into hiding. Are you ready to try again, or would you like to try something else?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What happened out there, Pilot? Why didn't you complete your patrol? I supposed I can give you another chance, if you wish. Or would you like to try something different?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What would you like to do now?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to advance through the ranks.
	if (response == "s_61")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_sendToPooja (player, npc);

			//-- NPC: Well, you're at the highest clearance I can grant you, perhaps you should go see Poo'ja Naberrie in Theed. Here, I'll show you where to look.
			string_id message = new string_id (c_stringFile, "s_62");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ready to do some piloting.
	if (response == "s_21")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			//-- NPC: Well, you've helped us clear our piloting backlog, so we'll have to find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_26");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPilotWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotTwoQuest (player, npc);

			//-- NPC: Good work on that patrol mission, by the way. Rezunin Starwing, one of the Naboo Pirate bosses is in the area. Take him down.
			string_id message = new string_id (c_stringFile, "s_25");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Ok, I need a pilot to run some patrols for me. That's gonna be you.
			string_id message = new string_id (c_stringFile, "s_24");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to work for the police force.
	if (response == "s_22")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			//-- NPC: Our police force work is up to date. I don't have anything pressing in that area right now.
			string_id message = new string_id (c_stringFile, "s_29");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didPoliceWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceTwoQuest (player, npc);

			//-- NPC: Doogal Belstar is at the top of our current most wanted list, here on Naboo. He leads the Mauler Disciples. I want you to see if you can track him down for us.
			string_id message = new string_id (c_stringFile, "s_28");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, I want to know what they're up to.
			string_id message = new string_id (c_stringFile, "s_31");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'd like to do Marine work.
	if (response == "s_41")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			//-- NPC: Impressive work against the Mauler higher-ups. I'm out of tasks for the Marine units right now. Perhaps we can find something else for you to do.
			string_id message = new string_id (c_stringFile, "s_43");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition_didMarineWorkOne (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineTwoQuest (player, npc);

			//-- NPC: Good work on the weapons cache, but now I need you to eliminate some of the more dangerous members of the Mauler organization.
			string_id message = new string_id (c_stringFile, "s_45");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: The Mauler Stronghold is a difficult place to get into, but I need somone to sabotage their weapons cache.
			string_id message = new string_id (c_stringFile, "s_47");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int legacy_naboo_lt_col_typho_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Colonel Panaka said he was sending some recruits my way. I'm not sure what your particular specialties are, but I have tasks open in three divisions, the RMR, the Police Force, and the Naboo Defense.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a pilot, Naboo Defense is what I'd like to do.
	if (response == "s_51")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPilotOneQuest (player, npc);

			//-- NPC: Very good. We've had to cut short some of our patrols because we're running out of pilots. Can you do some patrolling for me?
			string_id message = new string_id (c_stringFile, "s_53");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Police work sounds fine to me.
	if (response == "s_55")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantPoliceOneQuest (player, npc);

			//-- NPC: I want you to do some investigating. Check out the Maulers, and see what they're up to.
			string_id message = new string_id (c_stringFile, "s_57");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Royal Marine Regiment sounds like it might be for me.
	if (response == "s_59")
	{
		//-- [NOTE] 
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			legacy_naboo_lt_col_typho_action_grantMarineOneQuest (player, npc);

			//-- NPC: We have need of someone to infiltrate the Mauler Stronghold by force and destroy their weapons cache. You could be the one to pull it off.
			string_id message = new string_id (c_stringFile, "s_63");
			utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.legacy_naboo_lt_col_typho");

	setCondition (self, CONDITION_CONVERSABLE);
	setCondition (self, CONDITION_INTERESTING);
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setCondition (self, CONDITION_INTERESTING);
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.legacy_naboo_lt_col_typho");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_doneAll (player, npc))
	{
		//-- NPC: I really don't have anything else for you to do at the moment.
		string_id message = new string_id (c_stringFile, "s_33");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I need to increase my standing with the RSF.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_65");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 1);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_finishingPilotTwo (player, npc))
	{
		legacy_naboo_lt_col_typho_action_sendPilotTwoSignal (player, npc);

		//-- NPC: Good work with Rezunin. He was a thorn in our side. What can you do for us now?
		string_id message = new string_id (c_stringFile, "s_34");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_finishingPilotOne (player, npc))
	{
		legacy_naboo_lt_col_typho_action_sendPilotOneSignal (player, npc);

		//-- NPC: Good work on that patrol. What are you interested in doing for us right now?
		string_id message = new string_id (c_stringFile, "s_35");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_finishingPoliceTwo (player, npc))
	{
		legacy_naboo_lt_col_typho_action_sendPoliceTwoSignal (player, npc);

		//-- NPC: Doogal is finished, that's good. We may have to update our most wanted list now, see who's next in line for our attention. What's next for you?
		string_id message = new string_id (c_stringFile, "s_36");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_finishingPoliceOne (player, npc))
	{
		legacy_naboo_lt_col_typho_action_sendPoliceOneSignal (player, npc);

		//-- NPC: The Maulers will know you on sight, and hopefully show you a little respect. You did excellent work.  What would you like to do now?
		string_id message = new string_id (c_stringFile, "s_37");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_finishingMarineTwo (player, npc))
	{
		legacy_naboo_lt_col_typho_action_sendMarineTwoSignal (player, npc);

		//-- NPC: Excellent work against the Mauler higher-ups. I wonder if you're still looking for more work...
		string_id message = new string_id (c_stringFile, "s_38");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_finishingMarineOne (player, npc))
	{
		legacy_naboo_lt_col_typho_action_sendMarineOneSignal (player, npc);

		//-- NPC: Great work with those explosives. Are you ready to do some more work for me?
		string_id message = new string_id (c_stringFile, "s_39");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_failedPilotTwo (player, npc))
	{
		legacy_naboo_lt_col_typho_action_sendFailedPilotTwoSignal (player, npc);

		//-- NPC: Starwing got away?! That's bad news. With luck, we can take another shot at him before he decides to go into hiding. Are you ready to try again, or would you like to try something else?
		string_id message = new string_id (c_stringFile, "s_67");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_failedPilotOne (player, npc))
	{
		legacy_naboo_lt_col_typho_action_sendFailedPilotOneSignal (player, npc);

		//-- NPC: What happened out there, Pilot? Why didn't you complete your patrol? I supposed I can give you another chance, if you wish. Or would you like to try something different?
		string_id message = new string_id (c_stringFile, "s_68");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_isWorkingForTypho (player, npc))
	{
		//-- NPC: Come back when you've finished the task I gave you.
		string_id message = new string_id (c_stringFile, "s_40");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_worksForTypho (player, npc))
	{
		//-- NPC: What would you like to do now?
		string_id message = new string_id (c_stringFile, "s_13");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'd like to advance through the ranks.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition_readyForPooja (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ready to do some piloting.
		boolean hasResponse1 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePiloting (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'd like to work for the police force.
		boolean hasResponse2 = false;
		if (!legacy_naboo_lt_col_typho_condition_donePolicing (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I'd like to do Marine work.
		boolean hasResponse3 = false;
		if (!legacy_naboo_lt_col_typho_condition_doneMarining (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_21");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_22");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_41");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 11);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition_readyForTypho (player, npc))
	{
		legacy_naboo_lt_col_typho_action_endPointerQuestToTypho (player, npc);

		//-- NPC: Colonel Panaka said he was sending some recruits my way. I'm not sure what your particular specialties are, but I have tasks open in three divisions, the RMR, the Police Force, and the Naboo Defense.
		string_id message = new string_id (c_stringFile, "s_49");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm a pilot, Naboo Defense is what I'd like to do.
		boolean hasResponse0 = false;
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Police work sounds fine to me.
		boolean hasResponse1 = false;
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Royal Marine Regiment sounds like it might be for me.
		boolean hasResponse2 = false;
		if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_51");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_55");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_59");

			utils.setScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId", 22);

			npcStartConversation (player, npc, "legacy_naboo_lt_col_typho", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (legacy_naboo_lt_col_typho_condition__defaultCondition (player, npc))
	{
		//-- NPC: Sorry, I'm up to my eyes in paperwork and backlogged case files so I can't speak with you right now.
		string_id message = new string_id (c_stringFile, "s_66");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "legacy_naboo_lt_col_typho")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

	if (branchId == 1 && legacy_naboo_lt_col_typho_handleBranch1 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 2 && legacy_naboo_lt_col_typho_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && legacy_naboo_lt_col_typho_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && legacy_naboo_lt_col_typho_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && legacy_naboo_lt_col_typho_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && legacy_naboo_lt_col_typho_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && legacy_naboo_lt_col_typho_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && legacy_naboo_lt_col_typho_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && legacy_naboo_lt_col_typho_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && legacy_naboo_lt_col_typho_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && legacy_naboo_lt_col_typho_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.legacy_naboo_lt_col_typho.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

