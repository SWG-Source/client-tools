// ======================================================================
//
// padawan_the_ring_01.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.34 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.jedi_trials;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/padawan_the_ring_01";

// ======================================================================
// Script Conditions
// ======================================================================

boolean padawan_the_ring_01_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean padawan_the_ring_01_condition_acceptedQuest (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		return hasObjVar(player, "jedi_trials.padawan_trials.temp.acceptedTask");
	}
	return false;
}

// ----------------------------------------------------------------------

boolean padawan_the_ring_01_condition_targetWasEliminated (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		return hasObjVar(player, "jedi_trials.padawan_trials.temp.killedTarget");
	}
	return false;
}

// ----------------------------------------------------------------------

boolean padawan_the_ring_01_condition_targetWasSpokenTo (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		return hasObjVar(player, "jedi_trials.padawan_trials.temp.spokeToTarget_01");
	}
	return false;
}

// ----------------------------------------------------------------------

boolean padawan_the_ring_01_condition_isTrialPlayer (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		string trialName = jedi_trials.getJediTrialName(player);
	
		if ( trialName != null && trialName.equals("the_ring") )
		{	
			return !hasObjVar(npc, "padawan_trials.playerFailed");
		}
	}
	return false;
}

// ----------------------------------------------------------------------

boolean padawan_the_ring_01_condition_completedSuccess (obj_id player, obj_id npc)
{
	obj_id trialPlayer = getObjIdObjVar(npc, jedi_trials.PADAWAN_TRIAL_PLAYER_OBJVAR);
	if ( player == trialPlayer )
	{	
		string trialName = jedi_trials.getJediTrialName(player);
	
		if ( trialName != null && !trialName.equals("the_ring") )
		{	
			return hasObjVar(npc, "padawan_trials.playerSucceeded");
		}
	}
	return false;
}

// ======================================================================
// Script Actions
// ======================================================================

void padawan_the_ring_01_action_questFailure (obj_id player, obj_id npc)
{
	setObjVar(npc, "padawan_trials.playerFailed", true);
	
	messageTo(player, "handleQuestFailure", null, 1, false);
	return;
}

// ----------------------------------------------------------------------

void padawan_the_ring_01_action_questSuccess (obj_id player, obj_id npc)
{
	setObjVar(npc, "padawan_trials.playerSucceeded", true);
	
	messageTo(player, "handleTrialComplete", null, 1, false);
	return;
}

// ----------------------------------------------------------------------

void padawan_the_ring_01_action_sendToNextNpc (obj_id player, obj_id npc)
{
	setObjVar(npc, "padawan_trials.playerAccepted", true);
	setObjVar(player, "jedi_trials.padawan_trials.temp.acceptedTask", true);
	messageTo(player, "handleSetSecondLoc", null, 1, false);
	return;
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int padawan_the_ring_01_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] After player has spoken with the 'Swoop Con' and if player decided not to resolve the problem with violence.
	//-- NPC: Do you have the ring?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, he insisted on fighting for it. You're not worth the trouble.
	if (response == "s_5056dd2e")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			padawan_the_ring_01_action_questFailure (player, npc);

			//-- NPC: Coward! Why did I ever think you'd be able to help me anyway. Just go. Maybe I can buy another ring that looks just like it... with the exact same inscription... and... and... ah no. I'm doomed.
			string_id message = new string_id (c_stringFile, "s_4bbb35f5");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch4 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] If player has returned with "Bloodied Wedding Ring"
	//-- NPC: You're back. Did you get the ring?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I did, though it wasn't easy.
	if (response == "s_6fd316c5")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Why is it covered in blood?
			string_id message = new string_id (c_stringFile, "s_f7b65eb2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I got it the only way possible, by killing the low life that took it from you.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Little details like that don't matter.
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_1f8ddc7");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_252ebf4f");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why is it covered in blood?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I got it the only way possible, by killing the low life that took it from you.
	if (response == "s_1f8ddc7")
	{
		//-- [NOTE] Quest failed?
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "cough_polite");

			padawan_the_ring_01_action_questSuccess (player, npc);

			//-- NPC: You... you killed him? Wow, I never thought he'd refuse to return it and fight you for it. Well, I thought he might fight you, but not that he'd go so far.
			string_id message = new string_id (c_stringFile, "s_47749228");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Little details like that don't matter.
	if (response == "s_252ebf4f")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Is he ok?
			string_id message = new string_id (c_stringFile, "s_d403c577");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not exactly, depends on your definition of ok.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b7fd7db8");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Is he ok?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not exactly, depends on your definition of ok.
	if (response == "s_b7fd7db8")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Alive?
			string_id message = new string_id (c_stringFile, "s_3a25f75a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: In that case.. No.  He's not quite ok.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: In the literal, or figurative sense?
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_63fcf51c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9a1d24f0");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 8);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Alive?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: In that case.. No.  He's not quite ok.
	if (response == "s_63fcf51c")
	{
		//-- [NOTE] Quest failed?
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "cough_polite");

			padawan_the_ring_01_action_questSuccess (player, npc);

			//-- NPC: You... you killed him? Wow, I never thought he'd refuse to return it and fight you for it. Well, I thought he might fight you, but not that he'd og so far.
			string_id message = new string_id (c_stringFile, "s_618117e6");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: In the literal, or figurative sense?
	if (response == "s_9a1d24f0")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Literal!
			string_id message = new string_id (c_stringFile, "s_f61a9588");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: No, if you put it that way.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b4bf50d7");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 10);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch10 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Literal!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No, if you put it that way.
	if (response == "s_b4bf50d7")
	{
		//-- [NOTE] Quest failed?
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "cough_polite");

			padawan_the_ring_01_action_questSuccess (player, npc);

			//-- NPC: You... you killed him? Wow, I never thought he'd refuse to return it and fight you for it. Well, I thought he might fight you, but not that he'd og so far.
			string_id message = new string_id (c_stringFile, "s_21");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch12 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] Player returns before speaking with 'Swoop Con'
	//-- NPC: Why are you back? What's wrong?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing's wrong. I just haven't found the ring yet.
	if (response == "s_3faed3")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: No problem friend. Hurry back when you have it.
			string_id message = new string_id (c_stringFile, "s_a9ca1cc2");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I've changed my mind. Go get the ring yourself. I've got better things to do.
	if (response == "s_23b61565")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			padawan_the_ring_01_action_questFailure (player, npc);

			//-- NPC: Hmmph. Probably would have stolen the ring for yourself anyway.
			string_id message = new string_id (c_stringFile, "s_e20e68ad");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I'm in a tight spot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What is the problem?
	if (response == "s_7b35cfbc")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I sort of borrowed my deceased mother in laws wedding ring from my wife, then I sort of lost it when I was out racing my swoop.
			string_id message = new string_id (c_stringFile, "s_d1457e5b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is a tight spot.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Why not get it yourself?
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_91971db6");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_997d67e4");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should have been more careful.
	if (response == "s_7cb3d5a9")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Brilliant!  Your incredible skills of deduction amaze me.
			string_id message = new string_id (c_stringFile, "s_92a6af5a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: It's free the first time, next time it's going to cost you.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'm sorry, it was just a joke.
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a410e80d");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c6898781");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 22);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I sort of borrowed my deceased mother in laws wedding ring from my wife, then I sort of lost it when I was out racing my swoop.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is a tight spot.
	if (response == "s_91971db6")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Seriously... If it's not too much to ask do you think you could go out and try to find it for me?
			string_id message = new string_id (c_stringFile, "s_6d739213");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would be happy to.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds dangerous. If there's nothing in it for me, forget it.
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70c10eb");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ea66e5db");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why not get it yourself?
	if (response == "s_997d67e4")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: If I'm not here when my wife returns she is sure to suspect I've done something wrong.
			string_id message = new string_id (c_stringFile, "s_61a3322c");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would be happy to.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_39");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 20);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Seriously... If it's not too much to ask do you think you could go out and try to find it for me?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would be happy to.
	if (response == "s_70c10eb")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			padawan_the_ring_01_action_sendToNextNpc (player, npc);

			//-- NPC: Thank you very much, I've uploaded your datapad with the last location I remember having the ring on me.
			string_id message = new string_id (c_stringFile, "s_d91ac73");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds dangerous. If there's nothing in it for me, forget it.
	if (response == "s_ea66e5db")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			padawan_the_ring_01_action_questFailure (player, npc);

			//-- NPC: Rewards are not always monetary, or even tangible friend.  
			string_id message = new string_id (c_stringFile, "s_b3083e4e");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch20 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If I'm not here when my wife returns she is sure to suspect I've done something wrong.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would be happy to.
	if (response == "s_39")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			padawan_the_ring_01_action_sendToNextNpc (player, npc);

			//-- NPC: Thank you very much, I've uploaded your datapad with the last location I remember having the ring on me.
			string_id message = new string_id (c_stringFile, "s_41");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch22 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Brilliant!  Your incredible skills of deduction amaze me.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's free the first time, next time it's going to cost you.
	if (response == "s_a410e80d")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			padawan_the_ring_01_action_questFailure (player, npc);

			//-- NPC: Fair enough. At least you're listening. You are listening, right?
			string_id message = new string_id (c_stringFile, "s_dc264acf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure, how can I help you?
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8626b2e6");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm sorry, it was just a joke.
	if (response == "s_c6898781")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: It's okay, I'm just a bit wound up.  I could truly use your help, if you are willing of course.
			string_id message = new string_id (c_stringFile, "s_c2f7ce8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sure, how can I help you?
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_72");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 30);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fair enough. At least you're listening. You are listening, right?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure, how can I help you?
	if (response == "s_8626b2e6")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I sort of borrowed my deceased mother in laws wedding ring from my wife, then I sort of lost it when I was out racing my swoop.
			string_id message = new string_id (c_stringFile, "s_48");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is a tight spot.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Why not get it yourself?
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_62");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 24);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I sort of borrowed my deceased mother in laws wedding ring from my wife, then I sort of lost it when I was out racing my swoop.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is a tight spot.
	if (response == "s_50")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Seriously... If it's not too much to ask do you think you could go out and try to find it for me?
			string_id message = new string_id (c_stringFile, "s_52");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would be happy to.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds dangerous. If there's nothing in it for me, forget it.
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_54");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_58");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why not get it yourself?
	if (response == "s_62")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: If I'm not here when my wife returns she is sure to suspect I've done something wrong.
			string_id message = new string_id (c_stringFile, "s_64");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would be happy to.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_66");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Seriously... If it's not too much to ask do you think you could go out and try to find it for me?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would be happy to.
	if (response == "s_54")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			padawan_the_ring_01_action_sendToNextNpc (player, npc);

			//-- NPC: Thank you very much, I've uploaded your datapad with the last location I remember having the ring on me.
			string_id message = new string_id (c_stringFile, "s_56");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds dangerous. If there's nothing in it for me, forget it.
	if (response == "s_58")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			padawan_the_ring_01_action_questFailure (player, npc);

			//-- NPC: Rewards are not always monetary, or even tangible friend.  
			string_id message = new string_id (c_stringFile, "s_60");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If I'm not here when my wife returns she is sure to suspect I've done something wrong.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would be happy to.
	if (response == "s_66")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			padawan_the_ring_01_action_sendToNextNpc (player, npc);

			//-- NPC: Thank you very much, I've uploaded your datapad with the last location I remember having the ring on me.
			string_id message = new string_id (c_stringFile, "s_68");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's okay, I'm just a bit wound up.  I could truly use your help, if you are willing of course.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sure, how can I help you?
	if (response == "s_72")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: I sort of borrowed my deceased mother in laws wedding ring from my wife, then I sort of lost it when I was out racing my swoop.
			string_id message = new string_id (c_stringFile, "s_74");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is a tight spot.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Why not get it yourself?
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_76");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_88");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I sort of borrowed my deceased mother in laws wedding ring from my wife, then I sort of lost it when I was out racing my swoop.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is a tight spot.
	if (response == "s_76")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: Seriously... If it's not too much to ask do you think you could go out and try to find it for me?
			string_id message = new string_id (c_stringFile, "s_78");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would be happy to.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Sounds dangerous. If there's nothing in it for me, forget it.
			boolean hasResponse1 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_80");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_84");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 32);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why not get it yourself?
	if (response == "s_88")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			//-- NPC: If I'm not here when my wife returns she is sure to suspect I've done something wrong.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I would be happy to.
			boolean hasResponse0 = false;
			if (padawan_the_ring_01_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

				chat.chat (npc, player, message);
				npcEndConversation (player);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch32 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Seriously... If it's not too much to ask do you think you could go out and try to find it for me?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would be happy to.
	if (response == "s_80")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			padawan_the_ring_01_action_sendToNextNpc (player, npc);

			//-- NPC: Thank you very much, I've uploaded your datapad with the last location I remember having the ring on me.
			string_id message = new string_id (c_stringFile, "s_82");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds dangerous. If there's nothing in it for me, forget it.
	if (response == "s_84")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			padawan_the_ring_01_action_questFailure (player, npc);

			//-- NPC: Rewards are not always monetary, or even tangible friend.  
			string_id message = new string_id (c_stringFile, "s_86");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int padawan_the_ring_01_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: If I'm not here when my wife returns she is sure to suspect I've done something wrong.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I would be happy to.
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_multiple");

			padawan_the_ring_01_action_sendToNextNpc (player, npc);

			//-- NPC: Thank you very much, I've uploaded your datapad with the last location I remember having the ring on me.
			string_id message = new string_id (c_stringFile, "s_94");
			utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

			chat.chat (npc, player, message);
			npcEndConversation (player);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.padawan_the_ring_01");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.padawan_the_ring_01");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (padawan_the_ring_01_condition_completedSuccess (player, npc))
	{
		doAnimationAction (npc, "weeping");

		//-- NPC: Thank you again, friend. I'm sorry it had to come to violence, but I guess you had no other choice.
		string_id message = new string_id (c_stringFile, "s_d0ed20dc");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] After player has spoken with the 'Swoop Con' and if player decided not to resolve the problem with violence.
	if (padawan_the_ring_01_condition_targetWasSpokenTo (player, npc))
	{
		//-- NPC: Do you have the ring?
		string_id message = new string_id (c_stringFile, "s_112f3596");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No, he insisted on fighting for it. You're not worth the trouble.
		boolean hasResponse0 = false;
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5056dd2e");

			utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 2);

			npcStartConversation (player, npc, "padawan_the_ring_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] If player has returned with "Bloodied Wedding Ring"
	if (padawan_the_ring_01_condition_targetWasEliminated (player, npc))
	{
		//-- NPC: You're back. Did you get the ring?
		string_id message = new string_id (c_stringFile, "s_70e58d4e");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I did, though it wasn't easy.
		boolean hasResponse0 = false;
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6fd316c5");

			utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 4);

			npcStartConversation (player, npc, "padawan_the_ring_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] Player returns before speaking with 'Swoop Con'
	if (padawan_the_ring_01_condition_acceptedQuest (player, npc))
	{
		//-- NPC: Why are you back? What's wrong?
		string_id message = new string_id (c_stringFile, "s_c0eab960");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Nothing's wrong. I just haven't found the ring yet.
		boolean hasResponse0 = false;
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I've changed my mind. Go get the ring yourself. I've got better things to do.
		boolean hasResponse1 = false;
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3faed3");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_23b61565");

			utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 12);

			npcStartConversation (player, npc, "padawan_the_ring_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (padawan_the_ring_01_condition_isTrialPlayer (player, npc))
	{
		doAnimationAction (npc, "beckon");

		//-- NPC: I'm in a tight spot.
		string_id message = new string_id (c_stringFile, "s_3924c1a9");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What is the problem?
		boolean hasResponse0 = false;
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Should have been more careful.
		boolean hasResponse1 = false;
		if (padawan_the_ring_01_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7b35cfbc");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7cb3d5a9");

			utils.setScriptVar (player, "conversation.padawan_the_ring_01.branchId", 15);

			npcStartConversation (player, npc, "padawan_the_ring_01", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (padawan_the_ring_01_condition__defaultCondition (player, npc))
	{
		//-- NPC: She'll have my hide. I'm such a fool. How could I have done such a thing... how??!!
		string_id message = new string_id (c_stringFile, "s_16c644b4");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "padawan_the_ring_01")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.padawan_the_ring_01.branchId");

	if (branchId == 2 && padawan_the_ring_01_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 4 && padawan_the_ring_01_handleBranch4 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && padawan_the_ring_01_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && padawan_the_ring_01_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && padawan_the_ring_01_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 10 && padawan_the_ring_01_handleBranch10 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 12 && padawan_the_ring_01_handleBranch12 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && padawan_the_ring_01_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && padawan_the_ring_01_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && padawan_the_ring_01_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 20 && padawan_the_ring_01_handleBranch20 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 22 && padawan_the_ring_01_handleBranch22 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && padawan_the_ring_01_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && padawan_the_ring_01_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && padawan_the_ring_01_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && padawan_the_ring_01_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && padawan_the_ring_01_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && padawan_the_ring_01_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 32 && padawan_the_ring_01_handleBranch32 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && padawan_the_ring_01_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.padawan_the_ring_01.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

