// ======================================================================
//
// corellia_rebel_tier4.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.35 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.skill;
include library.space_flags;
include library.space_quest;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/corellia_rebel_tier4";

// ======================================================================
// Script Conditions
// ======================================================================

boolean corellia_rebel_tier4_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isWrongFaction (obj_id player, obj_id npc)
{
	//check the player has the correct novice skill box for this trainer
	return (!hasSkill(player,"pilot_rebel_navy_novice"));
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasCompletedTier3 (obj_id player, obj_id npc)
{
	/*
	if (!hasSkill( player, "pilot_rebel_navy_starships_03" ))
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_procedures_03" ))
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_droid_03" ))
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_weapons_03" ))
		return false;
	else if (!hasObjVar (player, "viopaPilot") )
		return false;
	else
		return true;
	*/
	
	if(space_flags.hasCompletedTierThree(player) && space_flags.hasSpaceFlag(player, "viopaPilot"))
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isOnMission (obj_id player, obj_id npc)
{
	return ( space_quest.hasQuest(player) );
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_failedMissionOne (obj_id player, obj_id npc)
{
	if ( 
			( 
				space_quest.hasAbortedQuest ( player, "survival", "corellia_rebel_tier4_1" ) ||
	   			space_quest.hasFailedQuest ( player, "survival", "corellia_rebel_tier4_1" ) 
	   		)
	   		&&
	   			!space_quest.hasWonQuest ( player, "space_battle", "corellia_rebel_tier4_1_b" )
	   	)
		return true;
	   	
	if ( 
			space_quest.hasAbortedQuest ( player, "space_battle", "corellia_rebel_tier4_1_b" ) ||
			space_quest.hasFailedQuest ( player, "space_battle", "corellia_rebel_tier4_1_b" ) 
		)
		return true;
		   
	if (
			space_quest.hasAbortedQuest ( player, "space_battle", "corellia_rebel_tier4_1_a" ) ||
			space_quest.hasFailedQuest ( player, "space_battle", "corellia_rebel_tier4_1_a" )
		)
		return true;

	return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_failedMissionTwo (obj_id player, obj_id npc)
{
	if ( 
			( 
				space_quest.hasAbortedQuest ( player, "assassinate", "corellia_rebel_tier4_2" ) ||
	   			space_quest.hasFailedQuest ( player, "assassinate", "corellia_rebel_tier4_2" ) 
	   		)
	   		&&
	   			!space_quest.hasWonQuest ( player, "rescue", "corellia_rebel_tier4_2_b" )
	   	)
		return true;
	   	
	if ( 
			space_quest.hasAbortedQuest ( player, "rescue", "corellia_rebel_tier4_2_b" ) ||
			space_quest.hasFailedQuest ( player, "rescue", "corellia_rebel_tier4_2_b" ) 
		)
		return true;
		   
	if (
			space_quest.hasAbortedQuest ( player, "delivery_no_pickup", "corellia_rebel_tier4_2_a" ) ||
			space_quest.hasFailedQuest ( player, "delivery_no_pickup", "corellia_rebel_tier4_2_a" )
		)
		return true;

	return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_failedMissionThree (obj_id player, obj_id npc)
{
	if ( 
			( 
				space_quest.hasAbortedQuest ( player, "space_battle", "corellia_rebel_tier4_3" ) ||
	   			space_quest.hasFailedQuest ( player, "space_battle", "corellia_rebel_tier4_3" ) 
	   		)
	   		&&
	   			!space_quest.hasWonQuest ( player, "survival", "corellia_rebel_tier4_3_b" )
	   	)
		return true;
	   	
	if ( 
			space_quest.hasAbortedQuest ( player, "space_battle", "corellia_rebel_tier4_3_a" ) ||
			space_quest.hasFailedQuest ( player, "space_battle", "corellia_rebel_tier4_3_a" ) 
		)
		return true;
		   
	if (
			space_quest.hasAbortedQuest ( player, "survival", "corellia_rebel_tier4_3_b" ) ||
			space_quest.hasFailedQuest ( player, "survival", "corellia_rebel_tier4_3_b" )
		)
		return true;

	return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_failedMissionFour (obj_id player, obj_id npc)
{
	if (
			( 
				space_quest.hasAbortedQuest ( player, "recovery", "corellia_rebel_tier4_4" ) ||
	   			space_quest.hasFailedQuest ( player, "recovery", "corellia_rebel_tier4_4" ) 
	   		)
	   		&&
	   			!space_quest.hasWonQuest ( player, "space_battle", "corellia_rebel_tier4_4_c" )
	   	)
		return true;
	   	
	if ( 
			space_quest.hasAbortedQuest ( player, "assassinate", "corellia_rebel_tier4_4_a" ) ||
			space_quest.hasFailedQuest ( player, "assassinate", "corellia_rebel_tier4_4_a" ) 
		)
		return true;
		   
	if (
			space_quest.hasAbortedQuest ( player, "rescue", "corellia_rebel_tier4_4_b" ) ||
			space_quest.hasFailedQuest ( player, "rescue", "corellia_rebel_tier4_4_b" )
		)
		return true;

	if (
			space_quest.hasAbortedQuest ( player, "space_battle", "corellia_rebel_tier4_4_c" ) ||
			space_quest.hasFailedQuest ( player, "space_battle", "corellia_rebel_tier4_4_c" )
		)
		return true;

	return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_failedMasterMission (obj_id player, obj_id npc)
{

	if ( space_quest.hasAbortedQuestRecursive( player, "destroy", "corellia_rebel_master" ) ||
		 space_quest.hasFailedQuestRecursive( player, "destroy", "corellia_rebel_master" ) )
		 return true;

	return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isReadyForTraining (obj_id player, obj_id npc)
{
	//see if the player has NOT completed all of tier 4
	//AND has enough XP to buy a tier-4 skill
	if (corellia_rebel_tier4_condition_hasCompletedTier4(player,npc))
		return false;//you're already done
		
	return ( space_quest.isPlayerQualifiedForSkill( player, "pilot_rebel_navy_starships_04" ) );
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasCompletedTier4 (obj_id player, obj_id npc)
{
	/*
	if (!hasSkill( player, "pilot_rebel_navy_starships_04" ))
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_procedures_04" ))
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_droid_04" ))
		return false;
	else if (!hasSkill( player, "pilot_rebel_navy_weapons_04" ))
		return false;
	else
		return true;
	*/
	
	return space_flags.hasCompletedTierFour(player);
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasMasterSkill (obj_id player, obj_id npc)
{
	return (hasSkill( player, "pilot_rebel_navy_master" ));
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isGettingMissionOneReward (obj_id player, obj_id npc)
{
	if (space_quest.hasReceivedReward( player, "survival", "corellia_rebel_tier4_1" ))
		return false;
		
	if (space_quest.hasWonQuest ( player, "space_battle", "corellia_rebel_tier4_1_a") ||
		space_quest.hasWonQuest ( player, "space_battle", "corellia_rebel_tier4_1_b") )
		return true;
	else
		return false;
	
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isGettingMissionTwoReward (obj_id player, obj_id npc)
{
	if (space_quest.hasReceivedReward( player, "assassinate", "corellia_rebel_tier4_2" ))
		return false;
		
	if (space_quest.hasWonQuest ( player, "delivery_no_pickup", "corellia_rebel_tier4_2_a") ||
		space_quest.hasWonQuest ( player, "rescue", "corellia_rebel_tier4_2_b") )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isGettingMissionThreeReward (obj_id player, obj_id npc)
{
	if (space_quest.hasReceivedReward( player, "space_battle", "corellia_rebel_tier4_3" ))
		return false;
		
	if (space_quest.hasWonQuest ( player, "space_battle", "corellia_rebel_tier4_3_a") ||
		space_quest.hasWonQuest ( player, "survival", "corellia_rebel_tier4_3_b") )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isGettingMissionFourReward (obj_id player, obj_id npc)
{
	if (space_quest.hasReceivedReward( player, "recovery", "corellia_rebel_tier4_4" ))
		return false;
		
	if (space_quest.hasWonQuest ( player, "space_battle", "corellia_rebel_tier4_4_c" ))
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isGettingMasterMissionReward (obj_id player, obj_id npc)
{
	if (!space_quest.hasCompletedQuestRecursive( player, "destroy", "corellia_rebel_master" ))
		return false;//haven't completed it, so no reward
	
	//you've completed it, so you must be here for the reward:
	if (!space_quest.hasReceivedReward( player, "destroy", "corellia_rebel_master" ))
		return true;
	else
		return false;	
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_readyForMissionOne (obj_id player, obj_id npc)
{
	if ( (hasSkill( player, "pilot_rebel_navy_starships_04" ) ||
		hasSkill( player, "pilot_rebel_navy_weapons_04" ) ||
		hasSkill( player, "pilot_rebel_navy_procedures_04" ) ||
		hasSkill( player, "pilot_rebel_navy_droid_04" )) && 
		space_flags.hasSpaceFlag(player, "viopaPilot") &&
		!space_quest.hasReceivedReward( player, "survival", "corellia_rebel_tier4_1" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_readyForMissionTwo (obj_id player, obj_id npc)
{
	string[] skills = { "pilot_rebel_navy_starships_04", 
						"pilot_rebel_navy_weapons_04",
						"pilot_rebel_navy_procedures_04",
						"pilot_rebel_navy_droid_04" };
	int j = 0;
	for ( int i=0; i<skills.length; i++ )
	{
		if ( hasSkill( player, skills[i] ) )
			j++;
	}
	if (( j == 2 ) && space_flags.hasSpaceFlag(player, "viopaPilot") &&
	!space_quest.hasReceivedReward( player, "assassinate", "corellia_rebel_tier4_2" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_readyForMissionThree (obj_id player, obj_id npc)
{
	string[] skills = { "pilot_rebel_navy_starships_04", 
						"pilot_rebel_navy_weapons_04",
						"pilot_rebel_navy_procedures_04",
						"pilot_rebel_navy_droid_04" };
	int j = 0;
	for ( int i=0; i<skills.length; i++ )
	{
		if ( hasSkill( player, skills[i] ) )
			j++;
	}
	if (( j == 3 ) && space_flags.hasSpaceFlag(player, "viopaPilot") &&
	!space_quest.hasReceivedReward( player, "space_battle", "corellia_rebel_tier4_3" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_readyForMissionFour (obj_id player, obj_id npc)
{
	string[] skills = { "pilot_rebel_navy_starships_04", 
						"pilot_rebel_navy_weapons_04",
						"pilot_rebel_navy_procedures_04",
						"pilot_rebel_navy_droid_04" };
	int j = 0;
	for ( int i=0; i<skills.length; i++ )
	{
		if ( hasSkill( player, skills[i] ) )
			j++;
	}
	if (( j == 4 ) && space_flags.hasSpaceFlag(player, "viopaPilot") &&
	!space_quest.hasReceivedReward( player, "recovery", "corellia_rebel_tier4_4" ) )
		return true;
	else
		return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasDroid04 (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_rebel_navy_droid_04" );
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasWeapon04 (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_rebel_navy_weapons_04" );
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasStarship04 (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_rebel_navy_starships_04" );
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasProcedures04 (obj_id player, obj_id npc)
{
	return hasSkill( player, "pilot_rebel_navy_procedures_04" );
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_canFlyNonTrackDuty (obj_id player, obj_id npc)
{
	if(space_flags.hasCompletedTierThree(player) && !space_quest.hasQuest(player))
		return true;
		
	return false;
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_onMyTrack (obj_id player, obj_id npc)
{
	return space_flags.isSpaceTrack(player, space_flags.REBEL_CORELLIA);
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_isPilot (obj_id player, obj_id npc)
{
	return space_flags.hasAnyPilotSkill(player);
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasMetMe (obj_id player, obj_id npc)
{
	return space_flags.hasSpaceFlag(player, "metTier4");
}

// ----------------------------------------------------------------------

boolean corellia_rebel_tier4_condition_hasNegativeFaction (obj_id player, obj_id npc)
{
	return ( factions.getFactionStanding(player, factions.FACTION_REBEL ) < 0.0f );
}

// ======================================================================
// Script Actions
// ======================================================================

void corellia_rebel_tier4_action_grantMissionOne (obj_id player, obj_id npc)
{
	space_quest.clearQuestFlags( player, "space_battle", "corellia_rebel_tier4_1_b" );
	space_quest.clearQuestFlags( player, "space_battle", "corellia_rebel_tier4_1_a" );
	space_quest.grantQuest( player, "survival", "corellia_rebel_tier4_1" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_grantMissionTwo (obj_id player, obj_id npc)
{
	space_quest.clearQuestFlags( player, "rescue", "corellia_rebel_tier4_2_b" );
	space_quest.clearQuestFlags( player, "delivery_no_pickup", "corellia_rebel_tier4_2_a" );
	space_quest.grantQuest( player, "assassinate", "corellia_rebel_tier4_2" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_grantMissionThree (obj_id player, obj_id npc)
{
	space_quest.clearQuestFlags( player, "survival", "corellia_rebel_tier4_3_b" );
	space_quest.clearQuestFlags( player, "space_battle", "corellia_rebel_tier4_3_a" );
	space_quest.grantQuest( player, "space_battle", "corellia_rebel_tier4_3" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_grantMissionFour (obj_id player, obj_id npc)
{
	space_quest.clearQuestFlags( player, "rescue", "corellia_rebel_tier4_4_b" );
	space_quest.clearQuestFlags( player, "assassinate", "corellia_rebel_tier4_4_a" );
	space_quest.clearQuestFlags( player, "space_battle", "corellia_rebel_tier4_4_c" );
	space_quest.grantQuest( player, "recovery", "corellia_rebel_tier4_4" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_grantMasterMission (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy", "corellia_rebel_master" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_grantDutyMissionOne (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "escort_duty", "corellia_rebel_tier4_1" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_grantDutyMissionTwo (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "rescue_duty", "corellia_rebel_tier4_1" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_grantDutyMissionThree (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "recovery_duty", "corellia_rebel_tier4_1" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_grantDutyMissionFour (obj_id player, obj_id npc)
{
	space_quest.grantQuest( player, "destroy_duty", "corellia_rebel_tier4_1" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_rewardForMissionOne (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "survival", "corellia_rebel_tier4_1" ))
	{
		space_quest.giveReward( player, "survival", "corellia_rebel_tier4_1", 10000, "object/tangible/ship/components/shield_generator/shd_mission_reward_rebel_taim_military_grade.iff" );
		factions.addFactionStanding( player, factions.FACTION_REBEL, 150.0f );
	}
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_rewardForMissionTwo (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "assassinate", "corellia_rebel_tier4_2" ))
	{
		space_quest.giveReward( player, "assassinate", "corellia_rebel_tier4_2", 10000, "object/tangible/ship/components/droid_interface/ddi_mission_reward_rebel_novaldex_low_latency.iff" );
		factions.addFactionStanding( player, factions.FACTION_REBEL, 150.0f );
	}
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_rewardForMissionThree (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "space_battle", "corellia_rebel_tier4_3" ))
	{
		space_quest.giveReward( player, "space_battle", "corellia_rebel_tier4_3", 10000, "object/tangible/ship/components/booster/bst_mission_reward_rebel_qualdex_halcyon.iff" );
		factions.addFactionStanding( player, factions.FACTION_REBEL, 150.0f );
	}
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_rewardForMissionFour (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "recovery", "corellia_rebel_tier4_4" ))
	{
		space_quest.giveReward( player, "recovery", "corellia_rebel_tier4_4", 10000, "object/tangible/ship/components/weapon/wpn_mission_reward_rebel_incom_tricannon.iff" );
		factions.addFactionStanding( player, factions.FACTION_REBEL, 150.0f );
	}
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_rewardForMasterMission (obj_id player, obj_id npc)
{
	if (!space_quest.hasReceivedReward( player, "destroy", "corellia_rebel_master" ))
	{
		space_quest.giveReward( player, "destroy", "corellia_rebel_master", 5000 );
		grantSkill( player, "pilot_rebel_navy_master");
	}
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_buyWeaponsSkill04 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_rebel_navy_weapons_04" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_buyStarshipsSkill04 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_rebel_navy_starships_04" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_buyDroidSkill04 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_rebel_navy_droid_04" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_buyProceduresSkill04 (obj_id player, obj_id npc)
{
	skill.purchaseSkill( player, "pilot_rebel_navy_procedures_04" );
}

// ----------------------------------------------------------------------

void corellia_rebel_tier4_action_setMetMe (obj_id player, obj_id npc)
{
	space_flags.setSpaceFlag(player, "metTier4", true);
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int corellia_rebel_tier4_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You're the wrong faction.
	//-- NPC: Eh? Who's that? Who are you? Tell me your faction!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm a privateer.
	if (response == "s_7e579e1e")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: Privateer? No privateers here! Go on, get out of here!
			string_id message = new string_id (c_stringFile, "s_649d2a19");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm an Imperial.
	if (response == "s_fb4c1f1f")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_raise_fist");

			//-- NPC: IMPERIAL?! I hate Imperials! Get out of here!
			string_id message = new string_id (c_stringFile, "s_2da082b6");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Eh? Who's that? You're no... wait, you ARE a Rebel pilot. But you don't fly with this unit. What are you doing here?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Do you have any missions that I could fly?
	if (response == "s_6222b64d")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition_canFlyNonTrackDuty (player, npc))
		{
			//-- NPC: I don't have any missions, but there are plenty of elective duty assignments you can perform. Is that something you think you could handle? Want me to explain it to you?
			string_id message = new string_id (c_stringFile, "s_f0743f82");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes, please.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c82e9a2f");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 6);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You should know better than that. Only your commanding officer can give you missions. And I am NOT your commanding officer, buddy! 
			string_id message = new string_id (c_stringFile, "s_c73f5fdb");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch6 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't have any missions, but there are plenty of elective duty assignments you can perform. Is that something you think you could handle? Want me to explain it to you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, please.
	if (response == "s_c82e9a2f")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fine, listen up. We are looking at several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 
			string_id message = new string_id (c_stringFile, "s_4bce8f6f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 172);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch8 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You're not through tier-3 yet
	//-- NPC: No no! You don't belong here!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't?
	if (response == "s_e0dc5fad")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Oh no! You've got WORK to do, junior!
			string_id message = new string_id (c_stringFile, "s_3bcc34e7");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do you know?
	if (response == "s_e86f1062")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stamp_feet");

			//-- NPC: BECAUSE I just know, that's how! None of your business how I know! Young people today are so rude! Shut your trap and get back to work!
			string_id message = new string_id (c_stringFile, "s_5e0e14bc");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You're busy.  Already doing a mission.  Hup hup, go do it.
	//-- NPC: %TU, you lack discipline! 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I just lack motivation. 
	if (response == "s_4ccb8d59")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "standing_raise_fist");

			//-- NPC: I'll motivate you with the tip of my BOOT! Get to work!
			string_id message = new string_id (c_stringFile, "s_549b97e6");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Whatever.
	if (response == "s_c1013aa7")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Is that your idea of a comeback? Grow up! Get to work!
			string_id message = new string_id (c_stringFile, "s_86df2510");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch14 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Traitor!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What?!
	if (response == "s_7090182")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_palm");

			//-- NPC: You've been assisting the Empire! Working AGAINST the Rebel Alliance!
			string_id message = new string_id (c_stringFile, "s_436c2ab2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you mean?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77e48d5b");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You've been assisting the Empire! Working AGAINST the Rebel Alliance!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you mean?
	if (response == "s_77e48d5b")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: What I mean, pliot, is that you are grounded until further notice. We can't trust YOU with any more missions.
			string_id message = new string_id (c_stringFile, "s_2744b825");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Ever?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ad621c08");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 16);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch16 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What I mean, pliot, is that you are grounded until further notice. We can't trust YOU with any more missions.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Ever?
	if (response == "s_ad621c08")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			//-- NPC: 'Til you improve your standing with the Rebel Alliance, I wouldn't trust you to guard a faamba.
			string_id message = new string_id (c_stringFile, "s_ee83a0c2");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch18 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have completed mission one and so here is your reward.
	//-- NPC: Mission accomplished! And you know what that means.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I sure do.
	if (response == "s_95c399f6")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Here's your duty pay. And let me know when you're ready for another mission! 
			string_id message = new string_id (c_stringFile, "s_6fdab943");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What does it mean?
	if (response == "s_6f679fea")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: Duty pay, you nitwit! Here you go. And let me know when you're ready for another mission!
			string_id message = new string_id (c_stringFile, "s_b4e09e44");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have completed mission two and so here is your reward.
	//-- NPC: See? You could save the day after all! Not bad.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You were right.
	if (response == "s_def742")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "flex_biceps");

			//-- NPC: I'm always right! Get that through your thick head. Here's your duty pay. See me later when you're ready for another mission.
			string_id message = new string_id (c_stringFile, "s_c4dc6b9d");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It wasn't easy.
	if (response == "s_b20c71fe")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "snap_finger1");

			//-- NPC: I don't give my pilots easy missions. But I DO pay them well. Here you go. See me later when you're ready for another mission.
			string_id message = new string_id (c_stringFile, "s_868699d3");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch24 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have completed mission three and so here is your reward.
	//-- NPC: I salute you, Rebel!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: No need for that.
	if (response == "s_aed5139a")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I give credit where credit is due! I've never seen somebody go after an Imperial base the way you did.
			string_id message = new string_id (c_stringFile, "s_f87c573");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Pft.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c72c337a");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Aw shucks.
	if (response == "s_cb6eb294")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Here's your duty pay! You've earned it.
			string_id message = new string_id (c_stringFile, "s_e3653d50");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Thanks.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_fc27931b");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 28);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I give credit where credit is due! I've never seen somebody go after an Imperial base the way you did.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Pft.
	if (response == "s_c72c337a")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Maybe you just want your duty pay.
			string_id message = new string_id (c_stringFile, "s_9a4a59e0");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That's fine.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f60b81f8");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 26);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch26 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Maybe you just want your duty pay.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That's fine.
	if (response == "s_f60b81f8")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: See you around, %TU.
			string_id message = new string_id (c_stringFile, "s_3172f07e");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch28 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Here's your duty pay! You've earned it.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Thanks.
	if (response == "s_fc27931b")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Let me know when you're ready for another mission.
			string_id message = new string_id (c_stringFile, "s_3bfd507c");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch30 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have completed mission four and so here is your reward.
	//-- NPC: You showed them! Let's see the Empire try to build a station in THIS system!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It's not going to happen anytime soon.
	if (response == "s_61485366")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Here's your duty pay. You've earned it.
			string_id message = new string_id (c_stringFile, "s_85fbc617");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Happy to serve.
	if (response == "s_197c8928")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "salute1");

			//-- NPC: And I'm happy to give you your duty pay, junior!
			string_id message = new string_id (c_stringFile, "s_4c97efb1");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You failed mission 1, do it over.
	//-- NPC: In my day, a pilot would NEVER leave a job unfinished!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not done yet! 
	if (response == "s_8f3b3edb")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			corellia_rebel_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Hurry up! There's no time to waste! 
			string_id message = new string_id (c_stringFile, "s_76c0ae11");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm ashamed.
	if (response == "s_88563b84")
	{
		doAnimationAction (player, "embarrassed");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "offer_affection");

			corellia_rebel_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Oh. I didn't mean to upset you! I just want to rescue those mining vessels. Sorry! Go on and try again! I'm sure you'll have no problem this time.
			string_id message = new string_id (c_stringFile, "s_fa6bfaff");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch36 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You failed mission 2, do it over.
	//-- NPC: You've got to get back out there. You realize what will happen if that Inquisitor gets a hold of your friends?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I know.
	if (response == "s_87d7e908")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			corellia_rebel_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: Your ship is waiting. Watch your back.
			string_id message = new string_id (c_stringFile, "s_16e3e795");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm on my way.
	if (response == "s_e42b260e")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "check_wrist_device");

			corellia_rebel_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: You don't have much time.
			string_id message = new string_id (c_stringFile, "s_13835aab");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You failed mission 3, do it over.
	//-- NPC: That was a complete disaster!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Hey, things don't always turn out right!
	if (response == "s_e30b9697")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			corellia_rebel_tier4_action_grantMissionThree (player, npc);

			//-- NPC: Don't sass me, junior! You should be ashamed of yourself! Now go back out there and clean up that mess!
			string_id message = new string_id (c_stringFile, "s_f5d1e7f4");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not done yet!
	if (response == "s_10eafe13")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantMissionThree (player, npc);

			//-- NPC: I should hope not! Good grief! 
			string_id message = new string_id (c_stringFile, "s_42b12fa4");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch42 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You failed mission 4, do it over.
	//-- NPC: Oh what is THIS. I told you to take out those freighters!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Those ships have guns!
	if (response == "s_4dd9a7b9")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "stamp_feet");

			corellia_rebel_tier4_action_grantMissionFour (player, npc);

			//-- NPC: YOUR ship has guns! Start using them, will you? And try aiming this time!
			string_id message = new string_id (c_stringFile, "s_2eec3c3b");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes, I know!
	if (response == "s_35dd4aa2")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "implore");

			corellia_rebel_tier4_action_grantMissionFour (player, npc);

			//-- NPC: Well then why aren't you DOING it? Get back out there!
			string_id message = new string_id (c_stringFile, "s_6561f20e");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You are ready for mission 1 in the Dathomir system.
	//-- NPC: OK %NU, let's talk business! This situation is SERIOUS!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: It is?
	if (response == "s_3609c810")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: YES! It's ALWAYS serious when we're talking about Imperials!
			string_id message = new string_id (c_stringFile, "s_99069e90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Who's talking about Imperials?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f27f6d98");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 46);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Some other time? I'm really busy!
	if (response == "s_3c546115")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: What? You worthless bum! Fine! See if I ever try to give you a mission again!
			string_id message = new string_id (c_stringFile, "s_9be0e2f1");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm here to help.
	if (response == "s_ef3e641a")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: So you are! I hope you don't mind helping pirates.
			string_id message = new string_id (c_stringFile, "s_e380bbf8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Not at all - if it helps the Rebellion.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_96bfa401");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 60);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch46 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: YES! It's ALWAYS serious when we're talking about Imperials!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who's talking about Imperials?
	if (response == "s_f27f6d98")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_to_self");

			//-- NPC: I am! Try and keep up, will you? 
			string_id message = new string_id (c_stringFile, "s_449db6d4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_67e6df55");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I am! Try and keep up, will you? 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay.
	if (response == "s_67e6df55")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Apparently, the Black Suns are preparing to launch an assault on one of our mining interests in the Dathomir system.
			string_id message = new string_id (c_stringFile, "s_d98a6441");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And how are the Imperials involved?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Mining interests? You make it sound almost legitimate.
			boolean hasResponse1 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d566a3c");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f35bbdbd");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 48);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch48 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Apparently, the Black Suns are preparing to launch an assault on one of our mining interests in the Dathomir system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And how are the Imperials involved?
	if (response == "s_d566a3c")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: They're not!
			string_id message = new string_id (c_stringFile, "s_ffeba47a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: But you said...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_997f2d21");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 49);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Mining interests? You make it sound almost legitimate.
	if (response == "s_f35bbdbd")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Look, you don't have to like what we do. But those miners aren't hurting anybody. They're just trying to feed their families. And you know those Black Suns will show no mercy.
			string_id message = new string_id (c_stringFile, "s_2284c46");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They're criminals.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2a5d0bf1");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 54);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch49 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're not!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: But you said...
	if (response == "s_997f2d21")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: Stop talking and start listening! Oh great, you've made me lose my train of thought. I'll start again. The Black Suns are preparing to launch an assault on one of our mining interests. 
			string_id message = new string_id (c_stringFile, "s_f5e4dcb6");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Can't the vessels defend themselves?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f5cc7cf3");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 50);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch50 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Stop talking and start listening! Oh great, you've made me lose my train of thought. I'll start again. The Black Suns are preparing to launch an assault on one of our mining interests. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can't the vessels defend themselves?
	if (response == "s_f5cc7cf3")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: They're outgunned! So we need to help them out.
			string_id message = new string_id (c_stringFile, "s_c95dbd1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Should I target the Black Suns directly?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9d0a2207");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 51);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch51 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're outgunned! So we need to help them out.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I target the Black Suns directly?
	if (response == "s_9d0a2207")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			corellia_rebel_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Negative! They outgun YOU, too. Don't try to blast them to smithereens. Just hold them off long enough to give our mining vessels the time they need to escape.
			string_id message = new string_id (c_stringFile, "s_526ce515");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, let's do this!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_410d920c");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 52);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch52 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Negative! They outgun YOU, too. Don't try to blast them to smithereens. Just hold them off long enough to give our mining vessels the time they need to escape.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, let's do this!
	if (response == "s_410d920c")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: One more thing Rebel. All the missions I provide you with will be high risk missions and might be too much to handle by yourself. If you have any favors you can call in, doing so during my missions might be a good idea.
			string_id message = new string_id (c_stringFile, "s_a434ad25");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch54 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Look, you don't have to like what we do. But those miners aren't hurting anybody. They're just trying to feed their families. And you know those Black Suns will show no mercy.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They're criminals.
	if (response == "s_2a5d0bf1")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: So are you, according to the Empire.
			string_id message = new string_id (c_stringFile, "s_dfc39325");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Touche.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b045a378");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 55);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch55 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So are you, according to the Empire.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Touche.
	if (response == "s_b045a378")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Back to those poor miners. They're outgunned; there's no way they can defend themselves. I want you to get out there and set up a defensive perimeter.
			string_id message = new string_id (c_stringFile, "s_9f50a13");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Should I target the Black Suns directly?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_100");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 56);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch56 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Back to those poor miners. They're outgunned; there's no way they can defend themselves. I want you to get out there and set up a defensive perimeter.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I target the Black Suns directly?
	if (response == "s_100")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Negative! They outgun YOU, too. Don't try to blast them to smithereens. Just hold them off long enough to give our mining vessels the time they need to escape.
			string_id message = new string_id (c_stringFile, "s_102");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, let's do this!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_104");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch57 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Negative! They outgun YOU, too. Don't try to blast them to smithereens. Just hold them off long enough to give our mining vessels the time they need to escape.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, let's do this!
	if (response == "s_104")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: One more thing Rebel. All the missions I provide you with will be high risk missions and might be too much to handle by yourself. If you have any favors you can call in, doing so during my missions might be a good idea.
			string_id message = new string_id (c_stringFile, "s_106");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch60 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: So you are! I hope you don't mind helping pirates.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not at all - if it helps the Rebellion.
	if (response == "s_96bfa401")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: It does. Very wise of you to recognize that.
			string_id message = new string_id (c_stringFile, "s_fcddb599");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's the situation?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_6318beeb");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It does. Very wise of you to recognize that.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the situation?
	if (response == "s_6318beeb")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Apparently, the Black Suns are preparing to launch an assault on one of our mining interests in the Dathomir system.
			string_id message = new string_id (c_stringFile, "s_115");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Can't the vessels defend themselves?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_117");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 62);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch62 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Apparently, the Black Suns are preparing to launch an assault on one of our mining interests in the Dathomir system.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can't the vessels defend themselves?
	if (response == "s_117")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: They're outgunned! So we need to help them out.
			string_id message = new string_id (c_stringFile, "s_119");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Should I target the Black Suns directly?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_121");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch63 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're outgunned! So we need to help them out.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Should I target the Black Suns directly?
	if (response == "s_121")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_no");

			corellia_rebel_tier4_action_grantMissionOne (player, npc);

			//-- NPC: Negative! They outgun YOU, too. Don't try to blast them to smithereens. Just hold them off long enough to give our mining vessels the time they need to escape.
			string_id message = new string_id (c_stringFile, "s_123");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright, let's do this!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_125");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 64);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch64 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Negative! They outgun YOU, too. Don't try to blast them to smithereens. Just hold them off long enough to give our mining vessels the time they need to escape.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright, let's do this!
	if (response == "s_125")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: One more thing Rebel. All the missions I provide you with will be high risk missions and might be too much to handle by yourself. If you have any favors you can call in, doing so during my missions might be a good idea.
			string_id message = new string_id (c_stringFile, "s_127");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch66 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You are ready for mission 2 in the Dathomir system.
	//-- NPC: Nice work out there. You really kicked those guys where it counts. Ready for round two?

	//-- [RESPONSE NOTE] Alright
	//-- PLAYER: Of course!
	if (response == "s_6adec6b0")
	{
		//-- [NOTE] Good answer! Here's the situation. 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Those miners are important to us and you saved them. We're grateful, so quid pro quo. You helped us; now we'll help you.
			string_id message = new string_id (c_stringFile, "s_5d976742");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I like the sound of that.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_cd445db2");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 67);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] No thanks
	//-- PLAYER: Oh, I'm not here for a mission! I was just looking for the cantina.
	if (response == "s_dcabe09b")
	{
		//-- [NOTE] You lazy ba...
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: With pilots like you, who needs Imperials? 
			string_id message = new string_id (c_stringFile, "s_f15aeb5");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Another errand for Nym the pirate?
	if (response == "s_beaf1664")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Not this time, my grumpy friend. This time, you'll be helping your beloved Rebellion. 
			string_id message = new string_id (c_stringFile, "s_87e30fff");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: In that case, I'm listening.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eb703128");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 75);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch67 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] Good answer! Here's the situation. 
	//-- NPC: Those miners are important to us and you saved them. We're grateful, so quid pro quo. You helped us; now we'll help you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I like the sound of that.
	if (response == "s_cd445db2")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Intel reports that the Emperor is sending an Inquisitor to Dathomir.
			string_id message = new string_id (c_stringFile, "s_8b4d636d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_841d27db");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 68);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch68 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Intel reports that the Emperor is sending an Inquisitor to Dathomir.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why?
	if (response == "s_841d27db")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What?
			string_id message = new string_id (c_stringFile, "s_900f01d1");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I said, 'Why?'
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c985b91");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 69);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch69 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I said, 'Why?'
	if (response == "s_c985b91")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: What am I, a mind reader? They're probably concerned about Alliance attacks.
			string_id message = new string_id (c_stringFile, "s_d6f2e64d");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why send an Inquisitor? 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_29544149");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 70);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch70 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What am I, a mind reader? They're probably concerned about Alliance attacks.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then why send an Inquisitor? 
	if (response == "s_29544149")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Well, %TU, think about it. The Empire needs to know what the Alliance is planning. And no one is better at gathering information than an Inquisitor.
			string_id message = new string_id (c_stringFile, "s_f59d9106");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Have you ever seen an Inquisitor up close?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_37e345fe");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 71);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch71 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, %TU, think about it. The Empire needs to know what the Alliance is planning. And no one is better at gathering information than an Inquisitor.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Have you ever seen an Inquisitor up close?
	if (response == "s_37e345fe")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: Once...a long, long time ago. Let's not talk about it right now. I want you to kill that Inquisitor before he reaches the Imperial fleet in Dathomir.
			string_id message = new string_id (c_stringFile, "s_9a01b8f2");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you crazy? No one can kill an Inquisitor!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_eb1c3d4e");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 72);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch72 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Once...a long, long time ago. Let's not talk about it right now. I want you to kill that Inquisitor before he reaches the Imperial fleet in Dathomir.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you crazy? No one can kill an Inquisitor!
	if (response == "s_eb1c3d4e")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_forward");

			corellia_rebel_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: Don't talk back to me, junior! And don't be ridiculous! The Inquisitors are mortal, like the rest of us. Believe me, they CAN be killed. I know that for a fact. Now I want you to get out there, immediately!
			string_id message = new string_id (c_stringFile, "s_f3603d67");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch75 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Not this time, my grumpy friend. This time, you'll be helping your beloved Rebellion. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: In that case, I'm listening.
	if (response == "s_eb703128")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: You should think about leaving the Alliance and working for Nym! You'll make more money.
			string_id message = new string_id (c_stringFile, "s_3b361a5f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not in it for the money.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_761dd18e");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 76);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch76 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You should think about leaving the Alliance and working for Nym! You'll make more money.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not in it for the money.
	if (response == "s_761dd18e")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: Think of your future, junior! Someday you'll be a crazy old man like me. Do you still want to be working then?
			string_id message = new string_id (c_stringFile, "s_a04dd423");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll take it as it comes.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7052aaab");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 77);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch77 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Think of your future, junior! Someday you'll be a crazy old man like me. Do you still want to be working then?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'll take it as it comes.
	if (response == "s_7052aaab")
	{
		doAnimationAction (player, "shrug_hands");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Oh! So enlightened! Bah! Never mind. Let's talk about your mission.
			string_id message = new string_id (c_stringFile, "s_29f4061f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Fine by me.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c2bbb747");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 78);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch78 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh! So enlightened! Bah! Never mind. Let's talk about your mission.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Fine by me.
	if (response == "s_c2bbb747")
	{
		doAnimationAction (player, "shrug_shoulders");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Here's the situation. Intel reports that the Emperor is sending an Inquisitor to Dathomir.
			string_id message = new string_id (c_stringFile, "s_d16ee99f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_156");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 79);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch79 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Here's the situation. Intel reports that the Emperor is sending an Inquisitor to Dathomir.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why?
	if (response == "s_156")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What?
			string_id message = new string_id (c_stringFile, "s_158");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I said, 'Why?'
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_160");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 80);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch80 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I said, 'Why?'
	if (response == "s_160")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: What am I, a mind reader? They're probably concerned about Alliance attacks.
			string_id message = new string_id (c_stringFile, "s_162");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Then why send an Inquisitor? 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_164");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 81);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch81 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What am I, a mind reader? They're probably concerned about Alliance attacks.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Then why send an Inquisitor? 
	if (response == "s_164")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: Well, %TU, think about it. The Empire needs to know what the Alliance is planning. And no one is better at gathering information than an Inquisitor.
			string_id message = new string_id (c_stringFile, "s_166");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Have you ever seen an Inquisitor up close?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_168");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 82);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch82 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, %TU, think about it. The Empire needs to know what the Alliance is planning. And no one is better at gathering information than an Inquisitor.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Have you ever seen an Inquisitor up close?
	if (response == "s_168")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Once...a long, long time ago. Let's not talk about it right now. I want you to kill that Inquisitor before he reaches the Imperial fleet in Dathomir.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Are you crazy? No one can kill an Inquisitor!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 83);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch83 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Once...a long, long time ago. Let's not talk about it right now. I want you to kill that Inquisitor before he reaches the Imperial fleet in Dathomir.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are you crazy? No one can kill an Inquisitor!
	if (response == "s_172")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "point_accusingly");

			corellia_rebel_tier4_action_grantMissionTwo (player, npc);

			//-- NPC: Don't talk back to me, junior! And don't be ridiculous! The Inquisitors are mortal, like the rest of us. Believe me, they CAN be killed. I know that for a fact. Now I want you to get out there, immediately!
			string_id message = new string_id (c_stringFile, "s_174");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch85 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You are ready for mission 3 in the Dathomir system.
	//-- NPC: Hey Rebel! I've got a new assignment for you. 

	//-- [RESPONSE NOTE] Alright
	//-- PLAYER: What is it?
	if (response == "s_1c8bddbb")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What?
			string_id message = new string_id (c_stringFile, "s_178");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I said, 'What is it?'!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_3b95cf2f");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 86);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] No thanks
	//-- PLAYER: Ship's in the shop.
	if (response == "s_f48fdfbd")
	{
		//-- [NOTE] You lazy ba...
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: Likely story. Why don't you just tell me that your dog ate your blasters?
			string_id message = new string_id (c_stringFile, "s_1af32f06");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Something good?
	if (response == "s_1551b284")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "curtsey1");

			//-- NPC: Something that will help the Rebellion. How does that sound to you?
			string_id message = new string_id (c_stringFile, "s_83f72ca3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Great.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7065a3b");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 95);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Not another Inquisitor assignment, I hope.
	if (response == "s_60080304")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			//-- NPC: No. Those don't come along very often. 
			string_id message = new string_id (c_stringFile, "s_7da3fc7a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I don't like targeting Inquisitors. You want to know why?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d054f893");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 103);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch86 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I said, 'What is it?'!
	if (response == "s_3b95cf2f")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: What kind of question is that?
			string_id message = new string_id (c_stringFile, "s_ff9fae78");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What?...Never mind. Do you have a mission for me?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8fc8c755");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 87);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch87 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What kind of question is that?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What?...Never mind. Do you have a mission for me?
	if (response == "s_8fc8c755")
	{
		doAnimationAction (player, "shake_head_disgust");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: As a matter of fact, I do! How did you know that?
			string_id message = new string_id (c_stringFile, "s_4057d656");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Lucky guess.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_840af840");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 88);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch88 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: As a matter of fact, I do! How did you know that?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Lucky guess.
	if (response == "s_840af840")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, the Empire has no idea what the Alliance has planned, thanks to you.
			string_id message = new string_id (c_stringFile, "s_882e7ab9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Even I don't know what the Alliance has planned.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d06c9b96");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 89);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch89 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, the Empire has no idea what the Alliance has planned, thanks to you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Even I don't know what the Alliance has planned.
	if (response == "s_d06c9b96")
	{
		doAnimationAction (player, "point_to_self");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_finger_warning");

			//-- NPC: That's because it's top secret! In the meantime, let's keep gunning for the Empire. You're going to Dathomir. I want you to join some of my men on a guerilla strike against an Imperial base.
			string_id message = new string_id (c_stringFile, "s_a6e1a5f4");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I like that!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bed0dfe7");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 90);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch90 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's because it's top secret! In the meantime, let's keep gunning for the Empire. You're going to Dathomir. I want you to join some of my men on a guerilla strike against an Imperial base.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I like that!
	if (response == "s_bed0dfe7")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I thought you would! Now you don't have to take the whole base apart. You're just going to pave the way for an Alliance B-Wing squadron. They've had their sights set on this base for a while. 
			string_id message = new string_id (c_stringFile, "s_8cac3c56");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why is the Alliance sending pirates? This sounds like strictly a Rebel operation. 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98199797");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 91);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch91 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I thought you would! Now you don't have to take the whole base apart. You're just going to pave the way for an Alliance B-Wing squadron. They've had their sights set on this base for a while. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why is the Alliance sending pirates? This sounds like strictly a Rebel operation. 
	if (response == "s_98199797")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: They're using my men because they know every inch of Dathomir space. They'll run rings around those Imperials. I suggest you follow their every move. I trained them all myself.
			string_id message = new string_id (c_stringFile, "s_1bb2f893");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You did?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7ae51280");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 92);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch92 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're using my men because they know every inch of Dathomir space. They'll run rings around those Imperials. I suggest you follow their every move. I trained them all myself.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You did?
	if (response == "s_7ae51280")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pose_proudly");

			corellia_rebel_tier4_action_grantMissionThree (player, npc);

			//-- NPC: Sure! I wasn't ALWAYS an old man, you know. I used to have a few more marbles rolling around upstairs. Now go on; get out of here.
			string_id message = new string_id (c_stringFile, "s_11769c29");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch95 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Something that will help the Rebellion. How does that sound to you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Great.
	if (response == "s_7065a3b")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Have you changed your mind?
			string_id message = new string_id (c_stringFile, "s_a2c04cca");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You mean, will I quit the Rebellion and start working for Nym full time? Forget it. It'll never happen.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7fca522a");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 96);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch96 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Have you changed your mind?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You mean, will I quit the Rebellion and start working for Nym full time? Forget it. It'll never happen.
	if (response == "s_7fca522a")
	{
		doAnimationAction (player, "shake_head_no");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shrug_shoulders");

			//-- NPC: Fine. Well the Alliance is feeling more confident, now that you've terminated that Inquisitor.
			string_id message = new string_id (c_stringFile, "s_7afc40b8");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: They'll send another one eventually.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c55a344e");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 97);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch97 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fine. Well the Alliance is feeling more confident, now that you've terminated that Inquisitor.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: They'll send another one eventually.
	if (response == "s_c55a344e")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: Yes, but the Rebels are being smart. They're deciding to strike now, while the iron is hot.
			string_id message = new string_id (c_stringFile, "s_9e1c0119");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What's the plan?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d1be90a9");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 98);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch98 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yes, but the Rebels are being smart. They're deciding to strike now, while the iron is hot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What's the plan?
	if (response == "s_d1be90a9")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're going to Dathomir. I want you to join some of my men on a guerilla strike against an Imperial base.
			string_id message = new string_id (c_stringFile, "s_ccac6a71");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I like the sound of that!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_f50be00f");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 99);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch99 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're going to Dathomir. I want you to join some of my men on a guerilla strike against an Imperial base.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I like the sound of that!
	if (response == "s_f50be00f")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: I thought you would! Now you don't have to take the whole base apart. You're just going to pave the way for an Alliance B-Wing squadron. They've had their sights set on this base for a while. 
			string_id message = new string_id (c_stringFile, "s_207");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why is the Alliance sending pirates? This sounds like strictly a Rebel operation. 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_209");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 100);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch100 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I thought you would! Now you don't have to take the whole base apart. You're just going to pave the way for an Alliance B-Wing squadron. They've had their sights set on this base for a while. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why is the Alliance sending pirates? This sounds like strictly a Rebel operation. 
	if (response == "s_209")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: They're using my men because they know every inch of Dathomir space. They'll run rings around those Imperials. I suggest you follow their every move. I trained them all myself.
			string_id message = new string_id (c_stringFile, "s_211");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You did?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_213");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 101);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch101 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're using my men because they know every inch of Dathomir space. They'll run rings around those Imperials. I suggest you follow their every move. I trained them all myself.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You did?
	if (response == "s_213")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			corellia_rebel_tier4_action_grantMissionThree (player, npc);

			//-- NPC: Sure! I wasn't ALWAYS an old man, you know. I used to have a few more marbles rolling around upstairs. Now go on; get out of here.
			string_id message = new string_id (c_stringFile, "s_215");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch103 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: No. Those don't come along very often. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I don't like targeting Inquisitors. You want to know why?
	if (response == "s_d054f893")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Why?
			string_id message = new string_id (c_stringFile, "s_220");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Because it makes another Inquisitors angry - angry at ME. And that's trouble I don't need.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_b45dc260");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 104);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch104 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Why?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Because it makes another Inquisitors angry - angry at ME. And that's trouble I don't need.
	if (response == "s_b45dc260")
	{
		doAnimationAction (player, "shiver");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: They'll never know it was you.
			string_id message = new string_id (c_stringFile, "s_268606ba");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [exhale loudly]
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7ce1c24");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 105);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch105 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They'll never know it was you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [exhale loudly]
	if (response == "s_7ce1c24")
	{
		doAnimationAction (player, "sigh_deeply");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: Remember what I said. They're just men.
			string_id message = new string_id (c_stringFile, "s_21374155");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Easy for you to say. They're not gunning for you.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_77633b61");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 106);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch106 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Remember what I said. They're just men.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Easy for you to say. They're not gunning for you.
	if (response == "s_77633b61")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "gesticulate_wildly");

			//-- NPC: They're not gunning for YOU either, junior! Now stop moping. Let's get your mind off all this. I've got a mission for you.
			string_id message = new string_id (c_stringFile, "s_a64a6f6f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK fine.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98588d70");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 107);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch107 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're not gunning for YOU either, junior! Now stop moping. Let's get your mind off all this. I've got a mission for you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK fine.
	if (response == "s_98588d70")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, the Empire has no idea what the Alliance has planned, thanks to you.
			string_id message = new string_id (c_stringFile, "s_229");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Even I don't know what the Alliance has planned.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_231");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 108);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch108 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, the Empire has no idea what the Alliance has planned, thanks to you.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Even I don't know what the Alliance has planned.
	if (response == "s_231")
	{
		doAnimationAction (player, "point_to_self");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: That's because it's top secret! In the meantime, let's keep gunning for the Empire. You're going to Dathomir. I want you to join some of my men on a guerilla strike against an Imperial base.
			string_id message = new string_id (c_stringFile, "s_233");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I like the sound of that!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_235");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 109);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch109 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: That's because it's top secret! In the meantime, let's keep gunning for the Empire. You're going to Dathomir. I want you to join some of my men on a guerilla strike against an Imperial base.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I like the sound of that!
	if (response == "s_235")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: I thought you would! Now you don't have to take the whole base apart. You're just going to pave the way for an Alliance B-Wing squadron. They've had their sights set on this base for a while. 
			string_id message = new string_id (c_stringFile, "s_237");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Why is the Alliance sending pirates? This sounds like strictly a Rebel operation. 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_239");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 110);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch110 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I thought you would! Now you don't have to take the whole base apart. You're just going to pave the way for an Alliance B-Wing squadron. They've had their sights set on this base for a while. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Why is the Alliance sending pirates? This sounds like strictly a Rebel operation. 
	if (response == "s_239")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: They're using my men because they know every inch of Dathomir space. They'll run rings around those Imperials. I suggest you follow their every move. I trained them all myself.
			string_id message = new string_id (c_stringFile, "s_241");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You did?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_243");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 111);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch111 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: They're using my men because they know every inch of Dathomir space. They'll run rings around those Imperials. I suggest you follow their every move. I trained them all myself.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You did?
	if (response == "s_243")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "wave_on_dismissing");

			corellia_rebel_tier4_action_grantMissionThree (player, npc);

			//-- NPC: Sure! I wasn't ALWAYS an old man, you know. I used to have a few more marbles rolling around upstairs. Now go on; get out of here.
			string_id message = new string_id (c_stringFile, "s_245");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch113 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You are ready for mission 4 in the Dantooine system.
	//-- NPC: We've got the Empire on the run! Let's kick 'em while they're down!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Are they really on the run?
	if (response == "s_5673ded2")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "embarrassed");

			//-- NPC: Well, no. Not exactly. . But we've managed to hit them pretty hard lately.
			string_id message = new string_id (c_stringFile, "s_1c669");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Absolutely.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d9cced1f");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 114);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Get somebody else to do your dirty work! I need a break.
	if (response == "s_9976a72")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Is it your naptime? Well fine then!
			string_id message = new string_id (c_stringFile, "s_9e3df78d");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Haha! You're joking, right?
	if (response == "s_5fe6ae71")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course not!
			string_id message = new string_id (c_stringFile, "s_f5427310");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: The Empire doesn't run. 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2ac3e9a6");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 123);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch114 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, no. Not exactly. . But we've managed to hit them pretty hard lately.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Absolutely.
	if (response == "s_d9cced1f")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: And now we have the chance to REALLY do some damage. And that's where you come in.
			string_id message = new string_id (c_stringFile, "s_5e4c5d9a");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What have you got for me?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_99d3b4fd");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 115);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch115 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And now we have the chance to REALLY do some damage. And that's where you come in.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What have you got for me?
	if (response == "s_99d3b4fd")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Apparently, the Empire's had just about enough of our activities in the area. I don't know who they hate more, smugglers or Rebels.
			string_id message = new string_id (c_stringFile, "s_50669846");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Same difference to them.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9a626852");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 116);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch116 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Apparently, the Empire's had just about enough of our activities in the area. I don't know who they hate more, smugglers or Rebels.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Same difference to them.
	if (response == "s_9a626852")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're probably right, %NU. We've received word that they're planning to build a space station in the Lok system. They'll use it to monitor all the activity in the area.
			string_id message = new string_id (c_stringFile, "s_e93fce3e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds like a problem.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_53544796");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 117);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch117 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're probably right, %NU. We've received word that they're planning to build a space station in the Lok system. They'll use it to monitor all the activity in the area.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like a problem.
	if (response == "s_53544796")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Yep. A BIG one. We can't let them build that station. So here's the plan! We're going to destroy the station components while they're en route to Lok.
			string_id message = new string_id (c_stringFile, "s_b607bb2e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Smart.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8729f60e");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 118);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch118 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yep. A BIG one. We can't let them build that station. So here's the plan! We're going to destroy the station components while they're en route to Lok.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Smart.
	if (response == "s_8729f60e")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Of course it's smart! It was my idea. Now listen up. Your first objective is to find out what route those Imperial freighters plan to use. 
			string_id message = new string_id (c_stringFile, "s_31fd5acf");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I do that?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_a9e4fd02");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 119);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch119 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course it's smart! It was my idea. Now listen up. Your first objective is to find out what route those Imperial freighters plan to use. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I do that?
	if (response == "s_a9e4fd02")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're going to kidnap one of their technical supervisors. He knows the plan. And pretty soon, you'll know the plan as well!
			string_id message = new string_id (c_stringFile, "s_127f2e23");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I find this guy?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_69a739e6");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 120);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch120 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're going to kidnap one of their technical supervisors. He knows the plan. And pretty soon, you'll know the plan as well!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find this guy?
	if (response == "s_69a739e6")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantMissionFour (player, npc);

			//-- NPC: Check your ship's computer. All the information you need is there. Good luck.
			string_id message = new string_id (c_stringFile, "s_abf08a18");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch123 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course not!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: The Empire doesn't run. 
	if (response == "s_2ac3e9a6")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well, whatever! You know what I mean. Don't be obstinate, junior!
			string_id message = new string_id (c_stringFile, "s_cbcfe001");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: You are crazy!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_4519dec9");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 124);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch124 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well, whatever! You know what I mean. Don't be obstinate, junior!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: You are crazy!
	if (response == "s_4519dec9")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: Crazy like a SQUALL! Don't underestimate me, %TU! I'm thinking, always thinking. I was smart enough to join this Alliance with the Rebels, wasn't I?
			string_id message = new string_id (c_stringFile, "s_8762aa6e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What do you mean?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_272");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 125);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch125 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Crazy like a SQUALL! Don't underestimate me, %TU! I'm thinking, always thinking. I was smart enough to join this Alliance with the Rebels, wasn't I?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What do you mean?
	if (response == "s_272")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: We could never hold our own against the Imperials. We needed your guns. And you needed us! A perfect situation.
			string_id message = new string_id (c_stringFile, "s_b1f1f364");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I guess so.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_af22b9b2");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 126);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch126 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We could never hold our own against the Imperials. We needed your guns. And you needed us! A perfect situation.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I guess so.
	if (response == "s_af22b9b2")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "pound_fist_chest");

			//-- NPC: And it's paid off. And now we have the chance to REALLY do some damage. And that's where you come in.
			string_id message = new string_id (c_stringFile, "s_dbcde2d3");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What have you got for me?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_277");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 127);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch127 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: And it's paid off. And now we have the chance to REALLY do some damage. And that's where you come in.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What have you got for me?
	if (response == "s_277")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Apparently, the Empire's had just about enough of our activities in the area. I don't know who they hate more, smugglers or Rebels.
			string_id message = new string_id (c_stringFile, "s_279");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Same difference to them.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_281");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 128);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch128 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Apparently, the Empire's had just about enough of our activities in the area. I don't know who they hate more, smugglers or Rebels.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Same difference to them.
	if (response == "s_281")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "rub_chin_thoughtful");

			//-- NPC: You're probably right, %NU. We've received word that they're planning to build a space station in the Lok system. They'll use it to monitor all the activity in the area.
			string_id message = new string_id (c_stringFile, "s_283");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Sounds like a problem.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_285");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 129);

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcSpeak (player, pp);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				prose_package pp = new prose_package ();
				pp.stringId = message;
				pp.actor.set (player);
				pp.target.set (npc);

				npcEndConversationWithMessage (player, pp);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch129 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're probably right, %NU. We've received word that they're planning to build a space station in the Lok system. They'll use it to monitor all the activity in the area.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Sounds like a problem.
	if (response == "s_285")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "nod_head_once");

			//-- NPC: Yep. A BIG one. We can't let them build that station. So here's the plan! We're going to destroy the station components while they're en route to Lok.
			string_id message = new string_id (c_stringFile, "s_287");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Smart.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_289");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 130);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch130 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yep. A BIG one. We can't let them build that station. So here's the plan! We're going to destroy the station components while they're en route to Lok.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Smart.
	if (response == "s_289")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_head");

			//-- NPC: Of course it's smart! It was my idea. Now listen up. Your first objective is to find out what route those Imperial freighters plan to use. 
			string_id message = new string_id (c_stringFile, "s_291");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: How do I do that?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_293");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 131);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch131 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Of course it's smart! It was my idea. Now listen up. Your first objective is to find out what route those Imperial freighters plan to use. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: How do I do that?
	if (response == "s_293")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: You're going to kidnap one of their technical supervisors. He knows the plan. And pretty soon, you'll know the plan as well!
			string_id message = new string_id (c_stringFile, "s_295");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I find this guy?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_297");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 132);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch132 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You're going to kidnap one of their technical supervisors. He knows the plan. And pretty soon, you'll know the plan as well!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I find this guy?
	if (response == "s_297")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantMissionFour (player, npc);

			//-- NPC: Check your ship's computer. All the information you need is there. Good luck.
			string_id message = new string_id (c_stringFile, "s_299");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch134 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You're ready for a new skill, which would you like?
	//-- NPC: Hey! You! You're still pretty wet behind the ears, you know. What you need are SKILLS! And you've come to the right place. I grant skills. Even to fools like yourself!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What skill am I to learn?
	if (response == "s_421")
	{
		//-- [NOTE] 
		if (!corellia_rebel_tier4_condition_hasStarship04 (player, npc))
		{
			//-- NPC: I don't think you'll be of any use to anyone until you learn about hyper-maneuverable starfighters.
			string_id message = new string_id (c_stringFile, "s_422");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Alright.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_426");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 135);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_rebel_tier4_condition_hasWeapon04 (player, npc))
		{
			//-- NPC: Do you like guns? And engines? You'd better, because you're going to learn about heavy starship component use.
			string_id message = new string_id (c_stringFile, "s_423");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I like guns.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_428");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 137);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (!corellia_rebel_tier4_condition_hasProcedures04 (player, npc))
		{
			//-- NPC: A smarmy nerf-herder like yourself could probably make good use of some special weapons and tactics training.
			string_id message = new string_id (c_stringFile, "s_424");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: OK. Thank you.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_432");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 140);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Since you don't seem to have any of your own, how about some DROID intelligence theory training.
			string_id message = new string_id (c_stringFile, "s_425");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Was that an insult?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_434");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 142);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch135 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't think you'll be of any use to anyone until you learn about hyper-maneuverable starfighters.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Alright.
	if (response == "s_426")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_buyStarshipsSkill04 (player, npc);

			//-- NPC: Calm down. Calm down. I don't think I've seen anyone so excited about starships.
			string_id message = new string_id (c_stringFile, "s_427");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch137 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Do you like guns? And engines? You'd better, because you're going to learn about heavy starship component use.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I like guns.
	if (response == "s_428")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_buyWeaponsSkill04 (player, npc);

			//-- NPC: Yeah, I BET you do.
			string_id message = new string_id (c_stringFile, "s_429");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What does that mean?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_430");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 138);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch138 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Yeah, I BET you do.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What does that mean?
	if (response == "s_430")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't know. Go away.
			string_id message = new string_id (c_stringFile, "s_431");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch140 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: A smarmy nerf-herder like yourself could probably make good use of some special weapons and tactics training.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: OK. Thank you.
	if (response == "s_432")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_buyProceduresSkill04 (player, npc);

			//-- NPC: Don't thank me. Just go away.
			string_id message = new string_id (c_stringFile, "s_433");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch142 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Since you don't seem to have any of your own, how about some DROID intelligence theory training.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Was that an insult?
	if (response == "s_434")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "giveup");

			corellia_rebel_tier4_action_buyDroidSkill04 (player, npc);

			//-- NPC: Oh no! You saw through my clever word-play and discovered my disdain for your intellectual capabilities! I really feel bad now!
			string_id message = new string_id (c_stringFile, "s_435");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I forgive you.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_436");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 143);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch143 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Oh no! You saw through my clever word-play and discovered my disdain for your intellectual capabilities! I really feel bad now!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I forgive you.
	if (response == "s_436")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Please go away. Now.
			string_id message = new string_id (c_stringFile, "s_437");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch145 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: It's about time you got here, pilot. Suit up.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Um...I don't think we've met.
	if (response == "s_34d61fa3")
	{
		corellia_rebel_tier4_action_setMetMe (player, npc);

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "slow_down");

			//-- NPC: You can call me Arkon. I imagine that Aqzow sent you?
			string_id message = new string_id (c_stringFile, "s_450d7208");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Yes.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d70dba34");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 146);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch146 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: You can call me Arkon. I imagine that Aqzow sent you?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Yes.
	if (response == "s_d70dba34")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "cover_mouth");

			//-- NPC: How unfortunate. 
			string_id message = new string_id (c_stringFile, "s_c384d95e");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Um, yes. Well, I'm here now.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2846f47d");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 147);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch147 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: How unfortunate. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Um, yes. Well, I'm here now.
	if (response == "s_2846f47d")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "tap_foot");

			//-- NPC: What did he say about me?
			string_id message = new string_id (c_stringFile, "s_981faf84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nothing I can repeat.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8209f997");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 148);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch148 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What did he say about me?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing I can repeat.
	if (response == "s_8209f997")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "shake_head_disgust");

			//-- NPC: Sounds like he hasn't changed. Bleh. I spit on him.
			string_id message = new string_id (c_stringFile, "s_3dc78ada");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Um, OK. Maybe I should just get to work. 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_afc14266");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 149);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch149 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Sounds like he hasn't changed. Bleh. I spit on him.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Um, OK. Maybe I should just get to work. 
	if (response == "s_afc14266")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Are you one of those goody two-shoes Rebels? You know, the kind that believes in the cause and all that?
			string_id message = new string_id (c_stringFile, "s_fc6dfba9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Of course!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: Really, I just want to work.
			boolean hasResponse1 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_325");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_345");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 150);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch150 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Are you one of those goody two-shoes Rebels? You know, the kind that believes in the cause and all that?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Of course!
	if (response == "s_325")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: True believers are boring.
			string_id message = new string_id (c_stringFile, "s_58e80738");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: And pirates are criminals.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d79718db");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 151);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Really, I just want to work.
	if (response == "s_345")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well listen up. We are dealing with several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 
			string_id message = new string_id (c_stringFile, "s_347");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_349");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 160);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch151 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: True believers are boring.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: And pirates are criminals.
	if (response == "s_d79718db")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Haha! Good point. Well then I'm a criminal. And you're going to help me. And then I'll help you - maybe. Do we understand each other?
			string_id message = new string_id (c_stringFile, "s_f51574ed");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Really, I just want to work.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_73313039");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 152);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch152 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Haha! Good point. Well then I'm a criminal. And you're going to help me. And then I'll help you - maybe. Do we understand each other?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Really, I just want to work.
	if (response == "s_73313039")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Well listen up. We are dealing with several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 
			string_id message = new string_id (c_stringFile, "s_28d914fc");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_42880861");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 153);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch153 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well listen up. We are dealing with several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay...
	if (response == "s_42880861")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What'd you say?
			string_id message = new string_id (c_stringFile, "s_dc241019");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I said, 'Okay'!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_48529763");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 154);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch154 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What'd you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I said, 'Okay'!
	if (response == "s_48529763")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hmph! Well here's some good news. One of our men has raided an Imperial outpost on Dathomir and he walked away with plenty of loot. Now all he has to do is escape! Easier said than done, of course, as the Empire wants their things back. So you could escort our man out of Dathomir...
			string_id message = new string_id (c_stringFile, "s_f13513cd");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Uh huh.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_c3363bcd");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 155);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch155 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hmph! Well here's some good news. One of our men has raided an Imperial outpost on Dathomir and he walked away with plenty of loot. Now all he has to do is escape! Easier said than done, of course, as the Empire wants their things back. So you could escort our man out of Dathomir...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Uh huh.
	if (response == "s_c3363bcd")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What?!
			string_id message = new string_id (c_stringFile, "s_337");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nothing!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_539f9a2e");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 156);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch156 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing!
	if (response == "s_539f9a2e")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			//-- NPC: Don't smart off to me!
			string_id message = new string_id (c_stringFile, "s_e3e480fe");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_7e602d1b");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 157);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch157 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't smart off to me!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not!
	if (response == "s_7e602d1b")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Here's some MORE good news. Another group of our men infiltrated a number of Imperial troop transports in the Endor system. But they can't take control of those ships - not yet. First we need a pilot to disable the transport controls. This requires a pilot with skills.
			string_id message = new string_id (c_stringFile, "s_6429f86b");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [nod]
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8e9d0848");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 158);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch158 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Here's some MORE good news. Another group of our men infiltrated a number of Imperial troop transports in the Endor system. But they can't take control of those ships - not yet. First we need a pilot to disable the transport controls. This requires a pilot with skills.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [nod]
	if (response == "s_8e9d0848")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hm, what else is there? Oh yes...you'll like this, it's a Rebel rescue mission! A Rebel fleet was carrying several scientists through the Dantooine system, they were ambushed by Imperial fighters! That's not right. I want a pilot to get out there and help those ships escape before more Imperial ships arrive! And that's it! So what's it going to be, junior?
			string_id message = new string_id (c_stringFile, "s_9bbb9999");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll take care of those Black Suns. 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'll escort those freighters. 
			boolean hasResponse1 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Let me take a crack at disabling those troop transports.
			boolean hasResponse2 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want to help those scientists.
			boolean hasResponse3 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Can you explain it again?
			boolean hasResponse4 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_647ad2bc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d9116b4d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8450d7db");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9943289");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9775f996");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 167);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch159 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hm, what else is there? Oh yes...you'll like this, it's a Rebel rescue mission! A Rebel fleet was carrying several scientists through the Dantooine system, they were ambushed by Imperial fighters! That's not right. I want a pilot to get out there and help those ships escape before more Imperial ships arrive! And that's it! So what's it going to be, junior?

	//-- [RESPONSE NOTE] Destroy Duty
	//-- PLAYER: I'll take care of those Black Suns. 
	if (response == "s_647ad2bc")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionFour (player, npc);

			//-- NPC: Then you're going to Dantooine! Goodbye! 
			string_id message = new string_id (c_stringFile, "s_d8209030");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Escort Duty
	//-- PLAYER: I'll escort those freighters. 
	if (response == "s_d9116b4d")
	{
		//-- [NOTE] Get to work in the Dathomir system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionOne (player, npc);

			//-- NPC: Then hurry up and get yourself over to Dathomir!
			string_id message = new string_id (c_stringFile, "s_fed79efe");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Recovery Duty
	//-- PLAYER: Let me take a crack at disabling those troop transports.
	if (response == "s_8450d7db")
	{
		//-- [NOTE] Get to work in the Endor system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionThree (player, npc);

			//-- NPC: Endor it is! Leave immediately!
			string_id message = new string_id (c_stringFile, "s_b0d064ef");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Rescue Duty
	//-- PLAYER: I want to help those scientists.
	if (response == "s_9943289")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionTwo (player, npc);

			//-- NPC: You think you're a hero or something? Fine! Go on! You'll find your scientists in the Dantooine system.
			string_id message = new string_id (c_stringFile, "s_835acc11");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can you explain it again?
	if (response == "s_9775f996")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fine, listen up. We are looking at several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 
			string_id message = new string_id (c_stringFile, "s_4bce8f6f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 172);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch160 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Well listen up. We are dealing with several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay...
	if (response == "s_349")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What'd you say?
			string_id message = new string_id (c_stringFile, "s_351");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I said, 'Okay'!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_353");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 161);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch161 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What'd you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I said, 'Okay'!
	if (response == "s_353")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hmph! Well here's some good news. One of our men has raided an Imperial outpost on Dathomir and he walked away with plenty of loot. Now all he has to do is escape! Easier said than done, of course, as the Empire wants their things back. So you could escort our man out of Dathomir...
			string_id message = new string_id (c_stringFile, "s_355");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Uh huh.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_357");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 162);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch162 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hmph! Well here's some good news. One of our men has raided an Imperial outpost on Dathomir and he walked away with plenty of loot. Now all he has to do is escape! Easier said than done, of course, as the Empire wants their things back. So you could escort our man out of Dathomir...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Uh huh.
	if (response == "s_357")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What?!
			string_id message = new string_id (c_stringFile, "s_359");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nothing!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_361");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 163);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch163 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing!
	if (response == "s_361")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			//-- NPC: Don't smart off to me!
			string_id message = new string_id (c_stringFile, "s_363");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_365");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 164);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch164 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't smart off to me!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not!
	if (response == "s_365")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Here's some MORE good news. Another group of our men infiltrated a number of Imperial troop transports in the Endor system. But they can't take control of those ships - not yet. First we need a pilot to disable the transport controls. This requires a pilot with skills.
			string_id message = new string_id (c_stringFile, "s_367");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [nod]
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_369");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 165);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch165 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Here's some MORE good news. Another group of our men infiltrated a number of Imperial troop transports in the Endor system. But they can't take control of those ships - not yet. First we need a pilot to disable the transport controls. This requires a pilot with skills.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [nod]
	if (response == "s_369")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hm, what else is there? Oh yes...you'll like this, it's a Rebel rescue mission! A Rebel fleet was carrying several scientists through the Dantooine system, they were ambushed by Imperial fighters! That's not right. I want a pilot to get out there and help those ships escape before more Imperial ships arrive! And that's it! So what's it going to be, junior?
			string_id message = new string_id (c_stringFile, "s_371");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll take care of those Black Suns. 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'll escort those freighters. 
			boolean hasResponse1 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Let me take a crack at disabling those troop transports.
			boolean hasResponse2 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want to help those scientists.
			boolean hasResponse3 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Can you explain it again?
			boolean hasResponse4 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_647ad2bc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d9116b4d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8450d7db");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9943289");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9775f996");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 167);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch166 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hm, what else is there? Oh yes...you'll like this, it's a Rebel rescue mission! A Rebel fleet was carrying several scientists through the Dantooine system, they were ambushed by Imperial fighters! That's not right. I want a pilot to get out there and help those ships escape before more Imperial ships arrive! And that's it! So what's it going to be, junior?

	//-- [RESPONSE NOTE] Destroy Duty
	//-- PLAYER: I'll take care of those Black Suns. 
	if (response == "s_647ad2bc")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionFour (player, npc);

			//-- NPC: Then you're going to Dantooine! Goodbye! 
			string_id message = new string_id (c_stringFile, "s_d8209030");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Escort Duty
	//-- PLAYER: I'll escort those freighters. 
	if (response == "s_d9116b4d")
	{
		//-- [NOTE] Get to work in the Dathomir system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionOne (player, npc);

			//-- NPC: Then hurry up and get yourself over to Dathomir!
			string_id message = new string_id (c_stringFile, "s_fed79efe");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Recovery Duty
	//-- PLAYER: Let me take a crack at disabling those troop transports.
	if (response == "s_8450d7db")
	{
		//-- [NOTE] Get to work in the Endor system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionThree (player, npc);

			//-- NPC: Endor it is! Leave immediately!
			string_id message = new string_id (c_stringFile, "s_b0d064ef");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Rescue Duty
	//-- PLAYER: I want to help those scientists.
	if (response == "s_9943289")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionTwo (player, npc);

			//-- NPC: You think you're a hero or something? Fine! Go on! You'll find your scientists in the Dantooine system.
			string_id message = new string_id (c_stringFile, "s_835acc11");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can you explain it again?
	if (response == "s_9775f996")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fine, listen up. We are looking at several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 
			string_id message = new string_id (c_stringFile, "s_4bce8f6f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 172);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch167 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] You have not completed tier-4, so do one of these duty missions until you xp your way to a new skill.
	//-- NPC: I'd like to see you do some WORK for a change! Sign up for something, pronto. Or do I have to explain it to you again?

	//-- [RESPONSE NOTE] Destroy Duty
	//-- PLAYER: I'll take care of those Black Suns. 
	if (response == "s_647ad2bc")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionFour (player, npc);

			//-- NPC: Then you're going to Dantooine! Goodbye! 
			string_id message = new string_id (c_stringFile, "s_d8209030");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Escort Duty
	//-- PLAYER: I'll escort those freighters. 
	if (response == "s_d9116b4d")
	{
		//-- [NOTE] Get to work in the Dathomir system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionOne (player, npc);

			//-- NPC: Then hurry up and get yourself over to Dathomir!
			string_id message = new string_id (c_stringFile, "s_fed79efe");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Recovery Duty
	//-- PLAYER: Let me take a crack at disabling those troop transports.
	if (response == "s_8450d7db")
	{
		//-- [NOTE] Get to work in the Endor system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionThree (player, npc);

			//-- NPC: Endor it is! Leave immediately!
			string_id message = new string_id (c_stringFile, "s_b0d064ef");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Rescue Duty
	//-- PLAYER: I want to help those scientists.
	if (response == "s_9943289")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionTwo (player, npc);

			//-- NPC: You think you're a hero or something? Fine! Go on! You'll find your scientists in the Dantooine system.
			string_id message = new string_id (c_stringFile, "s_835acc11");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can you explain it again?
	if (response == "s_9775f996")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fine, listen up. We are looking at several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 
			string_id message = new string_id (c_stringFile, "s_4bce8f6f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 172);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch172 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Fine, listen up. We are looking at several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Okay...
	if (response == "s_384")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What'd you say?
			string_id message = new string_id (c_stringFile, "s_386");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I said, 'Okay'!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_388");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 173);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch173 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What'd you say?

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I said, 'Okay'!
	if (response == "s_388")
	{
		doAnimationAction (player, "explain");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hmph! Well here's some good news. One of our men has raided an Imperial outpost on Dathomir and he walked away with plenty of loot. Now all he has to do is escape! Easier said than done, of course, as the Empire wants their things back. So you could escort our man out of Dathomir...
			string_id message = new string_id (c_stringFile, "s_390");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Uh huh.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_392");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 174);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch174 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hmph! Well here's some good news. One of our men has raided an Imperial outpost on Dathomir and he walked away with plenty of loot. Now all he has to do is escape! Easier said than done, of course, as the Empire wants their things back. So you could escort our man out of Dathomir...

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Uh huh.
	if (response == "s_392")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "listen");

			//-- NPC: What?!
			string_id message = new string_id (c_stringFile, "s_394");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Nothing!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_396");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 175);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch175 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: What?!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Nothing!
	if (response == "s_396")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "threaten");

			//-- NPC: Don't smart off to me!
			string_id message = new string_id (c_stringFile, "s_398");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'm not!
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_400");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 176);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch176 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Don't smart off to me!

	//-- [RESPONSE NOTE] 
	//-- PLAYER: I'm not!
	if (response == "s_400")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Here's some MORE good news. Another group of our men infiltrated a number of Imperial troop transports in the Endor system. But they can't take control of those ships - not yet. First we need a pilot to disable the transport controls. This requires a pilot with skills.
			string_id message = new string_id (c_stringFile, "s_402");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: [nod]
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_404");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 177);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch177 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Here's some MORE good news. Another group of our men infiltrated a number of Imperial troop transports in the Endor system. But they can't take control of those ships - not yet. First we need a pilot to disable the transport controls. This requires a pilot with skills.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: [nod]
	if (response == "s_404")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Hm, what else is there? Oh yes...you'll like this, it's a Rebel rescue mission! A Rebel fleet was carrying several scientists through the Dantooine system, they were ambushed by Imperial fighters! That's not right. I want a pilot to get out there and help those ships escape before more Imperial ships arrive! And that's it! So what's it going to be, junior?
			string_id message = new string_id (c_stringFile, "s_406");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: I'll take care of those Black Suns. 
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			//-- PLAYER: I'll escort those freighters. 
			boolean hasResponse1 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse1 = true;
			}

			//-- PLAYER: Let me take a crack at disabling those troop transports.
			boolean hasResponse2 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse2 = true;
			}

			//-- PLAYER: I want to help those scientists.
			boolean hasResponse3 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse3 = true;
			}

			//-- PLAYER: Can you explain it again?
			boolean hasResponse4 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse4 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_647ad2bc");

				if (hasResponse1)
					responses [responseIndex++] = new string_id (c_stringFile, "s_d9116b4d");

				if (hasResponse2)
					responses [responseIndex++] = new string_id (c_stringFile, "s_8450d7db");

				if (hasResponse3)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9943289");

				if (hasResponse4)
					responses [responseIndex++] = new string_id (c_stringFile, "s_9775f996");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 167);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch178 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Hm, what else is there? Oh yes...you'll like this, it's a Rebel rescue mission! A Rebel fleet was carrying several scientists through the Dantooine system, they were ambushed by Imperial fighters! That's not right. I want a pilot to get out there and help those ships escape before more Imperial ships arrive! And that's it! So what's it going to be, junior?

	//-- [RESPONSE NOTE] Destroy Duty
	//-- PLAYER: I'll take care of those Black Suns. 
	if (response == "s_647ad2bc")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionFour (player, npc);

			//-- NPC: Then you're going to Dantooine! Goodbye! 
			string_id message = new string_id (c_stringFile, "s_d8209030");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Escort Duty
	//-- PLAYER: I'll escort those freighters. 
	if (response == "s_d9116b4d")
	{
		//-- [NOTE] Get to work in the Dathomir system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionOne (player, npc);

			//-- NPC: Then hurry up and get yourself over to Dathomir!
			string_id message = new string_id (c_stringFile, "s_fed79efe");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Recovery Duty
	//-- PLAYER: Let me take a crack at disabling those troop transports.
	if (response == "s_8450d7db")
	{
		//-- [NOTE] Get to work in the Endor system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionThree (player, npc);

			//-- NPC: Endor it is! Leave immediately!
			string_id message = new string_id (c_stringFile, "s_b0d064ef");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Rescue Duty
	//-- PLAYER: I want to help those scientists.
	if (response == "s_9943289")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionTwo (player, npc);

			//-- NPC: You think you're a hero or something? Fine! Go on! You'll find your scientists in the Dantooine system.
			string_id message = new string_id (c_stringFile, "s_835acc11");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can you explain it again?
	if (response == "s_9775f996")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fine, listen up. We are looking at several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 
			string_id message = new string_id (c_stringFile, "s_4bce8f6f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 172);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch179 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Attention! Alliance Headquarters has approved your transfer. You are now serving under the command of Admiral Willham Burke, good man. You are to report to Admiral Burke immediately, so pack up your gear and get moving. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Who is Admiral Burke?
	if (response == "s_bbe3529b")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "explain");

			//-- NPC: Admiral Willham Burke is head of the fourth battle fleet. Last I heard, he was operating in and along the Outer Rim. Burke is a good man, a good leader, and a good soldier. He does the Alliance proud.
			string_id message = new string_id (c_stringFile, "s_bcbc9230");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: What does he want with me?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_86677c6b");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 180);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What if I don't have any gear, sir?
	if (response == "s_36f346a5")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "laugh");

			//-- NPC: Then it won't take you too long to pack! The orders don't include any details, but I can only assume it involves some operation that his fleet is about to undertake. You had best be ready for a challenge, pilot.
			string_id message = new string_id (c_stringFile, "s_8057f803");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Always, sir.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_ff9bf289");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 184);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch180 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Admiral Willham Burke is head of the fourth battle fleet. Last I heard, he was operating in and along the Outer Rim. Burke is a good man, a good leader, and a good soldier. He does the Alliance proud.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What does he want with me?
	if (response == "s_86677c6b")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: I don't have the answer to that one, pilot. But Admiral Burke requested you personally.
			string_id message = new string_id (c_stringFile, "s_6634adb9");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Where do I need to report?
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_bcc6b959");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 181);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch181 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: I don't have the answer to that one, pilot. But Admiral Burke requested you personally.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Where do I need to report?
	if (response == "s_bcc6b959")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: According to these orders, Admiral Burke is in Doaba Guerfel on Corellia. I recommend that you keep your eyes and ears open and you just might learn something. 
			string_id message = new string_id (c_stringFile, "s_d74e67f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Understood.
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_2c4789b");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 182);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch182 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: According to these orders, Admiral Burke is in Doaba Guerfel on Corellia. I recommend that you keep your eyes and ears open and you just might learn something. 

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Understood.
	if (response == "s_2c4789b")
	{
		doAnimationAction (player, "nod_head_once");

		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: He is expecting you right away, so I suggest you get a move on. If you fail to report to the Admiral in a timely fashion, it will reflect poorly on me, and that is something that I will not stand for! Dismissed, pilot.
			string_id message = new string_id (c_stringFile, "s_4ef35507");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch184 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Then it won't take you too long to pack! The orders don't include any details, but I can only assume it involves some operation that his fleet is about to undertake. You had best be ready for a challenge, pilot.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Always, sir.
	if (response == "s_ff9bf289")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			doAnimationAction (npc, "dismiss");

			//-- NPC: Then let's get a move on, pilot. The Admiral is not going to want to wait all day for you to arrive. You are to report into him at Doaba Guerfel on Corellia. Keep your wits about and you just might learn something. Dismissed, pilot.
			string_id message = new string_id (c_stringFile, "s_cb25bcd4");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int corellia_rebel_tier4_handleBranch186 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Back from your assignment with Admiral Burke, I see! I hope that everything went according to plan. I can use your help around here! 

	//-- [RESPONSE NOTE] Destroy Duty
	//-- PLAYER: I'll take care of those Black Suns. 
	if (response == "s_647ad2bc")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionFour (player, npc);

			//-- NPC: Then you're going to Dantooine! Goodbye! 
			string_id message = new string_id (c_stringFile, "s_d8209030");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Escort Duty
	//-- PLAYER: I'll escort those freighters. 
	if (response == "s_d9116b4d")
	{
		//-- [NOTE] Get to work in the Dathomir system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionOne (player, npc);

			//-- NPC: Then hurry up and get yourself over to Dathomir!
			string_id message = new string_id (c_stringFile, "s_fed79efe");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Recovery Duty
	//-- PLAYER: Let me take a crack at disabling those troop transports.
	if (response == "s_8450d7db")
	{
		//-- [NOTE] Get to work in the Endor system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionThree (player, npc);

			//-- NPC: Endor it is! Leave immediately!
			string_id message = new string_id (c_stringFile, "s_b0d064ef");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] Rescue Duty
	//-- PLAYER: I want to help those scientists.
	if (response == "s_9943289")
	{
		//-- [NOTE] Get to work in the Dantooine system.
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			corellia_rebel_tier4_action_grantDutyMissionTwo (player, npc);

			//-- NPC: You think you're a hero or something? Fine! Go on! You'll find your scientists in the Dantooine system.
			string_id message = new string_id (c_stringFile, "s_835acc11");
			utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Can you explain it again?
	if (response == "s_9775f996")
	{
		//-- [NOTE] 
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			//-- NPC: Fine, listen up. We are looking at several situations here, and I'd like to see what you can do. First of all, the Black Suns have been taking a lot of business from us lately. Nym doesn't like it. We need a pilot to send them a message. A LOUD message. Heh! 
			string_id message = new string_id (c_stringFile, "s_4bce8f6f");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: Okay...
			boolean hasResponse0 = false;
			if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_384");

				utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 172);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isMob (self)) || (isPlayer (self)))
		detachScript(self, "conversation.corellia_rebel_tier4");

	setCondition (self, CONDITION_CONVERSABLE);
	setName( self, "Worvog Arkon");
	setCondition( self, CONDITION_SPACE_INTERESTING );
	setInvulnerable(self, true);
	factions.setFaction( self, factions.FACTION_REBEL );
	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);
	setName( self, "Worvog Arkon");
	setCondition( self, CONDITION_SPACE_INTERESTING );
	setInvulnerable(self, true);
	factions.setFaction( self, factions.FACTION_REBEL );
	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);

	faceTo( self, player );
	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.corellia_rebel_tier4");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (!corellia_rebel_tier4_condition_isPilot (player, npc))
	{
		doAnimationAction (npc, "wave_on_dismissing");

		//-- NPC: You don't look like a pilot to me! 
		string_id message = new string_id (c_stringFile, "s_92c6d3dd");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You're the wrong faction.
	if (corellia_rebel_tier4_condition_isWrongFaction (player, npc))
	{
		doAnimationAction (npc, "listen");

		//-- NPC: Eh? Who's that? Who are you? Tell me your faction!
		string_id message = new string_id (c_stringFile, "s_774fb2f0");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm a privateer.
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm an Imperial.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7e579e1e");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_fb4c1f1f");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 2);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_rebel_tier4_condition_onMyTrack (player, npc))
	{
		//-- NPC: Eh? Who's that? You're no... wait, you ARE a Rebel pilot. But you don't fly with this unit. What are you doing here?
		string_id message = new string_id (c_stringFile, "s_84c908b8");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Do you have any missions that I could fly?
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6222b64d");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 5);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You're not through tier-3 yet
	if (!corellia_rebel_tier4_condition_hasCompletedTier3 (player, npc))
	{
		doAnimationAction (npc, "wave_finger_warning");

		//-- NPC: No no! You don't belong here!
		string_id message = new string_id (c_stringFile, "s_4f44d41d");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I don't?
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: How do you know?
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e0dc5fad");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e86f1062");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 8);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You're busy.  Already doing a mission.  Hup hup, go do it.
	if (corellia_rebel_tier4_condition_isOnMission (player, npc))
	{
		//-- NPC: %TU, you lack discipline! 
		string_id message = new string_id (c_stringFile, "s_256f3624");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I just lack motivation. 
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Whatever.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4ccb8d59");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_c1013aa7");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 11);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "corellia_rebel_tier4", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_rebel_tier4_condition_hasNegativeFaction (player, npc))
	{
		doAnimationAction (npc, "slit_throat");

		//-- NPC: Traitor!
		string_id message = new string_id (c_stringFile, "s_de1e215f");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What?!
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_7090182");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 14);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have completed mission one and so here is your reward.
	if (corellia_rebel_tier4_condition_isGettingMissionOneReward (player, npc))
	{
		doAnimationAction (npc, "applause_excited");

		corellia_rebel_tier4_action_rewardForMissionOne (player, npc);

		//-- NPC: Mission accomplished! And you know what that means.
		string_id message = new string_id (c_stringFile, "s_f95cd5f5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I sure do.
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What does it mean?
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_95c399f6");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6f679fea");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 18);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have completed mission two and so here is your reward.
	if (corellia_rebel_tier4_condition_isGettingMissionTwoReward (player, npc))
	{
		doAnimationAction (npc, "shrug_hands");

		corellia_rebel_tier4_action_rewardForMissionTwo (player, npc);

		//-- NPC: See? You could save the day after all! Not bad.
		string_id message = new string_id (c_stringFile, "s_da8e9da5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: You were right.
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: It wasn't easy.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_def742");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_b20c71fe");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 21);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have completed mission three and so here is your reward.
	if (corellia_rebel_tier4_condition_isGettingMissionThreeReward (player, npc))
	{
		doAnimationAction (npc, "salute1");

		corellia_rebel_tier4_action_rewardForMissionThree (player, npc);

		//-- NPC: I salute you, Rebel!
		string_id message = new string_id (c_stringFile, "s_10d8bf69");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: No need for that.
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Aw shucks.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_aed5139a");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_cb6eb294");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 24);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have completed mission four and so here is your reward.
	if (corellia_rebel_tier4_condition_isGettingMissionFourReward (player, npc))
	{
		doAnimationAction (npc, "laugh");

		corellia_rebel_tier4_action_rewardForMissionFour (player, npc);

		//-- NPC: You showed them! Let's see the Empire try to build a station in THIS system!
		string_id message = new string_id (c_stringFile, "s_81e25d24");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: It's not going to happen anytime soon.
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Happy to serve.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_61485366");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_197c8928");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 30);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You failed mission 1, do it over.
	if (corellia_rebel_tier4_condition_failedMissionOne (player, npc))
	{
		doAnimationAction (npc, "gesticulate_wildly");

		//-- NPC: In my day, a pilot would NEVER leave a job unfinished!
		string_id message = new string_id (c_stringFile, "s_4f755af2");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'm not done yet! 
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm ashamed.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8f3b3edb");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_88563b84");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 33);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You failed mission 2, do it over.
	if (corellia_rebel_tier4_condition_failedMissionTwo (player, npc))
	{
		doAnimationAction (npc, "stamp_feet");

		//-- NPC: You've got to get back out there. You realize what will happen if that Inquisitor gets a hold of your friends?
		string_id message = new string_id (c_stringFile, "s_1909d363");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I know.
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm on my way.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_87d7e908");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e42b260e");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 36);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You failed mission 3, do it over.
	if (corellia_rebel_tier4_condition_failedMissionThree (player, npc))
	{
		doAnimationAction (npc, "smack_self");

		//-- NPC: That was a complete disaster!
		string_id message = new string_id (c_stringFile, "s_d3d98e75");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Hey, things don't always turn out right!
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'm not done yet!
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_e30b9697");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_10eafe13");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 39);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You failed mission 4, do it over.
	if (corellia_rebel_tier4_condition_failedMissionFour (player, npc))
	{
		doAnimationAction (npc, "gesticulate_wildly");

		//-- NPC: Oh what is THIS. I told you to take out those freighters!
		string_id message = new string_id (c_stringFile, "s_78b8ac8f");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Those ships have guns!
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Yes, I know!
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_4dd9a7b9");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_35dd4aa2");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 42);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You are ready for mission 1 in the Dathomir system.
	if (corellia_rebel_tier4_condition_readyForMissionOne (player, npc))
	{
		doAnimationAction (npc, "pound_fist_palm");

		//-- NPC: OK %NU, let's talk business! This situation is SERIOUS!
		string_id message = new string_id (c_stringFile, "s_5bf6779f");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: It is?
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Some other time? I'm really busy!
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: I'm here to help.
		boolean hasResponse2 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3609c810");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_3c546115");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_ef3e641a");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 45);

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			npcStartConversation (player, npc, "corellia_rebel_tier4", null, pp, responses);
		}
		else
		{
			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);

			chat.chat (npc, player, null, null, pp);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You are ready for mission 2 in the Dathomir system.
	if (corellia_rebel_tier4_condition_readyForMissionTwo (player, npc))
	{
		//-- NPC: Nice work out there. You really kicked those guys where it counts. Ready for round two?
		string_id message = new string_id (c_stringFile, "s_1047bc70");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Of course!
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Oh, I'm not here for a mission! I was just looking for the cantina.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Another errand for Nym the pirate?
		boolean hasResponse2 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_6adec6b0");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_dcabe09b");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_beaf1664");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 66);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You are ready for mission 3 in the Dathomir system.
	if (corellia_rebel_tier4_condition_readyForMissionThree (player, npc))
	{
		doAnimationAction (npc, "beckon");

		//-- NPC: Hey Rebel! I've got a new assignment for you. 
		string_id message = new string_id (c_stringFile, "s_e8f374d9");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What is it?
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Ship's in the shop.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Something good?
		boolean hasResponse2 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Not another Inquisitor assignment, I hope.
		boolean hasResponse3 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1c8bddbb");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_f48fdfbd");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_1551b284");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_60080304");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 85);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You are ready for mission 4 in the Dantooine system.
	if (corellia_rebel_tier4_condition_readyForMissionFour (player, npc))
	{
		//-- NPC: We've got the Empire on the run! Let's kick 'em while they're down!
		string_id message = new string_id (c_stringFile, "s_ceb77d0c");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Are they really on the run?
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Get somebody else to do your dirty work! I need a break.
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Haha! You're joking, right?
		boolean hasResponse2 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5673ded2");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9976a72");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_5fe6ae71");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 113);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You're ready for a new skill, which would you like?
	if (corellia_rebel_tier4_condition_isReadyForTraining (player, npc))
	{
		doAnimationAction (npc, "beckon");

		//-- NPC: Hey! You! You're still pretty wet behind the ears, you know. What you need are SKILLS! And you've come to the right place. I grant skills. Even to fools like yourself!
		string_id message = new string_id (c_stringFile, "s_8907c99");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: What skill am I to learn?
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_421");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 134);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_rebel_tier4_condition_hasMetMe (player, npc))
	{
		//-- NPC: It's about time you got here, pilot. Suit up.
		string_id message = new string_id (c_stringFile, "s_71f899d5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Um...I don't think we've met.
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_34d61fa3");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 145);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] You have not completed tier-4, so do one of these duty missions until you xp your way to a new skill.
	if (!corellia_rebel_tier4_condition_hasCompletedTier4 (player, npc))
	{
		doAnimationAction (npc, "gesticulate_wildly");

		//-- NPC: I'd like to see you do some WORK for a change! Sign up for something, pronto. Or do I have to explain it to you again?
		string_id message = new string_id (c_stringFile, "s_a5958aac");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'll take care of those Black Suns. 
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'll escort those freighters. 
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Let me take a crack at disabling those troop transports.
		boolean hasResponse2 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want to help those scientists.
		boolean hasResponse3 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: Can you explain it again?
		boolean hasResponse4 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_647ad2bc");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d9116b4d");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8450d7db");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9943289");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9775f996");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 167);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (!corellia_rebel_tier4_condition_hasMasterSkill (player, npc))
	{
		doAnimationAction (npc, "beckon");

		//-- NPC: Attention! Alliance Headquarters has approved your transfer. You are now serving under the command of Admiral Willham Burke, good man. You are to report to Admiral Burke immediately, so pack up your gear and get moving. 
		string_id message = new string_id (c_stringFile, "s_b6b4b241");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Who is Admiral Burke?
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: What if I don't have any gear, sir?
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_bbe3529b");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_36f346a5");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 179);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
	{
		//-- NPC: Back from your assignment with Admiral Burke, I see! I hope that everything went according to plan. I can use your help around here! 
		string_id message = new string_id (c_stringFile, "s_d318a2bb");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: I'll take care of those Black Suns. 
		boolean hasResponse0 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: I'll escort those freighters. 
		boolean hasResponse1 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Let me take a crack at disabling those troop transports.
		boolean hasResponse2 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: I want to help those scientists.
		boolean hasResponse3 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: Can you explain it again?
		boolean hasResponse4 = false;
		if (corellia_rebel_tier4_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_647ad2bc");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_d9116b4d");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_8450d7db");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9943289");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_9775f996");

			utils.setScriptVar (player, "conversation.corellia_rebel_tier4.branchId", 167);

			npcStartConversation (player, npc, "corellia_rebel_tier4", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "corellia_rebel_tier4")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

	if (branchId == 2 && corellia_rebel_tier4_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && corellia_rebel_tier4_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 6 && corellia_rebel_tier4_handleBranch6 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 8 && corellia_rebel_tier4_handleBranch8 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && corellia_rebel_tier4_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 14 && corellia_rebel_tier4_handleBranch14 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && corellia_rebel_tier4_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 16 && corellia_rebel_tier4_handleBranch16 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 18 && corellia_rebel_tier4_handleBranch18 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && corellia_rebel_tier4_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 24 && corellia_rebel_tier4_handleBranch24 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && corellia_rebel_tier4_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 26 && corellia_rebel_tier4_handleBranch26 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 28 && corellia_rebel_tier4_handleBranch28 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 30 && corellia_rebel_tier4_handleBranch30 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && corellia_rebel_tier4_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 36 && corellia_rebel_tier4_handleBranch36 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && corellia_rebel_tier4_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 42 && corellia_rebel_tier4_handleBranch42 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && corellia_rebel_tier4_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 46 && corellia_rebel_tier4_handleBranch46 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && corellia_rebel_tier4_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 48 && corellia_rebel_tier4_handleBranch48 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 49 && corellia_rebel_tier4_handleBranch49 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 50 && corellia_rebel_tier4_handleBranch50 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 51 && corellia_rebel_tier4_handleBranch51 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 52 && corellia_rebel_tier4_handleBranch52 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 54 && corellia_rebel_tier4_handleBranch54 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 55 && corellia_rebel_tier4_handleBranch55 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 56 && corellia_rebel_tier4_handleBranch56 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 57 && corellia_rebel_tier4_handleBranch57 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 60 && corellia_rebel_tier4_handleBranch60 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && corellia_rebel_tier4_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 62 && corellia_rebel_tier4_handleBranch62 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 63 && corellia_rebel_tier4_handleBranch63 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 64 && corellia_rebel_tier4_handleBranch64 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 66 && corellia_rebel_tier4_handleBranch66 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 67 && corellia_rebel_tier4_handleBranch67 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 68 && corellia_rebel_tier4_handleBranch68 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 69 && corellia_rebel_tier4_handleBranch69 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 70 && corellia_rebel_tier4_handleBranch70 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 71 && corellia_rebel_tier4_handleBranch71 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 72 && corellia_rebel_tier4_handleBranch72 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 75 && corellia_rebel_tier4_handleBranch75 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 76 && corellia_rebel_tier4_handleBranch76 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 77 && corellia_rebel_tier4_handleBranch77 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 78 && corellia_rebel_tier4_handleBranch78 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 79 && corellia_rebel_tier4_handleBranch79 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 80 && corellia_rebel_tier4_handleBranch80 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 81 && corellia_rebel_tier4_handleBranch81 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 82 && corellia_rebel_tier4_handleBranch82 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 83 && corellia_rebel_tier4_handleBranch83 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 85 && corellia_rebel_tier4_handleBranch85 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 86 && corellia_rebel_tier4_handleBranch86 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 87 && corellia_rebel_tier4_handleBranch87 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 88 && corellia_rebel_tier4_handleBranch88 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 89 && corellia_rebel_tier4_handleBranch89 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 90 && corellia_rebel_tier4_handleBranch90 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 91 && corellia_rebel_tier4_handleBranch91 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 92 && corellia_rebel_tier4_handleBranch92 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 95 && corellia_rebel_tier4_handleBranch95 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 96 && corellia_rebel_tier4_handleBranch96 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 97 && corellia_rebel_tier4_handleBranch97 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 98 && corellia_rebel_tier4_handleBranch98 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 99 && corellia_rebel_tier4_handleBranch99 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 100 && corellia_rebel_tier4_handleBranch100 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 101 && corellia_rebel_tier4_handleBranch101 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 103 && corellia_rebel_tier4_handleBranch103 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 104 && corellia_rebel_tier4_handleBranch104 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 105 && corellia_rebel_tier4_handleBranch105 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 106 && corellia_rebel_tier4_handleBranch106 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 107 && corellia_rebel_tier4_handleBranch107 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 108 && corellia_rebel_tier4_handleBranch108 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 109 && corellia_rebel_tier4_handleBranch109 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 110 && corellia_rebel_tier4_handleBranch110 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 111 && corellia_rebel_tier4_handleBranch111 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 113 && corellia_rebel_tier4_handleBranch113 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 114 && corellia_rebel_tier4_handleBranch114 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 115 && corellia_rebel_tier4_handleBranch115 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 116 && corellia_rebel_tier4_handleBranch116 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 117 && corellia_rebel_tier4_handleBranch117 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 118 && corellia_rebel_tier4_handleBranch118 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 119 && corellia_rebel_tier4_handleBranch119 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 120 && corellia_rebel_tier4_handleBranch120 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 123 && corellia_rebel_tier4_handleBranch123 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 124 && corellia_rebel_tier4_handleBranch124 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 125 && corellia_rebel_tier4_handleBranch125 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 126 && corellia_rebel_tier4_handleBranch126 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 127 && corellia_rebel_tier4_handleBranch127 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 128 && corellia_rebel_tier4_handleBranch128 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 129 && corellia_rebel_tier4_handleBranch129 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 130 && corellia_rebel_tier4_handleBranch130 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 131 && corellia_rebel_tier4_handleBranch131 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 132 && corellia_rebel_tier4_handleBranch132 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 134 && corellia_rebel_tier4_handleBranch134 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 135 && corellia_rebel_tier4_handleBranch135 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 137 && corellia_rebel_tier4_handleBranch137 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 138 && corellia_rebel_tier4_handleBranch138 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 140 && corellia_rebel_tier4_handleBranch140 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 142 && corellia_rebel_tier4_handleBranch142 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 143 && corellia_rebel_tier4_handleBranch143 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 145 && corellia_rebel_tier4_handleBranch145 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 146 && corellia_rebel_tier4_handleBranch146 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 147 && corellia_rebel_tier4_handleBranch147 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 148 && corellia_rebel_tier4_handleBranch148 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 149 && corellia_rebel_tier4_handleBranch149 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 150 && corellia_rebel_tier4_handleBranch150 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 151 && corellia_rebel_tier4_handleBranch151 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 152 && corellia_rebel_tier4_handleBranch152 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 153 && corellia_rebel_tier4_handleBranch153 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 154 && corellia_rebel_tier4_handleBranch154 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 155 && corellia_rebel_tier4_handleBranch155 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 156 && corellia_rebel_tier4_handleBranch156 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 157 && corellia_rebel_tier4_handleBranch157 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 158 && corellia_rebel_tier4_handleBranch158 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 159 && corellia_rebel_tier4_handleBranch159 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 160 && corellia_rebel_tier4_handleBranch160 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 161 && corellia_rebel_tier4_handleBranch161 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 162 && corellia_rebel_tier4_handleBranch162 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 163 && corellia_rebel_tier4_handleBranch163 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 164 && corellia_rebel_tier4_handleBranch164 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 165 && corellia_rebel_tier4_handleBranch165 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 166 && corellia_rebel_tier4_handleBranch166 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 167 && corellia_rebel_tier4_handleBranch167 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 172 && corellia_rebel_tier4_handleBranch172 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 173 && corellia_rebel_tier4_handleBranch173 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 174 && corellia_rebel_tier4_handleBranch174 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 175 && corellia_rebel_tier4_handleBranch175 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 176 && corellia_rebel_tier4_handleBranch176 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 177 && corellia_rebel_tier4_handleBranch177 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 178 && corellia_rebel_tier4_handleBranch178 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 179 && corellia_rebel_tier4_handleBranch179 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 180 && corellia_rebel_tier4_handleBranch180 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 181 && corellia_rebel_tier4_handleBranch181 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 182 && corellia_rebel_tier4_handleBranch182 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 184 && corellia_rebel_tier4_handleBranch184 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 186 && corellia_rebel_tier4_handleBranch186 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.corellia_rebel_tier4.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

