// ======================================================================
//
// static_gcw_base_naboo_imperial_reward_npc.script
// Copyright 2004, Sony Online Entertainment
// All Rights Reserved.
//
// Created with SwgConversationEditor 1.36 - DO NOT EDIT THIS AUTO-GENERATED FILE!
//
// ======================================================================

// ======================================================================
// Library Includes
// ======================================================================

include library.ai_lib;
include library.chat;
include library.factions;
include library.gcw;
include library.static_item;
include library.utils;

// ======================================================================
// Script Constants
// ======================================================================

string c_stringFile = "conversation/static_gcw_base_naboo_imperial_reward_npc";

// ======================================================================
// Script Conditions
// ======================================================================

boolean static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (obj_id player, obj_id npc)
{
	return true;
}

// ----------------------------------------------------------------------

boolean static_gcw_base_naboo_imperial_reward_npc_condition_playerImperial (obj_id player, obj_id npc)
{
	return factions.isImperial(player);
}

// ----------------------------------------------------------------------

boolean static_gcw_base_naboo_imperial_reward_npc_condition_inControlOfBase (obj_id player, obj_id npc)
{
	return gcw.getPub30StaticBaseControllingFaction(npc) == gcw.IMPERIAL_CONTROL;
}

// ----------------------------------------------------------------------

boolean static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (obj_id player, obj_id npc)
{
	return gcw.getPub30StaticBaseCapturePhase(npc) >= 3;
}

// ----------------------------------------------------------------------

boolean static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (obj_id player, obj_id npc)
{
	return gcw.getPub30StaticBaseCapturePhase(npc) >= 4;
}

// ----------------------------------------------------------------------

boolean static_gcw_base_naboo_imperial_reward_npc_condition_isGodPlayer (obj_id player, obj_id npc)
{
	return isGod(player);
}

// ----------------------------------------------------------------------

boolean static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (obj_id player, obj_id npc)
{
	return gcw.getPub30StaticBaseCapturePhase(npc) == 5;
}

// ----------------------------------------------------------------------

boolean static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_2 (obj_id player, obj_id npc)
{
	return gcw.getPub30StaticBaseCapturePhase(npc) >= 2;
}

// ======================================================================
// Script Actions
// ======================================================================

void static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack1 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_health_a_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack2 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_health_b_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack3 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_health_c_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack4 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_health_d_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack5 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_health_e_04_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack1 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_action_a_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack2 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_action_b_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack3 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_action_c_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack4 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_action_d_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack5 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_action_e_04_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageAbsorb1 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_absorb_a_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageAbsorb2 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_absorb_b_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageAbsorb3 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_absorb_c_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageAbsorb4 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_absorb_d_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageAbsorb5 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_absorb_e_04_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageBoost1 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_booster_a_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageBoost2 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_booster_b_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageBoost3 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_booster_c_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageShield1 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_shield_a_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageShield2 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_shield_b_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantDamageShield3 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_damage_shield_c_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal1 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_heal_a_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal2 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_heal_b_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal3 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_heal_c_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction1 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_action_a_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction2 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_action_b_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction3 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_action_c_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantLastChance1 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_critical_heal_a_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantLastChance2 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_critical_heal_b_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantLastChance3 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_critical_heal_c_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantRallyBanner (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_rally_banner_04_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_advanceRewardPhase (obj_id player, obj_id npc)
{
	int newPhase = gcw.advancePub30StaticBaseCapturePhase(npc);
	sendSystemMessageTestingOnly(player, "Reward phase advanced to "+newPhase);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_regressRewardPhase (obj_id player, obj_id npc)
{
	int newPhase = gcw.regressPub30StaticBaseCapturePhase(npc);
	sendSystemMessageTestingOnly(player, "Reward phase regressed to "+newPhase);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_reportRewardPhase (obj_id player, obj_id npc)
{
	int phase = gcw.getPub30StaticBaseCapturePhase(npc);
	string next = gcw.getPub30TimeToNextPhaseString(npc);
	if (phase < 5)
		sendSystemMessageTestingOnly(player, "Current reward phase is: "+phase+". Next reward phase in "+next);
	else
		sendSystemMessageTestingOnly(player, "Current reward phase is: "+phase+". This is the final phase");
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal4 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_heal_d_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal5 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_heal_e_04_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction4 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_action_d_03_01", player);
}

// ----------------------------------------------------------------------

void static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction5 (obj_id player, obj_id npc)
{
	static_item.createNewItemFunction("item_gcw_base_reactive_action_e_04_01", player);
}

// ======================================================================
// Script %TO Tokens
// ======================================================================

string static_gcw_base_naboo_imperial_reward_npc_tokenTO_timeToNext (obj_id player, obj_id npc)
{
	return gcw.getPub30TimeToNextPhaseString(npc);
}

// ======================================================================
// Script %DI Tokens
// ======================================================================

// ======================================================================
// Script %DF Tokens
// ======================================================================

// ======================================================================
// handleBranch<n> Functions 
// ======================================================================

int static_gcw_base_naboo_imperial_reward_npc_handleBranch2 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: Greetings soldier! I'm in charge of supplies for our battle weary troopers. I have several orders en route and the longer you can keep the Rebel scum out of here, the more supplies will become available.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me a health pack.
	if (response == "s_16")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Stimpack E.
			string_id message = new string_id (c_stringFile, "s_31");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_50");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 3);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Stimpack D.
			string_id message = new string_id (c_stringFile, "s_46");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_52");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 5);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Stimpack C.
			string_id message = new string_id (c_stringFile, "s_47");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_54");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 7);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_2 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Stimpack B.
			string_id message = new string_id (c_stringFile, "s_48");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_56");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 9);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Stimpack A.
			string_id message = new string_id (c_stringFile, "s_49");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_58");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 11);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me an action heal injector.
	if (response == "s_25")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Adrenaline Injection E.
			string_id message = new string_id (c_stringFile, "s_32");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_64");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 13);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Adrenaline Injection D.
			string_id message = new string_id (c_stringFile, "s_60");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_66");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 15);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Adrenaline Injection C.
			string_id message = new string_id (c_stringFile, "s_61");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_68");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 17);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_2 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Adrenaline Injection B.
			string_id message = new string_id (c_stringFile, "s_62");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_70");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 19);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Adrenaline Injection A.
			string_id message = new string_id (c_stringFile, "s_63");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_277");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 21);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me a damage mitigation powder.
	if (response == "s_44")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Pain Numbing Powder C.
			string_id message = new string_id (c_stringFile, "s_84");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_86");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 23);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Pain Numbing Powder B.
			string_id message = new string_id (c_stringFile, "s_90");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_92");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 25);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Pain Numbing Powder A.
			string_id message = new string_id (c_stringFile, "s_96");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_98");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 27);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me a damage increase pack.
	if (response == "s_102")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Damage Booster Pack C.
			string_id message = new string_id (c_stringFile, "s_104");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_106");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 29);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Damage Booster Pack B.
			string_id message = new string_id (c_stringFile, "s_110");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_112");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 31);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Damage Booster Pack A.
			string_id message = new string_id (c_stringFile, "s_116");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_118");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 33);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me a damage shield.
	if (response == "s_124")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Personal Reactive Shield C.
			string_id message = new string_id (c_stringFile, "s_126");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_128");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 35);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Personal Reactive Shield B.
			string_id message = new string_id (c_stringFile, "s_132");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_134");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 37);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Personal Reactive Shield A.
			string_id message = new string_id (c_stringFile, "s_138");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_140");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 39);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me a reactive heal injector.
	if (response == "s_146")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Stim Injector E.
			string_id message = new string_id (c_stringFile, "s_259");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_263");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 41);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Stim Injector D.
			string_id message = new string_id (c_stringFile, "s_260");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_261");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 43);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Stim Injector C.
			string_id message = new string_id (c_stringFile, "s_148");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_150");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 45);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_2 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Stim Injector B.
			string_id message = new string_id (c_stringFile, "s_154");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_156");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 47);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Stim Injector A.
			string_id message = new string_id (c_stringFile, "s_160");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_162");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 49);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me a reactive action heal injector.
	if (response == "s_168")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector E.
			string_id message = new string_id (c_stringFile, "s_188");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_268");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 51);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector D.
			string_id message = new string_id (c_stringFile, "s_265");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_266");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 53);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector C.
			string_id message = new string_id (c_stringFile, "s_170");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_172");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 55);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_2 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector B.
			string_id message = new string_id (c_stringFile, "s_176");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_178");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 57);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector A.
			string_id message = new string_id (c_stringFile, "s_182");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_184");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 59);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me a last chance heal injector.
	if (response == "s_190")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Critical Reactive Stim Injector C.
			string_id message = new string_id (c_stringFile, "s_192");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_194");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 61);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_4 (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Critical Reactive Stim Injector B.
			string_id message = new string_id (c_stringFile, "s_198");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_200");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 63);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We are currently in possession of the Military Critical Reactive Stim Injector A.
			string_id message = new string_id (c_stringFile, "s_204");
			int numberOfResponses = 0;

			boolean hasResponse = false;

			//-- PLAYER: That is what I want.
			boolean hasResponse0 = false;
			if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
			{
				++numberOfResponses;
				hasResponse = true;
				hasResponse0 = true;
			}

			if (hasResponse)
			{
				int responseIndex = 0;
				string_id responses [] = new string_id [numberOfResponses];

				if (hasResponse0)
					responses [responseIndex++] = new string_id (c_stringFile, "s_206");

				utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 65);

				npcSpeak (player, message);
				npcSetConversationResponses (player, responses);
			}
			else
			{
				utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

				npcEndConversationWithMessage (player, message);
			}

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Give me a Rallying Banner.
	if (response == "s_212")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantRallyBanner (player, npc);

			//-- NPC: Excellent choice. The Rallying Banner can really turn the tide of a fight.
			string_id message = new string_id (c_stringFile, "s_214");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: - Military Pain Numbing Powders are still on order -
	if (response == "s_270")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We will need to hold the base for longer in order to receive a shipment of these.
			string_id message = new string_id (c_stringFile, "s_271");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: - Military Damage Booster Packs are still on order -
	if (response == "s_222")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We will need to hold the base for longer in order to receive a shipment of these.
			string_id message = new string_id (c_stringFile, "s_224");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: - Military Personal Reactive Shields are still on order -
	if (response == "s_226")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We will need to hold the base for longer in order to receive a shipment of these.
			string_id message = new string_id (c_stringFile, "s_228");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: - Military Critical Reactive Stim Injectors are still on order -
	if (response == "s_238")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We will need to hold the base for longer in order to receive a shipment of these.
			string_id message = new string_id (c_stringFile, "s_240");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: - Rallying Banners are still on order -
	if (response == "s_242")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: We will need to hold the base for longer in order to receive a shipment of these.
			string_id message = new string_id (c_stringFile, "s_244");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Advance the reward phase a step
	if (response == "s_247")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_advanceRewardPhase (player, npc);

			//-- NPC: Step it up.
			string_id message = new string_id (c_stringFile, "s_250");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: Regress the reward phase a step
	if (response == "s_249")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_regressRewardPhase (player, npc);

			//-- NPC: Take it back.
			string_id message = new string_id (c_stringFile, "s_252");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: What reward phase are we in, and what is the time until the next?
	if (response == "s_254")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_reportRewardPhase (player, npc);

			//-- NPC: The answer lies within.
			string_id message = new string_id (c_stringFile, "s_256");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	//-- [RESPONSE NOTE] 
	//-- PLAYER: When are you expecting more supplies?
	if (response == "s_225")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			//-- NPC: There are no more pending shipments. We are not expecting any more supplies for the time being.
			string_id message = new string_id (c_stringFile, "s_229");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			//-- NPC: The next shipment of supplies should arrive in %TO.
			string_id message = new string_id (c_stringFile, "s_231");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			prose_package pp = new prose_package ();
			pp.stringId = message;
			pp.actor.set (player);
			pp.target.set (npc);
			pp.other.set (static_gcw_base_naboo_imperial_reward_npc_tokenTO_timeToNext (player, npc));

			npcEndConversationWithMessage (player, pp);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch3 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Stimpack E.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_50")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack5 (player, npc);

			//-- NPC: Very well. Carry on, soldier.
			string_id message = new string_id (c_stringFile, "s_51");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch5 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Stimpack D.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_52")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack4 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_53");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch7 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Stimpack C.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_54")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack3 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_55");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch9 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Stimpack B.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_56")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack2 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_57");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch11 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Stimpack A.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_58")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantHealthPack1 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_59");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch13 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Adrenaline Injection E.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_64")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack5 (player, npc);

			//-- NPC: Very well. Carry on, soldier.
			string_id message = new string_id (c_stringFile, "s_65");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch15 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Adrenaline Injection D.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_66")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack4 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_67");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch17 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Adrenaline Injection C.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_68")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack3 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_69");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch19 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Adrenaline Injection B.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_70")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack2 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_71");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch21 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Adrenaline Injection A.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_277")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantActionPack1 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_278");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch23 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Pain Numbing Powder C.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_86")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageAbsorb3 (player, npc);

			//-- NPC: Very well. Carry on, soldier. This is the best quality that we can get at this base.
			string_id message = new string_id (c_stringFile, "s_88");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch25 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Pain Numbing Powder B.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_92")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageAbsorb2 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_94");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch27 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Pain Numbing Powder A.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_98")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageAbsorb1 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_100");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch29 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Damage Booster Pack C.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_106")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageBoost3 (player, npc);

			//-- NPC: Very well. Carry on, soldier. This is the best quality that we can get at this base.
			string_id message = new string_id (c_stringFile, "s_108");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch31 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Damage Booster Pack B.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_112")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageBoost2 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_114");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch33 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Damage Booster Pack A.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_118")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageBoost1 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_120");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch35 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Personal Reactive Shield C.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_128")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageShield3 (player, npc);

			//-- NPC: Very well. Carry on, soldier. This is the best quality that we can get at this base.
			string_id message = new string_id (c_stringFile, "s_130");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch37 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Personal Reactive Shield B.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_134")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageShield2 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_136");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch39 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Personal Reactive Shield A.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_140")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantDamageShield1 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_142");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch41 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Stim Injector E.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_263")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal5 (player, npc);

			//-- NPC: Very well. Carry on, soldier.
			string_id message = new string_id (c_stringFile, "s_264");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch43 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Stim Injector D.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_261")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal4 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_262");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch45 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Stim Injector C.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_150")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal3 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_152");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch47 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Stim Injector B.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_156")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal2 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_158");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch49 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Stim Injector A.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_162")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveHeal1 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_164");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch51 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector E.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_268")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction5 (player, npc);

			//-- NPC: Very well. Carry on, soldier.
			string_id message = new string_id (c_stringFile, "s_269");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch53 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector D.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_266")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction4 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_267");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch55 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector C.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_172")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction3 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_174");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch57 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector B.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_178")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction2 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_180");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch59 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Reactive Adrenaline Injector A.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_184")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantReactiveAction1 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_186");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch61 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Critical Reactive Stim Injector C.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_194")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantLastChance3 (player, npc);

			//-- NPC: Very well. Carry on, soldier. This is the best quality of these I can get at this location.
			string_id message = new string_id (c_stringFile, "s_196");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch63 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Critical Reactive Stim Injector B.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_200")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantLastChance2 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_202");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

int static_gcw_base_naboo_imperial_reward_npc_handleBranch65 (obj_id player, obj_id npc, string_id response)
{
	//-- [BRANCH NOTE] 
	//-- NPC: We are currently in possession of the Military Critical Reactive Stim Injector A.

	//-- [RESPONSE NOTE] 
	//-- PLAYER: That is what I want.
	if (response == "s_206")
	{
		//-- [NOTE] 
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			static_gcw_base_naboo_imperial_reward_npc_action_grantLastChance1 (player, npc);

			//-- NPC: Very well. Carry on, soldier. If you want better quality we will need to hold the base longer.
			string_id message = new string_id (c_stringFile, "s_208");
			utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

			npcEndConversationWithMessage (player, message);

			return SCRIPT_CONTINUE;
		}

	}

	return SCRIPT_DEFAULT;
}

// ----------------------------------------------------------------------

// ======================================================================
// User Script Triggers
// ======================================================================

trigger OnInitialize ()
{
	if ((!isTangible (self)) || (isPlayer (self)))
		detachScript(self, "conversation.static_gcw_base_naboo_imperial_reward_npc");

	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnAttach ()
{
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnObjectMenuRequest (obj_id player, menu_info menuInfo)
{
	int menu = menuInfo.addRootMenu (menu_info_types.CONVERSE_START, null);
	menu_info_data menuInfoData = menuInfo.getMenuItemById (menu);
	menuInfoData.setServerNotify (false);
	setCondition (self, CONDITION_CONVERSABLE);

	return SCRIPT_CONTINUE;
}

trigger OnIncapacitated (obj_id killer)
{
	clearCondition (self, CONDITION_CONVERSABLE);
	detachScript (self, "conversation.static_gcw_base_naboo_imperial_reward_npc");

	return SCRIPT_CONTINUE;
}

// ======================================================================
// Script Triggers
// ======================================================================

//-- This function should move to base_class.java
boolean npcStartConversation(obj_id player, obj_id npc, string convoName, string_id greetingId, prose_package greetingProse, string_id[] responses)
{
	Object[] objects = new Object[responses.length];
	System.arraycopy(responses, 0, objects, 0, responses.length);
	return npcStartConversation(player, npc, convoName, greetingId, greetingProse, objects);
}

// ----------------------------------------------------------------------

trigger OnStartNpcConversation (obj_id player)
{
	obj_id npc = self;

	if (ai_lib.isInCombat (npc) || ai_lib.isInCombat (player))
		return SCRIPT_OVERRIDE;

	//-- [NOTE] 
	if (!static_gcw_base_naboo_imperial_reward_npc_condition_inControlOfBase (player, npc))
	{
		//-- NPC: We have to hold the base before we can start bringing in supplies soldier, so get out there and fight.
		string_id message = new string_id (c_stringFile, "s_15");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (static_gcw_base_naboo_imperial_reward_npc_condition_playerImperial (player, npc))
	{
		doAnimationAction (npc, "salute2");

		doAnimationAction (player, "salute2");

		//-- NPC: Greetings soldier! I'm in charge of supplies for our battle weary troopers. I have several orders en route and the longer you can keep the Rebel scum out of here, the more supplies will become available.
		string_id message = new string_id (c_stringFile, "s_5");
		int numberOfResponses = 0;

		boolean hasResponse = false;

		//-- PLAYER: Give me a health pack.
		boolean hasResponse0 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse0 = true;
		}

		//-- PLAYER: Give me an action heal injector.
		boolean hasResponse1 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse1 = true;
		}

		//-- PLAYER: Give me a damage mitigation powder.
		boolean hasResponse2 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse2 = true;
		}

		//-- PLAYER: Give me a damage increase pack.
		boolean hasResponse3 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse3 = true;
		}

		//-- PLAYER: Give me a damage shield.
		boolean hasResponse4 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse4 = true;
		}

		//-- PLAYER: Give me a reactive heal injector.
		boolean hasResponse5 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse5 = true;
		}

		//-- PLAYER: Give me a reactive action heal injector.
		boolean hasResponse6 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse6 = true;
		}

		//-- PLAYER: Give me a last chance heal injector.
		boolean hasResponse7 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse7 = true;
		}

		//-- PLAYER: Give me a Rallying Banner.
		boolean hasResponse8 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse8 = true;
		}

		//-- PLAYER: - Military Pain Numbing Powders are still on order -
		boolean hasResponse9 = false;
		if (!static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse9 = true;
		}

		//-- PLAYER: - Military Damage Booster Packs are still on order -
		boolean hasResponse10 = false;
		if (!static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse10 = true;
		}

		//-- PLAYER: - Military Personal Reactive Shields are still on order -
		boolean hasResponse11 = false;
		if (!static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse11 = true;
		}

		//-- PLAYER: - Military Critical Reactive Stim Injectors are still on order -
		boolean hasResponse12 = false;
		if (!static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_3 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse12 = true;
		}

		//-- PLAYER: - Rallying Banners are still on order -
		boolean hasResponse13 = false;
		if (!static_gcw_base_naboo_imperial_reward_npc_condition_reward_phase_5 (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse13 = true;
		}

		//-- PLAYER: Advance the reward phase a step
		boolean hasResponse14 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition_isGodPlayer (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse14 = true;
		}

		//-- PLAYER: Regress the reward phase a step
		boolean hasResponse15 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition_isGodPlayer (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse15 = true;
		}

		//-- PLAYER: What reward phase are we in, and what is the time until the next?
		boolean hasResponse16 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition_isGodPlayer (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse16 = true;
		}

		//-- PLAYER: When are you expecting more supplies?
		boolean hasResponse17 = false;
		if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
		{
			++numberOfResponses;
			hasResponse = true;
			hasResponse17 = true;
		}

		if (hasResponse)
		{
			int responseIndex = 0;
			string_id responses [] = new string_id [numberOfResponses];

			if (hasResponse0)
				responses [responseIndex++] = new string_id (c_stringFile, "s_16");

			if (hasResponse1)
				responses [responseIndex++] = new string_id (c_stringFile, "s_25");

			if (hasResponse2)
				responses [responseIndex++] = new string_id (c_stringFile, "s_44");

			if (hasResponse3)
				responses [responseIndex++] = new string_id (c_stringFile, "s_102");

			if (hasResponse4)
				responses [responseIndex++] = new string_id (c_stringFile, "s_124");

			if (hasResponse5)
				responses [responseIndex++] = new string_id (c_stringFile, "s_146");

			if (hasResponse6)
				responses [responseIndex++] = new string_id (c_stringFile, "s_168");

			if (hasResponse7)
				responses [responseIndex++] = new string_id (c_stringFile, "s_190");

			if (hasResponse8)
				responses [responseIndex++] = new string_id (c_stringFile, "s_212");

			if (hasResponse9)
				responses [responseIndex++] = new string_id (c_stringFile, "s_270");

			if (hasResponse10)
				responses [responseIndex++] = new string_id (c_stringFile, "s_222");

			if (hasResponse11)
				responses [responseIndex++] = new string_id (c_stringFile, "s_226");

			if (hasResponse12)
				responses [responseIndex++] = new string_id (c_stringFile, "s_238");

			if (hasResponse13)
				responses [responseIndex++] = new string_id (c_stringFile, "s_242");

			if (hasResponse14)
				responses [responseIndex++] = new string_id (c_stringFile, "s_247");

			if (hasResponse15)
				responses [responseIndex++] = new string_id (c_stringFile, "s_249");

			if (hasResponse16)
				responses [responseIndex++] = new string_id (c_stringFile, "s_254");

			if (hasResponse17)
				responses [responseIndex++] = new string_id (c_stringFile, "s_225");

			utils.setScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId", 2);

			npcStartConversation (player, npc, "static_gcw_base_naboo_imperial_reward_npc", message, responses);
		}
		else
		{
			chat.chat (npc, player, message);
		}

		return SCRIPT_CONTINUE;
	}

	//-- [NOTE] 
	if (static_gcw_base_naboo_imperial_reward_npc_condition__defaultCondition (player, npc))
	{
		//-- NPC: Move along, citizen. This is not the place for a civilian!
		string_id message = new string_id (c_stringFile, "s_258");
		chat.chat (npc, player, message);

		return SCRIPT_CONTINUE;
	}

	chat.chat (npc, "Error:  All conditions for OnStartNpcConversation were false.");

	return SCRIPT_CONTINUE;
}

// ----------------------------------------------------------------------

trigger OnNpcConversationResponse (string conversationId, obj_id player, string_id response)
{
	if (conversationId != "static_gcw_base_naboo_imperial_reward_npc")
		return SCRIPT_CONTINUE;

	obj_id npc = self;

	int branchId = utils.getIntScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

	if (branchId == 2 && static_gcw_base_naboo_imperial_reward_npc_handleBranch2 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 3 && static_gcw_base_naboo_imperial_reward_npc_handleBranch3 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 5 && static_gcw_base_naboo_imperial_reward_npc_handleBranch5 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 7 && static_gcw_base_naboo_imperial_reward_npc_handleBranch7 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 9 && static_gcw_base_naboo_imperial_reward_npc_handleBranch9 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 11 && static_gcw_base_naboo_imperial_reward_npc_handleBranch11 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 13 && static_gcw_base_naboo_imperial_reward_npc_handleBranch13 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 15 && static_gcw_base_naboo_imperial_reward_npc_handleBranch15 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 17 && static_gcw_base_naboo_imperial_reward_npc_handleBranch17 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 19 && static_gcw_base_naboo_imperial_reward_npc_handleBranch19 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 21 && static_gcw_base_naboo_imperial_reward_npc_handleBranch21 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 23 && static_gcw_base_naboo_imperial_reward_npc_handleBranch23 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 25 && static_gcw_base_naboo_imperial_reward_npc_handleBranch25 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 27 && static_gcw_base_naboo_imperial_reward_npc_handleBranch27 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 29 && static_gcw_base_naboo_imperial_reward_npc_handleBranch29 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 31 && static_gcw_base_naboo_imperial_reward_npc_handleBranch31 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 33 && static_gcw_base_naboo_imperial_reward_npc_handleBranch33 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 35 && static_gcw_base_naboo_imperial_reward_npc_handleBranch35 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 37 && static_gcw_base_naboo_imperial_reward_npc_handleBranch37 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 39 && static_gcw_base_naboo_imperial_reward_npc_handleBranch39 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 41 && static_gcw_base_naboo_imperial_reward_npc_handleBranch41 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 43 && static_gcw_base_naboo_imperial_reward_npc_handleBranch43 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 45 && static_gcw_base_naboo_imperial_reward_npc_handleBranch45 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 47 && static_gcw_base_naboo_imperial_reward_npc_handleBranch47 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 49 && static_gcw_base_naboo_imperial_reward_npc_handleBranch49 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 51 && static_gcw_base_naboo_imperial_reward_npc_handleBranch51 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 53 && static_gcw_base_naboo_imperial_reward_npc_handleBranch53 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 55 && static_gcw_base_naboo_imperial_reward_npc_handleBranch55 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 57 && static_gcw_base_naboo_imperial_reward_npc_handleBranch57 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 59 && static_gcw_base_naboo_imperial_reward_npc_handleBranch59 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 61 && static_gcw_base_naboo_imperial_reward_npc_handleBranch61 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 63 && static_gcw_base_naboo_imperial_reward_npc_handleBranch63 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	if (branchId == 65 && static_gcw_base_naboo_imperial_reward_npc_handleBranch65 (player, npc, response) == SCRIPT_CONTINUE)
		return SCRIPT_CONTINUE;

	chat.chat (npc, "Error:  Fell through all branches and responses for OnNpcConversationResponse.");

	utils.removeScriptVar (player, "conversation.static_gcw_base_naboo_imperial_reward_npc.branchId");

	return SCRIPT_CONTINUE;
}

// ======================================================================

