trigger OnHearSpeech(obj_id objSpeaker, string strText)
{
	if (strText == "broadcast")
		sendSystemMessageGalaxyTestingOnly("Broadcasting now, speaker is " + objSpeaker + ", self is " + self);
	
	return SCRIPT_CONTINUE;
}
